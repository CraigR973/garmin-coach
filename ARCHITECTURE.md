# Garmin Coach — Architecture & Spec

Private AI fitness & sleep coach for one user ("Mark", Craig's dad), optionally a
second. Automates the twice-daily check-in he does by hand today: pulls his data,
holds his context permanently, and generates a morning verdict + post-workout
analysis with Claude.

**Product thesis:** he currently writes "handover" docs by hand to give an AI the
context it keeps forgetting. This app's core job is to **hold that knowledge as
living, editable state** so he never writes one again.

---

## 1. Stack (inherited from WC2026, gutted of the football domain)

- **Backend:** FastAPI + async SQLAlchemy + asyncpg, Postgres (Supabase), Alembic, APScheduler, pywebpush. Python **3.12** (`~/.local/bin/python3.12` — system `python3` is 3.7).
- **Frontend:** React 18 + Vite + Tailwind + shadcn/ui + recharts + react-hook-form + react-query. PWA (workbox) so it installs on his phone.
- **Shared:** `packages/shared` (Zod schemas + TS types) — swap WC scoring for fitness types.
- **Auth:** **passwordless device tokens only** — a one-time activation link mints
  a revocable 256-bit opaque device credential stored hashed in Postgres.
  The activation code can be opened as a link or pasted directly into the
  installed app's setup screen, so an iOS home-screen install — whose storage
  is isolated from Safari — can authenticate itself (Batch 172, #242).
  Batch 160 removes the inherited name/PIN + JWT fallback, refresh rotation,
  reset/change-PIN surfaces, lockout columns, and JWT secrets; activation,
  identity, and current-device revoke are the complete HTTP auth surface
  (DECISIONS #73–74/#77/#79/#240). Stripped of WC's leagues/groups/invites/email
  — **1–2 private users, no sign-up**.
- **Hosting:** Supabase (DB) + Railway (API) + Vercel (web). Deploy live early.
- **Envelope/conventions:** `/api/v1/`, `{data, meta, errors}`, snake_case DB / camelCase JSON, UTC `*_utc` columns, IANA timezone per user (`Europe/London`).

## 2. Data sources — ALL validated against real data (18–19 Jun 26)

| Source | Library/API | Auth | Notes |
|---|---|---|---|
| **Garmin** | `garminconnect` (unofficial) | email+pw, garth token cache (~1yr, no re-MFA) | Full coverage incl. Performance Condition (`directPerformanceCondition`) + Stamina (`directAvailable/PotentialStamina`) in activity time-series |
| **Hive** | `pyhiveapi` (sync) | email+pw, account uses AWS Cognito **SMS_MFA** | Live indoor temp via `API(token).getAll()` → `parsed[i].props.temperature`. Headless operation resumes from a cached Cognito refresh token (`HIVE_TOKENSTORE_B64`) via `REFRESH_TOKEN_AUTH`; seed once with `scripts/bootstrap_hive_tokenstore.py` (DECISIONS #59). |
| **Weather** | Open-Meteo | none (keyless) | KA1 2SD = Kilmarnock, **lat 55.6045, long -4.5249**. `past_days` + `wind_speed_unit=mph` gives daily high/low + overnight low/wind |

Spikes live in `~/garmin-spike/` (outside this repo). Raw sample JSON in `~/garmin-spike/out/` (Garmin) and `out_hive/` (Hive) — **canonical reference for real field shapes**.

**Sync jobs (APScheduler):** Hive temp poll every ~15 min; **wake-triggered morning run** — a `wake_check` poll every ~15 min within Mark's morning window (03:30–11:30 Europe/London) does a light sleep-only Garmin poll and, once his Garmin `sleepEnd` is *stable* (back-to-sleep guard), fires the Garmin+weather **sync + a "good morning" nudge** (`run_morning_sync`); **generating the brief itself is triggered by his check-in** (the primary trigger — Batch 85), with the 11:00 backstop generating for a morning he never engages (DECISIONS #87/#158/#217 — moved later from 09:30 in Batch 138 so a genuine lie-in isn't force-read on stale data; replaces the old fixed 06:30 cron); the hourly activity poll syncs rides/strength/flexibility/walks and nudges **"How did it feel?" without running an LLM**, the activity check-in generates the correct read inline, and a 20:30 local backstop generates any same-day unread sessions before tomorrow's morning packet (Batch 87, DECISIONS #160); **overnight bedroom fan-control loop** every ~15 min reconciles the Dreo air-circulator to the live indoor temp within 21:30–08:30 when the autopilot is on (DECISIONS #96-97). **Nightly `metric_baselines` refresh** at 02:30 Europe/London (Batch 228, DECISIONS #306) recomputes every active profile's trailing-84-night personal baselines. The slot is load-bearing: it is an hour before the wake window opens (`wake_detection.WINDOW_START` 03:30) so it never races the morning read, and it runs before the wake sync writes tonight's `sleep` row, so the night being judged is never inside the distribution it is judged against. NB Training Readiness is time-of-day live → morning analysis must read **after he wakes** (the whole point of the wake trigger). `recoveryTime` is in MINUTES.

Batch 195 makes those invocations observable without changing their hosting
owner. Each APScheduler fire and external `run_scheduled` fire crosses one typed
`succeeded` / `skipped` / `degraded` / `failed` result boundary and persists an
operator-only `job_runs` row in a separate transaction (cadence window,
timestamps, stable reason, integer counters); degraded/failed external runs exit
non-zero. Morning weather/Garmin dates commit at isolated recovery boundaries,
and every caught write-capable downstream step rolls its `AsyncSession` back
before continuing, so a poisoned historical row cannot suppress verdict
generation. A separate post-`main` GitHub workflow polls Railway-direct and
Vercel-proxied health for the expected merge SHA for at most 15 minutes and
fails loudly on staleness. The in-process scheduler remains enabled: choosing an
always-on API versus externalising every workload is the explicit hosting-cost
gate in Batch 195.4, and no cutover occurs until every external job has proved a
successful run (DECISIONS #277).

### Workout delivery — OUTPUT (validated 19 Jun 26)

Push direction only — *not* a data source (ingestion stays direct-from-Garmin). The app emits a structured workout → POST to his **intervals.icu** calendar (free API; athlete `i618709`) → intervals.icu's approved Zwift Training-Connections integration delivers it into Zwift's Custom Workouts automatically. **Power + timing proven exact** end-to-end. intervals.icu is a **delivery rail, NOT the system-of-record** (our DB owns the plan). Deterministic **`.ZWO` export** is the no-dependency fallback. Since Batch 29, the as-planned baseline is pushed when the plan is set; human approval gates only the morning sleep/recovery adjustment. **Outdoor** rides instead deliver directly to **Garmin Connect** (Batch 78, #151): the app builds Garmin's own structured-workout JSON from the same IR and uploads + schedules it on the existing garth session, so Garmin is a workout-*write* output for outdoor rides while ingestion stays read-only. Cadence nuance: Zwift overrides cadence on repeated-interval blocks with defaults (100/90) — emit cadence-critical reps as individual steps (confirm on PC). Spike: `~/garmin-spike/intervals_spike.py`.

Batch 12 stores delivery state separately from the plan in
`workout_delivery_proposals`: each proposal snapshots the `planned_workouts`
version, generated structured-workout IR, intervals.icu calendar payload, `.ZWO`
XML, approval state, and pushed event id. intervals.icu credentials live only in
environment variables (`INTERVALS_API_KEY`, `INTERVALS_ATHLETE_ID`) and the app
never ingests activity data from intervals.icu.

Batch 13 makes the rail *executable* (`services/executable_coaching.py`): on an
Amber morning verdict the job regenerates today's bike workout into an adjusted
proposal (deterministic IR transform — cut duration 20-30%, drop a zone, remove
HIT; Red can never emit VO2), with proposal/push provenance in the IR snapshot
and `analyses` audit rows (`workout_proposed` / `workout_pushed`).

Batch 29 (PR #44, DECISIONS #99) changes the delivery timing: block generation
and weekly restructure now reconcile the as-planned baseline to Zwift when the
plan is set, without per-workout approval. Mornings are review-only unless the
coach changed the workout from sleep/recovery; the Home Today card then lets Mark
Approve & upload the adjusted IR, Ignore it, Manual edit, Swap day, or Skip.
The delivery rail gained idempotent `replace_event`, `move_event`, and
`delete_event` operations keyed to the planned workout/version, with cloud-first
failure handling so local state does not claim a Zwift mutation that failed.

Batch 158 (PR #187, DECISIONS #238) closes the remaining replacement-atomicity
gap: rail calls made with `commit=False` never commit their caller's unit of
work, and the local workout pointer/version advances only after intervals.icu
accepts the replacement. Reconciliation treats the persisted delivered IR's
planned-workout id/version as the cloud-content fingerprint, so a stale pointer
self-heals on retry while legitimate move/swap re-slotting updates the same
fingerprint after cloud success. Malformed bike structure and missing FTP now
persist deduplicated structured failures in the existing proposal table and emit
context-rich warnings; intentional non-bike and exact-version no-ops remain
row-free. No migration.

Batch 30 (PR #45, DECISIONS #100) added a thin plan-action boundary over the real
`planned_workouts` rows:
`GET /api/v1/plan-actions/schedule` groups the live plan into rest/cycle/weights/
flexibility/mixed days, while mutations append workouts, swap an existing workout
into a day, skip a whole day, or record an unplanned actual. Rest stays the
absence of active workouts, not a stored rest row. Mixed-day delivery state now
prefers `planned_workout_id` before falling back to date-only legacy lookups, so
multiple same-day workouts can each keep their own delivery/action state.
Batch 76 tightens the skip semantics around that boundary: a whole-day skip now
means "skip what remains", not "rewrite the day" — sessions already marked
`status='completed'` or already logged via their latest adherence row stay put,
per-item skip 409s an already-done session before any delivery mutation, and the
Week tab labels a partially completed day as **Skip remaining** to match the
backend truth.

Batch 82 adds a manual light-reset control to the same plan-action boundary:
`POST /api/v1/plan-actions/weeks/{date}/reset` records the containing block as an
intended deload in `plan_blocks.goals_json.manualResetWeek`, versions that week's
active bike workouts into steady Z2 reset rows, keeps strength/flexibility on the
day, and reconciles delivery; `DELETE /api/v1/plan-actions/weeks/{date}/reset`
restores the prior bike versions. No migration: the block flag records intent,
while each reset workout carries `structured_workout.resetWeek` provenance so
morning analysis reads the reduced load as planned, not missed.

Batch 83 adds the missing organiser surface over the existing restructure rail:
the Week
tab can fetch `GET /api/v1/restructure/week-ahead` for a week banner, render the
proposed reshuffle by resolving the returned workout ids back through the loaded
schedule, and apply it in one tap through `POST /api/v1/restructure/apply`.
Engine rules and payload ownership stay in `weekly_restructure.py`; this is UI
wiring only and adds no migration. Shipped in PR #108 (squash `1616003`),
prod-verified on Railway + Vercel same-origin health and unauthenticated
`GET /api/v1/restructure/week-ahead` 401 smoke.

Batch 60 (DECISIONS #134) closes the loop after a ride: when the post-workout read
is generated it links the `analyses` row to the day's bike `planned_workouts` row
(new `analyses.planned_workout_id`) and flips that workout to `status='completed'`
(previously a dead enum value). Home then shows the read on the session's own
Today-card row — a compact done state with the tomorrow-impact, the ride check-in,
and the full read behind a disclosure — instead of the approve/upload controls or a
separate "After your ride" section (which is kept only for *unplanned* rides), and
`swap_day` refuses to re-slot a completed session in either direction (the Plan view
hides its Move, the endpoint 409s).

Batch 159 (DECISIONS #239) generalises that seam to cycle, strength, mobility,
and deliberate-walk reads. Every generator completes and links the matching
planned session before the model call, records activity-scoped
`generating`/`ready`/`failed` state, and preserves distinct claims for same-kind
two-a-days. Migration `022` backfills existing supported analyses onto active
planned sessions. The Week lookup first proves workout ownership, selects only
the four supported post-session analysis types, and returns an explicit
`absent`/`generating`/`failed`/`ready` contract; a 12-minute read-time stale
derivation prevents an orphaned generation from spinning forever. Regenerable
strength/mobility/walk reads require both the current prompt and current
activity-check-in version, while handover requires the current prompt and a
deterministic input-packet fingerprint.

Batch 14 makes the *week* adaptive (`services/weekly_restructure.py`): a
deterministic permutation engine reorders the week's bike sessions so VO2 and
Sweet-Spot are never on the same/adjacent days (hard rule), and, when a recovery
signal (readiness / HRV / morning-verdict trend) shows fatigue, defers hard
sessions later in the week. Applying a restructure versions the changed
`planned_workouts` days, audits it in `analyses` (`weekly_restructure`), and
reconciles changed bike workouts through the Batch 29 push-on-plan-set rail. It
is human-triggered via `GET/POST /api/v1/restructure/*`, not the scheduler. The
VO2 progression (incl. Rønnestad 30/15 from ~Wk7, ERG off) is a
shared `services/vo2_progression.py` toolkit used by both the plan seed and the
restructurer. No new migration.

Batch 16 generates whole future blocks (`services/block_generator.py`): a
deterministic generator emits a structured 13-week 2121 block (2 build / 1
recovery ×3, wk12 taper, wk13 consolidation) from profile/FTP, reusing the shared
block templates + the Batch 14 VO2 toolkit so generated VO2 days carry the 30/15
progression. The draft is a **refine-then-lock** workflow (Decision #16): it lives
as JSONB in `knowledge_base` at `section='generated_block'`, each generate/refine/
lock versions the row, and only `lock` writes the owned plan — versioning
`plan_blocks` + active `planned_workouts` so the block feeds the daily loop and
delivers the as-planned baseline through the Batch 29 push-on-plan-set rail.
Human-driven via `GET/POST /api/v1/block-generator/*` with a `/builder` PWA page;
`generate` refuses to clobber an unlocked draft so refinements are never silently lost
(DECISIONS #69). No new migration.

Batch 17 turns the accumulated history into proactive insight
(`services/insights.py` + `services/experiment_tracker.py`), all deterministic
(no LLM) and migration-free. **FTP-drift detection** reads the trend in ride
efficiency (watts-per-heartbeat) and flags rising/falling/stable with the
evidence window surfaced; **early-warning alerts** measure the HRV/sleep/readiness
slope and fire when ≥2 trends degrade *before* a Red (a Red already present is
`already_red`, not early); **driver/correlation analysis** ranks the strongest
Pearson movers of sleep/recovery over the 84-night+ history. These are surfaced
read-only via `GET /api/v1/insights/{ftp-drift,early-warning,drivers}` and audited
to `analyses` only for actual findings via `POST /api/v1/insights/run`. The
**experiment tracker** manages Mark's standing hypotheses (collagen, recovery-week
disruption, 04:00 waking) in the existing `experiments` table with a validated
`active`⇄`paused`→`concluded` lifecycle and an `analyses` audit trail, via
`GET/POST /api/v1/experiments/*` (DECISIONS #71-72).

## 3. Knowledge Base (the persistent context — replaces his handover docs)

Editable structured state fed into every analysis. Source: his handover doc (see
memory `reference_garmin_app_handover`). Includes:

- **Mark-facing Coach memory read:** the retained context is no longer only an internal admin editor; authenticated users can open a plain-language "Coach memory" view of their own stored context, while the raw JSON / plan-override tools remain admin-only on the same route (Batch 124 / DECISIONS #202).
- **Confirmed conversational memory:** Batch 151 adds an on-demand distillation pass over the last 30 days of user-authored follow-up chat, check-in/post-workout notes, and corrections. Strict typed extraction plus a code-side durable/transient and coaching-integrity filter produces pending proposals only. Batch 162 makes confirmation immutable and evidence-bound: acceptance re-resolves every cited user-owned source and accepts only the extracted wording, so a reviewer can confirm or reject but cannot substitute new text. Only accepted items version the `learned_context` KB section. Prompt packets expose statements structurally as `classificationImpact: none` + `contentRole: untrusted_user_data` + `untrustedQuotedData[]`; a fixed system instruction on morning and all four post-session readers says quotes are data, never instructions, and cannot contradict the deterministic verdict, measurements, data-quality rules, or safety guardrails. Batch 169 bounds that packet to the latest 12 eligible confirmed items and omits dated items older than one year, while fed-forward corrections are limited to the latest five within 45 days. Batch 181 narrows the former “stored measurement always wins” rule: an explicit report of what Mark's own device displayed is better evidence for that observed display and must be acknowledged as an app/device data-quality discrepancy, but it does not rewrite the stored row, alter the deterministic verdict, or weaken any coaching floor (DECISIONS #231/#243/#250/#261).

- **Profile:** age 57, FTP 280W, VO2max 54, HRV band 43–57ms, RHR 45, BP ~108/68, fitness age 48.
- **Data-quality rules (AI MUST obey):** never reference L/R power balance (single-sided meter, doubled); SpO2/HRV reliable only from 11 Jun (strap re-tightened); exclude wrist-HR strength sessions from recovery; ignore the constant/broken "Duration" column in his Excel export.
- **Age-adjustment:** sleep score is recomputed from Garmin sleep sub-scores + stored stage seconds against age/sex sleep-stage bands (Batch 61 / DECISIONS #135), replacing the old flat `+4`; `/sleep` shows healthy range bands plus Garmin young-adult targets where comparable.
- **Sleep protocol & thresholds:** pre-cool to 17°C, seal ~22:00, peak >19.5–20°C = thermal disruption; coherence breathing 20:00 (non-negotiable); bedtime 23:15; snack ≤21:30.
- **Training plan** (structured, stored, versioned — see §5).
- **Training schedule:** Mark's normal weekly rhythm/rest days as first-class
  context (Mon/Fri rest days; Saturday long ride), so review/verdict narratives
  do not suggest generic extra recovery days without checking his existing
  routine.
- **Active hypotheses:** collagen (don't reintroduce before 7 consecutive 74+ nights), recovery-week sleep disruption, 04:00 waking.

## 4. Analysis engine (the heart — Claude generates it)

Assembles a context packet (KB + DB data + rolling trend + plan) and calls Claude.

- **Morning:** sleep analysis (age-adjusted by the Batch 61 recompute) · physical-metrics read · **Metrics-vs-Baselines table** (baselines computed from his 84-night history) · thermal/environment review vs his targets · **Green/Amber/Red workout verdict** for today's full plan (cycling + strength). On Home, `/sleep`, and the morning brief itself (Batch 130, DECISIONS #208), that table now reads as `Metric | Last night | Status`, with the status column carrying the personal baseline range plus the explicit direction-aware ✔/⚠ cue, aligned to the same 3-column language as the sleep-stage age table (Batch 122 / DECISIONS #200). The thermal review uses the `/bedroom` chart's shared 21:30→09:00 local observation window, then measures `indoorPeakC`/low/last over Garmin's actual sleep interval when available; pre-bed low/onset/drop are separate pre-cool evidence, and missing sleep times fall back to the shared full-night window (Batch 92, DECISIONS #165). **Generated when Mark checks in** (Batch 85) — the wake job pre-syncs all inputs (stability-guarded from his Garmin `sleepEnd`) and nudges "good morning", then his check-in generates the brief on the spot, folding in his notes/questions (and answering a question left in the notes); an 11:00 backstop covers a skipped morning (DECISIONS #87/#158/#217; design in `docs/designs/wake-triggered-morning.md`). The brief **leads with a deterministic "Today" action block** (Batch 86, #159) — the eased-ride approval first-class and tappable-to-approve, plus any week swap and sleep/thermal nudges — assembled by the backend next to the prose (`verdict.todayActions`, surfaced on `AnalysisOut`, like `swapSuggestion`/`weeklyMix`, no migration) with the coaching reasoning kept below; the frontend `TodayActions` block taps the same approve/swap rail Home uses and gates the approve affordance on live delivery state.
  - Batch 148 adds a read-only `trainingWeekSoFar` packet assembled over the calendar week from the final active `planned_workouts`, explicit `workout_moved`/`workout_skipped`/`workout_removed`/`workout_replaced` audit rows, and real Garmin `activities` (with matched-plan linkage where a post-session analysis provides it). Only `executed` activities are completion truth; planned and changed records can never imply that a session happened. The morning and weekly-review prompts now treat `trainingSchedule` as the usual routine only, never this week's history, and ground weekday/load claims in this factual record. Recommendation attribution is deliberately not inferred because the current audits do not durably link an accepted move back to the specific app suggestion. No migration or shared-schema/verdict change (DECISIONS #227).
  - Batch 174 makes the same data-truth boundary explicit around adjacent days. Post-workout packets carry every active planned row on subject day +1 and +2 as `upcomingWorkouts`; prompt v12 treats those dated title/duration rows as ground truth and uses the prose `weeklyRhythm` only when a date has no real row. Morning packets load yesterday's `DailyMetric` even when no activity exists and separate `yesterdayLoad.wholeDayCost` (all-day stress and Body Battery drain/end) from the activity-derived `statusScope="exercise_only"`. The whole-day context is narrative-only (`classificationImpact="none"`): the deterministic verdict and training-load signal receive no new threshold or input. No migration, shared-schema, endpoint, or frontend change (DECISIONS #254; PR #203, squash `b051b7c`).
  - The `/brief` page now also offers a client-side **read-aloud** control (Batch 106 / DECISIONS #179): the stored markdown is converted into plain spoken prose and read through the browser's built-in `SpeechSynthesis` API. This is frontend-only and ephemeral — no hosted TTS, API write, or persisted audio/transcript. Batch 111 (DECISIONS #184) picks the best available *on-device* voice for the read rather than the platform's default: `selectBestVoice` scores `getVoices()` results by locale match then quality-keyword name signals, but only ever considers voices with `localService === true` — remote-synthesis voices (e.g. Chrome's default "Google UK English" voices, which send text to Google's servers despite living behind the same `SpeechSynthesis` API) are excluded outright to preserve #179's privacy rationale, falling back to the browser's own default voice when no local match exists. Batch 116 (DECISIONS #189) adds an **opt-in hosted/neural voice** on top of that on-device default: an explicit, off-by-default `Profile.hosted_tts_consent` flag (Settings → Voice) must be switched on before `BriefListenControls` will try `POST /api/v1/tts/synthesize` ahead of the on-device path; any hosted-call failure (stale consent, unconfigured model, network) falls straight back to on-device `SpeechSynthesis`, and synthesized audio is cached in-process only, never persisted. The hosted engine is **self-hosted Piper** (DECISIONS #190, superseding an initial OpenAI-hosted build), run via `services/piper_tts.py` shelling out to the `piper` CLI against a voice model baked into the Docker image — brief text never reaches a third party even when a user opts in, since synthesis happens on our own Railway infra. Synthesis takes tens of seconds at `medium` quality (DECISIONS #197), so `services/tts_pregenerate.py` warms the in-process audio cache in the background right after each brief is generated (DECISIONS #198) for consenting users, using a Python port of the frontend's markdown-to-speech transform (`services/markdown_speech.py`) to compute the same cache key the browser will request — no persistence, still in-memory only.
  - The `/sleep` hub now also supports **date browsing** for the retrospective side of the loop (Batch 107 / DECISIONS #180), and that history remains browseable even before today's check-in (Batch 108 / DECISIONS #181): the Last night tab can reload a past date's stored sleep snapshot plus the matching overnight room/fan chart from the existing date-addressable reads, while today's current-date detail stays gated until the morning check-in/brief exists. The calendar now opens collapsed by default, and a selected historical date also shows that whole day's stored verdict plus planned sessions in place. Batch 120 adds month-grid verdict colouring through a lightweight `GET /api/v1/sleep/verdicts?from&to` range read, so the visible calendar can tint stored Green/Amber/Red days without loading a full daily-loop per cell. This remains additive to the existing selected-day history read and overnight chart seam — no migration.
  - **Verdict framework:** GREEN = HRV balanced+stable, sleep ≥70 (age-adj ≥74), subjective ≥5. AMBER = HRV low/mild-unbalanced, sleep 60–69 → cut duration 20–30%, drop a zone, no HIT. RED = HRV unbalanced+declining, sleep <60 → sub/rest; **never VO2 on Red**. Reconcile a "Low" Garmin readiness as load-driven only when measured recovery and a current positive check-in justify it; Garmin **Poor** readiness is a hard cautious gate after Batch 61. Since Batch 56 / DECISIONS #129, a soft sleep score (age-adjusted 60–73) may stay Green only when Mark-specific recovery evidence is strong: measured clean HRV, resting HR inside his personal baseline band, a measured readiness score at or above the effective floor and outside Low/Poor, and a current subjective score ≥5. Batch 167 / DECISIONS #248 (PR #197, squash `596b2b9`) makes accumulated load a deterministic one-way safety input: a computed acute:chronic load ratio ≥1.50 or Garmin recovery time >24 hours sets an Amber ceiling, so a clean night or the Low-readiness `load_driven` interpretation cannot Green-light a fast ramp; an existing Red stays Red and an existing Amber stays Amber. The packet records the trigger/source under `verdict.trainingLoadCap`, and the model only explains the already-final light. Batch 168 / DECISIONS #249 closes the corresponding baseline-drift gap in the soft-sleep override: the effective readiness floor is `max(personal 84-day median, 60)`, so a sinking personal median can never make the rule more permissive than the existing no-history fallback (the readiness-52 / median-50 audit probe stays Amber while Mark's healthy readiness-66 case stays Green). The packet also compares the two 42-day readiness medians inside the trailing 84-day window; with at least 21 samples per half, a decline of 5 or more points emits a structured `verdict.readinessBaselineTrend` warning and a verdict reason. That alarm is warning-only rather than a colour cap — it can never soften the light, while the later chronic-action decision remains owned by Batch 171. Batch 170 / DECISIONS #251 adds three further deterministic, one-way hardenings: age credit that alone crosses the 74-point Green line is capped at Amber unless the same complete measured recovery/check-in evidence corroborates it; Poor readiness plus soft sleep, low subjective score, a hard prior day, or genuinely elevated RHR escalates Amber to Red; and absent HRV/readiness/check-in evidence cannot satisfy a discretionary Green exception. The structured `verdict.sleepCreditCeiling` and `verdict.cumulativeEscalation` records keep prompt v20 explanatory only. Missing baselines remain neutral rather than being guessed negative. No migration or shared-schema change. The acute Red/Amber gates, #133 personal/absolute readiness floor, #135 age-scoring model, and Red-never-VO2 guarantee remain intact. Batch 173 / DECISIONS #253 (PR #202, squash `04efcb3`) makes the Amber/Red morning *adjustment* coherent without touching the light: one deterministic scaling rule in `services/verdict_scaling.py` (`ease_amber_power_pct`, `ENDURANCE_CEILING_PCT=75`) is shared by the delivery transform (`adjust_ir_for_verdict`), the interval-editor "Scale down" preset, and the narrative, so an already-Zone-2 ride *holds* its intensity and is only shortened (never dropped below the Zone-2 floor — the 67% ride that used to become 54%/60%) while harder intervals drop a zone floored at the ceiling and capped at threshold. The adjustment is now proposed at check-in — not only the 11:00 backstop — because the "never silently re-adjust an acted-on ride" guard (`_deliberately_acted`: an explicit approver or a pushed changed IR) excludes the push-on-plan-set baseline (#99), so a pre-delivered ride still gets a *proposed* (never auto-pushed) adjustment that flips the Home card, and the check-in and backstop paths share the one rule. `verdict.verdictAdjustment` records the planned-vs-adjusted duration/%FTP for the narrative and chat, `classificationImpact="none"`; prompt → v22. Backend only; no migration or shared-schema change; delivery stays propose→approve→push (#29/#99) and Red-never-VO2 is unchanged. Batch 215 / DECISIONS #293 extends that one rule to **Red**, reversing half of #61: Red now splits on what the ride actually is. A ride whose hardest working step is already at or below `ENDURANCE_CEILING_PCT` holds its planned Zone-2 intensity and takes the lighter `RED_ENDURANCE_DURATION_SCALE = 0.85` cut, because sustained low-intensity work builds sleep pressure without the arousal harder sessions produce and gutting it works against the Red rule's own purpose; anything harder — and any day already carrying another session, the combined-load gate — takes the original recovery substitution unchanged (half duration, every step capped at `RECOVERY_CAP_PCT`). **Red-never-VO2 is untouched and is now unreachable by construction as well as by gate:** a ride carrying VO2 can never be classified endurance. The interval editor's pre-filled change is no longer the Amber preset on every morning but today's own verdict adjustment, absorbing the whole reduction into the editable block so it lands on the transform's total and the read, the delivered ride and the editor quote one figure; `verdictAdjustment.intensityHeldAtEndurance` now reads the outcome rather than the verdict's name, and gains `companionSession`. Prompt → v32. Backend + one frontend preset; no migration, no verdict-ladder or threshold change.
  - **Chronic structural action:** Batch 171 / DECISIONS #252 introduced an approval-gated seven-day deload without changing the daily Green/Amber/Red verdict. Batch 182 / DECISIONS #263 corrects the asymmetry in that first design: the deload now belongs **only** to the already-qualified sustained-marker path (a personal readiness/HRV/RHR band missed on ≥70% of ≥10 samples inside a ≥21-night window). A Red-cluster path is acute evidence instead. Each Red in the rolling seven-day window is deterministically qualified from that day's `recovery_time_min`/`acute_load`, HRV state/value against its personal floor, RHR against its personal ceiling, and the already-persisted morning `feel`/`notes`. A recognized acute cause in the check-in, or >24 hours of recovery debt beside intact measured HRV and in-band RHR, excludes the Red from cluster escalation; an unexplained or systemically strained Red counts. Two qualifying Reds can propose only a hard-for-easy **rearrangement** through the existing category-scoped swap preview → explicit Apply rail; the candidate pair stays inside one Monday–Sunday training week so its mix is preserved. A recovery/taper/consolidation `plan_block` with an active planned session inside the seven-day horizon suppresses either structural proposal as redundant. Holiday windows and deterministically classified morning-check-in causes are carried as `recordedTrainingContext`, so future quiet-period analysis can read persisted reasons without adding a duplicate form or an LLM-controlled verdict path. Batch 212 / DECISIONS #290 hardens that classification after it read two of Mark's bedroom-temperature notes as illness: a cause pattern now requires an illness- or alcohol-bearing qualifier, every check-in-derived row carries the `matchedText` that produced it so a tag can be explained — or recognised as wrong — from the packet alone, and a `chronicAction` that is neither triggered nor excluding a Red is internal bookkeeping the read does not mention at all. Sustained-marker deload proposals retain the safe transform (25% shorter, one zone lower, no HIT/VO2), acute same-day adjustments and already-acted proposals still take precedence, and audit tags remain idempotent. `verdict.chronicAction` carries qualification/suppression evidence with `verdictImpact="none"`; morning prompt v27 explains but cannot recolour the deterministic light. No migration, new endpoint, provider, auth, hosting, or verdict change; the shared swap schema gains optional date/id provenance for honest future-session wording.
  - Batch 194 / DECISIONS #276 narrows Batch 182's check-in exclusion after the live 07-31/08-01 inversion. `training_load` and `deliberate_rest` are endogenous evidence and always count. Only acute exogenous `alcohol`/`illness`/`travel` reasons may exclude a Red, and only when HRV/RHR do not contradict the note; the rolling seven-day window permits the newest one such exclusion, expiring after two days. The packet exposes the reason partition, cap, age bound and outcome, while `verdictImpact="none"` keeps the daily light unchanged; morning prompt v28 explains the frozen result. A current packet that clears or replaces an active deload also expires future undecided no-event `chronic_deload` proposals to `deleted`, recording an idempotent `workout_proposal_expired` audit reason. Approved/failed/pushed/past/other-origin/event-bearing proposals and cloud events stay untouched. No migration or shared/frontend contract change.
  - Batch 201 / DECISIONS #282 closes the remaining acute-ladder asymmetries. Age credit may lift a raw Garmin sleep score below 60 into Amber but can never carry it to Green; the existing complete-corroboration exception remains only for a raw score already outside Red that crosses the 74-point Green line. Low readiness is `load_driven` only with the existing positive sleep/HRV/check-in evidence, an observed ACWR ≤1.30 and no recovery clock above 24 hours; missing or 1.31–1.49 ACWR cannot relax the light. The one shared Amber transform remains 75% duration with Zone 2 held and harder work eased a zone, but its universal ceiling is now 94% FTP (the top of the app's Sweet Spot range) rather than 98% threshold, so delivery, editor, packet/action copy and prompt v30 stay coherent. The combined matrix proves every changed colour or power outcome is equally or more cautious. No migration or shared/frontend contract change.
- **Post-workout:** performance (power/HR/zones/cadence/PC/stamina/TE) · Mark's post-ride check-in
  (RPE / legs / feel / niggles, when present) · workout rating · guided recovery protocol (specific,
  timed) · impact on tomorrow. For a **structured** ride, execution is graded **per work interval**
  against its own %FTP target — the executed per-second trace is segmented on the planned IR's
  interval boundaries (Batch 12.1's `build_structured_workout_ir`) rather than judged by the
  whole-ride average, which is relabelled context; warm-up/recovery/cool-down power is described,
  never graded, and a `fade`/HR-drift signal grounds "held power / no fade" claims. A free/outdoor
  ride with no planned IR falls back to the whole-ride + zone-histogram read (Batch 44, DECISIONS #114).
  Batch 214 / DECISIONS #292 makes that segmentation survive a mid-ride pause and stops it grading
  what it cannot locate: the trace is sliced on Garmin's pause-excluding `sumMovingDuration` when the
  column is present and monotonic (the planned windows are timer time, so a 110 s pause used to shift
  every later window by its whole length — invisible on a steady block, fatal on a 12 s sprint), and a
  lap's `duration`/`elapsedDuration` preference follows the chosen clock. A work step at or below 30 s
  is neuromuscular and grades on **peak** power rather than window mean — `maxPowerWatts`/`peakPctFtp`
  are new — searched either side of its nominal window by the step's own duration when the boundaries
  were not observed, and never graded "over". Any work interval recording below an adjacent recovery
  is a segmentation failure rather than a performance result: the grade is withheld with a
  `gradeWithheldReason`, counted as `execution.ungradedCount`, and the prompt is forbidden from
  offering any trainer/ERG/pacing/fatigue explanation for it. `detect_ride_deviation` will not flag
  "every work interval in one direction" while any grade is withheld.
  Since Batch 87 / DECISIONS #160 this is **check-in-first for all four supported activity
  types** (ride / strength / flexibility / deliberate walk): sync nudges without generating,
  the generic activity-linked check-in dispatches to the correct reader and returns the new read
  inline, and an empty submit remains an explicit objective-read affordance. Each prompt answers a
  question in the notes from the packet; the 20:30 backstop preserves a read for ignored sessions.
  This remains read-only coaching — it never writes a delivery adjustment.
  **Shipped** in PR #112 (squash `011e342`; production verified on the merge SHA).
- **Tonight:** the static KB `sleep_protocol` is personalised by a deterministic, advisory
  `sleepProjection` on `/api/v1/daily-loop` (Batch 46, DECISIONS #116). It reads today's synced
  training timing/load, Mark's measured sleep drivers (Batch 17/34), bedroom temperature, overnight
  weather, and `fan_auto_enabled`, then emits a qualitative `routine` / `watch` / `protect` read
  plus 1-2 prep actions for Home's Tonight section. It deliberately predicts no numeric sleep score,
  writes no `analyses` row, and never changes Green/Amber/Red or fan thresholds; rest days and
  insufficient driver history fall back to the plain protocol. Batch 184 (DECISIONS #264) moves
  that input assembly into one shared service used by both the daily-loop and scheduler, applies the
  45-minute Hive freshness rule at that boundary, and makes the 20:00 push carry the same projection.
  `watch` and `protect` notify with evidence-naming copy, personalised `routine` stays silent, and the
  fixed fallback protocol is capped at once per profile-local Monday-Sunday week; the existing daily
  idempotency tag, 20-minute fire window, holiday-away skip and notification delivery rail remain.
- **Output rules:** bold each bullet headline; sleep summary line; ignore the phase-frequency system (he wants DAILY always); specific recovery suggestions.

Validated 19 Jun with a real sample → his verdict "fantastic." Demonstrated 5 wins over his Copilot flow: age-adjustment, plan-awareness, no wrong-screenshot errors, causal thermal insight, trend memory.

**Shared Anthropic text boundary (Batch 84, DECISIONS #157).** All six narrative
clients — morning, post-workout, strength, flexibility, walking, and the shared
review boundary used by reviews/trends/handover — call one helper
(`apps/api/src/services/anthropic_text.py`) instead of duplicating the Messages
call. `settings.anthropic_max_tokens` is `4096` (was `1600`, too low for a rich
morning brief); the helper treats `stop_reason == "max_tokens"` as an error so a
truncated response is never persisted as a finished analysis — the existing
staleness/regeneration path reruns it instead. No prompt or verdict change.

**Coach conversation context (Batch 119; ask-time assembly in Batch 178, PR #207
/ squash `5d5abee`, DECISIONS #258).** A follow-up chat on a read was originally grounded in exactly
one thing: that read's `context_packet`, frozen when the read was generated. From
Batch 178 the stored packet remains the read's own record — the read's markdown
was written from it — and `services/chat_context.py` layers a freshly assembled
app-state block alongside it *when the question is asked*: what has happened
since the read (activities, check-ins, plan-action audits, newer reads, and
whether the read still reflects the latest check-in, using Batch 159's check-in
version), today's plan, the week ahead, the monthly trend series plus the
year-on-year comparison, the latest weekly/monthly review conclusions, recent
sessions, and sleep history. It reuses the existing deterministic builders
(`TrainingWeekService.build_window`, `TrendsService.windows` +
`compute_year_on_year`, stored review rows) rather than duplicating them, is
capped at a documented character budget, and *names* anything trimmed so an
omission can never read as an absence. Deterministic pre-assembly, not tool-use:
the shared Anthropic text boundary has no tool support and the app's builders are
already cheap and unit-tested. Explanatory only — it cannot move Green/Amber/Red
or any floor, and plan changes stay on the propose/confirm rail (#29).
Batch 202 (DECISIONS #283) tightens that context contract: generated read packets
store `prompt.version` plus a SHA-256 `prompt.systemHash`, never the full
system prompt body, and anchored chat sanitizes legacy packets the same way
before serializing them. The "since this read" activity delta is keyed to app
ingest time (`Activity.created_at`) while still showing the activity start time;
closed subject-date workouts are labelled as current status, not as a timestamped
change, because `planned_workouts` has no status-change timestamp. Field-level
truncations are named in `omittedForLength`, the character budget is explicitly
best-effort if safe trims cannot fit, and today's ask-time context carries the
effective Garmin weight and VO2max with as-of dates. A workout proposal link in
chat now requires all three gates: Mark's question contains adjustment intent,
today's plan has a live adjustable bike workout, and the model answer includes
the service-stripped proposal marker.

**One coach conversation (Batch 179, PR #208 / squash `94f10b8`, DECISIONS #259).** Until this batch the
schema asserted that a conversation cannot exist without a document —
`brief_messages.analysis_id` was `NOT NULL` — so there was no way to just ask the
coach something, and Sleep (which borrows the morning read) and the
breathwork/strength/walking briefs (not `analyses` rows at all) could not host a
chat. Migration `026` makes the anchor optional, and the rows now *are* one
rolling per-user thread: the read a question came from is a **context seed**, not
a fence, and `origin_kind`/`origin_date` carry the same seed for the surfaces
with no read. `GET|POST /api/v1/coach/messages` serves that thread from a
launcher mounted app-wide in `Layout`; the per-read
`/api/v1/briefs/{id}/messages` surface is unchanged and still lists only that
read's turns, so a brief shows its own exchange while both write into one
conversation. Three consequences are load-bearing: the learning pipeline's joins
to `analyses` became **outer** joins, or every unanchored message would have
dropped out of Batch 151's distillation silently; the turn cap moved from
per-document to per-local-day, because a document no longer bounds the
conversation; and the propose affordance is keyed on **today's real plan state**
(`ChatContext.adjustable_workout_id` — active, unclosed, structured, a bike
session, and not inside a holiday window) rather than on `analysis_type`, so it
appears from any entry point exactly when something is adjustable. The floors,
the grounding and no-plumbing rules, the Batch 175 science lane, propose/confirm
and anti-sycophancy live in one `services/coach_policy.py` registry the
conversation composes from, and the deterministic read prompts are audited
against it rather than rewritten. Decision #29 and Red-never-VO2 are untouched.

**Derived facts carry their basis (Batch 217, PR #247 / squash `ba41918`).**
A fact in the packet now says *how the app reached it*, in a sentence Mark can be
told. The failure was not a missing reason: on 2026-08-20 he asked what the basis
of his 23:15 bedtime target was and the packet **held** the answer — the stored
section's `source`, `batch_5_seed` — while the coach said it would be speculating,
because `NO_PLUMBING_RULE` forbids repeating an internal token to him. A basis
expressed as an enum occupies the slot without being usable, so it is invisible to
a search for missing provenance. `INTERNAL_SOURCE_BASIS` translates a provenance
token to a sentence and `source_basis` returns `None` — omitting the key — for a
value it does not recognise, because `source` is not always a code constant (an
imported plan supplies its own). A `DERIVED_FACTS` registry beside `FLOORS` names
the covered facts, and `facts_missing_basis` treats an internal token in a `basis`
field as missing rather than present. The convention itself pre-existed in four
places (`trainingWeekSoFar.grounding`, Batch 212's `matchedText`/`basis`, Batch
214's `gradeBasis`/`boundarySourceNote`, `chat_context._state_meaning`); Batch 217
names it rather than adding a fifth copy, and applies it to the two facts still
bare — the weekly-mix accounting and the internal `source` tokens. Each
`weeklyMix` bucket carries a sentence naming the week window, the plan-derived
count, that the target is his own week's count rather than a standing quota, and
any skipped session silently excluded from it; `WeeklyMixCard` folds the same
sentences behind a "Where these numbers come from" disclosure. The eleventh floor,
`no_invented_derivation`, closes the other half: where no basis exists the model
invented one — a fabricated Garmin skin-temperature mechanism on 2026-08-14,
repeated the next day as established — and `GROUNDING_RULE` covered inventing
Mark's data but never the app's own reasoning. It is owned by the morning and
post-workout reads, the two surfaces carrying derived deterministic facts, and
composed into the conversation from the registry. Prompts → morning v33,
post-workout v18, chat v9. No migration; the shared schema gains one optional
`basis` string so a pre-217 stored read still parses.

**Conversation dates (Batch 183, PR #214 / squash `7d1d479`).** The shared
`CoachConversation` renderer inserts a local-date separator whenever the ordered
thread crosses a calendar-day boundary and shows a 24-hour local time on every
message. Every mount supplies the user's IANA timezone: the app-wide launcher
uses the authenticated profile, Morning Brief and Home use the daily-loop
timezone, and historical Week uses the profile timezone. The existing
`createdAtUtc` transport and storage, global index/order, and per-read filter are
unchanged. This is a frontend-only presentation change with no API, schema,
query, index, or migration work.

## 5. Data model (sketch — build from real JSON shapes in `~/garmin-spike/out/`)

- `profiles` (display name/role, timezone, lat/long, Garmin user profile pk, Hive home id;
  Garmin secrets stay in environment/secrets and garth token cache for v1)
- `refresh_tokens` (legacy table name retained for migration compatibility;
  SHA-256 hashes of long-lived device tokens and short-lived, single-use
  activation codes, discriminated by `purpose`; no JWT refresh credentials)
- `daily_metrics` (readiness, recovery_time_min, training_status, stress, body_battery, hrv_*, rhr_*, weight_kg, vo2max). **One row per `(user_id, calendar_date, phase)` since Batch 205 / DECISIONS #286**, where `phase` is `morning` (the wake observation the verdict is computed from, written by the wake sync) or `settled` (the closed-day observation, written by the `D-1..D-3` re-sync and both history importers). Before that it was one mutable row per day: `recorded_at_utc` is Garmin's own timestamp on the training-readiness reading, so the next morning's re-fetch of a closed day returned its *final* value and overwrote the record the verdict had been computed from (CI191-02). Read through `services.daily_metric_phase` so a consumer states which observation it means — `morning` for the engine and all retrospective narrative, `settled` for yesterday's whole-day cost and the Garmin freshness check. Stress and Body Battery drain remain running local-day totals that whole-day consumers may read only from coverage-proven settled rows. **Batch 224 / DECISIONS #302 records the one live-morning exception:** a genuinely partial Garmin window may expose Body Battery charge as the overnight recharge accumulated from local midnight to the wake sync, with a readable basis; unknown/complete windows fail closed, and part-day drain stays withheld with a reason instead of being compared with its full-day baseline. Body Battery end at wake remains a point-in-time level. **Batch 225 / DECISIONS #303 records the mirror-image exception:** `vo2max` is resolved through `index_post_activity_by_date`, which prefers the settled row because Garmin writes the number only after a qualifying activity — the wake row holds nothing rather than a partial figure. It falls back to morning (2026-06-21, the first two-phase date, is the one morning row that has ever carried a reading), and every recovery read on the same date is unaffected. Surfaces that need the value before the day's settled row exists use `body_metrics.resolve_effective_vo2max`'s 90-day lookback and must carry its as-of date. A date with no stored morning read falls back to `settled` rather than inventing a wake observation.
- `sleep` (score, qualifier, stage secs, spo2, resp, restless, factors_json)
- `metric_baselines` (persisted trailing-window summary stats for morning analysis, with reliability cutoffs such as pre-11 Jun SpO2/HRV exclusion). Two interchangeable producers, tagged by `source`: the 84-night xlsx import (`source=sleep_history_xlsx`, Batch 4) and — once real history exists — recomputation from stored `daily_metrics` + `sleep` (`source=db_history`, default trailing 84 nights; DECISIONS #88) — reachable both from the manual `src.metric_baselines_backfill` admin runner and, since Batch 228 / DECISIONS #306, from the nightly `baseline-refresh` scheduled job. Before that job existed **nothing refreshed a baseline at all**: `created_at` shows the only four refreshes ever performed were side effects of a human adding a new metric, the longest gap 46 days, and the drift landed on a safety gate (`effective_readiness_floor` is `max(personal_center, 60)`, and a stale median held it a point more permissive than Mark's own history warranted). Freshness is read from `window_end_date`, never from `updated_at` — `UpdatedAtMixin` has no `onupdate` and no trigger, so that column never advances. The morning read consumes all baseline rows regardless of source and falls back to the static KB profile bands when the table is empty.
- `activities` (+ `activity_timeseries`: power/hr/cadence/resp/performance_condition/stamina).
  `activity_timeseries` is by far the largest table (~85% of the database) and the only
  one replayable from an upstream source, so it is the single table whose *row data* the
  nightly backup excludes — restore leaves it empty and `garmin_history_backfill` refills
  it from Garmin (DECISIONS #262). Its write path is guarded: the poll rewrites an
  activity's samples only when the stored row count **or** the latest sample timestamp
  differs from the incoming payload, since details are fetched with `maxchart=2000` and a
  long ride returns exactly 2000 points both mid-session and once finished. A payload that
  parses to zero samples never deletes.
- `temperature_readings` (Hive poll) · `weather_daily` (daily high/low,
  overnight low/wind, precipitation, sunrise/sunset)
- `fan_state_readings` (overnight fan-control time series, migration `011`, Batch 31): one
  idempotent tick per within-window `run_fan_control` fire — `phase`/`auto_enabled`/`observed_temp_c`/
  `fan_on`/`fan_speed`/`action` (`apply`/`hold`/`no_data`/`auto_off`/`unreachable`/`winddown`) — so the
  `/bedroom` chart (`GET /api/v1/bedroom/overnight`, a pure temp×fan×sleep join) shows what the fan did
  and *explains* gaps. The same overnight summary also carries `warning_minutes` / `critical_minutes` and a
  one-glance `room_verdict` (green/amber/red) derived from the existing 19.5/20.0 °C thresholds, so Home and
  `/bedroom` can badge the night without a new query. The fan decision logic is unchanged; this only adds a
  write + a read (DECISIONS #101 / #104)
- `manual_entries` (BP, subjective, RPE, feel, supplements, food, plus
  adherence captured against the planned-workout version that was actually done; Batch 26 also links
  post-ride subjective check-ins to `activities.id` via nullable `activity_id`; migration `029` adds
  wake-date `sleep_setup_json`, and migration `030` adds explicit prior-night
  `rem_intervention_feedback_json`, where unknown remains distinct from `not_applied`)
- `planned_workouts` (structured intervals; **versioned** — VO2 sessions get revised mid-block) · `plan_blocks` (13-wk 2121: 2 build/1 recovery/wk12 taper/wk13 consolidation). A real hand-authored plan is **ingested from his docs** via `src.plan_import` (pure `build_plan_rows` + idempotent `import_plan`, tagged by `source`, e.g. `plan_no2_import`) reading a reviewed JSON in `apps/api/data/plans/`; it replaces the forward schedule from the plan's Monday start and leaves earlier weeks intact (DECISIONS #102). The Batch 5 auto-seed remains a placeholder for a stateless user.
- `workout_delivery_proposals` (approval-gated Zwift delivery snapshots:
  structured IR, intervals.icu payload, deterministic `.ZWO`, status/event id)
- `garmin_workout_deliveries` (outdoor-ride Garmin Connect delivery per slot,
  keyed by user+date: Garmin workout/schedule ids, uploaded payload, IR snapshot,
  status + `last_error` — Batch 78, kept separate from the intervals rail)
- `analyses` (stored Claude outputs and experiment-assignment/evaluation provenance) ·
  `experiments` (tracked hypotheses; four standing experiments after Batch 221, with source-owned
  forward nightly observations and immutable weekly REM assignments) · `knowledge_base`
- `conversation_learning_proposals` (migration `021`, Batch 151): user-scoped,
  RLS-enabled pending/accepted/rejected memory candidates with kind, proposed
  statement, source/date/analysis evidence, deduplication fingerprint, and
  explicit reviewer/timestamp. Pending/rejected rows never enter analysis
  packets; acceptance copies the reviewed statement into a new active version
  of `knowledge_base.section='learned_context'`
- `brief_generation_status` (per-`(user, subject_date)` morning-brief generation state — `generating`/`ready`/`failed` + a classified `reason` such as `billing`; migration `020`, Batch 141) surfaced on the daily-loop envelope so a failed generation resolves to a retryable error instead of an endless "Writing your brief" spinner, and a billing-classed failure alerts the operator (DECISIONS #220). Batch 144: a `generating` row orphaned past `settings.brief_generation_stale_after_minutes` (default 12 min) reads as `failed`/`stale` at envelope-serialization time — a read-time derivation off `updated_at`, no writer/migration/scheduler — with a mirrored 12-min client max-wait cap, so a stuck/orphaned generation can never spin forever (DECISIONS #223)
- `job_runs` (migration `027`, Batch 195): operator-only invocation evidence
  carrying the cadence window, start/finish, succeeded/skipped/degraded/failed
  status, stable reason and integer counters. It has RLS but no authenticated
  policy, and its independent transaction survives rollback of the job it
  records.
- `post_activity_generation_status` (migration `022`, Batch 159): one
  RLS-enabled row per `(user, activity)`, optionally linked to the completed
  planned workout, carrying subject date, supported post-session analysis type,
  `generating`/`ready`/`failed`, and a classified reason. It is deliberately
  activity-scoped rather than reusing the date-scoped morning-brief table so
  mixed training days cannot overwrite or misreport one another.
- `generation_requests` (migration `024`, Batch 161): durable SHA-256 request
  identities for paid morning/post-session generations, with
  `running`/`completed`/`failed`, a short reclaimable lease, and an optional
  completed `analyses.id`. Artifact-scoped PostgreSQL advisory locks serialize
  same-day/same-activity work before the paid boundary; the request identity
  coalesces identical retries while changed substantive input or prompt versions
  create a new `analyses` row, preserving Decision #219's history contract.

Seed `sleep`/`daily_metrics` with his **84-night backfill** (`12 Weeks Sleep Data` xlsx, 24 Mar–15 Jun; trust all cols except Duration).

Phase 1 Batch 1 implements this as additive migration `002`: the inherited
`profiles` table remains the private auth/user table, with Garmin Coach metadata
added to it, and the v1 domain tables live beside it. Data-shape evidence is in
`docs/v1-data-shape-audit.md`.

## 6. Roadmap

**v1 — daily loop:** 3 syncs + 84-night backfill; store plans (ingested from his docs) + per-day override; morning + post-workout analysis; manual check-in; adherence ("did he do it?"); evening nudges; data-quality guardian; thermal monitoring.

**v2 — the coach:** **Zwift workout delivery** (via intervals.icu) + **executable coaching** — on an Amber morning or a week-restructure, regenerate the adjusted workout and (on approval) push it straight to his trainer, so coaching is *acted on*, not just advised; dynamic weekly restructuring (never stack VO2+Sweet-Spot; defer on fatigue); holiday pause/resume (holiday=recovery-week-equiv; pre-holiday Build1→Build2 on return, Build2→repeat Build1); app-generated 13-wk blocks (refine-then-lock, conversational; Rønnestad 30/15 in the VO2 progression toolkit); real-time evening thermal alerts; early-warning drift alerts; driver/correlation analysis; experiment tracker; FTP-drift detection. `.ZWO` export as no-dependency fallback.

**v3 — long game:** strength watching-brief; hypothesis tracking; weekly/monthly deep reviews; year-on-year/seasonal; auto-generated handover-doc export.

**Conversational learning (shipped 2026-07-25; integrity hardening shipped in Batch 162; cap/decay in Batch 169; observed-data honesty in Batch 181):** distil durable facts / preferences / recurring themes out of Mark's conversations with the coach (follow-up chat, check-in + post-workout notes, corrections) into the persistent knowledge base — so the "living handover doc" grows from dialogue without becoming an unbounded prompt tail. **Extract-and-confirm, never a verdict lever:** Green/Amber/Red stay data-grounded (#133/#135, Red-never-VO2). Batch 150 shipped the post-workout chat signal; Batch 151 shipped the explicit Coach-memory trigger, strict typed extraction, deterministic durable/transient + sycophancy/data-quality filtering, RLS-backed pending proposals, and confirmed-only `learned_context` versioning in PR #180 (`071992e`; DECISIONS #228/#231). Batch 162 shipped exact-wording confirmation, current cited-evidence revalidation, structurally untrusted quoted data, and one fixed instruction/data boundary across every learned-context prompt consumer in PR #192 (`3a58debe`; DECISIONS #243). Batch 169 adds read-time memory bounds: `learnedContext` emits only the latest 12 eligible confirmed items, dated items older than 365 days decay out of the prompt, undated legacy items sort oldest and survive only when the cap has room, and `recentCorrections` feeds only the latest five rows from the last 45 days. Corrections remain context to weigh, never instructions. Batch 181 supersedes #250's over-broad measurement-defence wording only for an explicit own-device observation: the coach calls the stored number what the app recorded, defers to what Mark says his device displayed, and surfaces the mismatch as a data-quality problem; the correction still cannot rewrite stored objective data, completed-workout/plan state, the deterministic verdict, thresholds, data-quality rules, or executable safety gates.
## 7. Phase 0 status (live state in `STATUS.md`)

- [x] Repo seeded from WC2026 infra + cross-tool structure
- [x] **Phase 0a** — football domain stripped; clean auth skeleton (`@coach/shared`)
- [x] **Phase 0b** — provision Supabase + Railway + Vercel + GitHub
- [x] Deployable skeleton: auth + empty dashboard live
- [x] **Phase 1 Batch 1** — data model + profile seed shipped
- [x] **Phase 1 Batch 2** — Garmin sync foundation shipped
- [x] **Phase 1 Batch 3** — Hive + weather syncs shipped
- [x] **Phase 1 Batch 4** — 84-night backfill + baselines shipped
- [x] **Phase 1 Batch 5** — training plan + knowledge base shipped
- [x] **Phase 1 Batch 6** — morning analysis engine shipped
- [x] **Phase 1 Batch 7** — daily app loop surfaces shipped
- [x] **Phase 1 Batch 8** — post-workout analysis shipped
- [x] **Phase 1 Batch 9** — nudges + thermal monitoring shipped
- [x] **Phase 1 Batch 10** — v1 hardening + release polish shipped
- [x] **Phase 2 Batch 11** — Phase 1 debt clean-up shipped (player→user rename, migration 006, dead email service, ForgotPin, score-input JSDoc)
- [x] **Phase 2 Batch 12** — Zwift delivery rail shipped (intervals.icu push + `.ZWO` fallback, propose→approve→push, migration 007; Garmin `GARMIN_TOKENSTORE_B64` auth + Anthropic fail-closed validator). Production daily-loop *data* gate (non-null daily metrics/sleep/morning analysis) deferred to Batch 18 — prod has no Garmin daily-metrics/sleep sync yet (DECISIONS #57)
- [x] **Phase 2 Batch 13** — executable coaching (closed loop): an Amber morning verdict deterministically regenerates today's bike workout (75% duration, drop a zone, no HIT — `adjust_ir_for_verdict`) into an approval-gated proposal; Red can never emit VO2; approved proposals now push same-day (Batch 25 supersedes the original couple-days-ahead default); the week-ahead + propose→approve→push surface is the `/delivery` PWA page, with Home adding same-day approve/override/send; every proposal/push is audited in `analyses` (DECISIONS #61-62/#91). No new migration
- [x] **Phase 2 Batch 18** — production daily-loop data sync shipped (06:30 job now syncs Garmin daily metrics/sleep before morning analysis; Hive uses refresh-token auth and honest freshness gating; strict production smoke green on commit `707850d`)
- [x] **Phase 2 Batch 14** — dynamic weekly restructuring shipped: deterministic permutation engine keeps VO2 and Sweet-Spot off the same/adjacent days and defers hard sessions on a fatigue signal (readiness/HRV/verdict-trend); restructures version `planned_workouts`, audit in `analyses` (`weekly_restructure`), and reach Zwift only on approval via `GET/POST /api/v1/restructure/*` (human-triggered, not a scheduler job); VO2 progression incl. Rønnestad 30/15 centralized in `services/vo2_progression.py` (DECISIONS #63-65). No new migration
- [x] **Phase 2 Batch 15** — holiday pause/resume shipped: holidays treated as recovery-week equivalents; in-window workouts versioned as `status='skipped'`, `source='holiday_pause'`; on return, 2121 block continuation: Build1→Build2 (week S+1), Build2→repeat Build1 (week S-1); windows stored as JSONB in `knowledge_base` at `section='holiday_windows'`; frontend Holiday tab + `HolidayPage.tsx` with pause/resume UI (DECISIONS #66-68). No new migration
- [x] **Phase 2 Batch 16** — app-generated 13-week blocks shipped: deterministic generator (`services/block_generator.py`) emits a structured 13-week 2121 block from profile/FTP, reusing the shared block templates + Batch 14 VO2 toolkit (generated VO2 days carry 30/15); refine-then-lock workflow stored as JSONB in `knowledge_base` at `section='generated_block'` (versioned per generate/refine/lock); `lock` versions `plan_blocks` + active `planned_workouts` so the block feeds the daily loop and the Zwift rail on approval; `GET/POST /api/v1/block-generator/*` + `/builder` PWA tab (DECISIONS #69). No new migration
- [x] **Phase 2 Batch 17** — monitoring + insight shipped: deterministic FTP-drift detection (ride efficiency trend + evidence window), early-warning drift alerts (HRV/sleep/readiness slope, fires on ≥2 degrading trends before a Red), and driver/correlation analysis (Pearson movers of sleep/recovery over 84-night+ history) in `services/insights.py`, surfaced read-only via `GET /api/v1/insights/*` and audited to `analyses` on `POST /api/v1/insights/run`; experiment tracker (`services/experiment_tracker.py`) manages the standing hypotheses (collagen, recovery-week disruption, 04:00 waking) in the existing `experiments` table with a validated lifecycle + `analyses` audit, via `GET/POST /api/v1/experiments/*` (DECISIONS #71-72). No new migration
- [x] **Batch 219 — Nightly environment signals shipped** — the wake-date check-in records bedding, windows, blind position and pre-cool start as one structured setup; migration `029` persists it plus nightly circular-mean wind direction and mean relative humidity. The deterministic insight engine now sees humidity and bedroom mean/min/max temperature and computes REM/awake-minute outcomes; circular direction is stored for the longitudinal analyst rather than misused as a scalar Pearson input. Shipped via PR #249 / squash `2496db2` plus exact-north normalization PR #250 / squash `2c0b3d7`, with final PostgreSQL CI 1287 passed and exact-SHA production schema/reducer verification (DECISIONS #297).
- [x] **Batch 220 — Monthly longitudinal analyst shipped** — a deterministic 427-night columnar evidence packet enters Anthropic's Message Batch API on a monthly paid identity, returns locally revalidated schema-constrained findings, persists a durable analysis lifecycle, and idempotently appends the temperature result to the standing early-waking experiment. Coverage policy forces the current zero-setup result to inconclusive; a distinct subscribed operator is required before paid submission. Shipped via PR #251 / squash `9cdf7f7` plus production-gate rollback fix PR #252 / squash `c0f9451`, with final PostgreSQL CI 1306 passed and exact-SHA production fail-closed verification (DECISIONS #298).
- [x] **Batch 221 — Experiment observation loop shipped** — migration `030` records explicit REM-action application; immutable weekly assignments preserve stable IDs and exact shown copy; all four standing experiments receive idempotent source-owned nightly observations; and the morning packet carries a current read-only evaluation without changing the human-gated terminal status. Daily Loop/check-in join issued prior-night actions across the Monday boundary, and the conservative REM comparison keeps unknown separate from `not_applied`. Shipped via PR #253 / squash `f976d13`, with final PostgreSQL CI 1315 passed and exact-SHA production schema/rollback verification (DECISIONS #299).
- [x] **Batch 222 — Honest check-in freshness shipped** — the off-request morning check-in syncs idempotent Garmin/weather inputs before generation and refuses a read without a current morning `DailyMetric` (plus `Sleep` before the 11:00 backstop). Wake polling now terminates on those persisted inputs rather than an `Analysis`; low-cardinality packet-completeness provenance creates a new paid-request identity when an empty packet becomes complete, while preserving the old read as history. Home and Sleep expose the same proven readiness. Shipped via PR #254 / squash `4292339`, with final PostgreSQL CI 1322 passed and exact-SHA deployed-container boundary verification (DECISIONS #300).
- [x] **Batch 223 — Working-step bike classification shipped** — the freeform structured-bike builder classifies purpose from sustained interval work, so hotter warm-up/cool-down ramps cannot distort the weekly mix. Supra-threshold efforts up to 20 seconds are visible neuromuscular primers rather than phantom VO2 sessions; all-step Red safety remains independent and unchanged. Shipped via PR #255 / squash `0427bb7`, with final PostgreSQL CI 1328 passed and exact-SHA deployed-classifier verification (DECISIONS #301).
- [x] **Batch 224 — Live-morning Body Battery semantics shipped** — a strict settled-row query preserves complete-day charge/drain, while the absence of that row admits charge only from a proven partial morning window and names its midnight-to-sync basis. Part-day drain stays null and its explanation replaces the full-day range/status; unknown charge coverage fails closed with a reason. Point-in-time Body Battery end and coverage-gated stress/whole-day cost stay unchanged. Shipped via PR #256 / squash `711055a`, with final PostgreSQL CI 1330 passed, Chromium mobile/desktop verification, exact-SHA Railway/Vercel production, and read-only deployed-code boundary smoke (DECISIONS #302).
- [x] **Batch 225 — VO2 max reads the row that can hold it shipped** — Garmin recomputes VO2 max after a qualifying activity, so the wake row is structurally empty for that one field and the morning-preferred collapse reported `sampleCount: 0` for every month since two-phase writes began on 2026-06-21. A new field-level phase exception (`index_post_activity_by_date`) prefers the settled row and falls back to morning, restoring the trend series and the year-on-year window; `ageComparison` regains the VO2 max row the frontend has always had a code path for, and `dailyMetrics` carries the resolved reading with its as-of date. Recovery reads stay on the wake row and `garmin_sync` is unchanged. Shipped via PR #257 / squash `1217ec0`, with final PostgreSQL CI 1340 passed, exact-SHA Railway/Vercel production, read-only deployed-code smoke, and the stored monthly trend narrative regenerated (DECISIONS #303).
- [x] **Batch 226 — The closed day's cost meets its baseline shipped** — `yesterdayLoad.wholeDayCost` now carries `baselines`: the median, quartiles, delta and sample count for each finished-day figure, read from the stored `metric_baselines` rows through a `_baseline_comparison` frame shared with the metrics table. The morning table cannot make this comparison — it renders from a packet built at wake, before the subject date's settled row exists — so Batch 224's part-day withhold is unchanged and this is the one surface where a *finished* day can be compared. A figure with no stored baseline says so (production has none for stress or end-of-day level) and a figure with no value emits no entry at all; morning prompt v36 requires the compared form to replace the bare figure rather than sit beside it. Shipped via PR #258 / squash `bda7b7d`, with final PostgreSQL CI 1345 passed and exact-SHA read-only deployed-code verification (DECISIONS #304).
- [x] **Batch 227 — REM judged against Mark before a population shipped** — REM joins `BASELINE_SPECS` and the trends `METRICS` registry, so it finally has the median/quartile treatment every other tracked metric has plus a month-over-month series. One shared `age_norms.rem_sleep_pct` replaces two call sites that disagreed (16.41% vs 15.55% for the same night), named by `REM_PCT_BASIS` where it is surfaced and keeping Batch 61's measured-sleep denominator so the four stage bands stay calibrated. Morning prompt v37 and trends prompt v6 require REM to be read against his own median and quartiles before the age band, with a night above his own upper quartile described as a good night even when it sits below the band floor. Shipped via PR #259 / squash `2119baf`, with final PostgreSQL CI 1355 passed, the required `metric_baselines` backfill run, and exact-SHA deployed-code verification (DECISIONS #305).
- [x] **Batch 228 — A baseline nothing refreshes is one that slowly stops describing him shipped** — a `baseline-refresh` job at 02:30 Europe/London rebuilds every active profile's `metric_baselines` from stored history, registered in all three places the codebase requires (`create_scheduler`, `run_scheduled.JOBS`, `job_runs._LOCAL_DAILY_JOBS`) so its run history buckets as a local day rather than an hour. `run_evening_monitoring_alerts` gains an operator-only staleness detector — a structured `operator alert`, deliberately not one of Mark's stale-source pushes — firing once a profile's baselines trail its stored sleep history by four nights or more, a threshold taken from the five-day drift that actually moved his readiness median 59.0 → 61.0. `updated_at` is left alone and freshness derives from `window_end_date`. The slot is 02:30 Europe/London, corrected at `/batch-start` from the authored 03:30 whose stated reasons were false on both halves: it is now strictly before `wake_detection.WINDOW_START` and before the 03:00 UTC backup year-round. Shipped via PR #260 / squash `bfd2599`, with all sixteen PR checks green, PostgreSQL CI 1370 passed / 0 skipped, exact-SHA Railway/Vercel production and a read-only deployed-container smoke; the first scheduled run is 02:30 on 2026-08-27 (DECISIONS #306).
- [x] **Batch 230 — Every figure on the morning brief reconciles from what it shows** — REM's population frame now travels on its own baseline row as `ageFrame`, computed from the same `sleepRows` entry `SleepStageAgeTable` renders, because that table exists only on `/sleep` and on the brief and Home REM was the one age-normed metric shown with no band at all. `rem_sleep_pct` joins the trends packet's `personalBaselines` — it was absent while the prompt instructed the model to read REM against it, which is why the v6 narrative invented an anchor for REM alone while quoting readiness, RHR and HRV correctly — and `remAgeBand` travels with it so the rule requiring the band is not itself pointing at a missing number. One `REM_FRAMING_RULE` constant is embedded verbatim by both the morning and trends prompts, pinned by a test, because the defect class is two surfaces free to word one rule their own way; the morning-only `SLEEP_STAGE_MINUTES_RULE` makes minutes lead, since 48 minutes is the one REM figure that reconciles with Mark's watch. The stage denominator is named once for the group (`sleepStagePctBasis`) and is unchanged, pinned by a regression. The Body Battery drain row is repointed at the last finished day, quoting the figure `_yesterday_load_packet` already computes, with Batch 224's part-day withhold intact for the case it was written for; and `MetricComparisonTable`'s invisible 3% tolerance stops claiming membership of a printed range, naming the near-miss while the magnitude keeps measuring from the printed quartile. Shipped via PR #261 / squash `4c52106` with PostgreSQL CI **1381 passed / 0 skipped**, plus follow-up PR #262 / squash `aa5703c` closing a leak in this batch's own delivery — the first v7 generation printed a packet field name in Mark's prose, so both prompts now forbid it. Prompts land at morning v39 and trends month/season v8; both trend narratives were regenerated at closeout after the real `latest_narrative` lookup returned null for both buckets (DECISIONS #307).
- [x] **Batch 231 — The lever follows the data shipped** — chronic flags now read their own correlation outcome, and driver selection requires actionable/antecedent classification, unfavourable direction, n≥20 and |r|≥0.15; concurrent or unmitigable drivers cannot become levers. Correlations carry confidence and confounds, and the prompt forbids causal or “strongest lever” overclaiming. `daytime_stress_avg` became day−1 `prev_day_stress_avg`. Deterministic human-written `standing_habits` suppresses already-kept REM interventions before rotation, and historical assignments store the library total. Shipped via PR #263 / squash `c17cd4c`, PostgreSQL CI 1,411/0, exact-SHA production, and a read-only real-packet smoke that selected `bedroom_warning_minutes` at r=−0.2388 on n=64; production habit v2 suppresses `alcohol_free_evenings` (DECISIONS #308).
- [x] **Post-v2 review remediation** — v1+v2 code/security/functional review (`docs/reviews/v1-v2-review.md`); fixes shipped: Red-never-VO2 delivery gate (P1-2 — PR #14, #75); web CSP/headers + `react-router-dom` bump + CI dependency-audit gate (P2-1/2/3 — PR #16, #76); prod API docs disabled + stricter JWT-secret validator (subsequently retired with JWT auth in Batch 160) + backup `PGPASSWORD` (P3-5/6/7 — PR #17, #78). Open + optional: P3-4 (scheduler per-profile isolation), P3-9 (hygiene)
- [x] **Auth simplification Phases 1-2** — passwordless device-token activation live (migration `008`; initially dual-path `get_current_user`; `POST /api/v1/auth/activate`; `python -m src.activate` CLI; `/activate` PWA route — #73-74/#77), followed by the device-token-first PWA soak (PR #19, #79). The Phase 3-4 retirement of that temporary PIN/JWT fallback shipped in Batch 160 below
- [x] **Auth simplification Phases 3-4 / Batch 160 shipped** — device-token-only `get_current_user`; activate/me/revoke-only auth router; PIN/JWT frontend/API/helpers/dependencies removed; migration `023` anti-stranding gate + legacy-refresh revocation + PIN/lockout-column drop; JWT config and Railway secrets removed; recovery runbook added. Shipped via PR #189, squash `a0dd28c`, with exact-SHA production and schema verification (DECISIONS #240)
- [x] **Batch 161 — Concurrency, idempotency & workload budgets shipped** — atomic server-side activation plus client request coalescing; migration `024` generation identities and reclaimable leases; artifact-scoped transaction locks that reuse identical morning/post-session paid work while preserving changed-input history; fail-fast per-user/global Anthropic and Piper slots; and shared per-profile authenticated route budgets. Shipped via PR #190, squash `bc057ea`, with exact-SHA production, schema, auth-gate, and runtime-error verification (DECISIONS #241)
- [x] **Batch 172 — Installed-PWA activation entry shipped** — `AccessPage` accepts a pasted activation link or bare code (`apps/web/src/lib/activation.ts`) and calls the existing `activateDevice`, so the iOS home-screen install — whose storage is isolated from Safari — can authenticate its own device token, closing a Batch 160 regression that left the standalone app with no way in. Frontend-only, no migration. Shipped via PR #191, squash `3449c7e`, with exact-SHA production and a prod `/access` browser smoke (DECISIONS #242)
- [x] **Batch 162 — Learning-memory integrity shipped** — proposal confirmation is exact-wording only and revalidates every current user-owned cited source before the KB write; learned statements are structurally delimited as untrusted quoted data behind one fixed instruction/data guardrail in morning plus all four post-session prompts; cross-user proposal tests and an all-route auth inventory pin ownership. No migration or verdict/safety-rule change. Shipped via PR #192, squash `3a58debe`, with all CI waves green and exact-SHA production verification (DECISIONS #243)
- [x] **Batch 168 — Absolute readiness anchor + baseline-drift alarm shipped** — the soft-sleep override floors readiness at `max(personal 84-day median, 60)`, while two sufficiently sampled calendar 42-day halves emit a structured warning when the recent median falls by at least five points. The alarm is narrative-only and cannot set or soften Green/Amber/Red. No migration or shared-schema change. Shipped via PR #198, squash `d6fa01c`, with all implementation CI waves green and exact-SHA production verification (DECISIONS #249)
- [x] **Batch 174 — Honest analysis packets shipped** — post-workout reads ground the next two calendar days in active `planned_workouts` rows before consulting `weeklyRhythm`, and morning reads distinguish exercise-only status from yesterday's all-day Garmin stress and Body Battery cost even when no exercise occurred. The added context is narrative-only and cannot recolour Green/Amber/Red. Backend-only, no migration or shared-schema change. Shipped via PR #203, squash `b051b7c`, with all final CI waves green and exact-SHA production verification (DECISIONS #254)
- [x] **Batch 180 — Completed-day Garmin aggregates shipped** — the morning sync re-reads D-1…D-3 through the existing retry/upsert path; stress and Body Battery are usable as whole-day values only when their raw local coverage windows prove the day closed, and incomplete stress is excluded from driver inputs. The guarded repair rewrote the 42 affected 2026-06-21→2026-08-01 rows, after which all 404 closed production rows proved complete; eight `db_history` baselines were rebuilt. Backend-only, no migration/shared-schema/verdict change. Shipped via PR #209, squash `70443e7` (DECISIONS #260)
- [x] **Batch 181 — Observed-data honesty shipped** — every CheckMark surface describes figures as what the app recorded and treats a conflicting own-device display as better evidence for that observation, while the deterministic verdict, Red-never-VO2, safety floors, plan/completion state, and propose/confirm remain authoritative. Backend prompt-policy/tests/docs only; no migration/shared-schema/endpoint/frontend/provider/auth/hosting/verdict change. Shipped via PR #210, squash `65f7e87` (DECISIONS #261)
- [x] **Batch 182 — Context-aware chronic action shipped** — explained or expected Reds are qualified from stored check-in causes and recovery/HRV/RHR evidence before cluster escalation; a qualifying short cluster can only offer a same-training-week hard↔easy rearrangement, while planned recovery/taper/consolidation blocks suppress redundant structural action and the sustained-marker deload remains unchanged. Existing holiday/check-in state supplies durable context; no migration, new reason form, endpoint, provider, auth, hosting, or deterministic-verdict change. Shipped via PR #213, squash `7819434` (DECISIONS #263)
- [x] **Batch 194 — Bounded Red-cluster exclusions shipped** — endogenous `training_load`/`deliberate_rest` reasons always count; one newest acute `alcohol`/`illness`/`travel` Red may be excluded for no more than two days only when HRV/RHR stay unstrained. Clearing or replacing a deload signal expires future undecided no-event chronic-deload offers with idempotent audit evidence, without touching decided/cloud-backed rows or the daily verdict. Backend only, no migration or shared/frontend contract change. Shipped via PR #227, squash `813c449`, with all CI waves green and exact-SHA Railway/Vercel production plus deployed-container policy verification (DECISIONS #276)
- [x] **Batch 195 — Observable scheduled-job failures shipped** — poisoned morning write steps recover before verdict generation; every real scheduler/external invocation has a typed outcome, migration `027` records it independently in operator-only `job_runs`, and degraded/failed external runs exit non-zero. A post-successful-`main`-CI workflow alerts if Railway-direct or Vercel-proxied health does not reach the exact merge SHA within 15 minutes. The in-process scheduler and 20:30-only backstop stay enabled pending Craig's separate hosting decision. Shipped via PR #228, squash `2b47745`, with all CI waves green, the freshness workflow passing its first production run, and live migration/RLS/run-ledger verification (DECISIONS #277)
- [x] **Batch 196 — Backup restore proof shipped** — the external runner now exposes `backup-drill`, which restores the newest `coach_*.dump` into `BACKUP_RESTORE_DATABASE_URL` with `pg_restore --clean --if-exists --no-owner` and asserts schema/data invariants: coach table count, Alembic version, profile rows, analysis rows, and zero restored `activity_timeseries` rows because that replayable table is definition-only in the dump. Backup and restore-drill failures emit structured `operator backup alert` logs outside the end-user push/profile model and exit non-zero through Batch 195's job-result boundary. Runbooks define a weekly Sunday 04:00 UTC drill after the daily backup plus encrypted off-site copies with RPO/RTO. Shipped via PR #229, squash `b78a4f2`, with branch, PR-context and post-merge `main` CI green, the production freshness workflow passing, and exact-SHA Railway/Vercel production verification. No migration (DECISIONS #278).
- [x] **Batch 197 — Brief-consistent unprompted speech shipped** — morning-backed transitions compare today's stored morning packet with the latest stored prior packet, so the state-change coach speaks from the same chronic-action and weekly-mix state Mark's brief used and treats a missing comparison side as unknown. The seven-day global budget is a ranked spend: chronic action > experiment outcome > weekly mix, with one strictly higher-ranked pre-emption recorded in the existing analysis packet. Active holidays suppress the job before candidate generation. No migration, verdict, plan, endpoint, frontend, provider, auth, or hosting change. Shipped via PR #230, squash `972c5be`, with branch, PR-context and post-merge `main` CI green, the production freshness workflow passing, and exact-SHA Railway/Vercel production plus deployed-container policy verification (DECISIONS #279).
- [x] **Batch 199 — Fail-closed prompt floors shipped** — ten shared coaching floors each carry canonical wording, a semantic recognizer and an inverted negative control; real Anthropic-calling modules are discovered from the service tree and must be audited or explicitly exempt. Local-time ownership covers morning and every post-session read, and the deterministic load, sleep-credit, cumulative, readiness-trend and chronic-action protections are centrally guarded against narrative softening. Conversation learning is versioned/provenanced, and speech-side mutation tests inspect real stored plan/verdict state. Backend prompt-policy/tests only; no migration, endpoint, frontend, provider, auth, hosting, verdict or plan-behaviour change. Shipped via PR #232, squash `88a8441`, with all CI waves green and exact-SHA Railway/Vercel plus deployed-container policy verification (DECISIONS #281).
- [x] **Batch 200 — Rendered contrast and touch floors shipped** — the affected light-mode UI text now uses semantic text tokens instead of decorative fill tokens, light `--text-muted` clears elevated surfaces, the sleep-calendar selected foreground token is defined, and daily-path controls clear the 44 px hit-area floor. New Playwright coverage asserts rendered visible-text contrast on representative pages in both themes and rendered control boxes on the primary daily path, so the guard touches computed DOM properties rather than CSS declarations. Frontend-only, no migration or durable decision. Shipped via PR #233, squash `2608646`, with local web gates, branch/PR/main CI, Vercel preview, production freshness, exact-SHA Railway/Vercel health, web/protected-route and live-asset smokes green.
- [x] **Batch 201 — Verdict-ladder residuals shipped** — raw-Red sleep credit may rise only to Amber; Low readiness relaxes as load-driven only with positive recovery evidence, observed ACWR ≤1.30 and no recovery clock above 24 hours; the shared Amber transform keeps its 75% duration cut and Zone-2 hold while capping harder work at 94% FTP. Backend only, no migration or shared/frontend contract change. Shipped via PR #234, squash `c6e5f21`, with all final CI waves green and exact-SHA Railway/Vercel plus deployed-container boundary verification (DECISIONS #282).
- [x] **Batch 185 — Proactive weekly review delivery shipped** — Sunday 18:00 Europe/London now generates the ending ISO week's idempotent review, delivers its full markdown once as an analysis-anchored assistant turn in the app-wide coach conversation, sends one conclusion-bearing deep-link push, and exposes an unseen weekly turn through a per-profile client-side marker. A dedicated DST-safe Railway cron service complements the in-process scheduler, with a PostgreSQL advisory lock making overlap safe. Monthly remains manual; holidays skip; no migration or new endpoint. Shipped via PR #216, squash `368ef69` (DECISIONS #265-266)
- [x] **Batch 186 — Week-ahead guidance shipped** — weekly reviews now carry a deterministic `weekAhead` packet for the coming Monday-Sunday week: the forward `TrainingWeekService.build_window` schedule, active plan blocks, the week's own mix targets, hard/quality sessions, the hardest planned day and the previous night that protects it. Review prompt v7 folds this into the existing Sunday weekly-review coach message rather than creating a second push; monthly reviews stay retrospective with `weekAhead: null`. The packet is explanatory-only (`classificationImpact: none`) and cannot move the deterministic verdict or propose/confirm rail. No migration, shared schema, endpoint, frontend, provider, auth, or hosting change. Shipped via PR #217, squash `ccaed51` (DECISIONS #267).
- [x] **Batch 187 — State-change coach turns shipped** — `StateChangeCoachService` detects transitions in already-computed state (Batch 182 chronic action, experiment outcomes, and weekly-mix at-risk buckets), ranks chronic > experiment > mix, and writes at most one assistant turn per profile-local seven-day window into the app-wide coach thread with controlled origin `state_change`. It derives previous state from existing analysis packets rather than a new marker table, drops suppressed lower-ranked transitions instead of queueing them, and adds no push type. Stored `state_change_coach` audit rows declare `verdictImpact: none` and `planMutation: none`; Green/Amber/Red, safety floors, Red-never-VO2 and propose/confirm are unchanged. Shipped via PR #218, squash `a777da1`, with all branch/PR/post-merge CI waves green and exact-SHA Railway/Vercel production verification (DECISIONS #268).
- [x] **Batch 188 — Coach-conversation & prompt-policy integrity review shipped** — lane 1 of the 2026-08-05 wave-2 five-lane audit, diagnose-only. Maps the coach's spoken layer by what actually calls a model: 17 version constants but only 10 Anthropic-calling modules, each with an explicit in-audit/out-of-audit disposition. Establishes that `state_change_coach` is not a prompt surface at all (no model call, hand-written templates), that the genuinely uncovered LLM surface is the unversioned `conversation_learning`, and that Batch 181's flagged own-device-vs-deterministic contradiction is not real — it is bounded in the floor sentence, restated in all eight read prompts and disambiguated by `ANTI_SYCOPHANCY_RULE`. The hard boundary is confirmed intact: nothing spoken can move Green/Amber/Red, a safety floor, Red-never-VO2 or plan state. Report `docs/reviews/BATCH_188_COACH_CONVERSATION_REVIEW.md` ranks 2 High / 8 Medium / 7 Low (`CC188-01…20`) with `file:line` evidence and review-local remediation stubs; no ledger batch numbers are allocated until the whole wave is triaged. No product code, prompt text, prompt version, floor, verdict logic, migration, config or production data changed. Shipped via PR #219, squash `0d9384f` (DECISIONS #269).
- [x] **Batch 189 — Code review of the 157–187 delta and the new rails shipped** — lane 2 of the 2026-08-05 wave-2 five-lane audit, diagnose-only, over `7e77169..cc344f0` (82 commits, 228 files, +25,086 −3,362). Verifies each batch's "boundaries untouched" claim by tracing the data path rather than re-reading the diff, and **nine of ten hold**: Batch 173's `verdict_scaling.py` is provably downstream of the verdict, 176/177's `body_metrics.py` is read-only, 180's coverage guard genuinely gates the driver correlation while the verdict's only load inputs sit outside it, 178's `build_window` refactor is behaviour-preserving by equivalence, 179's nullable anchor is handled by every consumer, and 185's advisory lock is taken before its existence check. Also records a checked non-finding: `022`/`024` enable RLS without policies, but so do `015`/`019`/`020`/`021` — consistent, not an omission. The tenth claim fails and leads the report: Decision #268 names Batch 185's launcher as Batch 187's visibility rail, but the unread dot is hard-coded to `originKind === 'weekly_review'`, so a `state_change` turn lights nothing and pushes nothing. Second High: only the two jobs added by Batches 185/187 roll back, so one DB error poisons the shared Session and aborts the morning pipeline before the verdict — reproduced by execution. Report `docs/reviews/BATCH_189_CODE_REVIEW.md` ranks 2 High / 9 Medium / 9 Low (`CR189-01…20`) with `file:line` evidence and review-local remediation stubs; no ledger batch numbers are allocated until the whole wave is triaged. No product code, migration, config or production data changed, and no production database queried. Shipped via PR #220, squash `1839f79` (DECISIONS #270).
- [x] **Batch 190 — Data / security / ops review refresh shipped** — lane 3 of the 2026-08-05 wave-2 five-lane audit, diagnose-only, grounded in live read-only Supabase/Railway/Vercel evidence rather than repo reading alone. **The data boundary and the new API surface pass.** Production is at Alembic `026`: all 28 `coach` tables have RLS enabled, the four legacy client-facing tables carry the nine policies hardened by migration `025`, the other 24 have no policies and therefore deny all rows to non-owner roles, `anon`/`authenticated`/`service_role` hold no `USAGE`/`CREATE` or table grants on `coach`, no `coach` views, cross-schema foreign keys, triggers or function bodies bridge to the co-resident `public` app, and Supabase's advisor reports zero `coach` WARNs (24 informational deny-all notices only). The ten routers changed since `7e77169` expose 40 current endpoints — one deliberately public activation route, 39 reaching `get_current_user`/admin — and the repo-wide guard covers 91 routes with exactly four permitted public method/path pairs; the rolling coach thread filters by `user_id`, an optional read anchor is ownership-checked before context or model, unknown `originKind` normalizes to the controlled `general` origin, and cross-user learning-proposal list/mutation returns 404 with a regression proving no knowledge-base write. **The batch row's ephemeral-filesystem claim is corrected:** `api-volume` is ready and mounted at `/data/backups`, and the 4/5/6 August archives are owner-only PostgreSQL 17 custom-format dumps (latest 7,842,096 bytes, 27 `coach` table-data entries, `activity_timeseries` definition kept and rows excluded) with valid TOCs; the settled-stream count+timestamp guard and the empty-parse protection hold, so the 4 August egress mitigations are live. **What fails is the gap between recent success and dependable operation, and that leads the report:** Railway still marks the API `sleepApplication=true` while 10 of 11 externally runnable jobs live only in that process's in-process APScheduler (only `weekly-review` has an external cron); every scheduler coroutine catches its top-level exception and returns normally so `run_scheduled.py` exits 0 on internal failure; seven backup-failure audit rows spanning 29 July–4 August paged nobody, and the archive has been TOC-inspected but never restored into a disposable database — on the Supabase Free plan that is the sole recovery system. Deploy freshness has the same shape: the SHA is exposed and was current during the review, but nothing compares it with GitHub `main`, so close-out remains the only detector of a dropped webhook. Owner-role RLS bypass, the shared public-app/project/quota blast radius and unmonitored org egress round out the list. Report `docs/reviews/BATCH_190_DATA_SECURITY_OPS_REVIEW.md` ranks **3 High / 4 Medium / 2 Low** (`DS190-01…09`) with ranked evidence, an evidence ledger and review-local remediation stubs; no ledger batch numbers are allocated until the whole wave is triaged. No product code, migration, config, policy or production row changed — every production query was read-only. Shipped via PR #221, squash `b056558` (DECISIONS #271).
- [x] **Batch 191 — Coaching-integrity audit refresh shipped** — lane 4 of the 2026-08-05 wave-2 five-lane audit, diagnose-only, re-running the `COACHING_INTEGRITY_AUDIT.md` framework against the current engine and against the live `coach` schema. **Kickoff rule: a remediation counts as closed only when it is observed working on real data, not when the code implementing it is present.** On that rule **F2 is closed and fired for real** (2026-07-29: ACWR 1.61 with 1502 minutes on the recovery clock set `trainingLoadCap.applied=true` and turned a Green into an Amber), **F4 is closed and fired for real** (2026-07-31: `cumulativeEscalation.applied=true` made a Red out of a day whose sleep score was 76), **F3 and F5 are closed** (probes confirm a credited 74 is held at Amber unless HRV, resting HR, readiness and the check-in all corroborate, and that absence is neutral rather than positive evidence), **F8 stays RESOLVED** with no narrative softening on any non-Green morning, and **F9 plus both Batch 155 stubs shipped** (learned context 12 items / 365 days, corrections 5 / 45 days, `ANTI_SYCOPHANCY_RULE` in the chat floors). **F1's absolute anchor is load-bearing on 12 of 12 mornings:** the persisted 84-day personal readiness median is 50 (Q1 28, Q3 61), so `readinessEffectiveFloor` resolved to the absolute 60 rather than the personal centre (50.0–53.5) every day — without Batch 168 the soft-sleep override would be testing against 50. **One new gap opened, in the old shape, and it inverts a property the original audit credited:** the audit's second pillar was that the one gate-relevant input the user can edit can only ever *harden* the light. That still holds for the daily light and no longer holds for the structural rail — Batch 182's Red qualification lets one matching phrase in the persisted check-in text retire a Red from the chronic cluster, unconditionally, uncapped, undecaying and with no requirement that the physiology agree. Observed live: 08-01 → 08-04 carried `chronicAction.triggered=true, redMorningCount=2` and a seven-day deload whose first four sessions were approved and pushed; on 08-05, the first morning after Batch 182 deployed, both Reds became `explained_by_check_in` (07-31 `training_load`, 08-01 `alcohol`), the count fell 2 → 0 and the escalation switched off — with 08-01's resting HR at 48 against its own ceiling of 45, and two undecided `chronic_deload` proposals left standing for 08-08/08-09. The second High is structural: `daily_metrics` holds one mutable row per day that the evening sync overwrites (`recorded_at_utc` 19:00–21:30 on 12 of 14 days) while the verdict is computed at wake, so the packet's readiness exceeds the surviving row on 11 of 18 mornings and its recovery clock is lower on 12 of 18 — and Batch 182's `expected_training_debt` exclusion, the 84-day baselines the personal floors key off, the trend alarm and the chronic misses all read the evening value. The spoken layer has not undermined the deterministic one, largely because it has barely spoken (0 `state_change_coach` turns ever, newest scheduled `weekly_review` 2026-06-29, 0 sycophancy attempts across 41 user turns) — but none of the five deterministic protections added since Batch 155 (`trainingLoadCap`, `sleepCreditCeiling`, `cumulativeEscalation`, `readinessBaselineTrend`, `chronicAction`) is in `coach_policy.FLOORS`, so their non-softening rules are hand-written into one prompt, unprotected by the drift test, and not inherited by the chat. Verdict distribution across the three audit windows is 48% → 71% → 75% Green, reported and explicitly not explained on 12 mornings. Report `docs/reviews/BATCH_191_COACHING_INTEGRITY_REFRESH.md` ranks **2 High / 5 Medium / 1 Low** (`CI191-01…08`) with `file:line` and real-data evidence and review-local remediation stubs; a Mark-safe scorecard ships at `docs/reviews/BATCH_191_MARK_SCORECARD.md`; no ledger batch numbers are allocated until the whole wave is triaged. **Grade: B+ held, for entirely different reasons** — closing CI191-01/02 is what moves it to A−. No verdict logic, threshold, ladder branch, prompt, prompt version, plan, proposal, migration, config or production row changed; every production query was read-only and the two orphaned proposals were deliberately left in place. Shipped via PR #222, squash `92453a7` (DECISIONS #272).
- [x] **Batch 192 — Full-app UX / live-app review shipped** — lane 5 and the last of the 2026-08-05 wave-2 five-lane audit, diagnose-only, walking every route against live production data through the local bundle proxied to the production API (the Batch 156 method), at 390×844 in light and dark, with browser-only response fixtures used only for the two surfaces production provably lacks — verified before being simulated, since `coach.brief_messages` holds zero `state_change` and zero `weekly_review` rows, ever. **Every route works:** 15 of 15 rendered substantive production content in both themes, with zero page-level horizontal overflow across 30 captures, zero script or console errors, zero API responses ≥ 400, no blank sheet and no infinite spinner. Batch 156's `UX156-01` is closed (Coach memory loads and renders) and `UX156-02` is closed (one device row, not two); dark mode is contrast-clean on all 15 routes; Batch 183's local-date separators and per-message times work exactly as specified; and Batch 185's unseen marker works for the origin it knows about. **The whole-app property this lane exists to judge does not hold, and three independent defects compound on one surface — the coach.** `CoachLauncher.tsx:126` passes `'fixed right-4 z-tabbar tap-target relative'` to `cn()`, and because `fixed` and `relative` are both Tailwind position utilities tailwind-merge discards `fixed`: the launcher computes `position: relative; left: -16px`, renders in normal flow at the end of the layout (`rect.y` 2176 on a 2300 px page in an 844 px viewport, `inViewport: false`) and is clipped 16 px off the left edge — introduced by Batch 185's own commit `368ef69`, which added `relative` to anchor the new unread dot, and live in the deployed production bundle at desktop width too. The thread then opens at `scrollTop: 0` of a 28,380 px pane in a 388 px window, so the newest turn — where every proactive message lands — is 27,992 px below the fold and the first thing shown is 14 days old, on a 60-message window already truncating a real 82-message history with no "load older" affordance. And a failed `GET /api/v1/coach/messages` renders as "Nothing here yet. Ask whatever's on your mind.", because the launcher collapses every non-success state to an empty list with no error branch — 82 messages of coaching history read as deleted, with an invitation to start over. Had the Sunday review fired on 2026-08-09 as designed, it would have arrived on a button the user cannot see, in the one part of the pane that is never on screen. Batch 189's `CR189-01` is confirmed at the UX layer by execution (a `weekly_review` fixture lights the dot and renames the control; an identical `state_change` fixture lights nothing), and a wave-1 finding re-opens past the batch meant to close it: 75 text nodes across 13 of 15 light-mode routes fail WCAG AA, including the active bottom-tab label at 3.55:1, because Batch 163 defined the correct `*-text` tokens but the decorative fill tokens are still used as text — and `lib/semanticTextContrast.test.ts` stays green because it checks nine tokens against `--surface` in a CSS file and never renders a page. Report `docs/reviews/BATCH_192_UX_LIVE_APP_REVIEW.md` ranks **3 High / 5 Medium / 2 Low** (`UX192-01…10`) with `file:line`, measured geometry and 44 committed screenshots plus three machine-readable records in `docs/reviews/batch-192/`; remediation stubs `R192-A…J` stay review-local placeholders and no ledger batch numbers are allocated until the whole wave is triaged. No product behaviour, styling, component, prompt, plan, migration, config or production row changed; every fixture, the simulated 503 and the held `POST` were fulfilled inside the browser and never reached the production write path or the model provider, and the single review credential was revoked and verified dead. Shipped via PR #223, squash `1f7507e` (DECISIONS #273).
- [x] **v3 Batch 19** — strength watching-brief shipped (PR #21, #80): advisory-only `GET /api/v1/strength-brief` + `strengthBrief` in the daily-loop envelope; `is_strength_activity` delegates to `exclude_from_recovery` (Batch 8/Decision #49); `compute_strength_rollup` is a pure function computing 4w/12w `WindowStats` (session count, duration, load proxy, sessions/week) and trend from first-vs-second-half session rates; `StrengthBriefService.brief` is read-only; recovery-isolation invariant (#49) explicitly tested (no verdict/recovery fields in result, flag never mutated). No migration, no LLM, no new cron
- [x] **v3 Batch 20** — weekly & monthly deep reviews shipped (PR #23, #81): deterministic `compute_review_rollup` (pure, DB-free) aggregates a period's sleep/recovery/load+adherence/morning-verdicts/thermal into reproducible averages, counts, by-type load and first-vs-second-half trends; `ReviewService` reuses the Batch 19 strength brief + Batch 17 insights and generates the narrative through the thin Anthropic Messages boundary (#47, fakeable), stored in `analyses` as `weekly_review` / `monthly_review`; calendar-aligned windows (ISO week / calendar month) make `run` idempotent per period; `GET /api/v1/reviews/{period}` previews and never writes (#71), `POST /…/run` generates + stores; `/reviews` PWA tab. No migration, no new cron
- [x] **v3 Batch 21** — year-on-year & seasonal trends shipped (PR #24, #82): deterministic `compute_trend_windows` (pure, DB-free) buckets daily history into comparable month/season windows (meteorological seasons; Dec → next year's winter) with per-metric count/mean/median/min/max over 9 metrics, honouring the SpO2/HRV reliability cutoff (#45) *in the aggregation* with an explicit `excludedCount` (#44 provenance); `compute_year_on_year` computes same-period-vs-prior-year deltas requiring ≥5 samples both sides, else degrades to `insufficient_history` (true YoY ~Mar 2027); `TrendsService` reads the rows; the optional narrative reuses the Batch 20 Anthropic boundary (`AnthropicReviewClient` gained a backward-compatible `system_prompt` override) and stores to `analyses` as `seasonal_trend`, idempotent per window, reporting insufficient history deterministically without calling the model; `GET /api/v1/trends/seasonal|year-on-year|narrative` preview and never write (#71), `POST /…/narrative/run` generates + stores; `/trends` PWA tab. No migration, no new cron
- [x] **Batch 57 — data truth in reviews/trends shipped** (PR #79, DECISIONS #130): extends the Batch 20/21 packet contract with explicit review coverage/sample counts, first-half → second-half trend evidence, and source-state flags for missing plan rows / untracked strength so narrative prompts cannot turn absent inputs into "zero sessions" or "strength stopped". Review/trend prompt versions move to v3 and require from→to numbers plus sample counts for trend claims. Reviews API/shared schema and `/reviews` expose the coverage/source caveats. No migration, no new endpoint, and no speculative prod backfill; production smoke verified Railway + Vercel same-origin health on squash `4e0497a`, web `/` and `/reviews` 200, and the review/trend read paths 401 unauthenticated direct and via Vercel.
- [x] **Batch 58 — sleep-stage age-comparison table shipped** (PR #80, DECISIONS #131): extends the existing morning-analysis `ageComparison` packet with a sibling `sleepRows` group so Home keeps the compact Batch 55 folded age descriptors while the `/sleep` hub gets the deeper read Mark asked for. `age_norms.py` now carries coarse age-band norms for sleep duration plus Deep/Light/REM/Awake stage mix and Restless count; stage comparisons use percentages of measured sleep rather than raw minutes so the comparison tracks sleep shape across different total durations. `morning_analysis._age_comparison` threads the synced sleep fields through, shared schemas parse the widened payload, and `/sleep` renders a dedicated "Sleep stages vs your age" table. No migration, no new endpoint, no verdict/prompt change. PR #80 merged as squash `9c96bb6`; a narrow follow-up formatting commit `080e763` restored the repo-wide `ruff format --check` gate on `main`, and production smoke verified Railway + Vercel same-origin health on final SHA `080e763`, web `/` and `/sleep` 200, and unauthenticated `GET /api/v1/daily-loop` 401 direct and via Vercel.
- [x] **Batch 59 — chronic-pattern suggestions shipped** (PR #81, squash `fb90e3e`, DECISIONS #132): adds deterministic, read-only chronic sleep suggestions to `/api/v1/daily-loop` as optional `chronicSuggestions`, using a 4-week evidence window, at least 21 observed nights, Batch 58 age norms for stage/duration misses, existing personal baseline bands for related recovery markers, and measured sleep drivers from `InsightsService.drivers()` to prioritise the action mapping. `ChronicSuggestionsCard` renders inside the shared `SleepSnapshotBody`, so Home and `/sleep` show the same insufficient-history/clear/active state and evidence/actions. No migration, no new endpoint, no `analyses` row, no LLM call, and no verdict/delivery-rule change. Production verified on merge SHA `fb90e3e`: Railway + Vercel same-origin `/api/v1/health` both returned that SHA, web `/` and `/sleep` returned 200, and unauthenticated `GET /api/v1/daily-loop` returned 401 direct and via Vercel.
- [x] **Batch 61 — age-adjusted sleep norms & real age-adjusted score shipped** (PR #84, squash `7936fcb`, DECISIONS #135): replaces the flat Garmin `+4` with a pure `services/sleep_scoring.py` recompute from stored Garmin sleep sub-scores, stage seconds, and profile age/sex. Sleep-stage age rows now show healthy bands plus nullable Garmin young-adult targets for contrast; Restless is descriptive-only. Morning analysis, DB-history baselines, reviews, and chronic patterns use the central scorer where profile context exists, while older stored rows remain a fallback. `PROMPT_VERSION` is `morning-analysis-v4-2026-07-06`; Garmin Poor readiness remains a cautious gate, verified against Mark's real nights before ship. No migration, no new endpoint, no historical backfill by default. Production verified on merge SHA `7936fcbb0cb7e48e7ff03f6eeadd81f81c2e3523`: Railway + Vercel same-origin health returned the SHA, web `/` and `/sleep` 200, and unauthenticated `GET /api/v1/daily-loop` 401 direct and via Vercel. **Batch 93 follow-up (PR `9ee1f5c`) keeps Restless fully honest to that contract:** the descriptive-only row still renders for context, but it no longer emits a fabricated nightly `ageAverage`; the shared/web payload now carries `ageAverage: null`, and the age-adjusted score remains unchanged because Restless still has no sleep-stage band.
- [x] **Batch 65 — Separate cycling & strength + correct Mon/Sat mapping shipped** (PR #89, squash `cc445ac`, DECISIONS #138): Plan No. 2 now keeps cycling and strength as separate plan rows. Mondays are the Dumbbell full-body session; build-week Saturdays are a ride (`bike_endurance`, "Z2 + Neuromuscular") plus a Bodyweight `strength_maintenance` row on the same date using version-as-slot (ride v1, strength v2); recovery/consolidation/taper Saturdays remain ride-only. `import_plan` assigns incrementing per-date versions and `swap_day` scopes target detection by workout category, so moving a ride never drags strength/flex rows and ride-for-ride swaps preserve same-day strength. No migration, no plan-editor UI, no bike-prescription rewrite, and verdict / #133 / #134 / #135 / Red-never-VO2 invariants are unchanged. Production closeout re-imported the plan forward from 2026-07-13 (13 blocks / 87 workouts), verified 65 bike + 22 strength rows through 2026-10-11, reconciled 65 bike sessions to Zwift, and smoke-tested Railway + Vercel same-origin health on the merge SHA.
- [x] **Batch 66 — Swap-first recovery guidance shipped** (PR #90, squash `73c2edf`, DECISIONS #139): on an Amber/Red morning with a hard bike session scheduled, the verdict leads with a concrete one-tap week swap (`plan_swap_first`, reusing the Batch 66/`weekly_restructure` spacing primitives) instead of only offering to soften the session; softening remains the fallback. New `coaching_protocol` KB section records the swap-first preference; `SwapSuggestionCard` fires the existing `swap_day` action. No migration, no new endpoint; Green/Amber/Red, #133, #134, #135, and Red-never-VO2 unchanged. A follow-up (PR #91, squash `6643e3a`) fixed `weekly_restructure.apply_for_week` to preserve a split day's strength row instead of dropping it. Production verified on the merge SHA.
- [x] **Batch 67 — Real structured bike workouts + harden delivery parsing shipped** (PR #92, squash `5652c24`, DECISIONS #140): every bike session in `plan_no2.json` was a single collapsed block, so delivery produced a flat 55%/154 W VO2 ride, Z2 graded at the top of its band (en-dash defeated band-averaging), and drifted durations. `_expand_step` (`workout_delivery.py`) gains a ramp raw-step form so warm-up/cool-down ramps are real ramp IR steps, not flat; `_power_pct` no longer silently falls back to 55% on an unresolvable target — it raises 422, and a new import-time validation pass (`validate_deliverable_bike_workout`) rejects an unresolvable/single-block/no-ramp/duration-mismatched bike workout before it reaches Zwift; the band regex accepts the unicode dash family and collapses a band to its midpoint (`"65–72%"` → 68%). All 65 `bike_*` workouts were re-authored from the source doc (`Full 13 Wk. 2121 Plan No. 2 Start 06.07.26.docx`) into multi-step `steps` with `duration_min` tracing the summed steps; day placement (Dumbbells Mon / Bodyweight Sat) and FTP 280 W are unchanged from Batch 65. No migration; verdict / #133 / #135 / Red-never-VO2 invariants untouched. Closeout re-imported the plan forward from the resume Monday (2026-07-13, 13 blocks / 87 workouts) and reconciled all 65 bike sessions to Zwift (VO2 spot-checked as a real warm-up ramp + 5×2 min @120% intervals + cool-down ramp, not a flat block); production verified on the merge SHA.
- [x] **Batch 68 — Grade what he actually rode shipped** (PR #93, squash `76307c7`, DECISIONS #141): post-workout analysis now selects the latest pushed delivery proposal IR for the planned ride (workout-id first, same-date legacy fallback) before falling back to the planned row, so an accepted Amber/Red/manual override is graded as the workout actually delivered to Zwift. The packet carries `gradingTarget` and `workoutAdherence`/`changeSummary`; `PROMPT_VERSION` is `post-workout-analysis-v4-2026-07-09`. No migration; interval grading maths, whole-ride context guardrails, verdict / #133 / #135 / Red-never-VO2 unchanged. CI passed in both branch-push and PR-context waves; production smoke passed on Railway + Vercel same-origin health (`sha=76307c7`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel.
- [x] **Batch 69 — Accept = logged; move session-logging post-ride shipped** (PR #94, squash `1288f85`, DECISIONS #142): approving an eased/substituted ride now seeds `PlannedWorkoutAdherence` idempotently (`status='modified'`, accepted-adjustment `type`/`intensity`/`changeSummary` in `actualWorkoutJson`, `source='accepted_adjustment'`) so the app logs its own accepted change instead of asking Mark to re-type it. `/check-in` no longer asks "How did your sessions go?"; that logger moved onto the Batch 60 completed-ride Today-card flow as one combined post-ride form (Outcome, Actual minutes, and when changed: what he did instead / target intensity / what changed) saving both the ride check-in and the adherence together, feeding the Batch 68 grading read. CI caught two case/ordering bugs before merge (fixed in the same PR): `_accepted_adjustment_summary` compared a normalized `"Amber"/"Red"` verdict against lowercase literals, and `_accepted_adjustment_target` preferred a generic power-cap message over the more informative zone/duration text. No migration; verdict / #133 / #135 / Red-never-VO2 unchanged; unplanned rides keep the standalone post-ride check-in. Production verified on the merge SHA.
- [x] **Batch 70 — Weekly-mix maintenance & dynamic rebalancing shipped** (PR #95, squash `7598b6f`, DECISIONS #143): the morning verdict now tracks Mark's weekly quality mix (VO2×1 / Sweet-Spot×1 / Zone-2×3, derived from his own plan — a recovery week reads `target=0`) as a **soft, readiness-gated quota**. New pure `services/weekly_mix.py` (`summarize_weekly_mix`) reports per bucket done/due/at-risk over the week's planned + completed bike rows (`PlannedWorkout.status=='completed'`; today's eased hard session stops counting as a scheduled slot). When an Amber/Red morning eases today's hard bike session the verdict either **re-patches** it to a sound later day this week — reusing the Batch 66 `plan_swap_first` swap engine, now with an opt-in `PROTECTED_WEEKDAYS={Mon,Fri}` guard so a hard bike never lands on Mark's Dumbbells/coffee days — or states plainly **"no VO2 this week — readiness gets the veto"**, appended to `planAdjustments`. `verdict.weeklyMix` is always attached (so the week view shows the mix even on Green), surfaced via `AnalysisOut.weeklyMix` + shared `weeklyMixSchema`; a reusable `WeeklyMixCard` renders done/target chips on the Home Today card and the `/delivery` Plan page. `PROMPT_VERSION` → `morning-analysis-v7-2026-07-09`. Advisory only — nothing auto-schedules; no migration (derived read-only accounting); Green/Amber/Red / #133 / #135 / Red-never-VO2 and the ≥2-day no-stack rule untouched; empty-day *insertion* of a make-up session is out of scope (needs Batch 71's editable quick-add) — the re-patch only trades with an existing easier bike day. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `7598b6f`, web `/` + `/delivery` 200, auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel). Batch 213 / DECISIONS #291 closes a double-counting defect: a `swap_day` supersede gap could leave a superseded `skipped` `PlannedWorkout` row `is_active=true` alongside its replacement, and `summarize_weekly_mix` counted active rows rather than sessions, so a bucket's `target` could read one higher than the plan actually carries. `summarize_weekly_mix` now dedupes to one session per `workout_date` (the highest-version non-skipped row; a date where every row is skipped contributes nothing to `target`), and `swap_day` deactivates every active occupant on both the source and target dates rather than only the two rows chosen as swap partners.
- [x] **Batch 71 — Editable quick-add on the Week tab shipped** (PR #96, squash `b37e01d`, DECISIONS #144): tapping "+ Cycle / + Weights / + Flexibility" on `/delivery` used to always drop a hardcoded 45-min Zone-2 ride with no picker and no edit. A new per-category subtype catalog (`QUICK_ADD_CATALOG` in `services/plan_actions.py`) — cycle: endurance/sweet_spot/recovery; weights: maintenance/recovery; flexibility: mobility only — each carries a title, workout type, intensity target, default duration, and a `[min,max]` duration bound; `GET /api/v1/plan-actions/quick-add-options?category=` lists it, and `add_workout` / `POST /days/{date}/workouts` accept optional `subtype` + `durationMin` (`workout_for_selection` validates the duration against the chosen bound — 422 outside range — and sizes the bike warm-up/main/cool-down steps to sum to the chosen duration); omitting both preserves the exact prior hardcoded-default behaviour, so no existing caller regressed. A new `QuickAddSheet` (same `Sheet` pattern as `MoveWorkoutSheet`) replaces the instant-add buttons — tap a category, pick a subtype, edit the duration within its bound, submit. No migration; existing move/skip/swap flows unchanged; no per-interval workout editor (out of scope — the manual-override intensity dial + Batch 67's real structured workouts already cover that need). CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `b37e01d`, web `/` + `/delivery` 200, the new auth-gated `quick-add-options` endpoint 401 direct and via Vercel).
- [x] **Batch 72 — Chronic-REM coaching depth shipped** (PR #97, squash `92019ed`, DECISIONS #145): a persistent REM miss on the Batch 59 chronic-pattern card used to repeat the same two static lines. It now hands out a **focused window of two** from a new **ordered library of 12 REM-specific levers** (`services/rem_interventions.py`) — grounded in the sleep-protocol values (`bedtime`/`sealTargetTime`/`preCoolTemperatureC`/`coherenceBreathingTime`/`latestSnackTime`) or REM physiology — rotated deterministically from the ISO-Monday of the day (stable Mon–Sun, walking the whole library before repeating, a 6-week cycle) with **no persisted cursor and no migration**; a measured sleep driver biases the week toward its lever (thermal → cool-room, load → late-ride, stress → wind-down/offload). Wired into the `rem_sleep_pct` branch of `chronic_patterns._actions_for` only; `ChronicSuggestion` + shared `chronicSuggestionItemSchema` gain an optional `rotation` ({`periodLabel`,`shown`,`total`}); the shared `ChronicSuggestionsCard` shows a "Rotating focus — N of M levers this week, a fresh set next week" caption on Home and `/sleep`. No migration, no new endpoint, no Anthropic-boundary call (deterministic, fakeable without `ANTHROPIC_API_KEY`); Green/Amber/Red / #133 / #135 / Red-never-VO2 untouched (the Batch 59 read-only contract); no non-REM chronic suggestion changes (regression-guarded). CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `92019ed`, web `/` + `/sleep` 200, auth-gated `GET /api/v1/daily-loop` — now carrying `rotation` — 401 direct and via Vercel).
- [x] **Batch 73 — Editable quick-add on the Home Today card + legible edit-dial shipped** (PR #98, squash `72f92a8`, DECISIONS #146): Home now reuses the Batch 71 quick-add picker instead of instant-adding a fixed default — the Today-card `+ Cycle / + Weights / + Flexibility` buttons open the shared `QuickAddSheet`, fetch `quick-add-options`, and post `{category, subtype, durationMin}` through the existing `POST /api/v1/plan-actions/days/{date}/workouts` seam, so a 60-minute endurance/Z2 ride is one add on Home as well as Week. The existing workout edit dial stays a modest override seam (Duration 50-125, Intensity 50-120), but now shows those allowed ranges inline, clamps back to them on blur/submit, and benefits from a widened `apiFetch` detail formatter so FastAPI array-`detail` validation payloads render as useful messages instead of a generic `API error 422`. No migration, no backend contract change, and no change to workout-delivery adjustment math or the same-day override bounds; existing add/edit/swap/skip flows remain intact. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `72f92a8`, web `/` 200, auth-gated `GET /api/v1/plan-actions/quick-add-options?category=cycle` 401 direct and via Vercel).
- [x] **Batch 74 — Home honours a skipped session + Remove for added workouts shipped** (PR #99, squash `427074e`, DECISIONS #147): Home now uses the same active-workout filter as Week — `daily_loop._planned_workouts` excludes `status='skipped'` — so a skipped session leaves the Today card and stays gone after reload. User-added workouts (`source='plan_action_add'`) now have a dedicated remove path instead of overloading Skip: `ExecutableCoachingService.remove_workout(...)` deletes any live intervals.icu event through the existing delivery rail, sets `is_active=false`, and records a `workout_removed` audit row, while coach-planned sessions keep the unchanged `status='skipped'` adherence semantics and audit trail. `DashboardPage.tsx` keys off `workout.source` and gives an added workout a first-class **Remove** action on Home, with planned workouts keeping Skip. No migration, no scheduler change, and no delivery-rail broadening beyond reusing the existing delete path. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `427074e`, web `/` 200, and auth-gated `POST /api/v1/workout-delivery/planned-workouts/{id}/remove` 401 direct and via Vercel).
- [x] **Batch 77 — Structured workout builder + step-level editor shipped** (PR #102, squash `80ff8ba`, DECISIONS #150): Mark's spreadsheet builder (`Workout Structure for The App 10.07.26.xlsx`) now maps into the existing Batch 67 bike step grammar instead of a parallel workout model. A pure builder creates warm-up/cool-down ramps, optional Z2 lead-in, repeated interval pairs or a steady block, traced total duration, and a `delivery` flag; Week can add a custom bike workout (`customBike`) or structurally edit an existing bike row by versioning it on the same date. Indoor rides reconcile through the existing intervals.icu/Zwift rail; outdoor rides are stored with `delivery="outdoor"` and deliberately skipped by Zwift until Batch 78 owns Garmin/bike-computer delivery. No migration; no arbitrary graph editor; verdict / #133 / #135 / Red-never-VO2 unchanged. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `80ff8ba`, web `/` + `/delivery` 200, and auth-gated structured-edit endpoint 401 direct and via Vercel).
- [x] **Batch 78 — Indoor/Outdoor delivery: outdoor rides to Garmin Connect shipped** (PR #103, squash `eb4d085`, DECISIONS #151): an outdoor ride now delivers to **Garmin Connect** (→ Mark's Edge) from the same Batch 67 IR the app builds for Zwift, while indoor rides keep the intervals.icu/Zwift rail unchanged. Two `/batch-start` calls: **direct Garmin upload** via `garminconnect`'s `upload_workout`/`schedule_workout` on the existing garth session (not intervals.icu's own Garmin sync) — synchronous, honest, isolated; and **a new `garmin_workout_deliveries` table** (migration `014`) rather than reusing `workout_delivery_proposals`, so the Garmin write path never touches the live Zwift-rail queries. Pure `services/garmin_workout_export.py::build_garmin_workout(ir, ftp_watts)` maps the IR → Garmin cycling-workout JSON (`power.zone` targets in watts from FTP; ramp→range, steady→band). Async `GarminWorkoutDeliveryService` uploads+schedules via `asyncio.to_thread`, keyed by user+date (survives Batch 77 re-versioning), idempotent per version, honest on failure (`failed` + `last_error`, retried, never silently dropped — #97), delete-first replace on edit. `ExecutableCoachingService.reconcile_deliveries` also reconciles outdoor rides to Garmin, isolated per workout — Garmin is only touched when the window holds an outdoor ride, so the indoor-only path is unchanged. The Week tab surfaces the delivery status (Sent to Garmin / failed). Garmin stays ingestion-direct (#27); verdict / #133 / #135 / Red-never-VO2 unchanged. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `eb4d085`, web `/` + `/delivery` 200, auth-gated `GET /api/v1/plan-actions/schedule` 401 direct and via Vercel). **Deferred (live-use):** the Garmin device spot-check — an outdoor ride actually appearing on Mark's calendar/Edge with its targets.
- [x] **Batch 79 — Per-item Skip/Remove on the Week tab shipped** (PR #104, squash `12104ad`, DECISIONS #152): the Week tab's whole-day "Skip day" used to be the only way to drop a single workout — the exact trap Mark hit wanting to remove one added ride. Frontend-only: `WeekAheadPage.tsx`'s `WorkoutRow` now offers per-item **Skip** (any non-completed session) and **Remove** (only `source='plan_action_add'` sessions), reusing the exact Batch 74 endpoints Home already calls; whole-day action stays but is relabelled **Skip whole day**, no longer the only control. No migration; no backend change; verdict / #133 / #135 unchanged. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `12104ad`, web `/` + `/delivery` 200, auth-gated `POST /api/v1/workout-delivery/planned-workouts/{id}/skip` and `.../remove` 401 direct and via Vercel).
- [x] **Batch 80 — "Was overriding the plan the right call?" deviation verdict shipped** (PR #105, squash `1598d12`, DECISIONS #153): when a completed ride diverged from the day's planned/delivered session, the post-workout read (`post_workout_analysis.py`) now renders an explicit, honest good-call/bad-call verdict — Mark's Q1a — grounded in the objective grade, never sycophantic. Backend-only: a pure `detect_ride_deviation(...)` runs in `assemble_context_packet` and attaches a deterministic `rideDeviation` block (reusing the Batch 68 delivered-vs-actual grade in `execution` as the anchor); it flags a divergence only when a bike session was actually planned/delivered and he didn't do it as prescribed — `skipped_and_rode` (bike workout `status='skipped'` but a ride completed) or `easier_than_planned`/`harder_than_planned` (every gradable work interval off target in the same direction; a real attempt lands ≥1 on target, so a fade is not flagged) — and never flags an approved coach-adjustment (`wasApprovedAdjustment`, Batch 68/69) or a no-plan `free_ride`. The packet carries the `kind`/`reason`, objective `signals`, planned-vs-actual summary, and `morningReadiness`, so the judgement is the prompt's; `SYSTEM_PROMPT` gains the deviation instruction (never soften a poor call into praise) and `PROMPT_VERSION` → `post-workout-analysis-v5-2026-07-11` (regenerates older reads). The verdict flows through the existing `output_markdown`, so no frontend/shared change, no migration; verdict / #133 / #135 / Red-never-VO2 unchanged. CI green in both push and PR-context waves; production verified on the merge SHA (Railway + Vercel same-origin health `1598d12`, web `/` + `/delivery` 200, auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel). **Deferred (live-use):** the deviation copy is a phone eyeball on the next genuinely diverged ride.
- [x] **v3 Batch 22** — hypothesis evaluation shipped (PR #25, #83): deterministic, advisory per-experiment evaluator (`services/experiment_evaluation.py`) reusing the Batch 17 slope/Pearson math; dispatches on the experiment `slug` to three pure DB-free evaluators — **gate** (collagen: consecutive age-adjusted-74+ night streak → gate met = `supported`), **correlation** (early_waking_0400: Pearson-rank overnight low °C / sleep-stress vs an `awake_sleep_sec` disruption proxy → strong = `supported`, none = `refuted`; alcohol/late-snack surfaced as an unmeasured-coverage gap), **group_compare** (recovery_week_disruption: recovery- vs build-week mean age-adjusted sleep from `plan_blocks.block_type` → recovery worse = `supported`) — each skipping below its #71 sample gate; maps to a `supported`/`refuted`/`inconclusive` **recommendation only** that never changes status (concluding stays the human-gated terminal `POST /…/status` action, #72); `GET /api/v1/experiments/{id}/evaluate` previews and never writes, `POST /…/evaluate/run` records an `experiment_evaluation` audit row in `analyses` idempotent per (experiment, subject date); new `/experiments` PWA tab evaluates each hypothesis and accepts a recommendation through the existing conclude path. No migration, no LLM, no new cron
- [x] **v3 Batch 23** — auto-generated handover-doc export shipped (PR #26, #84, the #13 capstone): deterministic `build_handover_packet` (pure, DB-free) composes the full retained state — KB (the six known sections in hand-doc order), current plan/block + upcoming workouts, `metric_baselines`, recent weekly/monthly reviews (Batch 20), seasonal year-on-year (Batch 21, reused), experiments + their latest deterministic evaluation (Batch 22, reused), strength brief (Batch 19) — with the data-quality rules echoed under `dataQualityGuardrails`; `render_handover_markdown` (pure, no model) renders the portable markdown handover doc so the export always works and faithfully reflects current state (the round-trip guarantee), surfacing L/R balance only as a rule; `HandoverService.run` polishes it through the Batch 20 Anthropic boundary (handover-specific system prompt), stored in `analyses` as `handover_export`, idempotent per day, fakeable without `ANTHROPIC_API_KEY`; `GET /api/v1/handover` previews + never writes (experiments listed with `seed=False`), `POST /…/run` generates + stores, `GET /…/export` downloads the deterministic markdown as a `text/markdown` attachment; `/handover` PWA tab with deterministic preview + client-side download + generate-narrative. **This was the final v3 batch — the v1→v3 roadmap is now complete.** No migration, no new cron
- [x] **Batch 90 — Post-mobility read accuracy shipped** (PR #115, squash `43d4507`, DECISIONS #163): the mobility packet now supplies authoritative `subjectWeekday`, a 14-day actual-plan horizon, overlapping holiday windows, and per-workout `insideHolidayWindow`/`isLive`, so a holiday-skipped VO2 session cannot be cited as live. `mobilityBaseline` frames Mark's observed daily mobility as his baseline habit (cycling `weeklyRhythm` is not a mobility budget), and the prompt keeps its one light next step within mobility while preserving `advisoryOnly`/`neverFeedsRecoveryDecision`. The packet also gains `trainingSchedule`; the dead `analysis_rules` lookup is repaired across mobility/walk/strength/ride using the seeded data-quality + coaching-protocol sections, and authoritative weekdays are supplied across morning and all post-session reads. Prompt versions bumped. No migration, KB re-seed, frontend/shared change, verdict/delivery change, or #133/#135/Red-never-VO2 change. Production verified on the merge SHA; the next real post-mobility wording eyeball remains live-use verification.
- [x] **Batch 97 — Passive brief generation: notify-when-ready + staged loader shipped** (`0310cd6` + docs closeout, DECISIONS #170): the morning check-in no longer blocks on the LLM. `PUT /api/v1/daily-loop/{date}/manual-entry` still saves through the existing manual-entry seam, but for **today** it now queues a FastAPI `BackgroundTasks` job that reuses `ExecutableCoachingService.regenerate_after_morning_checkin(...)` off-request, then sends a one-per-day `brief-ready-{date}` push targeting `/brief` through the existing `NudgeAlertService` transport. `CheckInPage.tsx` swaps the blocking spinner for an immediate acknowledgement plus a timed three-stage waiting card (syncing overnight data → reading morning → writing brief), polls the normal daily-loop snapshot as the no-push fallback, and flips to **View brief** once the brief lands. No migration, no new API shape, no verdict / #133 / #135 / Red-never-VO2 change; Batch 85's downgrade-only / never-touch-an-approved-ride guardrails are preserved because the background task still calls the same regeneration service. GitHub Actions run `29210486906` passed all 7 jobs; production verified on implementation SHA `0310cd6` on Railway + Vercel same-origin health, web `/check-in` + `/brief` 200, and unauthenticated daily-loop 401 via Vercel. The real ready-push-on-device proof remains live-use verification, and Batch 99 is still the focused-client iOS deep-link follow-on.
- [x] **Batch 98 — Rest-day / holiday-aware morning verdict shipped** (PR #120, squash `a0e133d`, DECISIONS #171): the morning packet now exposes deterministic `restDay` context from `HolidayPauseService.get_windows(...)`, with a non-empty all-`skipped` plan as the fallback. The existing Green/Amber/Red floors still classify recovery, but rest days are explicitly `dayType=rest`; skipped/paused sessions are excluded from live VO2 and cannot trigger proceed/soften guidance, swap-first, eased-ride actions, hard-prior-day easing copy, or weekly-mix shortfall/re-patch prose. `SYSTEM_PROMPT` frames the colour as recovery on a rest day and bans narrating a skipped/holiday session as live; `PROMPT_VERSION` → `morning-analysis-v13-2026-07-12`. Backend packet/prompt only; no migration, frontend/shared change, or #133/#135/Red-never-VO2 change. Both final CI waves passed all seven jobs (backend 813/813) plus Vercel preview; production verified on the merge SHA via Railway + Vercel same-origin health, web `/` + `/brief` 200, and unauthenticated daily-loop 401 direct/proxied. The next genuine rest-day wording check remains deferred to live use.
- [x] **Batch 99 — iOS notification deep-link shipped** (PR #121, squash `5f2e02e`, DECISIONS #172): warm notification taps no longer depend on `WindowClient.navigate()`, which is a no-op on iOS PWAs. `sw.ts` now normalizes the payload URL and `postMessage`s it to the focused client through a shared helper, and a new app-shell `PushNavigationController` inside `BrowserRouter` listens on `navigator.serviceWorker.message` and routes the target through `react-router`; cold-start taps still use `openWindow(...)`. Existing payload builders already carried the intended deep-links and remain unchanged (`/check-in` for good-morning, `/brief` for brief-ready, `/` for verdict). Frontend + push-payload only; no migration, backend endpoint change, or verdict / #133 / #135 / Red-never-VO2 change. GitHub CI passed both push and PR-context waves plus the Vercel preview; production verified on the merge SHA via Railway + Vercel same-origin health, web `/` + `/check-in` 200, and unauthenticated same-origin daily-loop 401. The real handset tap with the PWA already open remains a live-use verification.
- [x] **Batch 104 — Right-surface placement shipped** (PR #125, squash `0b4027e`, DECISIONS #177): Climate is now a live-only room/fan surface, so the retrospective overnight chart no longer renders there and instead lives only on Sleep's Last night view, with Climate keeping a lightweight handoff back to Sleep. The rolling walking-base and breathwork summaries are no longer embedded in the Today workout card; Trends now carries a compact walking-base strip, while Sleep Tonight carries the breathwork rhythm context next to the sleep projection. Frontend-only, no API/shared-schema/migration or verdict change. Production verified on the merge SHA via Railway + Vercel same-origin health, web `/` 200, and unauthenticated same-origin daily-loop 401.
- [x] **Batch 125 — Resolve the Sleep ⇄ Climate overlap shipped** (PR #151, squash `5582957`, DECISIONS #203): keeps the Batch 104 split rather than folding Climate back into Sleep. Climate remains the canonical live room + fan-control surface and stays a primary tab; Sleep keeps the retrospective overnight story and wind-down context. The duplicated 4-stat bedroom block is removed from Sleep Tonight, replaced with one concise live summary plus a direct handoff to Climate, while Climate's own copy now makes clear that it owns the live read and Sleep owns the overnight chart. Frontend-only, no API/shared-schema/migration or verdict change; production verified on the merge SHA via Railway + Vercel same-origin health, web `/`, `/sleep`, and `/environment` 200, and unauthenticated same-origin daily-loop 401.
- [x] **Batch 126 — Right-size Week for a daily glance shipped** (PR #152, squash `d4c1a0b`, DECISIONS #204): keeps Week as a primary read-first surface for Mark while moving the organiser one layer down for Craig-as-admin. `WeekAheadPage` now matches the nav label with a **Week** header, leads with compact **This week** and **Coming up** summaries plus per-day rows and done/to-do totals, and hides the existing move/add/build/skip/remove/reset/restructure rail behind an explicit admin-only **Edit week** tab. Frontend-only, no API/shared-schema/migration or verdict change; production verified on the merge SHA via Railway + Vercel same-origin health, web `/` and `/delivery` 200, and unauthenticated same-origin plan-actions schedule 401.
- [x] **Batch 133 — Week shows what he actually did shipped** (PR #160, squash `32d9f6d`, DECISIONS #212): Week's payload boundary now includes a read-only per-day `activities` array on both the plan-schedule and week-ahead shapes (`activityKind`, `name`, `durationMin`, `startUtc`), sourced from already-synced `Activity` rows with no new ingestion or migration. `PlanActionService.schedule(...)` and `WorkoutDeliveryService.list_week_ahead(...)` classify those rows into the same four kinds the rest of the app uses, and suppress a same-kind activity when a completed planned workout already represents that day/category, so Week truth does not double-count a completed planned ride as both a done row and a `+ Ride` chip. `WeekAheadPage` renders the surviving unplanned activity as a lightweight muted kind+duration chip on the day in both the read-first Week glance and the admin edit view, so a walked holiday/rest day no longer reads as blank. Advisory/read-only only; no verdict/delivery/scheduler change. Production verified on the merge SHA via Railway + Vercel same-origin health, web `/` 200, and unauthenticated same-origin `GET /api/v1/plan-actions/schedule?days=7` 401 direct and proxied.
- [x] **Batch 134 — Climate/bedroom subsystem resumes on the last holiday day shipped** (PR #161, squash `709e72e`, DECISIONS #213): the holiday seam now deliberately carries two truths instead of overloading one. Inclusive `holiday.isActive` still describes the holiday day and the away night just gone, while new end-exclusive `holiday.awayTonight` describes whether tonight's bedroom/climate subsystem should stay dormant. `holiday_pause.py` adds `holiday_windows_away_overnight(...)` / `overnight_away_window_for_date(...)`, `DailyLoopService` computes both windows, `HolidayStateOut` plus the shared schema document `awayTonight`, and the overnight gates (`run_fan_control`, the thermal half of `run_evening_monitoring_alerts`, `run_evening_sleep_nudge`) now resume on `end_date` while remaining dormant on `end_date-1`. Climate, Home Tonight/Bedroom, Sleep Tonight, and the `protect-sleep` suppression now key off `awayTonight`; Sleep Last night/history, the morning thermal review gate, and holiday rest-day semantics intentionally stay on inclusive `isActive`. No migration, no change to plan-rest semantics, and no Green/Amber/Red / #133 / #135 / Red-never-VO2 change. Production verified on the merge SHA via Railway + Vercel same-origin health, web `/`, `/environment`, and `/sleep` 200, and unauthenticated `GET /api/v1/daily-loop` 401 direct and proxied.
- [x] **Batch 105 — Holiday-aware environment shipped** (PR #126, squash `08187fe`, DECISIONS #178): an unresumed holiday window covering the profile-local date is the private app's away signal. Evening sleep nudges skip that profile, monitoring keeps the independent Garmin/Hive/weather freshness guardian but disables its thermal branch, and the overnight fan pass exits before temperature reads, Dreo I/O, or `fan_state_readings` persistence. Batch 98 retrospective packets and the active-only scheduler guard share one date-window seam; early resume restores normal environment behavior immediately. Backend-only, no migration/API/shared/frontend or threshold/verdict change. Both final CI waves and post-merge `main` CI passed all 7 jobs; production health matched the merge SHA, the first live fan pass logged `holiday_away` with no tick, and the scheduled thermal branch skipped without a new alert. The implementation landed after the 20:00 sleep-nudge slot, so the next genuine holiday-evening fire remains live-use verification rather than a manufactured production action.
- [x] **Batch 109 — Holiday-aware Home & Sleep surfaces shipped** (PR #130, squash `443d19b`, DECISIONS #182): Batch 105's away-state now reaches the frontend through a minimal `/api/v1/daily-loop` `holiday { isActive, activeWindow }` payload, and the same flag keeps the evening bedroom-prep surfaces honest while Mark is away. Home's `Tonight` and `Bedroom` sections stay visible but go dormant with a Holiday handoff + resume date during an active window, Sleep's `Tonight` tab collapses to the same away-state instead of rendering prep/climate guidance, and the Home evening `Protect tonight's sleep` CTA is suppressed while away so the action strip cannot contradict the dormant surfaces. Frontend plus payload flag only, no migration or scheduler/verdict/fan-threshold change. Both final CI waves passed all 7 jobs plus the Vercel preview; production verified on Railway + Vercel same-origin health at the merge SHA, web `/` + `/sleep` + `/holiday` 200, and unauthenticated `/api/v1/daily-loop` 401 direct and via Vercel. The authenticated holiday-date UI eyeball remains deferred to live use because the protected preview review could only be taken as far as the login route without a live session.
- [x] **Post-roadmap — world-class UI/UX redesign shipped** (PR #35, squash merge `dea04da`, DECISIONS #90): a full screen-by-screen redesign of the PWA so it looks professional, is simple for Mark, and visibly serves his original requirements. A real markdown renderer (`components/Markdown.tsx`) so the AI verdict's **bold headlines** + tables render; the **Metrics-vs-Baselines table** (§4) surfaced from the already-computed `context_packet.metricsVsBaselines` (additive `daily_loop.py` serializer + shared schema, no migration) and rendered with ✔/⚠; bottom nav cut 10→4 tabs (Home/Plan/Trends/More) + desktop nav fixed; Home rebuilt read-first with the check-in split to `/check-in`; Plan shows the whole week incl. strength (today lit by verdict); Trends got recharts charts; every screen de-jargoned. Presentation-layer only — no backend coaching logic, no new endpoints, no migration.
- [x] **Post-roadmap daily-flow loop Batches 24-26 shipped** (DECISIONS #90-92): Home now follows the day phase from data state, same-day workout approval can deliver or manually override the sleep-adjusted session through the existing intervals.icu rail, and the post-ride card captures Mark's RPE / legs / feel / niggles as an activity-linked `manual_entries` row. Post-workout analysis context includes that `postRideCheckIn` and regenerates when the latest stored analysis predates the subjective input, while morning verdict reads stay isolated to date-level manual entries.
- [x] **Batch 28 — age-comparison axis + merged last-night table shipped** (PR #42, squash `0738c2a`, DECISIONS #98): shows Mark, on Home, how last night sits against both his own normal **and** the typical person his age — one glanceable card, no new Garmin call, no migration. `services/age_norms.py` encodes static sex×decade population norms for VO₂max / RHR / overnight HRV (privacy-safe, ~50th-pct guides) and `build_age_comparison` classifies each metric **direction-aware** (`good`/`neutral`/`warn`; a RHR *below* average reads green). Garmin fitness age is read from the already-synced `daily_metrics.raw_payload['max_metrics_vo2'][0].generic.fitnessAge` (computed server-side, no longer surfaced directly since the merged-table design made a standalone banner redundant). The age axis is merged with the existing `metricsVsBaselines` read into a single `MetricComparisonTable` embedded inside the 'Last night's sleep' card, replacing the REM/Deep/SpO₂ stat grid; comparison columns are difference-forward (verdict + numeric delta, `in range` / `N above/below`; `—` where a frame is absent). `AgeComparisonCard` deleted. Frontend-only after the backend additions; Green/Amber/Red verdict untouched. *(Batch 35 later folded the baseline range into this table as a muted sub-line, tinted last night's value by band, dropped the separate 'vs your normal' column, and retired the standalone `/baselines` page + `MetricsBaselineTable`.)*
- [x] **Batch 27 — bedroom fan control (Dreo air-circulator) shipped** (PR #41, squash `9f09e52`, DECISIONS #95-97): closes the loop from the Hive thermal read to a physical response. A server-side `DreoFanClient` (`pydreo_community`, mirrors the Hive boundary — env creds + cached-token-with-password-fallback, secret-safe; the **mode-before-speed** rule #95) drives Mark's `DR-HPF008S`; a pure decision core (`services/fan_control.py`: `loop_phase` + `decide_fan_action`) maps the live indoor temp onto a bounded fan target against the Batch 9 sleep-disruption thresholds (on 19.5 / off 19.0 hysteresis; ladder 19.5→3 / 20→5 / 21→7, capped 7), and `scheduler.run_fan_control` (15-min interval; overnight 21:30–08:30 + an 08:30–09:00 wind-down off; idle/no-cloud by day) reconciles only the difference — idempotent, degrading gracefully when creds/cloud/temp are absent (#96). A `Profile.fan_auto_enabled` master switch (migration `010`, default true) gates the loop; `routers/fan.py` adds `PUT /api/v1/fan/auto` (preference) + `POST /api/v1/fan/command` (manual drive, takes control = auto-off **only on success**). **Batch 100 extends this to multi-fan truth without changing the overnight control contract** (PR #122, merge `0e60d5f`): `thermalState.fans` is now a per-fan list keyed by Dreo serial + device label/model, manual commands target a specific `fanId`, and the auto-target fan stays pinned to `DREO_DEVICE_SN` (or the first controllable fan when unset) even if another fan is manually selected. The daily-loop fan read is now a **best-effort live Dreo inventory/state merge**: the auto-target fan still carries the existing computed overnight intent (`describe_fan_intent`, including the concrete next-on time), while the rest of the list carries live Dreo state (speed, preset mode, oscillation); if Dreo is unavailable the payload falls back to the old single synthetic bedroom-fan intent so Home/Sleep still load. Production closeout verified Railway + Vercel same-origin health on the merge SHA, web `/` + `/sleep` 200, auth-gated daily-loop 401 direct/proxied, and a live Dreo-account eyeball of 3 controllable fans with one pinned auto-target. Needs `DREO_USERNAME`/`DREO_PASSWORD` in Railway to actuate.
- [x] **Batches 101 + 102 — Climate tab and presentation polish shipped** (PR #123, squash `1bd2ac6`, DECISIONS #174-175): the room/fan surface now has a dedicated top-level `Climate` tab at `/environment`, with legacy `/bedroom` retired into it, while Sleep keeps only the concise thermal summary plus an `Open Climate` handoff and Home's compact bedroom detail also routes there. On top of that, Home's top lockup now uses a coach-like personal status line instead of a bare greeting, and the shared `SleepPrepBody` says what Tonight is based on so far — personalized training/driver/room evidence when present, or an explicit fallback note when the app is still on the default routine. Frontend-only, no migration or backend endpoint change. Production closeout verified Railway + Vercel same-origin health on the merge SHA, web `/environment` + `/sleep` 200, and auth-gated daily-loop 401 direct/proxied.
- [x] **Batch 30 — Home day controls + rearrangeable week plan shipped** (PR #45, squash `263460f`, DECISIONS #100): adds the `plan-actions` schedule/action boundary over real `planned_workouts`, day labels for cycle/weights/flexibility/rest/mixed, Home day-level add/skip/actual controls while preserving per-workout Today-card actions, and a live rearrangeable Plan page with add/move/skip/rest-day swap-in controls. Production smoke verified the implementation SHA on Railway + Vercel same-origin health, web `/`, and the new plan-actions route auth gate.
- [x] **Batch 31 — Overnight temperature × fan × sleep chart shipped** (PR #46, squash `d13d05c`, DECISIONS #101): makes the Batch 27 fan autopilot legible. New `fan_state_readings` time series (migration `011`) writes one idempotent tick per overnight `run_fan_control` fire across every branch — `apply`/`hold`/`auto_off`/`no_data`/`unreachable`/`winddown` — so chart gaps are explained, not blank; the fan decision logic itself is untouched. A pure `services/bedroom_overnight.py` + `routers/bedroom.py` `GET /api/v1/bedroom/overnight` is a read-only night-window join of temperature + fan + the night's sleep hypnogram (from `sleep.raw_payload['sleepLevels']`), defaulting to the last completed night with a pager, kept off `/api/v1/daily-loop`. `/bedroom` gets a recharts dual-axis `BedroomOvernightChart` (temp line + fan-speed step, 19.5/20.0 °C threshold lines, sleep overlay, muted degraded spans, night pager, empty state) plus a one-line Home glance. Production smoke verified the implementation SHA on Railway + Vercel same-origin health, web `/`, and the new bedroom-overnight route auth gate.
- [x] **Batch 32 — Plan page tap-to-move day picker shipped** (PR #48, squash `5d7804f`, DECISIONS #103): replaces the Plan page's fragmented relocation UI (three forward-only `Move <date>` buttons plus a separate rest-day swap-in panel) with one reusable mobile-first `MoveWorkoutSheet` bottom sheet per workout. The visible plan window widens 7→14 days so sessions can move into next week without a separate calendar; the picker lists each visible day with weekday/date, current contents, a Today marker, and the current day disabled, then calls the existing workout-delivery `swap` route so move-vs-swap still resolves server-side. Frontend-only: no new endpoint, no migration. Production smoke verified the merge SHA on Railway + Vercel same-origin health, web `/`, and an auth-gated `swap` route `401` direct and via the Vercel rewrite.
- [x] **Batch 33 — Bedroom overnight temperature verdict shipped** (PR #49, squash `8389985`, DECISIONS #104): extends the existing Batch 31 overnight summary with `warning_minutes` (>=19.5 °C), `critical_minutes` (>=20.0 °C), and a pure `room_verdict` classifier (`green` / `amber` / `red`) gated by `RED_CRITICAL_MINUTES=60`, then carries those fields through the existing `GET /api/v1/bedroom/overnight` response and shared schema with no new table, endpoint, or cloud call. Home's overnight glance and the `/bedroom` chart header now show a verdict badge using the app's existing verdict colour system but room-specific labels (`Green` / `Amber` / `Red`). Production smoke verified the merge SHA on Railway + Vercel same-origin health, web `/`, and the auth-gated bedroom-overnight route `401` direct and via the Vercel rewrite.
- [x] **Batch 34 — Bedroom temperature × sleep correlation shipped** (PR #50, squash `43cdf3a`, DECISIONS #105): extends the existing Batch 17 driver-correlation read and Batch 22 `early_waking_0400` evaluator with four bedroom-derived keys from the Batch 31 series (`bedroom_warning_minutes`, `bedroom_critical_minutes`, `bedroom_fan_ran_minutes`, `bedroom_peak_fan_speed`), keyed by wake-morning sleep date. Bedroom driver correlations carry a deterministic grouped-mean `summary` sentence so the output reads as advisory evidence rather than just a Pearson coefficient. Missing bedroom data remains `None`; there is no new stats engine, table, endpoint, migration, cron/cloud call, or automatic fan-threshold tuning. Production smoke verified the merge SHA on Railway + Vercel same-origin health, web `/`, and the auth-gated `insights/drivers` route `401` direct and via the Vercel rewrite.
- [x] **Batch 35 — Last night as the single morning hub shipped** (PR #54, squash `a93644b`, DECISIONS #106): folds the baseline range into the Home `MetricComparisonTable` — last night's value is tinted by its direction-aware in/out-of-band tone with the `lower–upper` range as a muted sub-line — and drops the separate 'vs your normal' column (back to 3 columns), so the standalone `/baselines` page is **retired**: `pages/BaselinesPage.tsx`, its `App.tsx` route + lazy import, and `components/MetricsBaselineTable.tsx` are deleted, with `MetricBaselineRow` + `formatBaseline` moved into `MetricComparisonTable.tsx`. The retrospective `OvernightGlance` (room verdict badge + indoor range) moves into `SleepSnapshotCard`; the evening `BedroomSummaryCard` keeps tonight's live fan/bedroom read and the outdoor overnight-low stat. Partially reverses Batch 28's kept `/baselines` link (#98). Frontend-only, no backend change, no migration. Production smoke verified the merge SHA on Railway + Vercel same-origin health and web `/`.
- [x] **Batch 36 — Unified Today card shipped** (PR #56, squash `3087d7a`, DECISIONS #107): recomposes Home's Today surface into one day card with the verdict shown once, per-workout rows scoped to their own action state, and a day footer for add/view-week/skip/actual controls. No endpoint, payload, mutation, backend, or migration change. Production smoke verified Railway + Vercel same-origin health, web `/`, and the auth-gated daily-loop route.
- [x] **Batch 37 — Collapse-not-remove Home sections shipped** (PR #57, squash `f9da1ba`, DECISIONS #109): Home now renders the full section superset every load, with one data-state-derived section expanded and the rest collapsed-but-present with summaries; the clock can only reorder evening prep sections, never add/hide sections. `CollapsibleSection`, `homeSections`, and the refactored `DashboardPage` replace the old phase-branch disappearance model. Frontend-only, no backend/payload/migration change. Production smoke verified Railway + Vercel same-origin health, web `/`, and the auth-gated daily-loop route.
- [x] **Batch 40 — Post-flexibility/mobility analysis shipped** (PR #58, squash `9a05176`, DECISIONS #110): adds a Batch-8-style post-session path for Garmin mobility activities using a name-based selector (`"mobility"` in activity name, never broad `other`; yoga excluded), a lean HR-vs-resting/consistency/planned-session/check-in packet, a mobility-coach Anthropic boundary, idempotent `analyses.analysis_type='post_flexibility'` storage keyed by `activity_id`, scheduler wiring after ride analysis, `postFlexibilityAnalyses` in `/api/v1/daily-loop`, shared schema support, a Home "Flexibility read" card, and a dry-run/commit backfill runner for the historical mobility routine. Advisory only: no power/FTP/zones/cadence/stamina/PC/TE/time-series and no Green/Amber/Red or recovery impact. No migration. Production smoke verified merge SHA `9a05176` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 41 — Walking integration shipped** (PR #59, squash `d9b5a69`, DECISIONS #111): adds a deterministic walking brief (`services/walking_brief.py`, `GET /api/v1/walking-brief`, `walkingBrief` on `/api/v1/daily-loop`), advisory active-recovery walk volume in the morning packet (`classificationImpact: none`), and a threshold-gated deliberate-walk post-session path (`analysis_type='post_walk'`) using a lean HR/pace packet with HR-zone distribution and no power/FTP keys. The hourly Garmin poll runs the walk pass after ride + flexibility analysis, Home shows a "Walking base" panel plus "Walk read" cards, and `walk_analysis_backfill.py --since ... [--commit]` handles historical qualifying walks. No migration. Production smoke verified merge SHA `d9b5a69` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/walking-brief` direct and via Vercel.
- [x] **Batch 43 — Post-strength analysis shipped** (PR #61, squash `29e9d49`, DECISIONS #113): adds the per-session strength read as the direct counterpart to Batch 40, reusing the existing `is_strength_activity` selector (Batch 19, via `exclude_from_recovery`, #49/#80 — no new classifier) and Batch 19's `compute_strength_rollup` for consistency. New `services/post_strength_analysis.py` assembles a lean HR-vs-resting/consistency/planned-session/check-in packet (no power/FTP/cadence/stamina/PC/TE/zones/time-series), runs through a strength-coach Anthropic boundary, and stores idempotent `analyses.analysis_type='post_strength'` keyed by `activity_id`. The hourly Garmin poll runs the strength pass after ride + flexibility, `postStrengthAnalyses` surfaces on `/api/v1/daily-loop` with shared schema support, Home shows a "Strength read" advisory card, and `strength_analysis_backfill.py --since ... [--commit]` handles the historical backlog. Advisory only — never feeds Green/Amber/Red or recovery; the Batch 19 rollup brief is unchanged and complementary. No migration. Production smoke verified merge SHA `29e9d49` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 42 — Breathwork integration shipped** (PR #60, squash `743770f`, DECISIONS #112): adds a deterministic breathwork consistency brief (`services/breathwork_brief.py`, `GET /api/v1/breathwork-brief`, `breathworkBrief` on `/api/v1/daily-loop`) plus a pure `should_recommend_breathwork(signal)` verdict-lever that appends a breathwork suggestion to `planAdjustments` only on a Red verdict, recovery-low readiness, or unbalanced/low HRV, never changing the Green/Amber/Red classification or feeding recovery math. Home renders a compact "Breathwork rhythm" line in the Today section. No per-session LLM analysis, no HRV-correlation engine, no migration. Production smoke verified merge SHA `743770f` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/breathwork-brief` direct and via Vercel.
- [x] **Batch 44 — Interval-resolved ride analysis shipped** (PR #63, squash `432e6b4`, DECISIONS #114): fixes the accuracy bug Mark named — the post-ride read judged a structured ride by its blended whole-ride average power (or its name). New pure `services/ride_intervals.py` segments the stored per-second trace (#93) on the planned IR's interval boundaries (`build_structured_workout_ir`, Batch 12.1 — no new Garmin call) and grades each **work** interval on its own %FTP target (NP/%FTP/zone/adherence/power-fade/HR-drift), describing but never grading warm-up/recovery/cool-down. `assemble_context_packet` carries `plannedWorkoutIr` + `intervals` + an `execution` summary (whole-ride average kept but relabelled context), the `SYSTEM_PROMPT`/`outputRules` grade the work intervals, and `PROMPT_VERSION` → `post-workout-analysis-v2-2026-07-03` with a prompt-version-aware `_analysis_is_current` regeneration. Free/outdoor rides (no IR) fall back to the whole-ride + zone-histogram read. Optional Home "Interval execution" table (daily-loop `intervals`/`execution` + `rideIntervalSchema`); one-off `ride_analysis_backfill.py`. Recovery isolation preserved — narrative + grading only, Green/Amber/Red untouched; no migration. Production smoke verified merge SHA `432e6b4` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 145 — Executed-boundary interval grading shipped** (PR #172, squash `d28743c`, DECISIONS #224): Garmin activity summaries do not contain lap boundaries, so cycling sync now captures the separate `get_activity_splits` payload inside the existing `activities.raw_summary` JSONB (no migration). Segmentation prefers credible one-lap-per-step boundaries. Garmin/Zwift imports in both the canonical sample and Mark's affected 2026-07-23 ride expose only one whole-ride lap, however, so a fail-closed 5-second target-power alignment recovers executed steps only when it fits the known structured target sequence within 8 %FTP RMSE and materially beats the planned clock; otherwise the Batch 44 planned-duration path remains unchanged. Each interval/execution packet records `actual_laps`, `actual_trace`, or `planned_durations`, and prompt version v8 marks old reads stale. The affected 2×15-minute plateaus resolve to ~249.5/249.6 W instead of blending in the 170 W recovery; verdict/recovery logic is untouched. Branch, PR, and post-merge CI passed; production served the exact merge SHA through Railway and Vercel.
- [x] **Batch 147 — Per-interval workout editor shipped** (PR #174, squash `cb666de`, DECISIONS #226): the bike source grammar gains an additive primary interval block with reps plus independent work/rest duration, absolute %FTP, and cadence. A pure mapper round-trips native or legacy sessions while preserving warm-up/cool-down/primer steps, and deterministic scale/Sweet Spot/Z2 presets feed a mobile Today-card Current-vs-Change-to editor. Approval versions the planned row and reuses the existing proposal → approve → intervals.icu/Zwift rail; zero-rest Z2 emits one continuous work step, rest cadence reaches ZWO, legacy pattern expansion is unchanged, and Red-never-VO2 remains hard. No migration or verdict/prompt change. Both CI waves and Vercel preview passed; production verified on the merge SHA through Railway and Vercel.
- [x] **Batch 148 — Factual training-week grounding shipped** (PR #175, squash `8e249c5`, DECISIONS #227): one read-only calendar-week record joins the final active plan, explicit workout-change audits, Garmin execution, and proved activity-to-plan matches. Morning and weekly-review prompts use it for accumulated-week claims and treat the knowledge-base schedule as a nominal routine only; only activities are execution truth, and recommendation attribution is not inferred. Morning v15 / reviews v4 invalidate stale narratives. No migration, shared-schema, verdict, or safety-gate change. Both CI waves, Vercel preview, post-merge main CI, and exact-SHA Railway/Vercel production checks passed.
- [x] **Batch 45 — Proactive push + fan-reconciled nudges shipped** (PR #65, squash `0d6ad9f`, DECISIONS #115): the morning verdict and every ride/strength/flexibility/walk post-workout read now push when generated, reusing the existing notification rail and `analyses`-tag idempotency so each verdict pushes once per profile/day and each analysis once per activity. Breathwork still has no per-session push. Evening thermal nudges now defer to the Batch 27 fan autopilot when armed and escalate a single "check the fan" alert only when the room is warm and the fan cannot cope; fan decision logic, Green/Amber/Red, and recovery isolation are untouched. No migration. Production smoke verified merge SHA `0d6ad9f` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 46 — Evening sleep projection shipped** (PR #66, squash `5bf9940`, DECISIONS #116): adds a pure deterministic `services/sleep_projection.py` core and optional `sleepProjection` daily-loop field so Home's Tonight section can personalise the static wind-down protocol from today's training load/timing, Mark's measured sleep drivers, bedroom/weather context, and fan-auto state. It is qualitative only (`routine` / `watch` / `protect`), degrades to the plain protocol on rest/insufficient-history days, and never writes `analyses`, predicts a numeric sleep score, changes Green/Amber/Red, or tunes fan thresholds. Shared schema support and Home rendering are included; the optional evening push/audit row and post-workout "impact tonight" line remain deferred. No migration. Production smoke verified merge SHA `5bf9940` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 48 — Explicit daily/block loop model shipped** (PR #68, squash `d9060a1`, DECISIONS #118): makes "the loop" a first-class object — a pure `services/daily_loop_state.py` model + orchestration seam (`describe_loop_state`, "where is Mark + what's next"), mirrored by a generalised frontend `useDailyPhase`. Generalises the cycling-shaped phase (`post_ride` → `post_training` off *any* ride/strength/flexibility/walk read, so a non-ride day advances instead of being stuck `pre_ride`) and adds a first-class evening `wind_down` phase in place of the 20:00 clock reorder; classifies the active plan block (build/recovery/taper/consolidation/transition) with `consolidation` as the explicit end-of-block boundary. The daily-loop payload gains a read-only `loopState { dayPhase, blockPhase, nextAction, atBlockBoundary }` (derived from post-session counts + planned workouts + the profile-local clock + a new `_active_block` query); the frontend prefers the server stage with a local fallback + live-clock `wind_down` overlay, and `homeSections` uses a `hasRide`-aware `primarySection` (non-ride reads lead the Today card; `wind_down` leads Tonight). Scope chosen at `/batch-start` was **model + frontend, no rewire** — the shipped Batch 45–47 wiring is left untouched (can adopt the seam later). Behaviour-preserving beyond those two intended generalisations; no verdict/fan/analysis-engine change, no new coaching logic, no migration, `loopState` optional in the shared schema so pre-48 cached payloads still parse. Production smoke verified merge SHA `d9060a1` on Railway + Vercel same-origin health, web `/`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 53 — Branded verdict, hero & login shipped** (PR #74, squash `c84945e`, DECISIONS #123): brings the generated CheckMark mark into the running app through the shared `Logomark`, makes `VerdictHero` the branded daily heartbeat on the Batch 52 surface system, elevates the login splash, adds the mark to the mobile/desktop top-bar lockups, and promotes Home's Next strip into a full-width primary action band. Presentation-only: no verdict logic, daily-loop payload, endpoint, auth flow, migration, or rebrand change. Production smoke verified merge SHA `c84945e` on Railway + Vercel same-origin health, web `/` and `/login`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 54 — Home hierarchy & calm density shipped** (PR #75, squash `8fb90a2`, DECISIONS #124): turns Home into a curated brief without changing the data model or daily-loop contract. A new pure `splitPrimaryDetail` keeps the existing lead/action-override section prominent while the rest recede under a quiet "More detail" grouping through `CollapsibleSection` `variant="secondary"`; the session-card action cluster collapses to one primary + one secondary + a dropdown overflow; the greeting/date lockup compacts so `VerdictHero` sits higher; collapsed summaries truncate on a word boundary via `truncateWords`; and section expand gets a subtle `framer-motion` enter animation that honours reduced-motion preferences. Presentation/interaction only — no payload, endpoint, auth, or migration change, and the one-expanded model, evening float, and Batch 51 desktop lanes are preserved. Production smoke verified merge SHA `8fb90a2` on Railway + Vercel same-origin health, web `/` and `/login`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batch 55 — Screen polish & states shipped** (PR #76, squash `aa2c7e0`, DECISIONS #125): finishes the four-batch front-end premium pass by applying the Batch 52 system to Check-in, Sleep, and Week, and adding a shared on-brand `EmptyState`/`ErrorState`/`OfflineNotice` in place of the generic "please try again" cards. `MetricComparisonTable`'s mostly-empty "vs your age" column folds into a per-row descriptor shown only where an age norm exists; `CheckInPage` moves from five scattered save buttons to one unified "Save check-in" action over the same endpoints; `SleepPrepBody`'s evidence disclosure and `WeekAheadPage`'s instructional card are visually lightened. Presentation only — no data, endpoint, analysis-engine, or migration change, and no nav/IA change. Production smoke verified merge SHA `aa2c7e0` on Railway + Vercel same-origin health, web `/` and `/login`, and auth-gated `GET /api/v1/daily-loop` direct and via Vercel.
- [x] **Batches 153–156 — Four-part full-app review shipped** (PRs #182–#185; DECISIONS #233–#236): diagnose-only code/engine-hotspot, data/security, coaching-integrity, and live UX reviews now form one current evidence set. Each report ranks material findings and assigns provisional remediation stubs without silently folding fixes into the audit. Batch 156 closes the programme with all 15 routes captured in both themes against production data plus focused state/viewport evidence; its lead open issue is the broken production Coach-memory contract (`holiday_windows` outside the shared section enum). No product behavior, verdict, prompt, plan, migration, or runtime configuration changed in the review programme.
- [x] **Batch 158 — Delivery-rail atomicity & observability shipped** (PR #187, squash `bf7474b`, DECISIONS #238): `commit=False` is transaction-owner-safe on cloud failure; replacement pointers advance only after intervals.icu success; delivered IR id/version is the reconciliation fingerprint; interval edits and generic re-syncs preserve durable retry intent without duplicate creates; malformed/missing-FTP bike rows persist structured failures while expected no-ops remain row-free. No migration or verdict/prompt/safety-rule change. Branch, PR, and post-merge main CI passed; production served exact SHA `bf7474b` through Railway and Vercel.
- [x] **Batch 159 — Completed-session reads shipped** (PR #188, squash `5aaa594`, DECISIONS #239): cycle/strength/mobility/deliberate-walk generators share completion and planned-workout linkage; migration `022` adds activity-scoped persisted generation state and backfills existing supported reads; the owned Week lookup exposes explicit absent/generating/failed/ready states and selects only supported post-session types; prompt plus input currentness now governs strength/mobility/walk and handover regeneration. Branch, PR-context, and post-merge main CI passed all seven jobs; Railway and Vercel production served the exact merge SHA.
