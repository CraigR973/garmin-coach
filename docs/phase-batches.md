# Phase batches

Batch ledger for Garmin Coach v1. A batch is shipped only after `/closeout`
finishes; until then leave the row unstruck. When shipped, strike through the
batch title and set the status to `Shipped`.

Tier map from `DECISIONS.md` #19:

| Tier | Work | Claude | Codex |
|---|---|---|---|
| 🔴 High | Complex reasoning: data-model boundaries, scheduler/sync edge cases, analysis-engine prompts, verdict logic, debugging | Opus | GPT-5.5 |
| 🟢 Mid | Well-specified implementation: CRUD, components, migrations, tests, mechanical refactors | Sonnet | GPT-5.4 |

## v1 batch plan

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 1 — Data model + profile seed~~ | 🔴 High | Shipped | 1.1 Data-shape audit from `~/garmin-spike/out/` and `~/garmin-spike/out_hive/`.<br>1.2 Alembic/SQLAlchemy model for `users`, `daily_metrics`, `sleep`, `activities`, `activity_timeseries`, `temperature_readings`, `weather_daily`, `manual_entries`, `planned_workouts`, `plan_blocks`, `analyses`, `experiments`, `knowledge_base`.<br>1.3 Shared Zod/TS schemas for the v1 stored shapes.<br>1.4 Seed Mark's admin profile directly in DB. | Replace the old football skeleton with a real Garmin Coach storage foundation and a private seeded user. | Models/migration are derived from the real spike JSON, not old football tables; snake_case DB and camelCase JSON conventions hold; UTC `*_utc` columns and user IANA timezone exist; Mark can log in after direct seed; tests cover migration/model serialization and auth/profile seed path; backend pytest, ruff, mypy pass. |
| ~~Batch 2 — Garmin sync foundation~~ | 🔴 High | Shipped | 2.1 Store Garmin credential reference/token-cache strategy without committing secrets.<br>2.2 Implement Garmin client service around `garminconnect`/garth using spike fields.<br>2.3 Sync daily metrics, sleep, body battery, HRV, RHR, weight/VO2 and activity summaries.<br>2.4 Sync activity details/time-series including power, HR, cadence, respiration, Performance Condition and Stamina. | Pull Garmin data into the v1 model with idempotent, testable service boundaries. | Service parses representative files including `training_readiness.json`, `sleep.json`, `hrv.json`, `body_battery.json`, `activities.json`, `activity_details.json`, `activity_metric_channels.json`; `recoveryTime` is stored as minutes; repeated syncs upsert without duplicates; secrets stay out of repo/logs; tests cover fixture parsing, idempotency, and failure handling; backend pytest, ruff, mypy pass. |
| ~~Batch 3 — Hive + weather syncs~~ | 🔴 High | Shipped | 3.1 Implement Hive client wrapper around sync `pyhiveapi` re-login.<br>3.2 Persist live indoor temperature readings from `getAll()` product/device shapes.<br>3.3 Implement Open-Meteo service for Kilmarnock weather daily history/forecast.<br>3.4 Add scheduler jobs for Hive polling and morning weather sync without triggering analysis yet. | Bring the environmental context into the database on the cadence the product needs. | Hive parsing is based on `~/garmin-spike/out_hive/getAll.json`, `getDevices.json`, and `getProducts.json`; indoor temperature reads `parsed[i].props.temperature`; weather uses lat `55.6045`, long `-4.5249`, `wind_speed_unit=mph`, and captures daily high/low plus overnight low/wind; jobs are idempotent and timezone-aware; tests cover parser fixtures, scheduler registration, retries, and no-secret logging; backend pytest, ruff, mypy pass. |
| ~~Batch 4 — 84-night backfill + baselines~~ | 🟢 Mid | Shipped | 4.1 Add one-shot import tooling for `12 Weeks Sleep Data` xlsx, 24 Mar-15 Jun.<br>4.2 Backfill `sleep` and `daily_metrics` from the spreadsheet.<br>4.3 Compute/store baseline helpers used by morning analysis.<br>4.4 Add admin-safe rerun/dry-run behaviour. | Seed the historical sleep/recovery baseline that makes the coach smarter than a one-day read. | Import trusts all spreadsheet columns except the broken constant `Duration`; dates map to the correct user timezone and UTC storage; reruns are idempotent; baselines are reproducible from the 84-night history and exclude known unreliable periods where appropriate; dry-run summarizes intended writes; tests cover parsing, ignored Duration, idempotency, and baseline calculations; backend pytest, ruff, mypy pass. |
| ~~Batch 5 — Training plan + knowledge base~~ | 🟢 Mid | Shipped | 5.1 Store editable knowledge-base sections from `ARCHITECTURE.md` §3.<br>5.2 Ingest current structured training plan and plan blocks.<br>5.3 Add per-day planned-workout override path with version history.<br>5.4 Add internal API/admin UI surfaces for viewing/editing retained state. | Give the app the persistent context his handover docs used to provide. | KB includes profile, data-quality rules, age-adjustment, sleep protocol, active hypotheses, and plan context; planned workouts are structured and versioned, including VO2-session revision support; per-day overrides preserve prior versions; APIs use the standard envelope; tests cover CRUD/versioning and data-quality rule persistence; backend and frontend tests/build/lint pass for touched surfaces. |
| ~~Batch 6 — Morning analysis engine~~ | 🔴 High | Shipped | 6.1 Build context-packet assembler from KB, daily data, baselines, environment, manual entries, and today's full plan.<br>6.2 Implement Claude call boundary with stored prompt/version metadata.<br>6.3 Encode Green/Amber/Red verdict framework and output rules.<br>6.4 Trigger morning analysis from the 06:30 local sync path and store the result. | Generate the daily morning verdict that replaces the manual Copilot handover loop. | Analysis packet includes the metrics-vs-baselines table, thermal/environment review, plan-aware cycling + strength verdict, age-adjusted sleep, and data-quality guardrails; Red never recommends VO2; Low Garmin readiness can be reconciled as load-driven only when recovery signals justify it; Claude outputs are stored in `analyses`; prompt/version is inspectable; tests cover packet assembly, verdict edge cases, no-balance rule, and stored output; backend pytest, ruff, mypy pass. |
| ~~Batch 7 — Daily app loop surfaces~~ | 🟢 Mid | Shipped | 7.1 Dashboard for today's morning verdict, key metrics, plan, and thermal state.<br>7.2 Manual check-in for BP, subjective score, RPE, feel, supplements, and food.<br>7.3 Adherence capture: did he do the planned work, and what changed?<br>7.4 Data-quality guardian warnings in the UI/API. | Make the daily loop usable from the phone without needing direct DB access. | Mark can open the PWA and see today's verdict/plan/status; manual entries persist and feed later analyses; adherence is captured against the planned workout/version; data-quality warnings prevent L/R balance, bad SpO2/HRV windows, wrist-HR strength recovery, and broken Duration from leaking into decisions; tests cover API and UI flows; frontend build/test/lint and backend pytest/ruff/mypy pass. |
| ~~Batch 8 — Post-workout analysis~~ | 🔴 High | Shipped | 8.1 Detect newly synced rides from hourly Garmin activity polling.<br>8.2 Assemble post-workout context from activity summary, zones, PC/stamina/time-series, plan, and morning verdict.<br>8.3 Generate/store Claude post-workout analysis.<br>8.4 Surface guided recovery protocol and tomorrow impact. | Close the loop after rides with performance read, recovery guidance, and next-day implications. | New rides trigger once, not repeatedly; strength sessions using wrist HR are excluded from recovery decisions; analysis includes power/HR/zones/cadence/Performance Condition/Stamina/Training Effect where present; output gives a workout rating, specific timed recovery protocol, and tomorrow impact; tests cover trigger idempotency, context assembly, and stored output; backend pytest, ruff, mypy pass. |
| ~~Batch 9 — Nudges + thermal monitoring~~ | 🔴 High | Shipped | 9.1 Evening nudge scheduler and notification copy for sleep protocol.<br>9.2 Thermal monitoring rules for indoor temperature thresholds.<br>9.3 Data-source freshness alerts for missing Garmin/Hive/weather inputs.<br>9.4 PWA push/notification preferences adapted from the inherited infra. | Use the live data to prevent the known sleep failure modes before bedtime. | Evening nudges are daily, timezone-aware, and respect quiet hours/preferences; thermal alerts respond to pre-cool/seal/peak thresholds including >19.5-20°C disruption risk; stale-source alerts distinguish Garmin, Hive, and weather gaps; inherited push infra no longer uses football terminology; tests cover scheduler timing, preference gating, thermal thresholds, and stale-source handling. |
| ~~Batch 10 — v1 hardening + release polish~~ | 🟢 Mid | Shipped | 10.1 End-to-end smoke scripts for the daily loop.<br>10.2 Observability/runbook updates for syncs, analysis, and deploys.<br>10.3 Security/privacy pass for secrets, logs, private-user assumptions, and backups.<br>10.4 Mobile PWA polish for installability and offline-safe daily views. | Make v1 supportable enough for daily use by Mark. | Runbooks describe common sync/analysis failures and recovery; logs expose job health without secrets; backups and env ownership are clear; mobile install path works; offline/freshness states are clear; final backend/frontend test suites and builds pass; production deploy is verified through `/api/v1/health`, app login, and a non-mutating daily-loop smoke check. |

## v2 batch plan

Batch ledger for Garmin Coach v2 — "the coach". Decomposed from `ARCHITECTURE.md`
§6 (v2 roadmap) and `DECISIONS.md` #25–33. Same conventions as v1: a batch is
shipped only after `/closeout` finishes; leave the row unstruck and `Planned`
until started.

**Sequencing rationale.** The **Zwift delivery rail (Batch 12)** is the
foundational dependency for everything else in v2 — executable coaching, dynamic
restructuring, holiday resume, and app-generated blocks all assume the app can
push a structured workout — so it lands first among the substantive batches.
Each later batch delivers its output *through* that rail under the
propose → approve → push contract (Decision #29).

**Phase 1 debt decision.** The Phase 1 retrospective items are scheduled as a
**standalone clean-up Batch 11 first**, ahead of the substantive v2 work. The
driver is the WC2026 "player" → user rename (Decision/debt #3): it touches
`auth.py`, `models/profile.py`, `models/notification.py`, and router error
details, and grows more expensive with every endpoint v2 adds — so it is cheapest
to clear before Batch 12 onward expand the API surface. The smaller items
(ForgotPin flow, dead `email.py`, `score-input.tsx` JSDoc, residual "predictions"
offline-queue refs) ride along in the same batch.

**Production daily-loop gate.** The 2026-06-21 production smoke check proved the
API/auth/daily-loop surface is live for Mark, but the actual daily data loop is
not yet healthy: Mark had to be seeded, the response had today's
`subjectDate=2026-06-21` but `morningAnalysis`, Garmin daily metrics/sleep,
Hive thermal values, weather, and analyses were all empty, and the first
post-seed Hive poll logged `hive temperature poll failed`. Railway production
was also missing the expected data/analysis environment variables
(`GARMIN_EMAIL`, `GARMIN_PASSWORD`, `GARMIN_TOKENSTORE`, `HIVE_EMAIL` /
`HIVE_PASSWORD`, `ANTHROPIC_API_KEY`) and logged `environment="development"`
because `ENVIRONMENT=production` was not set. Treat this as the first Batch 12
carry-in before adding new v2 delivery surfaces: production must show real
Garmin + Hive + weather + morning-analysis data for Mark, not just HTTP 200 and
login success.

**Update (2026-06-21 closeout).** Batch 12 shipped the delivery rail, the Garmin
`GARMIN_TOKENSTORE_B64` token-blob auth, and the Anthropic fail-closed validator,
but the strict data gate is still **not** met: production has no Garmin
daily-metrics/sleep sync — `GarminSyncService.sync_daily` is defined (Batch 2)
but has no caller in `src/`, so today's daily metrics + sleep are never synced
and the morning verdict has no readiness/sleep input. The data gate is relocated
to **Batch 18** (DECISIONS #57).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 11 — Phase 1 debt clean-up~~ | 🟢 Mid | Shipped | 11.1 Remove or replace the broken `ForgotPinPage.tsx` flow (UI says "enter email" + sends `{email}`, but the API expects `{display_name}` and logs the reset token for manual use); update `App.tsx`, `LoginPage.tsx`, `PinResetPage.tsx` to either drop the route or show a plain "contact Craig to reset your PIN" message.<br>11.2 Delete the dead `apps/api/src/services/email.py` service and the unused `resend_api_key` / `EMAIL_FROM` settings.<br>11.3 Rename WC2026 "player" internals to user/profile: `PlayerRole`, `get_current_player`, `CurrentPlayer`, `AdminPlayer` in `auth.py`; `PlayerRole(StrEnum)` in `models/profile.py`; `player_id` columns in `models/notification.py` (with migration); `"Player not found"` HTTP error details in routers.<br>11.4 Fix the `score-input.tsx` line-11 JSDoc and rename the residual "predictions" offline-queue references (`offlineQueue.ts`, `sw.ts`, `score-input.tsx`) to coaching/submission terms. | Clear the catalogued Phase 1 tech debt before v2 grows the codebase and the player→user rename gets more expensive. | PIN-reset no longer claims email or POSTs `{email}` — route is removed or shows contact-Craig copy consistent with Decision #20; `email.py` and the unused email settings are deleted with no remaining imports/references; backend "player" identifiers are renamed to user/profile, `notification` `player_id` columns are migrated, and HTTP error details updated, with no behaviour change to auth or the daily loop; offline-queue + JSDoc "predictions" references renamed; backend pytest/ruff/mypy and frontend test/build/lint pass. |
| ~~Batch 12 — Zwift delivery rail~~ | 🔴 High | Shipped | 12.0 Resolve the production daily-loop gate from the 2026-06-21 smoke: seed/confirm Mark, set Railway `ENVIRONMENT=production`, Garmin, Hive, and Anthropic vars, let/manual-trigger the relevant jobs, and verify stored rows plus API payload before building v2 on top.<br>12.1 Build a structured-workout intermediate representation from `planned_workouts` (warmup / intervals / cooldown steps with %FTP target, duration, and cadence).<br>12.2 intervals.icu calendar POST client (free API, athlete `i618709`), OUTPUT-only, with secret handling and no activity ingestion via intervals.icu.<br>12.3 Deterministic `.ZWO` export as the no-dependency fallback.<br>12.4 propose → approve → push workflow with nothing written silently; emit cadence-critical reps as individual steps and verify the IntervalsT cadence override on PC Zwift before locking the emit format. | Give the app a validated, approval-gated rail to deliver a structured workout into Zwift — the foundation for all v2 coaching, only after the v1 daily loop is truly producing live data for Mark. | Production daily-loop smoke passes against the Railway API with Mark login and today's response containing non-null Garmin daily metrics/sleep, non-null `morningAnalysis`, non-empty Hive thermal and weather fields, and no scheduler ERROR lines for the same window; Railway env ownership is documented and all required source/analysis vars are set without leaking secrets. A planned workout converts to the structured IR and to both an intervals.icu calendar payload and a deterministic `.ZWO` file; power + timing match the validated end-to-end result (Decision #25); intervals.icu stays OUTPUT-only with ingestion still direct from Garmin (#27); every push is propose → approve → push with no silent writes (#29); cadence-critical reps emit as individual steps and the IntervalsT cadence-override behaviour is documented as verified/unverified on PC (#32); secrets stay out of repo/logs; tests cover IR generation, `.ZWO` determinism, payload shape, and the approval gate; backend pytest/ruff/mypy pass and touched frontend surfaces build/test/lint. |
| ~~Batch 13 — Executable coaching (closed loop)~~ | 🔴 High | Shipped | 13.1 On an Amber morning verdict, regenerate the adjusted workout (cut duration 20–30%, drop a zone, no HIT) as a structured-workout proposal; Red never produces a VO2 push.<br>13.2 Auto-push approved workouts a couple of days ahead and surface the week-ahead in the UI (Decision #31).<br>13.3 PWA approval surface (propose → approve → push) wired to the Batch 12 rail.<br>13.4 Record every proposal and push in the `analyses` audit log. | Make coaching executable: an Amber morning or restructure regenerates the workout and, on approval, delivers it to Zwift — acted on, not just advised (Decision #30). | An Amber morning (or restructure) produces an adjusted structured workout honouring the verdict framework, and Red never emits VO2; nothing reaches Zwift without explicit approval (#29); approved workouts deliver via the Batch 12 rail, idempotently, and are recorded in `analyses`; the week-ahead is visible in the UI; tests cover Amber-triggered regeneration, the approval gate, idempotent delivery, and audit storage; backend pytest/ruff/mypy and frontend test/build pass. |
| ~~Batch 14 — Dynamic weekly restructuring~~ | 🔴 High | Shipped | 14.1 Weekly restructure engine that never stacks VO2 + Sweet-Spot on the same/adjacent days, reordering within the week.<br>14.2 Defer-on-fatigue logic driven by recovery signals and verdict trend.<br>14.3 Add Rønnestad 30/15 to the VO2 progression toolkit (build weeks ~Wk7+, ERG off, even-paced ~105–110% FTP / 15 s easy — Decision #33).<br>14.4 Restructure proposals versioned on `planned_workouts` and delivered only via propose → approve → push (Batch 12/13). | Let the plan adapt within the week to fatigue and session-spacing rules instead of running a fixed slate. | The restructurer never schedules VO2 and Sweet-Spot back-to-back; fatigue defers hard sessions based on recovery signals; Rønnestad 30/15 is selectable in the VO2 progression with its documented constraints; restructures are versioned on `planned_workouts` and reach Zwift only on approval; tests cover the no-stack rule, defer-on-fatigue, 30/15 emission, and versioning; backend pytest/ruff/mypy pass. |
| ~~Batch 15 — Holiday pause/resume~~ | 🟢 Mid | Shipped | 15.1 Capture a holiday window (start/end) treated as a recovery-week equivalent.<br>15.2 Correct block continuation on return: pre-holiday Build1→Build2; Build2→repeat Build1.<br>15.3 Versioned plan updates plus a UI surface to pause/resume. | Handle holidays without derailing the 13-week block structure. | A holiday window is treated as a recovery-week equivalent; on return the block continues per the documented rule (Build1→Build2; Build2→repeat Build1); plan changes are versioned and visible in the UI; tests cover both continuation cases and the recovery-equivalent treatment; backend pytest/ruff/mypy and touched frontend surfaces pass. |
| ~~Batch 16 — App-generated 13-week blocks~~ | 🔴 High | Shipped | 16.1 Conversational generator that produces a 13-week 2121 block from profile/FTP/history.<br>16.2 Refine-then-lock workflow — mould it, fix errors, then lock (Decision #16).<br>16.3 Generated VO2 days use the Batch 14 VO2 progression toolkit (incl. Rønnestad 30/15).<br>16.4 Locked blocks feed the daily loop and deliver via the Zwift rail on approval. | Let the app generate future training blocks the way Mark moulded them by hand, so the plan is never forgotten. | The generator emits a structured, versioned 13-week 2121 block (2 build / 1 recovery / wk12 taper / wk13 consolidation); refine-then-lock preserves edits and locks on confirmation; generated VO2 days can use the 30/15 progression; locked blocks feed the daily loop and the Zwift rail under approval; tests cover generation shape, refine/lock versioning, and progression rules; backend pytest/ruff/mypy and frontend pass. |
| ~~Batch 17 — Monitoring + insight~~ | 🔴 High | Shipped | 17.1 FTP-drift detection from power/HR trends, with the evidence window surfaced.<br>17.2 Early-warning drift alerts on degrading recovery/HRV/sleep trends ahead of a Red.<br>17.3 Driver/correlation analysis identifying the strongest movers of sleep/recovery from history.<br>17.4 Experiment tracker for active hypotheses (collagen, recovery-week disruption, 04:00 waking) writing to `experiments`/`analyses`. | Turn the accumulated history into proactive insight: drift warnings, correlations, and tracked experiments. | FTP-drift is detected and surfaced with its evidence window; early-warning alerts fire on degrading trends before a Red verdict; driver/correlation analysis identifies the strongest movers of sleep/recovery from the 84-night+ history; the experiment tracker manages active hypotheses with status/outcomes and writes audit rows; tests cover drift detection, alert thresholds, correlation computation, and experiment lifecycle; backend pytest/ruff/mypy pass. |
| ~~Batch 18 — Production daily-loop data sync~~ | 🔴 High | Shipped | 18.1 Wire `GarminSyncService.sync_daily` into the production scheduler — sync today's daily metrics + sleep (readiness, body battery, HRV, RHR, sleep stages) for active profiles, 429-safe, before the 06:30 morning analysis runs.<br>18.2 Ensure the morning analysis and `/api/v1/daily-loop` then read non-null daily metrics/sleep.<br>18.3 Verify the deferred Batch 12 strict daily-loop smoke passes in production.<br>18.4 Fix production Hive auth so the strict smoke's Hive-thermal field and the "no scheduler ERROR lines" condition can pass: resume from a persisted Cognito refresh token (`HIVE_TOKENSTORE_B64`) because the account requires SMS_MFA, and render scheduler tracebacks (`format_exc_info`) — DECISIONS #59. | Close the production daily-loop data gate deferred from Batch 12 (DECISIONS #57): make the live morning verdict run on real readiness + sleep data, not empty inputs. | A scheduled production job syncs today's Garmin daily metrics + sleep for active profiles idempotently and 429-safely; the 06:30 morning analysis consumes them; the strict `scripts/smoke_daily_loop.py` (`SMOKE_STRICT_DAILY_LOOP=1`) passes against the Railway API with Mark login (non-null daily metrics/sleep, non-null morning analysis, Hive thermal + weather, no scheduler ERROR lines); tests cover the sync wiring and the morning-analysis-with-data path; backend pytest/ruff/mypy pass. |

## v3 batch plan

Batch ledger for Garmin Coach v3 — "the long game". Decomposed from
`ARCHITECTURE.md` §6 (v3 roadmap). Same conventions as v1/v2: a batch is shipped
only after `/closeout` finishes; leave the row unstruck and `Planned` until
started. One batch per v3 roadmap bullet, capstone last. Decision numbers are
assigned at `/batch-start` (next free is #80).

**Narrative reviews use the Claude boundary; rollups stay deterministic.** Unlike
the deterministic engines of Batches 14/16/17 (#63/#69/#71), v3's reviews, the
trends narrative, and the handover export are inherently *narrative*, so they
reuse the thin Anthropic Messages boundary with stored prompt/version metadata in
`analyses` (the Batch 6/8 pattern, #47) — fakeable in tests without
`ANTHROPIC_API_KEY`. The *rollup packets* that feed them stay deterministic and
unit-testable.

**Human/API-triggered, not new scheduler crons.** Following #64/#71, reviews,
trends, and the handover export generate on demand via `POST /…/run` and record to
`analyses`; `GET` previews never write. An optional scheduled `/run` can be layered
on later without changing the engines — no silent background rewriting and no
racing the 06:30 pipeline.

**Migration-free where possible.** Outputs land in `analyses` under new
`analysis_type` values (`weekly_review`, `monthly_review`, `seasonal_trend`,
`experiment_evaluation`, `handover_export`); structured state reuses
`knowledge_base` / `metric_baselines` (#44/#68/#70/#71/#72). Any batch that
genuinely needs a new table must justify it.

**Sequencing.** Batch 19 (strength watching-brief) is the self-contained, no-LLM
opener and preserves the #49 recovery-isolation invariant. Batch 20 establishes
the v3 review boundary that Batches 21 and 23 reuse. Batch 22 *extends* the Batch
17 experiment tracker (#72) rather than replacing it. Batch 23 (handover export) is
the integrative capstone (#13) and lands last because it summarizes the output of
every batch before it.

**Seasonal / year-on-year degrades gracefully.** True year-on-year needs ≥1 year
of history; the 84-night backfill seeds from ~24 Mar 2026, so real YoY comparisons
mature ~Mar 2027. Batch 21's engine ships now and reports insufficient history
until the window fills, consistent with #71's sample-size gates.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 19 — Strength watching-brief~~ | 🟢 Mid | Shipped | 19.1 Classify strength / non-cycling sessions from already-synced Garmin activities (reuse the Batch 8 wrist-HR strength detection), with no new ingestion.<br>19.2 Compute deterministic strength rollups — frequency, volume/duration, and a load proxy — over rolling windows, reusing the `metric_baselines`/`analyses` homes rather than a new table where possible.<br>19.3 Surface an advisory strength "watching brief" (recent sessions, frequency-vs-target, trend) read-only in the daily loop / a panel and via API.<br>19.4 Preserve and test the recovery-isolation invariant: wrist-HR strength stays excluded from recovery/verdict decisions (#49). | Give Mark a light-touch advisory view of his strength work — is he doing it, is it trending, is it being neglected — without ever letting wrist-HR strength corrupt cycling recovery decisions. | Strength sessions are identified from already-synced activities with no new ingestion; deterministic frequency/volume/load-proxy rollups are reproducible over rolling windows and reuse `analyses`/`metric_baselines` rather than adding a table where possible (any new table is justified and migrated); the watching brief is advisory and read-only and never alters the Green/Amber/Red verdict or recovery protocol; the #49 exclusion of wrist-HR strength from recovery decisions is preserved and explicitly tested; backend pytest/ruff/mypy and any touched frontend surfaces build/test/lint pass. |
| ~~Batch 20 — Weekly & monthly deep reviews~~ | 🔴 High | Shipped | 20.1 Deterministic weekly rollup packet assembler aggregating the trailing week's sleep, recovery/HRV/readiness, training load + adherence, morning verdicts, thermal/environment, the strength brief (Batch 19), and Batch 17 insights.<br>20.2 Claude review boundary reusing the #47 pattern (thin Anthropic Messages call; store `prompt_version`/`model_name`/raw response + markdown in `analyses`, `analysis_type='weekly_review'`); narrative covers trends, wins, concerns, and concrete recommendations.<br>20.3 Monthly review variant over the wider window (`analysis_type='monthly_review'`), sharing the rollup + boundary machinery.<br>20.4 Read-only Reviews PWA surface + `GET/POST /api/v1/reviews/*`, human/API-triggered `/run`, `GET` previews never write (#71). | Turn the accumulated daily history into a periodic narrative deep-dive — a weekly and monthly review that reads trends, calls out wins and concerns, and recommends adjustments — the way a coach reviews a training block. | The weekly/monthly rollup packets are deterministic and reproducible from stored data; the review is generated through the thin Anthropic boundary with stored prompt/version metadata in `analyses` (#47) and is fakeable in tests without `ANTHROPIC_API_KEY`; reviews are human/API-triggered via `POST /…/run`, idempotent per period, and `GET` previews never write (#71); the narrative respects the data-quality rules (no L/R balance, SpO2/HRV reliability window, wrist-HR strength excluded from recovery, ignore the broken Duration column); no new migration unless justified; tests cover packet assembly, the review boundary, idempotency, and the API/UI surface; backend pytest/ruff/mypy and frontend test/build/lint pass. |
| ~~Batch 21 — Year-on-year & seasonal trends~~ | 🔴 High | Shipped | 21.1 Deterministic seasonal/period aggregation engine that buckets history into comparable windows (month/season) with summary stats, reusing the `metric_baselines` provenance pattern (#44) and the reliability cutoffs (#45).<br>21.2 Year-on-year comparison: same-period-vs-prior-year deltas for sleep, recovery, FTP/VO2, and thermal, with explicit insufficient-history handling when <1 year exists.<br>21.3 Read-only surface via `GET /api/v1/trends/*` (and/or folded into the Reviews surface), with the evidence window/sample counts shown.<br>21.4 Optional narrative summary through the Batch 20 review boundary when enough history exists; otherwise report "insufficient history" deterministically. | Add the long-horizon lens — this period vs the same period last year, plus seasonal patterns (winter vs summer sleep, FTP seasonality, thermal) — so the coach reasons across seasons, not just the current block. | The aggregation engine produces reproducible per-window summary stats honouring the documented reliability cutoffs (#45); year-on-year comparisons compute same-period deltas and degrade gracefully — reporting insufficient history rather than misleading numbers — until a full year of data exists (history starts ~24 Mar 2026, so true YoY ~Mar 2027); the evidence window/sample counts are surfaced (consistent with #71); the read-only API/UI exposes trends without writing on `GET`; any narrative reuses the Batch 20 boundary; tests cover the windowing, the YoY deltas, and the insufficient-history paths; backend pytest/ruff/mypy and touched frontend pass. |
| ~~Batch 22 — Hypothesis evaluation~~ | 🔴 High | Shipped | 22.1 Per-experiment evidence-window evaluator: for each active experiment in the Batch 17 tracker (#72), gather the relevant data window and compute a deterministic before/after or correlation read using the Batch 17 insights math (slope/Pearson).<br>22.2 Conclusion recommendation mapping the evaluation to supported/refuted/inconclusive with the evidence surfaced — recommendation only, never an auto-conclude (the tracker's `concluded` stays human-gated and terminal — #72).<br>22.3 Surface the evaluation on the experiments PWA + `GET /api/v1/experiments/{id}/evaluate`; accepting a recommendation flows through the existing `POST /api/v1/experiments/*` conclude path.<br>22.4 Audit each evaluation in `analyses` (`analysis_type='experiment_evaluation'`), idempotent per subject window. | Close the loop on Mark's standing hypotheses (collagen, recovery-week disruption, 04:00 waking): automatically evaluate each experiment's evidence and recommend a conclusion for him to accept, instead of leaving the tracker a manual lifecycle. | Each active experiment gets a deterministic evidence-window evaluation reusing the Batch 17 insights math, skipping with an explicit reason when history is insufficient (#71 sample gates); the recommended outcome is one of supported/refuted/inconclusive with its evidence surfaced; the recommendation never changes experiment status on its own — concluding remains the human-gated, terminal `POST /…/conclude` action (#72); evaluations audit to `analyses` idempotently; no new migration (reuses `experiments`/`analyses`); tests cover the evaluator, the recommendation mapping, the never-auto-conclude guard, and the audit; backend pytest/ruff/mypy and frontend pass. |
| ~~Batch 23 — Auto-generated handover-doc export~~ | 🔴 High | Shipped | 23.1 Deterministic handover assembler gathering the full living state — KB (profile, data-quality rules, age-adjustment, sleep protocol, hypotheses), current plan/block, baselines, recent reviews (Batch 20), seasonal/YoY (Batch 21), experiments + evaluations (Batch 22), and the strength brief (Batch 19) — into a structured handover packet.<br>23.2 Generate the handover document through the Batch 20 review boundary (#47) into the doc shape Mark used to hand-write, stored in `analyses` (`analysis_type='handover_export'`) with prompt/version metadata.<br>23.3 Export to portable markdown and/or PDF, downloadable from the PWA + `GET/POST /api/v1/handover/*` (human/API-triggered).<br>23.4 Round-trip sanity: the export faithfully reflects current retained state (KB/plan/hypotheses) so it can seed another AI without Mark re-writing context. | Fulfil the product thesis (#13): the app generates the "handover doc" Mark used to hand-write to brief an AI — pulling all retained state and recent insight into one portable export — so he never writes one by hand again. | The handover assembler deterministically gathers KB + plan + baselines + recent reviews + seasonal + experiments/evaluations + strength brief into an inspectable packet; the document is generated through the thin Anthropic boundary with stored prompt/version metadata (#47), fakeable in tests without `ANTHROPIC_API_KEY`; export produces portable markdown and/or PDF downloadable from the PWA, human/API-triggered with `GET` previews non-writing; the export faithfully reflects current retained state (data-quality rules and reliability cutoffs intact); no new migration unless justified; tests cover packet assembly, the generation boundary, and the export format; backend pytest/ruff/mypy and frontend test/build/lint pass. |

## Post-roadmap — daily-flow redesign batch plan

Batch ledger for the daily-flow Home redesign — re-sequencing the PWA around
Mark's actual day (wake → sleep snapshot → verdict → sleep-adjusted workout →
approve/override → Zwift → post-ride analysis → tomorrow + sleep prep) and
closing the bedroom climate loop. Same conventions as v1–v3: a batch is shipped
only after `/closeout` finishes; leave the row unstruck and `Planned` until
started. Decision numbers are assigned at `/batch-start` (next free is #99).
Builds on the world-class UI/UX redesign (PR #35, DECISIONS #90) and the
executable-coaching rail (Batches 12–13).

**Batch 28 (age-comparison axis + merged last-night table) was added to this
ledger retrospectively.** It was built ad-hoc in a 2026-06-28 session rather than
via `/batch-start`, and is recorded here for parity. It is built on
`feat/age-comparison` and **not yet shipped** — strike the row and set `Shipped`
at `/phase-closeout`. Decision: #98.

**Front-end-led, thin backend delta.** The morning verdict already rewrites the
real %FTP targets on an Amber/Red day (`services/executable_coaching.py` —
`adjust_ir_for_verdict`, Decision #30), and the morning check-in already feeds
the verdict (`services/morning_analysis.py` folds `manualEntries` into the
context packet). So most of the daily flow is *already built* — Batch 24 is a
pure presentation re-architecture over the existing `/api/v1/daily-loop` payload
with no backend change and no migration. The net-new logic is concentrated in
Batch 25 (same-day delivery + manual override) and Batch 26 (post-ride check-in
into the analysis).

**Sequencing.** Batch 24 ships first as the visible win and is independent of every
open question. Batch 25 carries the one real unknown — same-day intervals.icu →
Zwift latency — in a throwaway 25.0 spike, and supersedes the lead-days=2
auto-push (#31). Batch 26 mirrors the morning check-in after the ride. Batch 27
(fan) is a separate integration track, reusing the Garmin/Hive cloud-client pattern (no home hub), and gates
nothing before it. Tiers per #19: 24 is Mid (well-specified component work);
25–27 are High (delivery edge cases, analysis-engine wiring, a new integration +
control loop).

**Same-day supersedes #31.** Decision #31 auto-pushes approved workouts a couple of
days ahead; the product now wants the morning's sleep-adjusted session approved
and delivered *that day* ("most like having an actual coach"). Batch 25 records a
new decision superseding #31. The implementation shipped in PR #38 with same-day
push + manual override; the remaining live-use note is to observe real Zwift
appearance timing on the first same-day send.

**Batch 29 (Today-card actions + push-on-plan-set delivery) shipped in PR #44
on 2026-06-29.** Designed with Craig on 2026-06-29 (DECISIONS #99; full spec
`docs/designs/today-card-actions.md`). It
turns the Home "Today" card into the one place Mark acts on his day — showing the
next workout of *any* type with Edit/Swap/Skip (no coach changes) or
Approve/Ignore/Edit/Swap/Skip (sleep-adjusted) — and moves delivery to
**push-when-the-plan-is-set** so mornings are review-only and every action
re-syncs an already-delivered Zwift event. This **supersedes #91's
same-day-on-approval** (and #31's lead-days=2), and deliberately reverses
#29/#30's "nothing delivered without approval" for the *baseline* (approval now
gates only the morning adjustment).

**Batch 30 (Home day controls + rearrangeable week plan) shipped in PR #45 on
2026-06-30.** It turns the live plan into
day categories (cycle / weights / flexibility / rest / mixed), gives Home
day-level add/skip/actual controls while keeping each workout on the Batch 29
Today card, and replaces the old Plan page with a real grouped schedule that can
add workouts, move workouts, skip days, and swap an existing workout into a rest
day. Decision: #100. Full spec:
`docs/designs/home-day-controls-week-plan.md`.

**Batch 31 (Overnight temperature × fan × sleep chart) is specced, not started.**
Designed with Craig on 2026-06-30 from the "morning check-in should graph overnight
room temp vs when the fan was on/off/what setting" idea. It makes the Batch 27 fan
autopilot **legible**: the room temperature is already captured every 15 min
(`temperature_readings`), but the fan's on/off/speed history is only *logged*, never
persisted — so the batch adds a `fan_state_readings` time-series table written by
`run_fan_control`, a pure read-only `GET /api/v1/bedroom/overnight` join (temp + fan +
the night's sleep), a recharts dual-axis chart on `/bedroom`, and a one-line glance on
Home. A 2026-06-30 prod read confirmed the room runs warm enough that the fan fires
every night now, so there is real variation to plot. Decision number assigned at
`/batch-start` (next free #101). Full spec: `docs/designs/bedroom-overnight-chart.md`.

**Batch 32 (Plan page tap-to-move day picker) shipped (PR #48, squash `5d7804f`), prod-verified.** Designed
with Craig on 2026-07-01 from a "drag-and-drop between days?" idea for the weekly
plan page. The decision was **not** to use drag-and-drop: the Plan page
(`/delivery`) is edited by Mark on his phone, and touch-dragging a card across a
vertically scrolling day list is unreliable for the primary user. Instead it
replaces the page's two fragmented move controls — the per-workout `Move <date>`
trio (limited to the next 3 days) and the separate rest-day swap-in list — with
**one bottom-sheet day picker** per workout that can move a session to any day in
either direction. The picker is built as a reusable `MoveWorkoutSheet` (so Home's
Today-card swap can adopt it later) over the page's plan window, widened 7→14 days
so "push it into next week" works without a separate calendar. It is
**frontend-only**: both existing controls already call the same
`ExecutableCoachingService.swap_day` (move when the target is empty, swap when
occupied, either direction — #99/#97), so there is no backend change and no
migration. Shipped verification on the merge SHA `5d7804f`: Railway and Vercel
same-origin `/api/v1/health` both returned that SHA, web `/` returned `200`, and
unauthenticated `POST /api/v1/workout-delivery/planned-workouts/{id}/swap`
returned `401` both direct and through the Vercel rewrite. Decision #103. Full spec:
`docs/designs/plan-move-day-picker.md`.

**Batch 33 (Bedroom overnight temperature verdict) is specced, not started.**
Designed with Craig on 2026-07-01 as a direct, small follow-on to Batch 31: rather
than making Mark read the overnight chart, classify each night's room into the
app's existing Green/Amber/Red vocabulary from minutes spent at/above the
19.5 °C / 20.0 °C thresholds Batch 31 already charts, shown as a badge next to
the Home glance and the `/bedroom` chart header. Purely additive over
`services/bedroom_overnight.py`'s existing `summarize_overnight` — no new table,
no new endpoint, no migration. Decision number assigned at `/batch-start` (next
free #104). Full spec: `docs/designs/bedroom-temperature-verdict.md`.

**Batch 34 (Bedroom temperature × sleep correlation) is specced, longer-term, not
started.** Discussed with Craig on 2026-07-01 as the "does the room actually cost
sleep, and could that inform the fan's own thresholds" idea. It deliberately
reuses the existing Batch 17 driver-correlation engine
(`insights.compute_drivers`/`DRIVER_KEYS`) and the Batch 22 `early_waking_0400`
experiment evaluator rather than building new statistics — it just gives them new
candidate driver values (bedroom warning/critical minutes, fan-run minutes) from
the Batch 31 series, closing an item Batch 31 explicitly deferred (DECISIONS #101
non-goals). Advisory-only by design (#71/#72): it never writes to
`fan_control.py`'s thresholds itself, and needs weeks of accumulated
`fan_state_readings` before it has anything to say, so it is sequenced well after
Batch 31/33 have been live, not immediately following them. Decision number
assigned at `/batch-start` (next free #105). Full spec:
`docs/designs/bedroom-temperature-sleep-correlation.md`.

**Batches 35–37 (Home refinements) are specced, not started.** Reviewed with
Craig on 2026-07-01 from four Home-page thoughts, decomposed into three batches
that reinforce each other (each shrinks the surface the next one organises), to be
built in order 35 → 36 → 37. They are refinements on the Batch 24 single-phase
Home and the Batch 28 merged last-night table, all frontend-only except where
noted. Decision numbers assigned at `/batch-start` (next free #106).

**Batch 35 (Last night as the single morning hub)** combines two of the ideas
that both restructure the one "Last night's sleep" card, so they ship as one
coherent card change: (1) fold the baseline **range** into the Home
`MetricComparisonTable` and tint last night's number by whether it sat in range —
which makes the standalone `/baselines` page redundant, so it is **retired** (the
range data — `lowerQuartile`/`upperQuartile` — is already on the daily-loop
payload, and `/baselines` is only reachable from the sleep card, not any tab); and
(2) pull the **retrospective** overnight-room read (`OvernightGlance` + the Batch
33 room verdict) into that same card, leaving **tonight's live** fan/bedroom
controls in the evening card. Partially reverses Batch 28's "keep the `/baselines`
link" (#98). 🟢 Mid, frontend-only, no migration. Full spec:
`docs/designs/last-night-morning-hub.md`.

**Batch 36 (Unified Today card)** collapses Home's "today's workout" from a
summary card **plus** N separate per-session cards into **one** Today card — day
header (with the verdict badge shown **once**, not duplicated per session),
session rows each keeping their own Edit/Swap/Skip/Approve controls scoped, and a
day-level footer (Add / Skip day / "I did something else"). Pure recomposition of
existing pieces (`DayPlanCard` + `TodayCard`); Batch 29's delivery-action contract
and the `/api/v1/daily-loop` payload are unchanged. 🟢 Mid, frontend-only, no
migration. Full spec: `docs/designs/unified-today-card.md`.

**Batch 37 (Collapse-not-remove Home sections)** changes Home from **swapping**
whole section-sets by phase (Batch 24, where off-phase sections are *absent* — a
ride makes the sleep snapshot disappear) to **keeping every section present but
collapsing** the ones that aren't primary for the current state, so nothing ever
disappears from Home and object permanence is preserved. Keeps **data state** (not
the clock) as the driver of *what's primary* — the clock only nudges *ordering*
(float bedroom-prep up in the evening), never adds or hides a section. Sequenced
**after** 35 + 36 because those shrink Home to a few rich cards first, so the
collapse model reads as a short list of panels, not a wall of drawers. Supersedes
the Batch 24 remove-model (referenced as the "read-first Home" in #91/#97). 🔴
High (revises a shipped Home model + needs the expand/order judgment), frontend-
only, no migration. Full spec: `docs/designs/home-collapse-not-remove.md`.

**Batches 38–39 (in-app guided non-cycling sessions) are WITHDRAWN (2026-07-02,
DECISIONS #108).** They were specced with Craig on 2026-07-01 from the idea that
non-cycling workouts should be *coached through in the app* the way a bike workout
is coached through in Zwift — an in-app interval **player** for strength (38) and an
uploaded follow-along **video** for flexibility (39), each logging completion into a
new `guided_sessions` table and reconciling it with the Garmin activity. **Dropped
because Mark tracks his strength and flexibility sessions on his watch and only
wants the *post-workout analysis* in the app, not an in-app player to run the
session** — so the players, the `guided_sessions` table + migration, and the
completion/reconciliation machinery are all unneeded. The specs
`docs/designs/strength-guided-player.md` and `docs/designs/flexibility-video-player.md`
are kept but banner-marked WITHDRAWN (history, not deletion). This surfaced a gap —
Mark wanted the analysis for **both** strength and flexibility, but only flexibility
had a per-session read planned (Batch 40); strength had only the Batch 19 rollup
brief. That gap is filled by the new **Batch 43 (post-strength analysis)** below.
The reserved decision numbers #109 (Batch 38) and #110 (Batch 39) are retired unused.

**Batches 40–43 (non-cycling activity analysis) are specced, not started.** Batches
40–42 were specced with Craig on 2026-07-02 after a live census of Mark's **full**
Garmin history (4,280 activities, Dec 2014 → Jul 2026, 10 `activityType.typeKey`s)
surfaced three near-daily non-cycling habits the coach is blind to — **mobility,
walking, and breathwork**; **Batch 43 (post-strength analysis)** was added the same
day in the 38/39 replan (DECISIONS #108) as strength's per-session counterpart to
Batch 40. Four batches, buildable roughly in order 40 → 41 → 42 with 43 alongside 40
(independent of each other and of the Home-refinement batches 35–37; Batch 40 is the
flagship, 43 its closest sibling). Each maps a non-cycling type onto the *right*
existing pattern — the Batch 8 per-session analysis, the Batch 19 deterministic
brief, or the morning-verdict lever — rather than forcing all through the
power-centric ride analysis. Decision numbers assigned at `/batch-start` (40 → #110,
41 → #111, 42 → #112, 43 → #113, after #108 the replan and Batch 37's #109).
**Landmine carried into every spec:** Garmin's `other` bucket is two populations — 47 recent
"…Mobility Workout" sessions (`distance 0`) **and** 153 old 2016–2020 misclassified
road rides (`distance > 0`, already matched as rides by name) — so the flexibility
selector keys on the activity **name**, never `typeKey == "other"`.

**Batch 40 (Post-flexibility/mobility analysis)** gives the near-daily mobility
routine the per-session read cycling gets: the *same* Batch 8 architecture (hourly
poll → packet → Anthropic boundary → `analyses` `analysis_type='post_flexibility'`
→ daily-loop) but a **lean, purpose-built packet** (duration, HR-vs-resting,
consistency/streak, planned session, check-in) and a mobility-coach prompt, because
a 15-min stretch has no power/cadence/FTP/stamina/time-series. Name-based selector
(yoga excluded); the ~47 existing sessions are backfilled. 🔴 High (analysis-engine
prompt + new packet), backend + frontend, no migration. Full spec:
`docs/designs/post-flexibility-analysis.md`.

**Batch 41 (Walking integration)** treats 450 walks as the Z1–Z2 aerobic base /
active recovery they are, with (1) a deterministic **walking brief** (Batch 19
clone), (2) additive **active-recovery volume context** in the morning packet
(verdict math untouched), and (3) a **threshold-gated per-session analysis** for
*deliberate* walks (≥ ~30 min or ~3 km) on an **HR/pace** packet (not power),
`analysis_type='post_walk'`. Ambient walks stay brief-only; walking already carries
per-second detail (#93), so no new ingestion. 🔴 High, backend + frontend, no
migration. Full spec: `docs/designs/walking-integration.md`.

**Batch 42 (Breathwork integration)** tracks the near-daily breathwork habit with a
deterministic **consistency brief** (Batch 19 clone) and — the actionable part — a
**verdict recovery-lever**: on a Red/low-readiness/unbalanced-HRV morning the
verdict *recommends* a breathwork session via an additive `plan_adjustments` string,
using the recovery signal it already computes. No per-session analysis (a 3-min
drill has nothing to analyse) and no change to the Green/Amber/Red classification.
🔴 High (touches verdict output), backend + frontend, no migration. Full spec:
`docs/designs/breathwork-integration.md`.

**Batch 43 (Post-strength analysis)** is strength's per-session read — the direct
counterpart to Batch 40 for the sessions Mark tracks on his watch — added in the
2026-07-02 replan (DECISIONS #108) after the in-app strength *player* (old Batch 38)
was withdrawn. Same Batch 8 machinery as Batch 40 (hourly poll →
`generate_for_pending_strength` → lean packet → thin Anthropic boundary → `analyses`
`analysis_type='post_strength'` → daily-loop), keyed on the **Garmin strength
activity**. It is even higher-reuse than Batch 40: the selector already exists
(`is_strength_activity` via `exclude_from_recovery`, #49/#80) and the consistency
read reuses Batch 19's `compute_strength_rollup`, so the only new pieces are a lean
HR/consistency packet (no power/FTP/time-series) and a strength-coach prompt. It
*complements* the Batch 19 rollup brief (that is the weekly watching-brief; this is
the per-session narrative). Recovery isolation (#49/#80) preserved — strength stays
advisory. 🔴 High (analysis-engine prompt + new packet), backend + frontend, no
migration. Full spec: `docs/designs/post-strength-analysis.md`.

**Batch 44 (Interval-resolved ride analysis)** specced with Craig on 2026-07-02 after
Mark reviewed the app's post-ride read against Copilot's and named a real accuracy bug:
it *"bases it on average power expecting the power for the full workout to be in sweet
spot range, which isn't right — only the sweet spot intervals will be… it needs to
analyse the workout as a set, not base it all on its name."* A code audit confirmed it:
the `post_workout` packet hands Claude the **whole-ride** `avgPowerWatts` + a whole-ride
`timeSeriesSummary.power.avg` + a coarse zone histogram and the **planned** workout, but
never the **executed effort segmented into intervals and graded against each interval's
target** — so on any structured session the blended average sits below the work band
(warm-up + recovery + cool-down drag it down) and the model either anchors on it or
pattern-matches the **name**. This is the **first batch that goes back *into* the cycling
packet** to enrich it (Batches 40–43 forked *lean* packets away from it *because* those
non-cycling types have no power/time-series); cycling is the one type that carries
per-second power, and today we flatten it. The fix reuses the two things already shipped —
Batch 12.1's `build_structured_workout_ir` (ordered steps with `durationSec` +
`powerStartPct`/`EndPct` %FTP targets) and the DECISIONS #93 per-second `ActivityTimeSeries`
— to segment the trace on the IR's interval boundaries and grade each **work** interval on
its own target (warm-up/recovery/cool-down power described, never graded), with a `fade`
flag that grounds "no fade / no surges". No new Garmin call and **no migration** in v1;
Garmin ground-truth splits (`get_activity_splits`, which would also unlock free/outdoor
rides) and a concision/density pass (Mark's deflected "refined vs dense" question) are
deliberately deferred. The recovery decision and Green/Amber/Red are untouched — narrative
+ grading only. 🔴 High (analysis-engine prompt + packet). Full spec:
`docs/designs/interval-resolved-ride-analysis.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 24 — Time-aware home (daily-flow rebuild)~~ | 🟢 Mid | Shipped | 24.1 Add a `useDailyPhase` hook deriving the day phase from **data state, not the clock** — pre-ride = today's `morningAnalysis` present with no post-workout analysis yet; post-ride = a post-workout analysis exists for today — with time only as a tiebreaker, so it is correct whether Mark rides at 06:00 or 18:00.<br>24.2 Rebuild `pages/DashboardPage.tsx` into the single-phase flow: verdict hero → compact sleep snapshot → today's workout card (this phase only); post-ride swaps to ride analysis + tomorrow + sleep-prep + bedroom. Strength/rest days render a clean "nothing to ride today" state.<br>24.3 Demote the dense reads to tap-to-expand: the metrics-vs-baselines table, the full morning markdown read, and bedroom/weather detail move off Home into lightweight detail routes reachable from each card's chevron; nothing is hard-hidden (bottom-nav tabs + chevrons keep every read reachable).<br>24.4 Tests: phase derivation across fixtures, each Home state (pre-ride / post-ride / rest-day / offline), and the detail routes; rewrite `DashboardPage.test.tsx`. | Replace the read-everything dashboard with a calm, time-aware Home that surfaces the one step of Mark's day that matters now and keeps the rich data one tap away — using only the existing `/api/v1/daily-loop` payload. | Home renders exactly one coherent phase at a time, chosen from data state (verified across pre-ride, post-ride, rest-day, and offline fixtures); the demoted reads (metrics-vs-baselines table, full morning read, bedroom/weather detail) no longer sit on Home but remain reachable via card chevrons and the existing tabs (no hard-hide); no backend change, no new endpoint, no migration; frontend test/build/lint pass and the backend suite is untouched. |
| ~~Batch 25 — Same-day delivery + manual override~~ | 🔴 High | Shipped | 25.0 De-risk (throwaway spike, outside the repo like `~/garmin-spike`): time intervals.icu → Zwift same-day delivery end to end, and decide the trigger — live push on morning approval vs. pre-stage the base session overnight and push only the adjustment at wake. Record the outcome as a decision.<br>25.1 Same-day push: deliver today's approved proposal immediately on approval instead of the `DEFAULT_LEAD_DAYS=2` auto-push, superseding #31 with a new decision; keep propose → approve → push (#29) and Red-never-VO2 at the gate.<br>25.2 Manual override: let Mark accept the verdict-adjusted session or manually dial duration/intensity before approving, reusing the `adjust_ir_for_verdict` IR and the Batch 12 delivery rail; the override is the IR that is pushed and audited in `analyses`.<br>25.3 Wire the Batch 24 workout card's Approve / Override actions to the endpoint; non-bike (strength/rest) days surface "nothing to send to Zwift today" cleanly.<br>25.4 Tests: same-day push path, override IR build + push, Red-never-VO2 preserved, audit rows, non-bike handling. | Make the morning workout an acted-on, same-day decision — the sleep-adjusted session is approved (or manually overridden) that morning and pushed to Zwift now — so the loop feels like a live coach, not a two-day-ahead schedule. | Approving today's workout delivers it same-day through the intervals.icu event rail; first live use should still observe real Zwift appearance timing. A manual override produces a valid structured-workout IR that is the exact artifact pushed and audited in `analyses`; propose → approve → push (#29) and the Red-never-VO2 delivery gate are preserved and explicitly tested; #31's lead-days=2 auto-push is superseded by DECISIONS #91; backend pytest/ruff/mypy and frontend test/build/lint passed; PR #38 and production smoke were green. |
| ~~Batch 26 — Post-ride check-in into the analysis~~ | 🔴 High | Shipped | 26.1 Capture a post-ride check-in (RPE / how it felt / legs / niggles) reusing the existing `ManualEntry` `rpe` / `subjective_score` fields tied to the activity; no new table unless justified.<br>26.2 Feed it into the post-workout analysis packet (the Batch 8 service) so the read reflects subjective input; verify, and if needed strengthen, that a morning check-in submitted after wake re-folds into the verdict.<br>26.3 Surface the "how did it feel?" affordance on the Batch 24 post-ride ride card → the check-in form → the analysis visibly reflects it.<br>26.4 Tests: post-workout analysis with and without subjective input, morning re-fold timing, capture persistence; recovery-isolation (#49) intact. | Give Mark an after-ride check-in that shapes the post-workout analysis — the mirror of the morning check-in — so his own felt experience is part of the coach's read of the session. | Shipped in `b6c92b9` with migration `009`: post-ride check-ins persist as activity-linked `manual_entries` using the existing `rpe` / `subjective_score` shape; `PostWorkoutAnalysisService` includes `postRideCheckIn` and marks stale analyses pending when a newer check-in is not covered; date-level morning entries remain isolated from activity-linked entries; the Home post-ride card captures and displays the saved check-in; branch + main CI passed backend pytest/ruff/mypy, Alembic migration check, security audit, and frontend test/build/lint; production route smoke passed. |
| ~~Batch 27 — Bedroom fan control (Dreo 508S)~~ | 🔴 High | Shipped | 27.0 De-risk spike (throwaway, like `~/garmin-spike`): pair the Dreo 508S (DR-HPF008S) in the Dreo app, then drive it from `pydreo-community` (the standalone PyPI lib that the official `dreo-team/hass-dreoverse` wraps) with the Dreo account creds — confirm on/off, speed %, oscillate, and a state read from a plain script, the way the Garmin/Hive spikes did. If the direct cloud path proves unstable, fall back to the Home Assistant route (HA OS hub + `hass-dreoverse` + HA REST API). Record the chosen path as a decision.<br>27.1 Dreo client wrapper around `pydreo-community`, mirroring the Hive boundary: cloud auth from env creds (+ cached token like Garmin/Hive), secret-safe and never frontend-exposed; on/off + speed % + oscillate + state read.<br>27.2 Airflow control loop — the 508S moves air, it is **not** a thermostat and cannot hold a setpoint — so map the live indoor temp to fan on/off + speed % against the sleep-disruption thresholds already in the thermal model (Batch 9; >19.5–20°C), overnight only, bounded + idempotent.<br>27.3 Evening "Bedroom — Auto" surface in the Batch 24 post-ride / evening Home state: fan state + the temp it is responding to, a manual override, and preferences.<br>27.4 Tests: client wrapper against fixtures, control-loop thresholds/bounds/idempotency, no-secret logging, graceful degradation when the fan/cloud is unreachable. | Act on the bedroom temperature the app already monitors — drive the Dreo 508S fan to add airflow when the room runs warm enough to threaten sleep — closing the loop from the Hive thermal read to a physical response, reusing the Garmin/Hive cloud-client pattern with no extra home hardware. | A server-side Dreo client (via `pydreo-community`, or the HA REST fallback if the spike requires it) controls the 508S — on/off, speed %, oscillate — with credentials/token held server-side and never exposed; the overnight loop raises airflow as the live indoor temp crosses the documented sleep-disruption thresholds and backs off below them, idempotently and within safe bounds, degrading gracefully when the fan is unreachable; surfaced with a manual override in the evening Home state; tests cover the client, the control loop, secret hygiene, and the unreachable path; backend pytest/ruff/mypy and frontend test/build/lint pass. |
| ~~Batch 28 — Age-comparison axis + merged last-night table *(unplanned, added retrospectively)*~~ | 🟢 Mid | Shipped | 28.1 Add the "vs the average for your age" axis: Garmin **fitness age** read from the already-synced `daily_metrics.raw_payload['max_metrics_vo2'][0].generic.fitnessAge` (no column, no migration, no extra Garmin call), plus **VO₂max / resting HR / overnight HRV** vs static sex×decade population norms in a new pure `services/age_norms.py`; `build_age_comparison(...)` classifies each metric **direction-aware** (a resting HR below average reads green); threaded through the morning context packet → `daily_loop._serialize_analysis` → Home; `sex` added to the seeded KB profile.<br>28.2 Merge the age axis with the existing #44/#45 "vs your own baseline" read into one Home table (`MetricComparisonTable`), joining `metricsVsBaselines` with `ageComparison` by metric (resting HR exact key, HRV bridged `hrv_7_day_avg_ms`⟷`hrv_overnight_ms`, VO₂max appended as age-only), rendering `—` where a frame is absent (only RHR + HRV have both).<br>28.3 Difference-forward comparison columns: Last night is a plain value; "vs your normal" and "vs your age" state the verdict + numeric difference (`in range` / `N above` / `N below`), tone direction-resolved.<br>28.4 Fold the table into the existing "Last night's sleep" card (replacing its REM/Deep/SpO₂ stat grid), drop the fitness-age banner + standalone title, keep the `/brief` + `/baselines` detail links; `AgeComparisonCard` removed. | Show Mark, on Home, how last night sits against both his own normal **and** the typical person his age — one glanceable card, an additive read with no new Garmin call and no migration. | Population norms are static and privacy-safe (no external call); fitness age is read from the already-synced VO₂max payload (no migration); the comparison is pure and direction-aware (resting HR below average reads green); the two reads merge into one difference-forward table that degrades to `—` where a frame is absent; the read is backend-additive only — the Green/Amber/Red verdict is untouched and `metricsVsBaselines` still serves the `/baselines` detail route; the `/brief` + `/baselines` Home links are preserved (the only Home entry point to those routes); tests cover `age_norms`, the merged table, and the Home wiring; backend ruff/mypy/pytest stay green (frontend-only delta after 28.1) and frontend lint/test/build pass. DECISIONS #98. |
| ~~Batch 29 — Today-card actions + push-on-plan-set delivery~~ | 🔴 High | Shipped | 29.0 De-risk (throwaway spike, outside the repo like `~/garmin-spike`): prove the intervals.icu event lifecycle this batch depends on — create → update-in-place (or delete+recreate) → move date → delete — and confirm how each propagates to Zwift and its timing (the #91 latency note). Record the chosen replace/move mechanism as a decision.<br>29.1 Push-on-plan-set delivery: deliver each workout to Zwift when it lands in the plan (on block generation / restructure), superseding #91 (same-day-on-approval) and #31 (lead-days=2) with a new decision; the as-planned baseline goes out **without** per-workout approval (reverses #29/#30 for the baseline only), approval now gates only the morning adjustment.<br>29.2 Extend the delivery rail (currently create+delete) with **replace** (manual edit / Approve-adjusted) and **move** (swap day), keyed idempotently to `planned_workout_id`+`version`; audit every create/replace/move/delete as an `analyses` row; a failed re-sync leaves local state honest (#97).<br>29.3 Two-state Today card off the existing `adjustment.changed` flag: no-changes → Edit / Swap day / Skip; changes → Approve & upload / Ignore (pure dismiss) / Manual edit / Swap day / Skip, with `planAdjustments` inline. Red-never-VO2 still gates Approve.<br>29.4 Swap = unified move-or-swap (empty target → move, occupied → swap); Skip = mark-only `planned → skipped` (no migration — `status` is already `String(50)`) + Zwift delete, no reshuffle; universal card — change `useDailyPhase` so any workout type leads the day (`rest_day` only when nothing is planned), non-bike has no upload.<br>29.5 Tests: two-state selection, swap resolution, skip transition, Red-never-VO2 preserved, rail create/replace/move/delete idempotency + audit, push-on-plan-set fires on plan create/restructure, failed-resync honesty, and the web card across both states + all types. | Make the Home "Today" card the single place Mark acts on his day — the next workout of any type, already sitting in Zwift, that he can approve a sleep-adjustment for, edit, swap to another day, or skip — so the loop feels like a live coach managing his Zwift calendar rather than a static schedule. | Shipped in PR #44 (`8b5a71e`): delivery moves to push-when-plan-is-set (superseding #91/#31, recorded as #99); the baseline reaches Zwift without per-workout approval while approval still gates the morning adjustment and Red-never-VO2; the rail gains idempotent replace + move (keyed to id+version) and audits every mutation, with failed re-syncs leaving local state honest; the Today card renders both data-driven states and any workout type, with Swap (move-or-swap), Skip (mark-only, migration-free), Ignore (pure dismiss), and non-bike-has-no-upload all working; main CI and production smoke passed. DECISIONS #99. |
| ~~Batch 30 — Home day controls + rearrangeable week plan~~ | 🔴 High | Shipped | 30.1 Confirm the day-state model against the real active plan: cycle, weights, flexibility/mobility, rest as no active workout, and future mixed days as multiple active categories on one date.<br>30.2 Add a plan-action API that returns the grouped live schedule and supports Add workout, Swap an existing workout into a day, Skip whole day, and Record actual/did-something-else without a migration.<br>30.3 Rebuild Home's Today section around a day label plus day-level controls, while rendering every same-day workout through the Batch 29 Today card so per-workout delivery actions still work.<br>30.4 Replace the Plan page's hard-coded week with the live grouped schedule, including rest-day swap-in and per-workout move controls.<br>30.5 Preserve mixed-day delivery state by preferring `planned_workout_id` over date-only delivery lookups and by keeping target-date secondary workouts active during reslotting. | Let Mark treat the plan like a live week he can adjust: add cycle/weights/flexibility to any day, turn a rest day into a workout day or swap a workout into it, skip a whole day, and record what actually happened, all while the existing Zwift delivery rail stays honest. | Shipped in PR #45 (`263460f`): `GET /api/v1/plan-actions/schedule` and the plan-action mutations are covered by backend tests; Home and Plan render real schedule permutations including rest and mixed-day-ready cases; bike additions reconcile through the Batch 29 delivery rail while weights/flexibility remain local plan rows; skip and swap preserve local/Zwift honesty; no migration. PR checks and production smoke passed. DECISIONS #100. |
| ~~Batch 31 — Overnight temperature × fan × sleep chart~~ | 🔴 High | Shipped | 31.0 De-risk (quick prod read, throwaway like `~/garmin-spike`): confirm whether `sleep.raw_payload` carries a per-interval sleep-levels hypnogram for Mark (decides the sleep overlay: faint background band vs. sleep-window `ReferenceArea` + awake/restless annotation), and confirm the temp-vs-fan-loop fire offsets so a nearest-time join is the right read alignment.<br>31.1 Persist fan state: new `fan_state_readings` table (migration `011`, mirrors `temperature_readings`), written by `run_fan_control` — refactor `_apply_fan_control` to return its result and write one idempotent tick per within-window fire, covering the `auto_off` / `no_data` / `unreachable` / `winddown` branches so chart gaps are explained; the fan **decision** logic and thresholds are untouched.<br>31.2 Read API: new `routers/bedroom.py` `GET /api/v1/bedroom/overnight` — a pure DB read (never writes) that night-windows (reusing `fan_control` window constants) and joins `temperature_readings` + `fan_state_readings` + the night's `sleep` row, defaulting to the last completed night, with a recent-nights pager list; standard envelope + shared Zod schema; kept off `/api/v1/daily-loop`.<br>31.3 Chart on `/bedroom`: a recharts dual-axis `BedroomOvernightChart` (temperature line + fan-speed step, `ReferenceLine`s at 19.5/20.0 °C, sleep overlay per 31.0, muted segments for `auto_off`/`unreachable`), with a night pager and empty state, below the existing fan card on `BedroomPage.tsx`.<br>31.4 Home glance: a one-line last-night summary ("19→21 °C, fan ran 3.5 h, peak speed 5") on the evening/post-ride Home, linking to `/bedroom`; Home stays lean.<br>31.5 Tests: pure night-windowing/join + glance summary + hypnogram extraction; DB-backed one-tick-per-fire across all branches + idempotency + the overnight endpoint join/default; web chart (temp+fan, threshold lines, sleep overlay present/absent, pager, empty state). | Make the bedroom climate loop legible — chart, per night, the room temperature, what the fan actually did against the sleep-disruption thresholds, and the night's sleep behind it — so Mark can see whether the room cost him sleep and whether the fan is helping, and so the Batch 27 autopilot is debuggable and trusted rather than silent in the logs. | A `fan_state_readings` time-series table records one idempotent tick per overnight `run_fan_control` fire (including the auto-off/no-temp/unreachable branches, so gaps are explained not blank) with **no change to the fan decision logic, thresholds, or graceful degradation**; `GET /api/v1/bedroom/overnight` is a pure DB read that joins temp + fan + sleep for a night, defaults to the last completed night, and never writes; `/bedroom` shows a dual-axis temp×fan chart with the 19.5/20.0 °C threshold lines and the sleep overlay, with a night pager and a clear empty state for pre-deploy/no-data nights; Home gains a one-line glance linking through; the new table justifies itself as a genuine time series (not `analyses`, #71) and adds no new Hive/Dreo cloud calls and no secrets; tests cover the persistence branches + idempotency, the read/join/default, the glance summary, and the chart states; backend pytest/ruff/mypy and frontend test/build/lint pass and Alembic `011` up/down is checked in CI. Decision #101 (assigned at `/batch-start`). |
| ~~Batch 32 — Plan page tap-to-move day picker~~ | 🟢 Mid | Shipped | 32.1 Replace the `WorkoutRow` `Move <date>` trio (next-3-days only) and the rest-day "swap a workout into this rest day" list on `WeekAheadPage.tsx` (`/delivery`) with a single **Move** button per workout that opens a reusable bottom-sheet day picker component (`MoveWorkoutSheet`, shadcn `Sheet` `side="bottom"`, already in the UI kit) — built reusable so Home's Today-card swap can adopt it later.<br>32.2 Widen the page's schedule window from `days=7` to `days=14` (backend already caps at 14); the picker lists exactly those visible days — weekday + date, what's already scheduled (title(s) or "Rest"), a Today marker, current day disabled — and on selection calls the single `POST /api/v1/workout-delivery/planned-workouts/{id}/swap` with the chosen `targetDate`, letting the backend resolve move-vs-swap; derived from the already-fetched schedule (no new query).<br>32.3 Retire `swapIntoMutation` from this page and drop the rest-day swap-in block; the `/plan-actions/days/{date}/swap-in` route stays for any other caller.<br>32.4 Tests: rewrite `WeekAheadPage.test.tsx` for the picker (day-list contents, today marker, current-day disabled, select fires `swap` with the right date, sheet open/close); remove the old `Move <date>`/swap-in assertions. | Make rescheduling a workout on the Plan page one consistent, phone-friendly gesture — a tap-to-move day picker that can move any session to any day in either direction — instead of the current next-3-days move buttons plus a separate rest-day swap-in list, and without the touch-unfriendly drag-and-drop the idea started from. | Frontend-only (no backend change, no migration): the Plan page exposes one Move control per workout that opens a reusable bottom-sheet day picker (`MoveWorkoutSheet`) over the page's plan window, widened 7→14 days so a session can move into next week without a separate calendar; selecting a day moves the workout to any date in either direction via the existing `swap` route (the backend `swap_day` already resolves move-vs-swap, #99/#97), removing the next-3-days limit and the separate rest-day swap-in control; drag-and-drop is explicitly not used (mobile-primary user); the picker is built reusable so Home can adopt it in a follow-up; `WeekAheadPage.test.tsx` covers the picker states and the swap call; web lint/test/build pass and the backend suite stays green. Decision #103. |
| ~~Batch 33 — Bedroom overnight temperature verdict~~ | 🟢 Mid | Shipped | 33.1 Extend `summarize_overnight` (`services/bedroom_overnight.py`) with `warning_minutes` (readings `>= THRESHOLD_ON_C` 19.5 °C) and `critical_minutes` (`>= THRESHOLD_CRITICAL_C` 20.0 °C), computed the same way `fan_ran_minutes` already is, plus a pure `room_verdict(warning_minutes, critical_minutes) -> green/amber/red` classifier gated by a single named `RED_CRITICAL_MINUTES` constant.<br>33.2 Extend `OvernightSummaryOut` (`routers/bedroom.py`) and the shared Zod schema with `warningMinutes`, `criticalMinutes`, `roomVerdict` — same endpoint, same envelope, no migration.<br>33.3 Frontend: a `Badge` (reusing `verdictBadgeVariant`'s success/warning/error mapping) next to the Home overnight glance and the `/bedroom` chart header.<br>33.4 Tests: pure classifier boundary cases (0 min, just under/at/over `RED_CRITICAL_MINUTES`, warning-only vs. critical nights); DB-backed endpoint returns the new fields for a seeded night; web renders the correct badge per verdict. | Give Mark a one-glance Green/Amber/Red verdict for how the bedroom did last night, reusing the app's existing verdict vocabulary, so he doesn't have to read the Batch 31 chart to know whether the room cost him sleep. | Shipped in PR #49 (`8389985`): purely additive over the existing Batch 31 series and thresholds — no new table, no new endpoint, no new cloud call, and no change to the fan control loop or its thresholds; the classifier is pure and unit-tested at its boundaries; `GET /api/v1/bedroom/overnight` carries the new summary fields; Home's overnight glance and the `/bedroom` chart header show the correct verdict badge; branch CI and production smoke passed on Railway + Vercel same-origin health, web `/`, and the auth-gated bedroom-overnight route `401`. Decision #104. |
| ~~Batch 34 — Bedroom temperature × sleep correlation~~ | 🔴 High | Shipped | 34.1 Add four bedroom-derived driver keys (`bedroom_warning_minutes`, `bedroom_critical_minutes`, `bedroom_fan_ran_minutes`, `bedroom_peak_fan_speed`, sourced from the Batch 31 series and keyed by the wake-morning date) to `insights.DRIVER_KEYS` and `InsightsService._driver_records`, so the existing `GET /api/v1/insights/drivers` ranks them alongside the current candidates via the unmodified `compute_drivers`/`pearson` engine.<br>34.2 Add the same four keys to the `early_waking_0400` experiment's measured-candidate list (`experiment_evaluation.py`), closing the item Batch 31 explicitly deferred (DECISIONS #101).<br>34.3 A pure plain-language sentence layer over a driver's correlation + grouped means (e.g. "Nights with 60+ min above 20°C average 6 points lower sleep score (12 nights measured)"), surfaced wherever the driver ranking / `early_waking_0400` evaluation already renders.<br>34.4 Tests: pure driver-record assembly (incl. nights with no bedroom data degrading to `None`, not crashing), the sentence template over fixed correlation fixtures, the existing sample-size gate's insufficient-history path, and the `early_waking_0400` evaluator picking up the new candidates without changing its gate/recommendation logic elsewhere. | Answer, from Mark's own measured data, whether the bedroom's overnight temperature/fan activity actually moves his sleep score — advisory evidence a human can later use to inform the fan's thresholds, not a model that changes them itself. | Reuses the existing Batch 17 `compute_drivers`/`pearson` engine and the Batch 22 `early_waking_0400` evaluator with no new statistics, no new table, and no new endpoint (extends `GET /api/v1/insights/drivers` and `GET /api/v1/experiments/{id}/evaluate`); the sample-size gate correctly reports insufficient history until enough `fan_state_readings` nights have accumulated; nothing in this batch writes to `fan_control.py`'s thresholds or any other config — the output is advisory text only; tests cover the driver-record assembly, the sentence template, the sample-size gate, and the experiment-evaluator wiring; backend pytest/ruff/mypy pass, no migration. Decision #105 (assigned at `/batch-start`). |
| ~~Batch 35 — Last night as the single morning hub~~ | 🟢 Mid | Shipped | 35.1 Fold the baseline **range** into Home's `MetricComparisonTable`: show last night's value tinted by the existing in/out-of-band tone (`baselineDiff`) with the `lower–upper` range as a muted sub-line, and **drop the now-redundant "vs your normal" column** so the table stays three columns on mobile; keep colour as an enhancement only (the "vs your age" ✔/⚠ icon+text and the range text remain).<br>35.2 Retire the `/baselines` page: delete `pages/BaselinesPage.tsx` + its route/`lazy` import in `App.tsx` + test, remove the `baselinesLink` `DetailLinkCard` from `SleepSnapshotCard` (keep the `/brief` link), and move the `MetricBaselineRow` type out of the now-deleted `MetricsBaselineTable.tsx` (the reliability note already lives in `MetricComparisonTable`).<br>35.3 Move the **retrospective** overnight-room read (`OvernightGlance` + Batch 33 room verdict badge, already fetched via `useBedroomOvernight`) into `SleepSnapshotCard` under the table; the evening `BedroomSummaryCard` keeps **tonight's live** indoor/thermostat/fan read (settle at `/batch-start` whether the overnight-low stat also moves to the sleep card).<br>35.4 Tests: table renders the range sub-line + tints the number by band, "vs your normal" gone, room glance/verdict inside the sleep card, `/baselines` link gone + `/brief` kept; delete `BaselinesPage.test.tsx`, update `DashboardPage.test.tsx`. | Make "Last night's sleep" the one morning read — sleep + baselines (range inline, number coloured) + how the room actually was — so the separate baselines page and the sibling overnight glance both fold into it. | Frontend-only, no backend change and no migration (baseline quartiles and the bedroom-overnight summary are already on their payloads); the Home table shows the baseline range and tints last night's number by in/out-of-band while keeping a non-colour signal (icon/text/range) for accessibility; the `/baselines` route and page are removed with the `/brief` detail link preserved and no dead references left; the retrospective overnight-room read renders inside the sleep card while the live fan/bedroom controls stay in the evening card; this partially reverses Batch 28's kept-`/baselines`-link (#98), recorded in the new decision; web lint/test/build pass and the backend suite stays green. Shipped in PR #54 (`a93644b`): full web vitest 60/17 + lint + build green, backend CI green, prod smoke green on Railway + Vercel same-origin health (`sha=a93644b`) and web `/` 200. Decision #106. Full spec: `docs/designs/last-night-morning-hub.md`. |
| ~~Batch 36 — Unified Today card~~ | 🟢 Mid | Shipped | 36.1 Rework `DayPlanCard` into a single **Today** `Card`: header shows `"{label} day"` + the verdict `Badge` **once** (drop the duplicate per-session badge), body holds the day's session(s) as rows, footer holds the day-level actions.<br>36.2 Turn the per-workout `TodayCard` into an internal `WorkoutRow` sub-component rendered in the body (divider between sessions, not a nested card), **preserving each row's own local state** (`panel` none/edit/swap/skip, `ignored`, duration/intensity inputs) so multiple sessions expand independently and their Edit/Swap/Skip/Approve/Ignore controls stay scoped.<br>36.3 Move `AddWorkoutButtons` + `ActualWorkoutForm` into the card footer; the rest-day (no-workout) case renders the same single card with the "rest is the plan" empty state + footer actions (no orphan empty "Today's session" card).<br>36.4 Tests: one card for rest / 1-workout / 2-workout (mixed) days, verdict badge appears exactly once, each row's panel opens independently and fires the right mutation with the right `workoutId`, coach-adjustment Approve/Ignore on the correct row only, footer day-level actions still work; update `DashboardPage.test.tsx`. | Present the day's workout as one coherent Today card — day header + session rows + day footer — instead of a summary card orbited by separate per-session cards with a duplicated verdict badge. | Frontend-only recomposition with no new logic: no change to the Batch 29 delivery-action contract, no route change, no `/api/v1/daily-loop` payload change, no migration; one card renders per day across rest/single/mixed cases with the verdict shown once and each session's controls fully scoped inside it; web lint/test/build pass and the backend suite stays green. Decision #107 (assigned at `/batch-start`). Full spec: `docs/designs/unified-today-card.md`. |
| ~~Batch 37 — Collapse-not-remove Home sections~~ | 🔴 High | Shipped | 37.1 Add a reusable `CollapsibleSection` (shadcn `Collapsible`/`Accordion`) — header with a one-line summary + chevron, body lazy-rendered when open so a collapsed data-heavy section (e.g. the bedroom-overnight query) costs nothing.<br>37.2 Rework `DashboardPage` to render the **superset** of sections once and choose which **one** is expanded from `useDailyPhase` (pre-ride → Today, post-ride → After your ride, rest-day → Last night), rendering the rest **collapsed but present** with their summary in the header — replacing the three near-duplicate phase branches with one ordered section list.<br>37.3 Let the clock **nudge ordering only** (after ~20:00 float the Tonight/bedroom-prep section up); the clock never adds or hides a section — state stays the driver of what's primary, so Home is correct whether Mark rides at 06:00 or 18:00. Collapse state is derived per load (no sticky manual state in this batch).<br>37.4 Record the decision superseding the Batch 24 remove-model (off-phase sections become collapsed-but-present, not absent; the single-primary focus and data-state driver are kept), citing the "read-first Home" #91/#97.<br>37.5 Tests: per state the right section is expanded and the rest render collapsed-but-present (assert the collapsed summary text is in the DOM), expanding reveals the body, the evening ordering nudge reorders without changing which sections exist, offline still renders; rewrite `DashboardPage.test.tsx` around the section list. | Stop Home from making sections disappear as the day changes — keep every section present but collapse the non-primary ones (object permanence), driven by data state with the clock only reordering, so nothing Mark might want is ever gone from Home. | Frontend-only, no backend/payload change and no migration; Home renders the full section set every load with exactly one section expanded per data state and the rest collapsed-but-present (verified across pre-ride/post-ride/rest-day/offline), strengthening Batch 24's "no hard-hide"; the clock only nudges ordering and never adds/removes a section; sequenced after Batches 35–36 so the collapse model is a short panel list, not a wall of drawers; the new decision records the supersession of the Batch 24 remove-model (#91/#97); web lint/test/build pass and the backend suite stays green. Shipped in PR #57 (`f9da1ba`): CI green across backend pytest/ruff/mypy + Alembic + security audit + web build + Vercel preview, production smoke green on Railway + Vercel same-origin health (`sha=f9da1ba`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via the Vercel rewrite. Decision #109. Full spec: `docs/designs/home-collapse-not-remove.md`. |
| ~~Batch 38 — In-app guided strength player~~ | 🔴 High | **Withdrawn** | Withdrawn 2026-07-02 (DECISIONS #108). An in-app interval *player* to run a strength session in the PWA (the Zwift-equivalent), with a new `guided_sessions` table + migration and Garmin reconciliation. **Dropped** — Mark tracks strength on his watch and wants only the post-workout *analysis*, not a player. The analysis need is met by the new **Batch 43 (post-strength analysis)**. Reserved decision #109 retired unused. Spec kept, banner-marked WITHDRAWN: `docs/designs/strength-guided-player.md`. | — | — |
| ~~Batch 39 — In-app flexibility video~~ | 🟢 Mid | **Withdrawn** | Withdrawn 2026-07-02 (DECISIONS #108). An admin-uploaded follow-along *video* Mark plays in-app on the flexibility day, reusing Batch 38's `guided_sessions`. **Dropped** — same reason: Mark does flexibility on his watch and wants only the analysis, which **Batch 40 (post-flexibility analysis)** already delivers from the Garmin activity. Reserved decision #110 retired unused. Spec kept, banner-marked WITHDRAWN: `docs/designs/flexibility-video-player.md`. | — | — |
| ~~Batch 40 — Post-flexibility (mobility) analysis~~ | 🔴 High | Shipped | 40.1 Pure name-based `is_flexibility_activity` (`"mobility" in activity_name`, **not** `typeKey=="other"` — that bucket also holds 153 old misclassified rides) + a pure DB-free `compute_flexibility_consistency` rollup (streak / week / 4-week frequency), with tests that `other`-typed rides are not selected.<br>40.2 `assemble_flexibility_packet` (duration, avg/max HR vs resting HR, consistency, planned session, activity-linked check-in — **no** power/cadence/FTP/stamina/PC/TE/time-series) + a mobility-coach `SYSTEM_PROMPT`; reuse the thin Anthropic boundary (#47, fakeable).<br>40.3 `pending_flexibility_activities` + idempotent `generate_for_pending_flexibility` storing `analyses` `analysis_type='post_flexibility'` with `activity_id`.<br>40.4 Scheduler wiring (hourly poll runs the flexibility pass after rides) + `postFlexibilityAnalyses` on `/api/v1/daily-loop` + shared Zod schema.<br>40.5 Frontend flexibility read on the flexibility day (reuse the post-workout card + Markdown).<br>40.6 One-off backfill of the ~47 existing mobility sessions; tests + green gates. | Give Mark's near-daily mobility routine the per-session read cycling gets — same Batch 8 machinery, a lean HR/consistency packet, a mobility-coach prompt — instead of it being silently dropped as `other`. | Shipped in PR #58 (`9a05176`): backend + frontend, no migration (`analysis_type` is already `String(50)`). Mobility sessions are selected by **name** (never `typeKey=="other"`, so old misclassified rides cannot leak in) with yoga excluded; the packet carries HR-vs-resting + consistency and no power/zone/time-series keys; generation is idempotent (one per activity, regenerated on a newer check-in) and fakeable without `ANTHROPIC_API_KEY`; `post_flexibility` analyses surface on the daily loop/Home; recovery isolation (#49/#80) is preserved and tested. PR checks green across backend pytest/ruff/mypy, Alembic, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=9a05176`), web `/` 200, and auth-gated daily-loop 401 direct and via Vercel. Decision #110. Full spec: `docs/designs/post-flexibility-analysis.md`. |
| ~~Batch 41 — Walking integration~~ | 🔴 High | Shipped | 41.1 Deterministic **walking brief** (Batch 19 clone): pure `compute_walking_rollup` (4w/12w session count / distance / duration / sessions-per-week + trend) + read-only `WalkingBriefService` + `GET /api/v1/walking-brief` + `walkingBrief` daily-loop field + shared schema.<br>41.2 Additive **active-recovery volume context** in the morning packet (recent deliberate-walk distance/duration alongside the existing `totalSteps`); Green/Amber/Red math untouched.<br>41.3 `is_deliberate_walk` (named `WALK_ANALYSIS_MIN_DURATION_SEC` / `_MIN_DISTANCE_M` thresholds) + an **HR/pace** `assemble_walk_packet` (pace, avg/max HR, HR-zone distribution from the per-second HR channel, elevation unavailable when not stored — **not** power/FTP) + a Zone-2 walking-coach `SYSTEM_PROMPT`.<br>41.4 `pending_walk_activities` + idempotent `generate_for_pending_walks` → `analyses` `analysis_type='post_walk'`; scheduler wiring; daily-loop surfacing; shared schema.<br>41.5 Frontend walking-brief panel + a deliberate-walk read on Home (reuse the post-workout card).<br>41.6 Backfill qualifying historical deliberate walks; tests + green gates. | Treat Mark's 450 walks as the Z1–Z2 aerobic base / active recovery they are — a rollup brief, active-recovery context for the verdict, and a per-session read for deliberate walks — without forcing ambient walks through an LLM or touching the verdict. | Shipped in PR #59 (`d9b5a69`): backend + frontend, no migration. A deterministic walking brief + rollup (Batch 19 pattern) surface read-only; recent deliberate-walk volume enriches the morning packet as advisory active-recovery context with the Green/Amber/Red classification unchanged; only walks clearing the OR threshold (30 min or 3 km) get an HR/pace `post_walk` analysis (ambient walks stay brief-only), fakeable without `ANTHROPIC_API_KEY`; walking already carries per-second detail (#93) so there is no new ingestion or storage-cap risk; `walk_analysis_backfill.py` handles qualifying historical walks. PR checks green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=d9b5a69`), web `/` 200, and auth-gated walking-brief 401 direct and via Vercel. Decision #111. Full spec: `docs/designs/walking-integration.md`. |
| ~~Batch 42 — Breathwork integration~~ | 🔴 High | Shipped | 42.1 Deterministic **consistency brief** (Batch 19 clone): pure `compute_breathwork_rollup` (4w/12w frequency + trend) + read-only `BreathworkBriefService` + `GET /api/v1/breathwork-brief` + `breathworkBrief` daily-loop field + shared schema.<br>42.2 **Verdict recovery-lever**: a pure `should_recommend_breathwork(signal)` + recommendation copy appended to the morning `plan_adjustments` only on the Red/low-readiness/unbalanced-HRV signal the verdict already computes; the Green/Amber/Red classification is untouched.<br>42.3 Frontend breathwork consistency line + the recommendation rendered inline with the existing plan-adjustments.<br>42.4 Tests.<br>42.5 Green gates. | Track Mark's near-daily breathwork habit and let the morning verdict actively recommend a session on low-recovery mornings — turning logged data into a coaching lever — without a pointless per-session analysis of a 3-min drill. | Shipped in PR #60 (`743770f`): backend + frontend, no migration. A deterministic breathwork consistency brief surfaces read-only (Batch 19 pattern); on a Red/recovery-low-readiness/unbalanced-HRV morning the verdict appends a breathwork recommendation via an additive `plan_adjustments` string using the already-computed recovery signal, with the Green/Amber/Red classification unchanged; there is no per-session LLM analysis and no HRV-correlation engine (deferred); recovery isolation (#49/#80) is preserved. PR checks green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=743770f`), web `/` 200, and auth-gated breathwork-brief 401 direct and via Vercel. Decision #112. Full spec: `docs/designs/breathwork-integration.md`. |
| ~~Batch 43 — Post-strength analysis~~ | 🔴 High | Shipped | 43.1 Reuse the **existing** `is_strength_activity` selector (Batch 19, delegates to `exclude_from_recovery`, #49/#80) — no new selector; a test confirms it picks Garmin `strength_*` sessions and rejects rides + `mobility`.<br>43.2 `assemble_strength_packet` (duration, avg/max HR vs resting HR, consistency/frequency via Batch 19's pure `compute_strength_rollup`, planned session, activity-linked check-in — **no** power/cadence/FTP/stamina/PC/TE/time-series) + a strength-coach `SYSTEM_PROMPT`; reuse the thin Anthropic boundary (#47, fakeable).<br>43.3 `pending_strength_activities` + idempotent `generate_for_pending_strength` storing `analyses` `analysis_type='post_strength'` with `activity_id`.<br>43.4 Scheduler wiring (hourly poll runs the strength pass after the ride + flexibility passes) + `postStrengthAnalyses` on `/api/v1/daily-loop` + shared Zod schema.<br>43.5 Frontend strength read on the weights day (reuse the post-workout card + Markdown).<br>43.6 One-off backfill of existing strength sessions; tests + green gates. | Give Mark's strength sessions the per-session read cycling and (via Batch 40) mobility get — same Batch 8 machinery, a lean HR/consistency packet, a strength-coach prompt — so the strength workouts he tracks on his watch get the post-workout analysis he asked for, complementing (not replacing) the Batch 19 rollup brief. | Backend + frontend, no migration (`analysis_type` is already `String(50)`): strength sessions are selected by the existing `is_strength_activity` (`exclude_from_recovery`, #49/#80) with no new classification code; the packet carries HR-vs-resting + consistency (reusing `compute_strength_rollup`) and no power/zone/time-series keys; generation is idempotent (one per activity, regenerated on a newer check-in) and fakeable without `ANTHROPIC_API_KEY`; `post_strength` analyses surface on the daily loop; existing strength sessions are backfilled; recovery isolation (#49/#80) is preserved and tested (the analysis never feeds verdict/recovery); the Batch 19 rollup brief is unchanged and complementary; backend pytest/ruff/mypy and web lint/test/build pass. Shipped in PR #61 (`29e9d49`): CI green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=29e9d49`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #113. Full spec: `docs/designs/post-strength-analysis.md`. |
| ~~Batch 44 — Interval-resolved ride analysis~~ | 🔴 High | Shipped | 44.1 Pure DB-free `segment_ride_intervals(timeseries, ir, ftp_watts)` + a role classifier (warmup/work/recovery/cooldown from the IR `label`/`kind`/target) + per-interval NP / %FTP / zone / fade computation over the stored per-second `ActivityTimeSeries`; tests that a warm-up→2×SS→cool-down IR yields **work** intervals graded at the right %FTP with warm-up/cool-down **ungraded** and a decaying block flags `fade`.<br>44.2 Wire `intervals` + an `execution` summary + the planned `structured_workout_ir` (`build_structured_workout_ir`, Batch 12.1 — packet currently carries only raw `structuredWorkout`) into `assemble_context_packet`; **relabel the whole-ride average power as context**, not the execution verdict.<br>44.3 Extend `SYSTEM_PROMPT` + `outputRules` — grade execution on the **work intervals** vs their %FTP targets; whole-ride average is expected below target on a structured session and is never under-performance — and bump `PROMPT_VERSION`; thin Anthropic boundary unchanged/fakeable.<br>44.4 Free/outdoor-ride fallback: no planned IR → current whole-ride + zone-histogram behaviour, no regression.<br>44.5 One-off backfill regenerating recent structured rides through the new packet (precedent: #51 outdoor-ride + Batch 40 backfills).<br>44.6 *(optional, small)* Frontend interval table under the post-ride card (the card already renders the analysis markdown). Tests + green gates. | Fix the accuracy bug Mark named — the ride analysis judges a structured workout by its **blended whole-ride average power** (or its **name**) instead of grading each **work interval** against its own target, so warm-up/recovery/cool-down power drags the read below target and mis-rates a clean session. Analyse the workout *as a set*. | Backend-focused (+ optional frontend interval table), **no migration** in v1: intervals are derived in packet assembly from the already-stored per-second trace (DECISIONS #93) + the planned IR (Batch 12.1) — no new Garmin call; work intervals are graded `on`/`over`/`under` vs their %FTP band while warm-up/recovery/cool-down power is described, never graded; a `fade` flag (first-third vs last-third) grounds "no fade / no surges"; the whole-ride average is relabelled context and the prompt forbids treating it as under-performance; `PROMPT_VERSION` is bumped and generation stays fakeable without `ANTHROPIC_API_KEY`; a ride with no planned IR or no per-second trace degrades to the current whole-ride analysis rather than erroring; the recovery decision / Green-Amber-Red is untouched (narrative + grading only); recent structured rides are backfilled; backend pytest/ruff/mypy and web lint/test/build pass. Shipped in PR #63 (`432e6b4`): CI green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=432e6b4`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #114. Full spec: `docs/designs/interval-resolved-ride-analysis.md`. |

## Post-roadmap — passive-first loop batch plan

Batch ledger for the **passive-first reassessment** (2026-07-03) — re-reading the
app against Mark's refined user story: two nested feedback loops (**last night's
sleep → today's training**, daily; and a **completed multi-week block → the next
block**, per block), run on the principle **"effortless, not empty"** — the app
does the work and reaches out, while the rich data stays there for Mark to browse
(he enjoys it). Same conventions as v1–v3: a batch is shipped only after
`/closeout` finishes; leave the row unstruck and `Planned` until started. Decision
numbers assigned at `/batch-start` (next free is **#117**).

**What already exists (the reassessment's finding).** Most of the loop is built:
the wake-triggered morning verdict (sleep → readiness → sleep-adjusted plan via
`executable_coaching`), per-session post-workout reads for all five modalities
(ride / strength / flexibility / walk + the breathwork brief), and — crucially —
the real-world actuation Mark wants: the **Batch 27 Dreo fan autopilot already
drives the bedroom temperature overnight**. The corrected "effortless, not empty"
principle is also already well served by the Home (collapse-not-remove) + Trends /
Reviews / bedroom chart / interval-execution split. The structure is sound; these
batches are **connective tweaks, not a rebuild.**

**The gaps these batches close.** (1) The two highest-value outputs — the morning
verdict and each post-workout analysis — are generated silently and **never
pushed**; only the static 20:00 nudge + thermal/stale alerts push, so the app is
browse-first when the story wants push-first. (2) The evening thermal nudges still
tell Mark to hand-cool the room as if the fan autopilot doesn't exist. (3) There
is **no projection of how today's training will affect tonight's sleep** (the
"Tonight" section is the static protocol). (4) **Block-to-block progression
doesn't exist** — `generate_block_plan` is profile/FTP-only and never reads how
the last block went. (5) The loop is **implicit** — `DailyPhase` is `pre_ride |
post_ride | rest_day` (ride-shaped, no evening phase), which is why the pushes are
piecemeal.

**Sequencing.** **Batch 45** (proactive push + fan-reconciled nudges) ships first
— smallest, highest-impact, turns the app push-first and fixes the fan/nudge
contradiction. **Batch 46** (evening sleep projection) and **Batch 47**
(block-to-block progression) build the two missing loop stages and can proceed in
parallel — 46 closes the daily loop back to sleep, 47 builds the second, per-block
coupling. **Batch 48** (explicit loop model) is the **optional consolidating
refactor** that makes the loop first-class — deferrable, sequenced last, worth
doing only once the piecemeal wiring of 45–47 costs more than the refactor. Tiers
per #19: all 🔴 High (scheduler edge cases, analysis-engine reasoning, verdict /
plan logic, a shipped-model revision).

**Batch 45 (Proactive push + fan-reconciled nudges)** turns the app push-first.
The morning verdict and each post-workout analysis are generated by the scheduler
but **never pushed** — only the static 20:00 nudge + thermal/stale alerts reach
Mark — so today he must open the app to find his verdict/read. This wires a push
into the two existing completion points (`run_morning_weather_sync` after
`generate_and_store`; the hourly poll after `generate_for_pending_*`), reusing
`send_notification` + the `_send_once` `analyses`-tag idempotency so each
verdict/analysis pushes **exactly once** (09:30-backstop + regeneration safe). It
also reconciles the evening thermal nudges with the Batch 27 fan autopilot: defer
the manual pre-cool/seal nudges when `fan_auto_enabled` and the loop is
applying/holding, and escalate a "check the fan" push **only** when the fan can't
cope (`unreachable` / `no_data` / can't-reach-target) — the "notify only if he must
physically act" model. No migration. 🔴 High. Full spec:
`docs/designs/proactive-push-fan-reconciled.md`.

**Batch 46 (Evening sleep projection)** closes the daily loop **back to sleep**.
Today the forward-looking read is training-only ("impact on tomorrow") and the
"Tonight" Home section is the static protocol; nothing projects how today's
training will affect tonight's sleep. A deterministic, advisory `sleep_projection`
derives a qualitative directional read (**no numeric score** — false precision, per
#114) from today's training load + Mark's own measured sleep drivers (Batch 17/34)
+ the overnight weather, and layers 1–2 personalised prep actions onto the static
KB protocol, surfaced in the Tonight section (and optionally pushed via Batch 45).
Advisory only — never touches the verdict or the fan thresholds (#71/#72); degrades
to the plain protocol on a rest day / before driver history exists. 🔴 High. Full
spec: `docs/designs/evening-sleep-projection.md`.

**Batch 47 (Block-to-block progression)** builds the second feedback coupling Mark
named — **completed multi-week block → next block**. `generate_block_plan` is
profile/FTP-only and never reads how the last block went, though the signals are
all shipped (Batch 17 FTP-drift, Batch 20 rollups, Batch 44 execution grades,
`manual_entries` adherence). A deterministic `block_progression` core aggregates
the completed block's load / adherence / execution / drift / trend into a
next-block proposal (recommended FTP + focus carry-forward) that **seeds** the
generator draft with its evidence — the athlete still refines-then-locks
(#16/#69), nothing auto-applies. Reuses the existing engines, no new stats, no
LLM, no migration. 🔴 High. Full spec:
`docs/designs/block-to-block-progression.md`.

**Batch 48 (Explicit daily/block loop model) — optional, consolidating.** The loop
is implicit across scheduler jobs, Home sections, and services, which is why the
pushes are hand-wired and why `DailyPhase` is ride-shaped (`pre_ride | post_ride |
rest_day`, `post_ride` off ride analyses only, no evening phase). This
behaviour-preserving refactor makes the day/block loop a first-class state model
with an orchestration seam ("where is Mark + what's next") that Batches 45–47 hook
into, generalises the phase to any modality (a strength/walk-only day advances to
post-training), and gives evening a real `wind_down` phase instead of a 20:00 clock
nudge. **Deferrable** — 45–47 ship without it; sequenced last, when the piecemeal
wiring costs more than the refactor. No new coaching logic, no migration. 🔴 High.
Full spec: `docs/designs/explicit-loop-model.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 45 — Proactive push + fan-reconciled nudges~~ | 🔴 High | Shipped | 45.1 Push the **morning verdict** on generation — in `run_morning_weather_sync` after `analysis_result.generated`, send one verdict push (colour + one-line headline, `data.url='/'`), idempotent per (profile, subject_date) via the `_send_once` `analyses`-tag pattern so the 09:30 backstop + any regeneration never double-push.<br>45.2 Push each **post-workout analysis** on generation — after each `generate_for_pending_*` pass, send one push per **newly generated** analysis (ride / strength / flexibility / walk), idempotent per `activity_id` so a newer check-in / `PROMPT_VERSION` bump doesn't re-push; breathwork has no per-session analysis (#112) so nothing to push.<br>45.3 **Fan-reconcile** the evening/thermal nudges (`evaluate_thermal_alert` / `run_monitoring_alerts`): when `fan_auto_enabled` and the latest `fan_state_readings.action` ∈ {apply, hold, winddown} suppress the manual pre-cool/seal nudges; escalate a "check the fan" push only on {unreachable, no_data} or room-≥-critical-while-fan-maxed; keep the manual protocol nudges unchanged when the autopilot is off.<br>45.4 Quiet-hours honesty (a suppressed push still records its audit row — no retry) + dedupe/idempotency tests + green gates. | Make the app push-first — proactively deliver the morning verdict and every post-workout read the moment they land, and stop telling Mark to hand-cool a room the Batch 27 fan is already managing — so the coach reaches out instead of waiting to be opened. | The morning verdict and each ride/strength/flexibility/walk analysis send **exactly one** web-push when generated, idempotent across the 09:30 backstop and any regeneration (tagged `analyses` dedupe, reusing `_send_once` / `send_notification`), honouring quiet hours (suppressed pushes still audit, no retry); the evening thermal nudges defer to the Batch 27 autopilot when `fan_auto_enabled` and the loop is applying/holding/winding-down, and escalate a "check the fan" push only when the fan is unreachable/no-data or cannot reach target, while the existing manual protocol nudges are preserved unchanged when the autopilot is off; the fan decision logic, the Green/Amber/Red verdict, and recovery isolation (#49/#80) are untouched; each push is wrapped so a failure never blocks the pass that triggered it; no migration; backend pytest/ruff/mypy pass and any touched frontend builds. Shipped in PR #65 (`0d6ad9f`): backend-only, no migration; `NudgeAlertService` gains `push_morning_verdict` (once per profile/day) + `push_workout_analysis` (once per activity) reusing `_send_once`/`send_notification` + the `analyses`-tag dedupe, wired into `run_morning_weather_sync` + `run_garmin_activity_poll` (`_push_new_analyses`), each wrapped so a failure never blocks the pass; new audit types `verdict_push`/`analysis_push` are never surfaced; breathwork pushes nothing (#112); `evaluate_thermal_alert` takes a `FanReconcileState` and suppresses the manual nudges under an armed autopilot, escalating a single "check the fan" push only when the room is warm and the fan can't cope; quiet-hours-suppressed pushes still audit (no retry); the fan decision logic, the Green/Amber/Red verdict, and recovery isolation (#49/#80) are untouched. A CI-only FK failure (a test inserting an `Analysis` with a fabricated `activity_id`) was fixed by seeding a real `Activity` before merge; CI then green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=0d6ad9f`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #115. Full spec: `docs/designs/proactive-push-fan-reconciled.md`. |
| ~~Batch 46 — Evening sleep projection~~ | 🔴 High | Shipped | 46.1 Pure DB-free `services/sleep_projection.py`: today's training-load signals (late / high-intensity / big-TSS / evening timing) + Mark's measured sleep drivers (Batch 17 `compute_drivers` + Batch 34 bedroom keys) + overnight weather → a **qualitative directional read** (no numeric sleep-score prediction, per #114) + 1–2 **personalised** prep actions layered on the static KB `sleep_protocol`; rest day / insufficient driver history → falls back to the plain protocol.<br>46.2 Surface in the **Tonight** Home section (`SleepPrepBody`) with its evidence one tap away; optionally append an "impact on tonight's sleep" line to the post-workout read.<br>46.3 *(optional, deferred)* Push the personalised evening projection via the Batch 45 rail, superseding the static 20:00 nudge on days it has something to say (idempotent per subject-date; optional `analyses` `analysis_type='sleep_projection'` audit row).<br>46.4 Tests (directional read + prep actions across hard-late/easy-early/rest and warm/cool overnight; no numeric score; static-protocol fallback) + green gates. | Close the loop back to sleep — project how today's training is likely to affect tonight's sleep and turn the static evening protocol into a personalised wind-down, so Mark can prepare the room and routine for the best chance of a good night. | Shipped in PR #66 (`5bf9940`): backend + frontend, no migration. A deterministic, advisory projection derives a qualitative directional read (no numeric sleep-score prediction) from today's synced training load + Mark's own measured sleep drivers (Batch 17/34) + bedroom/weather context, and layers 1–2 personalised prep actions on top of the static KB sleep protocol; it degrades to the plain protocol on a rest day or before enough driver history exists; it surfaces in the Tonight Home section with evidence behind a details disclosure; the optional evening push/audit row and post-workout "impact tonight" line are deferred; it **never** changes the Green/Amber/Red verdict or the fan thresholds (advisory, #71/#72). CI green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=5bf9940`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #116. Full spec: `docs/designs/evening-sleep-projection.md`. |
| ~~Batch 47 — Block-to-block progression~~ | 🔴 High | Shipped | 47.1 Pure DB-free `services/block_progression.py`: aggregate the **completed** block's stats into a `BlockOutcome` — achieved-vs-planned load + adherence (Batch 20 rollups + `manual_entries`), execution-grade summary (Batch 44 per-interval `on`/`over`/`under`), FTP-drift verdict (Batch 17), and recovery/verdict trend — then map it to a **next-block proposal**: recommended FTP (bump on over-target + rising drift; hold/cut on under-target + falling drift), focus carry-forward, and a structural nudge (e.g. extra recovery on a degraded trend). Recommendation only, evidence attached.<br>47.2 **Seed** `generate_block_plan` from the proposal (FTP + focus) instead of the static `DEFAULT_FTP_WATTS`/profile default, surfaced in the `/builder` draft with its evidence; the **refine-then-lock** workflow is unchanged (#16/#69) and nothing auto-applies — `generate` still refuses to clobber an unlocked draft.<br>47.3 Block-boundary trigger: at consolidation week / block end, surface a "plan your next block" prompt on Home (optionally pushed via Batch 45).<br>47.4 Tests (outcome→proposal mapping; generator seeding; never-auto-apply guard; insufficient-history fallback) + green gates. | Build the second feedback coupling Mark asked for — let each completed multi-week block program the next one. Aggregate how the block actually went (load, adherence, interval execution, FTP-drift, trend) into a next-block proposal (FTP + focus) that seeds the generator, so periodisation is driven by real performance instead of a static template — while the athlete still refines-then-locks. | Shipped in PR #67 (`0241bf5`): backend + frontend, no migration, no LLM. A deterministic, advisory `block_progression` core aggregates a completed block's load/adherence/execution-grade/FTP-drift/trend (reusing Batches 17/20/44 + `manual_entries`, no new stats engine) into a next-block proposal — a recommended FTP change + focus carry-forward with its evidence surfaced; the block generator seeds the next draft's FTP + focus from the proposal instead of the static default; the proposal is never auto-applied — the refine-then-lock workflow (#16/#69) stays the only path that mutates the plan and generation still refuses to clobber an unlocked draft; it degrades to the static default before a full block of history exists. CI green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=0241bf5`), web `/` 200, and auth-gated `POST /api/v1/block-generator/generate` 401 direct and via Vercel. Decision #117. Full spec: `docs/designs/block-to-block-progression.md`. |
| ~~Batch 48 — Explicit daily/block loop model *(optional, consolidating)*~~ | 🔴 High | Shipped | 48.1 A single day/block **loop-state model** (`services/daily_loop_state.py` + a generalised `useDailyPhase`): the day advances morning → training → post-training → evening wind-down → night; generalise `post_ride` → **`post_training`** off any `post_*` analysis (ride/strength/flexibility/walk) and add a first-class **`wind_down`** evening phase (not just the `EVENING_HOUR` clock reorder); the block advances build/recovery/…/consolidation → next block.<br>48.2 An **orchestration seam** ("where is Mark in his day/block + what's next") that Batches 45/46/47 consume instead of each re-deriving state.<br>48.3 Point `homeSections`/`useDailyPhase` at the generalised phase (non-ride days lead with their post-session read; evening is a real phase), preserving the existing per-state Home renders.<br>48.4 Tests (phase derivation across all modalities + rest + wind-down; block-phase transitions; Home primary selection; no visual regression) + green gates. | Make the loop a first-class object — model Mark's day and training block as explicit states with an orchestration seam, generalise the cycling-shaped `DailyPhase` to any modality, and give evening a real wind-down phase — so the passive-push, sleep-projection, and block-progression work hooks into one loop model instead of each re-deriving where he is. | An explicit day/block loop-state model derives the current phase from **any** modality (a strength/walk/flexibility/breathwork-only day advances to post-training, not stuck pre-ride) and adds a first-class evening wind-down phase (replacing the 20:00 clock-only reorder); an orchestration seam answers "where is Mark + what's next" that Batches 45–47 consume; Home's primary-section selection reads the generalised phase with the existing per-state renders preserved; it is a **behaviour-preserving refactor** — no change to the verdict/fan/analysis engines and no new coaching logic; **deferrable** and sequenced after 45–47; no migration; backend pytest/ruff/mypy and frontend test/build/lint pass. Shipped in PR #68 (`d9060a1`): backend + frontend, no migration, no LLM. Scope chosen at `/batch-start` was **model + frontend, no rewire** — a pure `services/daily_loop_state.py` generalises the phase (`post_ride`→`post_training` off any modality; a first-class evening `wind_down`) and classifies the block (`consolidation` = end-of-block boundary), with `describe_loop_state` as the orchestration seam; the daily-loop payload gains a read-only `loopState` and the generalised `useDailyPhase`/`homeSections` read it (a non-ride day advances to its post-session read; evening leads with Tonight) — but the shipped Batch 45–47 wiring is left untouched, so the literal "45–47 consume the seam" is intentionally scoped to available-not-rewired. Behaviour-preserving beyond those two intended generalisations; no verdict/fan/analysis-engine change, no new coaching logic, `loopState` optional in the shared schema so pre-48 cached payloads still parse. CI green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=d9060a1`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #118. Full spec: `docs/designs/explicit-loop-model.md`. |

## Post-roadmap — Home & navigation UX batch plan

Batch ledger for the **Home & navigation UX read** (2026-07-03) — a world-class
UI/UX pass over the shipped app against the two-loop intent (**sleep→training**
daily, **block→block** per block) on the **"effortless, not empty"** principle,
now that the app is **push-first** (Batch 45). Same conventions as v1–v3 and the
passive-first plan: a batch is shipped only after `/closeout` finishes; leave the
row unstruck and `Planned` until started. Decision numbers assigned at
`/batch-start` (next free is **#119**).

**The finding.** The bones are strong — three-tabs-plus-More (`navConfig.ts`),
verdict-first Home (`VerdictHero`), and collapse-not-remove (Batch 37) are all
right and are **kept**. Two structural gaps remain: (1) the layout is
training-forward — the *sleep* half of the daily loop (last night, tonight's
projection, the Batch 27 bedroom-fan autopilot) has **no nav presence**, reachable
only via Home detail-links, while `Trends` (a browse-when-curious surface) holds a
primary slot; and (2) Home hasn't caught up to push-first — the one expanded
section is chosen by *time/phase*, so an actionable item (a pending coach
adjustment, an unlogged ride check-in) can sit **collapsed and invisible** in a
non-primary section, and the collapsed summaries don't signal that they need a tap.
A third, minor issue: several "More" labels are engineer-speak for a 57-year-old
non-technical daily user.

**Sequencing.** **Batch 49** (nav & IA refactor + Sleep hub) ships first — the
load-bearing change: primary tabs become Home / Week / Sleep, a `/sleep` hub
composes the existing sleep/bedroom surfaces (`/bedroom` is absorbed), and "More"
is re-tiered + de-jargoned. **Batch 50** (action-first Home) then makes Home's
expanded section + a new "Next" strip driven by *what needs Mark* over time-of-day,
with state-signalling collapsed summaries; it deep-links evening into the Batch 49
Sleep hub. **Batch 51** (desktop two-column) is an **optional, low-priority**
layout-only polish, sequenced last. All three are **frontend-only, no migration**.
Tiers per #19: 49 🟢 Mid (component/route work), 50 🔴 High (revises the shipped
Home selection model), 51 🟢 Mid (optional).

**Batch 49 (Navigation & IA refactor + Sleep hub)** gives the sleep loop a front
door. Primary tabs become **Home / Week / Sleep**; a new `/sleep` hub with a
**Last night | Tonight** split composes existing components (`MetricComparisonTable`,
`OvernightGlance`, `BedroomOvernightChart`, `SleepPrepBody`, `BedroomBody`) — no
new data — and `/bedroom` redirects into it; `Trends` is demoted to "More", which
is re-tiered into **For you** (Reviews, Trends, Holiday) / **Coaching** (New
training block, Experiments) / **Setup** (Coach memory, Handover, Settings) with
de-jargoned labels (Plan→Week, Tests→Experiments, Plan builder→New training block,
Coach state→Coach memory; "Handover" kept — Mark's own word). `TabBar`/`MoreMenu`/
`TopBar` render off `navConfig.ts` generically, so no structural nav change. No
migration. 🟢 Mid. Full spec: `docs/designs/navigation-sleep-hub.md`.

**Batch 50 (Action-first Home)** catches Home up to push-first. A deterministic,
payload-only `nextAction` resolver drives a single "Next" strip under the verdict
hero (pending coach change > unlogged ride check-in > daily check-in >
protect-sleep > all-clear), and the same resolver **overrides** the phase→primary
selection so the section holding the top action is the one expanded — regardless of
time of day. Collapsed section summaries gain a warning tone + dot when they need a
tap; the duplicated Today verdict badge is dropped; and (given Batch 49) evening
`tonight`/`bedroom` become compact cards that deep-link into the Sleep hub. All
signals already ride `/api/v1/daily-loop`; no backend change, no migration. 🔴 High.
Full spec: `docs/designs/action-first-home.md`.

**Batch 51 (Desktop two-column dashboard) — optional.** On `md+` viewports, lay the
ordered Home section list out as two lanes (act: verdict/Next/Today/After-your-ride;
context: Last night/Tonight/Bedroom) instead of one narrow column, using the same
section components; mobile stays the single stacked column and the section/action
model is unchanged. The real user is on a phone PWA, so this is polish, not a loop
gap — **deferrable, sequenced last**. No migration. 🟢 Mid. Full spec:
`docs/designs/desktop-dashboard-layout.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 49 — Navigation & IA refactor + Sleep hub~~ | 🟢 Mid | Shipped | 49.1 `lib/navConfig.ts`: new `PRIMARY_TABS` (Home / Week / Sleep) with `Trends` demoted; re-tier `MORE_GROUPS` into **For you** (Reviews, Trends, Holiday) / **Coaching** (New training block, Experiments) / **Setup** (Coach memory, Handover, Settings); `SECONDARY_PATHS` recomputes. `TabBar`/`MoreMenu`/`TopBar` render off it unchanged.<br>49.2 De-jargon labels: Plan→Week, Tests→Experiments, Plan builder→New training block, Coach state→Coach memory; keep "Handover" (Mark's own word).<br>49.3 New `pages/SleepPage.tsx` with a **Last night \| Tonight** segmented control; **extract** `SleepSnapshotBody` / `SleepPrepBody` / `BedroomBody` / `OvernightGlance` out of `DashboardPage.tsx` into reusable modules and move the `BedroomOvernightChart` mount off `BedroomPage` so both Home (compact) and `/sleep` (full) share them.<br>49.4 `App.tsx`: add `/sleep`; redirect `/bedroom` → `/sleep`; retire/thin `BedroomPage`.<br>49.5 Tests (nav renders + active-state, `SleepPage` composition, `/bedroom` redirect, Home unchanged after extraction) + green gates. | Give the daily loop's sleep half a real nav home and re-tier "More" so user-value destinations aren't buried next to developer tooling — so the nav tells the two-loop story instead of the training half alone, and the labels read for a non-technical daily user. | Primary tabs are Home / Week / Sleep; a `/sleep` hub with a Last night \| Tonight split composes the existing `MetricComparisonTable` / `OvernightGlance` / `BedroomOvernightChart` / `SleepPrepBody` / `BedroomBody` with **no new endpoint, data, or payload change**, and `/bedroom` redirects into it; `Trends` is one tap away under a re-tiered, de-jargoned "More"; `TabBar`/`MoreMenu`/`TopBar` are structurally unchanged (render off `navConfig.ts`); the sleep/bedroom body components are extracted from `DashboardPage` and Home still renders identically (no visual regression); "Handover" is kept as Mark's own vocabulary; no migration; web lint/test/build pass and the backend suite is untouched. Decision assigned at `/batch-start`. Full spec: `docs/designs/navigation-sleep-hub.md`. |
| ~~Batch 50 — Action-first Home~~ | 🔴 High | Shipped | 50.1 Pure payload-only `lib/homeActions.ts` — `nextAction(data, {phase, isEvening})` → `{label, to?, sectionKey?, tone}` from the deterministic priority list: pending coach change (`delivery.changed && isBike && !ignored`) > `postWorkoutAnalyses` with `postRideCheckIn == null` > `!manualEntry` > evening & `sleepProjection.tone === 'protect'` > all-clear.<br>50.2 A single **"Next" strip** under `VerdictHero` rendering that action (replacing the lone Check-in button; Check-in becomes the fallback).<br>50.3 `lib/homeSections.ts`: `primarySection` gains an override — `actionSection(nextAction) ?? primarySection(phase, {hasRide})` — so the pending-action section is the one expanded, regardless of phase; ordering/evening-float otherwise unchanged.<br>50.4 `CollapsibleSection` header takes a `tone` + `--fill-warning` dot; section-summary helpers return `{text, tone}` (Today→warning on a pending change; After-your-ride→warning on an unlogged check-in); drop the duplicated Today verdict badge; make `tonight`/`bedroom` bodies compact + deep-link to `/sleep`.<br>50.5 Tests (`homeActions` priority order; action override beats phase primary; collapsed warning dot/copy; verdict once; offline) + green gates. | Catch Home up to being push-first — make the expanded section and a new "Next" action strip driven by what needs Mark first and time-of-day second, so an actionable item (a pending adjustment, an unlogged ride check-in) is never stranded in a collapsed off-phase section, and a closed section can signal that it needs a tap. | A deterministic, payload-only `nextAction` resolver drives one "Next" strip under the verdict hero and **overrides** the Batch 37/48 phase→primary selection so the section holding the top pending action is the one expanded — regardless of time of day (action-over-phase always); collapsed summaries carry a warning tone + dot when they need action; the duplicated Today verdict badge is removed (VerdictHero canonical); evening `tonight`/`bedroom` deep-link into the Batch 49 Sleep hub; every signal already rides `/api/v1/daily-loop` so there is **no backend/payload change and no migration**; exactly one section stays expanded (need first, time second); the updated `homeActions`/`homeSections`/`DashboardPage` tests pass and web lint/build are clean with the backend suite untouched. Decision assigned at `/batch-start`. Full spec: `docs/designs/action-first-home.md`. |
| ~~Batch 51 — Desktop two-column dashboard~~ *(optional)* | 🟢 Mid | Shipped | 51.1 On `md+`, partition `orderedSections` into an **act** lane (verdict, Next strip, Today / After-your-ride) and a **context** lane (Last night, Tonight, Bedroom) via a responsive `grid-cols-1 md:grid-cols-2`, using the same section components; the primary/expanded section leads its lane.<br>51.2 Mobile stays the single stacked column; `homeSections`/`useDailyPhase`/`homeActions`/section bodies unchanged.<br>51.3 Tests (desktop renders two columns; mobile preserves the single column; same sections + expanded state as Batch 50) + green gates. | Use the horizontal space on desktop viewports — lay Home out as a two-column dashboard instead of one narrow column — without changing the mobile-first phone layout or the section/action model. | On `md+` the ordered Home sections render as an act lane + a context lane sharing the same components with the primary section leading its lane; the mobile single-column layout, the expanded/collapsed model, ordering, and the Batch 50 action logic are all unchanged; it is **layout-only** with no new content, no backend/payload change, and no migration; genuinely optional and deferrable (may be dropped at `/batch-start`); web lint/test/build pass and the backend suite is untouched. Decision assigned at `/batch-start`. Full spec: `docs/designs/desktop-dashboard-layout.md`. |

## Post-roadmap — Front-end premium UX batch plan

Batch ledger for the **front-end premium review** (2026-07-03) — a world-class
UI/UX pass over the shipped app. North-star agreed with Craig: **calm, premium
health companion** (quiet confidence, one clear action per screen, data on
demand); brand latitude **keep CheckMark, integrate it better**. Same conventions
as the prior plans: a batch is shipped only after `/closeout` finishes; leave the
row unstruck and `Planned` until started. Decision numbers assigned at
`/batch-start` (next free is **#122**). Overview + rationale + the annotated live
walk-through: `docs/designs/frontend-premium-review.md`.

**The finding.** The architecture is genuinely good — real token system, sound
3-tab IA, verdict-first Home, collapse-not-remove — and is **kept**. The gap is
**execution polish + brand presence**: on the live app the dark surface tiers sit
in one narrow near-black band so cards don't separate and Home reads as a flat
monotone wall (P0); form inputs are dark-on-dark and barely legible — an
accessibility problem for a 57-year-old daily user (P0); the verdict, the
product's heartbeat, is an uncrafted faint box (P0); and there is **no logomark
anywhere in the running app** even though a strong mark (a checkmark-as-heartbeat
on a teal→green tile with a verdict-gauge arc) already exists as the PWA/favicon
icon (P1). Net: it reads as competent developer-dark, not premium calm companion.

**Sequencing.** **Batch 52** (design foundations) ships first — the load-bearing
change: it rebuilds the surface/elevation ramp, the input/control tier, and the
label/type system at the token + primitive layer, so every later batch inherits a
premium base. **Batch 53** (branded verdict, hero & login) then makes the identity
felt — the existing mark brought in-app, a crafted `VerdictHero`, a mark-led login
splash, a top-bar mark, and the "Next" strip promoted to a real action band.
**Batch 54** (Home hierarchy & calm density) turns Home from a wall of equal
accordions into a curated brief (recede secondary sections, collapse the button
cluster to one primary + overflow, tighten the first screen, animate expand).
**Batch 55** (screen polish & states) applies the system across Sleep / Check-in /
Week and gives loading/empty/error states character. All four are **frontend-only,
no migration**. Tiers per #19: 52 🔴 High (system-wide visual foundation, design
judgment), 53 🔴 High (brand craft), 54 🟢 Mid (Home recomposition), 55 🟢 Mid
(applies the system).

**Batch 52 (Design foundations)** rebuilds the visual foundation. The dark
`--surface-*`/`--border*` tiers are re-spaced into a clear value ramp (the light
palette already separates well), the elevation/shadow tokens are refined, and the
input/control tier (`input`/`label`/`select`/`card`/`button` + the shared textarea
style) gets its own elevated fill, a stronger border, a clear focus ring, and
AA-legible placeholder/label. Mono-uppercase is pulled back to eyebrows / numerics
/ the wordmark; form labels become sentence-case. Token + primitive only — no
per-screen layout change, no endpoint/payload/migration. 🔴 High. Full spec:
`docs/designs/design-foundations.md`.

**Batch 53 (Branded verdict, hero & login)** weaves the identity through the app.
The existing (currently icon-only) mark is brought in-product via a `Logomark`
(login splash hero, top bar, verdict cue); `VerdictHero` is redesigned into a
crafted, elevated, branded heartbeat on the Batch 52 surfaces (Green/Amber/Red +
pending semantics unchanged); the login splash is mark-led with the wordmark
gradient actually applied; and the `NextActionStrip` becomes a full-width primary
action band tied under the verdict. Presentation only — no change to verdict logic
or `/api/v1/daily-loop`, no rename/rebrand, no migration. 🔴 High. Full spec:
`docs/designs/branded-verdict-hero-login.md`.

**Batch 54 (Home hierarchy & calm density)** turns Home into a curated brief. It
keeps collapse-not-remove (Batch 37), the action override (Batch 50), and the
desktop two-lane (Batch 51), but visually recedes the secondary/context sections
under a quiet "More detail" grouping so the eye lands on verdict → action band →
Today; the session card's five-button cluster collapses to one primary + a "More
options" overflow; the greeting folds into a compact lockup so the verdict sits
higher; summaries truncate on word boundaries; and section expand gets a subtle,
reduced-motion-aware animation. Every signal already rides `/api/v1/daily-loop`; no
payload change, no migration. 🟢 Mid. Full spec:
`docs/designs/home-hierarchy-calm-density.md`.

**Batch 55 (Screen polish & states)** finishes the pass. It applies the Batch 52
system across Check-in (legible inputs, sentence-case labels, lower density),
Sleep (surfaces, the mostly-empty "vs your age" column, the heavy evidence
disclosure), and Week (surfaces, card nesting), and adds a shared, on-brand
empty/error/offline pattern (say what happened + a clear recovery CTA) in place of
the generic "please try again" cards. Presentation only — no data/endpoint/
analysis-engine change, no migration. 🟢 Mid. Full spec:
`docs/designs/screen-polish-states.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 52 — Design foundations~~ | 🔴 High | Shipped | 52.1 `src/index.css` (both palettes) + `src/theme/tokens.ts` mirror: re-space the dark `--surface-*`/`--border*` tiers into a clear value ramp; refine the elevation/shadow tokens; add any control-fill/label vars; keep the pre-mount theme script + the on-primary/on-accent AA notes.<br>52.2 Input/control tier: `components/ui/input.tsx`, `label.tsx`, `select.tsx`, `card.tsx`, `button.tsx` adopt elevated fills, a stronger idle border, a clear focus ring (reuse `shadow-glow`), and higher-contrast placeholder/label; factor the two inline `textareaClassName` copies (`DashboardPage`, `CheckInPage`) to the shared style.<br>52.3 Label/type system: reserve mono-uppercase for eyebrow/kicker/numeric/wordmark; form `Label` → sentence-case + legible; document the type scale.<br>52.4 `tailwind.config.ts`: expose any new tokens; hold light + dark at WCAG AA.<br>52.5 Verification: existing web vitest green (update only pinned-class assertions), AA contrast spot-check, `tsc`/lint/build clean, backend untouched, headless-preview confirm (Home/Check-in/Sleep, dark+light). | Lift the design system from developer-dark to calm premium at the token + primitive layer — so cards separate, controls are legible, and Batches 53–55 inherit a premium base — without any per-screen layout change. | The dark surface tiers have clear, deliberate value steps (the light palette's separation is kept); inputs have their own elevated fill, a focus ring, and AA-legible placeholder/label; mono-uppercase is confined to eyebrows/numerics/wordmark and all form labels are sentence-case; the change is **token + primitive only** with no endpoint/payload/migration and no per-screen layout change; the existing web suite passes with only pinned-class assertions updated; `tsc --noEmit`/lint/build are clean under Node 20 and the backend suite is untouched. Decision #122. Full spec: `docs/designs/design-foundations.md`. |
| ~~Batch 53 — Branded verdict, hero & login~~ | 🔴 High | Shipped | 53.1 `components/Brand.tsx` (+ a `Logomark`/mark export) rendering the existing `public/brand/checkmark-icon-primary.svg` mark (generated from `generate-icons.mjs` — do not hand-edit).<br>53.2 `components/VerdictHero.tsx`: redesign into a crafted, elevated, branded heartbeat on the Batch 52 surfaces — a branded ring echoing the verdict-gauge arc; Green/Amber/Red + pending semantics and `verdictCopy` unchanged.<br>53.3 `pages/LoginPage.tsx`: mark-led splash, tighter composition, the wordmark gradient applied; invite-only/PIN flow unchanged.<br>53.4 `components/TopBar.tsx`: the mark in the mobile + desktop lockups.<br>53.5 `pages/DashboardPage.tsx`: `NextActionStrip` → a full-width primary action band tied under the verdict; give the brass accent a defined secondary role (one accent/view).<br>53.6 Tests (`VerdictHero` per state incl. pending; `LoginPage` mark + toggle; `DashboardPage` action band per state + top-bar mark + offline) + green gates. | Weave the CheckMark identity through the app — bring the existing (icon-only) mark in-product, make the verdict a crafted branded heartbeat, elevate the login splash, add the top-bar mark, and promote the "Next" strip to a real action band — so the brand is felt, not just labelled. | Shipped via PR #74 / squash `c84945e`: the existing mark renders in-app (login hero, top bar, verdict cue) at defined sizes; `VerdictHero` is elevated and branded with the Green/Amber/Red + pending logic and copy unchanged; the login splash is mark-led with the wordmark gradient; the "Next" strip is a full-width primary action band; brass is held to the default action band/CTA while warning/all-clear stay semantic; it is **presentation only** — no verdict-logic, endpoint, payload, or migration change and no rename/rebrand (CheckMark + emerald/teal/brass kept); focused and full web tests pass, `tsc`/lint/build are clean with the backend untouched; PR CI and production smoke are green. Decision #123. Full spec: `docs/designs/branded-verdict-hero-login.md`. |
| ~~Batch 54 — Home hierarchy & calm density~~ | 🟢 Mid | Shipped | 54.1 `pages/DashboardPage.tsx`: fold the greeting + date into a compact lockup so the verdict sits higher; collapse the `WorkoutRow` five-button cluster to one primary (Approve, or Edit) + one secondary + a "More options" overflow (reuse `dropdown-menu`) for Manual edit / Swap day / Skip / Ignore.<br>54.2 `components/CollapsibleSection.tsx`: a quieter "secondary" visual variant for the context sections; a reduced-motion-aware expand/collapse animation.<br>54.3 `lib/homeSections.ts`: any grouping/order for the "More detail" divider — keep the one-expanded model, evening float, and Batch 51 lanes intact.<br>54.4 A word-boundary truncation helper in `lib/` for section summaries.<br>54.5 Tests (`DashboardPage`: overflow exposes the moved actions, primary stays visible, all sections render with correct expanded state, verdict once, offline; `homeSections`: ordering/float/lanes/action-override preserved) + green gates. | Turn Home from a wall of equal accordions into a curated brief — clear reading order (verdict → action → Today) with the rest receding, one primary action per card, a tighter first screen, word-boundary truncation, and a subtle expand animation. | Shipped via PR #75 / squash `8fb90a2`: Home now reads verdict → action band → Today with the secondary/context sections visually receded under a quiet "More detail" grouping (still present — collapse-not-remove kept); the session card shows one primary + one secondary with the rest in a "More options" overflow; the greeting is a compact lockup; summaries truncate on word boundaries; section expand animates and honours `prefers-reduced-motion`; the one-expanded model, evening float, action override, and Batch 51 desktop lanes are unchanged; **no payload/endpoint/migration change**; local web tests/lint/build, PR CI, and production smoke on the merge SHA are green. Decision #124. Full spec: `docs/designs/home-hierarchy-calm-density.md`. |
| ~~Batch 55 — Screen polish & states~~ | 🟢 Mid | Shipped | 55.1 `pages/CheckInPage.tsx`: adopt the Batch 52 inputs + sentence-case labels; lower density; a clearer per-section save model (endpoints unchanged).<br>55.2 `pages/SleepPage.tsx` + `components/MetricComparisonTable.tsx` + `components/SleepPrepBody.tsx`: apply the surfaces/type; fold/redesign the mostly-empty "vs your age" column; lighten the evidence disclosure; tidy footnotes (no data change).<br>55.3 `pages/WeekAheadPage.tsx`: apply surfaces; reduce card nesting; schedule/move flow unchanged.<br>55.4 A shared `EmptyState`/`ErrorState` component (say what happened + a recovery CTA, CDS voice) adopted in the Home/Sleep/Week/Check-in error/empty branches.<br>55.5 Tests (`CheckInPage`/`SleepPage`/`WeekAheadPage` pass with the new markup; the empty/error branches render the shared component) + green gates. | Apply the premium system across Sleep / Check-in / Week and give loading/empty/error/offline states character and a clear recovery action — finishing the calm-premium pass so the whole app is consistent. | Check-in, Sleep, and Week render on the Batch 52 surfaces with legible inputs/sentence-case labels, the "vs your age" column and evidence disclosure redesigned (presentation only), and reduced card nesting; a shared on-brand empty/error/offline pattern with a recovery CTA replaces the generic "please try again" cards; there is **no data, endpoint, analysis-engine, or migration change** and no nav/IA change; the page tests pass and `tsc`/lint/build are clean under Node 20 with the backend untouched. Shipped via PR #76 / squash `aa2c7e0`. Decision #125. Full spec: `docs/designs/screen-polish-states.md`. |
| ~~Batch 56 — Verdict calibration & personal baselines~~ | 🔴 High | Shipped | 56.1 Soft-sleep recovery override in `morning_analysis._morning_verdict`: age-adjusted sleep 60-73 may stay Green only when HRV is clean, RHR is inside Mark's personal band, readiness is not low/poor, and subjective is absent or >=5; Red floor unchanged.<br>56.2 Morning packet carries personal baseline bands, structured `trainingSchedule`, and a compact `yesterdayLoad` summary with linked post-session analyses.<br>56.3 Reviews/trends carry personal baseline bands; readiness trend now needs both >5% movement and >4 absolute points before it is called a trend.<br>56.4 Add `training_schedule` as a seeded KB section (Mon/Fri rest days) and add `readiness_score` to DB-history baselines.<br>56.5 Tests + backend lint/type gates. | Calibrate the coach to Mark's normal recovery instead of generic thresholds: good HRV/RHR/readiness can hold a mediocre sleep night; review language judges numbers against his baselines; and the engine knows his rest-day routine plus yesterday's work. | Shipped via PR #78 / squash `20437e8`: no migration, no endpoint change, Red-never-VO2 untouched, baseline rows remain provenance-tagged. PR CI went green across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, web/shared vitest, and the Vercel preview; production smoke verified Railway + Vercel same-origin `/api/v1/health` on the merge SHA, web `/` and `/login` 200, and unauthenticated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #129. Full spec: `docs/designs/coaching-calibration-and-data-truth.md`. |
| ~~Batch 57 — Data truth in reviews/trends~~ | 🔴 High | Shipped | 57.1 Review packets expose coverage/sample counts for sleep, recovery, morning verdicts, thermal, activities, plan rows, and adherence capture.<br>57.2 Review packets/API expose absent-vs-zero source state for active plan rows and tracked strength activities; prompts treat those flags as binding so missing inputs are not narrated as "stopped" or "zero sessions".<br>57.3 Review trend evidence carries first-half → second-half means, deltas, and sample counts; review/trend prompts require from→to numbers and sample counts for every trend claim.<br>57.4 Reviews page surfaces coverage, trend basis, and source-state caveats; shared schemas parse the expanded contract.<br>57.5 Tests + backend/frontend lint/type gates. | Make reviews and trends honest about what the app actually knows: incomplete coverage is visible, missing sources are not narrated as behaviour, year-on-year gaps remain "insufficient history", and trend prose cites the concrete numbers behind the claim. | Shipped via PR #79 / squash `4e0497a`: no migration, no new endpoint, no prod backfill run inside the batch. Existing diagnosis SQL and the resumable Garmin history backfill remain the operational path for any future missing coverage; Step 0 already showed prod daily metrics/sleep/activities cover 2025-06-24 → 2026-07-05. Local focused tests passed for reviews/trends backend, Reviews/Trends web, and shared schemas; lint/type gates clean except known web Fast-Refresh warnings. PR CI and Vercel preview went green; production smoke verified Railway + Vercel same-origin `/api/v1/health` on the merge SHA, web `/` and `/reviews` 200, and unauthenticated `GET /api/v1/reviews/weekly` plus `GET /api/v1/trends/narrative?bucket=month` 401 direct and via Vercel. Decision #130. Full spec: `docs/designs/coaching-calibration-and-data-truth.md`. |
| ~~Batch 58 — Sleep-stage age-comparison table~~ | 🟢 Mid | Shipped | 58.1 Add sleep-stage age norms to `age_norms.py` for duration plus Deep/Light/REM/Awake stage mix and Restless count.<br>58.2 Extend `build_age_comparison` / morning-analysis packet wiring so the existing `ageComparison` payload keeps its compact headline rows and adds a sibling `sleepRows` group for the deeper Sleep-page read.<br>58.3 Render the dedicated `/sleep` table (Duration/Deep/Light/REM/Awake/Restless) with the shared payload, keeping Home's compact `MetricComparisonTable` unchanged.<br>58.4 Shared schema/test updates and focused backend/frontend lint/type gates. | Add the clean additive feature Mark asked for: show how his sleep duration and stage balance compare with the typical person his age, without changing verdict logic or introducing a new endpoint. | Shipped via PR #80 / squash `9c96bb6`, with a narrow follow-up `chore: format batch 58 age norms` (`080e763`) to restore the repo-wide `ruff format --check` gate on `main`. No migration, no new endpoint, no verdict/prompt change. Stage comparisons use percentages of measured sleep rather than raw minutes so the comparison reflects sleep shape across different total durations. Main CI went green after the formatting follow-up across backend pytest/ruff/mypy, Alembic migration check, security audit, web build, and web/shared tests; production smoke verified Railway + Vercel same-origin `/api/v1/health` on final SHA `080e763`, web `/` and `/sleep` 200, and unauthenticated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #131. Full spec: `docs/designs/coaching-calibration-and-data-truth.md`. |
| ~~Batch 59 — Chronic-pattern suggestions~~ | 🔴 High | Shipped | 59.1 Deterministic pattern-detector for metrics chronically below age norm and/or personal baseline over N weeks (REM, deep, duration, related sleep/recovery markers).<br>59.2 Grounded suggestion mapping that turns those chronic patterns into concrete actions, prioritised by `insights.drivers` so the advice targets Mark's measured movers.<br>59.3 Surface the suggestions on Sleep/Home with explicit evidence windows and graceful insufficient-history handling.<br>59.4 Tests + backend/frontend lint/type gates. | Turn repeated sleep-pattern misses into grounded actions instead of leaving the Sleep surface as a snapshot only. | Implemented as optional `chronicSuggestions` on `/api/v1/daily-loop`: a pure 4-week detector requires 21 observed sleep nights, reports `insufficient_history` / `clear` / `active`, compares stage/duration misses against age norms and related recovery markers against personal baseline bands, and maps them to sleep-protocol actions prioritised by measured `insights.drivers`. Home and `/sleep` both render the shared `ChronicSuggestionsCard` through `SleepSnapshotBody`. No migration, no new endpoint, no `analyses` row, no LLM call, no verdict/delivery-rule change. Local gates passed: backend chronic/age-norm pytest, touched backend ruff/format/mypy, shared schema test/typecheck, focused SleepPage vitest, web typecheck/lint/build. Shipped PR #81, squash `fb90e3e`, prod-verified. Decision #132. |

## Post-roadmap — 2026-07-06 Mark feedback batch plan

Craig relayed three snags from Mark on 2026-07-06: (1) a ride's Today-card row kept
showing the approve/upload/ignore controls after the ride was done, with the read in
a separate "After your ride" section — it should show the read on the session's own
row, per-workout (he may finish 1 of 2); (2) no way to log the morning check-in from
the Sleep page; (3) completed workouts could still be moved in the plan. Verified
against the code before scoping: `PlannedWorkout.status` never became `completed` (a
dead enum value) and there was no stored workout↔analysis link, so this needs a
persisted completion signal, not a pure front-end tweak. Two calls settled at
`/batch-start` (Craig): **persist completion** (migration + status flip, not a
serializer-derived flag) and **compact + link** for the Home display (a done badge +
tomorrow-impact + check-in, full read behind a disclosure). Decision **#134**.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 60 — Completed-workout consolidation~~ | 🔴 High | Shipped | 60.1 Persist completion: migration `012` adds `analyses.planned_workout_id`; new `services/workout_completion.py` matches an activity to the day's planned session by local date + category; ride `generate_and_store` links the `analyses` row and flips the matched bike `planned_workouts` row to `status='completed'`; daily-loop exposes `plannedWorkoutId` on the ride analysis.<br>60.2 Home: a completed session's Today-card row shows a compact ✓ read (tomorrow-impact + ride check-in + "View analysis" disclosure) instead of the approve/upload/edit/swap/skip controls; the standalone "After your ride" section is kept only for *unplanned* rides (`plannedWorkoutId == null`); the `log-ride` next-action points a matched ride at the Today card.<br>60.3 Move-lock: `swap_day` 409s a completed source *or* target; the Plan view hides Move + shows a "Done" badge; both key off the already-exposed `status`.<br>60.4 Sleep: a "Morning check-in" link at the foot of `/sleep` → the existing `/check-in`.<br>60.5 Tests + backend/frontend lint/type gates.<br>60.6 Follow-up (revises #127): the morning check-in is made **optional** — "review last night" + "check in" collapse into one step; the blocking `!manualEntry` rung is dropped from `homeActions` (both ladders) and `/sleep` frames the check-in as an optional card. | Close Mark's 2026-07-06 feedback: a done ride shows its read on its own session row (not lingering ride controls or a separate section), completed workouts can't be moved, and the morning check-in is reachable from Sleep (and no longer nagged). | Ride-scoped completion (strength/flexibility/walk deferred to a fast-follow on the same helper); no historical backfill (completion matters for today's Home + the forward plan window; today's rides flip on the next hourly poll); `swap_day` refuses a completed source/target; Home completed row shows the read not the controls, with the standalone section only for unplanned rides; Sleep→check-in link present + optional; tests ship (backend DB tests for the flip/link + swap 409, shared `plannedWorkoutId`, web completed-row / move-lock / sleep-link / homeActions). **Shipped via PR #83 / squash `23de7e7`; production verified on the merge SHA — Railway + Vercel same-origin `/api/v1/health` returned `sha=23de7e7944a034490d4514cc9ef52013e31a23d7`, web `/` 200, `GET /api/v1/daily-loop` 401 unauthenticated direct and via Vercel.** Local gates: backend pytest 486 passed / 152 skipped + ruff/format/mypy clean, shared 11 + typecheck, web 162 + tsc/lint/build clean. Decision #134 (the check-in-optional follow-up revises #127). |

## Post-roadmap — 2026-07-06 sleep-score age-norm batch plan

Mark's second 2026-07-06 thread (separate from the completed-workout snags in
Batch 60): the "typical" values shown next to his sleep are Garmin's young-adult
lab targets, not norms for a 57-year-old, so age-normal sleep is scored/flagged as
poor. He backed this with a Copilot "age-adjusted norms" table + sources. Verified
against the code before scoping: the app's **age-adjusted sleep score is a flat
Garmin `+4`** (`garmin_sync.py:428`, `morning_analysis.py:747`) that inherits
Garmin's per-stage young-adult penalty, and the Sleep-page **"Typical" column is a
single population *average*** that warns below the midpoint — so his complaint is
right for the score and a presentation problem for the column. Garmin's own
`sleepScores` payload (already stored in `Sleep.factors_json`) exposes the
young-adult target bands (e.g. REM optimal **21–31%**), which both proves the
point and gives us the structure to recompute against age bands. Copilot's
*direction* is right but several of its numbers aren't adoptable (REM 12–17% too
low vs Ohayon 2004; Deep 20–30% too high; Restless/Awake are device-proprietary;
some HR/HRV rows are athlete-Mark's values, not 57-yr norms). Scope chosen with
Craig: **full fix in one batch** — healthy age *bands* + a *real* age-adjusted
score (not +4). Proposed Decision **#135** (revisits #14, extends #131). Full
spec: `docs/designs/age-adjusted-sleep-norms.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 61 — Age-adjusted sleep norms & real age-adjusted score~~ | 🔴 High | Shipped | 61.1 `services/age_norms.py`: turn per-metric single averages into `(low, high)` **healthy age bands** (sex × decade) for the sleep-stage rows; band-aware `_classify` (in-band = neutral/good, warn only meaningfully outside, edge tolerance); demote **Restless** to descriptive-only (Garmin-proprietary, no defensible population band); general-fitness rows (VO₂max/RHR/HRV) unchanged.<br>61.2 New pure `services/sleep_scoring.py`: recompute the age-adjusted score by re-qualifying the age-sensitive components (REM/Deep/Light/Awake) against age bands using Garmin's own `factors_json.sleepScores` structure, with a **calibration guard** (all-Garmin-optimal ≈ Garmin score) and a **downgrade guard** (never below raw). Computed at analysis time from stored inputs — no migration, no re-sync; `garmin_sync.py:428`'s inline `+4` removed; `morning_analysis`/DB-history baselines/`reviews`/`chronic_patterns` call the one function when profile context exists and fall back to stored history when it does not.<br>61.3 UI: rename "Typical" → **"Healthy range (50–59)"** showing the band; add an opt-in quiet **"Garmin target"** contrast on the Sleep-page deep table (`SleepStageAgeTable.tsx`, `MetricComparisonTable.tsx`); keep the guide/not-medical footnote and Batch 55 restraint.<br>61.4 Prompt line so the LLM narrates against the age band not Garmin's qualifier; shared Zod schema picks up band low/high + Garmin target fields; bump `PROMPT_VERSION`.<br>61.5 Tests + real-night safety re-verification + gates. | Stop scoring and flagging Mark's age-normal sleep against young-adult targets: show a healthy range for his age (not a point average or Garmin's number) and make the age-adjusted score a genuine recompute instead of a flat +4. | Shipped via PR #84 / squash `7936fcb`; no migration, no new endpoint, forward-only (no historical score backfill run at closeout). Score can only ease a young-adult penalty relative to Garmin raw; Red floor, #133 soft-sleep override, Poor-readiness cautious gate, and Red-never-VO2 unchanged. **Verdict-interaction safety re-verified against Mark's real nights** (`railway run`, read-only): 4 Poor-readiness days that crossed the sleep `>=74` threshold resolve Amber, not Green. Tests shipped: `test_age_norms.py` band cases, new `test_sleep_scoring.py`, `test_morning_analysis.py` Poor-readiness guard, shared band/target schema, web band/rename/contrast render; local backend full pytest **502/152**, ruff/format/mypy clean, shared **12** + typecheck, web **163** + tsc/lint/build under Node 20. PR #84 CI green in both push and PR contexts after DB-backed morning-analysis expectations were aligned; production verified on merge SHA `7936fcbb0cb7e48e7ff03f6eeadd81f81c2e3523` — Railway + Vercel same-origin `/api/v1/health` returned the SHA, web `/` and `/sleep` 200, and unauthenticated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #135. Full spec: `docs/designs/age-adjusted-sleep-norms.md`. |

## Post-roadmap — 2026-07-07 first-open performance batch plan

Mark's report: "data is slow to load when I first open the app." Measured against
the live system before scoping so we fix the real cause. Warm production timings
(2026-07-07, unauthenticated) show the container is **not** cold-starting —
Railway `/api/v1/health` returns in ~0.10 s direct and ~0.11 s through the Vercel
proxy, because the in-process APScheduler keeps the process alive. The real causes
are two things that only line up on a fresh launch: (1) the client has **no
persisted React Query cache** and `staleTime` defaults to 0 (`apps/web/src/App.tsx`),
so a cold open blocks the whole Home screen on one round-trip behind an empty cache
and shows a full skeleton; and (2) that one blocking request — `GET
/api/v1/daily-loop` — is the fattest endpoint in the app, recomputing a **120-day**
driver correlation (`services/insights.py`) plus a chronic-pattern scan and running
~15 **sequential** DB queries on every load (`_envelope`, `get_snapshot`). Scope
chosen with Craig: **fix in one batch** — persist the cache so the last brief paints
instantly, move the deterministic correlation maths off the hot path into the daily
morning sync, thin/parallelize the snapshot round-trips, and verify Railway↔Supabase
region colocation. Proposed Decision **#136**. Full spec:
`docs/designs/first-open-performance.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 62 — First-open latency: persist cache & thin daily-loop~~ | 🔴 High | Shipped | 62.1 Persist the React Query cache to `localStorage` (v5 `persist-client` + `query-sync-storage-persister`): `PersistQueryClientProvider` in `App.tsx`, `staleTime`≈60 s on `useDailyLoop`, `buster`=build SHA, `maxAge` 24 h, `dehydrateOptions` persists only the daily-loop key, and login/activate/unlock/logout clear the persisted client alongside the existing `queryClient.clear()`.<br>62.2 Precompute `InsightsService.drivers()` + `ChronicPatternSuggestionService.suggestions()` once in `run_morning_weather_sync` and store via the existing `analyses` `driver_correlation` audit row (no migration); `_envelope` reads today's stored packet and falls back to live compute when missing/stale.<br>62.3 Cut & parallelize the snapshot's ~15 serial round-trips: collapse the four post-`{workout,flexibility,strength,walk}` analysis SELECTs into one `analysis_type IN (...)` query partitioned in Python, and run the three briefs (strength/walking/breathwork) concurrently via `asyncio.gather`, each on its own short-lived `AsyncSessionLocal()` (one session can't multiplex).<br>62.4 Verify Railway↔Supabase region colocation (a config-only win if mismatched) and add a `SELECT 1` warm-ping to an existing 15-min job so the first open rarely hits a cold pooled connection.<br>62.5 Before/after authenticated daily-loop timing, web persistence + backend read-through/parallel-payload tests, and gates. | Make the first open paint the last brief instantly and cut authenticated `GET /api/v1/daily-loop` server time, without changing any decision output. | Warm server confirmed (not a cold-start problem); first open with a prior cached brief renders content immediately with no full skeleton (background refetch swaps fresh data in place); authenticated daily-loop server time materially reduced against the 62.5 baseline; daily-loop payload byte-identical for a fixed DB state (verdict / #133 / #134 / #135 invariants untouched); persisted cache bounded by `maxAge` and cleared on logout; no migration; backend pytest/ruff/mypy, shared typecheck, web vitest/tsc/lint/build (Node 20) all pass; closeout prod smoke on the merge SHA. Decision #136 (assigned at `/batch-start`). Full spec: `docs/designs/first-open-performance.md`. |

## Post-roadmap — 2026-07-07 summary-feedback batch plan

Mark's 2026-07-07 ask: let him rate the accuracy of any summary the app generates —
sleep, workout, the morning verdict, or the coach's suggested workout edits — and
tell it when it's wrong. Challenged and reframed with Craig before scoping: at n=1 a
star-rating pipeline has no aggregate to learn from, so the value is the *correction*
(a one-line "I actually slept fine"), not the score, and the highest-value move is
capturing his subjective read cheaply *before* the verdict as well as correcting it
after. Mark agreed with the combination and named the real constraint — a quick
morning check-in "will be significantly less than what I'm doing now": the input must
undercut his current manual routine, not add to it ("a guide, not a magic wand").

Verified against the code before scoping: (1) every AI summary is already a single
`analyses` row (`coaching.py:413`) with a stable UUID + `analysis_type` (`morning`,
`post_workout`/`_walk`/`_strength`/`_flexibility`, `weekly_review`, `monthly_review`,
`seasonal_trend`) + `output_markdown`, so one feedback primitive keyed to
`analysis_id` covers the whole app; (2) the subjective-read→verdict loop already
exists — the manual-entry PUT fires `regenerate_after_morning_checkin`
(`daily_loop.py:1123`), so lightening the check-in is a UX job not new plumbing, and
the current `/check-in` (`CheckInPage.tsx`) is the heavy multi-card form Mark is
escaping; (3) no feedback/rating table exists; (4) the `/sleep` review is computed
client-side (`sleepReview.ts`) and is *not* an `analyses` row, so it has no server id
to attach feedback to and defers (piggybacks on the day's morning analysis).

Scope chosen with Craig: **two batches** — 63 lightens the check-in first (Mark's
felt pain), 64 adds the rate-&-correct primitive. Corrections **feed the next read**
(included in the next generation's `context_packet`, not auto-regenerated). Ratings
use **two axes by content type** — summaries: accuracy; suggested edits: agreement.
No rating dashboard/aggregation (n=1). Proposed Decision **#137** (assigned at
`/batch-start`). Full spec: `docs/designs/summary-feedback.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 63 — Lighten the morning check-in (fast path)~~ | 🟢 Mid | Shipped | 63.1 Make the `/check-in` default a **quick check-in**: a tap button-group for overall (writes `manual_entries.subjective_score`) + 2–3 one-tap chips (slept well / energy / niggle) folded into `feel`/`notes`; the BP, supplements/food, and per-workout adherence cards move behind a "More" disclosure. Reuses `manualEntryInputSchema` + `PUT /api/v1/daily-loop/{date}/manual-entry` — no new endpoint, no migration.<br>63.2 Confirm the quick save still fires `regenerate_after_morning_checkin` (`daily_loop.py:1123`, inherited via the manual-entry PUT) so the verdict + eased ride still reshape from his read.<br>63.3 Home: a one-tap quick-check-in affordance; stays optional (Batch 60 / #134) — no blocking `!manualEntry` rung reintroduced.<br>63.4 Web tests (quick-form render/save, chip→column mapping, regen still fires) + backend/shared/web gates. | Cut the daily check-in from a multi-field form to a few taps so it undercuts Mark's current manual routine ("significantly less than what I'm doing now"), without losing the check-in→verdict regeneration. | Shipped in PR #87 (squash `bc17f59`): the `/check-in` default is a 5-button "Overall" tap group (`subjectiveScore` 2/4/6/8/10) plus three one-tap chips (Slept well / Low energy → `feel`, Niggle → `notes`, toggled as a comma-joined token); BP, the free-text feel/notes inputs, supplements/food, and per-workout adherence sit behind a reused `CollapsibleSection` "More", collapsed by default and never required; no backend/shared file changed, so the existing `manualEntryInputSchema` + `PUT /manual-entry` endpoint and its `regenerate_after_morning_checkin` call are unchanged and still fire on a quick save; the check-in was already optional (Batch 60/#134) so no Home change was needed. CI green across all 7 jobs (backend pytest/ruff/mypy, Alembic migration check, security audit, web build, web+shared vitest) in both push and PR contexts, plus a Vercel preview; squash-merged to `main` as `bc17f59`. **Production HTTP smoke could not run from this environment** — the agent egress policy denies CONNECT (403) to `api-production-e2bc7.up.railway.app` and `garmin-coach.vercel.app` (confirmed via the agent-proxy status, not transient); Craig to confirm the standard merge-SHA smoke from a permitted network. Part of Decision #137. |
| ~~Batch 64 — Rate & correct any summary (feedback primitive)~~ | 🔴 High | Shipped | 64.1 New `feedback` table + Alembic migration `013`: `id` uuid PK, `user_id`→`profiles` (CASCADE), `analysis_id`→`analyses` (CASCADE), `kind` (`summary`\|`suggestion`), `rating` (short per-axis string), `correction_text` Text nullable, `created_utc`; unique `(user_id, analysis_id)` for upsert; index `(user_id, analysis_id)`.<br>64.2 `PUT /api/v1/analyses/{analysis_id}/feedback` (upsert) in the `{data, meta, errors}` envelope, **user-scoped** (404/403 if the analysis isn't the caller's); `feedbackInputSchema`/`feedbackSchema` in `packages/shared`; daily-loop serializers surface any existing feedback on each analysis so the widget shows current state.<br>64.3 Reusable `FeedbackControl` web component: one-tap axis buttons (accuracy for summaries, agreement for suggested edits), a negative tap discloses an optional "what did we get wrong?" textarea; mounted on `VerdictHero`, the post-session read cards, and `/reviews`; `kind` set per surface.<br>64.4 Feed corrections forward: the morning + post-workout context-packet assemblers (`morning_analysis.py`) include the most recent corrections for that user so the next read can acknowledge/adjust; bump the affected `PROMPT_VERSION`.<br>64.5 Tests (migration/model, endpoint upsert + user-scoping, shared schema, widget render/axis/correction reveal, packet-includes-recent-corrections) + gates. | Let Mark rate any AI summary and, when it's off, correct it in his own words; persist it user-scoped and let recent corrections shape the next read — the "guide, not magic wand" loop. | Any `analyses`-backed summary (verdict, post-`workout`/`walk`/`strength`/`flexibility`, `weekly_review`/`monthly_review`/`seasonal_trend`) rates in one tap with an optional correction; one row per `(user, analysis)` via upsert; a user can only rate their own analysis; recent corrections appear in the next generation's `context_packet` and the prompt may act on them; verdict / #133 / #134 / #135 invariants + delivery rules unchanged; **no rating dashboard/aggregation** (n=1); `/sleep` client-side review deferred (piggybacks on the day's morning analysis); migration `013`; new endpoint; backend pytest/ruff/mypy, shared typecheck, web vitest/tsc/lint/build (Node 20) pass; closeout prod smoke on merge SHA. Decision #137 (assigned at `/batch-start`). Full spec: `docs/designs/summary-feedback.md`. |

## Post-roadmap — 2026-07-07 workout-scheduling batch plan

Mark's 2026-07-07 feedback from running the week entirely in the app: (1) he agreed
with the app that a poor-sleep day was no day for the scheduled VO2 and **juggled the
week around by hand** — his preference is to **swap sessions across the week, not
soften them** — and it "worked perfectly"; (2) the app has **Bodyweight on Monday and
Dumbbells on Saturday, the wrong way round**; (3) Saturday's ride and dumbbells are
**one combined workout**, so moving the Saturday Z2 to Thursday dragged the dumbbells
too — cycling and strength must be **separate so they move independently**.

Verified against the code before scoping: (1) points 2 and 3 share one root cause —
every build-week Saturday in `apps/api/data/plans/plan_no2.json` is a single
`bike_endurance` row titled `"Z2 + Neuromuscular + Dumbbells"` (the strength is prose
welded onto a ride, invisible to the `workout_type`-keyed day category,
`workout_categories.py:40`); (2) **multi-workout days already work end-to-end** — the
schedule returns a list per day (`plan_actions.py:33`) and the Plan page renders one
Move-able `WorkoutRow` per workout (`WeekAheadPage.tsx:208`) via the existing
version-as-slot on `(user_id, workout_date, version)` — only the importer (flat
`version=1`, `plan_import.py:201`) and swap-target detection (`_active_workout_on`
`.limit(1)`, `executable_coaching.py:898`) still assume one workout per day; (3) a
**swap-not-soften engine already exists but is unreachable** — `weekly_restructure.py`
defers hard sessions on fatigue via `GET/POST /api/v1/restructure/*` and no web page
consumes it, while the morning verdict only ever recommends softening
(`_plan_adjustments`, `morning_analysis.py:1211`).

Scope chosen with Craig: **two batches** — 65 splits the row + corrects the Mon/Sat
mapping (the visible defect Mark hit), 66 makes the coach recommend and offer a
week-swap instead of only softening (surfacing the engine that already exists).
Neither needs a migration. Proposed Decisions **#138** (65) and **#139** (66),
assigned at `/batch-start`. Full spec: `docs/designs/workout-scheduling-feedback.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 65 — Separate cycling & strength + correct Mon/Sat mapping~~ | 🔴 High | Shipped | 65.1 Rewrite `apps/api/data/plans/plan_no2.json`: Monday strength → the **Dumbbell** session (~22 min full-body circuit from `Dumbbell & Bodyweight 19.06.26.docx`); split each build-week Saturday into **two entries for the same `dow`** — a cycle (`bike_endurance`, "Z2 + Neuromuscular", existing bike steps minus the "Then: 20min dumbbells" tail) and a **Bodyweight** strength session — so Dumbbells↔Bodyweight swap days and Saturday's ride & strength become distinct rows.<br>65.2 `import_plan` assigns **incrementing versions per date** (cycle v1, strength v2, deterministic order) instead of a flat `version=1`, so same-day rows satisfy the `(user_id, workout_date, version)` constraint; `build_plan_rows` already emits one row per entry.<br>65.3 Scope swap-target detection (`_active_workout_on`/`swap_day`) to the **same category as the source** so moving a ride never drags a same-day strength row and swapping two ride days leaves both days' strength in place; keep the Batch 60 completed-session 409 guards.<br>65.4 Re-import to prod **forward from next Monday** (`start_date` override) so Mark's already-hand-juggled current week is preserved, then `reconcile_deliveries` re-syncs Zwift for the new bike rows — an explicit closeout step, not a blind re-run.<br>65.5 Tests (importer versioning + split-day, swap-engine category-scoped multi-workout, plan-JSON shape, web split-day render guard) + gates. | Split the welded Saturday ride+dumbbells into two independently-movable sessions and put the right strength on the right day (Dumbbells Monday, Bodyweight Saturday), so moving a ride never drags the strength with it. | Saturday shows two cards — a ride (Z2 + Neuromuscular) and a Bodyweight strength — each with its own Move; Monday shows the Dumbbell session; moving the Saturday ride to another day leaves the Saturday strength in place; swap swaps ride-for-ride and never silently drags a second same-day workout; the importer assigns per-date versions with no unique-constraint violation; the prod re-import preserves Mark's current hand-juggled week (forward-from-next-Monday) and Zwift reconciles for the new bike rows; **no migration** (existing version-as-slot); verdict / #133 / #134 / #135 invariants + delivery rules unchanged; backend pytest/ruff/mypy, shared typecheck, web vitest/tsc/lint/build (Node 20) pass; closeout prod smoke on merge SHA. Decision #138 (assigned at `/batch-start`). Full spec: `docs/designs/workout-scheduling-feedback.md`. |
| ~~Batch 66 — Swap-first recovery guidance~~ | 🔴 High | Shipped | 66.1 Record the preference in the knowledge base (coaching protocol): when readiness is low and a hard session (VO2/Sweet-Spot) is scheduled, **rearrange the week — move the hard session to a better day and pull an easier session forward — before softening**; soften only when the week can't be rearranged.<br>66.2 On Amber/Red with a hard session scheduled, `_plan_adjustments` (`morning_analysis.py:1211`) + the morning prompt **lead** with a concrete rearrangement computed from `weekly_restructure` ("move VO2 to <next good day>, bring <easier session> forward"), softening as the explicit fallback; bump `PROMPT_VERSION`.<br>66.3 Surface the existing restructure **preview → apply** (`GET /api/v1/restructure/week-ahead` + `POST /apply`) from the verdict surface so one tap rearranges the week through the Batch 12/13 approval rail — no new engine (minimal fallback: deep-link the per-workout Move sheet pre-targeted at the suggested day).<br>66.4 Keep the Amber/Red ease-the-ride transform (`adjust_ir_for_verdict`) and Red-never-VO2 as the untouched fallback; a swapped VO2 still passes the ≥2-day no-stack rule and Red-never-VO2 at delivery.<br>66.5 Tests (verdict leads with swap on low-readiness + hard session, restructure reachable from verdict, softening fallback intact, spacing/Red-never-VO2 preserved) + gates. | When readiness is low and a hard session is scheduled, have the coach recommend — and offer to apply in one tap — a week swap (move the hard session, pull an easier one forward), matching Mark's own instinct, instead of only offering to soften. | The morning verdict on an Amber/Red day with a hard session leads with a concrete swap suggestion computed from the existing restructure engine and is **actionable** from the app (restructure preview + apply, or the pre-targeted Move sheet), not just prose; softening the ride stays available as the fallback and `adjust_ir_for_verdict` is unchanged; the ≥2-day no-stack spacing and Red-never-VO2 hold for any swapped session; swap-first is recorded in the KB; prompt version bumped; **no migration** (restructure engine + endpoints already exist); verdict / #133 / #134 / #135 invariants unchanged; backend pytest/ruff/mypy, shared typecheck, web vitest/tsc/lint/build (Node 20) pass; closeout prod smoke on merge SHA. Decision #139 (assigned at `/batch-start`). Full spec: `docs/designs/workout-scheduling-feedback.md`. |

## Post-roadmap — 2026-07-08 workout-fidelity & feedback-loop batch plan

Mark's 2026-07-07/08 feedback after a week of accepting the app's recommendations
(five screenshots, eleven observations). Root cause verified against the code and the
plan data: **every one of the 65 bike sessions in `apps/api/data/plans/plan_no2.json`
is authored as a single collapsed step** (`{label, target: <prose>, minutes}`) with the
real structure only in a prose `summary` the delivery engine never parses — so VO2
delivers as a flat 154 W Z1 ride (the `_power_pct` `return 55` fallback,
`workout_delivery.py:782`), Z2 "65–72%" is graded "Under" against 72 (an en-dash defeats
the band regex, `:757`), warm-up/cool-down ramps never survive, and `duration_min` drifts
(VO2 60 vs ~47). Two further gaps: the post-workout read grades the **planned** VO2 row,
never the eased/substituted ride he actually did (`post_workout_analysis.py:626`); and the
"Changed it" logger sits inside the **morning** check-in, captures only minutes/RPE, and
never reaches the read. One new feature: no weekly-mix accounting exists (observation 4).

Scope: **four batches** — 67 re-authors the plan as real steps + hardens the parser (the
trunk fix, resolves 1/2/11), 68 grades the delivered ride not the stale plan (5), 69 makes
accepting a recommendation self-logging and moves logging post-ride (3), 70 adds
weekly-mix maintenance (4, decision-gated). None needs a migration; 67 is the dependency
for 68/70. Proposed Decisions **#140–#143**, assigned at `/batch-start`. Full spec:
`docs/designs/workout-fidelity-and-feedback-loop.md`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 67 — Real structured workouts + harden delivery parsing~~ | 🔴 High | Shipped | 67.1 **Extend the step grammar to author ramps** (`_expand_step` emits only steady steps today, so even the engine's own VO2 warm-up is flat), then re-author every `bike_*` workout in `apps/api/data/plans/plan_no2.json` — transcribed from the authoritative source doc `Full 13 Wk. 2121 Plan No. 2 Start 06.07.26.docx` (all 13 weeks fully specified as fixed %FTP) — into real `steps` (warm-up ramp, work interval(s) at an **explicit fixed %**, recovery valleys, cool-down ramp); `duration_min` = summed step durations (fixes the VO2 60→~47 drift).<br>67.2 Remove the silent `return 55` fallback in `_power_pct` (`workout_delivery.py:782`) — raise 422 on an unresolvable target — and add an import-time validation pass so a malformed plan fails before Zwift.<br>67.3 Accept en-dash / unicode range separators in the band regex (`workout_delivery.py:757`) and collapse a band to its **midpoint**, so "65–72%" delivers ~68% and grades the 65% floor "on" (±5 tolerance) instead of "under" vs 72.<br>67.4 Re-import **forward from the resume Monday** (`import_plan` `start_date` override) so the reset/off week isn't clobbered; `reconcile_deliveries` re-pushes the corrected bike rows — explicit closeout step.<br>67.5 Tests (grammar: ramp raw-step→ramp IR step; en-dash midpoint; unresolvable→422; multi-step expansion; plan-JSON shape guard: no single-block bike step, `duration_min`==sum, warm-up+cool-down present, **total time traces** planned/app total==summed steps; delivered-ZWO ramp/interval snapshot) + gates.<br>67.6 **Decided (Mark 2026-07-09):** keep **Dumbbells Mon / Bodyweight Sat as separate rows** (transcribe content from the doc but keep Batch 65's placement — no revert); **endurance ranges → midpoint** (Long Z2 68%, easy/recovery Z2 62%); **total time must trace plan→app**; **FTP stays 280W**; Mon & Fri "no hard bike" is a Batch 70 concern, not a placement change here. | Replace the flat single-block bike prescriptions with real structured sessions (ramps + intervals at the correct %) and close the two parser holes, so VO2 delivers as intervals — not a flat 154 W Z1 ride — and Z2 is graded against its band, not its top. | Every `bike_*` workout expands to a multi-step IR with warm-up/cool-down ramps and work intervals at the authored %; VO2 reaches Zwift as 5×2 min @120% with ramps (not flat 55%); an unresolvable target raises 422 at import/propose (no silent 55% ride); "65–72%" delivers 68% (midpoint) and a 64.9% ride grades "on"; `duration_min` == summed steps **and the app-displayed total matches** (total time traces plan→app); day placement stays Dumbbells Mon / Bodyweight Sat as separate rows; FTP 280W; prod re-import runs forward from the resume Monday and Zwift reconciles; **no migration**; verdict / #133 / #135 / Red-never-VO2 unchanged; backend pytest/ruff/mypy, shared typecheck, web vitest/tsc/lint/build (Node 20) pass; closeout prod smoke + a Zwift spot-check. Decision #140 (assigned at `/batch-start`). Full spec: `docs/designs/workout-fidelity-and-feedback-loop.md`. |
| ~~Batch 68 — Grade what he actually rode~~ | 🔴 High | Shipped | 68.1 `_planned_ride_ir` (`post_workout_analysis.py:626`) resolves the grading IR from the latest **pushed delivery proposal** for the ride (the eased/substituted/overridden IR that went to Zwift), falling back to the planned row only when no proposal exists.<br>68.2 Carry the accepted adjustment's origin (`amber_regeneration`/`red_substitution`/`manual_override`) + the adherence `changeSummary` into the packet so the read names the substitution instead of narrating a VO2 attempt; bump `PROMPT_VERSION` (regenerates stale reads via `_analysis_is_current`).<br>68.3 Tests (Red-substituted ride grades against the easy IR not the planned VO2; read names the substitution; planned fallback intact for un-adjusted rides; work-interval grading guardrails unchanged) + gates. | Make the post-workout read grade and narrate the ride he actually did (the delivered/accepted workout), not the original planned VO2 — so accepting "drop the VO2, do easy Z2" is read as an easy Z2. | Shipped in PR #93 (`76307c7`): packet assembly now prefers the latest pushed delivery proposal IR by planned-workout id, with same-date fallback for legacy delivery rows, and only then falls back to the planned row; `gradingTarget` + `workoutAdherence.actualWorkoutJson.changeSummary` are included; `PROMPT_VERSION` bumped to `post-workout-analysis-v4-2026-07-09`. A Red-substituted delivered proposal grades the easy delivered work as on-target instead of under against VO2; un-adjusted rides still grade the planned row; whole-ride-context + grade-work-intervals guardrails unchanged; **no migration**; verdict / #133 / #135 / Red-never-VO2 invariants unchanged. CI green across both branch-push and PR-context waves (backend pytest/ruff/mypy, Alembic migration check, security audit, web build, web/shared vitest) plus Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=76307c742928645c4d24f09ac86199e6d09b54c6`), web `/` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #141. Full spec: `docs/designs/workout-fidelity-and-feedback-loop.md`. |
| ~~Batch 69 — Accept = logged; move session-logging post-ride~~ | 🟢 Mid | Shipped | 69.1 Approving the app's eased/substituted ride auto-writes `PlannedWorkoutAdherence` (`status='modified'`, `changeSummary` = the app's adjustment) so "Changed it" is pre-filled from what he accepted, not re-typed.<br>69.2 Relocate "How did your sessions go?" out of the morning `/check-in` (`CheckInPage.tsx:384`) onto the completed-ride Today card / post-ride check-in (the Batch 60 completed-row flow), so logging is offered after the ride.<br>69.3 Render the missing `intensity` control and let "Changed it" capture a structured "what I did instead" (type + target) — the field Batch 68 reads — not just free text.<br>69.4 Tests (accept→adherence idempotent write; morning check-in no longer shows session logging; post-ride surface does; structured change round-trips) + web + backend gates. | Stop asking pre-ride how the ride went, and stop making him re-enter a change he accepted from the app — accepting a recommendation logs itself, and session logging moves to a post-ride surface. | Shipped in PR #94 (squash `1288f85`): `approve_adjustment` idempotently seeds the adherence row (`status='modified'`, accepted-adjustment `type`/`intensity`/`changeSummary` in `actualWorkoutJson`) so accepting a recommendation logs itself; `/check-in` no longer asks "How did your sessions go?"; the Batch 60 completed-ride Today-card flow gained the moved logger as one combined post-ride form (Outcome, Actual minutes, and when changed: what he did instead / target intensity / what changed) that saves both the check-in and the adherence together. Two case/ordering bugs caught by CI (not local focused tests) were fixed before merge: `_accepted_adjustment_summary` compared against lowercase `"amber"/"red"` when `_normalize_verdict` returns `"Amber"/"Red"`, so accepted adjustments always fell through to the generic message; and `_accepted_adjustment_target` checked `powerCapPct` before `zoneDropPct`, so an accepted Amber ride reported "Capped at 98% FTP" instead of "75% duration, 13 points easier". **no migration**; verdict / #133 / #135 / Red-never-VO2 unchanged; full CI green (backend pytest/ruff/mypy, Alembic migration check, security audit, web build, web+shared vitest) in both push and PR contexts plus Vercel preview; production smoke passed on Railway + Vercel same-origin health (`sha=1288f853769215cec41439e60e715d7fd9cd5794`), web `/` and `/check-in` 200, and auth-gated `GET /api/v1/daily-loop` 401 direct and via Vercel. Decision #142. Full spec: `docs/designs/workout-fidelity-and-feedback-loop.md`. |
| ~~Batch 70 — Weekly-mix maintenance & dynamic rebalancing~~ | 🔴 High | Shipped | 70.1 Pure helper reads the week's planned + completed bike sessions by sub-category (Z2 / VO2 / Sweet-Spot from `workout_type`) against the target mix (Z2×3 / VO2×1 / SS×1) and reports done / due / at-risk.<br>70.2 When readiness drops a VO2/SS, the verdict proposes **re-patching it into a later same-week slot if spacing/metrics allow** (reusing the Batch 66 `weekly_restructure` engine), or **explicitly states** "no VO2 this week — the right call on this recovery" instead of silently losing it; surfaced in the week view + verdict.<br>70.3 Advisory only — never auto-schedules; the ≥2-day no-stack rule + Red-never-VO2 hold for any re-patch; softening stays the fallback.<br>70.4 Tests (quota accounting; readiness-dropped VO2 → re-patch when spacing allows / explicit "not this week" otherwise; invariants preserved) + gates. | When a hard session is dropped for readiness, notice the week is short of its VO2/Sweet-Spot and either offer to re-patch it or say plainly it won't be made up — maintaining Mark's fitness mix without silently going too easy on him. | The week's mix (done/due/at-risk) is computed from planned + completed rows; a readiness-dropped VO2 yields a re-patch suggestion when the ≥2-day spacing allows and an explicit "not this week" otherwise; nothing auto-schedules; Red-never-VO2 + no-stack hold; **confirmed (Mark 2026-07-09): soft readiness-gated quota** (mix is a protected target, readiness vetoes, shortfall explained); **Mon & Fri protected — never re-patch a hard bike session onto them** (Monday keeps its Dumbbells); **no migration**; verdict / #133 / #135 invariants unchanged; full gates; closeout smoke. Decision #143 (assigned at `/batch-start`). Full spec: `docs/designs/workout-fidelity-and-feedback-loop.md`. |
| ~~Batch 71 — Editable quick-add on the Week tab~~ | 🟢 Mid | Shipped | 71.1 A per-category subtype catalog (`QUICK_ADD_CATALOG` in `services/plan_actions.py`) — cycle: endurance/sweet_spot/recovery; weights: maintenance/recovery; flexibility: mobility — each with a title, workout type, intensity target, default duration, and a `[min,max]` bound; `GET /api/v1/plan-actions/quick-add-options?category=` lists it.<br>71.2 `add_workout` / `POST /days/{date}/workouts` accept optional `subtype` + `durationMin`; `workout_for_selection` validates the duration against the chosen option's bound (422 outside range) and sizes the bike warm-up/main/cool-down steps to sum to the chosen duration; omitting both preserves the exact prior hardcoded-default behaviour.<br>71.3 New `QuickAddSheet` (same `Sheet` pattern as `MoveWorkoutSheet`) replaces the instant-add buttons on `WeekAheadPage` — tap a category, pick a subtype (duration defaults from the option), edit the duration within its bound, submit.<br>71.4 Tests (catalog listing + unknown-category 422; subtype+duration override producing the right workout/steps; out-of-bounds duration → 422; updated `WeekAheadPage.test.tsx` driving the picker) + gates. | Let Mark pick and tweak what a "+ Cycle/Weights/Flexibility" add inserts, instead of always dropping the same fixed 45-min Z2 ride. | Adding a session on the Week tab offers a session choice and an editable duration rather than a silent hardcoded default; existing add/move/skip flows unchanged; no per-interval editor (out of scope — the manual-override dial + Batch 67's real structured workouts already cover that need); **no migration**; full gates; closeout prod smoke. Decision #144. Full spec: `docs/designs/workout-fidelity-and-feedback-loop.md`. |
| ~~Batch 72 — Chronic-REM coaching depth~~ | 🔴 High | Shipped | His REM has been low since he got the watch; the Batch 59 chronic-pattern card (`chronic_patterns.py`) shows the same standing list. Give a **broader, rotating set of REM interventions delivered one or two at a time** (focused, not a static list) for a persistent miss. Separate sleep thread from 67–71.<br>72.1 New pure `services/rem_interventions.py` — an ordered library (≥8; ships 12) of REM-specific levers grounded in the sleep protocol values/physiology.<br>72.2 `select_rem_interventions` hands out a focused window of two, calendar-week-seeded (deterministic, stateless, **no migration**), walking the whole library before repeating; a measured sleep driver biases the week toward its lever.<br>72.3 Wire into the `rem_sleep_pct` branch of `chronic_patterns._actions_for` only; `ChronicSuggestion` gains an optional `rotation`; no other chronic metric changes.<br>72.4 Shared `chronicSuggestionItemSchema` gains optional `rotation`; `ChronicSuggestionsCard` shows a rotating-focus caption.<br>72.5 Tests + gates. | For a chronic REM miss, rotate through a wider, focused set of interventions (one or two at a time) instead of repeating the same three suggestions. | A persistent REM miss surfaces a focused 1–2 intervention set that rotates over time rather than the same static list; grounded in the sleep protocol; a measured sleep driver still biases the set; non-REM chronic suggestions are unchanged; deterministic/stateless rotation with no migration and no verdict/Anthropic-boundary change; no regression to Batch 59; full gates. Shipped in PR #97 (squash `92019ed`): CI green in both push and PR-context waves plus Vercel preview; production verified on the merge SHA (Railway + Vercel same-origin `/api/v1/health` both `92019ed`, web `/` + `/sleep` 200, auth-gated `GET /api/v1/daily-loop` — now carrying `rotation` — 401 direct and via Vercel). Decision #145. Full spec: `docs/designs/workout-fidelity-and-feedback-loop.md`. |

## Post-roadmap — 2026-07-10 Mark feedback batch plan

Mark's 2026-07-10 note (one screenshot, three problems) while trying to swap the
day's scheduled "Z2 + Neuromuscular + Dumbbells" for a plain 1-hour outdoor Z2
ride. **The screenshot is the Home tab, not the Week tab** — the
`Edit / Swap day / More options` cluster and the `Duration % / Intensity % /
Apply & sync` editor only exist on `DashboardPage.tsx`. Root cause verified
against the code:

1. **Home's "+ Cycle" can't make an hour, and typing 133% throws "API error
   422".** Home's add buttons (`AddWorkoutButtons`, `DashboardPage.tsx:1021`) POST
   `{category}` only, so the backend inserts the fixed first-catalog default — a
   45-min Zone 2 endurance ride (`default_workout_for_category`,
   `plan_actions.py:208`). Home never got the editable picker the **Week tab**
   received in Batch 71 (`QuickAddSheet` + `subtype`/`durationMin`), so there is no
   add-time duration choice. To resize, the per-session Edit dial scales duration,
   but it is capped at 125% both client-side (`max={125}`,
   `DashboardPage.tsx:1317`) and server-side (`durationScalePct`
   `Field(ge=50, le=125)`, `workout_delivery.py:79`). 45×1.25 = 56 min, so an hour
   is **unreachable from the 45-min base** regardless; and an HTML `max` does not
   stop typing, so **133** is sent and Pydantic 422s. That 422 arrives as an
   array-`detail`, which `apiFetch` cannot stringify (`api.ts:88` only surfaces a
   string `detail`), so it degrades to the raw "API error 422" toast — never
   telling him the bound.
2. **No way to author a bespoke ride (some Z3, or sprints).** The cycle quick-add
   catalog is exactly endurance / sweet-spot / recovery (`QUICK_ADD_CATALOG`,
   `plan_actions.py:60`) — no tempo/Z3, no VO2/efforts, and no custom-interval
   builder anywhere. His fallback instinct is correct and already works: an
   unplanned/freeform ride is read post-hoc (the "After your ride" section,
   `postWorkoutAnalyses` with no `plannedWorkoutId`, `DashboardPage.tsx:376`) — but
   nothing in the UI tells him that is the intended path.
3. **Skip "said done" but the session is still there; there is no delete.** For a
   bike session the only destructive control is Skip (overflow "More options").
   `skip_workout` flips `status='skipped'` and deletes the Zwift event but **leaves
   `is_active=True`** (`executable_coaching.py:788` mutates only status). The
   **Week/Plan** schedule query hides skipped rows (`status != 'skipped'`,
   `plan_actions.py:233`), but the **Home** daily-loop query filters only
   `is_active` and never excludes skipped (`daily_loop.py:441`), so the skipped
   session keeps rendering on Home with no badge — indistinguishable from
   untouched. And a workout Mark *added himself* can only be skipped, never removed.

Scope: **three batches**, one per problem — 73 brings Batch 71's editable
quick-add to the Home Today card and makes the edit-dial 422 legible (resolves 1),
74 makes Home honour a skipped session and gives an added workout a real Remove
(resolves 3), 75 adds a couple of ride subtypes (tempo/Z3, a simple VO2/efforts)
and surfaces the "just ride it — I'll read it after" path for anything truly
bespoke (resolves 2). All frontend-led over endpoints that already exist; none
needs a migration. Proposed Decisions **#146–#148**, assigned at `/batch-start`.
Full spec: this section (a design doc can be added at `/batch-start` if 74's
skip-vs-remove semantics warrant one).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 73 — Editable quick-add on the Home Today card + legible edit-dial~~ | 🟢 Mid | Shipped | 73.1 Reuse the Batch 71 `QuickAddSheet` on Home's `DayPlanBody` (`DashboardPage.tsx`) — the `+ Cycle/Weights/Flexibility` buttons open the picker (subtype + editable duration within the option's `[min,max]`) instead of instant-adding the fixed default; `addWorkoutMutation` sends `{category, subtype, durationMin}` (the endpoint + `workout_for_selection` bounds already exist from Batch 71), so a 60-min Z2 ride is one add. Home also needs the `quick-add-options` query the Week page already has.<br>73.2 Make the edit-dial error legible: clamp the Duration %/Intensity % inputs to their real bounds and show the allowed range inline; extend `apiFetch` (`api.ts:88`) to stringify FastAPI array-`detail` so a bound violation reads as a message, not "API error 422".<br>73.3 **Decide at `/batch-start`:** whether to also raise the `durationScalePct` ceiling above 125% (`workout_delivery.py:79` + `DashboardPage.tsx:1317`) or leave the dial a modest ±25% tweak now that add-time duration covers the hour case.<br>73.4 Tests (Home add opens the picker and posts subtype+duration; out-of-bounds duration surfaces the bound; edit-dial over-max shows a legible message not a raw 422) + web/backend gates. | Let Mark choose and size a ride when he adds it on Home — pick a 60-min Z2 in one step — and never see a bare "API error 422" from the resize dial. | Adding a session on the Home Today card offers the same subtype + editable-duration picker as the Week tab (Batch 71 parity), posting `subtype`/`durationMin`; a 45→60-min ride is reachable at add-time; the Edit dial rejects out-of-range input with the allowed range shown, not a raw 422, and `apiFetch` surfaces FastAPI validation detail everywhere; existing add/edit/swap/skip flows unchanged; no migration; full gates; closeout prod smoke. Decision #146 (assigned at `/batch-start`). |
| ~~Batch 74 — Home honours a skipped session + Remove for added workouts~~ | 🟢 Mid | Shipped | 74.1 Exclude skipped from Home: add `PlannedWorkout.status != WORKOUT_STATUS_SKIPPED` to the daily-loop `_planned_workouts` query (`daily_loop.py:441`) so a skipped session leaves the Today card, matching the Week `schedule()` query (`plan_actions.py:233`).<br>74.2 Give a user-added workout (`source='plan_action_add'`) a genuine **Remove** — **decided at `/batch-start`** whether Remove deactivates the row (`is_active=False`, no skipped-adherence noise) while the coach's planned sessions keep Skip's adherence-tracked semantics; Remove still deletes any pushed Zwift event.<br>74.3 UI: the added-ride row offers Remove (not just Skip buried in the overflow); a removed/skipped session disappears immediately and after reload.<br>74.4 Tests (Home excludes a skipped workout; Remove deactivates an added workout and deletes its Zwift event; planned-session Skip still records adherence; the two pages agree) + gates. | When Mark skips or removes a session it actually leaves the Home card — and a ride he added himself can be removed outright, not just skipped. | A skipped session no longer renders on Home (parity with the Week tab); an added workout has a Remove that deactivates it and clears its Zwift event; the coach's planned sessions keep Skip's adherence semantics; skip/remove persist across reload; no migration (the `status`/`is_active` columns already exist); full gates; closeout smoke. Decision #147 (assigned at `/batch-start`). |
| ~~Batch 75 — Bespoke-ish rides: more subtypes + explicit post-hoc path~~ | 🟢 Mid | Shipped | 75.1 Extend `QUICK_ADD_CATALOG` cycle options (`plan_actions.py:60`) with **tempo/threshold (Z3)** and a simple **VO2/"with efforts"** preset, authored as real structured steps (warm-up/work/recovery/cool-down at explicit %FTP) using the Batch 67 step grammar, each with sane `[min,max]` bounds.<br>75.2 Surface a plain "doing something different? just ride it — I'll read it after" affordance near the add controls / rest-day copy, pointing at the existing post-hoc read path (unplanned rides → "After your ride", `DashboardPage.tsx:376`), so Mark knows he needn't pre-build.<br>75.3 A full custom-interval builder stays **out of scope** (the app's model for arbitrary rides is post-hoc analysis) — noted, not built.<br>75.4 Tests (new subtypes expand to a valid multi-step IR at the right %; catalog listing includes them; copy renders) + gates. | Cover the common "a ride with some Z3 / some efforts" case in one tap, and make it obvious that anything truly bespoke can just be ridden and read afterwards. | The cycle quick-add offers tempo/Z3 and a VO2/efforts preset that deliver as real structured sessions (not a flat block); the post-hoc "just ride it" path is discoverable in the UI; no interval-builder scope creep; no migration; full gates; closeout smoke. Decision #148 (assigned at `/batch-start`). |

## Post-roadmap — 2026-07-10 workout-organiser feedback batch plan

Mark's follow-up 2026-07-10 note on organising workouts (the earlier 2026-07-10
note drove Batches 73–75): *"Tried adding a ride to today and changing time which
worked but when I went to skip it to get rid of it it cleared all cycling
including the workout I'd already done and checked in on so now thinks no cycling
at all today. … The workout part still seems a bit clunky / rigid. It also
doesn't seem to know about light reset week this week, holiday next week and
restart 13 week block 2 20/07. I have some ideas on this to make it more flexible
leave it with me."*

**Reconciled against the code (2026-07-10).** Two distinct things:

1. **A real data-loss bug.** "Skip day" is a *whole-day* action: `POST
   /api/v1/plan-actions/days/{date}/skip` → `skip_day` (`plan_actions.py:393`)
   skips **every** active session on the date via `_active_workouts_on`
   (`plan_actions.py:432`), whose filter is only `is_active AND status !=
   'skipped'` — it does **not** exclude `completed`. Completion sets
   `status='completed'` but leaves `is_active=True` (`workout_completion.py:75`),
   so a day-skip flips the already-ridden, checked-in session to `skipped` → "no
   cycling at all today." The Week tab makes this the *only* skip control
   (`WeekAheadPage.tsx:285`), so getting rid of one added ride there forces the
   whole-day nuke. Per-item `skip_workout` (`executable_coaching.py:771`) has the
   same missing guard — unlike `swap_day`, which already refuses to move a
   completed session (`executable_coaching.py:874`). A correctness fix,
   independent of the redesign → **Batch 76**.

2. **The "clunky / rigid / doesn't know about …" theme is mostly *surfacing*, not
   missing engines.** What Mark's asking for largely already exists, fragmented
   across separate screens the organiser knows nothing about:
   * **reset week** — the 13-week `BLOCK_SEQUENCE` is 2-build/1-recovery
     (`coaching_state.py:493`), so a "light reset week" *is* a `recovery` block;
     but the slate is auto-anchored to the seed Monday (`_current_cycle_start`,
     `coaching_state.py:21`) with **no control to say "this week is a deload,"** so
     it rarely lines up with his real life. *(The one genuinely-new piece.)*
   * **holiday** — a full pause/resume feature exists (`holiday_pause.py`, page
     `/holiday`) but lives on its own screen, disconnected from the organiser.
   * **restart block 2 on 20/07** — the Block Generator (`/builder`) already
     generates a fresh 13-week block **anchored to any start date** and locks it
     in; **verified** the lock cleanly supersedes the overlapping seeded plan by
     date-versioning (`block_generator.py:475`) — so this is *discoverability*,
     not a bug.

   Mark has said he wants to lead this redesign ("I have some ideas … leave it
   with me"). Since the engines exist and are correct, the remaining work is
   UX/design — surfacing deload/holiday/block-anchor *inside* the organiser plus
   the unifying "rearrange the whole week in one tap" restructure. **Held as
   design-pending below — not batched until his direction lands.**

**Scope chosen with Craig:** ship the data-loss bug now as **Batch 76**; hold the
flexibility redesign as a Mark-led design-pending item (candidate scope recorded
so nothing is lost).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 76 — Skip never clears a completed or logged session~~ | 🟢 Mid | Shipped | 76.1 `skip_day` / `_active_workouts_on` (`plan_actions.py:393`/`432`) exclude `status='completed'` (and any adherence-logged session) so a whole-day Skip only touches still-outstanding sessions — a ride already ridden + checked in stays and still counts as the day's cycling.<br>76.2 Mirror the Batch 60 `swap_day` completed-guard (`executable_coaching.py:874`) onto per-item `skip_workout` (`executable_coaching.py:771`): skipping an already-completed session is refused (409 "already done") instead of silently rewriting history.<br>76.3 **Decide at `/batch-start`:** whether a completed session simply can't be day-skipped (skip the rest, leave it) or whether the whole-day Skip control is hidden/annotated once the day holds a completed session; and whether "logged" means `status='completed'` only, or also any `ManualEntry`/adherence row for the date.<br>76.4 Tests (whole-day Skip on a day with one completed + one outstanding ride skips only the outstanding one and the completed ride still reads as cycling; per-item skip of a completed session 409s; genuinely-outstanding skip/remove flows unchanged; Home ↔ Week parity) + web/backend gates. | Skipping a day — or one session — can never erase a ride Mark already did and checked in on; only still-outstanding sessions are ever skipped. | A whole-day Skip on a day holding a completed, checked-in ride leaves that ride intact (still counts as the day's cycling) and skips only outstanding sessions; per-item skip of a completed session is refused with a clear message, matching `swap_day`; genuinely-outstanding skip/remove flows are unchanged; **no migration** (`status`/`is_active` already exist); verdict / #133 / #135 / Red-never-VO2 invariants unchanged; backend pytest/ruff/mypy, shared typecheck, web vitest/tsc/lint/build (Node 20) pass; closeout prod smoke on merge SHA. Decision #149 (assigned at `/batch-start`). |
| ~~Batch 77 — Structured workout builder & step-level editor (Mark's Excel spec)~~ | 🔴 High | Shipped | 77.1 A builder form matching Mark's `Workout Structure for The App 10.07.26.xlsx` fields — indoor/outdoor, warm-up (Y/N + mins), Z2 lead-in (Y/N + mins) @55% FTP, intervals (int-1 mins+%FTP / int-2 mins+%FTP / repeats) **or** single block (mins+%FTP), cool-down (Y/N + mins) — authored into the existing Batch 67 structured IR (`build_structured_workout_ir`); warm-up/cool-down default to a 45→75% ramp (his note #1), Z2 lead-in @55% (note #2). Promotes `QUICK_ADD_CATALOG`/`workout_for_selection` (`plan_actions.py`) from fixed presets to a full custom builder — the piece Batch 75.3 deferred.<br>77.2 **Structured edit of a planned session** (his note #4): edit the actual steps (durations/%FTP/repeats) rather than only whole-workout %-scale (`apply_manual_override_to_ir`, `executable_coaching.py:215` — the current Edit dial); a new service/endpoint versions the row (existing version-as-slot) and re-syncs Zwift via `reconcile_deliveries`.<br>77.3 Save-as-bespoke: a built workout becomes a `plan_action_add` planned workout on the chosen day (indoor → Zwift as today; outdoor → Batch 78).<br>77.4 Validation (step durations sum to total; %FTP within bounds; Red-never-VO2 still gates delivery) + web/backend tests + gates. | Let Mark build a workout to his own structure, and edit a planned session step-by-step instead of just scaling its time/intensity — the app authoring both into the same structured format it already uses internally. | Shipped via PR #102 / squash `80ff8ba`. The Excel fields are transcribed in `docs/designs/structured-workout-builder.md`; a pure builder authors the existing Batch 67 bike step grammar, add-workout accepts a `customBike` payload, and structured edits version the planned row then re-sync indoor sessions via `reconcile_deliveries`. Outdoor custom workouts are stored with `delivery="outdoor"` but deliberately skipped by Zwift delivery until Batch 78. **No migration**; no arbitrary-graph editor; verdict / #133 / #135 / Red-never-VO2 unchanged. Gates green in both branch-push and PR-context CI waves; production verified on merge SHA `80ff8ba`: Railway + Vercel same-origin health returned the SHA, web `/` + `/delivery` returned 200, and auth-gated structured-edit endpoint returned 401 direct and via Vercel. Decision #150. |
| ~~Batch 78 — Indoor/Outdoor delivery: outdoor rides to Garmin Connect~~ | 🔴 High | Shipped | 78.1 Carry the builder's **Indoor/Outdoor** choice onto the workout; indoor keeps the intervals.icu→Zwift rail (`workout_delivery.py`), outdoor routes to Garmin instead (no Zwift event).<br>78.2 A Garmin workout-export path: build Garmin Connect's structured-workout JSON from the Batch 67 IR (distinct from the ZWO/intervals payloads) and upload + schedule it on Mark's Garmin calendar via the **existing authenticated** garth/`garminconnect` session (today read-only — `garmin_sync.py` only pulls), so it syncs to his Edge to follow on the road. **Decide at `/batch-start`:** direct Garmin upload vs. routing through intervals.icu's own Garmin sync.<br>78.3 Honest, isolated delivery (#97): an outdoor upload failure surfaces on the workout and never silently drops; idempotent re-sync on edit.<br>78.4 Tests (IR→Garmin-workout JSON; outdoor flag routes to Garmin not Zwift; indoor path unchanged) + gates. | Let Mark build an outdoor ride and have it land in Garmin Connect → his bike computer, from the same structured workout the app already builds for Zwift. | An outdoor workout appears on Mark's Garmin device with its targets; indoor workouts still deliver to Zwift unchanged; the Garmin write path is isolated and honest on failure; direct-upload vs intervals.icu-sync decided at `/batch-start`; **migration decision at `/batch-start`** (an indoor/outdoor flag can ride in `structured_workout` JSONB to avoid one); verdict / #133 / #135 / Red-never-VO2 unchanged; full gates; closeout prod smoke incl. a Garmin device spot-check. Decision #151. **Shipped via PR #103 / squash `eb4d085`;** direct Garmin upload (`upload_workout`/`schedule_workout` on the garth session) + a new `garmin_workout_deliveries` table (migration `014`, applied cleanly on Railway startup); reconcile routes outdoor→Garmin (isolated, idempotent, honest on failure) while indoor→Zwift is unchanged; Week tab shows the delivery status. Prod-verified on the merge SHA (Railway + Vercel same-origin health `eb4d085`, web `/` + `/delivery` 200, auth-gated `GET /api/v1/plan-actions/schedule` 401 direct and via Vercel). Garmin device spot-check deferred to live use. |
| ~~Batch 79 — Per-item Skip/Remove on the Week tab~~ | 🟢 Mid | Shipped | 79.1 Add per-workout **Skip** and **Remove** to the Week tab's workout rows (`WeekAheadPage.tsx`), calling the existing `/planned-workouts/{id}/skip` and `/planned-workouts/{id}/remove` (Batch 74) — the actions Home already exposes (`DashboardPage.tsx:235`/`245`) — and keep whole-day "Skip day" as a separate, clearly-labelled action, not the only tool.<br>79.2 Remove stays added-only (`source='plan_action_add'`; `remove_workout` already 409s otherwise); Skip keeps its adherence semantics.<br>79.3 Tests (Week-tab per-item skip/remove; removing one added ride leaves the rest of the day intact; Home ↔ Week parity) + web gates. | Let Mark get rid of one added ride from the Week tab without the whole-day "Skip day" — the exact trap he hit. | Shipped via PR #104 / squash `12104ad`. The Week tab offers per-item Skip + Remove (added-only) reusing the Batch 74 endpoints; whole-day Skip remains but is no longer the only control; **no migration**; verdict / #133 / #135 unchanged. Gates green in both branch-push and PR-context CI waves; production verified on the merge SHA: Railway + Vercel same-origin `/api/v1/health` both returned `12104ad`, web `/` + `/delivery` returned 200, and unauthenticated `POST` to `.../planned-workouts/{id}/skip` and `.../remove` returned 401 both direct and via Vercel. Decision #152. |
| ~~Batch 80 — "Was overriding the plan the right call?" deviation verdict~~ | 🔴 High | Shipped | 80.1 When a completed ride diverged from the day's planned/delivered session (a self-chosen ride, or a skipped plan + his own ride), the post-workout read (`post_workout_analysis.py`) renders an explicit, honest **deviation verdict** — was the override the right call given readiness + the week's goals — grounded in the objective grade (power/HR/execution), never sycophantic; bump `PROMPT_VERSION`.<br>80.2 Detect divergence deterministically (planned session skipped/absent, or actual type/target ≠ planned) and pass it into the packet so the verdict is anchored, reusing the Batch 68 delivered-vs-actual grading.<br>80.3 Tests (self-chosen easy ride on a hard-planned low-readiness day → "reasonable call"; an unjustified skip of a key session → a plain "this set you back"; an on-plan ride → no deviation verdict) + gates. | When Mark does his own thing instead of the plan, tell him plainly whether it was the right call (his Q1a) — judged on the numbers, not just validated. | A diverged ride gets an explicit good-call/bad-call read grounded in the objective grade; on-plan rides unaffected; honest on a bad call (no sycophancy); `PROMPT_VERSION` bumped; **no migration**; Red-never-VO2 / #133 / #135 unchanged; full gates; closeout smoke. Decision #153 (assigned at `/batch-start`). |
| ~~Batch 81 — Calendar-aware week view~~ | 🟢 Mid | Shipped | 81.1 The Week tab shows each week's **character** from `PlanBlock` (`sequence_index`/`block_type`) — "Build n/13", "Reset", "Holiday", "Taper" — plus any active holiday window (`holiday_pause` KB), so the organiser visibly knows where he is in the block.<br>81.2 Surface the existing **Holiday** (`/holiday`) and **block-restart** (`/builder`) as entry points from the organiser (link/sheet), reachable in-context rather than only as separate screens; no engine change.<br>81.3 Tests (week header renders block character; holiday window shown; entry points present) + web gates. | Make the organiser aware of the plan's shape — reset weeks, holidays, block boundaries — so it stops feeling like it "doesn't know." | Each week shows its block character + any holiday; Holiday/Builder reachable from the organiser; read-only surfacing of existing data/engines; **no migration**; full gates; closeout smoke. Decision #154 (assigned at `/batch-start`). |
| ~~Batch 82 — Reset-week control (mark a week as a light reset)~~ | 🔴 High | Shipped | 82.1 A "mark this week as a light reset" action on the organiser that **eases the week's rides to Z2 and keeps strength** (Mark's spec), as a deterministic transform reusing the structured-IR easing (`adjust_ir_for_verdict` / step rewrite), versioning the affected planned workouts (existing version-as-slot) and re-syncing Zwift via `reconcile_deliveries`.<br>82.2 Record the reset (KB or block flag — **decide at `/batch-start`**, prefer no migration) so the verdict/insights read it as an **intended deload**, not missed load.<br>82.3 Manual only (no auto-detection this batch); reversible (unset restores the prior versions); Red-never-VO2 + spacing unaffected.<br>82.4 Tests (mark-reset eases rides to Z2, keeps strength, re-syncs; unset restores; recorded as intended deload) + gates. | Let Mark tell the plan "this week is a light reset" and have it ease the rides to Z2 while keeping strength — his exact ask — instead of relying on the auto-anchored 2121 lining up. | Shipped in PR #107, squash `67ae54f9aa8f00ab1ddc11cd2cefdc044a159c96`. Decision #155 chose the **block flag** path (`plan_blocks.goals_json.manualResetWeek`) for no-migration intent; `POST /api/v1/plan-actions/weeks/{date}/reset` versions active non-completed bike rows into `source='reset_week'` steady-Z2 (`65% FTP`) rows while keeping strength/flexibility and preserving indoor/outdoor delivery; `DELETE /api/v1/plan-actions/weeks/{date}/reset` restores the saved prior bike versions; schedule labels the banner `Light reset`, the Week tab offers Light reset/Restore week, and morning analysis marks the reduced load as intended deload. Red-never-VO2 + spacing unchanged. Local gates green (backend **568/196**, shared **14**, web **199**, lint/type/build); main CI run `29147443453` green across all 7 jobs; production verified with Railway + Vercel same-origin `/api/v1/health` on `67ae54f...`, web `/` + `/delivery` 200, and unauthenticated `POST`/`DELETE /api/v1/plan-actions/weeks/2026-07-07/reset` 401 direct + via Vercel. |
| ~~Batch 83 — One-tap week restructure (wire the existing engine to the web)~~ | 🟢 Mid | Shipped | 83.1 A **preview→apply** UI on the organiser consuming the existing `GET /api/v1/restructure/week-ahead` + `POST /api/v1/restructure/apply` (`restructure.py` / `weekly_restructure.py`) — show the proposed reshuffle and apply it in one tap through the Batch 12/13 approval rail. Batch 66 shipped only the Move-sheet fallback; **no web page consumes the restructure endpoints today**.<br>83.2 Honest/idempotent apply (#97); the ≥2-day no-stack + Red-never-VO2 hold for any moved session.<br>83.3 Tests (preview renders the engine's proposal; apply versions the changed days + re-syncs Zwift; guardrails hold) + gates. | Give Mark "rearrange the whole week in one tap" — the flexibility he asked for — by wiring the restructure engine that already exists to the web. | Shipped in PR #108 (squash `1616003`): shared schemas parse the restructure envelope; the Week organiser shows a `Rearrange week` banner action, opens a preview sheet backed by the existing preview endpoint, resolves the returned workout ids back through the loaded schedule so the proposed swaps read clearly, and applies the same `week_start` through the existing apply endpoint before invalidating schedule + daily-loop. No engine change, **no migration**. Local verification: shared `schemas.test.ts` **15 passed**, focused web `WeekAheadPage.test.tsx` **12 passed**, web lint clean aside from the repo's 6 pre-existing Fast-Refresh warnings, web build clean. Production verified on merge SHA `1616003`: Railway + Vercel same-origin health returned the SHA, web `/` returned 200, and unauthenticated `GET /api/v1/restructure/week-ahead` returned 401 direct and via Vercel. Decision #156. Depends on Batch 81. |
| ~~Batch 84 — Morning brief truncation fix (systemic)~~ | 🟢 Mid | Shipped | 84.1 Raise the shared output ceiling `anthropic_max_tokens` (`config.py:54`, currently `1600`) so the richest brief fits — **decide the value at `/batch-start`** (e.g. 3072–4096); it is shared by every analysis client via `settings.anthropic_max_tokens` (morning, post-workout, strength, walk, flexibility, reviews/trends/handover).<br>84.2 Make the Anthropic boundary honest about truncation: `generate()` (`morning_analysis.py:133` + its five siblings) today stores whatever text returns with **no `stop_reason` check**, so a `max_tokens` cut is persisted as a finished read. Detect `stop_reason == "max_tokens"` and never store a half-written analysis — **decide at `/batch-start`**: raise (so the existing staleness/regeneration path retries) vs. flag, and whether to centralise the six duplicated HTTP boundaries rather than patch each.<br>84.3 Tests (a `max_tokens` stop is treated as error/flagged, not stored as final; a normal `end_turn` response is unchanged; the raised ceiling is respected) + backend gates. | Stop long coach reads — the Morning Brief and any other section — being silently cut off mid-sentence (Mark's 2026-07-11 report). | A long brief renders complete; a `max_tokens` stop is never persisted as a finished read; the ceiling is raised once and shared across all analysis clients; **no migration**; verdict / #133 / #135 / Red-never-VO2 unchanged; backend pytest/ruff/mypy (+ shared/web gates if touched) pass; closeout prod smoke on merge SHA (a real long brief renders whole). Decision #157 (assigned at `/batch-start`). |
| ~~Batch 85 — "Say good morning" flow: check-in-first brief, data pre-loaded~~ | 🔴 High | Shipped | 85.1 Split the wake job `run_morning_weather_sync` (`scheduler.py:265`) at its existing **sync → generate** seam: keep the sync phase wake-triggered so all external inputs (Garmin daily/sleep, Hive indoor temp, weather) are pulled and sitting in Postgres before Mark is up; move generation **off** the schedule so it fires on his check-in. The 09:30 backstop keeps auto-generating for a morning he never engages (fallback, not primary).<br>85.2 Replace the wake auto-generate-and-push with a **"good morning?" nudge fired after sync completes** (so tap-time is always post-sync → fast; reuse the existing push path). **Decide at `/batch-start`**: nudge copy + that the nudge replaces (not duplicates) today's morning-verdict push.<br>85.3 Make the check-in the **primary generate trigger** — the check-in save (`PUT /api/v1/daily-loop/{date}/manual-entry`, `daily_loop.py:1150`) force-regenerates today's brief with his just-entered notes, generalising `regenerate_after_morning_checkin` (`executable_coaching.py:335`) from "only when the verdict worsens to Amber/Red on a pending ride" to "always, on submit". All check-in fields stay optional (empty submit = today's objective brief). Preserve the guardrails: subjective stays **downgrade-only** (never upgrades a Red/Amber to Green) and an **approved/pushed ride is never silently re-adjusted** (Decision #29).<br>85.4 Answer his questions: extend `SYSTEM_PROMPT` (`morning_analysis.py:61`) to detect a question in the check-in notes and answer it, grounded in the packet (his sleep + thermal/overnight-temp rows are already assembled); bump `PROMPT_VERSION`.<br>85.5 Frontend: the check-in bottom button becomes **generate my brief** and surfaces the result, with a "reading your morning…" pending state over the LLM call (`CheckInPage.tsx`). **Decide at `/batch-start`**: button copy + whether Home/Sleep nudge "check in for today's plan".<br>85.6 Tests (sync-then-nudge ordering; check-in submit force-regenerates and reflects notes; empty submit unchanged; downgrade-only holds — a cheery check-in never upgrades a Red; approved/pushed ride untouched; backstop still generates on a no-check-in morning; a question in notes is answered) + backend/web gates. | Turn the morning from something the app did before Mark woke into something he initiates — say good morning, optionally add notes/questions, get a brief generated on the spot from his input, with all data pre-loaded so it's fast. | Wake syncs then nudges; the check-in generates the brief (force-regenerate) with notes/questions folded in; empty check-in = today's objective read; the 09:30 backstop still covers a skipped morning; downgrade-only + never-touch-approved-ride hold; a question in the notes is answered from the packet; `PROMPT_VERSION` bumped; **no migration** (confirm at `/batch-start`); verdict / #133 / #135 / Red-never-VO2 unchanged; full backend + web gates; closeout prod smoke on merge SHA **plus a live-morning eyeball** (nudge fires post-sync, tap generates, brief reflects the check-in — cannot be smoke-tested). Depends on Batch 84. Decision #158 (assigned at `/batch-start`). |
| ~~Batch 86 — Hybrid actionable brief: "Today" actions on top, reasoning below~~ | 🔴 High | Shipped | 86.1 The morning read leads with a structured **"Today" action list** (2–4 items — the workout adjustment first-class, plus sleep/thermal actions) above the existing reasoning prose + question-answer. The model emits **structured actions** (type + text + referenced `planned_workout_id`) alongside `output_markdown`, following the established pattern of structured fields carried next to the markdown (`swapSuggestion` / `planAdjustments` / `weeklyMix` on `AnalysisOut`); bump `PROMPT_VERSION`.<br>86.2 Shared schema: add the actions shape to `morningAnalysis` (`@coach/shared` + `AnalysisOut`); **decide the action contract at `/batch-start`**.<br>86.3 Frontend: a `TodayActions` block at the top of the brief (Home Today card + `/brief`) rendering the structured actions; the workout-adjustment action is **tappable to approve** via the existing approve/swap endpoints (no new backend capability — the rail Home already uses). Keep the **structured data durable and the visual layout swappable** so the format can be tuned after Mark reacts.<br>86.4 Tests (structured actions parse + render; the ride action approves via the existing endpoint; reasoning + answer still render below; a brief with no actionable items degrades gracefully) + backend/shared/web gates. | Make the brief scannable and actionable — the day's actions up top, workout adjustment tappable to approve — without losing the coaching reasoning Mark reads for the "why". | The brief shows a Today action block on top with the workout adjustment first-class and tappable-to-approve (existing endpoints), reasoning + answer below; structured action data is separable from its layout; `PROMPT_VERSION` bumped; **no migration** (structured actions ride in the analysis payload — confirm at `/batch-start`); verdict / #133 / #135 / Red-never-VO2 unchanged; full backend/shared/web gates (Node 20); closeout prod smoke on merge SHA + a phone eyeball of the action block. Depends on Batch 85. Decision #159 (assigned at `/batch-start`). |
| ~~Batch 87 — "How did it feel?" flow: check-in-first post-workout read~~ | 🔴 High | Shipped | 87.1 Split the hourly `run_garmin_activity_poll` (`scheduler.py:546`; registered `scheduler.py:1183`) at its **sync → generate** seam, mirroring Batch 85's `run_morning_sync` split: keep the Garmin **activity sync** on the poll so a finished ride/strength/flexibility/walk lands in Postgres, but move **analysis generation** off the poll's primary path — today the poll both syncs *and* auto-generates inline (`generate_for_pending_rides` `post_workout_analysis.py:243` + the strength/flexibility/walk siblings). Generation now fires on his post-workout check-in; a backstop still auto-generates a session he never checks in on.<br>87.2 After sync, fire a **per-activity "how did it feel?" nudge** (new `build_*` plan mirroring `build_good_morning_plan` `nudge_alerts.py:167`, idempotent per activity like the existing `analysis-{activity_id}` tag) deep-linking to that activity's post-ride check-in on Home. This **replaces** the "your analysis is ready" push (`build_analysis_push_plan` `nudge_alerts.py:187`, sent via `_push_new_analyses` `scheduler.py:695`) on the primary path — that push now fires **only from the backstop**, exactly Batch 85's nudge-replaces-verdict-push move. **Decide nudge copy at `/batch-start`.**<br>87.3 Make the post-workout check-in the **primary generate trigger**: the post-ride check-in save (`upsert_post_ride_checkin` `daily_loop.py:1228`, and the completed-ride logger path for planned rides, `DashboardPage.tsx:317`/`:349`) generates that activity's read inline via the existing `generate_and_store` (`post_workout_analysis.py:366` for rides + the strength/flexibility/walk siblings), **dispatching by activity type** so the correct service runs — instead of only saving and leaving the lagged next-poll regen (`_analysis_is_current` / `_analysis_covers_activity_checkin`) to pick it up. The plumbing under this is **already generic across all four types**: the check-in write endpoint (`daily_loop.py:1228`) stores a `ManualEntry` keyed by `activity_id`, and every service already consumes it (`_post_ride_checkin` → `postRideCheckIn`; `_activity_checkin` → `activityCheckIn` in `post_flexibility/strength/walk_analysis.py`) with per-service staleness — so this phase is *wiring the inline trigger to the right service*, not new per-type plumbing. All fields stay optional (empty submit = the objective read). Post-workout analysis is **read-only** (no delivery writes), so there is **no** "never re-adjust an approved ride" guardrail to preserve here — simpler than the morning.<br>87.4 Backstop: an **evening sweep** generates any still-unread session from the day so an ignored workout still gets a read — and, critically, **before the next morning's packet** consumes yesterday's ride read (`assemble_context_packet` reads the latest post-workout analysis). **Decide the backstop time / pending-hours threshold at `/batch-start`** (must land before the next-morning sync).<br>87.5 Answer his questions: extend the post-workout `SYSTEM_PROMPT` to detect and answer a question left in the post-ride check-in notes, grounded in the packet (the check-in already reaches it as `postRideCheckIn`), mirroring Batch 85.4; bump `PROMPT_VERSION` (regenerates stale reads via the existing staleness check).<br>87.6 Frontend: the completed-ride / post-ride check-in becomes the **generate CTA** with a "reading your ride…" pending state over the LLM call, surfacing the returned read inline (`DashboardPage.tsx`); a Home affordance invites the check-in while a session is unread (mirror `GoodMorningCta.tsx`). **This is where "all four types" costs the most:** today only **rides** have a check-in form (the rich completed-ride logger + standalone post-ride check-in, `DashboardPage.tsx:317`/`:349`); strength/flexibility/walk **render** their read but have **no** check-in surface, so their (backend-supported) `activityCheckIn` is never written — so add a **minimal "how did it feel?" check-in** (RPE/feel/notes, posting to the same generic endpoint) for strength/flexibility/walk, rides keeping the richer logger. **Build-time dependency to confirm at `/batch-start`:** Batch 60's consolidated completed-*row* treatment was ride-scoped (strength/flex/walk completion was left as a fast-follow), so those reads may still render in a standalone analyses section rather than a Today row — confirm where the non-ride check-in attaches before building. **Decide at `/batch-start`:** button copy + whether generation is a **hard gate** on the check-in or keeps an explicit **"just read it now"** affordance (a finished ride is not a once-a-day event with a natural pause like waking — he may want the read on the spot).<br>87.7 Tests (sync-then-nudge ordering with no auto-generate on the primary poll; check-in submit generates inline and reflects notes; empty submit = objective read; the backstop still generates an un-checked-in session before morning; a question in the notes is answered; the nudge replaces the analysis-ready push on the primary path) + backend/shared/web gates. | Bring the post-workout loop into line with the Batch 85 morning **for every workout type (ride / strength / flexibility / walk)**: after a session the app pulls the data and invites a "how did it feel?" check-in, and his tap generates the read *from his input* — instead of auto-writing a read that ignores his input until a later poll (Mark's original "my typed input goes nowhere" complaint that motivated Batch 85). | A newly synced session syncs then nudges (no auto-generate on the primary poll); the post-ride check-in generates the read inline with RPE/feel/notes folded in; empty check-in = the objective read; the evening backstop still covers a session he never checks in on, before the next morning packet needs it; a question in the notes is answered; post-workout stays **read-only** (no delivery writes); `PROMPT_VERSION` bumped; **no migration** (confirm at `/batch-start`); verdict / #133 / #135 / Red-never-VO2 unchanged; full backend + web gates; closeout prod smoke on merge SHA **plus a live eyeball** (finish a ride → nudge → check-in tap generates a read reflecting the input — cannot be smoke-tested). Post-workout analog of the shipped Batch 85. **Scope resolved (Craig, 2026-07-11): all four post-workout types in one batch** (ride / strength / flexibility / walk — all run the same poll loop today). The check-in write endpoint + per-service consumption + staleness are **already generic across all four**, so the incremental cost of "all four" is the **inline-generate dispatch by type** (87.3) and a **minimal non-ride check-in surface** on the strength/flexibility/walk Today rows (87.6) — rides keep their richer logger. Decision #160 (assigned at `/batch-start`). |

**Update (2026-07-10, Mark's Excel + answers).** Mark supplied his own spec — the
`Workout Structure for The App 10.07.26.xlsx` builder fields — plus answers, so the
redesign is no longer open-ended:

* His structure is, field-for-field, the app's existing Batch 67 IR (warm-up ramp,
  optional Z2 lead-in, interval sets / single block at explicit %FTP, cool-down) —
  so the **builder + step-level editor is now scoped as Batch 77**, and his note #4
  ("adjust structurally, not just a percent of time/intensity") is exactly the
  Edit-dial gap it closes.
* His **Indoor/Outdoor** field + Q2 (build an outdoor ride into Garmin Connect →
  bike computer) is scoped as **Batch 78**.
* **Reset-week behaviour is now specified:** a light reset = **Z2 + strength only**
  (drop VO2 / Sweet-Spot). Firms the "tell the plan this week is a deload" piece.

**Now batched (2026-07-10, after Mark's responses).** His Excel + answers closed the
genuine design gaps, so the organiser redesign is no longer Mark-blocked — the
remaining work is surfacing engines we already have + displaying data we already
store, which is ours to design (Mark reacts to a mockup at build). Numbered as:
**79** (per-item Skip/Remove on the Week tab), **80** (the "was overriding the plan
the right call?" deviation verdict — his Q1a), **81** (calendar-aware week view —
block character + Holiday/Builder entry points), **82** (reset-week control — ease a
week to Z2, keep strength — his specified reset behaviour), **83** (one-tap week
restructure — wire the existing `/api/v1/restructure/*` preview→apply to the web).

**Only remaining un-numbered thread:** chronic feed-forward (his Q1b) — the
baseline-drift **F1** + load-not-gated **F2** items — which belong to the
coaching-integrity audit remediation (`COACHING_INTEGRITY_AUDIT.md`), not this
workout thread.

**Near-term calendar (confirmed with Mark 2026-07-10; execute via existing tools).**
(1) This week = light reset: ease Sat 11/07 Sweet-Spot → Z2 (only hard session
left; today already done as a completed 75-min Z2). (2) Holiday pause **12–16/07**
(back evening of the 16th; the 17th is already empty). (3) Optional warm-up
18–19/07 left in place (already Z2 / Bodyweight). (4) **Block 2 = restart the
authored Plan No. 2 afresh, Week 1 = Mon 20/07** — Mark wants the *original* block 2,
so the tool is a **re-import of `plan_no2.json` with `--start-date 2026-07-20`** (the
Batch 65/67 "re-import forward from Monday" move — `plan_import` DELETEs
`plan_blocks` ≥ start and re-lays them, then `reconcile_deliveries` re-syncs Zwift),
**not** the generic `/builder`. FTP stays 280 (retest after the block). (5) "Unless
the app suggests something better" is a no-op today — `block_progression` needs a
*completed* 13-week block and there is no such history yet, so it defaults to the
standard 2121 (= the original Plan No. 2). Execution needs prod DB access (a
`plan_import` run + Zwift reconcile), same as prior closeout re-imports — it can't
be driven from the dev sandbox.

**Now batched (2026-07-11, Mark's feedback — two items).** Mark reported (1) the
Morning Brief cutting off mid-sentence (screenshot ended on "*Yesterday's ride was
a*"), and (2) that ad-hoc questions he types into the check-in "Anything worth
noting" field go nowhere — the field is one-way context, and the brief is generated
at wake *before* he checks in (`scheduler.py:265`), so his input never reaches it.
Reconciled against the code with Craig and shaped into a "say good morning" redesign:
he initiates the brief from his check-in (data pre-synced so it's fast), it answers
any question in his notes, and it leads with a scannable actions-on-top format.
**Split into three by verification profile, not by topic** (they are one experience
but verify very differently, so they ship in dependency order): **84** truncation fix
(verifies in minutes; a live bug, so it ships first and de-risks the longer briefs
downstream), **85** the check-in-first reorder + answer-his-questions (backend +
prompt; needs a live-morning eyeball; the behavioural heart, proven before any UI
rides on it), **86** the hybrid actionable brief (structured "Today" actions +
tappable-to-approve; additive UI, landing last so Mark can react to the format and we
tune it). Root cause + reconciliation detail: truncation is `max_tokens`-driven
(`config.py:54` = `1600`) with no `stop_reason` guard in the shared Anthropic
boundary; the check-in note already reaches the packet (`manualEntries[].notes`) but
nothing regenerates the brief to answer it and the prompt has no "answer the question"
behaviour.

**Now scoped (2026-07-11, Craig — post-workout symmetry).** Craig asked to confirm
that every interaction follows one flow — pull data → offer a manual check-in → he
taps to generate the AI narrative — and whether that holds for the morning brief
(Batch 85/86) *and* every post-workout situation. Traced against the code: the
**morning matches** (wake fires `run_morning_sync` = sync + "good morning" nudge, no
LLM; the check-in force-regenerates the brief inline, `daily_loop.py:1184`; 09:30
backstop covers a skipped morning), but **post-workout is currently the reverse** —
the hourly `run_garmin_activity_poll` (`scheduler.py:546`) syncs the activity **and
auto-generates the read in the same loop** (`generate_for_pending_rides`
`post_workout_analysis.py:243`, + strength/flexibility/walk siblings), pushing
"analysis ready"; the post-ride check-in (`upsert_post_ride_checkin`
`daily_loop.py:1228`) only **saves** and leaves a **lagged next-poll regen**
(`_analysis_is_current`) to fold his input in. So his RPE/feel/notes never shape the
*first* read — the exact "my typed input goes nowhere" complaint that motivated
Batch 85, one loop over. Scoped as **Batch 87** — the post-workout analog of 85,
reusing 85's nudge/backstop/check-in-first machinery: split the poll's sync from
generate, nudge "how did it feel?" after sync, make the check-in the generate
trigger (inline `generate_and_store`), and add an **evening backstop** (must run
before the next morning packet consumes yesterday's ride read). Read-only path, so
no delivery-write guardrail and (likely) **no migration**. 🔴 High (poll seam +
prompt behaviour, like 85). Decision #160 reserved (assigned at `/batch-start`).
**Scope resolved (Craig, 2026-07-11): all four post-workout types** (ride /
strength / flexibility / walk) in one batch, not rides-first. The code makes this
cheaper than it first looked — the check-in **write endpoint**
(`upsert_post_ride_checkin`, keyed by `activity_id`) and **per-service
consumption** are already generic across all four (rides read `postRideCheckIn`;
strength/flexibility/walk read `activityCheckIn`, each with its own staleness
guard), so the only genuinely per-type work is (a) dispatching the **inline
generate** to the right service by activity type, and (b) adding a **minimal
check-in surface** for strength/flexibility/walk on their Today rows (today only
rides have a check-in form, so those types' `activityCheckIn` is never written).
**Open `/batch-start` calls:** (1) hard-gate generation on the check-in vs. keep a
"just read it now" affordance (a finished workout isn't a once-a-day event with a
natural pause like waking); (2) backstop time/threshold; (3) nudge + button copy.

## Post-roadmap — 2026-07-11 full-control workout editor batch plan

**Scoped 2026-07-11 with Craig — full control over a cycling workout on any given
day.** Craig wants Mark to be able to take full control of a ride's structure when he
chooses, beyond the Batch 77 builder's single interval-pair-or-block shape and the
Today-card's whole-workout duration/intensity scale (Mark already has the basic manual
edits; this is the whole canvas on demand). **Traced against the code — the delivery
format is *not* the limit:** `expand_structured_steps` (`services/workout_delivery.py:139`)
already expands an arbitrary-length ordered `steps` list (ramp / steady /
interval-pattern, any count, any order) into the IR that both the intervals.icu/Zwift
push (`reconcile_deliveries`) and the Batch 78 Garmin export already consume. The
single-pair-or-block ceiling lives only in the builder spec `CustomBikeWorkoutSpec`
(`services/structured_workout_builder.py`) and its UI — so "full control" is a builder +
UI change over the same rail Mark already uses, not a delivery rewrite, and the code that
actually talks to Zwift/Garmin is untouched. **Two decisions taken with Craig:**
(1) control level = **free-form segment builder** — add/reorder any number of segments,
each with its own duration + %FTP; (2) guardrails = **soft warnings** — on Mark's
explicit manual path the three gates (power 45–150% FTP, ramp-required, Red-never-VO2)
become non-blocking warnings, while the coach/automated authoring path keeps them
**hard**. The path-scoped soft-gating — and specifically making Red-never-VO2 a *warning*
on a workout Mark explicitly authored for himself (a deliberate, recorded reversal of the
long-held "Red-never-VO2 unchanged" invariant, scoped strictly to manual authoring) — is
the batch's central design point. Full spec:
`docs/designs/full-control-workout-editor.md`. Depends on the shipped Batch 77.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 88 — Full-control free-form workout editor~~ | 🔴 High | Shipped | 88.1 Generalise the builder spec: replace the fixed-field `CustomBikeWorkoutSpec` (`services/structured_workout_builder.py`) with an **ordered list of segments** (`kind ∈ ramp/steady/interval`, each with its own duration + %FTP), reusing the exact per-kind step emission `build_custom_bike_workout` already does inline so any count/order authors the existing Batch 67 `steps` grammar; the `structured_workout` shape, `totalDurationMin`, and workout-type classification stay unchanged downstream.<br>88.2 **Path-scoped soft warnings:** on Mark's explicit manual authoring path only, the three delivery gates become warn-not-block — power outside 45–150% FTP (`_required_power`), no warm-up/cool-down ramp (`validate_deliverable_bike_workout`, `workout_delivery.py:178`), and VO2 on a Red-readiness day (Red-never-VO2 delivery gate). The coach/automated authoring path keeps all three **hard**. **Decide the mechanism at `/batch-start`** (spec flag vs. a separate manual-authoring validation entrypoint).<br>88.3 Warnings channel: the add/edit responses carry only hard `errors` today — add a `warnings: [...]` list so a **successful** save returns advisory messages the UI can surface. **Decide the contract at `/batch-start`.**<br>88.4 Frontend: a segment-list editor (add/remove/reorder rows, kind selector + fields) on the same Week-tab build + structured-edit surfaces Batch 77 shipped, surfacing warnings inline ("heads-up… deliver anyway") rather than blocking submit; a live power-profile preview + running total from the expanded IR (`powerStartPct/powerEndPct/durationSec`) so Mark sees the workout before it ships (**confirm in scope at `/batch-start`**).<br>88.5 Reuse the existing add (`customBike`) + structured-edit (`POST …/planned-workouts/{id}/structured`, `plan_actions.py:349`) endpoints so versioning, indoor Zwift re-sync, and outdoor Garmin routing carry over unchanged; completed workouts still cannot be structurally edited.<br>88.6 Tests (a many-segment free-form workout authors valid IR + delivers; segments in any order preserve order; each soft gate warns-not-blocks on the manual path but still hard-fails coach/automated authoring; the warnings channel surfaces on a successful save; structured edit versions + re-syncs) + backend/shared/web gates. | Let Mark take full control of a ride's structure on any given day — build an arbitrary multi-segment workout, warned but never blocked — over the same delivery rail he already uses. | A free-form multi-segment workout authors valid IR and delivers indoor→Zwift / outdoor→Garmin unchanged; segment order is preserved; on Mark's explicit manual path the power / ramp / Red-never-VO2 gates warn but don't block, while coach/automated authoring keeps them hard (proven by test); the `warnings` channel surfaces advisory messages on a successful save; structured edit still versions + re-syncs and completed workouts stay non-editable; **no migration** (segments serialize into the existing `structured_workout` `steps` — confirm at `/batch-start`); verdict / #133 / #135 unchanged; Red-never-VO2 stays hard everywhere **except** the explicit manual authoring path (Decision #161 records the scoped reversal); full backend/shared/web gates (Node 20); closeout prod smoke on merge SHA + a phone eyeball of the segment editor. Depends on Batch 77. Decision #161 (assigned at `/batch-start`). |

### Batch 89 — Enable RLS on the coach.* health-data tables

Surfaced by the 2026-07-11 full-app QA review (`QA_REVIEW_2026-07-11.md`, Finding 1).
The Supabase security advisor flags a **critical `rls_disabled`** lint: 18 of 23
`coach.*` tables (all of Mark's health data — `sleep`, `daily_metrics`,
`analyses`, `activities`, `manual_entries`, …) have Row Level Security **off**,
because only migration 001's five auth/notification tables were ever put under
RLS; every table added in 002-014 was left uncovered. This project's Postgres is
**shared with a separate public-facing movie app** whose anon key ships in its
frontend, which sharpens the concern. **Verified during scoping:** the `anon` and
`authenticated` roles hold **no grants** on the `coach` schema today, so nothing
is actually reachable via the Data API — the fix is **defense-in-depth**, not an
open breach. The backend connects as the `postgres` owner (bypasses RLS), so
enabling RLS with no policies is deny-all for `anon`/`authenticated` and leaves
the app untouched.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 89 — Enable RLS on the coach.* tables~~ | 🟢 Mid | Shipped | 89.1 New migration `015_coach_rls.py` — `ENABLE ROW LEVEL SECURITY` on the 18 `coach` tables migration 001 left uncovered (the 17 remaining model tables + `alembic_version`), **no policies** (owner bypasses; `anon` has no grants → deny-all, matching `audit_log`). Table list kept as a `RLS_TABLES` constant. Downgrade disables. Wrapped in migration 001's exact `IF EXISTS (auth schema)` guard so it runs **only on Supabase** and is a no-op on plain Postgres (CI `migration-check` + unit-test Postgres stay green).<br>89.2 Regression test (`tests/test_coach_rls_migration.py`): 001's set ∪ 015's set == the coach model tables on `Base.metadata` — a future table shipping without RLS fails the test.<br>89.3 Correct `QA_REVIEW_2026-07-11.md` Finding 1 + STATUS open item (b) to the accurate exposure picture (data not currently reachable; RLS is the backstop). | Every `coach` table carries RLS as defense-in-depth, matching the auth tables from migration 001, so a future stray `GRANT` or exposed-schema change can never leak Mark's health data — and the Supabase critical advisor clears. | Migration enables RLS on the 18 tables on Supabase and is a guarded no-op on plain Postgres (CI `migration-check` upgrade→downgrade + pytest green); the app is unaffected (owner bypass); no policies added; regression test proves 001+015 cover every coach model table; **no schema/data change** beyond the RLS flag; verdict / #133 / #135 / Red-never-VO2 untouched; backend gates green; closeout prod smoke on merge SHA **plus a post-deploy Supabase advisor recheck** (the `rls_disabled` critical lint clears and the daily-loop still serves for Mark — RLS can't be smoke-tested from the app alone). Surfaced by the QA review. Decision #162 (assigned at `/batch-start`). |

## Post-roadmap — 2026-07-11 flexibility-read accuracy batch plan

Mark's 2026-07-11 note (two screenshots) after logging a mobility/stretch session:
"couple of errors in apps feedback." **Both screenshots are one `post_flexibility`
read** — the system prompt's shape is acknowledge → consistency → heart-rate →
*one light next step* (`post_flexibility_analysis.py:44`), and the "Advisory only —
this analysis does not inform recovery status or cycling session decisions" footer
is that prompt verbatim. Every sentence Mark flagged is **LLM-generated from the
context packet** (`assemble_flexibility_packet`, `post_flexibility_analysis.py:222`),
so each error is missing/stale packet data or a loose prompt, never a template
string. Root cause verified against the code:

1. **"today (Friday)" — wrong day.** The packet hands the model
   `subjectDate: "2026-07-11"` as a bare ISO string (`post_flexibility_analysis.py:241`)
   and **no weekday name**, so the model computed the day itself and got it wrong —
   11 Jul 2026 is a **Saturday**. This is latent everywhere: no packet builder (morning
   verdict or any `post_*`) emits a weekday string, so any read that names a day is
   guessing.
2. **"mobility on Mondays and Sundays … today's session is a bonus on top" /
   "volume on the fuller side."** Straight from the `training_plan` seed's `weeklyRhythm`
   (`coaching_state.py:148` — "Monday recovery or mobility strength" + "Sunday light
   strength or mobility"), fed to the packet as `trainingPlan`
   (`post_flexibility_analysis.py:251`). That rhythm is the **cycling-block scaffold**;
   it models mobility as a Mon/Sun add-on, not Mark's daily baseline. The data even
   contradicts it — consistency shows an 8-day streak and 6 mobility sessions this
   week (i.e. daily) — but the model anchored to the plan text over the behaviour.
3. **"a VO2 focus day (Tuesday) … let tomorrow be easy," blind to the holiday.**
   "Tuesday VO2 focus" is again just `weeklyRhythm[1]` (`coaching_state.py:150`), a
   generic template line — next Tuesday is **14 Jul, inside the holiday window Mark
   set**. Holiday awareness is absent from every analysis packet: a grep for "holiday"
   across morning + all `post_*` returns zero hits, and this packet loads only
   **today's** planned workouts (`workout_date == subject_date`,
   `post_flexibility_analysis.py:362`), never next week's — even though
   `HolidayPauseService` already stores the window (`get_active_window`,
   `holiday_pause.py:142`) and flips in-window workouts to `status='skipped'`
   (`holiday_pause.py:212`). So the read had no signal the week was off and invented a
   live VO2 Tuesday.
4. **Daily flexibility framed as overdoing it / a whole-day recovery call.** The
   numbers are right and mobility-only (`compute_flexibility_consistency`,
   `post_flexibility_analysis.py:133`); 6/week *is* the habit. The "fuller side / take
   tomorrow genuinely easy" reading comes from (2)'s stale 2×/week model plus a loose
   "one light next step" instruction (`post_flexibility_analysis.py:44`) with no
   guardrail that mobility is low-load baseline — so the model escalated to whole-day
   recovery advice that **contradicts the packet's own
   `guardrails.neverFeedsRecoveryDecision`** (`post_flexibility_analysis.py:271`) and
   the advisory-only footer. **Two adjacent latent bugs found while tracing:** the
   packet's `analysisRules` pulls `knowledge_base.get("analysis_rules", …)`
   (`post_flexibility_analysis.py:252`) — a section that is **never seeded** (real
   sections are `data_quality_rules` / `coaching_protocol`), so it is **always `{}`** in
   all four post_* reads; and the flexibility packet **omits `training_schedule`** (only
   the morning verdict pulls it, `morning_analysis.py:303`), the section whose
   "respect Mon/Fri rest" and "**describe an exception as an exception instead of
   rewriting the routine**" notes (`coaching_state.py:175`) directly temper this
   mis-framing.

Scope: **one batch** — all four threads live in the same packet + prompt
(`post_flexibility_analysis.py`), so splitting would churn one file four ways. It is
🔴 High (analysis-engine prompt + framing/reasoning). Because the prose is
LLM-generated, tests assert on **packet assembly + prompt inputs** via the fakeable
client (the Batch 6/8 pattern, #47), not on model text. Proposed Decision **#163**,
assigned at `/batch-start`. Full spec: this section (a short design doc can be added
at `/batch-start` if the 90.3 daily-mobility-modelling choice warrants one).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 90 — Post-mobility read: right day, holiday-aware, daily-mobility-as-baseline~~ | 🔴 High | Shipped | 90.1 **Weekday: supply it, never let the model derive it.** Add an explicit `subjectWeekday` (`subject_date.strftime("%A")`) to the flexibility packet (`assemble_flexibility_packet`, `post_flexibility_analysis.py:238`) and an output rule that the model must use the supplied weekday and never compute the date. Fixes "today (Friday)" for a Saturday session. **Decide at `/batch-start`:** thread the same `subjectWeekday` through the other LLM reads (morning verdict, walk, strength, ride — none supply one today) so they can't make the same slip, or keep it flexibility-scoped now.<br>90.2 **Holiday awareness in the packet.** Load `HolidayPauseService.get_active_window` plus any window opening within the read's forward horizon and surface it (window dates + a "next week is a holiday" flag); add a prompt rule not to cite plan sessions (VO2 etc.) that fall inside a holiday window as live. **Decide at `/batch-start`:** holiday-window flag only vs. also pulling the next 7–14 days of planned workouts (already carrying `status='skipped'` from `holiday_pause.py:212`) so the near-future is data-driven, not template-driven.<br>90.3 **Stop reading daily mobility as plan overshoot or recovery load.** Reframe packet + prompt so the observed cadence (packet already computes `currentStreak`/`sessionsThisWeek`/`sessionsPerWeek4w`) is Mark's **baseline daily habit** and `weeklyRhythm` is the *cycling* scaffold only — not a mobility budget; kill the "bonus on top / fuller side" framing. The "one light next step" must stay **within mobility** and never issue a day/bike recovery verdict, honouring `guardrails.neverFeedsRecoveryDecision` (`post_flexibility_analysis.py:271`) + the advisory-only footer. **Decide at `/batch-start` (central design point)** where "daily mobility is baseline" lives: (a) prompt framing over the existing consistency signal [recommended — tightest, self-correcting, no KB re-seed]; (b) a durable KB fact shared by all reads (new `routine` line or folded into `training_schedule`); (c) rewrite the `training_plan.weeklyRhythm` seed (heaviest — KB re-seed + touches every read).<br>90.4 **Give the read the context it's missing + version bump.** Fix the always-empty `analysisRules` key (`post_flexibility_analysis.py:252` references the never-seeded `analysis_rules`; real sections are `data_quality_rules`/`coaching_protocol`) and add `training_schedule` to the packet (the "describe an exception, don't rewrite the routine" + Mon/Fri-rest notes reinforce 90.3). Bump `PROMPT_VERSION` (`post_flexibility_analysis.py:40`). **Decide at `/batch-start`:** fix the empty-`analysis_rules` key in the flexibility packet only, or in the identical spot in walk/strength/ride too (`post_walk_analysis.py:232`, `post_strength_analysis.py:231`, `post_workout_analysis.py:329`).<br>90.5 **Tests + gates.** Fakeable-client packet tests: the packet carries the correct `subjectWeekday` for a known Saturday date; an active/upcoming holiday window surfaces and a template VO2 day inside it is flagged/suppressed; the packet frames daily mobility as baseline (baseline flag/field present, no "exceeds plan") and now ships `training_schedule` + a non-empty rules block; the read stays advisory (no recovery-decision output rule). Backend pytest/ruff/mypy (+ any touched shared/web) green. | Make Mark's post-mobility read factually correct and correctly framed — right weekday, holiday-aware, and treating his daily flexibility as the baseline habit it is rather than plan overshoot or recovery-worthy load — without letting an advisory mobility read make whole-day recovery calls. | The flexibility packet supplies an explicit weekday and the read never miscomputes the day; holiday windows are visible so it never cites a plan session inside a holiday as live; daily mobility is framed as consistency-driven baseline, not as exceeding the plan or as fatigue load, and the "one light next step" stays within mobility and never issues a day/bike recovery verdict (honouring `neverFeedsRecoveryDecision` + the advisory-only footer); the packet no longer ships an always-empty `analysisRules` and includes `training_schedule`; `PROMPT_VERSION` bumped; **no migration** (packet/prompt/framing only — confirm at `/batch-start`); verdict / #133 / #135 / Red-never-VO2 untouched; full backend gates; closeout prod smoke on merge SHA **plus a phone eyeball** of the next real post-mobility read (correct weekday, holiday respected, no "bonus/overdoing" framing). Surfaced by Mark's 2026-07-11 feedback. Decision #163 (assigned at `/batch-start`). |

## Post-roadmap — 2026-07-12 Mark feedback batch plan

Mark's 2026-07-12 morning note flagged four issues in the day's morning brief. All
four were reproduced against the stored analysis + source data for
`subject_date = 2026-07-12` (user Mark, `coach` schema); the first-guess "phantom
subjective feel" was **disproved by the data** and re-scoped. Root causes, each
verified against code + the actual packet Mark saw:

1. **Sleep time an hour behind ("sleeping at 00:17" vs the real 01:17).** Stored
   `sleep_start_utc = 2026-07-12T00:17:34Z` is correct UTC (= 01:17 BST). The packet
   ships only `sleepStartUtc`/`sleepEndUtc` (`_sleep_packet`, `morning_analysis.py:759`;
   `_dt` appends "Z", `:1517`) and nothing — packet or prompt — converts to
   `Europe/London`, so the LLM prints the UTC clock verbatim. Invisible in winter
   (UTC==GMT), off by exactly 1h under BST. Profile timezone is correctly
   `Europe/London`, so this is purely a presentation gap in the read. Same family:
   one 08:30 regen mis-dated its header "13 July" — the model deriving the date
   instead of using `subjectDate`.
2. **Bedroom "peaked at 24.1°C overnight" — wrong window.** The verdict's thermal
   window is a fixed **18:00→08:00 local** (`_overnight_window_utc`,
   `morning_analysis.py:1504`), so `max()` grabs the **24.05°C reading at ~18:1x BST**
   — daytime, before the pre-cool, ~7h before sleep onset (01:17). The true
   asleep-period peak was **20.2°C**. The pre-cool low **18.64°C** *was* captured
   (`indoorLowC`) but only flagged `precool_target_missed` and buried under the bogus
   peak, so a night Mark cooled 24→18.6 read as "ran significantly too warm." The
   `/bedroom` chart uses a *different* window (**21:30→09:00**,
   `bedroom_overnight.night_window:49`), so the two surfaces already disagree.
3. **"Age average restless moments is 13" — a fabricated number.** `restless=47`; the
   packet shipped `ageAverage: 13` from a hard-coded `averages` table
   (`age_norms.py:130`). Although the metric is `descriptive_only=True`, the
   descriptive branch still emits `age_average` (`:428`), so the number leaks and the
   model states it. It contradicts the module's own docstring ("no defensible
   population band … shown for context only", `:28`) and Garmin's ≤5/hr guidance
   (~35/night is fine), making Mark's normal 47 look alarming.
4. **"Subjective feel 6" he didn't recognise giving — NOT a phantom.** The data shows
   Mark *did* set it: `manual_entries.subjective_score = 6`,
   `feel = "A bit more tired this morning"`. He tapped **"OK" (=6)** in the Batch 63
   "Overall" chips (`OVERALL_OPTIONS`, `CheckInPage.tsx:26`) but doesn't connect that
   to "subjective feel", and the brief threw the internal term "subjective feel 6 /
   subjectiveScore 6/10" back at him — jargon he never saw. Two real problems: the
   read leaks the internal 0–10 term (backend, 91.3), and the input isn't recognisable
   as the feel check (frontend, 94; design resolved with Craig 2026-07-12 — keep the
   one-tap word scale, never show the number, rename + surface).

**Sequencing.** 91/92/93 are independent, migration-free backend fixes; run 92 and 91
first (the trust-eroding wrong facts), then 93 (honesty). 94 is frontend and **lands
with or after 91** (94.3 needs 91.3's "OK" wording for the vocabulary to match
end-to-end). Tiers per #19: 91/92 High (analysis-engine prompt + verdict-adjacent
thermal correctness); 93/94 Mid (well-specified data fix / component work). The #4
split is deliberate — the prose leak (91.3) is deterministic and ships now; the
check-in UX (94) carries the design. Decision numbers assigned at `/batch-start` (next
free **#164**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 91 — Morning brief read fidelity: local clock times + faithful check-in wording~~ | 🔴 High | Shipped | 91.1 Add profile-timezone-localized fields to the morning packet — `sleepStartLocal`/`sleepEndLocal` and a localized date label — computed from `sleep_start_utc`/`sleep_end_utc` in `player.timezone`, **alongside** the existing `*Utc` fields (`_sleep_packet`, `morning_analysis.py:759`), so bed/wake render 01:17 / 08:32 (BST), never raw 00:17 / 07:31 (UTC).<br>91.2 Instruct the read to use the localized fields for all human-facing times/dates and never print a `*Utc` value or re-derive the date; extend Batch 90's authoritative-`subjectWeekday` rule (#163) to time-of-day and the header date (kills the intermittent "13 July"). Bump `PROMPT_VERSION`.<br>91.3 Speak the check-in back in his words: map `subjectiveScore` → its check-in label (6→"OK", from `OVERALL_OPTIONS`, `CheckInPage.tsx:26`) in the packet and/or prompt so the read says "you said you felt OK" — never "subjective feel 6" / "subjectiveScore 6/10". **Decide at `/batch-start`:** map in the packet vs. a prompt instruction.<br>91.4 Tests: local-time formatting across BST **and** GMT (DST boundary); packet carries both UTC + local; prompt-version bump; label mapping; regression that no `*Utc` value or raw 0–10 term leaks into a rendered read. | State bed/wake times, the date, and Mark's check-in in his own local, human terms — fixing the hour-behind sleep time (00:17→01:17), the intermittent wrong date, and the "subjective feel 6" he never recognised giving. | Bed/wake render in `Europe/London`, BST-correct (evidence: `2026-07-12T00:17Z` must read 01:17); the packet exposes localized fields beside the `*Utc` ones and the prompt forbids printing UTC or re-deriving date/weekday; the check-in is referenced by its label ("OK"), never the raw 0–10 / `subjectiveScore` term; `PROMPT_VERSION` bumped; **no migration**, no frontend change; verdict / #133 / #135 / Red-never-VO2 unchanged; backend pytest/ruff/mypy green; closeout prod smoke on merge SHA **plus a phone eyeball** of the next real morning brief (local bed/wake time, correct date, "OK" wording). Surfaced by Mark's 2026-07-12 feedback. Decision #164 (assigned at `/batch-start`). |
| ~~Batch 92 — Overnight thermal window: sleep-anchored peak + credited pre-cool~~ | 🔴 High | Shipped | 92.1 Re-window the thermal peak to the actual sleep period — replace the fixed 18:00→08:00 window (`_overnight_window_utc`, `morning_analysis.py:1504`) with one anchored to `sleep_start_utc`→`sleep_end_utc` (wind-down fallback when sleep times are missing), so `indoorPeakC` reflects the asleep period (~20.2°C) not pre-bed daytime heat (24.05°C at ~18:1x).<br>92.2 Credit the pre-cool: compute/surface the pre-bed low (e.g. `preCoolLowC` / at-onset temp) as a distinct signal so the read acknowledges "you pre-cooled to 18.6°C" instead of only firing `precool_target_missed`; re-tune the flag so a genuine cool-down isn't narrated as a failure. **Decide the pre-cool signal shape at `/batch-start`.**<br>92.3 Unify window semantics with the `/bedroom` chart (`bedroom_overnight.night_window:49`, 21:30→09:00) so the verdict and chart share one definition of "the night's window."<br>92.4 Tests: peak windowed to sleep excludes an early-evening daytime spike (fixture from the 2026-07-12 series: 24.05 pre-bed vs 20.2 asleep); pre-cool low surfaced + credited; missing-sleep fallback; chart/verdict window parity. | Report what the room did **while Mark was asleep** and credit his pre-cool — fixing the "peaked at 24.1°C" that was really a ~18:1x daytime reading and the "totally missed my pre-cool" complaint. | `indoorPeakC` is computed over the sleep period and excludes pre-bed daytime readings (evidence: the 2026-07-12 night must read ~20.2°C, not 24.05); the pre-cool low is surfaced and the read credits it rather than only flagging a miss; verdict and bedroom-chart share one window definition; **no migration**; thermal stays advisory/environmental and does not change the Green/Amber/Red recovery verdict; verdict / #133 / #135 / Red-never-VO2 unchanged; backend pytest/ruff/mypy green; closeout prod smoke on merge SHA **plus a phone eyeball** of the next warm-night thermal read. Surfaced by Mark's 2026-07-12 feedback. Decision #165 (assigned at `/batch-start`). |
| ~~Batch 93 — Age-norm honesty: no fabricated "restless average"~~ | 🟢 Mid | Shipped | 93.1 Stop emitting a numeric age-average for descriptive-only, no-band metrics — remove the invented `restless_moments_count.averages` table (or null its `ageAverage` in the descriptive branch of `_build_rows`, `age_norms.py:421`) so the packet no longer asserts "age average 13".<br>93.2 Make the surface honest: show the value with an explicit "no age reference" (matching the module docstring, `age_norms.py:28`) **or** — **Craig's micro-decision at `/batch-start`** — a per-hour-derived reference from Garmin's ≤5/hr guidance against actual time-in-bed, not a fabricated nightly count.<br>93.3 Prove no ripple: `restless_moments_count` is absent from `_SLEEP_BANDS`, so the age-adjusted sleep score (#135) is unaffected — lock that with a test.<br>93.4 Tests: descriptive-only metric emits no `ageAverage`; the restless row still renders value + context; age-adjusted sleep score unchanged. | Stop the coach asserting an invented "age average restless moments is 13" that contradicts its own stated principle and Garmin's ≤5/hr guidance — the number Mark rightly challenged. | Shipped directly on `main` as `9ee1f5c` after local implementation landed there: descriptive-only metrics now emit `ageAverage: null` instead of a fabricated nightly average, the restless row still renders value + context honestly, and the age-adjusted sleep score remains unchanged because `restless_moments_count` still has no `_SLEEP_BANDS` entry. **No migration**; verdict / #133 / #135 / Red-never-VO2 unchanged; GitHub CI passed all seven jobs; production verified on implementation SHA `9ee1f5c` with Railway + Vercel same-origin health on that SHA, web `/` + `/sleep` 200, and unauthenticated `/api/v1/daily-loop` 401 via Vercel. Surfaced by Mark's 2026-07-12 feedback. Decision #166. |
| ~~Batch 94 — Check-in "feel": one recognisable vocabulary, surfaced by the verdict~~ | 🟢 Mid | Shipped | Design resolved 2026-07-12 (Craig): keep Batch 63's one-tap word scale, never show Mark the 0–10 number, rename the input to name the concept, and surface the current feel next to the verdict; reverting to a numeric 1–10 field is explicitly rejected (undoes #63's speed + re-introduces the number that caused the confusion) unless Mark asks for it.<br>94.1 Name the concept — `CheckInPage.tsx` now labels the existing Rough/Meh/OK/Good/Great one-tap scale **How you feel today** instead of **Overall**, still posting the same stored `subjectiveScore` and never rendering the raw 0–10 number in the UI.<br>94.2 Surface it by the verdict — Home now shows the saved feel directly under the verdict as `You said: **OK** · a bit more tired` with an inline **Change** link back to `/check-in`, reusing the same manual-entry path instead of adding a second state rail.<br>94.3 One vocabulary end-to-end — a shared frontend helper centralises the score→word mapping so the UI label, the tapped word, and the brief's phrasing (91.3) all use the same term (`OK`); the number appears on no surface.<br>94.4 Tests + web gates: `CheckInPage.test.tsx` and `DashboardPage.test.tsx` lock the renamed label, no-number contract, and Home recap round-trip; no manual-entry API change. | Let Mark recognise where he tells the app how he feels and hear it spoken back in the same words — closing the "can't find where to input this / it's not in the morning check-in" half of his report, without losing Batch 63's one-tap speed. | Shipped directly on `main` as `5ff3c54` with a docs-only closeout follow-up: the daily feel input is named as such and discoverable next to the verdict; one vocabulary (word, never number) now spans the input, the Home recap, and the brief; one-tap save still posts `subjectiveScore` unchanged (**no API/migration change**); GitHub CI passed all seven jobs on run `29191632779`; production verified first on the implementation SHA and rechecked after the docs-only redeploy with Railway + Vercel same-origin health, web `/` 200, and unauthenticated daily-loop 401 via Vercel. Numeric-scale revert stays out of scope unless Mark requests it. The phone eyeball that the renamed feel input is more recognisable remains deferred to live use. Surfaced by Mark's 2026-07-12 feedback. Decision #167. |

## Post-roadmap — 2026-07-12 walkthrough UX batch plan

On 2026-07-12 Craig drove the full reset "just woke up" flow through the PWA on
prod as Mark (data snapshotted + restored) and logged **13 UX/flow points**. All 13
were reconciled against code the same day — full findings, root causes, and
clickable refs live in [`WALKTHROUGH_FEEDBACK.md`](../WALKTHROUGH_FEEDBACK.md).
Unlike the Mark-note batches above (analysis-engine correctness), these are almost
all **front-of-app UX** — the morning Home, the check-in→brief flow, notifications,
and the fan/environment surface — plus one coaching-correctness item.

**Key structural finding.** Four Home points (2, 3, 9, 12) share one root: since
Batch 85 the pre-brief Home shows a "Say good morning" hero (`DashboardPage.tsx:662`),
but the action resolver (`homeActions.ts`) and section model (`homeSections.ts`)
have **no "brief not generated yet" concept** — so the action strip falls to "You're
all set" (`homeActions.ts:118`, contradicting the hero), the rest-day section model
auto-expands Last-night sleep (`homeSections.ts:39-44`), and nothing claims the top
slot for the check-in/unread brief. Batch 95 introduces that state once and the
cluster dissolves.

**Clustering → batches (95–102).** 95 pre-check-in Home state + check-in
discoverability (pts 2, 3, 4, 12); 96 brief lifecycle — unviewed surfacing + button
state (pts 8, 9); 97 passive generation — notify-when-ready + staged loader (pt 6);
98 rest-day/holiday-aware morning verdict (pt 7, **correctness**); 99 iOS
notification deep-link (pt 1); 100 multi-fan support + richer telemetry (pt 10); 101
dedicated Environment/Climate tab (pt 11); 102 presentation polish — personal status
line + "Tonight" data transparency (pts 5, 13).

**Sequencing.** 98 is the only 🔴 High (Mark is getting factually wrong rest-day
coaching) — run it first, independent backend. 95 → 96 → 97 is the natural Home/brief
chain (each builds on the prior's brief-state model); 99 lands with/before 97 so the
ready-push deep-links correctly. 100 → 101 (the tab needs the richer per-fan data).
102 is independent polish, anytime. **98 follows through the shipped Batch 90.2**
(holiday awareness added to the *flexibility* packet; 90's own notes flagged
threading the same treatment through the morning verdict). Tiers per #19: 98 High
(verdict correctness); the rest Mid (scoped frontend / feature work). Decision numbers
assigned at `/batch-start` (next free **#170**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 95 — Pre-check-in Home: honest state + check-in discoverability~~ | 🟢 Mid | Shipped | 95.1 Give Home a first-class "brief not generated yet" state: the hero shows `GoodMorningCta` ("Say good morning", `DashboardPage.tsx:662`) while the action strip falls through to **"You're all set"** (`homeActions.ts:118`) because the morning ladder's first rung needs `morningAnalysis != null` (`homeActions.ts:91-93`) and `NextActionStrip` renders unconditionally (`DashboardPage.tsx:686`). Add a pre-brief rung so the action agrees with the hero.<br>95.2 Stop auto-expanding Last-night sleep pre-brief — on `rest_day` `primarySection` returns `lastNight` and `splitPrimaryDetail` expands it (`homeSections.ts:39-44`, `DashboardPage.tsx:532-537`) even before any check-in; when no brief exists, lead with the check-in CTA and collapse `lastNight` to a one-line headline. **Decide at `/batch-start`:** model the pre-brief state as a new primary override vs. a guard in the existing `primary` selection.<br>95.3 Make the check-in equally discoverable from Sleep — render the `GoodMorningCta` (or equivalent) at the top of the `/sleep` hub when `daily.manualEntry == null` (pt 4), reusing the `/check-in` link.<br>95.4 Tests: pre-brief Home shows one coherent CTA (no "You're all set" beneath a "Say good morning" hero); `lastNight` collapses pre-brief and the check-in leads; Sleep shows the prompt iff no check-in exists; post-brief Home unchanged. | Make the pre-check-in Home tell one coherent story — invite the check-in, don't contradict it with "You're all set", don't pre-empt the coached read by auto-expanding raw sleep — and make the check-in discoverable from Sleep too. | **Kickoff decision:** guard in the existing primary/action selection, not a new override mechanism (Decision #168). Shipped via PR #118, squash `7e9770c`: `homeActions.ts` gains a `say-good-morning` rung so the Next strip agrees with the hero; `DashboardPage.tsx` guards `rest_day`'s pre-brief primary to `today` instead of auto-expanding `lastNight`; `SleepPage.tsx` reuses `GoodMorningCta` when `manualEntry == null`. **Frontend-only, no API/migration.** Web `tsc`/vitest (69 targeted + 222 full)/lint/build all green; CI green across push + PR-context waves plus Vercel preview; production verified on merge SHA `7e9770c` (Railway + Vercel same-origin health, web `/` 200, unauthenticated daily-loop 401 direct and via Vercel). Covers walkthrough pts 2, 3, 4, 12. Surfaced by Craig's 2026-07-12 walkthrough. The phone eyeball of the reset pre-check-in Home remains deferred to live use. |
| ~~Batch 96 — Brief lifecycle: unviewed-brief surfacing + button state~~ | 🟢 Mid | Shipped | 96.1 Track "brief viewed" per day (mirror `hasReviewedSleep`/`sleepReview.ts`), set when the brief is opened (`/brief`).<br>96.2 Top unviewed-brief CTA — when a brief exists but is unviewed, render a prominent "Your morning brief is ready — read it" **above** `TodayActions` (`DashboardPage.tsx:682`) so the unread read outranks the "Pre-cool the bedroom" thermal action (`morning_analysis.py:1159`) that currently sits highest (pt 9). Today the full brief lives at `/brief`, linked only from *inside* the Last-night section (`DashboardPage.tsx:560`).<br>96.3 Button state — on `CheckInPage`, once `brief` exists (`CheckInPage.tsx:332-350`) switch the primary button from "Get today's brief" to "View brief" (→ `/brief`/Home) instead of re-generate (`CheckInPage.tsx:319-330`) (pt 8).<br>96.4 Tests: unviewed brief leads Home above the action cards; opening it clears the flag; the check-in button reads "View brief" post-generation. | Give the freshly-generated, unread brief the top slot on Home and stop offering "generate" once it already exists. | Shipped via PR #119, squash `f7c4b16` (Decision #169): new `apps/web/src/lib/briefReview.ts` (`hasReviewedBrief`/`markBriefReviewed`, mirrors `sleepReview.ts`) tracks the per-day flag, set when `MorningBriefPage.tsx` mounts with a loaded analysis. `DashboardPage.tsx` renders a new `UnviewedBriefCta` above `TodayActions` whenever a brief exists and is unreviewed, outranking the thermal/plan action cards; opening `/brief` clears the flag. `CheckInPage.tsx`'s primary button now branches on `briefExists` (fresh-save `brief` state or `data.morningAnalysis` already loaded) and reads "View brief" (→ `/brief`) instead of re-offering to regenerate. **Frontend-only, no API/migration.** Web `tsc`/vitest (51 targeted + 226 full)/lint/build all green; CI green across push + PR-context waves plus Vercel preview; production verified on merge SHA `f7c4b16` (Railway + Vercel same-origin health, web `/` 200, unauthenticated daily-loop 401 direct and via Vercel). Covers pts 8, 9. Builds on Batch 95's brief-state model. Surfaced by Craig's 2026-07-12 walkthrough. The phone eyeball of the CTA/button-state change remains deferred to live use. |
| ~~Batch 97 — Passive brief generation: notify-when-ready + staged loader~~ | 🟢 Mid | Shipped | 97.1 Make generation non-blocking — today the check-in blocks on the LLM behind a "Reading your morning…" spinner (`CheckInPage.tsx:319-330`); submit should return immediately with an "I'll notify you when it's ready" acknowledgement and let Mark leave.<br>97.2 Backend async + ready-push — `regenerate_after_morning_checkin` generates inline and does **not** push (confirmed during the walkthrough); move generation off the request and fire a "brief ready" push on completion (reuse the verdict-push transport), deep-linking to the brief (with Batch 99).<br>97.3 Staged loader — replace the single spinner with a Domino's-tracker-style staged progress UI (syncing → reading → writing your brief).<br>97.4 Tests. **Decide at `/batch-start`:** background mechanism (in-request `BackgroundTask` vs a scheduler job vs a job row the client polls); staged loader as real progress vs timed stages; fallback when push is denied. | Let Mark leave the check-in and be pulled back by a notification when the brief is ready, with a polished progress read instead of a blocking spinner. | Shipped on `main` via implementation commit `0310cd6`: today's check-in now returns immediately, queues the existing regeneration seam in a FastAPI `BackgroundTasks` job, and sends a one-per-day `brief-ready-{date}` push targeting `/brief`; the page shows a staged progress card and falls back to polling the normal daily-loop snapshot with manual refresh when push is denied or missed. No migration, no new API shape, no verdict / #133 / #135 / Red-never-VO2 change. Local gates: full backend pytest **609 passed / 201 skipped**, backend `ruff check` clean, full web vitest **226 passed**, web build clean; local mypy still carries the known unrelated `pydreo` missing-stub baseline and web lint still carries the 6 pre-existing Fast Refresh warnings. GitHub Actions run `29210486906` passed all 7 jobs. Production verified on implementation SHA `0310cd6ae40c142e301b6c6af8581e48f3ae05ee`: Railway + Vercel same-origin `/api/v1/health` both served that SHA, web `/check-in` + `/brief` returned 200, and unauthenticated `/api/v1/daily-loop` returned 401 via Vercel. The live ready-push-on-device proof remains deferred to real use, and Batch 99 stays the focused-client iOS deep-link follow-on. Decision #170. |
| ~~Batch 98 — Rest-day / holiday-aware morning verdict~~ | 🔴 High | Shipped | 98.1 Assemble a deterministic rest-day/holiday flag into the morning packet — none exists today (`morning_analysis.py:332-398`); load `HolidayPauseService` active/upcoming window (mirroring shipped Batch 90.2 for the flexibility read) and/or derive "rest day" when every active planned workout is `skipped`.<br>98.2 Gate plan guidance on `workout.status` — `_plan_adjustments` (`morning_analysis.py:1501-1519`) ignores status and emits **"Proceed with the planned workout"** even for a `skipped` holiday ride; treat a skipped/holiday day as rest. The approve-ride *action* already guards `skipped` (`morning_analysis.py:1128-1134`) — bring the verdict/prose path to parity.<br>98.3 Prompt rule — `SYSTEM_PROMPT` (`morning_analysis.py:76-137`) gains: on a rest/holiday day, frame the verdict as a rest day and do **not** narrate the paused session as a live training decision; bump `PROMPT_VERSION`.<br>98.4 Tests: a holiday / all-skipped day yields a rest-day verdict that neither recommends nor relitigates the skipped endurance ride; a normal training day is unchanged. **Kickoff decision:** reuse Batch 90.2's holiday-window loader **and** derive rest from a non-empty all-`skipped` day; either signal can independently close the stale-row/data-gap failure. | Stop the morning brief coaching a ride on a day Mark isn't training — a holiday/rest day must read as rest, not "proceed with the planned workout". | Shipped via PR #120, squash `a0e133d` (Decision #171): deterministic `restDay` context, status-gated plan guidance, rest-day swap/action/weekly-shortfall suppression, and prompt v13. Both final CI waves passed all seven jobs (backend 813/813) plus Vercel preview; production verified on the merge SHA via Railway + Vercel same-origin health, web `/` + `/brief` 200, and unauthenticated daily-loop 401 direct/proxied. **No migration**; verdict floors (#133/#135/Red-never-VO2) untouched. Covers pt 7 (**correctness**). The next genuine rest/holiday morning wording check remains deferred to live use. |
| ~~Batch 99 — iOS notification deep-link~~ | 🟢 Mid | Shipped | 99.1 Fix the service-worker navigation — `notificationclick` focuses an existing client and calls `client.navigate(url)` (`sw.ts:107-123`), a **no-op on iOS PWAs**, so a running app stays on its last view; on a focused client `postMessage` the target URL and route via the SPA (an app-shell listener), keeping `openWindow(url)` for the cold-start path.<br>99.2 Ensure payloads carry a deep-link — confirm each push (`good_morning_nudge`, verdict, brief-ready) sets `data.url` (else it defaults to `/`, `sw.ts:109`); point the morning nudge at `/check-in`.<br>99.3 Tests: `notificationclick` posts/navigates to the payload URL; payload builders set `data.url`. | Tapping a coach notification opens the right screen, not wherever the app was last left. | Shipped via PR #121, squash `5f2e02e` (Decision #172): the service worker now normalizes the payload URL and `postMessage`s it to an already-open client instead of relying on `WindowClient.navigate()`, and a new app-shell listener routes that message through `react-router`; cold-start taps still deep-link via `openWindow`. Existing payload builders already satisfied the batch contract and remain unchanged (`/check-in` for good-morning, `/brief` for brief-ready, `/` for verdict). **Frontend + push-payload only, no migration.** GitHub CI passed both push and PR-context waves across all 16 checks plus the Vercel preview; production verified on merge SHA `5f2e02e9c4ef862ac8c7b5fec10b027bae25791b` via Railway + Vercel same-origin health, web `/` + `/check-in` `200`, and unauthenticated same-origin daily-loop `401`. Covers pt 1. Pairs with Batch 97 (ready-push deep-link). Surfaced by Craig's 2026-07-12 walkthrough. The real handset tap with the PWA already open remains deferred to live use. |
| ~~Batch 100 — Multi-fan support + richer fan telemetry~~ | 🟢 Mid | Shipped | 100.1 Multi-device in the Dreo client — the picker returns `fans[0]` and drops the rest (`dreo_fan.py:265-268`), so the app can only ever see/control one fan; enumerate all controllable fans with stable identities.<br>100.2 Richer state — `DreoFanState` already carries `oscillating` (`dreo_fan.py:98-103`) but the API exposes only `autoEnabled/isOn/speed` (`fan.py:39-43`); surface oscillation/"position", per-fan identity, and a computed **next-on time** from the overnight schedule (`run_fan_control`/`fan_control.loop_phase`).<br>100.3 Payload + frontend — the daily-loop `thermalState.fan` and the frontend `FanState`/`fanStatusText` (`dailyFlow.ts:53-94`) + `BedroomBody.tsx` become a per-fan list with the fuller detail.<br>100.4 Tests. **Decide at `/batch-start`:** number of fans / whether identity is user-labelled; scope of "next-on" (concrete time vs coarse "until tonight"). | Show Mark exactly what each of his fans is doing — setting, oscillation/position, and when it next comes on — across all of them, not just fan #1. | Shipped via PR #122, merge `0e60d5f`: the Dreo client now enumerates every controllable fan with stable serial+label identity, manual commands target an explicit `fanId`, `thermalState.fans`/frontend fan cards expose per-fan speed + oscillation + preset state, and the auto-target fan carries the existing overnight intent with concrete `21:30` next-on timing while the others surface live Dreo state. No migration; single-fan setups keep the old behavior through the fallback path. Closeout CI passed after two narrow fixes (date-stabilized manual-entry queue test and `pydreo` import/lint cleanup); production verified on Railway + Vercel same-origin health at the merge SHA, web `/` + `/sleep` 200, unauthenticated daily-loop 401 direct/proxied, and a real Dreo-account eyeball showed 3 controllable fans with one pinned auto-target. Covers pt 10. Precedes Batch 101. Surfaced by Craig's 2026-07-12 walkthrough. Decision #173. |
| ~~Batch 101 — Dedicated Environment/Climate tab~~ | 🟢 Mid | Shipped | 101.1 New route/tab — add `/environment` (or "Climate") to `navConfig.ts` + `TabBar.tsx` + routes in `App.tsx`.<br>101.2 Move controls — relocate the full fan/thermal control surface (`BedroomBody.tsx`) into the new tab.<br>101.3 Keep context in Sleep — leave a concise thermal summary + the single next action in the Sleep bedroom card (the pt 11 hybrid recommendation), linking through to the new tab.<br>101.4 Tests + nav. **Decide at `/batch-start`:** tab name (Environment vs Climate); top-level `TabBar` entry vs under the "More" menu (nav real-estate for an older user). | Give the growing multi-fan control surface its own home without ripping the sleep-relevant thermal summary out of the Sleep narrative. | Shipped via PR #123, squash `1bd2ac6`: new top-level `Climate` tab at `/environment`, legacy `/bedroom` retired into it, full multi-fan controls moved there, and Sleep's Tonight view reduced to concise thermal stats plus an `Open Climate` handoff link. **Frontend-only, no migration.** Both push and PR-context CI waves passed all 16 checks plus the Vercel preview; production verified on the merge SHA via Railway + Vercel same-origin health, web `/environment` + `/sleep` 200, and unauthenticated daily-loop 401 direct and via Vercel. Depends on Batch 100's per-fan data. Surfaced by Craig's 2026-07-12 walkthrough. Decision #174. |
| ~~Batch 102 — Presentation polish: personal status line + "Tonight" data transparency~~ | 🟢 Mid | Shipped | 102.1 Personal status line — the greeting is a bare "Good morning/…" (`copy.ts:41-46`, rendered `DashboardPage.tsx:650`) and the verdict a two-word label; compose a personable one-liner (name + verdict + time), Jarvis-style ("You're good to go today"), in `VerdictHero`/greeting (pt 13).<br>102.2 "Tonight" data transparency — extend `SleepPrepBody`'s evidence disclosure (`SleepPrepBody.tsx`) to state what the projection is based on so far (early: temperature only) and grow it through the day (pt 5).<br>102.3 Tests. **Decide at `/batch-start`:** ship 102.1 and 102.2 together or split (unrelated surfaces). | Small trust/warmth wins — a status line that sounds like a coach, and a "Tonight" card honest about how much it yet knows. | Shipped via PR #123, squash `1bd2ac6`: Home's top lockup now reads as a personable status line, and the shared Tonight body now states what the evening read is based on so far (personalized training/driver/room context or an explicit fallback note). **Frontend-only, no migration.** Both push and PR-context CI waves passed all 16 checks plus the Vercel preview; production verified on the merge SHA via Railway + Vercel same-origin health, web `/environment` + `/sleep` 200, and unauthenticated daily-loop 401 direct and via Vercel. Covers pts 5 and 13. Surfaced by Craig's 2026-07-12 walkthrough. Decision #175. |

## Post-roadmap — 2026-07-13 walkthrough follow-up batch plan

On 2026-07-13 Craig ran a second prod walkthrough (13th reset to pre-check-in,
driven as Mark) — this time against the **just-shipped Batches 95–102** — and logged
8 points. All 8 reconciled against the current code (full write-up in
[`WALKTHROUGH_FEEDBACK.md`](../WALKTHROUGH_FEEDBACK.md), 2026-07-13 section). Most are
**follow-up gaps in the batches that just landed**, several in the exact code they
touched:

1. **Pre-brief Home shows the CTA twice.** Batch 95 fixed the "You're all set"
   contradiction by adding a `say-good-morning` rung (`homeActions.ts:95-96`), which
   now echoes the `GoodMorningCta` hero's "Say good morning" + "Get today's brief"
   (`GoodMorningCta.tsx:38,44`) — two prompts for one action.
2. **Last-night sleep still reachable pre-brief** — the `lastNight` section renders
   collapsed under "More detail" before a brief exists (Batch 95 only stopped the
   auto-expand).
3. **Sleep page ungated** — Batch 95.3 added the check-in CTA (`SleepPage.tsx:88`) but
   the overnight chart / tonight / bedroom stats still render pre-check-in.
4. **Overnight graph on both surfaces** — `OvernightChartCard` renders on
   `SleepPage.tsx:124` *and* `EnvironmentPage.tsx:75`; wanted Sleep-only, Climate
   current-only.
5. **No calendar** — the Sleep page is a `last-night`/`tonight` binary
   (`SleepPage.tsx:21-26`).
6. **No read-aloud** — no TTS anywhere.
7. **Walking base + breathwork mis-filed** — both render in the Today workout card
   (`DashboardPage.tsx:1044-1045`).
8. **Thermal subsystem ignores holiday** — `run_evening_sleep_nudge` (`scheduler.py:180`),
   `run_evening_monitoring_alerts` (`scheduler.py:200`) / `evaluate_thermal_alert`
   (`nudge_alerts.py:264`), and `run_fan_control` (`scheduler.py:975`) never consult
   `HolidayPauseService`; the 12th (a holiday) fired an evening_nudge (19:00) + a
   **critical** thermal_alert (20:45). Batch 98 made only the *morning verdict*
   holiday-aware.

**Sequencing.** 105 (holiday env) is the correctness item — Mark gets false critical
alerts and a fan cooling an empty room on holiday — run it first, independent backend,
sibling of Batch 98. 103 (pre-check-in gating) directly finishes Batch 95 and is the
biggest UX win. 104 (placement) + 107 (calendar) pair with the sleep/climate split. 106
(TTS) is independent. Tiers per #19: 105 High (thermal correctness); the rest Mid.
Decision numbers assigned at `/batch-start` (next free **#176**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 103 — Pre-check-in gating: Home + Sleep~~ | 🟢 Mid | Shipped | 103.1 De-duplicate the pre-brief CTA — Batch 95 added a `say-good-morning` rung (`homeActions.ts:95-96`) that now echoes the `GoodMorningCta` hero's "Say good morning" + "Get today's brief" button (`GoodMorningCta.tsx:38,44`); pre-brief Home shows the same prompt twice. Suppress the strip's rung while the hero renders (or fold the hero CTA into the strip). (Issue 1)<br>103.2 Hide last-night sleep pre-brief — Batch 95 stopped auto-expanding `lastNight` but the section still renders collapsed under "More detail" (`DashboardPage` sections + `splitPrimaryDetail`); don't render it until a brief exists. (Issue 2)<br>103.3 Gate the Sleep page — Batch 95.3 added the check-in CTA (`SleepPage.tsx:88`) but the body (metrics, overnight chart, tonight projection, bedroom stats) still renders pre-check-in; show only the `GoodMorningCta` until `manualEntry`/brief exists. (Issue 3) **Decide at `/batch-start`:** this makes the *optional* check-in (Batch 60/#127) a soft gate to seeing sleep — confirm the stance (gate fully vs. minimal headline + gate the detail).<br>103.4 Tests: pre-brief Home shows one CTA (no duplicate say-good-morning); `lastNight` absent pre-brief; `/sleep` shows only the CTA pre-check-in; post-check-in unchanged. | Make the whole pre-check-in surface (Home + Sleep) show one clear "say good morning" and nothing else — no duplicate CTA, no last-night sleep, no Sleep-page data until Mark has checked in. | Shipped via PR #124 / squash `b3f458c`: pre-brief Home now shows only the `GoodMorningCta`, with the duplicate Next strip suppressed and raw `lastNight` removed until a brief exists; `/sleep` shows only that same hero until a check-in or generated brief exists, then the unlocked post-check-in Sleep experience is unchanged. **Frontend-only, no API/migration**; local vitest/lint/build green; production verified on Railway + Vercel same-origin health at the merge SHA, web `/` + `/sleep` 200, and unauthenticated `/api/v1/daily-loop` 401 direct and via Vercel. Covers 13th Issues 1, 2, 3. Follows through Batch 95. Decision #176. |
| ~~Batch 104 — Right-surface placement: overnight graph + walking/breathwork~~ | 🟢 Mid | Shipped | 104.1 Overnight graph → Sleep only — `OvernightChartCard` renders on both `SleepPage.tsx:124` and `EnvironmentPage.tsx:75`; remove it from `EnvironmentPage` so Climate is the live room + fan controls only and the retrospective chart lives solely with Sleep. (Issue 4)<br>104.2 Relocate walking base + breathwork — both render inside `DayPlanBody` (the Today/workout card) at `DashboardPage.tsx:1044-1045`; move breathwork to the Sleep/wind-down context (a recovery lever) and walking base to a baseline/Trends strip, keeping the "credit non-cycling activity" intent but out of the today-workout card. (Issue 7)<br>104.3 Tests + nav. **Settled at `/batch-start`:** walking base moves to **Trends** (not Sleep/Home), and Climate keeps a lightweight **last night → Sleep** link after the chart moves out. | Put each surface's content where it belongs — the overnight review with Sleep (not Climate) and walking/breathwork out of the today-workout card — so Climate is about *now* and the workout card is about *today's session*. | Shipped via PR #125, squash `0b4027e`: the overnight chart now lives only on Sleep, Climate shows live room + fan controls only plus a lightweight handoff back to Sleep, and the Today workout card no longer carries the rolling walking-base or breathwork summaries. A compact walking-base strip now lives on Trends, and Sleep Tonight now carries the breathwork rhythm context. **Frontend-only, no API/migration**; focused vitest/lint/build green; production verified on Railway + Vercel same-origin health at the merge SHA, web `/` 200, and unauthenticated `/api/v1/daily-loop` 401 via Vercel. Covers 13th Issues 4, 7. Refines Batches 101/86. Surfaced by Craig's 2026-07-13 walkthrough. Decision #177. |
| ~~Batch 105 — Holiday-aware environment: evening nudges + fan~~ | 🔴 High | Shipped | 105.1 Gate evening nudges on holiday — `run_evening_sleep_nudge` (`scheduler.py:180`) and `run_evening_monitoring_alerts` (`scheduler.py:200`) / `evaluate_thermal_alert` (`nudge_alerts.py:264`) don't consult `HolidayPauseService`; skip the evening cooling nudge + thermal alert when an active holiday window covers the date. (Issue 8)<br>105.2 Pause the fan autopilot on holiday — `run_fan_control` (`scheduler.py:975`) runs regardless; skip/idle it during an active holiday window so it isn't cooling an empty bedroom.<br>105.3 Share Batch 98's window load — reuse the holiday/`restDay` lookup Batch 98 added to `morning_analysis` so the evening/fan jobs use one source.<br>105.4 Tests: an active holiday window suppresses the evening nudge, the thermal alert, and the fan tick; a normal day is unchanged (fixture: the 2026-07-12 holiday fired evening_nudge 19:00 + critical thermal_alert 20:45). **Settled at `/batch-start`:** the existing active holiday window is the private app's away flag; do not add a parallel away-state model. | When Mark's on holiday, the whole thermal subsystem goes dormant — no evening cooling nudges, no critical thermal alerts, no fan autopilot for an empty room. | Shipped via PR #126 / squash `08187fe`: scheduled work now uses the active-only form of Batch 98's shared holiday-window truth. An active holiday skips the sleep nudge, disables only the thermal monitoring branch (the independent stale-source guardian remains live), and makes fan control a true no-op before temperature/Dreo/tick work; early resume and normal dates retain the old behavior. **Backend-only, no migration/API/shared/frontend or threshold/verdict change.** Relevant pytest **133 passed / 27 skipped**, Ruff/format and mypy clean; both final CI waves plus post-merge `main` CI passed all 7 jobs. Production confirmed Mark's active 12–16 July window, health on the merge SHA, a real `holiday_away` fan skip with no tick, and a scheduled thermal skip with no new alert. The implementation landed after the day's 20:00 nudge slot, so the next genuine holiday-evening sleep-nudge fire remains live-use proof rather than a manufactured production action. Covers 13th Issue 8 (**correctness**). Decision #178. |
| ~~Batch 106 — Read-aloud brief (text-to-speech)~~ | 🟢 Mid | Shipped | 106.1 Add a "Listen" control to the morning brief — no TTS exists anywhere; add a play/pause control on `MorningBriefPage`/the `Markdown` render that speaks the brief's plain text via the browser `SpeechSynthesis` API (zero-cost, offline). (Issue 6)<br>106.2 Plain-text extraction + controls — strip markdown to readable prose, sensible pacing, pause/stop; ephemeral (persist nothing).<br>106.3 Tests + a11y. **Settled at `/batch-start`:** use built-in `SpeechSynthesis` first (free, offline, private) rather than a hosted TTS service. | Let Mark listen to his morning brief instead of reading it. | Shipped via PR #127, squash `495149b`: the brief now has Listen / Pause / Resume / Stop controls that read it aloud via `SpeechSynthesis`, and markdown is converted into clean spoken prose before playback. **Frontend-only, no API/migration**; both CI waves passed all 7 jobs plus the Vercel preview; production verified on Railway + Vercel same-origin health at the merge SHA, web `/` + `/brief` 200, and unauthenticated `/api/v1/daily-loop` 401 direct and via Vercel. The phone/speaker playback eyeball remains deferred to live use. Covers 13th Issue 6. Surfaced by Craig's 2026-07-13 walkthrough. Decision #179. |
| ~~Batch 107 — Sleep/climate calendar~~ | 🟢 Mid | Shipped | 107.1 Extend beyond last-night/tonight — the Sleep page is a `last-night`/`tonight` split (`SleepPage.tsx:21-26`); add a calendar/date browse of past nights (sleep + the overnight room/fan chart per date) plus a look-ahead. The overnight chart already has a night pager (Batch 31) and `GET /api/v1/bedroom/overnight` supports recent-nights — build on those. (Issue 5)<br>107.2 History reads — confirm the sleep/metrics history endpoints cover the calendar range (Trends/Reviews already read history); add any missing date-addressable read.<br>107.3 Tests. **Decide at `/batch-start`:** scope — a sleep/climate calendar (pairs with Issue 4's split) vs. a whole-app calendar unifying workouts (Week tab) + sleep + environment; new surface vs. extension of Sleep/Week. | Let Mark browse his sleep and room history by date — a real calendar — instead of only last night and tonight. | Shipped via PR #128, squash `7cca6e2`: `/sleep` now has a month-grid calendar on the Last night tab that reloads a selected past date's stored sleep snapshot plus the matching overnight room/fan chart, while Tonight stays the current forward look-ahead. **Frontend-only, no API/migration**; it reuses the existing date-addressable daily-loop and bedroom-overnight reads, keeps the pre-check-in gate unchanged, and covers raw-sleep-only dates with a compact fallback summary. Both CI waves passed all 7 jobs plus the Vercel preview; production verified on Railway + Vercel same-origin health at the merge SHA, web `/` 200, and unauthenticated `/api/v1/daily-loop` plus `/api/v1/bedroom/overnight` 401 via Vercel. Covers 13th Issue 5. Scope stayed pinned to Sleep/Climate, not a whole-app calendar. Decision #180. The phone eyeball of the calendar browse flow remains deferred to live use. |

## Post-roadmap — 2026-07-14 walkthrough follow-up batch plan

The 13th walkthrough continued into the 14th (the reset-13th test rolled past BST
midnight), and Craig logged a further wave. All reconciled against current code — with
**Batches 95–107 now all shipped**, these are follow-up gaps in that just-landed work
(full write-up in [`WALKTHROUGH_FEEDBACK.md`](../WALKTHROUGH_FEEDBACK.md), 2026-07-14
section). Two need no batch:

- **"Generated for the 14th" is not a bug** — the clock rolled past midnight, so
  `_local_today` (`daily_loop.py:128`) correctly returned the 14th; the reset-13th
  test simply went stale.
- **"Remove last night from Climate" is already done** — Batch 104 removed
  `OvernightChartCard` from `EnvironmentPage`.

The rest → Batches 108–112:

1. **The pre-check-in gate swallowed the calendar** — Batch 103's `hasSleepAccess`
   (`SleepPage.tsx:105`) hides the whole page, including Batch 107's `SleepDateCalendar`
   (`:124/:128`), until today's check-in — so other dates aren't browsable pre-check-in.
2. **The calendar is expanded by default and shows sleep-only** for a past date
   (`SleepPage.tsx:133-168`), though `historyData` holds the whole day.
3. **Home `tonight`/`bedroom` ignore holiday** — Batch 105 gated only the backend
   scheduler; the frontend sections have no holiday awareness.
4. **Check-in flow leftovers** — the staged progress is a card below the form
   (`CheckInPage.tsx:405-453`) not in-place, and the inline brief render
   (`:456-473`) still duplicates what Home's `UnviewedBriefCta` now shows.
5. **Home says the verdict twice** — the lockup `personalStatusLine`+date
   (`DashboardPage.tsx:684-691`) duplicates `VerdictHero` (`:697`).
6. **Read-aloud is robotic** — Batch 106 shipped the default `SpeechSynthesis` voice.
7. **Stale/redundant notifications** — the critical thermal alert deep-links to the
   retired `/bedroom` route (`nudge_alerts.py:388`, now a redirect, `App.tsx:115`), and
   `verdict_push` (→`/`) overlaps `brief_ready_push` (→`/brief`) for the same brief.

**Sequencing.** 108 first — it's a regression (Batch 103's gate broke Batch 107's
calendar). 110 (brief-flow + verdict de-dup) is a quick UI cleanup. 109 completes Batch
105's holiday-awareness on the frontend. 112 is backend notification hygiene. 111 (voice)
hinges on the on-device-vs-hosted-TTS privacy call. Tiers per #19: all Mid (108's
calendar-gate is a regression fix, worth prioritising) — no 🔴 High this wave, since
Issue 12 turned out not to be a defect. Decision numbers assigned at `/batch-start` (next
free **#181**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 108 — Sleep calendar: today-scoped gate + collapse-default + whole-day~~ | 🟢 Mid | Shipped | 108.1 Make the pre-check-in gate today-scoped — Batch 103's `hasSleepAccess` gate (true only once today's `manualEntry` or `analysis` exists, `SleepPage.tsx:105`) hides the whole page including Batch 107's `SleepDateCalendar` (`SleepPage.tsx:124/:128`) until today's check-in; let the calendar + historical dates through, gating only *today's* sleep detail. (Issue 10)<br>108.2 Collapse the calendar by default — `SleepDateCalendar` renders expanded; default it collapsed behind a toggle. (Issue 15a)<br>108.3 Whole-day history view — the selected-date view renders sleep+room only (`historyData` from the date's daily-loop) though it already holds the whole day; surface that day's workouts + verdict too. **Settled at `/batch-start`:** extend the Sleep history view in place rather than adding a shared whole-day viewer reused by the Week tab.<br>108.4 Tests: calendar + past dates reachable pre-check-in; today's detail still gated; calendar collapsed by default; a past date shows the whole day. | Fix the 103×107 interaction — the calendar and other dates stay browsable before today's check-in, the calendar isn't expanded by default, and a past date shows the whole day, not just sleep. | Shipped via PR #129, squash `a0719e2`: `/sleep` now keeps the calendar and past dates browseable before today's check-in while still gating today's current-date detail, defaults the calendar collapsed behind a Show/Hide toggle, and adds an in-place historical **The whole day** summary card with the stored verdict plus planned sessions. **Frontend-only, no API/migration**; it reuses the existing date-addressable daily-loop and bedroom-overnight reads, preserves the current-day pre-check-in gate, and keeps the change scoped to Sleep rather than a shared day viewer. Both CI waves passed all 7 jobs plus the Vercel preview; production verified on Railway + Vercel same-origin health at the merge SHA `a0719e2`, web `/sleep` 200, and unauthenticated `/api/v1/daily-loop` plus `/api/v1/bedroom/overnight?date=2026-07-13` 401 via Vercel. Covers 13th Issues 10, 15. Follows through Batches 103 + 107 (fixes a regression). Decision #181. The phone eyeball of the pre-check-in calendar browse flow remains deferred to live use. |
| ~~Batch 109 — Holiday-aware Home & Sleep surfaces~~ | 🟢 Mid | Shipped | 109.1 Gate the Home `tonight` + `bedroom` sections on holiday — they have no holiday awareness (`homeSections.ts`/`DashboardPage`); Batch 105 gated only the backend scheduler. Hide (or collapse to an "away — resumes <date>" note) these sections during an active holiday. (Issue 9)<br>109.2 Same on Sleep — the Tonight tab's bedroom/prep honours holiday too.<br>109.3 Confirm the holiday flag on the daily-loop payload (mirror `plan_actions.isHoliday`/`character.is_holiday`); add it if the payload doesn't already carry it. **Decide at `/batch-start`:** hide entirely vs. a dormant "away — resumes <date>" placeholder.<br>109.4 Tests: an active holiday hides tonight/bedroom on Home + Sleep; a normal day is unchanged. | When Mark's away, the Home and Sleep bedroom-prep surfaces go dormant too — completing Batch 105's backend holiday-awareness on the frontend. | Shipped via PR #130 / squash `443d19b`: the existing holiday-window seam now reaches `/api/v1/daily-loop` as a minimal `holiday { isActive, activeWindow }` payload, and Home plus Sleep use that one flag to render dormant away-state placeholders instead of live evening/bedroom guidance. Home's **Tonight** and **Bedroom** sections keep their place in the information architecture but swap to a Holiday handoff with a resume date, Sleep's **Tonight** tab collapses to the same away-state, and the Home evening **Protect tonight's sleep** action is suppressed while the holiday window is active so the action strip cannot contradict the dormant UI. **Frontend + payload flag only, no migration**; both final CI waves passed all 7 jobs plus the Vercel preview; production verified on Railway + Vercel same-origin health at merge SHA `443d19b`, web `/` + `/sleep` + `/holiday` `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. An authenticated holiday-date surface eyeball remains deferred to live use because the protected preview review could only be taken as far as the login route without a live session. Covers 13th Issue 9. Completes Batch 105 on the frontend. Surfaced by Craig's 2026-07-14 walkthrough. Decision #182. |
| ~~Batch 110 — Brief-flow UI de-dup: in-place progress + drop duplicated renders~~ | 🟢 Mid | Shipped | 110.1 In-place generation progress — the staged progress is a separate card below the form (`CheckInPage.tsx:405-453`); move it to the point of entry (replace the button/entry area). (Issue 11a)<br>110.2 Remove the inline brief render — `CheckInPage.tsx:456-473` still renders `VerdictHero` + the full brief Markdown below the form; remove it now that Home surfaces the unviewed brief (Batch 96 `UnviewedBriefCta`). (Issue 11b)<br>110.3 De-dup the Home verdict lockup — the greeting lockup `personalStatusLine(verdict, name)` + date (`DashboardPage.tsx:684-691`) duplicates `VerdictHero` (verdict + the same `dateLabel`, `:697`); drop the redundant date and/or stop echoing the verdict twice. (Issue 14)<br>110.4 Tests. | Remove the redundant/duplicated UI the recent batches left in the brief flow — progress in-place, no inline brief on the check-in page, and the verdict/date shown once on Home. | Shipped via PR #131, squash `1f46ac4`: the check-in page now shows the staged generation progress in the button's own spot instead of a separate card below it, and no longer renders the brief inline once it lands — Home's `UnviewedBriefCta` (Batch 96) already surfaces it, so Check-in's job ends at "saved". Home's greeting lockup drops its own date line since `VerdictHero`/`GoodMorningCta` right below already carry it. **Frontend-only, no API/migration**; full web vitest (243 passed), `tsc --noEmit`, lint, and production build all clean; both CI waves passed all 7 jobs plus the Vercel preview. Production verified on Railway + Vercel same-origin health at merge SHA `1f46ac4`, web `/`, `/check-in`, `/brief` all `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. Covers 13th Issues 11, 14. Follows through Batches 96/97/102. Surfaced by Craig's 2026-07-14 walkthrough. Decision #183. The Vercel preview visual eyeball remains deferred — the preview is behind Vercel account SSO, not the app's own invite-only login, so it could not be reached without Craig's Vercel credentials. |
| ~~Batch 111 — Natural read-aloud voice~~ | 🟢 Mid | Shipped | 111.1 Upgrade the brief's read-aloud voice — Batch 106 shipped with the default robotic `SpeechSynthesis` voice; select a natural/engaging voice (best available `SpeechSynthesis` voice per platform) or introduce a neural/hosted TTS. (Issue 13)<br>111.2 **Decide at `/batch-start` (central):** best on-device `SpeechSynthesis` voice (free, private, quality varies by device) vs. a hosted neural TTS (much better voice, but sends the health-data brief text to a third party — weigh against the app's paid-API-for-privacy stance). Default to the best on-device voice; go hosted only if quality demands it, with explicit consent.<br>111.3 Tests + a11y. | Make the read-aloud sound like a person, not a robot — without quietly shipping Mark's health data to a cloud voice service. | The brief reads aloud in a natural voice; if a hosted TTS is chosen, the health-data privacy tradeoff is explicitly decided/consented; **frontend (+ TTS integration if hosted)**; gates green; closeout eyeball on device. Covers 13th Issue 13. Follows through Batch 106. Surfaced by Craig's 2026-07-14 walkthrough. Shipped via PR #132, squash `c90f825`: stayed on-device (Decision #184) rather than hosted TTS, holding Decision #179's privacy line — `selectBestVoice` picks the best `localService===true` voice by locale + quality-keyword name, explicitly excluding remote-synthesis voices like Chrome's default "Google UK English" (which sends text to Google's servers despite exposing the same `SpeechSynthesisVoice` shape). **Frontend-only, no API/migration**; full web vitest (249 passed), `tsc --noEmit`, lint, and production build all clean; both CI waves passed all 7 jobs plus the Vercel preview. Production verified on Railway + Vercel same-origin health at merge SHA `c90f825`, web `/` and `/brief` both `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. The real-device voice-quality eyeball remains deferred to live use — voice availability/quality is OS/browser-dependent and can't be verified headlessly, same as Batch 106's original playback eyeball. |
| ~~Batch 112 — Notification cleanup: stale routes + redundant brief push~~ | 🟢 Mid | Shipped | 112.1 Fix the stale thermal route — the critical thermal alert's `data.url` is `/bedroom` (`nudge_alerts.py:388`), a redirect-only legacy route (`App.tsx:115` → `/environment`); point it (and any other `/bedroom` deep-links) directly at `/environment`. (Issue 17)<br>112.2 Consolidate the "brief ready" notifications — `verdict_push` (→`/`, `nudge_alerts.py:161`, from `run_morning_weather_sync`) and `brief_ready_push` (→`/brief`, Batch 97) both announce the morning brief with different URLs; converge on one notification/URL (prefer `/brief`), retiring the redundant path. **Decide at `/batch-start`:** whether the 09:30 backstop sends `brief_ready` too vs. keeps a distinct verdict push.<br>112.3 Thermal URL consistency — align the four thermal severities on one deep-link target.<br>112.4 Tests: notification builders point at live routes; one brief-ready notification per brief. | Retire the stale/redundant notifications the shipped batches left behind — no dead `/bedroom` deep-links and one coherent "your brief is ready" push. | Shipped via PR #133, squash `d78981b`: the 09:30 backstop (`run_morning_weather_sync`) now calls the same `push_brief_ready` the check-in path uses instead of a separate `verdict_push`, so exactly one `/brief` notification fires per day regardless of which path generates the brief first (`build_verdict_push_plan`/`ANALYSIS_TYPE_VERDICT_PUSH`/`push_morning_verdict` removed outright). All five thermal-alert severities — including the fan-can't-cope critical alert that pointed at the redirect-only `/bedroom` route — now share one `THERMAL_URL = "/environment"` target. **Backend-only, no migration, no web changes**; both CI waves passed all 7 jobs plus the Vercel preview (no visual diff, backend-only); production verified on Railway + Vercel same-origin health at merge SHA `d78981b`, web `/` `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. Covers 13th Issue 17. Holiday-gating already handled by Batch 105. Surfaced by Craig's 2026-07-14 walkthrough. Decision #185. The live on-device trigger of a thermal alert + brief-ready push to eyeball the actual deep-link click-through remains deferred to live use — push click-through can't be exercised headlessly. |

## Post-roadmap — 2026-07-14 walkthrough follow-up batch plan (wave 2)

A second 14th wave (the walkthrough continued through the morning). 13 points reconciled
against current code — **Batches 108–112 now shipped too** (full write-up in
[`WALKTHROUGH_FEEDBACK.md`](../WALKTHROUGH_FEEDBACK.md), 2026-07-14 wave 2 section). One is
already fixed and one is partial:

- **Fixed:** the check-in progress now replaces the button in place with no inline brief
  (Batch 110, `CheckInPage.tsx:378`) — that observation predated the fix.
- **Partial:** Batch 110 removed the duplicate *date* above the verdict, but the personable
  status line still doubles it (`DashboardPage.tsx:733`) → Batch 115.

The rest → Batches 113–119:

1. **Holiday still leaks thermal where the scheduler-only gate (105) didn't reach** — the
   brief's thermal review + "pre-cool" action (`morning_analysis.py:1202/1226`) and Sleep's
   Last-night fan chart (`SleepPage.tsx:194`) all still show on a holiday; and the pre-cool
   action links to `/sleep` instead of `/environment` (`:1222`).
2. **Check-in CTAs don't clear** — Home's hero gates on `analysis` not `manualEntry`
   (`DashboardPage.tsx:739`, so Batch 97's async window still nags), and Sleep's "Add today's
   check-in" card shows *because* `hasSleepAccess` is true after check-in (`SleepPage.tsx:286`).
3. **Home doubles the verdict/greeting** (`:733` above `:740`) and stacks "You're all set"
   (`:770`) against a Today card saying the same.
4. **Read-aloud is still robotic** — Batch 111 picked the best *on-device* voice
   (`BriefListenControls.tsx:60`); the natural voice needs the deferred hosted-TTS path.
5. **Calendar chevrons page by month** (`SleepDateCalendar.tsx:40`), not day.
6. **Feedback is only a 3-way rating** (`FeedbackControl.tsx`) — Mark wants more specific.
7. **No follow-up conversation** on a brief — it answers only the single check-in question.

**Sequencing.** 113 (holiday-thermal) and 114 (check-in CTAs) are the most user-visible daily
annoyances — do first. 115 (Home de-dup) is quick. 116 (hosted voice) and 119 (follow-up chat)
are the two heavier, decision-gated pieces (both carry a health-data choice — a third-party
voice API, a conversational surface). 117 (calendar day-nav) + 118 (feedback vocabulary) are
small. Tiers per #19: all Mid. Decision numbers assigned at `/batch-start` (next free **#186**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 113 — Holiday hides all thermal surfaces (brief + last-night) + pre-cool link~~ | 🟢 Mid | Shipped | 113.1 Holiday-gate the brief's thermal content — `build_today_actions`/`_thermal_action` (`morning_analysis.py:1202/1226`) still emit "Pre-cool the bedroom" on a holiday, and the packet/prompt still include the thermal/environment review (`include_thermal_environment_review`); suppress both when `restDay.insideHolidayWindow`. (Issues 3, 7)<br>113.2 Fix the pre-cool link — `_thermal_action` href is `/sleep` (`morning_analysis.py:1222`); point it at `/environment` post-Batch-101. (Issue 12)<br>113.3 Holiday-gate Sleep's Last-night fan — the Last-night tab still renders `OvernightChartCard` on holiday (`SleepPage.tsx:194`); suppress/replace the fan chart+glance with a dormant note when `holiday.isActive` (mirror the Tonight tab's "Holiday away" card). (Issue 6)<br>113.4 Tests: a holiday brief has no thermal review + no pre-cool action; the pre-cool action (off-holiday) links to `/environment`; Sleep Last-night hides the fan on holiday; off-holiday unchanged. | Complete the "holiday = away → no thermal/fan anywhere" principle across the brief (review + action) and the Sleep last-night surface, and fix the pre-cool link to the Climate tab. | Shipped via PR #134, squash `45e008b`: settled the kickoff fork (113.4) as gating on `restDay.insideHolidayWindow` specifically, not any `restDay.isRestDay` — an all-skipped local rest day still happens with Mark at home, so the bedroom thermal read stays relevant there. `environment.thermalReview` is now null and `include_thermal_environment_review` drops from `prompt.outputRules` on a holiday, `_thermal_action` never fires so no "Pre-cool the bedroom" action reaches `todayActions`, and its href moves from the stale `/sleep` to `/environment` unconditionally. Sleep's Last-night tab now swaps `OvernightChartCard` for a dormant "Holiday away" card mirroring the Tonight tab's existing placeholder (Batch 109) when `holiday.isActive`. **CI:** both branch-push and PR-context waves passed all 7 jobs plus the Vercel preview (preview sat behind Vercel account SSO, so the visual eyeball was deferred to live use, consistent with prior batches). **Production on merge SHA `45e008bbdd3316f8f0b57ade2e2b0368482f0b2d`:** Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/`, `/sleep`, and `/brief` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. **Boundaries:** no migration, no scheduler/verdict/threshold change; Green/Amber/Red, #133, #135, Red-never-VO2 untouched. Covers 14th Issues 3, 6, 7, 12. Extends Batches 105/109. Surfaced by Craig's 2026-07-14 walkthrough. Decision #186. **Deferred live-use:** the closeout holiday-date phone eyeball of the brief (no thermal section/pre-cool action) and Sleep's Last-night tab (dormant card) remains deferred — production has no live holiday window right now to eyeball against. |
| ~~Batch 114 — Check-in CTAs clear after check-in~~ | 🟢 Mid | Shipped | 114.1 Home hero — it shows `GoodMorningCta` while `analysis == null` (`DashboardPage.tsx:739`), so the Batch 97 async window (checked in, brief pending) still shows "say good morning"; gate on `manualEntry` so once checked in it shows a "generating your brief…" state, not the check-in CTA. (Issue 8)<br>114.2 Sleep card — the "Add today's check-in" card renders whenever `hasSleepAccess` is true (`SleepPage.tsx:286`), i.e. after check-in; gate it on `manualEntry == null` so it's offered only when not yet checked in. (Issue 11)<br>114.3 Tests: post-check-in Home shows a generating/there state not the check-in CTA; Sleep hides the Add-check-in card once checked in; pre-check-in unchanged. | Once Mark has checked in, no surface still invites him to check in — Home shows "generating", Sleep drops the add-check-in card. | Shipped via PR #135, squash `c7c2dba`: gated both surfaces on `manualEntry` rather than on whether an analysis/brief exists yet. Home's hero gains a new `BriefGeneratingCta` ("Writing your brief…") rendered when `analysis == null && manualEntry != null`, between `VerdictHero` and the original `GoodMorningCta`. Sleep's "Add today's check-in" card now only renders when `manualEntry == null`, so it still offers the optional check-in when the sleep surface unlocked via the 09:30 backstop's auto-generated brief without a manual check-in, but drops once he's actually checked in. **CI:** both branch-push and PR-context waves passed all 7 jobs plus the Vercel preview (preview sat behind Vercel account SSO, so the visual eyeball was deferred to live use, consistent with prior batches). **Production on merge SHA `c7c2dbad69fb553ac5f3a91718a88a45e8f61d13`:** Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/` and `/sleep` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. **Boundaries:** frontend-only, no API/shared-schema/migration change; Green/Amber/Red, #133, #135, Red-never-VO2 untouched. Covers 14th Issues 8, 11. Follows through Batches 95/97. Surfaced by Craig's 2026-07-14 walkthrough. Decision #187. **Deferred live-use:** the phone eyeball of the checked-in-but-generating Home state and the check-in-card drop on Sleep remain deferred — verified via component tests asserting the exact DOM/ARIA states instead, since manufacturing the live async window against prod data isn't practical to stage. |
| ~~Batch 115 — Home lockup + Today/all-set de-dup~~ | 🟢 Mid | Shipped | 115.1 De-dup the status line vs the verdict hero — the lockup `personalStatusLine(verdict, name)` (`DashboardPage.tsx:733`) restates the verdict (and greeting) directly above `VerdictHero` (`:740`); Batch 110 removed the duplicate date but the line still doubles the verdict — fold the personable line into the hero or drop it. (Issue 4)<br>115.2 Resolve the "You're all set" / Today overlap — `NextActionStrip`'s all-set state (`DashboardPage.tsx:770`) and the Today section both say "nothing to do"; suppress the all-set strip when the Today section already conveys it (esp. rest/holiday days). (Issue 5)<br>115.3 Tests. | Home states the verdict once and doesn't stack "You're all set" against a Today card that already says the same. | Shipped via PR #136, squash `b97e90b`: the status-line paragraph above `VerdictHero` is gone once a verdict exists — `personalStatusLine(...)` now feeds the hero's own `line` prop instead of a separate `<p>` restating the same "good to go" wording; pre-verdict, the plain greeting still shows above the check-in CTAs since those carry no greeting of their own. The quiet "You're all set" `NextActionStrip` is now suppressed specifically when `action.key === 'all-set' && (dayState.isRest || holiday.isActive)` — an active Next action still always surfaces regardless of day type. **Frontend-only, no migration**; targeted `DashboardPage.test.tsx` 45 passed (3 new), full web vitest 254 passed, `tsc --noEmit` clean, lint clean (pre-existing warnings only), production build clean. **CI:** both branch-push and PR-context waves passed all 7 jobs plus the Vercel preview. **Production on merge SHA `b97e90b9dc286ff932ab71c33c7cb8b744cbc958`:** Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. Covers 14th Issues 4, 5. Follows through Batches 102/110. Surfaced by Craig's 2026-07-14 walkthrough. Decision #188. **Deferred live-use:** the phone eyeball of the folded verdict line and the rest/holiday all-set suppression remains deferred — the Vercel preview sits behind account SSO, consistent with prior batches. |
| ~~Batch 116 — Hosted/neural read-aloud voice~~ | 🟢 Mid | Shipped | 116.1 Escalate the read-aloud voice — Batch 111 shipped an on-device `selectBestVoice` (`BriefListenControls.tsx:60`) that's still robotic; add a hosted/neural TTS path for a natural voice. (Issue 2)<br>116.2 **Decide at `/batch-start` (central, re-opens 111's call):** hosted neural TTS sends the health-data brief text to a third-party voice API — weigh against the paid-API-for-privacy stance; if adopted, gate on explicit consent + a private on-device fallback; consider an Anthropic-adjacent or self-hosted option first.<br>116.3 Backend TTS proxy (if hosted) + audio playback + caching; tests + a11y. | A genuinely natural brief voice — accepting a deliberate, consented privacy tradeoff if we go hosted, not the robotic on-device default. | Shipped via PR #137, squash `78c8fad`: kickoff decisions made with Craig at `/batch-start` were (116.2) consent-gated hosted TTS with automatic on-device fallback, not an unconditional swap, and (engine) OpenAI's TTS API over self-hosting (Piper) or ElevenLabs. `Profile.hosted_tts_consent` (migration `016`, default false, toggled via new Settings "Voice" section) gates `POST /api/v1/tts/synthesize` (`routers/tts.py` + `services/openai_tts.py`, mirrors the existing `anthropic_text.py` httpx pattern); consent is re-checked server-side on every call, not just remembered client-side. `BriefListenControls.tsx` tries the hosted voice first when consent is on and falls back to on-device `SpeechSynthesis` on any failure (stale consent, unconfigured key, network); synthesized audio is cached in-process only, never persisted. `daily-loop`'s payload gains `hostedTtsConsent: bool`. **Boundaries:** the on-device default path (#179/#184) is unchanged; no scheduler/verdict/threshold change; Green/Amber/Red, #133, #135, Red-never-VO2 untouched. Backend pytest 618 passed (210 skipped DB-backed), `ruff check`/`mypy src` clean; web vitest 259 passed (1 pre-existing unrelated `WeekAheadPage` flake), `tsc --noEmit` clean, lint clean (pre-existing warnings only), production build clean. **CI:** both branch-push and PR-context waves passed all 7 jobs plus Vercel. **Production on merge SHA `78c8fadd1078a5d775c4a062102a37417c42355e`:** Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. Covers 14th Issue 2. Re-opens Batch 111's deferred hosted-voice option. Surfaced by Craig's 2026-07-14 walkthrough. Decision #189. **Deferred live-use:** the phone eyeball of the hosted-voice audio quality and the Settings toggle remains deferred — the Vercel PR preview sits behind account SSO, consistent with prior batches. **Engine amendment shipped (DECISIONS #190, PR #138, squash `ba7b509`):** Craig asked about the OpenAI cost immediately after this shipped and requested a free alternative; the hosted engine is swapped from OpenAI's TTS API to self-hosted Piper (`services/piper_tts.py`, no ongoing API cost, no third-party data exposure), superseding only the engine choice — the consent-gated, on-device-fallback design is unchanged. This project's CI doesn't build the Dockerfile, so the merge was paused for explicit go-ahead and the real Railway build was watched live: `piper-tts==1.4.2` + `onnxruntime` installed cleanly, both voice-model `curl` downloads succeeded, image built and passed its healthcheck. **Production on merge SHA `ba7b50997fbe23d50c5692bf9c69d80d57e5130f`:** Railway `/api/v1/health` served that SHA; web `/` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. **Still unverified:** actual synthesized audio quality — the build succeeding proves the model downloaded and Piper is installed, not that a live `POST /api/v1/tts/synthesize` call produces good audio; needs a real check with the Settings toggle on. **Voice/timeout hotfix shipped (DECISIONS #193, PR #141, squash `fe337dc`):** the real synthesize call Craig tried returned a 502 after 46.4s — `medium`-quality Piper synthesis exceeded the 45s timeout on Railway's CPU for a full brief. Voice moved to `en_GB-southern_english_female-low` (faster, gender change incidental to picking a name confidently believed to exist), timeout raised 45→60s. Watched live in the real Railway build: the new voice's `curl` download succeeded, confirming the guessed Hugging Face path was correct. **Production on merge SHA `fe337dcc2c2f5041b0978d7096e41cc08e2a4ae7`:** Railway `/api/v1/health` served that SHA; web `/` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401`. **Still open:** whether `low`-tier synthesis now fits in 60s and how the resulting audio actually sounds — needs one more real check with the toggle on. **CSP fix shipped (DECISIONS #195, PR #143, squash `fa20b79`):** the backend was fine (verified valid, non-silent audio via `railway ssh`); the actual silence was `vercel.json`'s CSP missing `media-src`, defaulting to `default-src 'self'` which doesn't cover the `blob:` URLs used for hosted-audio playback. Added `media-src 'self' blob:`. **Voice-quality reversion shipped (DECISIONS #197, bundled into PR #144's squash `9cb43d6` due to a `git checkout` race between two concurrent sessions sharing a working directory — see STATUS.md for the merge-mechanics note):** Craig heard the `low`-quality voice for the first time after the CSP fix and reported it "very robotic". A live `railway ssh` benchmark with realistic full-brief-length text showed `low` (23.27s) wasn't dramatically faster than `medium` (33.83s) — reverted to `en_GB-alan-medium`, timeout raised to 90s. Verified live in production (health SHA `dd1d048` matches `origin/main`, `config.py` confirms `en_GB-alan-medium`). **Still open:** whether the voice now sounds acceptable and completes within 90s for a real brief — needs one more live check with Craig. |
| ~~Batch 117 — Sleep calendar day-stepping~~ | 🟢 Mid | Shipped | 117.1 Day-step the calendar nav — the prev/next chevrons page by month (`addMonths`/`subMonths`, `SleepDateCalendar.tsx:40`); make them step one day, keeping the month grid for jumping. (Issue 9)<br>117.2 Tests: chevrons move ±1 day bounded by maxDate; the month grid still selects any day. | The sleep calendar's back/forward buttons move day-by-day, not month-by-month. | Shipped via PR #139, squash `79dc055`: the header prev/next chevrons on `SleepDateCalendar` now step `selectedDate` by one day via `addDays`/`subDays`, bounded only by `maxDate` (`nextDayDisabled = !isAfter(maxDateObj, selectedDateObj)`) — no lower bound exists elsewhere in the app either, so none was added here. The expanded month grid keeps working for jumping to an arbitrary date; its own month-to-month paging moved to a new dedicated prev/next-month control pair inline with the `MMMM yyyy` label inside the expanded section, so month navigation didn't get lost when the header chevrons were repurposed. **Frontend-only, no migration**; 4 new `SleepDateCalendar.test.tsx` cases (day-step both directions, `maxDate` disabling "Next day", month-grid date selection still working) plus full web vitest 264 passed, `tsc --noEmit` clean, lint clean, production build clean. **CI:** both branch-push and PR-context waves passed all 8 checks (7 jobs + Vercel) each. **Production on merge SHA `79dc055a6a744b55018c6d7cdc2756fd5a77b3a4`:** Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/` and `/sleep` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. Covers 14th Issue 9. Follows through Batches 107/108. Surfaced by Craig's 2026-07-14 walkthrough. Decision #191. **Deferred live-use:** the phone eyeball of the day-step chevrons and the relocated month-nav control remains deferred — the Vercel PR preview sits behind account SSO, consistent with prior batches; verified instead via component tests asserting the exact date-step/disabled-state DOM behavior. |
| ~~Batch 118 — More specific brief feedback~~ | 🟢 Mid | Shipped | 118.1 More granular feedback — `FeedbackControl` (`FeedbackControl.tsx`) is a 3-way rating (Spot on / A bit off / Way off; Agree / Not for me / Already doing) + free-text; add specific options so Mark can say *what* is off without typing. (Issue 13)<br>118.2 **Decide at `/batch-start` (design):** the vocabulary — dimension tags (too cautious / about right / too aggressive; or sleep read / load read / thermal / plan) or a reason picker on a negative tap; keep it one-tap and Mark-legible; extend the shared `feedbackInputSchema`.<br>118.3 Tests + shared schema. | Let Mark give specific, low-effort feedback on a brief — what's off, not just how much — sharpening the correction signal that feeds the next read. | Shipped via PR #140, squash `e208509`: kind-scoped multi-select reason-tag chips (`summary`: Sleep read / Load read / Thermal read / Plan/schedule / Other; `suggestion`: Too cautious / Too aggressive / Bad timing / Not practical / Other) revealed alongside the existing correction box on a negative tap, decided at `/batch-start` over always-visible dimension tags or a cautious/aggressive slider (the slider can't cover the accuracy-rating `summary` axis). `feedback.reason_tags` (migration `017`, JSONB array default `[]`); `FeedbackService` validates tags against the kind; `recent_corrections`/`RecentCorrection.to_packet()` now also feed reason-tags-only rows forward into the morning packet alongside free-text corrections. **No change to the rating axis, correction flow, Green/Amber/Red, #133, #135, or Red-never-VO2.** Backend pytest 623 passed (213 skipped DB-backed locally; CI ran the DB-backed cases against real Postgres), `ruff check`/`mypy src` clean, shared `schemas.test.ts` 19 passed, web vitest 265/266 passed (1 pre-existing `WeekAheadPage` flake, confirmed unrelated), `tsc --noEmit` clean, lint clean, production build clean. **CI:** both branch-push and PR-context waves passed all 8 checks (7 jobs + Vercel) each. **Production on merge SHA `e208509616ad8bf1bb500254801d3268a9744d5d`:** Railway logs confirmed the `016 -> 017` migration ran cleanly on container start; Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. Covers 14th Issue 13. Follows through Batch 64's feedback loop. Surfaced by Craig's 2026-07-14 walkthrough. Decision #192. **Deferred live-use:** the phone eyeball of the reason-tag chips remains deferred — verified instead via component tests asserting the exact toggle/kind-scoping DOM behavior, consistent with prior batches. |
| ~~Batch 119 — Follow-up chat on a brief~~ | 🟢 Mid | Shipped | 119.1 Conversational follow-up — Mark can ask further questions about a generated brief and get grounded answers; today the brief only answers one question left in the check-in notes (the `SYSTEM_PROMPT` rule). Add a chat surface on the brief + a backend endpoint that takes the brief's context packet + the question and responds. (Issue 10)<br>119.2 Context + history — reuse the stored morning `context_packet` as grounding, keep a short per-brief conversation history, and keep answers within the packet (no new claims), mirroring the brief's guardrails.<br>119.3 **Decide at `/batch-start`:** conversation storage (new table vs. transient), turn limits/cost, and whether answers can trigger plan actions or stay advisory.<br>119.4 Tests: a follow-up is answered from the packet; guardrails (Red floor / no fabrication) hold; history threads. | Let Mark actually converse with his coach about a brief — "why", "what if", "should I…" — with grounded answers, not just the one-shot check-in question. | Shipped via PR #142, squash `beb135c`: kickoff decisions settled the three 119.3 forks — a new `brief_messages` table (018/019, RLS) keyed to `analysis_id`, not transient; a 10-user-question-per-brief cap; and chat-triggered proposals gated by a deterministic keyword check on Mark's own question (never the model), calling the existing workout-delivery propose endpoint — Decision #29's propose→approve→push gate untouched. `services/brief_chat.py`/`routers/brief_chat.py` (`GET`/`POST /api/v1/briefs/{analysis_id}/messages`), a backward-compatible `prior_messages` extension to the shared Anthropic boundary, and `BriefFollowUpChat.tsx` on the Morning Brief page. **No change to Green/Amber/Red, #133, #135, or the morning brief's own generation path.** CI caught two test-only bugs (a fixture using a non-enum `workoutType` and a fabricated FK) before merge, fixed without touching production code. **CI:** both branch-push and PR-context waves passed all 8 checks (7 jobs + Vercel) each. **Production on merge SHA `beb135c7008991b3d18079fe34bdd55b5cedb028`:** Railway + Vercel same-origin `/api/v1/health` both served that SHA; web `/` and `/brief` returned `200`; unauthenticated `/api/v1/daily-loop` returned `401` both direct and via Vercel. Covers 14th Issue 10 (largest of the wave). New capability. Surfaced by Craig's 2026-07-14 walkthrough. Decision #194. **Deferred live-use:** the phone eyeball of asking a follow-up and, on a deliverable-ride day, tapping "Propose this adjustment" remains deferred — CI can't exercise a live Anthropic call or a real device thread, consistent with prior batches. |

## Post-roadmap — 2026-07-14 walkthrough follow-up batch plan (wave 3)

A short third 14th wave (2 points). One needed no new batch: the "Pre-cool the bedroom
tonight" still on Home is the brief's un-holiday-gated `_thermal_action` `todayAction` —
already **Batch 113.1**; the holiday window (12–16 Jul) is genuinely active and the Home
`tonight`/`bedroom` *sections* correctly go dormant (`DashboardPage.tsx:696`), so only the
un-gated action leaks. The other is a new calendar enhancement → Batch 120. Decision numbers
assigned at `/batch-start` (next free **#195**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 120 — Sleep calendar: verdict colour-coding~~ | 🟢 Mid | Shipped | 120.1 Colour each calendar day by that day's verdict — `SleepDateCalendar`'s cells are selection-styled only (`SleepDateCalendar.tsx:156-160`); tint each date by its stored morning verdict (Green/Amber/Red) so Mark can scan his history at a glance. (wave-3 Issue 2)<br>120.2 Per-date verdict data — the calendar needs each date's verdict cheaply; add a light per-date verdict map (a small `from`/`to` verdicts read, or fold verdicts into the history/bedroom-overnight read the calendar already does) rather than a full daily-loop per cell. **Decide at `/batch-start`:** new light endpoint vs. extend an existing history read.<br>120.3 Legend + a11y — colour isn't the only cue (a dot/label too); tests. | Let Mark scan his sleep calendar and see at a glance which days were Green / Amber / Red. | Shipped via PR #144, squash `9cb43d6` (Decision #196): a dedicated lightweight `GET /api/v1/sleep/verdicts?from&to` range read powers presentational calendar tinting with `G`/`A`/`R` non-colour cues, a legend, and verdict-aware aria labels. Final CI passed both branch-push and PR-context waves plus the Vercel preview; production verified on Railway + Vercel same-origin health, web `/sleep` `200`, and unauthenticated `GET /api/v1/sleep/verdicts?...` `401` both direct and proxied. **No migration**; selected-day history fetch, overnight chart endpoint, verdict logic, Green/Amber/Red thresholds, #133, #135, and Red-never-VO2 unchanged. The final phone eyeball of the coloured calendar remains deferred to live use. |

## Post-roadmap — 2026-07-15 UX review batch plan

Craig's 2026-07-15 full-app UI/UX walkthrough — all 16 routes, verified on-device
against Mark's real production data on a live holiday rest day (audit artifact +
findings D1/P1–P7). Coverage is strong (14/16 user stories fully met, several
unusually well — verdict-first hero, the `copy.ts` plain-English system, one-tap
check-in); the gaps are in optimality, plus one real defect. Issues:

1. **Holiday self-contradiction (defect)** — on a live holiday morning the Brief
   ("Green — Rest Day (Holiday)", thermal omitted) and Sleep ("Holiday away", chart
   dormant, Batch 113) honour the holiday flag, but Home's Last-night sleep section
   still shows the live overnight room glance and the Climate tab shows the live room
   + active fan controls, and Home's hero reads "you're good to go tonight" (train
   framing, not rest). Home's `SleepSnapshotBody` renders `OvernightGlance` with
   `showOvernightGlance` defaulting true (`SleepSnapshotBody.tsx:26/40`);
   `EnvironmentPage.tsx` has no holiday gate; `personalStatusLine` (`copy.ts:55`)
   ignores rest/holiday.
2. **Metrics-vs-baselines lost its status column** — Mark named `Metric · Last night
   · Baseline · Status ✔/⚠` as the table he loves; `MetricComparisonTable.tsx`
   condensed it to 2 columns with the ✔/⚠ only on the age sub-line. The pattern he
   wants already exists on the same Sleep page in `SleepStageAgeTable` (`Stage · Last
   night · Healthy range · ✓/⚠`) and in the brief markdown's 4-column table.
3. **Home stacks too many prompts** — up to five attention blocks (feel recap,
   `FeedbackControl`, `UnviewedBriefCta`, `TodayActions`, `NextActionStrip`) between
   the verdict and the first section (`DashboardPage.tsx:757–789`); the amber "NEXT ·
   Review last night's sleep" button even duplicates the section right below it, and
   the daily rating ask sits under the verdict every morning.
4. **Coach memory is an internal panel, not Mark's** — the store meant to kill his
   handover doc is titled "Coach state", described as "the internal editor",
   admin-gated (showed "unavailable" for Mark on the live pass), and edited as raw
   JSON / plan-block / 13-week map (`CoachStatePage.tsx`). The thesis feature isn't
   Mark-facing.
5. **Sleep ⇄ Climate overlap** — the same 4-stat bedroom block renders on both Sleep's
   Tonight tab (`SleepPage.tsx`) and Climate (`EnvironmentPage.tsx`); the room chart
   lives on Sleep, the controls on Climate, a summary on both — users must learn where
   each fact lives.
6. **Week is an editor in a daily slot** — `WeekAheadPage` (titled "Plan") leads with a
   full editor (per-day add Cycle/Build ride/Weights/Flexibility, move/skip/remove/
   reset/restructure) in a primary tab for a user whose plan "won't change".
7. **Small type for a 57-year-old** — load-bearing readouts at 10–11px (the baseline
   "your normal" sub-line + "for your age" note in `MetricComparisonTable`, the verdict
   eyebrow in `VerdictHero`, the "More detail" label + mono eyebrows in `DashboardPage`).
8. **Presentation slips** — every analysis prompt self-titles "You are Garmin Coach"
   (`morning_analysis.py:83`, `post_workout_analysis.py:61`, `post_flexibility_analysis.py:48`,
   `brief_chat.py:52`) so the brief renders "Garmin Coach — <date>" inside a CheckMark
   shell; nav "Week" opens a page titled "Plan"; the bedroom fan surfaces its raw
   configured name ("Replacement 2 keeper").

Not batched: whether the check-in-gated verdict (no colour on a cold open until
check-in / the 09:30 backstop) matches Mark's "morning answer, always" expectation
(review P7) — a product question to confirm with Mark directly, not a build.

**Sequencing.** 121 (holiday consistency) is the correctness item — do first; the
holiday flag already reaches the client, so it's applying an existing gate to Home's
last-night glance + Climate + the hero line. 122 (status column) is the highest
"Mark will love it" win and reuses the in-house sleep-stage pattern. 123 (Home
de-stack) and 127 (type sizes) are quick frontend passes. 124 (Mark-facing coach
memory) is the largest — a new presentation layer plus a non-admin read — and 126
(right-size Week) both carry a `/batch-start` design decision. 125 (Sleep/Climate)
and 128 (presentation consistency) are small. Tiers per #19: all 🟢 Mid. Decision
numbers assigned at `/batch-start` (next free **#198**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 121 — Holiday-consistent Home & Climate~~ | 🟢 Mid | Shipped | 121.1 Home last-night room glance — `sections.lastNight.body` renders `<SleepSnapshotBody>` with `showOvernightGlance` defaulting true (`SleepSnapshotBody.tsx:26/40`, `DashboardPage.tsx:632`), so `OvernightGlance` shows the live overnight room card on a holiday; gate it on `holiday.isActive` (dormant note / `showOvernightGlance={false}`), mirroring Sleep's Last-night "Holiday away" card (Batch 113.3).<br>121.2 Climate tab — `EnvironmentPage.tsx` renders live room + fan controls unconditionally; when `holiday.isActive`, show a dormant "Holiday away — resumes {endDate}" state (mirror Batches 109/113) instead of a live room + active fan controls.<br>121.3 Hero framing — `personalStatusLine(verdict, name)` (`copy.ts:55`) says "you're good to go tonight" regardless of rest/holiday; on a `restDay`/holiday day the hero line should read as recovery/holiday (align with the brief's "Green — Rest Day (Holiday)"), reusing the `restDay`/`holiday` signals already on the daily-loop payload.<br>121.4 Tests: on a holiday day Home's last-night section hides the room glance, Climate shows the dormant away card, and the hero reads rest/holiday not "good to go"; off-holiday unchanged. | On a holiday/rest day every surface tells one story — Home, Climate, Sleep and the Brief all reflect "away" — instead of Home + Climate showing live room data the Brief says is unavailable. | Shipped via PR #147, squash `059ac74` (Decision #199): `SleepSnapshotBody.tsx` hides the overnight room glance and shows a dormant "Holiday away" note during a holiday (mirrors Batch 113.3's Sleep card); `EnvironmentPage.tsx`'s Climate tab shows a dormant "Holiday away — resumes {date}" card with a `/holiday` link in place of live room/fan controls (mirrors Batches 109/113); `personalStatusLine` reads "Today's a rest day — recovery is the plan" instead of "You're good to go" on a rest/holiday day, reusing the existing `dayState.isRest`/`holiday.isActive` signals already computed on Home (no new backend field). Final CI passed both branch-push and PR-context waves, all 16 checks, plus the Vercel preview; production verified on Railway + Vercel same-origin health, web `/`, `/environment`, `/sleep` all `200`, unauthenticated `/api/v1/daily-loop` `401` both direct and proxied. **No migration**; Green/Amber/Red, #133, #135, Red-never-VO2 unchanged. The phone eyeball of the dormant Climate/Home cards remains deferred — production has no live holiday window to eyeball against. |
| ~~Batch 122 — Metrics-vs-baselines: explicit ✔/⚠ status~~ | 🟢 Mid | Shipped | 122.1 Restore the status Mark named — `MetricComparisonTable.tsx` shows `Metric · Last night` with baseline range + age folded into sub-lines and the ✔/⚠ only on the age line; add an explicit per-row status cue on the main line so each metric reads ✔ in-range / ⚠ out, matching his `Metric · Last night · Baseline · Status ✔/⚠` ask.<br>122.2 Reuse the in-house pattern — `SleepStageAgeTable` already renders `Stage · Last night · Healthy range · ✓/⚠` on the same Sleep page; align the metrics table's columns/tone treatment with it so the two tables share one language (don't invent a third).<br>122.3 **Decide at `/batch-start`:** explicit 3rd status/baseline column vs. a status glyph on the condensed 2-column layout (keep mobile-legible); confirm the direction against the Copilot table Mark loves.<br>122.4 Tests: each row shows a direction-aware ✔/⚠; in-range vs out-of-band tones hold; renders on Home + Sleep. | Give Mark back the at-a-glance ✔/⚠ status he explicitly loves, reusing the sleep-stage table's existing treatment rather than a third table style. | Shipped via PR #148, squash `6949fdd` (Decision #200): `MetricComparisonTable.tsx` now uses an explicit third `Status` column aligned with `SleepStageAgeTable` rather than a condensed 2-column glyph treatment; the third column carries the baseline range plus the restored direction-aware main status cue (`in range`, `10 below`, etc.), while age comparison stays as a secondary sub-line. Age-only rows such as VO₂max also get an explicit main status cue, and lower-is-better metrics stay green when they are out of band in the good direction. Final CI passed both branch-push and PR-context waves, all 16 checks, plus the Vercel preview; production verified on Railway + Vercel same-origin health, web `/` + `/sleep` `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and proxied. **No migration**; Green/Amber/Red, #133, #135, Red-never-VO2 unchanged. The final phone eyeball of the 3-column table at 375 px remains deferred to live use. |
| ~~Batch 123 — De-stack Home: one loud next step~~ | 🟢 Mid | Shipped | 123.1 Reduce stacked prompts — between `VerdictHero` and the first section Home stacks the feel recap, `FeedbackControl`, `UnviewedBriefCta`, `TodayActions`, and `NextActionStrip` (`DashboardPage.tsx:757–789`); consolidate so exactly one "next thing to do" is prominent — e.g. fold the feel recap into the hero, and ensure the `NextActionStrip` doesn't duplicate the section immediately below it (the amber "Review last night's sleep" button sat right above that section).<br>123.2 Demote the daily rating ask — `FeedbackControl` ("How does this land?") sits directly under the verdict every morning; move it to the end of the read or make it quieter so it doesn't compete with the verdict/next action.<br>123.3 Tests. | One clear primary action under the verdict, not five stacked prompts plus a daily rating ask. | Shipped via PR #149, squash `ac11d31` (Decision #201): the feel recap is folded into `VerdictHero` (no separate `MorningFeelRecap` card), unread briefs suppress `TodayActions` so only one loud prompt leads under the hero, and `NextActionStrip` only renders when it does not duplicate either a visible Today action block or the lead section directly below it. The morning `FeedbackControl` moved into the opened Last-night read in a quieter dashed container beneath `SleepSnapshotBody`, so it stays on Home without competing with the verdict/next-step surface. Final CI passed both branch-push and PR-context waves, all 16 checks, plus the Vercel preview deployment; production verified on Railway + Vercel same-origin health, web `/` `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and proxied on merge SHA `ac11d31`. **No migration**; pre-verdict / pre-check-in states unchanged; Green/Amber/Red, #133, #135, Red-never-VO2 untouched. The final phone eyeball of the thinner Home stack remains deferred to live use. |
| ~~Batch 124 — "What your coach knows about you" (Mark-facing context)~~ | 🟢 Mid | Shipped | 124.1 Plain-English context view — `CoachStatePage` is titled "Coach state", described as "the internal editor", admin-gated (showed "unavailable" for Mark), and edited as raw JSON / plan-block / 13-week map; add a Mark-facing "What your coach knows about you" read (profile facts, data-quality rules, sleep protocol, active hypotheses, plan rhythm) in plain language, with the raw/structured editor behind an admin toggle.<br>124.2 Access — expose at least a read to Mark himself (not admin-only), since it is his context; keep destructive/structured edits admin-gated.<br>124.3 **Decide at `/batch-start`:** how much Mark can edit in his own words vs. read-only; and unify the label (nav "Coach memory" vs page title "Coach state").<br>124.4 Tests. | Make the feature the whole thesis rests on — living, editable context that replaces his handover doc — actually Mark-facing, not an internal admin panel. | Shipped via PR #150, squash `bbd5b13` (Decision #202): a new authenticated `GET /api/v1/coach-memory` read reuses the existing coaching-state snapshot for every signed-in user, `CoachStatePage` becomes a plain-language **Coach memory** read for Mark with profile/data-quality/sleep-protocol/plan-rhythm/hypothesis/framework cards, and the raw JSON / plan-override tools stay behind an explicit admin-only editor toggle on the same route. The nav/page wording is unified on "Coach memory", no new source of truth or migration was added, and the new read is auth-gated in production (`/api/v1/coach-memory` unauthenticated `401` via Vercel). Final CI passed both branch-push and PR-context waves, all 16 checks, plus the Vercel preview deployment; the preview route itself remained behind Vercel account SSO, so visual review was limited to confirming the deploy and SSO gate. Production verified on Railway + Vercel same-origin health and web `/` on merge SHA `bbd5b13`. |
| ~~Batch 125 — Resolve the Sleep ⇄ Climate overlap~~ | 🟢 Mid | Shipped | 125.1 One home per fact — the same 4-stat bedroom block (Indoor now / Thermostat / Overnight low / Wind) renders on both Sleep's Tonight tab (`SleepPage.tsx`) and Climate (`EnvironmentPage.tsx`); the room chart is on Sleep, the controls on Climate, a summary on both. Remove the duplicate so each bedroom fact lives once, with a single clear cross-link.<br>125.2 **Decide at `/batch-start`:** keep the split (Sleep = story, Climate = controls) with crisp handoffs, or fold Climate's live read into Sleep's Tonight; and confirm whether Climate still warrants a primary tab.<br>125.3 Tests. | Users no longer learn "chart on Sleep, controls on Climate, summary on both" — each bedroom fact has one home with a clear handoff. | Shipped via PR #151, squash `5582957` (Decision #203): the split held, with Climate staying the canonical live room + fan-control surface and Sleep keeping the retrospective overnight story + wind-down context. Sleep Tonight no longer repeats the full 4-stat bedroom block; it now shows one concise live summary plus a direct `Open Climate` handoff, while Climate's own copy explicitly owns the live read and Sleep owns the overnight chart. A tiny shared `bedroomLiveSummary(...)` helper now keeps the Sleep/Home summary wording in one place. **Frontend-only, no API/migration**; targeted vitest/lint/build green; production verified on Railway + Vercel same-origin health at the merge SHA, web `/`, `/sleep`, and `/environment` `200`, and unauthenticated `/api/v1/daily-loop` `401` via Vercel. Covers 15th Issue 5 and refines Batches 101/102/104. |
| ~~Batch 126 — Right-size Week for a daily glance~~ | 🟢 Mid | Shipped | 126.1 Glance-first Week — `WeekAheadPage` (titled "Plan") leads with a full editor (per-day add Cycle/Build ride/Weights/Flexibility, move/skip/remove/reset/restructure) in a primary tab for a user whose plan "won't change"; lead with a simple "this week — what's on each day, done / to-do" glance, with the editor one layer down.<br>126.2 **Settled at `/batch-start`:** Mark keeps Week as a primary read-only glance; Craig-as-admin gets the organiser one layer down, so editing affordances are hidden/demoted for Mark on this surface.<br>126.3 Tests. | The primary Week tab reads as "here's your week" first, with the powerful editor behind it. | Shipped via PR #152, squash `d4c1a0b` (Decision #204): `WeekAheadPage` now matches the nav label with a **Week** header and leads with a compact read-first glance, splitting the page into **This week** and **Coming up** with per-day rows plus done/to-do totals instead of exposing the organiser rail first. The full move/add/build/skip/remove/reset/restructure controls remain intact behind an explicit admin-only **Edit week** tab, so Mark sees the weekly read first while Craig still has the organiser one layer down. **Frontend-only, no API/shared-schema/migration**; targeted Week vitest, lint, and build were green; final PR-context and post-merge `main` CI both passed; production verified on Railway + Vercel same-origin health at the merge SHA, web `/` + `/delivery` `200`, and unauthenticated same-origin `GET /api/v1/plan-actions/schedule?days=14` `401`. |
| ~~Batch 127 — Legibility pass for the daily reader~~ | 🟢 Mid | Shipped | 127.1 Raise type minimums — load-bearing readouts sit at 10–11px: the baseline "your normal" sub-line + "for your age" note (`MetricComparisonTable.tsx`), the verdict eyebrow (`VerdictHero.tsx`), the "More detail" label + mono eyebrows (`DashboardPage.tsx`). Raise anything carrying meaning to a comfortable minimum for a 57-year-old while keeping the type scale coherent.<br>127.2 Tests / visual check. | Nothing that carries meaning is set at a size that strains the actual user's eyes. | Shipped via PR #153, squash `940a9d3` (Decision #205): `MetricComparisonTable.tsx` now renders the table/status copy and both explanatory notes at `text-sm`, `VerdictHero.tsx` raises the date label plus recap/verdict eyebrows to `text-xs`, and `DashboardPage.tsx` raises the Home **More detail** / **Next** mono labels to `text-xs`. No API/shared-schema/migration change, no copy or verdict-logic change, and the type scale stays coherent while removing 10–11px from meaning-bearing daily-reader text. Final CI passed both pre-merge waves (`push` `29452942623`, `pull_request` `29452944590`) and post-merge `main` run `29453051885`; production verified on Railway + Vercel same-origin health at merge SHA `940a9d3`, web `/`, `/sleep`, `/delivery` `200`, and unauthenticated `/api/v1/daily-loop` + `/api/v1/coach-memory` `401` via Vercel. The final handheld-distance eyeball of the raised type remains deferred to live use. |
| ~~Batch 128 — Presentation consistency: brief branding, titles, device names~~ | 🟢 Mid | Shipped | 128.1 Rebrand the reads — the morning/post-workout/flexibility/brief-chat prompts self-title "You are Garmin Coach" (`morning_analysis.py:83`, `post_workout_analysis.py:61`, `post_flexibility_analysis.py:48`, `brief_chat.py:52`) and the brief renders "Garmin Coach — <date>" inside a CheckMark shell (also the `garmin_workout_export.py` names). **Decide at `/batch-start`:** does the user-facing coach identify as "CheckMark" (align with the shell) or is "Garmin Coach" intentional — then make it consistent.<br>128.2 Nav/title consistency — nav "Week" opens a page titled "Plan"; unify nav label ↔ page title (fold the "Coach memory"/"Coach state" rename into Batch 124).<br>128.3 Friendly device names — the bedroom fan surfaces its raw configured name ("Replacement 2 keeper") in Mark's UI; map device ids to friendly names (e.g. "Bedroom fan").<br>128.4 Tests. | The product speaks with one name and human labels — no "Garmin Coach" inside a CheckMark app, no "Plan" under a "Week" tab, no "Replacement 2 keeper" fan. | Shipped via PR #154, squash `059ea2f` (Decision #206): the user-facing coach identity is now consistently **CheckMark** across the morning/post-activity/follow-up/review/trend/handover prompt surfaces, the deterministic handover/export titles, workout-export metadata, and the notification test copy, while leaving the API title, internal helper docstrings, and activation CLI description untouched. Repo-truth note: Batch 126 had already shipped the `/delivery` page header as **Week** on `origin/main`, so Batch 128 carried that forward rather than introducing a second title change. Dreo fan labels are normalized at the backend boundary so the overnight auto-target renders as **Bedroom fan** and any additional controllable fan gets a generic fallback such as **Other fan (1234)** instead of leaking raw setup names. **No migration**; no verdict/delivery/scheduler logic change; Green/Amber/Red, #133, #135, Red-never-VO2 untouched; fan selection semantics unchanged. Final CI passed both branch-push and PR-context waves, all 16 checks, plus the Vercel preview; production verified on Railway + Vercel same-origin health at merge SHA `059ea2f`, web `/` `200`, and unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. |

## Post-roadmap — 2026-07-16 walkthrough re-run batch plan

A fresh expert UI/UX walkthrough on synced `origin/main` (after 121-128 shipped),
rendering Home/Brief/Sleep/Climate/Week with realistic data. Two earlier findings
were already fixed by shipped work (Week redesign #152, Sleep⇄Climate overlap #151);
the items below are the gaps that survived. All 🟢 Mid, frontend-only, no migration.
Decision numbers assigned at `/batch-start` (next free **#207**).

- **129** bundles the correctness/polish gaps into one pass: the metrics-table
  direction allow-list is incomplete (a good above-normal reading can render as a
  ⚠ warning), Climate repeats a single fan's status twice and prints
  "Mode unknown · Oscillation unknown", the workout subtitle leaks the raw enum
  ("Bike sweet spot"), tonight's prep actions use a "done"-style green check, and a
  couple of copy strings use developer jargon ("canonical", "rail one layer down").
- **130** carries the flow follow-up: surface a compact metrics-vs-baselines snapshot
  inside the Brief itself (Mark's stated favourite artifact, currently one tap away).
- **131** is design-pending — a longitudinal strength "watching-brief" (should he do
  more/less resistance work over time), his explicit ask that is still only partially
  met; needs a scope decision with Mark before building.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 129 — Post-review polish: metric direction, Climate de-dup, copy & bullets~~ | 🟢 Mid | Shipped | 129.1 Metrics direction — `MetricComparisonTable.tsx`'s `HIGHER_IS_BETTER`/`LOWER_IS_BETTER` sets omit `readiness_score` and `average_respiration`, two of the 8 baseline rows the backend emits, so an above-normal readiness (good) renders as an amber ⚠; add both (readiness higher-is-better, respiration higher-is-worse).<br>129.2 Climate de-dup — `BedroomBody.tsx` prints a single fan's live status on both the "Overnight autopilot" row and the "Bedroom fan" card, and shows literal "Mode unknown · Oscillation unknown"; make the autopilot row describe the toggle and hide the mode/oscillation line when unreported.<br>129.3 Copy — drop "canonical" (`EnvironmentPage.tsx`) and "rail one layer down" (`WeekAheadPage.tsx`), and replace the raw enum subtitle ("Bike sweet spot"/"Bike z2") with a shared `workoutTypeLabel` helper used by Home, Week, and Sleep.<br>129.4 Prep bullets — `SleepPrepBody.tsx` marks to-do prep actions with a green check that reads as "done"; use a neutral bullet.<br>129.5 Tests. | The daily reader never sees a good reading flagged as a warning, duplicated/"unknown" fan status, a raw workout enum, developer jargon, or a to-do dressed as done. | Shipped via PR #155, squash `8a58618` (Decision #207): `MetricComparisonTable.tsx` now maps `readiness_score` (higher-is-better) and `average_respiration` (higher-is-worse), so all 8 emitted baseline rows are direction-aware and a good above-normal reading no longer renders as ⚠; `BedroomBody.tsx` describes the autopilot toggle instead of repeating the fan's live status and hides the mode/oscillation line when unreported; a shared `workoutTypeLabel()` (`lib/workoutCategories.ts`) replaces three enum-prettifier copies (Home/Week/Sleep) so the subtitle reads "Sweet spot"/"Zone 2" not "Bike sweet spot"; "canonical" (`EnvironmentPage.tsx`) and "…rail one layer down" (`WeekAheadPage.tsx`) copy dropped; `SleepPrepBody.tsx` prep actions use a neutral bullet. **Frontend-only, no migration/API/schema/verdict change.** Web vitest **293 passed** (new: readiness/respiration direction cases, `workoutCategories.test.ts`, `BedroomBody.test.tsx`, `SleepPrepBody.test.tsx`), tsc + eslint clean, build clean. CI green both waves; production verified on Railway direct + Vercel same-origin health SHA `8a58618`, web `/`+`/sleep`+`/environment`+`/delivery`+`/brief` `200`, unauthenticated `/api/v1/daily-loop` `401` both paths. |
| ~~Batch 130 — Metrics-vs-baselines snapshot in the Brief~~ | 🟢 Mid | Shipped | 130.1 Surface a compact `Metric · Last night · Status ✔/⚠` snapshot inside `MorningBriefPage` (the table Mark named as his favourite), reusing `MetricComparisonTable` rather than a new style, without duplicating the full Sleep-page treatment.<br>130.2 Tests. | The brief — Mark's primary read — contains the summary table he singled out, not just a link to it. | — |
| ~~Batch 131 — Strength watching-brief~~ | 🟢 Mid | Shipped | 131.1 **Decide with Mark first:** he asked for a standing read on whether to do more/less resistance work over time; today strength gets per-session reads but no longitudinal watching-brief. Scope what signal drives it and where it surfaces before building. | Mark's explicit strength watching-brief ask is met, not just partially. | Shipped via PR #157, squash `3cbd020` (Decision #209): a first-cut `StrengthTrendCard` on `/trends`, mirroring the existing `WalkingBaseCard` pattern, fed by the rolling 4w/12w strength trend Batch 19 already computed but the shared `dailyLoopSchema` never declared (so the frontend silently dropped it). Built without Mark's sign-off on scope, on Craig's explicit call to ship a best-guess version and let his feedback refine it — window length (4w/12w vs longer) and whether load/duration should weight the trend, not just frequency, remain open questions for him. **Frontend + shared-schema only, no backend/migration change** (the engine already existed); targeted vitest/tsc/lint/build green; CI green both waves (16 checks) plus Vercel; production verified on Railway + Vercel same-origin health at merge SHA `3cbd020`, web `/` + `/trends` `200`, unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. |

## Post-roadmap — 2026-07-16 rest-day activity surfacing batch plan

Traced from a live case — Mark's holiday walk on 16 Jul ("South Lakeland Walking",
70 min, on the last day of an active 12–16 Jul holiday window that had skipped his
Sweet Spot bike). The *treatment* is already correct and stays: the walk is captured
as an `Activity`, read as advisory active-recovery (`post_walk`, ≥30 min / 3 km via
`is_deliberate_walk`), never counts as load, never touches the skipped session, and
rolls into the Trends walking-base volume. What's wrong is *placement*:

- **Home (Batch 132)** — the walk lives only inside the Today card (`DayPlanBody` →
  `PendingWorkoutCheckIns` before its read generates, `WalkReadList` after), which on a
  rest day is collapsed under "More detail": the rest-day primary section is `lastNight`
  (`homeSections.ts:39`) and the `nextAction` ladder has a rung for an unlogged *ride*
  (`firstUnloggedRide`→`log-ride`, `homeActions.ts:112`) but **none for a walk** — even
  though the push nudge ("Your walk is synced — check in and I'll read it") already fired.
  **132** adds the missing walk rung so the walk floats to the top *while it's fresh*
  (decided 2026-07-16: dynamic, not a static re-rank of sleep), then recedes once seen.
- **Week/Plan (Batch 133)** — it doesn't appear at all: the plan-schedule
  (`planScheduleDaySchema`, `schemas.ts:404`) and week-ahead (`weekAheadEnvelopeSchema`,
  `schemas.ts:1238`) payloads are planned-workouts-only, so a day he actually walked
  renders as a blank rest day. **133** surfaces the day's synced activities on the day
  (decided 2026-07-16: shown on Mark's read-first Week glance, kind + duration).

Both prominence calls were made by Craig on 2026-07-16 (recorded in the rows below),
open to Mark's later feedback — same posture as Batch 131. Making the walk *more* visible
does not conflict with rest-day integrity: acknowledging an activity is not the same as
prompting training, and the advisory/no-load treatment is untouched. **132** is
frontend-only; **133** adds a read-only field to two existing endpoints (no new ingestion,
no migration). Decision numbers assigned at `/batch-start` (next free **#212**,
after Batch 134 takes **#213**).

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 132 — Surface the walk on a rest-day Home~~ | 🟢 Mid | Shipped | 132.1 Walk rung in the Home action ladder — `nextAction` (`homeActions.ts:90`) resolves a ride's unlogged check-in (`firstUnloggedRide`→`log-ride`, `sectionKey:'today'`) but has no walk equivalent; add a parallel rung (new `NextAction.key`, e.g. `read-walk`) that reads the already-present payload (`pendingPostWorkoutActivities` with `activityKind==='walk'`, and/or a fresh `postWalkAnalyses` entry) so a synced walk becomes the top Home action once the required morning sleep-review step is done, floating and expanding the Today card that holds it — mirroring the ride precedent and the push nudge that already fires — and clears once the walk is read/seen (server `pendingPostWorkoutActivities` empties when the read generates; a per-day 'seen' flag mirrors `hasReviewedSleep`/`hasReviewedBrief` for the generated read). Advisory-toned, not `warning`.<br>132.2 **Decided (2026-07-16): dynamic freshness, not a static re-rank.** Leave `primarySection('rest_day')` = `lastNight` (`homeSections.ts:39`) as the resting rest-day lead — do *not* make the phase primary walk-aware. Instead the 132.1 rung's existing action override (`actionSection(nextAction) ?? primarySection(phase)`, `DashboardPage.tsx:578`) floats the Today card to the top *only while the walk is fresh* (synced + not yet read/seen) and lets it fall back under sleep once seen. Net: walk leads when new → recedes to the Today card once consumed → last-night sleep is the calm resting lead. No phase-primary change — this collapses into 132.1.<br>132.3 Rest-day copy — the empty-plan Today copy ("Rest is the plan today… Just ride it — I'll read it after", `DashboardPage.tsx:1086`) is ride-centric and reads oddly on a day he already walked; acknowledge a logged activity when one exists.<br>132.4 Tests (walk rung fires while a walk is pending/unread and clears once read; rest-day+walk-no-ride leads/floats the Today card; copy branch) + web + shared gates. | On a rest or holiday day, the activity Mark actually did is what Home surfaces — not something buried beneath last night's sleep. | Shipped via PR #159, squash `5413013`: a synced walk on a rest/holiday day now drives the single Home action and floats the Today card holding its pending check-in / advisory read to the top *while fresh*, then recedes once read/seen with `lastNight` unchanged as the resting rest-day primary; the empty rest-day copy no longer implies he did nothing. **Frontend-only, no API/shared-schema/migration**; local verification stayed green (`homeActions.test.ts` 32 passed, `DashboardPage.test.tsx` 53 passed, shared vitest 19 passed, web/shared `tsc --noEmit`, eslint 0 errors, build clean). Both branch-push and PR-context CI waves passed all 16 checks plus the Vercel preview. Production verified on Railway + Vercel same-origin `/api/v1/health` SHA `541301302eb477925585ceb45dd74e20dbd2d7e9`, web `/` `200`, and unauthenticated `/api/v1/daily-loop` `401` via Vercel. The live signed-in rest-day-walk Home eyeball remains deferred to real use. |
| ~~Batch 133 — Week/Plan shows what he actually did~~ | 🟢 Mid | Shipped | 133.1 Add activities to the week payload — the plan-schedule day (`planScheduleDaySchema`, `schemas.ts:404`, Mark's read-first Week glance) and the week-ahead editor shape (`weekAheadEnvelopeSchema`, `schemas.ts:1238`) are planned-workouts-only; add a per-day read-only `activities` array with a light shape (`activityKind` ride/strength/flexibility/walk, name, `durationMin`, `startUtc`) sourced from already-synced `Activity` rows for the date range. No new ingestion, no migration.<br>133.2 Backend — join `Activity` rows into the schedule / week-ahead endpoints (`plan_actions.py` `schedule()` ~`:233` and the week-ahead source), classifying with the same `post_activity_kind` Home uses so kinds agree; read-only, no adherence/verdict effect.<br>133.3 Frontend — render the day's unplanned activity in `WeekAheadPage` (titled "Week") as a distinct lightweight chip (e.g. "＋ Walk · 70 min"), visually separate from planned rows and from a completed *planned* workout (which already shows via `status='completed'`), so a holiday/rest day he walked no longer reads as blank.<br>133.4 **Decided (2026-07-16): the chip shows on the day on Mark's read-first Week glance (Batch 126), not only the admin editor.** It carries the basics — kind + duration (e.g. "Walk · 70 min"); richer per-activity detail is out of scope for this batch.<br>133.5 Tests (schedule/week endpoints include the day's activities; a walk on a skipped/rest day renders a chip; a completed planned ride is not double-counted as both a done row and a chip) + backend + web + shared gates. | The Week view is an honest record — planned work *and* the unplanned activity he actually did — so a walked holiday week isn't a blank rest week. | Shipped via PR #160, squash `32d9f6d`: the plan-schedule and week-ahead payloads now carry the day's synced activities via a documented shared-schema field, and Week's read-first glance renders an unplanned walk / strength / ride as a distinct kind+duration chip on the day without double-counting completed planned sessions. **No new ingestion or migration**; the advisory / no-load and Green/Amber/Red invariants are untouched. Local verification stayed green (backend focused pytest **28 passed / 27 skipped**, shared vitest **19 passed**, web `WeekAheadPage.test.tsx` **15 passed**, backend/shared/web typechecks clean, eslint 0 errors, build clean). Both branch-push and PR-context CI waves passed all 16 checks plus the Vercel preview. Production verified on Railway + Vercel same-origin `/api/v1/health` SHA `32d9f6d5c6fad600b7779996fe16cbbb16ac20c9`, web `/` `200`, and unauthenticated `GET /api/v1/plan-actions/schedule?days=7` `401` both direct and via Vercel. The live signed-in walked-rest-week Week eyeball remains deferred to real use. |

## Post-roadmap — 2026-07-16 climate resumes on the last holiday day

Traced from the same 12–16 Jul holiday as Batches 132–133. The holiday form labels its
two dates "First day of holiday" / "Last day of holiday" (`HolidayPage.tsx:165`/`:175`),
and Mark travels home on the last day and sleeps in his own bed that night — so the night
of `end_date` is a *home* night, not an away night. Today the bedroom/climate subsystem
gates "away" **end-inclusive** (`active_holiday_window_for_date`, `holiday_pause.py:85`),
so on the last day the Climate tab shows "Holiday away — the room read and fan controls
stay dormant … Climate resumes {endDate}" — a line that already contradicts itself
(`EnvironmentPage.tsx:52`/`:58`) — and the overnight fan autopilot stays a dormant no-op
through the whole 21:30→00:00 pre-sleep slice on the very night he's back, only waking
after midnight once the local date rolls to `end_date+1` (`scheduler.py:1049`, window
`21:30→08:30`, `fan_control.py:46`). So he comes home to a hot room and the fan does
nothing for the first ~2.5 h — exactly the window that matters.

Craig confirmed (2026-07-16) scoping this to the **whole evening / "is he home tonight"
subsystem**, not just the fan: fan control, the bedtime thermal alert, the sleep-protocol
nudge, and the Climate/Tonight UI all resume on `end_date`. The fix is a one-night shift on
the *evening* side only — away nights become `[start, end-1]` (end-exclusive) for those
surfaces. The plan side is deliberately left inclusive: the last day stays a holiday **rest**
(the workout is still `skipped`, `_skip_workouts`), and the morning "last-night" reads —
Sleep's history tab (`SleepPage.tsx:217`) and the brief's thermal review
(`morning_analysis.py`, `insideHolidayWindow`) — correctly stay dormant on `end_date`
morning because they describe the away night just gone. Decision **#213** assigned at
`/batch-start`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 134 — Climate/bedroom subsystem resumes on the last holiday day~~ | 🔴 High | Shipped | 134.1 Backend "away tonight" semantics — in `holiday_pause.py` add an **end-exclusive** helper `overnight_away_window_for_date(windows, d)` (with a `holiday_windows_away_overnight` mirror of `holiday_windows_covering_date`, and a service method `get_overnight_away_window_for_date`) returning the active window only for `start_date ≤ d < end_date`: Mark returns home the night of `end_date`, so the bedroom/climate subsystem is away only for the nights `[start, end-1]`. Leave the end-inclusive `active_holiday_window_for_date` / `holiday_windows_covering_date` untouched — the plan-rest and morning "last-night" reads still use them.<br>134.2 Evening/overnight scheduler jobs resume on `end_date` — repoint `run_fan_control` (`scheduler.py:1049`), the thermal half of `run_evening_monitoring_alerts` (`scheduler.py:230`), and `run_evening_sleep_nudge` (`scheduler.py:197`) from `get_active_window_for_date` to the end-exclusive `get_overnight_away_window_for_date`, so on the night of `end_date` the overnight fan autopilot reconciles the live room temp, the bedtime thermal check runs, and the sleep-protocol nudge fires — while the last away night (`end_date-1`) stays dormant. No change to fan thresholds, the wind-down, or the `_record_fan_state` chart tick.<br>134.3 Daily-loop seam + Climate/Tonight surfaces — `daily_loop.py` computes `overnight_away_window` alongside the existing inclusive `active_holiday_window`; `HolidayStateOut` (`routers/daily_loop.py:601`) + shared `dailyLoopHolidayStateSchema` (`schemas.ts:1066`) gain a backend-computed `awayTonight` (= end-exclusive away), mirroring how the holiday seam itself landed (Batch 105/109). Repoint the "tonight"/climate surfaces at `awayTonight`: EnvironmentPage Climate tab (`EnvironmentPage.tsx:41`), Home Tonight + Bedroom cards (`DashboardPage.tsx:696`/`:717`), the protect-sleep suppression (`homeActions.ts:92`), and Sleep's Tonight tab (`SleepPage.tsx:238`). Leave `isActive` (inclusive) driving the surfaces that describe the away night just gone — Sleep's Last-night/history tab (`SleepPage.tsx:217`) and the all-set suppression (`DashboardPage.tsx:758`, already `isRest || isActive`) — and leave the morning brief's inclusive `insideHolidayWindow` thermal-review gate untouched, so nothing keyed to "last night" flips on `end_date` morning.<br>134.4 Tests + DECISIONS #213 — pure-function boundary (`away_overnight` true at `start`, true at `end-1`, false at `end` and after; active-only identity); each scheduler job runs on `end_date` and stays dormant on `end_date-1`; `awayTonight` true `<end` / false on `end` on the payload; Climate tab live on `end_date` while the history tab stays dormant; refresh the now-required `holiday` fixtures the way Batch 109 did. Backend pytest/ruff/mypy + web + shared vitest/tsc/eslint/build + closeout smoke. | When Mark gets home on the last day of a holiday, the bedroom/climate subsystem is live *that night* — overnight fan autopilot, bedtime thermal alert, sleep-protocol nudge, and the Climate/Home-Tonight/Sleep-Tonight surfaces all resume on `end_date` — while the day stays a plan rest and the morning "last-night" reads stay dormant. | Shipped via PR #161, squash `709e72e`: the holiday seam now deliberately splits into inclusive `isActive` for the day / morning / last-night story and end-exclusive `awayTonight` for the evening bedroom-climate subsystem, so the overnight gates and tonight-facing Climate/Home/Sleep surfaces resume on `end_date` while the day itself stays holiday-rest and the morning-after surfaces stay dormant. `holiday.awayTonight` is now a documented shared-schema field distinct from inclusive `isActive`; **no migration**; the propose→approve→push contract, Green/Amber/Red, #133, #135, and Red-never-VO2 are untouched. Local verification stayed green (backend focused pytest **80 passed / 29 skipped**, backend `ruff check` clean, scoped backend `mypy` clean, shared vitest **19 passed**, shared `tsc --noEmit` clean, web focused vitest **102 passed**, web `tsc --noEmit` clean, build clean). The first PR run caught one stale expectation (`awayTonight` on `2026-07-14`), fixed in follow-up commit `c1b1870`, after which both branch-push and PR-context CI waves passed all checks plus the Vercel preview. Production verified on Railway + Vercel same-origin `/api/v1/health` SHA `709e72efbe89a331011d8aae0c52a9e31678a1c0`, web `/`, `/environment`, and `/sleep` `200`, and unauthenticated `GET /api/v1/daily-loop` `401` both direct and via Vercel. The live signed-in holiday-return evening eyeball remains deferred to real use. |

## Post-roadmap — 2026-07-16 tap a workout for detail

Craig noticed the workouts on the Week view aren't clickable. The read-first glance
rows (`GlanceDayRow`, Mark's primary surface) and the admin editor rows (`WorkoutRow`)
are display-only, and there is no workout-detail route anywhere in `App.tsx`, so a tap
has nowhere to go — the plan rows are dead ends. Everything a detail view needs is
already on the plan-schedule payload (`planActionWorkoutSchema`: title, workout type,
planned duration, intensity target, status, source, `structuredWorkout`,
`outdoorDelivery`), so this is **frontend + shared-module only — no backend, no
migration**. The structured bike breakdown and power-profile renderer already exist
inside `StructuredWorkoutSheet` (Batch 88); rather than duplicate them, this batch
extracts the read-only parts into a shared module so the new detail sheet and the
existing editor draw the same ride shape. Decision **#214** assigned at `/batch-start`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 135 — Tap a workout to open a detail sheet~~ | 🟢 Mid | Shipped | 135.1 Shared read model — extract the `structuredWorkout` blob parser, the segment→power-bars expander, and a new read-only per-segment summariser into `apps/web/src/lib/structuredWorkout.ts`, and the power-profile SVG into `apps/web/src/components/PowerProfilePreview.tsx`, both lifted from `StructuredWorkoutSheet.tsx` (which now imports them, keeping only its editor-specific validation/submit). The shared parser is *faithful* — it returns empty segments for an unstructured session rather than the editor's starting template, so a read-only surface can tell "has structure" from "none".<br>135.2 `WorkoutDetailSheet` — a new read-only bottom sheet (`apps/web/src/components/WorkoutDetailSheet.tsx`) on the existing `Sheet` primitive showing title, discipline label + icon, status (Completed / Skipped / To do), planned duration, intensity target, indoor/outdoor + Garmin-delivery state, and coach-planned-vs-you-added source; for a structured bike session it renders the reused power-profile preview + a warm-up / intervals / cool-down breakdown, and other sessions show an honest "No structured breakdown for this session." line. It never edits.<br>135.3 Wire tap-to-open on the Week view (`WeekAheadPage.tsx`) — the read-first glance rows become buttons that open the sheet (Mark's surface), and the admin editor rows get their title/summary block wrapped in a button opening the same sheet, leaving the existing Move / Edit structure / Skip / Remove controls untouched and un-nested. New `detailWorkout` page state; each row is an accessible `<button>` with a per-workout aria-label.<br>135.4 **Decided at `/batch-start`:** (a) scope = planned workouts on **both** the glance and the admin editor; the unplanned-activity chips (Batch 133) stay non-interactive — they carry only kind + duration and have no detail payload. (b) the sheet is **read-only** — editing stays in `StructuredWorkoutSheet` / Move / Skip / Remove; the detail sheet never writes.<br>135.5 Tests (`WorkoutDetailSheet.test.tsx`: structured-bike breakdown + power profile, metadata-only non-bike, completed status; `WeekAheadPage.test.tsx`: glance tap opens the sheet, editor tap opens the sheet without the move picker) + web vitest / tsc / eslint / build gates. | A workout on the Week view is tappable, opening a read-only detail sheet with the full session read — the structured breakdown for a ride, the metadata for the rest — so the plan rows are no longer dead ends. | Tapping any planned workout on the read-first glance opens a bottom detail sheet with title, discipline, status, planned duration, intensity target, source, indoor/outdoor + Garmin-delivery state, and — for a structured bike session — the interval breakdown + power-profile preview reusing the extracted renderer (not a duplicate); the same sheet is reachable from the admin editor rows without breaking their Move / Edit / Skip / Remove controls; the unplanned-activity chips stay non-interactive; frontend + shared-module refactor only, no API/shared-schema/migration change; Green/Amber/Red, #133, #135, Red-never-VO2 untouched; web vitest/tsc/eslint/build green. | Shipped via PR #162, squash `236dacb`: the Week view's display-only workout rows are now tappable into a read-only `WorkoutDetailSheet` on both the read-first glance and the admin editor rows (Move / Edit structure / Skip / Remove untouched), and the Batch 88 structured-ride blob parser + power-profile SVG were extracted into shared `apps/web/src/lib/structuredWorkout.ts` + `apps/web/src/components/PowerProfilePreview.tsx` so the editor and the new sheet draw one ride shape rather than two (the shared parser is faithful — empty segments for an unstructured session — with the editor's warm-up→steady→cool-down template left in the editor). **Frontend + shared-module refactor only, no API/shared-schema/migration**; the unplanned-activity chips (Batch 133) stay non-interactive and the sheet never edits; Green/Amber/Red, #133, #135, Red-never-VO2 untouched. New `WorkoutDetailSheet.test.tsx` (3) + 2 new `WeekAheadPage.test.tsx` cases; refactored `StructuredWorkoutSheet.test.tsx` (6) + full `WeekAheadPage.test.tsx` (15) stayed green; web + shared `tsc --noEmit` clean, eslint 0 errors, production build clean (Node 20). Both branch-push and PR-context CI waves passed all 8 jobs — including the `Unit tests (web + shared)` job — plus the Vercel preview. Production verified on merge SHA `236dacbbbe2ddb886d90e44b616e61cbb3cb7386`: Railway direct + Vercel same-origin `/api/v1/health` both served that SHA, web `/` and `/delivery` `200`, and unauthenticated `GET /api/v1/plan-actions/schedule?days=14` `401` both direct and via Vercel. The signed-in on-device eyeball of tapping a Week workout remains deferred to real use. |

## Post-roadmap — 2026-07-16 tap a logged activity for detail

Immediately after Batch 135 shipped, Craig tried tapping a **walk** on the Week view and
nothing happened. That is by design from Batch 135 — 135.4(a) deliberately scoped the
unplanned-activity chips (Batch 133's `+ Walk · 70 min` badges) as non-interactive — but the
expectation is fair: everything on the Week should be tappable. The Week payload carries only
the light `planActivitySchema` shape (kind, name, `durationMin`, `startUtc`), so Craig chose
(2026-07-16) a **basic-details** sheet over pulling richer activity stats (distance, HR) or the
coach's post-walk read onto the Week, which would need backend payload work. Frontend-only.
Decision **#215** assigned at `/batch-start`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 136 — Tap a logged activity for its details~~ | 🟢 Mid | Shipped | 136.1 New read-only `ActivityDetailSheet` (`apps/web/src/components/ActivityDetailSheet.tsx`) on the `Sheet` primitive showing the logged activity's kind icon + label, name, duration, local start time and day, plus a note that it was done outside the plan; handles the light `planActivitySchema` shape (kind / name / `durationMin` / `startUtc`).<br>136.2 Extract the Batch 135 `WorkoutDetailSheet` label/value row into a shared `apps/web/src/components/DetailRow.tsx` used by both detail sheets, so the two sheets render metadata identically with no duplicate.<br>136.3 Wire the Week view activity chips (`WeekAheadPage.tsx` glance `GlanceDayRow` + admin `ScheduleDayCard`) to open the sheet — each `+ {activityLabel}` badge becomes an accessible `<button>` styled via `badgeVariants`; new `detailActivity` page state.<br>136.4 **Decided at `/batch-start` (Craig, 2026-07-16):** basic details only — the Week payload carries just kind/name/duration/start, so richer activity stats (distance, HR) and the coach's post-walk read stay out of scope (they would need backend payload work). Frontend-only, no API/shared-schema/migration.<br>136.5 Tests (`ActivityDetailSheet.test.tsx`: walk kind/name/duration/rows; null-duration ride omits the Duration row; `WeekAheadPage.test.tsx`: tapping a walk chip opens the sheet) + web vitest / tsc / eslint / build gates. | Every logged activity on the Week view is tappable into a lightweight detail sheet — closing the Batch 135 gap where the activity chips were non-interactive. | Tapping a logged-activity chip (walk / ride / strength / flexibility) on the Week view — read-first glance and admin editor — opens a read-only sheet with the activity's kind, name, duration, and local start time; frontend-only, no API/shared-schema/migration; the Batch 135 planned-workout sheet is unchanged; Green/Amber/Red, #133, #135, Red-never-VO2 untouched; web vitest/tsc/eslint/build green. | Shipped via PR #163, squash `dbc3f72`: the Week view's logged-activity chips (walk / ride / strength / flexibility) are now tappable on both the read-first glance and the admin editor, opening a lightweight read-only `ActivityDetailSheet` (kind, name, duration, local start time/day), closing the Batch 135 gap where a tapped walk did nothing. Basic details only (Craig's call — the Week payload carries just `planActivitySchema`'s light shape; richer stats + the coach's post-walk read stay out of scope). The Batch 135 `WorkoutDetailSheet` label/value row was extracted into a shared `apps/web/src/components/DetailRow.tsx`; the chips are `<button>`s styled via the exported `badgeVariants` (unchanged muted-pill look). **Frontend-only, no API/shared-schema/migration**; the Batch 135 workout sheet untouched; Green/Amber/Red, #133, #135, Red-never-VO2 untouched. New `ActivityDetailSheet.test.tsx` (2) + a new `WeekAheadPage.test.tsx` case; `WorkoutDetailSheet.test.tsx` (3) stayed green through the `DetailRow` extraction; web `tsc --noEmit` clean, eslint 0 errors, production build clean (Node 20). Both branch-push and PR-context CI waves passed all 8 jobs — including the `Unit tests (web + shared)` job — plus the Vercel preview. Production verified on merge SHA `dbc3f721737645f99c49100d7ba6e06e32d7d63b`: Railway direct + Vercel same-origin `/api/v1/health` both served that SHA, web `/` and `/delivery` `200`, and unauthenticated `GET /api/v1/plan-actions/schedule?days=14` `401` both direct and via Vercel. The signed-in on-device eyeball of tapping a walk chip remains deferred to real use. |

## Post-roadmap — 2026-07-16 premium UI/UX polish pass

Craig asked for a final world-class UI/UX review of the whole web app to make sure it is
"optimal and the most premium it can be." The audit (code-grounded across `apps/web/src` —
tokens, app shell, primitives, Dashboard/Trends/Sleep, plus cross-cutting greps) found the
foundation already premium (WCAG-verified token ramps, deliberate motion vocabulary, unified
empty/error/offline states, `Input` already dodging the iOS focus-zoom trap, reduced-motion
respected almost everywhere). What remains is the last 5% — a handful of seams where the
polish is uneven, plus one real light-mode defect and one iOS field defect. Batch 137 captures
all of it as **one frontend-only polish pass**; a few items are genuine judgement calls and are
flagged **decide at `/batch-start`** rather than pre-decided. **Not started — planned only.**
Could be split (137.1–137.2 defects shipped first, 137.3–137.5 polish second) if Craig prefers.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 137 — Premium UI/UX polish pass (design review)~~ | 🟢 Mid | Shipped | 137.1 **Theme-aware charts (light-mode defect).** `TrendsPage.tsx` (`:209`–`:222`) and `BedroomOvernightChart.tsx` (`colors.*` at `:61`,`:71`,`:82`,`:96`,`:109`–`:168`) feed the **dark-only** `colors` object from `theme/tokens.ts` straight into recharts `stroke`/`fill`/`tick`, so in **light mode** the grid + axes render dark-teal (`#2E4C4E`) on white and axis labels use the dark muted grey. No chart file consults `useTheme`. Derive chart colours from `useTheme().resolved` (or read the live CSS vars via `getComputedStyle`) so grid / axis / line follow the active palette; the `<Tooltip contentStyle>` already uses `var(--*)` and is correct — leave it.<br>137.2 **Form-field correctness (iOS defect + drift).** `textarea.tsx:16` is `text-sm` (14 px) on every viewport → iOS Safari zooms the page on focus; match `input.tsx:27`'s `text-base sm:text-sm` (16 px on mobile, no zoom). `PinInput.tsx:95`–`:100` still uses the pre-Batch-52 `bg-surface border-border` treatment; move it onto the shared `controlFieldClassName` (`--control` fill + `--control-border` + focus ring, exported from `input.tsx`) so the login PIN cells sit at the same elevation as every other field.<br>137.3 **Unify the save affordance.** The purpose-built `SaveButton` primitive (`components/ui/save-button.tsx` — three-state, width-stable, animated check draw-in, reduced-motion aware, already unit-tested) has **zero imports**; the real save CTAs each hand-roll a label swap (`DashboardPage.tsx:1665`/`:1704`/`:1727`/`:2251`, `SettingsPage.tsx:100`/`:241`, `HolidayPage.tsx:149`/`:193`, `WeekAheadPage.tsx:984`, `BlockGeneratorPage.tsx:215`/`:368`, `PinResetPage.tsx:136`, `LoginPage.tsx:101`) and only `CheckInPage.tsx:439` shows a spinner — so pending/saved reads differently on every screen. Adopt `SaveButton` at these sites for one save language app-wide. **Decide at `/batch-start`:** convert every call site, or only the primary write flows (leaving one-shot auth submits like Login / PIN-reset as-is)? **Decided (Craig, 2026-07-17): async saves only.** **Build finding:** in this app nearly every save refetches and unmounts/switches its own button on success (Holiday pause→active card & resume→pause form, the Dashboard/Week confirm panels collapse, Check-in transitions to a generating view), so `SaveButton`'s headline Saved✓ can't show there — it would flash then unmount. So `SaveButton` was adopted only where the full idle→saving→saved lifecycle is genuinely visible — **Settings → Change PIN** — and the primitive gained an optional leading-`icon` prop + a `useSaveButtonState(isPending, isSuccess)` react-query adapter; the long-running (`Check-in`, `Generate block`), collapse-on-success (Dashboard/Week confirm actions) and navigate-away (Login, PIN-reset) buttons keep their existing spinner/label affordance, which is the correct fit. Broadening to a uniform *pending* affordance on the rest is offered as a follow-up.<br>137.4 **Loading + navigation + motion polish.** (a) `.animate-shimmer` (`index.css:376`, with a light/dark-tuned `--shimmer-stripe`) is defined but unused; `skeleton.tsx:12` uses a plain `animate-pulse` opacity blink. **Decide:** wire the shimmer sweep into `Skeleton` (more premium, infra already exists) or delete the dead utility. **Decided: wired the shimmer into `Skeleton`.** (b) No route scroll reset — `App.tsx` has no `ScrollRestoration`; add a `ScrollToTop` that resets to top on PUSH/REPLACE and is a no-op on POP/back (complementing `PageTransition`'s directional slide), so navigating into a route no longer lands mid-page. (c) `TabBar.tsx:53`/`:86`'s active-tab underline animates via framer `layoutId`+spring, which bypasses the CSS `prefers-reduced-motion` block (JS-driven layout animation); gate it with `useReducedMotion()` the way `PageTransition` and `SaveButton` already do.<br>137.5 **Toaster + pinch-zoom (decisions).** (a) `App.tsx:97` `<Toaster position="bottom-right" richColors>` floats toasts into the mobile thumb / bottom-tab-bar zone and uses sonner's own green/red instead of `--success`/`--error`; **decide** a bottom-centre, tab-bar-safe placement + token-aligned colours. **Decided: yes** — a new `AppToaster` component (bottom-centre, offset clear of the tab bar, `theme` tracking the in-app toggle, token left-accent colours, `richColors` dropped). (b) `index.html` viewport sets `maximum-scale=1.0, user-scalable=no`, disabling pinch-zoom (WCAG 1.4.4 concern — real for Mark as an older primary user); with inputs at 16 px (and textareas after 137.2) the focus-zoom rationale is gone. **Decide with Craig** whether to drop `user-scalable=no`. **Decided: dropped it** — pinch-zoom re-enabled.<br>137.6 **Tests + gates.** Chart colours swap with `resolved` theme (light vs dark); textarea font-size class; PinInput field classes; `SaveButton` idle→saving→saved at a converted call site; `ScrollToTop` resets on PUSH and no-ops on POP; TabBar indicator honours reduced motion. Web vitest / tsc / eslint / build green. | A final premium-polish pass from a UI/UX design review: charts that follow light/dark, form fields that don't zoom or drift from the control-field system, one unified save affordance, an agreed skeleton treatment, scroll-to-top on navigation, reduced-motion-safe tab motion, and revisited toaster / pinch-zoom choices. | Charts render palette-correct grid/axis/line in **both** themes; textareas are 16 px on mobile (no iOS focus-zoom) and the PIN cells use the shared control-field fill; the save/pending/saved affordance is identical across the converted call sites via `SaveButton`; skeleton loading uses one agreed treatment; navigating to a new route lands at the top while back preserves feel; the tab indicator respects reduced motion; the toaster placement/colour and `user-scalable` decisions are resolved with Craig. **Frontend-only — no API / shared-schema / migration**; verdict logic + advisory/no-load + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; web vitest / tsc / eslint / build green. | Shipped via PR #164, squash `9e71731`: a frontend-only design-review polish pass. **137.1** theme-aware recharts (`lib/chartColors.ts` + `useChartColors()`) so `TrendsPage`/`BedroomOvernightChart` follow light/dark rather than freezing to the dark palette on white (the `<Tooltip>` `var()` styling was already correct and stays). **137.2** `Textarea` 16px on mobile (iOS focus-zoom fix), `PinInput` onto the shared `controlFieldClassName`. **137.3** `SaveButton` adopted at **Settings → Change PIN only** — build finding: nearly every save in this app refetches and unmounts/switches its own button on success (Holiday pause→active card, Dashboard/Week confirm panels collapse, Check-in→generating view), so the Saved✓ can't show (it would flash then unmount); the primitive gained an optional leading-`icon` prop + a `useSaveButtonState` react-query adapter, and the long-running / collapse-on-success / navigate-away buttons keep their existing spinner/label affordance (a uniform *pending* affordance on the rest is an offered follow-up). **137.4** `Skeleton` shimmer sweep, new `ScrollToTop` (resets on PUSH, no-ops on POP), `TabBar` active-underline `layoutId` spring gated on `useReducedMotion()`. **137.5** new `AppToaster` (bottom-centre, tab-bar-safe offset, in-app `theme`, token left-accent colours, `richColors` dropped) + `index.html` drops `maximum-scale/user-scalable=no` to re-enable pinch-zoom. **Frontend-only, no API/shared-schema/migration**; verdict/advisory/Green-Amber-Red/#133/#135/Red-never-VO2 untouched. New tests: `chartColors` (2), `save-button` (3), `useSaveButtonState` (2), `ScrollToTop` (1), `PinInput` (1), `skeleton` (1) + a Textarea-16px case in `controls.test.tsx`; web `tsc --noEmit` clean, eslint 0 errors (6 known warnings), production build clean (Node 20). Both branch-push and PR-context CI waves passed all 7 jobs — including `Unit tests (web + shared)` — plus the Vercel preview; the local full-suite `WeekAheadPage` cold-start `userEvent` 5000ms timeouts (pre-existing #122/#214 flakiness in an untouched file) did not recur in CI. Production on merge SHA `9e717312aa0205346e656673945335210a87f723`: Railway `/api/v1/health` served that SHA; web `/`, `/trends`, `/settings`, `/environment` returned `200` and the served viewport meta confirms the new build (no `user-scalable=no`); unauthenticated `/api/v1/daily-loop` returned `401` both direct and via the Vercel same-origin proxy. The on-device eyeball of the light-mode charts, the shimmer, the repositioned toasts, pinch-zoom, and the Change-PIN Saved✓ remains deferred to real use. |

## Post-roadmap — 2026-07-17 daily-loop freshness / stale-cache fix

Craig reported the app showing **yesterday's** brief on open. Investigation (no backend bug —
`_local_today` in `daily_loop.py` correctly uses the profile's IANA timezone, not raw UTC) traced
it to two caching layers stacking on a cold/slow-network PWA open: `sw.ts`'s `NetworkFirst` route
for `/api/v1/daily-loop` (`:57`–`:67`) falls back to its up-to-24h Workbox cache after only a
**5 s** timeout with no signal to the page that the response is stale; and `queryClient.ts`'s
`PersistQueryClientProvider` (`maxAge` 24 h, `staleTime` 60 s) paints that same stale response
instantly on launch. Neither layer is wrong to exist (they're the deliberate Batch 62.1
offline/fast-cold-open design) — the gap is that a stale hit is invisible: no banner, no forced
refetch, no manual escape hatch. The daily-loop envelope already carries `subjectDate`
(`packages/shared/src/schemas.ts:259`), so staleness is detectable **frontend-only**, no API/
schema change needed. While tracing this, a second, related bug turned up in the same problem
class: `WeekAheadPage.tsx:144` computes its "is this today?" highlight via
`new Date().toISOString().slice(0, 10)` — the UTC calendar date — which is wrong for a UK user
during BST (from ~23:00 UTC the local date has already rolled over while the UTC date hasn't),
so the Week view can highlight the wrong day exactly at the boundary this bug lives in. Bundled
into the same batch since both are "the app shows the wrong day" defects in adjacent code.
Craig also asked to fold in a related but separate request: push out the hard 09:30 morning
backstop (Decision #87/#158/#185) that force-generates the brief regardless of whether Mark has
actually stably woken, so a genuine lie-in doesn't get read on stale data — while keeping the
guarantee that he never ends up with zero brief on a day he doesn't engage, just later. This
bumps the batch tier to 🔴 High (scheduler/verdict-timing edge case) since the frontend items
alone were 🟢 Mid. **Not started — planned only.**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 138 — Daily-loop freshness, stale-cache fix & later morning backstop~~ | 🔴 High | Shipped | 138.1 **Detect and surface staleness.** In `useDailyLoop`/`DashboardPage.tsx`, compare the served envelope's `data.subjectDate` against the actual local-today (derive from `player.timezone` via the same logic the backend uses, not raw browser locale, so client and server agree at the BST/GMT boundary) and expose an `isStale` flag. When stale, show a dismissible-but-persistent-until-fresh banner ("Showing {date}'s data — tap to refresh") rather than silently displaying an old brief with no indication. **Decide at `/batch-start`:** banner placement/copy and whether it's a top banner vs. inline card on the Home screen.<br>138.2 **Make the refresh actually fresh.** The banner's refresh action must bypass the Workbox cache (e.g. a cache-busting fetch or `cache: 'reload'`) and force a real `queryClient.refetchQueries(['daily-loop'])` — tapping it must not just re-serve the same 24h-old SW cache entry it's warning about. Also bump `sw.ts`'s `api-daily-loop` route `networkTimeoutSeconds` from 5s so a cold/slow mobile connection gets a fairer shot at a live response before falling back. **Decide at `/batch-start`:** the new timeout value (e.g. 8–10s) — trade-off between a snappier fallback and giving slow connections a real chance.<br>138.3 **Fix the UTC-vs-local "today" bug on Week.** `WeekAheadPage.tsx:144`'s `todayIso` must use the same local-today derivation as 138.1 (profile timezone, not `new Date().toISOString()`), so the "today" highlight can't land on the wrong day across the BST/GMT boundary.<br>138.4 **Push the morning backstop later (Craig, 2026-07-17: keep the guaranteed-verdict safety net, just move the deadline out).** Today `wake_detection.py`'s `BACKSTOP = time(9, 30)` force-fires `is_morning_ready` regardless of wake stability once the clock passes it (`past_backstop` short-circuits to `"fire"` even on unfinalized/no sleep data), and the separate `morning_backstop` APScheduler cron calls `run_morning_weather_sync` directly at 09:30 Europe/London as a belt-and-suspenders guarantee (Decision #87, refined by #158/#185) — calibrated to Mark's 363-night backfill (median wake 08:22, p90 ≈ 08:50, latest observed 09:24, `WINDOW_END = time(10, 0)`). Craig wants a later cutoff so a genuine lie-in doesn't get force-read on stale/unfinalized data. Move `BACKSTOP` and the `morning_backstop` cron's `hour`/`minute` later, and extend `WINDOW_END` in step so `wake_check`'s polling window still covers the new backstop time (the window-gate gives up if `now_local.time() > WINDOW_END` before ever reaching the backstop check) — the guarantee-a-verdict-by-X behaviour itself is unchanged, only pushed out. **Decide at `/batch-start`:** the new backstop/window-end times (e.g. `BACKSTOP = 10:30`, `WINDOW_END = 11:00`, keeping the ~30 min buffer between them) — Craig's call on how much later. Append a `DECISIONS.md` entry recording the retune (mirrors how #87 documents the original calibration).<br>138.5 **Tests.** Staleness banner shows when `subjectDate` ≠ local-today and hides once a fresh fetch lands; refresh action triggers a real (non-cached) refetch; `WeekAheadPage` today-highlight uses local-today across a UTC/BST boundary case (e.g. 2026-07-17 23:30 UTC = 2026-07-18 00:30 BST); `wake_detection.py` unit tests updated for the new `BACKSTOP`/`WINDOW_END` constants (existing exhaustive `is_morning_ready` matrix in `test_wake_detection.py` re-parametrized, not weakened); scheduler test confirms `morning_backstop` cron fires at the new time. Backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | The app never silently shows a stale day's brief without telling the user, gives them a one-tap real refresh, "today" is computed consistently (profile timezone) everywhere on the frontend, and a genuine sleep-in no longer gets force-read on stale data before he's actually stably awake — while still guaranteeing he never ends up with zero brief on a day he doesn't engage. | A cold/slow-network open that lands on cached daily-loop data shows a visible "showing {date}'s data" banner with a working refresh action that bypasses the service-worker cache; `sw.ts`'s daily-loop network timeout is increased from 5s; `WeekAheadPage`'s today-highlight uses the same local-today logic and no longer relies on `new Date().toISOString()`; `wake_detection.py`'s `BACKSTOP` and `WINDOW_END`, and the `morning_backstop` cron, move to a later, Craig-agreed time while still guaranteeing a verdict that day; a `DECISIONS.md` entry records the retune; **no API/shared-schema/migration beyond the constant/cron-time change**; verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | Shipped via PR #165, squash `f23078b`: the PWA no longer silently opens on yesterday's brief — `DashboardPage` shows a `StaleDataNotice` (sibling to `OfflineNotice`) with a cache-bypassing **Refresh** when the served `subjectDate` isn't local-today, computed by a new `Intl`-based `localTodayIso(profileTz)` in `lib/dailyFlow.ts` (which also fixed `WeekAheadPage`'s UTC-date today-highlight at the BST boundary); `fetchDailyLoop`'s forced refresh passes `cache:'reload'` and the SW `api-daily-loop` timeout went 5s→10s. The morning backstop moved 09:30→11:00 (`wake_detection.WINDOW_END` 10:00→11:30, `morning_backstop` cron 11:00; stability guard/cadence/floors unchanged) so a genuine lie-in isn't force-read on stale data — only lie-in/watch-not-worn days move, still guaranteed a verdict (Decision #217). Decisions settled at `/batch-start` (Craig): 11:00 backstop, top notice + Refresh, 10s timeout. **Frontend + backstop constant/cron-time only; no API/shared-schema/migration**; Green/Amber/Red, #133/#135, Red-never-VO2 untouched; `ARCHITECTURE.md` §sync-jobs/morning + `docs/designs/wake-triggered-morning.md` ticked to the new times. New tests: `dailyFlow.test.ts` (5), `DashboardPage.test.tsx` (+3), `WeekAheadPage.test.tsx` (+1), `test_wake_detection.py` (+1 & retuned), `test_scheduler.py` (backstop cron `hour='11'`); backend pytest 634 passed/227 skipped, ruff/`format`/mypy clean (113 files), web tsc/eslint clean, build clean (Node 20). CI's first wave caught a real regression the noisy local full-run masked — `fetchDailyLoop`'s new `init` param made the normal fetch two-arg, breaking `SleepPage`'s single-arg `apiFetch` assertion; fixed by keeping the normal path single-arg (only the forced refresh passes an init, amend `e3869e4`), after which both CI waves passed all 7 jobs + the Vercel preview. Production verified on merge SHA `f23078b723bb9e20960ef0616c5130eff4c9ad41`: Railway + Vercel same-origin `/api/v1/health` both served it, web `/` `200`, unauthenticated `/api/v1/daily-loop` `401` direct and via Vercel. On-device eyeball of the stale banner/refresh and the 11:00 backstop firing deferred to live use. |

## Post-roadmap — 2026-07-18 check-in edits lost to a warm-resume refetch

Mark reported his **check-in details aren't being saved**. Investigation found **no backend bug** — `DailyLoopService.upsert_manual_entry` (`services/daily_loop.py:232`) and the read-back `_manual_entry` (`:427`) are symmetric on `entry_date` and the write commits correctly; prod data confirms the write path works on days without an interfering refetch. The defect is in `CheckInPage.tsx`: the `useEffect([query.data])` at `:132` mirrors the server's `manualEntry` into local `manualForm` state on **every** change to the loaded data, with no guard for unsaved edits. On Mark's iOS home-screen PWA, `refetchOnWindowFocus` (`lib/queryClient.ts:22`) is enabled **and** deliberately widened to fire on `pageshow`/warm resume (`lib/resumeRefetch.ts`, installed at `App.tsx:77`), so any warm resume mid-check-in (screen unlock, a glance at a notification, an app-switch) refetches `['daily-loop']`; because a fresh envelope differs (new `meta.generatedAtUtc`), React Query's structural sharing yields a **new** `query.data` reference and the seed effect re-runs, resetting the form to the server's still-empty `manualEntry`. He then taps "Get today's brief" and an **empty** entry is saved. **Prod evidence:** his 2026-07-18 morning `manual_entry` was created 09:05:49 with every field null and the brief generated 47 s later (so the PUT fired, empty), versus 11/12 Jul where the row was created then updated ~1–47 min later with his real values (no interfering resume), and 7 Jul's two rows 8 s apart (one empty, one full) showing the same race from the other side. `resumeRefetch.ts:18` documents the exact contract — the widened refetch is "dirty-state-safe" only because editors preserve in-progress edits across it; `CheckInPage` was the one place that didn't. **Frontend-only, no API/shared-schema/migration.** **Not started at write time — planned then built same session.**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 139 — Check-in edits survive a warm-resume refetch~~ | 🔴 High | Shipped | 139.1 **Guard the form-seed against background refetches.** In `CheckInPage.tsx`, add a `dirtyRef` set true on any edit — `setManual` and `toggleChip`, which together cover the subjective-feel buttons, the quick chips, and the feel / notes / BP / supplements / food inputs. The `useEffect([query.data])` that mirrors `data.manualEntry` into `manualForm` returns early when `dirtyRef.current` is set, so a focus / warm-resume refetch can no longer overwrite unsaved edits. A successful save resets `dirtyRef` in `saveMutation.onSuccess`, so the invalidation's refetch re-seeds the form cleanly from the just-stored values. Pristine-form seeding (first load, and an entry that arrives later — e.g. the 11:00 backstop generating while the page is open) is unchanged, since the guard only trips once he has edited.<br>139.2 **Tests.** A mid-edit background refetch of `['daily-loop']` returning a distinct envelope (new `meta.generatedAtUtc`, `manualEntry` still null — the real production structural-sharing condition) preserves the typed subjective score + chip, and the subsequent save PUTs those values, not empties (a regression test verified to fail without 139.1); a pristine form still adopts a server entry that arrives on a later refetch. Web vitest / tsc / eslint green. | His check-in always saves what he actually entered — a warm resume or focus refetch landing mid-check-in can never silently blank the form and persist an empty entry. | Typing into the check-in then a background `['daily-loop']` refetch preserves the entered values; saving after such a refetch sends the typed values in the PUT body, not empties; first-load / backstop seeding of a pristine form still works; **frontend-only, no API/shared-schema/migration**; verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; web vitest / tsc / eslint green. | Shipped via PR #166, squash `a6dc4ee`: `CheckInPage`'s form-seed `useEffect` (`:132`) now early-returns while a `dirtyRef` is set, so a `refetchOnWindowFocus` / `pageshow` warm-resume refetch landing mid-check-in can no longer blank Mark's unsaved edits and save an empty entry; the flag is set by `setManual` / `toggleChip` (covering the feel buttons, quick chips, and every free-text / BP field) and cleared on a successful save so the post-save invalidation re-seeds cleanly from the stored values. Root cause was frontend-only — `DailyLoopService.upsert_manual_entry` and the read-back `_manual_entry` are symmetric on `entry_date` and commit fine; prod showed his 2026-07-18 morning `manual_entry` written 09:05:49 all-null with the brief 47 s later. **Frontend-only, no API/shared-schema/migration**; verdict / Green-Amber-Red / #133 / #135 / Red-never-VO2 untouched; no `ARCHITECTURE.md` change (no roadmap/spec/data-model impact). New `CheckInPage.test.tsx` cases: a mid-edit refetch returning a distinct envelope (new `meta.generatedAtUtc`, `manualEntry` null — the real structural-sharing condition) preserves the typed score + chip and the save PUTs them, not empties (verified to fail without the guard), and a pristine form still adopts a later server entry; full `CheckInPage.test.tsx` 10 passed, web `tsc --noEmit` + eslint clean. Both CI waves (branch-push + PR-context) passed all jobs — Build web, ruff, migration check, security audit, mypy, pytest, **Unit tests (web + shared)** — plus the Vercel preview. Production verified on merge SHA `a6dc4ee34626376037cc0d9b361cd09393479a6e`: Railway + Vercel same-origin `/api/v1/health` both served that SHA, the Vercel production deployment is READY at the merge commit, web `/` and `/check-in` `200`, unauthenticated `/api/v1/daily-loop` `401` both direct and via the Vercel proxy. On-device eyeball of a real iOS warm-resume mid-check-in deferred to Mark's live use (live-browser blocked on this Mac — [[ui-walkthrough-method]]); the discriminating unit test stands in. Follow-ups noted (not bundled): the empty-payload overwrite in `upsert_manual_entry` (an all-empty PUT still replaces a populated row) and morning-entry uniqueness (the 7 Jul duplicate rows). |

## Post-roadmap — 2026-07-18 post-workout re-read shows a stale RPE

Mark reported that after keying his post-ride **RPE as 7 by mistake**, changing it to **4**, and tapping
**"Read my workout" again**, the analysis **still read RPE 7**. Investigation found **no save or
generation bug** — `DailyLoopService.upsert_post_ride_checkin` writes rpe=4 and the endpoint calls
`generate_post_activity_read(..., force=True)`, which regenerates through
`PostWorkoutAnalysisService.generate_and_store`; that reads the latest check-in and produces a fresh
**RPE-4** read. The defect is on the **read path**: `generate_and_store` **never supersedes** the prior
analysis — every re-read (this check-in-first path *and* the hourly poll after a `PROMPT_VERSION` bump)
`session.add`s a *new* `Analysis` row, and there is no unique constraint on
`analyses (user_id, activity_id, analysis_type)`. Every other reader resolves "the current read" via
`latest_analysis_for_activity` (newest `generated_at_utc`), but `DailyLoopService._post_activity_analyses`
returned **every** analysis for the day, so a corrected ride's two rows [RPE 4 newer, RPE 7 older] both
reached the envelope. The frontend then rendered the stale one: `DashboardPage.tsx` built `rideByWorkoutId`
with `.set(plannedWorkoutId, ride)` over that newest-first list, so **last-write-wins left the *oldest*
(RPE 7) row** in the map for a planned ride (an unplanned ride showed both as duplicate cards). **Backend
read-path + a small frontend client-side guard; no API/shared-schema/migration; the `analyses` table and
`generate_and_store` insert semantics are untouched (historical rows are deliberately retained — feedback
links to a specific analysis id, and `PROMPT_VERSION` bumps regenerate through a new row).** **Built this
session — planned then built same session.**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 140 — Post-workout re-read surfaces the latest analysis, not a stale duplicate~~ | 🔴 High | Shipped | 140.1 **Reduce the daily-loop snapshot to the latest analysis per activity (backend).** In `DailyLoopService._post_activity_analyses` (`services/daily_loop.py`), after the existing single collapsed query (ordered `Activity.start_utc desc, Analysis.generated_at_utc desc`), keep only the *first* row seen per `activity_id` within each analysis type — i.e. the newest read — dropping the older duplicate rows a re-read accumulates. This is the same latest-wins rule `latest_analysis_for_activity` already applies, moved to the one reader that lacked it, and fixes both the planned-ride map bug and the unplanned-ride duplicate cards at the payload boundary. Do **not** delete rows or add a unique constraint — historical analyses are retained on purpose (feedback FK, `PROMPT_VERSION` regeneration, reviews/history).<br>140.2 **Harden the frontend against duplicates (defense-in-depth).** In `DashboardPage.tsx`, dedupe `postWorkouts` to the newest read per `activityId` (`latestRides`, keeping the first occurrence of the newest-first list) *before* the planned/unplanned split, so the `rideByWorkoutId` map and `unplannedRides` can never resurface a stale read even if the payload ever carries a duplicate again — replacing the last-write-wins map that silently picked the oldest.<br>140.3 **Tests.** Backend: two post_workout analyses for one activity (RPE 7 then RPE 4) collapse to only the RPE-4 row in `_post_activity_analyses` (verified to fail without 140.1). Frontend: a planned ride whose payload carries a stale RPE-7 + fresh RPE-4 pair renders only the RPE-4 read behind a single "View analysis" (verified to fail without 140.2). Backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | A corrected post-workout read always shows the latest analysis — re-reading a ride after fixing its RPE (or any check-in field) surfaces the fresh read, never a stale duplicate, on both the planned-ride Today row and the standalone unplanned-ride section. | Re-reading a ride after correcting RPE 7→4 shows the RPE-4 read, not the RPE-7 one; the daily-loop payload carries at most one post-activity analysis per activity (the newest); no duplicate read cards for an unplanned ride; **backend read-path + frontend guard only, no API/shared-schema/migration**; the `analyses` table, `generate_and_store` insert semantics, verdict + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | Built on `claude/dads-feedback-mii8u3` (Decision #219). `_post_activity_analyses` now keeps the newest analysis per activity per type; `DashboardPage` dedupes rides to `latestRides` before the planned/unplanned split. Local gates green: backend pytest **862 passed** (Postgres-backed), ruff + `format --check` clean, mypy clean (113 files); web `tsc --noEmit` clean, `DashboardPage.test.tsx` **57 passed**, eslint clean on touched files, production build clean (Node 22). New `test_post_activity_analyses_keep_latest_per_activity` and the new `DashboardPage.test.tsx` case both verified to fail without their respective fix. **Shipped via PR #167, squash `c0fb581`:** both CI waves (branch-push run #836 + PR-context) passed all 7 jobs — Build web, ruff, migration check, security audit, mypy, pytest, **Unit tests (web + shared)** — plus the Vercel preview. Production verified on merge SHA `c0fb5817bad4b70cfa8a366cd3c7beb9f108dfea`: Railway `/api/v1/health` served `{"status":"ok","sha":"c0fb581…"}`, Vercel same-origin `/api/v1/health` served the same SHA, web `/` `200`, unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. Docs closed out on `main` (`docs/phase-batches.md` struck + `Shipped`, `STATUS.md`, `DECISIONS.md` #219). On-device eyeball of a real corrected-RPE re-read deferred to live use; the two discriminating tests stand in. |

## Post-roadmap — 2026-07-21 brief-generation failure resilience & credit-balance alerting

On 2026-07-21 Mark reported the app **"frozen on writing today's brief."** Root cause was **not** a code
bug — the **Anthropic API account ran out of credits**, so every `POST https://api.anthropic.com/v1/messages`
returned **400** `invalid_request_error` ("Your credit balance is too low…"). Brief generation *is* a Claude
call, so `_generate_brief_after_checkin` (`routers/daily_loop.py:160`) caught the `HTTPStatusError` raised by
`generate_anthropic_text` (`services/anthropic_text.py:52`, whose `response.raise_for_status()` also **discards
the error body**), logged `"morning check-in background generation failed"`, and returned — leaving **no
`morning` analysis row** for the day. The frontend has **no failure state**: `CheckInPage.tsx` renders the
`BriefGeneratingCta` "Writing your brief" and polls `['daily-loop']` every `BRIEF_READY_POLL_MS` while
`queuedAtMs` is set, so it waited **hours** for a brief that would never arrive — no error, no timeout, no retry
— and Mark had no signal anything was wrong (he checked in ~09:35 BST, retried ~13:35 BST, still nothing). The
outage was **silent to Craig** too: no alert fired; it surfaced only because Mark reported a frozen app (Craig
then topped up billing and the 2026-07-21 brief was regenerated out of band). Two resilience gaps this batch
closes, both around the *existing* LLM boundary — neither changes generation logic or the verdict: **(1)** a
brief-generation failure must resolve to an honest, retryable state instead of an infinite spinner; **(2)** an
Anthropic billing/credit failure must **alert the admin** so it's caught before a user reports it, with the
failure *reason* captured in the logs (recovering it this time needed a manual out-of-band API call). Tier
🟢 Mid — failure-state UI + a notification hook + a logging fix + tests. The one design fork — **how the client
learns a generation failed** (a client-side timeout/poll-cap vs. a backend-recorded failure signal on the
daily-loop envelope) — is settled at `/batch-start`, and if the backend-signal route is chosen (API/shared-schema,
maybe a persisted marker) it may pull the batch toward 🔴 High. **Decisions settled at `/batch-start` (Craig,
2026-07-21):** 141.1 → **backend failure signal** — a persisted per-`(user, subject_date)` `briefGeneration.status`
(`generating | ready | failed` + reason) on the daily-loop envelope, so the client shows an immediate, accurate,
retryable error (this lifts the batch to **🔴 High** and adds a small Alembic migration); 141.3 → **both** a
structured `error`-level log event *and* an admin push (the push half needs Craig seeded as a prod admin push
recipient; the log half works regardless). Trigger: first billing-classed failure, deduped once/day via the
existing `_already_recorded` pattern. **Building on `feat/batch-141-brief-failure-resilience` (Decision #220).**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 141 — Brief-generation failure surfaces to the user + credit-balance alerting~~ | 🔴 High | Shipped | 141.1 **Surface a failed generation to the user (backend signal + frontend).** The check-in "Writing your brief" flow (`CheckInPage.tsx` + `BriefGeneratingCta.tsx`) must resolve to an honest failure state — a "Couldn't finish your brief — tap to try again" card with a working **Retry** — instead of polling `['daily-loop']` forever while `queuedAtMs` is set. Retry re-triggers generation the same way a check-in does (regenerate today), not just re-poll. **Decide at `/batch-start`:** how the client detects failure — (a) a **client-side timeout / poll-cap** (no `morning` analysis after N polls / T seconds ⇒ show failure; simplest, frontend-only, but can't distinguish genuinely-slow from failed) vs. (b) a **backend-recorded failure signal** the daily-loop envelope exposes (e.g. `briefGeneration.status = generating\|ready\|failed`), which the client reads for an immediate, accurate error + retry (honest, but adds API/shared-schema and likely a persisted/transient marker written by `_generate_brief_after_checkin`'s except branch).<br>141.2 **Capture the Anthropic error body for diagnosis (backend).** `generate_anthropic_text` (`services/anthropic_text.py:52`) calls `response.raise_for_status()`, which drops the response body — the "credit balance too low" reason was invisible in `railway logs` and had to be recovered with a manual API call. On a non-2xx, log the parsed Anthropic `error.type` / `error.message` (**never** the `x-api-key`) before raising, so the reason (credit balance / prompt-too-long / model / rate-limit) is in the logs. Raised exception type + call-site behaviour unchanged.<br>141.3 **Alert the admin on a billing/credit failure (backend).** When a brief or post-workout generation fails with the Anthropic credit/billing error (a 4xx `invalid_request_error` whose message indicates balance) — and/or when generation has failed repeatedly — notify Craig (admin) via the existing path (`NudgeAlertService` / web-push) and/or a structured error-log alert, so the outage is caught before a user reports it. Anthropic exposes no balance on the messages API, so the practical trigger is a **billing-classed failure**, not a proactive balance poll; enabling **auto-reload** in the Anthropic console is a complementary ops action for Craig, **not** a code phase. **Decide at `/batch-start`:** alert channel (admin push vs. log/observability only) and trigger (first billing-classed failure vs. an N-failures threshold) to avoid alert spam.<br>141.4 **Tests.** Frontend: a generation that never yields a `morning` analysis (or emits a `failed` signal, per 141.1's decision) resolves to the failure card, and Retry re-triggers generation — not an endless spinner (verified to fail without 141.1). Backend: a 400 credit-balance response is logged with its `error.message` and no key material, and is classified as billing so it raises an admin alert (per 141.3), while a non-billing 4xx does not alert (or alerts only past the threshold). Backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | A failed brief (or post-workout) generation never leaves the user staring at an endless "Writing your brief" — it shows an honest, retryable error — and an Anthropic billing/credit outage alerts Craig proactively instead of surfacing only when Mark reports a frozen app, with the failure *reason* now visible in the logs. | The check-in flow resolves a failed generation to a visible "couldn't finish — try again" state whose Retry re-triggers generation; the Anthropic `error.type`/`error.message` is logged on any non-2xx with no key leakage; a credit/billing-classed failure raises an admin alert (channel/threshold per `/batch-start`); **generation logic + the verdict + Green/Amber/Red + #133/#135/Red-never-VO2 untouched**; migration only if 141.1's backend-signal route is chosen (prefer none); backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. Surfaced by the 2026-07-21 Anthropic credit outage; Decision #220 (settled at `/batch-start`: backend failure signal + both alert channels). **Shipped via PR #168, squash `f421506`:** new `brief_generation_status` table (migration `020`, RLS-guarded like 015/019) + `BriefGenerationStatusService` drive `briefGeneration.{status,reason}` on the daily-loop envelope (a real analysis on the day outranks a stale status row); `CheckInPage` resolves a `failed` signal to a retryable `ErrorState` (Retry re-runs the check-in generate) and Home shows a `BriefFailedCta`, both derived from the envelope so a failure shows even on a cold reopen; `generate_anthropic_text` logs the Anthropic `error.type`/`error.message` (never the `x-api-key`) and raises a classified `AnthropicApiError`; `notify_admin_generation_failure` emits an `error`-log event + an admin push decoupled from the app `admin` role (dormant until `admin_alert_user_id` is seeded in prod). Shared-schema field is optional+nullable for pre-141 cache compatibility; generation logic + verdict + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; `ARCHITECTURE.md` §5 gains the table. Local gates green: backend pytest **646 passed / 229 skipped**, `ruff`/`mypy` clean (114 files); web vitest **341 passed** (54 files), `tsc`/eslint clean, production build clean (Node 20). Both CI waves (branch-push + PR-context) passed all 16 checks — Build web, ruff, migration check, security audit, mypy, pytest, **Unit tests (web + shared)** — plus the Vercel preview. Production verified on merge SHA `f4215067091ac9a3df49aa7064d8d7faa219952f`: Railway `/api/v1/health` served that SHA (migration `020` applied — `coach.brief_generation_status` confirmed present via a read-only `information_schema` check), Vercel same-origin `/api/v1/health` served the same SHA, web `/` + `/check-in` `200`, unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. Deferred: the on-device failure-card + Retry eyeball (needs a real generation failure) and seeding `admin_alert_user_id` in prod to activate the push channel. |
| ~~Batch 142 — Sleep read states time-in-bed vs time-asleep correctly~~ | 🔴 High | Shipped | 142.1 **Pass a labelled time-in-bed to the model.** `services/morning_analysis.py` `_sleep_packet` currently gives the model `sleepStartLocal`/`sleepEndLocal`, `durationMin`, and `awakeSleepMin`, but **no explicit in-bed total** — so on 2026-07-19 the model mislabelled `durationMin` (6h26 asleep) as "in bed", then subtracted the 62-min awake *again* to invent a "5h5 actual sleep". Add `timeInBedMin` = the bed→wake window (`sleep_end_utc − sleep_start_utc`), falling back to `durationSec + awakeSleepSec + unmeasurableSleepSec` when a timestamp is missing. `durationMin` = Garmin `sleepTimeSeconds`, which is **asleep excluding awake**; surface that meaning explicitly to the model. **Decide at `/batch-start`:** add `timeAsleepMin` alongside `durationMin` (prefer additive, per 141's optional-field cache rule) vs rename.<br>142.2 **Prompt guardrail so the model states, never re-derives, the figures.** In `SYSTEM_PROMPT` add a sleep-arithmetic rule: state time-in-bed from `sleep.timeInBedMin` and time-asleep from the asleep field; the asleep total already excludes `awakeSleepMin`, so never subtract awake from it; in-bed = asleep + awake + brief unmeasurable. Keep the existing "state bed/wake from `sleepStartLocal`/`sleepEndLocal`" rule.<br>142.3 **Tests.** A packet with sleepStart 00:37 / sleepEnd 08:05 / durationSec 6h26 / awake 62 min yields `timeInBedMin` 448 (7h28) and an asleep figure of 386 (6h26); an assembled-packet test asserts both present and correctly valued; a missing-timestamp night falls back to the summed in-bed total. Backend pytest/ruff/mypy green. | The morning brief reports time-in-bed and time-asleep as Garmin actually recorded them — his 7h28 in bed / 6h26 asleep night is never garbled into a fictitious "5h5 actual sleep" again. | `_sleep_packet` carries a labelled `timeInBedMin` and an unambiguous time-asleep figure; the prompt forbids re-subtracting awake from the asleep total; his 2026-07-19 night renders in-bed 7h28 + asleep 6h26; **backend-only, no migration; the shared daily-loop schema, verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched**; backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-23 feedback (2026-07-19 note). **Shipped via PR #169, squash `a18b35b`:** `_sleep_packet` now carries `timeInBedMin` (bed→wake window via new `_time_in_bed_min`, `duration+awake+unmeasurable` fallback) + an explicitly-named `timeAsleepMin` beside the unchanged `durationMin` (**additive, not rename** — `durationMin` has zero Python consumers); `SYSTEM_PROMPT` gains the sleep-arithmetic rule (state each figure, never subtract awake from the asleep total) and `PROMPT_VERSION` bumps `v13 → v14-2026-07-24`. Decision #221. Backend-only, no migration, no shared-schema change; verdict + Green/Amber/Red + #133/#135/Red-never-VO2 untouched (proved: `age_adjusted_sleep_score` reads stage seconds not `duration_sec`, `_morning_verdict` never reads it). Local gates green: backend pytest **649 passed / 229 skipped** (+3 new non-DB tests), `ruff`/`format`/`mypy` clean (114 files). Both CI waves (branch-push + PR-context) passed all 7 jobs — Lint (ruff), Typecheck (mypy), Unit tests (pytest), Unit tests (web + shared), Build web, Security audit, Migration check — plus the Vercel preview. Production verified on merge SHA `a18b35b7e15a1d15ec1fe9c0b6f8d1e46eec70cd`: Railway + Vercel same-origin `/api/v1/health` both served that SHA, web `/` `200`, unauthenticated `/api/v1/daily-loop` `401` both direct and via Vercel. Out-of-scope follow-up flagged: `garmin_sync.py:478` synthesizes `duration_sec` *incl.* awake when `sleepTimeSeconds` is absent (rare, doesn't hit Mark's real nights). |
| ~~Batch 143 — Chat & post-workout Anthropic failures return a clean error, not a 500~~ | 🔴 High | Shipped | Context: the 2026-07-20/21 `Unexpected token 'I', "Internal S"... is not valid JSON` errors on "Ask about the brief" and the post-ride check-in. Both call Anthropic synchronously in-request and let `AnthropicApiError` propagate → FastAPI 500 with a plain-text `Internal Server Error` body → the web client's JSON parse throws. Batch 141 only hardened the morning-brief background task.<br>143.1 **Convert the brief-chat LLM failure into a structured error.** In `routers/brief_chat.py` `ask_brief_followup` / `services/brief_chat.py` `ask`, catch `AnthropicApiError` from `generate()` and return the `{data,meta,errors}` envelope with a user-facing message and the classified reason (billing / rate_limit / overloaded / …), an appropriate 502/503 — never a bare 500 with a non-JSON body. Reuse the 141 classifier; do not persist a half-written turn on failure.<br>143.2 **Same for the post-ride check-in generation.** In `routers/daily_loop.py` `upsert_post_ride_checkin` the `generate_post_activity_read(..., force=True)` call is unguarded — wrap it so an Anthropic failure still commits the check-in (his RPE/feel/notes are saved) and returns the snapshot with a non-fatal `errors[]` note + retry affordance, instead of 500-ing the whole request. **Decide at `/batch-start`:** keep post-ride generation inline-but-guarded vs move it to the 141 background+status pattern (prefer inline-guarded to avoid scope creep).<br>143.3 **Alert on billing-classed day-time failures.** Route a billing-classed `AnthropicApiError` from these paths through the existing `notify_admin_generation_failure` (141), so a mid-day outage is caught the same way as a morning-brief outage.<br>143.4 **Frontend surfaces a friendly message, never the raw parse error.** In `apps/web/src/lib/api.ts` `apiFetch`, the `!resp.ok` catch re-throws the `resp.json()` SyntaxError verbatim when the body isn't JSON — fix so a non-JSON error body falls back to the envelope `detail` or a clean `API error {status}`; the chat + post-ride surfaces show a retryable error.<br>143.5 **Tests.** Backend: a billing-classed Anthropic failure on the chat endpoint returns a JSON envelope with a billing-reason error (not a 500) and raises one admin alert; the post-ride endpoint still persists the check-in and returns 2xx with an `errors[]` note when generation fails. Frontend: `apiFetch` given a 500 with a plain-text body throws `API error 500` (or the detail), never `Unexpected token`. Backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | A mid-day Anthropic hiccup (credit outage, rate-limit) on "Ask about the brief" or the post-ride check-in shows an honest, retryable message and still saves what he entered — never the cryptic `Unexpected token 'I'` JSON error he hit twice. | brief-chat and post-ride check-in return the `{data,meta,errors}` envelope on an Anthropic failure (no bare 500 / non-JSON body); a billing-classed failure raises the 141 admin alert; the post-ride check-in's RPE/feel/notes persist even when the read can't generate; `apiFetch` never surfaces a JSON parse SyntaxError for a non-JSON error body; **verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; no migration**; backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. Surfaced by Mark's 2026-07-23 feedback (2026-07-20/21 notes). **Shipped via PR #170, squash `ab905df`:** both LLM-in-request boundaries now fail cleanly — `ask_brief_followup` catches `AnthropicApiError` → retryable `HTTPException` (503 billing/rate_limit/overloaded, 502 other) via new `anthropic_http_status`/`anthropic_user_message` (`services/anthropic_text.py`), no half-written turn (the model call precedes every DB write); `upsert_post_ride_checkin` generates the read inside a **SAVEPOINT** (`db.begin_nested()` + `commit=False` + `db.commit()` on success) so a failure rolls back only the half-written analysis + its completion flip, never the already-committed check-in, and returns 200 + a non-fatal `errors[]` note (`post_workout_read_failed`) with the ride re-surfaced as a pending read carrying the saved check-in; billing failures on both paths route through 141's `notify_admin_generation_failure`; `apiFetch` falls back to `API error {status}` on a non-JSON body (never the parse `SyntaxError`) and new `lib/postWorkoutRead.ts` drives a retry toast on the post-ride surfaces. Decision #222. No migration, no shared-schema change; verdict + Green/Amber/Red + #133/#135/Red-never-VO2 untouched. **CI caught a fixture artifact on the first branch-push wave** — a full `db.rollback()` reverted the fixture's transaction-scoped `SET search_path` (`relation "analyses" does not exist`); the SAVEPOINT fix cleared it (prod unaffected — search_path is connection-level via `server_settings` in `database.py`). Both CI waves (branch-push + PR #170 context) then passed all 7 jobs — Lint (ruff), Typecheck (mypy), **Unit tests (pytest) 880 passed**, Unit tests (web + shared) 346+19, Build web, Security audit, Migration check — plus the Vercel preview. Production verified on merge SHA `ab905df4bcc718e44692c0d8ea797ac6b0c6286b`: Railway + Vercel same-origin `/api/v1/health` both served that SHA, web `/` + `/check-in` + `/brief` `200`, unauthenticated `/api/v1/daily-loop` and `GET /api/v1/briefs/{id}/messages` `401` both direct and via the Vercel proxy. Deferred live-use: the on-device eyeball of a real day-time Anthropic failure (chat toast + post-ride retry card) needs an actual outage; the two discriminating DB-backed tests stand in. |
| ~~Batch 144 — An orphaned "generating" brief resolves instead of spinning forever~~ | 🔴 High | Shipped | Context: Batch 141 flips a *cleanly-failed* generation to a retryable card, but a background task orphaned by a process restart or a hung Anthropic call leaves the `brief_generation_status` row stuck at `generating` — no reaper, no client max-wait — the 2026-07-21 90-minute-spinner class.<br>144.1 **Server-side staleness resolution.** In `services/brief_generation_status.py` / the daily-loop envelope read, treat a `generating` row older than a threshold (configurable, ~10–15 min) as `failed` with reason `stale`/`timeout`. **Decide at `/batch-start`:** prefer a read-time derivation (no writer, no migration, no scheduler) unless a persisted transition is needed to fire the admin alert.<br>144.2 **Client max-wait backstop.** In `CheckInPage.tsx` (and `BriefGeneratingCta`/Home), cap the "Writing your brief" wait: after the threshold with no `morning` analysis and no `failed` signal, resolve to the retryable failure card, so the client self-corrects even if the envelope is slow to report `failed`.<br>144.3 **Tests.** Backend: a `generating` status row older than the threshold reads as `failed`/`stale` on the envelope (a fresh one still reads `generating`). Frontend: a wait exceeding the cap with no analysis resolves to the retry card, not an endless spinner. Backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | No brief generation can leave him staring at "Writing your brief" indefinitely — a stuck or orphaned generation resolves to an honest, retryable state within minutes, from either the server or the client. | a `generating` status older than the threshold surfaces as a retryable failure on the daily-loop envelope; the check-in flow self-resolves past a client max-wait; the existing clean-failure (141) + ready paths are unchanged; **verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; prefer no migration**; backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. Ops (not a code phase): seed `admin_alert_user_id` in prod to activate the 141 push channel. Surfaced by Mark's 2026-07-23 feedback (2026-07-21 note). **Shipped via PR #171, squash `dbc47b8`:** (144.1, read-time derivation settled at `/batch-start`) `_serialize_brief_generation` reads a `generating` row whose `updated_at` is older than `settings.brief_generation_stale_after_minutes` (new config, default **12 min**) as `failed`/`reason="stale"` on the daily-loop envelope — off the existing `UpdatedAtMixin.updated_at`, no new column/writer/scheduler; a real analysis still outranks any status row; **no admin alert for the stale case** (the 141 push is dormant in prod and read-path alerting has no natural dedup). (144.2) `CheckInPage` arms a single 12-min `window.setTimeout` when the wait begins (cleared the instant it ends), self-resolving to the same retryable `ErrorState` via a new `waitTimedOut` flag; Home is envelope-driven, so 144.1 alone flips its `BriefGeneratingCta` → `BriefFailedCta` on a cold reopen (no separate client timer). No migration, no shared-schema change; verdict + Green/Amber/Red + #133/#135/Red-never-VO2 untouched. Decision #223. Both CI waves passed all 7 jobs + Vercel preview; production verified on merge SHA `dbc47b8c61d3ce496ba5390777ae7c86e9430826` (Railway + Vercel same-origin `/api/v1/health` both served it, web `/` + `/check-in` `200`, unauthenticated `/api/v1/daily-loop` `401` direct and via Vercel). |
| ~~Batch 145 — Interval grading aligns to the ride's actual laps, not the planned clock~~ | 🔴 High | Shipped | Context: the 2026-07-23 post-workout read graded his sweet-spot intervals at 226–232 W instead of the true 250 W. `services/ride_intervals.py` `segment_ride_intervals` slices the executed trace on the **planned** IR's cumulative `durationSec` boundaries and ignores actual lap markers, so when he rode shorter intervals than the plan (2×15 instead of the planned 2×25 — which he could not express in-app, see the deferred interval editor), the planned 25-min "work" window swept up the trailing 170 W recovery seconds and blended the average down. Erg mode virtually guarantees correct power delivery, so the fix is boundary alignment, not power correction.<br>145.1 **Confirm the lap data we hold.** Establish whether the stored activity (`activity_timeseries` / activity summary `raw_payload`) already carries per-lap/step boundaries from Garmin for a controlled/erg workout, using the canonical sample JSON in `~/garmin-spike/out/`. **Decision at `/batch-start` (2026-07-24): capture + validated trace fallback, no migration.** The stored summary has only `lapCount`; Garmin's separate splits endpoint carries `lapDTOs`, so cycling sync captures it inside existing `activities.raw_summary.activitySplits`. Both the canonical controlled ride and Mark's affected 2026-07-23 ride expose only one whole-ride lap, so laps alone cannot fix the reported grade: segmentation prefers credible step laps, then a fail-closed target-power trace alignment against the known structured IR, and uses the planned clock last.<br>145.2 **Segment on actual boundaries when available.** Extend `segment_ride_intervals` (and its caller in `services/post_workout_analysis.py`) to slice the trace on credible executed lap boundaries when present — matching each lap to its planned step by order/role — or on the executed target-power transitions when Garmin supplies only a whole-ride lap; fall back to the current planned-duration slicing when neither executed source is reliable. Keep the existing warmup/work/recovery/cooldown role classification and grade-only-work behaviour; record `actual_laps` / `actual_trace` / `planned_durations` so the read stays honest.<br>145.3 **Tests.** A trace where the executed work interval is shorter than the planned step, with laps marking the true 250 W work block and the 170 W recovery, grades the work interval at ~250 W (not the blended ~226–232 W); the real one-whole-lap Garmin shape resolves through validated trace transitions; a planless/lapless ride still degrades to the whole-ride read; a noisy/non-matching trace retains the planned fallback; role classification and adherence grading are unchanged for a plan ridden as prescribed. Backend pytest/ruff/mypy green. | A structured ride is graded on the power he actually held in each work interval — read from the ride's real executed boundaries — so an erg-mode 2×250 W session is never marked down to 226–232 W because the grader counted the recovery valleys. | interval segmentation prefers credible actual lap/step boundaries, then a tightly validated structured-target trace alignment, and falls back to planned durations when neither is reliable; his 250 W erg intervals grade at ~250 W; the lapless/free-ride path is unchanged; **the analysis prompt's "grade the work intervals, whole-ride average is context only" contract, verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; no migration**; backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-23 feedback; paired with the deferred interval editor. **Shipped via PR #172 / squash `d28743c`:** local gates passed, all branch/PR/main CI waves passed, the Vercel preview was green, and production smoke verified exact-SHA Railway + Vercel health, readiness, web, and auth gating. Decision #224. |
| ~~Batch 146 — Feel score: the real 0–10 input Mark asked for~~ | 🟢 Mid | Shipped | Context: the subjective feel score already exists and feeds the verdict, but Batch 94 replaced the numeric input with a 5-word one-tap scale (Rough/Meh/OK/Good/Great = 2/4/6/8/10). Mark asked for a literal 0–10 input; decision this wave is to give him the number back, keeping the words as labels.<br>146.1 **Numeric 0–10 feel input.** In `CheckInPage.tsx` and the shared `lib/subjectiveFeel.ts`, let him enter/select a full 0–10 value while keeping the word anchors as guidance (words at 2/4/6/8/10). Preserve the stored contract (`subjectiveScore` int 0–10) and the warm-resume dirty-guard (139) so a numeric edit survives a background refetch. **Decide at `/batch-start`:** 0–10 slider/stepper vs 11 chips — pick the one-tap-friendly option that reads cleanly on mobile.<br>146.2 **Keep the read speaking his word, honour the finer number.** `subjective_score_label` already maps any 0–10 to a word; confirm the brief still says "you said you felt OK" (never a raw "6/10") while the finer value flows into the existing verdict gates (`<5` Amber, `≥5` recovery-good) unchanged. No verdict-threshold changes.<br>146.3 **Tests.** Entering an odd value (e.g. 7) persists `subjectiveScore=7`, survives a mid-edit `['daily-loop']` refetch (139 guard), and maps to the right word; verdict gates behave identically at the boundaries (4→Amber, 5→not). Web vitest/tsc/eslint/build + backend pytest green. | Mark can log exactly how he feels on a 0–10 scale as he asked, while the brief keeps speaking in his own words and the verdict keeps using the number. | **Shipped via PR #173 / squash `247bc1c`:** Check-in now uses a compact slider + visible number field for exact 0–10 entry, with Rough/Meh/OK/Good/Great retained as quick anchors at 2/4/6/8/10; shared Zod and FastAPI request models explicitly accept `subjectiveScore` integer 0–10, and serializers preserve `0` rather than treating it as null (including post-ride forms sharing the field). The brief still receives/speaks `subjectiveLabel`, never a raw ratio, and the deterministic verdict boundary is unchanged (`4` Amber, `5` not). No migration, no prompt-version bump, no Green/Amber/Red, #133/#135, or Red-never-VO2 change. Local gates: backend pytest **656 passed / 232 skipped**, Ruff/format/mypy clean; shared **20 passed** + typecheck; web **349 passed** + typecheck/lint/build. Both branch-push and PR-context CI waves passed all 7 jobs plus Vercel preview; production verified on exact SHA `247bc1c` with Railway/Vercel health, web `/` + `/check-in` `200`, unauthenticated daily-loop `401`, and the production Check-in bundle carrying the new 0–10 control strings. Decision #225. |

**Deferred — Interval-level workout editing (Mark's 2026-07-23 feedback, point 5).** Editing today is a global duration%/intensity% dial (`apply_manual_override_to_ir`) that scales every step — warm-up, work, recovery, cool-down — uniformly, so he cannot restructure intervals (2×25 → 2×15) or adjust rests independently. A real per-interval editor is **design-first and Mark-led**; **his design notes landed 2026-07-24 and it is now specced as Batch 147 below.** Batch 145 fixes the most painful downstream symptom (mis-graded intervals) independently.

## Post-roadmap — 2026-07-24 per-interval workout editor (Mark's 2026-07-23 feedback, point 5)

Mark's deferred interval-editor request (above) arrived with **design notes** on 2026-07-24: a per-interval table
(**No. of intervals**; **work** time / %FTP / cadence; **rest** time / %FTP / cadence), warm-up & cool-down **held
constant irrespective of the workout**, an app-suggested **"Change to"** column he can edit, and an **"Approve and
upload to Zwift"** button — with **Z2 collapsing to 1 interval, rest 0:00** since it's a single continuous effort.
His worked example is scaling/converting the VO2 session (`Warm-up 10 min 55→80%` · `3 min 72%` · `2 min 55%` ·
`5× (2 min @ 120% / 2 min @ 60%)` · `Cool-down 10 min 70→45%`) up/down, or into Sweet Spot or Z2, without touching
the warm-up/cool-down. Design pass reconciled the notes to code this session — findings: (1) today's only edit is
the global two-dial `apply_manual_override_to_ir` (`services/executable_coaching.py:224`), which scales **every**
step uniformly (warm-up/cool-down included) and can't change rep count, so none of his asks are expressible;
(2) the compact source `structured_workout` interval step (`{label, pattern:"5x 2min/2min", target, repeats,
cadenceRpm}`, `services/workout_delivery.py:848`) maps almost 1:1 to his 7-row table, but the string `pattern` +
single `cadenceRpm` **can't carry independent work/rest cadence or a first-class rest %FTP**, and recovery steps are
built with **cadence dropped** (`_expand_pattern`, `services/workout_delivery.py:884`) — so his rest cadence (75→70)
has no path today; (3) `phase` is label-derived (`_phase`, `:911`) so warm-up/cool-down are already identifiable to
hold constant, but the VO2 example's two "primer" steady steps (`3 min 72%`, `2 min 55%`) are phase `interval` and
have **no slot in his table**; (4) cadence already reaches the `.ZWO` (Zwift) file (`build_zwo_xml`, `:311`) though
not the Garmin export (`garmin_workout_export.py:88`, power-only); (5) the **`.ZWO`/intervals.icu/Garmin deliver
rails + propose→approve→push already exist** — the frontend lands rides via intervals.icu (`intervalsEventId`), so
"upload to Zwift" **is** the existing deliver path (ZWO carries cadence, reaches Zwift through intervals.icu).
**Design decision (closest to his notes): edit the source block, not the expanded IR** — add an *additive* structured
interval-block source form carrying first-class work/rest `{time, powerPct, cadenceRpm}`, re-expand → re-derive the
ZWO/intervals/Garmin proposal, and **pass warm-up, cool-down, and any non-block primer steps through untouched**.
`structured_workout` is JSONB so the new form is **additive — no migration**; the edit payload adds shared-schema/API
fields. Tier 🔴 High (new source form + expander branch + source⇄table mapper + suggestion generator + editor UI +
deliver wiring + the Z2 edge case, all around the existing LLM-free delivery boundary). **Not started — planned only.**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 147 — Per-interval workout editor: edit reps / work / rest, warm-up & cool-down fixed, approve & upload~~ | 🔴 High | Shipped | 147.1 **Additive structured interval-block source schema.** Extend `_expand_step`/`_expand_pattern` in `services/workout_delivery.py` with a new block form — `{"label", "block": {"repeat", "work": {time, "powerPct", "cadenceRpm"}, "rest": {time, "powerPct", "cadenceRpm"}}}` — expanding to work+rest IR steps with **independent** cadence on both (closing the `cadence=None` recovery drop at `:884` so his rest cadence 75→70 finally has a path). Keep the legacy `pattern`/`repeats` + single `cadenceRpm` form expanding **unchanged** (existing plans/imports). A `rest` of `0:00` (or omitted) emits **no** recovery step and `repeat == 1` emits a single work step — the Z2 collapse. Mirror the new form in the shared Zod schema (additive/optional for pre-147 cache compatibility, per 141's rule). **Decide at `/batch-start`:** field names/units (minutes+seconds vs total seconds; `powerPct` absolute-%FTP vs a band).<br>147.2 **Source⇄table round-trip mapper (pure).** A DB-free mapper reads a planned ride's `structured_workout` and surfaces its editable interval block(s) as Mark's table rows (No. of intervals; work time/%FTP/cadence; rest time/%FTP/cadence), while listing warm-up, cool-down, and any non-block "primer" steady steps as **read-only pass-through** context. Applying an edit writes the block back into a fresh `structured_workout` and a **new `PlannedWorkout` version** (existing `uq_planned_workouts_user_date_version` versioning), leaving the warm-up/cool-down/primer step JSON **byte-identical**. **Decide at `/batch-start`:** whether v1 edits every block or only the primary block (Mark's current sessions are single-block).<br>147.3 **Suggested "Change to" pre-fill.** When the app proposes a change to a scheduled ride, pre-fill the editable column with a suggested alternative Mark can then edit: (a) **scale** the existing block, and (b) **convert to a named type — Sweet Spot or Z2** — from **deterministic, FTP-seeded templates** (not the LLM, for predictable/safe power), with Z2 setting `repeat=1`, rest `0:00`. Reuses the block schema from 147.1; no verdict/threshold logic touched. **Decide at `/batch-start`:** template defaults (e.g. Sweet Spot ~88–94% FTP work / Z2 ~65% FTP) and whether v1 ships scale + both named templates or a subset.<br>147.4 **"Approve & upload to Zwift" wiring + safety.** Route the edited block through the **existing** propose→approve→push proposal flow (edited block → new IR → new ZWO/intervals/Garmin proposal → approve → deliver); button copy in Mark's language ("Approve & upload to Zwift"), mechanism unchanged (lands via intervals.icu; ZWO carries cadence). Preserve **Red-never-VO2** at push (`blocks_red_vo2`, `services/executable_coaching.py:121`) so a hand-edited ≥VO2 block on a Red day is still blocked, and validate inputs to sane bounds. **Decide at `/batch-start`:** input control (steppers vs numeric fields) + exact validation ranges (reps, work/rest duration, work/rest %FTP, cadence rpm).<br>147.5 **Editor UI.** Replace the two-% dial with the per-interval table — Current vs "Change to" columns, warm-up/cool-down shown fixed/greyed, one-tap type presets (keep VO2 / →Sweet Spot / →Z2), and the Approve & upload button; mobile-first and one-tap-friendly, honouring the 139 warm-resume dirty-guard if it shares Check-in state. **Decide at `/batch-start`:** host surface (`DashboardPage` today-card vs the Week tab).<br>147.6 **Tests.** The new block form expands work+rest with independent cadence incl. rest cadence (previously dropped); a 1-rep/0-rest block yields a single work step and still passes `validate_deliverable_bike_workout` (warm-up+cool-down+ramp intact); the mapper round-trips Mark's VO2 session leaving warm-up/cool-down/primer steps byte-identical; a scaled block and a →Z2 conversion produce the expected IR; an edited ≥VO2 block is still blocked on a Red day; a legacy `pattern` plan expands unchanged (regression). Backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. | Mark can restructure a scheduled ride interval-by-interval — rep count, and work & rest time / %FTP / cadence — exactly as his table asks, with **warm-up and cool-down always left alone**; edit an app-suggested alternative (scale / →Sweet Spot / →Z2); and **Approve & upload** it, with Z2 collapsing to a single continuous effort (1 interval, rest 0:00). | A per-interval editor exposes reps + work/rest {time, %FTP, cadence} for a ride's interval block(s); warm-up, cool-down and non-block primer steps are preserved untouched; **rest cadence now reaches the ZWO**; the app pre-fills a suggested "Change to" (scale / →Sweet Spot / →Z2) Mark can edit; Approve & upload runs the existing propose→approve→push deliver path (surfaced as Zwift, lands via intervals.icu); Z2 → 1 interval / rest 0:00; the **legacy `pattern` form still expands unchanged**; **Red-never-VO2 + verdict + Green/Amber/Red + #133/#135 untouched; additive JSONB schema, no migration** (shared-schema/API additions only); backend pytest/ruff/mypy + web vitest/tsc/eslint/build green. Surfaced by Mark's 2026-07-23 feedback (point 5); design notes reconciled to code this session (paired with the shipped Batch 145 grader fix). **Shipped via PR #174, squash `cb666de`: both CI waves and the Vercel preview passed; Railway + Vercel same-origin health served the exact merge SHA, web returned 200, and both new routes returned 401 direct and proxied.** |

## Post-roadmap — 2026-07-24 morning brief doesn't "join up" plan vs. swap vs. executed (Mark feedback)

Mark reported (2026-07-24) that the app **"doesn't seem to be joining things up."** He swapped Tuesday's hard VO2 to
Saturday **on the app's own recommendation** (it judged him not ready for VO2 on Tuesday), yet today's morning brief in
more than one place narrates the week as if he **completed VO2 on Tuesday** — e.g. *"…meaning Tuesday's VO2 and
yesterday's sweet-spot block have accumulated but haven't pushed you into a ramp-risk zone."* His diagnosis: to be
trustworthy the coach has to remember **what it recommended, what got changed, and what he actually did**, and ground the
read in the last of those. Design pass reconciled this to code — **root cause:** the morning brief is a single LLM call
whose entire world is the JSON packet (`build_morning_user_prompt`, `services/morning_analysis.py:713`, dumps the packet
verbatim as the prompt), and that packet carries **no factual day-by-day record of the training week**. Its only
history surfaces are today-only planned workouts (`_planned_workouts`, `:556`, `workout_date == subject_date`),
yesterday's aggregate load (`_yesterday_load`, `:598`), weekly-mix **bucket counts** (`services/weekly_mix.py` — not
attached to any weekday), Garmin's own acute:chronic numbers, and — critically — the **static nominal weekly template**
`knowledgeBase.trainingSchedule` (fed at `:381`; content at `services/coaching_state.py:177`, literally
`"Tuesday": "VO2 or higher-intensity bike focus"`). With no factual week to read, the model reconstructs "what's
accumulated" from that nominal routine + the aggregate counts and pins the sessions onto their **template** days — so it
credits "Tuesday's VO2" even though *this* week the VO2 was moved to Saturday and Tuesday was easy. The system prompt
reinforces the template as live truth ("Respect the trainingSchedule rest days", `:122`) and nowhere tells the model it
is a *nominal routine, not this week's record*. **All three of Mark's signals already exist in the DB, just never
assembled for the brief:** (1) **recommended** — `verdict.swapSuggestion` at generation time; (2) **changed** — `swap_day`
(`services/executable_coaching.py:1005`) versions the rows (old `is_active=False`, new re-slotted at the swapped dates)
**and** writes a `workout_moved` audit `Analysis` row carrying a human summary ("Swapped 2026-07-21 and 2026-07-25.") +
the planned-workout linkage (`_record_action_audit`, `:1235`) — one of a full set of change audits
(`workout_moved`/`_skipped`/`_removed`/`_replaced`, `:68`–`76`); (3) **executed** — `Activity` rows
(`models/coaching.py:100`) with real `training_load`, and `PlannedWorkout.status` completed/skipped (`:353`). **Design
decision: assemble a factual "training week so far" — planned → changed → executed, per day — from those existing rows,
feed *that* to the brief, and demote the nominal `trainingSchedule` in the prompt to "the usual routine, not this week's
record."** All reads of existing tables — **backend-only, no migration**; the packet gains a section and the prompt a
rule + a `PROMPT_VERSION` bump. Distinct from the deferred "rearrange the whole week in one tap" restructure UI and the
Batch 147 interval editor — this is about **memory/grounding**, not editing; it complements the executed-vs-planned
thread (Batch 145 grading on executed boundaries, Batch 80 deviation verdict). Tier 🔴 High (new cross-table history
assembler feeding the LLM read + prompt/`PROMPT_VERSION` change). **Shipped via PR #175 / squash `8e249c5`; the as-built policy is recorded in DECISIONS #227.**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 148 — Ground the morning brief in the real training week (recommended → changed → executed), not the nominal routine~~ | 🔴 High | Shipped | 148.1 **Assemble the factual "training week so far" (backend, read-only).** A new read-only builder (e.g. a `TrainingWeekService` / helper feeding `morning_analysis.py`) that, over a recent window, produces a **per-day** record joining: the day's **planned** session(s) (type / title / status from the active `PlannedWorkout` versions); **what changed** (moved-in / moved-out / skipped / removed, from the `workout_moved`/`_skipped`/`_removed`/`_replaced` audit `Analysis` rows, which carry a summary + planned-workout linkage); and **what was actually executed** (matched `Activity` with `training_load` + key power/TE, or an explicit "skipped" / "rest day"). So a swapped-away VO2 reads *planned VO2 → moved to Sat* and its Tuesday shows the easy ride he actually did — never a phantom completed VO2. **Decide at `/batch-start`:** (a) window — rolling **last 7 days** vs **calendar-week-to-date** (Mon→today, matching `weekly_mix`'s `week_start`); (b) reconstruct "changed" from the **audit rows** (explicit, carry a summary, can distinguish app-recommended vs Mark-initiated) vs from `PlannedWorkout` **version history** (inactive rows) — prefer audits; (c) whether v1 attributes "**you took the app's suggestion**" (the *recommended* leg of Mark's ask) or ships planned→changed→executed factually first and defers attribution.<br>148.2 **Feed it to the brief and demote the nominal routine (packet + prompt).** Add the assembled record to the morning packet (e.g. `trainingWeekSoFar`) beside the existing `plannedWorkouts`/`yesterdayLoad`; in `SYSTEM_PROMPT` add a grounding rule — narrate "what you've done / what's accumulated this week" **strictly from `trainingWeekSoFar`**, never from `knowledgeBase.trainingSchedule` (which describes the **usual** routine — which day *typically* holds which session — not what happened); never credit a **moved-away** session as executed, and where it helps, acknowledge a session as *moved* (Mark's swap). Bump `PROMPT_VERSION` so stale reads regenerate. **Decide at `/batch-start`:** whether v1 also applies the same demotion/grounding to the **weekly-review** narrative (`services/reviews.py` feeds the identical `trainingSchedule` at `:1023`, so the same nominal-template-as-truth confabulation can surface there) or defers the review surface to a follow-up.<br>148.3 **Tests.** A week where Tuesday's planned VO2 was swapped to Saturday (a `workout_moved` audit + versioned rows) and Tuesday's real `Activity` is an easy Z2 ride renders in `trainingWeekSoFar` as Tue = *planned VO2 → moved to Sat → executed easy Z2 (load X)* and Sat = the VO2, with **no** "Tuesday VO2 completed" anywhere (verified to fail without 148.1); a skipped day shows skipped, not the template's session; a plain unchanged week reads the sessions on the days actually executed; the packet carries `trainingWeekSoFar` and the prompt no longer instructs the model to read history off `trainingSchedule`. Backend pytest/ruff/mypy green. | The morning brief's load / "what you've done this week" narrative is grounded in what Mark **actually did** (and what got **changed** from what was **recommended**) — so a session he swapped away is never narrated as completed, and the coach visibly "joins up" recommended → changed → executed instead of reciting the nominal weekly routine. | The packet carries a factual per-day `trainingWeekSoFar` joining planned → changed (moved/skipped/removed audits) → executed (`Activity` load / status) over the window; the system prompt narrates accumulated load from that record and treats `knowledgeBase.trainingSchedule` as the nominal routine, not this week's history; a swapped-away VO2 never appears as a completed Tuesday VO2 in the read; `PROMPT_VERSION` is bumped; **verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; backend-only, all reads of existing tables, no migration / no shared-schema change**; backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-24 feedback; complements Batch 145 (grading on executed boundaries) and Batch 80 (deviation verdict); distinct from the deferred whole-week restructure UI and the Batch 147 interval editor. **Shipped via PR #175, squash `8e249c5`: both CI waves and the Vercel preview passed; post-merge `main` CI passed; Railway + Vercel same-origin health served the exact merge SHA; web returned 200; and the morning-loop plus weekly-review routes returned 401 direct and proxied.** |

## Post-roadmap — 2026-07-24 Week starts on Monday (Mark feedback)

Mark wants the Week tab to read as a stable calendar week beginning on **Monday**, rather
than as seven days beginning whenever he opens it. The current page does not actually show
a calendar week: `WeekAheadPage.fetchSchedule()` requests
`GET /api/v1/plan-actions/schedule?days=14` without `start_date`, the route defaults the
range to profile-local today, and the frontend then labels `schedule.slice(0, 7)` as
**This week** and `slice(7)` as **Coming up**. On Friday 24 July that makes "This week"
Friday 24→Thursday 30 July and "Coming up" Friday 31 July→Thursday 6 August — stable
seven-day chunks, but not weeks.

The backend already exposes the needed seam: the schedule route accepts an explicit
`start_date`, `PlanActionService.schedule()` is range-generic, and the shared response
already reports `startDate`; no endpoint, schema, database or plan-engine change is
needed. The frontend also already has both correct inputs: `localTodayIso(player.timezone)`
for the profile-local calendar day (Batch 138) and Monday arithmetic in
`weekStartForDate()` for whole-week restructure. **Settled with Craig (2026-07-24):**
Monday is the fixed anchor. Keep the existing 14-day payload as **two complete calendar
weeks** — current Monday→Sunday under **This week**, then next Monday→Sunday under
**Coming up** — rather than preserving 14 future days. This brings the visible Week tab
into line with `weekly_mix`, weekly reviews and Batch 148's factual training history, all
of which already define a training week from Monday.

One existing interaction makes a naive query-only change unsafe. The admin-only
**Edit week** organiser and `MoveWorkoutSheet` consume the same fetched dates; a
Monday-anchored window includes elapsed days from earlier in the current week, and today
the sheet disables only the workout's current date. Those historical dates must remain
visible as the honest record Mark asked for, but must not become new write targets:
past-day add/build/move/edit/skip/remove controls are unavailable, and past dates cannot
be selected in the move sheet. The current week's banner-level Reset/Restore/Rearrange
controls stay available even though its header now sits on Monday — those actions are
week-scoped, not attempts to rewrite the elapsed Monday. Today and future dates retain
the existing day controls. This deliberately refines Decision #103's *rolling* 14-day
window while preserving its core promise — a tap-to-move picker can still push a workout
anywhere in the whole next calendar week. Decision number assigned at `/batch-start`
(implemented as **#229** because local docs had already claimed #228). **Shipped
via PR #177 / squash `546070f`; production verified.**

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 149 — Anchor the Week view to Monday~~ | 🟢 Mid | Shipped | 149.1 **Profile-local Monday anchor.** Derive the current week's Monday from `localTodayIso(player.timezone)` using date-only/UTC-safe calendar arithmetic (reuse/extract `weekStartForDate`; never raw `new Date().toISOString()` or browser-locale weekday math), include it in the query key, and fetch `/api/v1/plan-actions/schedule?start_date={monday}&days=14`. Keep the API's generic today-default unchanged for other callers.<br>149.2 **Two real calendar weeks.** Keep the existing first-seven/second-seven presentation, but its inputs are now guaranteed to be current Mon→Sun (**This week**) and next Mon→Sun (**Coming up**). Today retains its existing profile-timezone badge; planned-session/done/to-do totals cover the whole Monday-start week, completed planned work from earlier days counts as done, and Batch 133's logged-activity chips remain visible under their existing no-double-count rule (unplanned activities do not silently become planned-session totals). Example regression: on Fri 2026-07-24 the payload starts Mon 2026-07-20, This week ends Sun 2026-07-26, and Coming up is Mon 2026-07-27→Sun 2026-08-02.<br>149.3 **Past-day write guard in the admin organiser.** Elapsed days earlier than profile-local today remain visible and tappable for the existing read-only workout/activity detail sheets, but expose no day/workout add/build/move/edit/skip/remove mutation. The move sheet either omits or disables target dates `< today` with an explicit unavailable state; today, future days, and the source workout's current-day disabling keep their existing behavior. Preserve current-week Reset/Restore/Rearrange on its Monday header because those actions target the containing week, not the elapsed header date; preserve Decision #103's one tap-to-move sheet and next-calendar-week reach; do not widen the API beyond its existing 14-day cap.<br>149.4 **Tests + decision record.** Update `WeekAheadPage.test.tsx` request mocks for the explicit `start_date`; add Friday and Sunday cases proving Monday anchoring/two Mon→Sun groups, a BST/UTC-boundary case proving the anchor follows the profile-local date, planned totals/completed history plus existing activity chips from elapsed current-week days, and an admin case proving past days are readable but their day/workout mutations and move targets are unavailable while today/future controls and current-week banner actions still work. Append DECISIONS #229 at implementation to record the fixed-Monday refinement of #103. Web vitest/tsc/eslint/build green. | Make Week mean a predictable calendar week: every visit begins on Monday, shows the whole current week in context, and previews the whole next week instead of shifting both seven-day groups forward every day. | Week requests a profile-local Monday `start_date` and renders **This week = Mon→Sun** plus **Coming up = next Mon→Sun**; today is still highlighted correctly at BST/GMT boundaries; elapsed days remain in the record, completed planned sessions count toward done, and existing logged activities remain visible, but elapsed dates cannot be mutated or selected as move targets; today/future day controls, current-week banner actions and the one-tap move sheet remain intact through the end of next week; **frontend-only, no API/shared-schema/migration/plan-engine change**; verdict logic + Green/Amber/Red + #133/#135/Red-never-VO2 untouched; web vitest/tsc/eslint/build green. Surfaced by Mark's 2026-07-24 feedback and aligned with Batch 148's Monday-start factual training week. **Shipped via PR #177, squash `546070f`: branch-push, PR-context, and post-merge main CI all passed all 7 jobs; Vercel preview passed; Railway + Vercel same-origin health served the exact merge SHA; web `/` returned 200; and the explicit Monday schedule route returned 401 unauthenticated via Vercel.** |

## Post-roadmap — 2026-07-24 — Conversational learning batch plan

Craig's 2026-07-24 requirement: the coach should learn **durably** from its
conversations with Mark — distilling standing facts, preferences, terminology,
and recurring themes out of the follow-up chat, check-in notes, post-workout
notes, and corrections, and folding them into the persistent, editable knowledge
base so context compounds instead of each exchange being forgotten. This
completes the product thesis ("hold that knowledge as living, editable state so
he never writes a handover doc again") and goes beyond the two adjacent loops
that deliberately stop short of it: the brief follow-up chat (Batch 119) is
grounded only in one brief's `context_packet` and makes no durable claim, and the
rate-&-correct loop (`recent_corrections` / `RecentCorrection.to_packet()`) feeds
a correction forward into the next generation's packet only, never into standing
state.

**Sequencing (Craig's call).** Option B first, then learning. Today the only
threaded chat is on the **morning** brief (`BriefFollowUpChat` mounted on
`MorningBriefPage`); after a workout there is no chat, only a one-shot check-in
note plus a single answered question. **Batch 150** gives the post-workout read
its own follow-up chat so a second conversational surface exists; **Batch 151**
then learns from **any** chat — so the learning covers both surfaces from day one
rather than shipping morning-only and revisiting. Batch 150 extends an existing
rail (the chat endpoint is already keyed to any `analysis_id`); Batch 151 is the
substantive analysis-engine + knowledge-base + coaching-integrity work.

**Hard guardrail carried by both.** Learning must never become a lever on the
verdict — the 2026-07-10 coaching-integrity audit named sycophancy/gaming the top
acute risk, and a naive "learn from what he says" loop is exactly that failure
mode. Green/Amber/Red (#133/#135), Red-never-VO2, and the data-quality rules stay
data-grounded and unchanged; learned items are durable facts/preferences only,
surfaced as editable KB entries Mark/Craig confirm (extract-and-confirm),
consistent with the Mark-facing Coach-memory read (#202) — never silently
applied. Decision numbers assigned at each `/batch-start`. This plan decomposes
the requirement recorded as DECISIONS #228.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 150 — Post-workout follow-up chat~~ | 🟢 Mid | Shipped | 150.1 **Reuse the analysis-agnostic chat endpoint.** `brief_messages` + `GET/POST /api/v1/briefs/{analysis_id}/messages` (`routers/brief_chat.py`) are already keyed to any `analysis_id` (user-scoped 404/403), and a post-workout `analyses` row already stores the `context_packet` + `output_markdown` that `BriefChatService.ask` grounds on — so **no new table/endpoint/migration**. Verify the post-workout packet is sufficient to answer from and that history/turn-cap/`_packet_json` paths are packet-shape-agnostic.<br>150.2 **Generalize the chat prompt across read types.** `brief_chat.SYSTEM_PROMPT` is morning-worded ("a brief you already wrote"); make it speak to "a read you already wrote" (or a per-`analysis_type` variant) while keeping every floor identical — never VO2 on Red, no L/R balance, local times, never narrate a skipped/holiday session as live.<br>150.3 **Scope the adjustment-proposal affordance to the morning only.** `_wants_adjustment` + `_todays_adjustable_workout_id` read the morning packet's `plannedWorkouts`/`restDay` to offer "propose today's ride" — meaningless once the session is done; gate that affordance to morning analyses so a post-workout chat is advisory-only (it already returns None on a non-morning packet — make that explicit + tested). **Decide at `/batch-start`:** advisory-only (recommended) vs. letting a post-workout chat propose *tomorrow's* adjustment.<br>150.4 **Mount the chat on the post-workout surface + tests.** Add `BriefFollowUpChat analysisId={analysis.id}` to the post-workout read disclosure on `DashboardPage.tsx` (the `postWorkoutAnalyses` row, ~:1852); the component already takes only `analysisId`. **Decide at `/batch-start`:** Home disclosure only vs. also any dedicated read view. Tests: a post-workout read threads a follow-up answered from its own packet; guardrails hold; the proposal affordance is absent on a post-workout chat; turn cap + 403/404 ownership hold; the chat renders on the post-workout surface. | Let Mark converse with his coach about a finished session — "why", "what does that mean for tomorrow", "was that too hard" — the same threaded follow-up the morning brief already has, instead of a one-shot check-in note. | Shipped via PR #179, squash `fc4203e` (Decision #230): the existing analysis-keyed `brief_messages` table and `/api/v1/briefs/{analysis_id}/messages` endpoint now support post-workout reads without a new table, endpoint, migration, or shared schema change. The chat prompt and UI copy speak about a generic "read", the prompt includes `Read type`, and the deterministic adjustment-proposal affordance is explicitly gated to `analysis_type == "morning"`, so post-workout chat is advisory-only while the morning brief's existing proposal path remains intact. Home mounts `BriefFollowUpChat` inside the completed-ride read disclosure; v1 adds no dedicated read page. Shared floors hold identically (Red-never-VO2, no L/R balance, local times, no skipped/holiday-as-live); Green/Amber/Red, #133/#135, Red-never-VO2, and the morning brief's own chat are untouched. Local gates: backend pytest 665 passed / 236 skipped, backend Ruff/format/mypy clean, targeted web 61 passed, isolated WeekAhead 22 passed, web tsc/eslint/build clean; full web vitest hit the known WeekAhead full-suite timeout flake (350 passed / 2 timed out). Branch-push, PR-context, and post-merge main CI all passed all 7 jobs; Vercel preview passed; Railway + Vercel same-origin health served the exact merge SHA; web `/` + `/brief` returned 200; unauthenticated chat messages returned 401 direct and via Vercel. |
| ~~Batch 151 — Learn durably from any chat~~ | 🔴 High | Shipped | 151.1 **Distil durable knowledge from conversations.** A deterministic-triggered pass reads recent `brief_messages` across all analysis types (morning + post-workout after Batch 150), plus check-in/post-workout note text and corrections, and calls the shared Anthropic boundary (`anthropic_text.py`) to extract **candidate durable items** — facts / preferences / terminology / recurring themes — each with its evidence (source analysis + date) and a proposed KB destination. Strictly-typed output; the extractor is prompt- **and** code-constrained so it can never emit anything about verdict thresholds, the data-quality rules, or Red-never-VO2.<br>151.2 **Durable-vs-transient taxonomy.** Keep standing facts/preferences/patterns (equipment names, foods that disagree with him, schedule constraints, stated goals, chronic niggles); never persist transient state (today's mood/fatigue/one-off soreness). Encode the taxonomy as an explicit, tested filter — including the sycophancy trap: a conversation full of "I feel great / just tell me I'm fine" yields **no** verdict-affecting learning.<br>151.3 **Extract-and-confirm storage + review.** Candidates land as **pending, editable proposals** Mark/Craig accept/edit/reject — reusing the Mark-facing Coach-memory read (#202) and the existing KB editing rail; accepted items become real `knowledge_base` entries (or structured profile fields) that feed the morning/post-workout packets like the rest of the KB. Nothing auto-applies. **Decide at `/batch-start`:** storage destination (new KB sections vs profile fields vs the experiment/hypothesis tracker for recurring themes) and whether a pending-proposals store needs a migration.<br>151.4 **Trigger, privacy, tests.** **Decide at `/batch-start`:** a batched periodic distillation pass (after each brief, or a daily/weekly job) vs per-turn. Same paid-API / no-third-party stance; the pass runs on our own infra; RLS on any new store. Tests: extraction shape, taxonomy filtering, confirm-before-apply (no silent write), the verdict-guardrail invariants, and that accepted items flow into the next packet. | Turn Mark's conversations into the coach's living memory — the app distils durable facts/preferences/themes from any chat and folds them, on confirmation, into the persistent knowledge base, so it stops forgetting what he tells it. | Shipped via PR #180, squash `071992e` (Decision #231): an explicit Coach-memory action batches the last 30 days of user-authored chat across every analysis type plus check-in/post-workout notes and corrections. Strict typed extraction must cite exact source evidence; code independently rejects unverifiable quotes, transient state, duplicates, sycophantic/verdict pressure, Red/VO2 rules, thresholds, and known data-quality rules. Migration `021` adds user-scoped, RLS-backed `conversation_learning_proposals`; Mark/Craig can edit then accept or reject, and only accepted items version `knowledge_base.section='learned_context'`. Morning and ride/strength/flexibility/walk packets receive confirmed items as `learnedContext` with `classificationImpact='none'`, so Green/Amber/Red, #133/#135, Red-never-VO2, thresholds, data-quality rules, and objective metrics remain unreachable from the learning write path. The first branch CI run exposed a mypy 2.3 SQLAlchemy inference issue, fixed in `6173e14`; subsequent branch-push, PR-context, and post-merge main CI passed all 7 jobs, including Postgres lifecycle/packet tests and Alembic upgrade/downgrade, and the Vercel preview passed. Production Railway + Vercel health served exact merge SHA `071992e`; web `/` + `/coach-state` returned 200 and the new learning route returned 401 unauthenticated both direct and proxied. |

## Post-roadmap — 2026-07-25 — Completed-workout read on the Week view + ERG-always profile (Mark feedback)

Mark's 2026-07-25 feedback, two points folded into one batch. **(1)** Tapping a
completed session on the Week view opens `WorkoutDetailSheet` (Batch 135), which
only ever renders the *originally planned* structure — he wants to look back and
see what he actually did and how he performed. The post-workout read already
persists (one `analyses` row per completed session, keyed to `planned_workout_id`
+ `activity_id`, holding `output_markdown` + `verdict`) and Home surfaces it the
day of, but the Week rail carries no link to it (`PlanWorkoutOut` has no analysis
reference), so it is unreachable later. **(2)** Mark rides **every** Zwift session
in ERG and asked the coach to know this so it never second-guesses his power. He
weighed the one wrinkle — ERG blunts the very short 30/15-style surges, the single
such session he does per week — and chose ERG-on always, because toggling ERG off
mid-session is impractical and can cause trainer/technical issues on his setup, so
the coach must never suggest switching ERG off. Recorded authoritatively in the
`profile` KB so it reaches every packet via `athleteProfile`, rather than left to
the conversation-learning path (which needs a manual distil + accept and would be
dormant here anyway). Calibration decided with Craig 2026-07-25: honest-note, not
a nag.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 152 — Historic post-workout read on the Week view + ERG-always profile fact~~ | 🟢 Mid | Shipped | 152.1 **Read lookup endpoint.** New read-only `GET /api/v1/plan-actions/workouts/{planned_workout_id}/analysis` returning the latest `Analysis` for that `planned_workout_id` regardless of post-* type, shaped `{analysisId, verdict, read (output_markdown), generatedAtUtc}` — reuse the `AnalysisOut` serialisers in `routers/daily_loop.py` (~:716) with user-scoped 404. Lazy fetch on sheet-open, chosen over inlining markdown into the whole-week `/schedule` payload. No migration.<br>152.2 **Render the read in the sheet.** In `apps/web/src/components/WorkoutDetailSheet.tsx`, when `status === 'completed'`, fetch and show the read (verdict pill + `Markdown`) *alongside* the existing planned `Session structure` — Mark wants both "what I did" and "how I performed" — reusing Home's read presentation. **Decide at `/batch-start`:** read-only vs also mounting the `FeedbackControl` + `BriefFollowUpChat` Home already attaches to a completed read.<br>152.3 **Honest states.** Completed-but-no-read-yet, still-generating, and generation-failed each render an explicit one-line state — never a blank sheet or an endless spinner. Completed strength/flexibility/walk sessions with a read show it via the same lookup; a completed session with no read is stated plainly.<br>152.4 **Durable ERG profile fact.** Version the `profile` KB section (`knowledge_base.section='profile'`) to add: rides all Zwift/indoor sessions in ERG, so prescribed %FTP is delivered as-is; runs ~one short-sharp-sprint session per week in ERG too and accepts the slight softening; never suggest switching ERG off (impractical + trainer/technical issues on his setup). Content update + version bump, no migration; reaches every morning/post-session packet via `athleteProfile`.<br>152.5 **Prompt guardrail + honest-note calibration.** The read must (a) treat ERG-held interval power as delivered = target and never frame it as under-performance (extends the whole-ride-average guard at `post_workout_analysis.py` ~:78), and (b) never recommend ERG-off. Decided with Craig 2026-07-25: on the one weekly short-sharp-sprint session (an `erg_mode="off"` protocol he runs in ERG anyway) the read adds a **single honest line** that ERG smoothed the surges so the neuromuscular hit was lighter, with **no** suggestion to change anything (honest-note, not a nag). Bump `PROMPT_VERSION`. | Let Mark look back at any completed session from the Week view and see how he actually performed — the persisted post-workout read, not just the plan — and make the coach permanently aware he trains in ERG so it never mis-reads his power or tells him to toggle ERG off. | Tapping a completed Week workout shows its **persisted** post-workout read (verdict + narrative) beside the planned structure and survives reopen (retrieved, never regenerated); absent / still-generating / failed reads show honest states, not blanks or infinite spinners; Home's completed-ride read is unchanged. The ERG fact is active in the `profile` KB and appears as `athleteProfile` in the morning and all post-session packets; a post-workout read on an ERG session never implies power under-performance from the ERG-held target and never suggests switching ERG off, while the weekly surge session gets the single honest ERG-softening line with no change-suggestion. `PROMPT_VERSION` bumped; the new endpoint + ERG behaviour covered by tests. **New read-only endpoint + KB content update only — no migration, no shared-schema change beyond the read DTO.** Verdict logic + Green/Amber/Red + #133/#135 + Red-never-VO2 untouched. Backend pytest/ruff/mypy green; web vitest/tsc/eslint/build green for touched surfaces. Surfaced by Mark's 2026-07-25 feedback. **Shipped via PR #181, squash `35a90d7` (Decision #232):** all 7 CI jobs green on branch-push + PR-context; Vercel preview passed; prod verified on merge SHA `35a90d739e0e9b9095beb9c72eb4c39a470014c7` (Railway `/api/v1/health` returned the SHA, web `/` 200, new `plan-actions/workouts/{id}/analysis` route 401 unauthenticated direct + via Vercel). ERG closeout op wrote Mark's live `profile` KB to v2 (`batch_152_erg_setup`) adding `indoorTrainerMode`, preserving FTP 280 / age 57. |

## Post-roadmap — 2026-07-25 — Full-app review (four-phase audit) batch plan

Craig asked (2026-07-25) for another review of the app **including the recent
changes** (Batches 147–152: interval editor, training-week grounding, Monday
Week, post-workout chat, conversational learning, completed-read + ERG). Scoped
into **four diagnose-only review batches** so the work is tracked and actioned
like everything else in the ledger.

**Model — diagnose, then remediate.** Each review batch is **diagnose-only**: it
produces a committed report doc and ranks its findings, and **no product code
change lands inside a review batch** (the single exception is the trivial ops act
of seeding `admin_alert_user_id`, called out in 154). Every material finding
becomes its own scoped remediation batch afterward — exactly how the 2026-07-10
`COACHING_INTEGRITY_AUDIT.md` fed the (still-open) F1/F2 remediation. A review
never edits verdict logic, Green/Amber/Red, #133/#135, or Red-never-VO2.

**Scope decision (Craig, 2026-07-25 — "for the full app?").** Full-app is the
point for **154/155/156** because the property each judges is *weakest-link or
emergent*: security is only as strong as the least-guarded table/route, and
coaching integrity + UX consistency arise from the whole system, not any one
batch. **153 is deliberately scoped** to the recent batches **plus** the
high-risk engine hotspots — a literal every-file read of ~70k LOC / 247 source
files of shipped, CI-green code is low-yield, so the deep code read targets where
bugs actually hide.

**Ordering.** Recommended **153 → 154 → 155 → 156** (facts/bugs first, then
security, then behaviour, then experience — each leans on the prior), but the
batches are independent and any can be pulled forward (e.g. lead with the
coaching-integrity audit) without renumbering. `/batch-start` assigns the
DECISIONS number when each is actually started.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 153 — Code review: recent batches (147–152) + engine hotspots (scoped, diagnose-only)~~ | 🔴 High | Shipped | 153.1 **Deep-read the six recent batch diffs on `main`** and re-verify each batch's own "boundaries untouched" claim actually holds in code: 147 interval editor (`services/workout_delivery.py` block grammar + shared Zod), 148 training-week grounding (`services/morning_analysis.py`, `services/reviews.py`), 149 Monday Week (`WeekAheadPage.tsx`, `lib/dailyFlow.ts`), 150 post-workout chat (`routers/brief_chat.py`), 151 conversational learning (`conversation_learning_proposals`, `learned_context` write path, migration `021`), 152 completed-read endpoint + ERG prompt (`routers/plan_actions.py`, `services/post_workout_analysis.py`). For each, confirm verdict / Green-Amber-Red / #133 / #135 / Red-never-VO2 are genuinely unreached (raise a finding if not).<br>153.2 **Engine-hotspot sweep (not every file).** The coaching brain + packet assembly (`morning_analysis.py`, `post_workout_analysis.py`, the **19** `PROMPT_VERSION`-carrying files), the `plan-actions` / `workout_delivery` delivery path, and any auth/RLS-touching router. Hunt correctness bugs, silent `except`/swallowed errors, duplicate-row growth (cf. Batch 140's still-missing `analyses` unique constraint), N+1 reads, and dead code.<br>153.3 **Test-gap analysis.** Which recent-batch behaviours rest only on DB-backed tests that skip locally / run in CI; where a discriminating test is missing for a claimed invariant. **Decide at `/batch-start`:** whether to fold `pnpm audit` / Python dependency-audit into 153 or leave it to 154; report location (`docs/reviews/` vs a top-level `CODE_REVIEW_147_152.md`).<br>153.4 **Report.** Ranked findings, each with a proposed remediation-batch stub. | A grounded correctness / tech-debt / test-gap read of the recent work and the highest-risk engine code — without a low-yield exhaustive 70k-LOC pass. | A committed report ranks findings most-severe first; every 147–152 boundary claim is re-verified in code (or a violation is raised); each material finding carries a proposed remediation batch; **diagnose-only — no product code change lands in this batch.** **Shipped via PR #182, squash `7fca2cf`:** branch-push, PR-context, and post-merge `main` CI all passed seven jobs; Vercel preview passed; production Railway and Vercel same-origin health served the exact merge SHA, web `/` returned 200, and the unauthenticated daily-loop route returned 401 direct and proxied. |
| ~~Batch 154 — Full-app data / security review (diagnose-only)~~ | 🔴 High | Shipped | 154.1 **RLS coverage audit across every `coach.*` table** (all migrations, incl. the 015 / 019 / 020 / 021 RLS family), not just recent ones — confirm each health-data table has RLS enabled and correct user-scoping; cross-check Supabase `get_advisors` for security/RLS warnings.<br>154.2 **Conversational-learning write-path threat model (Batch 151).** Can a crafted chat inject bad durable context into `learned_context`? Verify the confirmed-only accept gate, the code-side rejection of transient/verdict/threshold/Red-VO2/data-quality items, evidence-citation enforcement, and that `classificationImpact='none'` truly leaves the learning write path unable to move the verdict or thresholds.<br>154.3 **Secret & auth hygiene.** Anthropic key never logged (Batch 141 claim — re-verify), Garmin/Hive token-blob handling, JWT + name/PIN auth surface, envelope error-body leakage. Includes the carried ops gap: seed the dormant `admin_alert_user_id` in prod (Batch 141's push channel) — the one non-diagnose action allowed in this batch.<br>154.4 **Endpoint authorization sweep.** Every router's ownership / 403 / 404 scoping, with explicit attention to the recently-added routes (`plan-actions/workouts/{id}/analysis`, the learning routes, the analysis-keyed chat routes) — prove no cross-user read. **Decide at `/batch-start`:** whether seeding `admin_alert_user_id` rides in 154 or spins a tiny standalone ops batch; report location. | Whole-app security posture — weakest-link, so full-app by necessity — with the recent learning / chat / read write-paths modelled explicitly. | Every `coach.*` table's RLS state is recorded; the learning write path is proven unable to reach verdict / thresholds / data-quality rules; no secret is logged on any path; endpoint ownership scoping is confirmed or gaps raised; report committed with ranked findings → remediation batches; **diagnose-only apart from optionally seeding `admin_alert_user_id`.** **Shipped via PR #183, squash `07aa0e0`:** branch-push, PR-context, and post-merge `main` CI all passed seven jobs; Vercel preview and production deploy passed; Railway and Vercel same-origin health served the exact merge SHA, web `/` returned 200, and the unauthenticated daily-loop route returned 401 direct and proxied. |
| ~~Batch 155 — Full-app coaching-integrity audit refresh (diagnose-only)~~ | 🔴 High | Shipped | 155.1 **Refresh the 2026-07-10 framework.** Re-run `COACHING_INTEGRITY_AUDIT.md`'s lenses (strong-vs-acute gaming/sycophancy, weak-vs-chronic drift; prior grade B/B+; top gaps **F1** baseline-drift, **F2** load-not-gated) and record whether F1/F2 are still open.<br>155.2 **Audit the new coaching surfaces for integrity regressions.** (a) **ERG-always trust** (152): does "trust ERG-held power as delivered" ever mask genuine under-performance or over-reach, and is the weekly surge honest-note calibrated (note, not nag)? (b) **Conversational learning** (151): can accumulated `learned_context` sycophantically drift tone/verdict over time even with `classificationImpact='none'`? (c) **Post-workout chat** (150): advisory-only — does it stay non-gaming under pressure ("just tell me I'm fine")? (d) **Training-week grounding** (148): does the factual planned→changed→executed narrative ever confabulate a moved-away or skipped session as done?<br>155.3 **Re-grade against real `coach`-schema data** (Prong B, as in the original audit — real reads, not synthetic), spanning recent days incl. ERG sessions and any chat-learned items; honest scorecard, no narrative softening. **Decide at `/batch-start`:** window of real reads to grade; whether to re-produce the Mark-safe scorecard artifact. | Confirm the coach's honesty / safety / non-gaming held as the recent brain-surface changes landed — the emergent property no single batch could prove. | F1/F2 status resolved; each new surface (ERG / learning / chat / week-grounding) integrity-assessed against real reads; the re-grade is recorded with no softening; updated `COACHING_INTEGRITY_AUDIT.md` (+ optional Mark-safe artifact) committed; regressions become remediation batches; **diagnose-only.** **Shipped via PR [#184](https://github.com/CraigR973/garmin-coach/pull/184), squash `42637f3` (Decision #235):** F1 (self-recalibrating baselines) and F2 (load can't move the light) both re-verified **still open, unchanged in code**, with F2 now carrying its first real-morning instance (07-24 load-driven Green on LOW readiness + hard yesterday). The four new brain surfaces (148/150/151/152) add no new path to the deterministic verdict — ERG-always trust is grounded in real recorded execution data with a calibrated honest-note, conversational learning is defended by five layers and unread by `_morning_verdict` (0 proposals ever distilled), post-workout chat is structurally advisory-only (`proposals_on_nonmorning_chat=0` live), and training-week grounding sources `executed` only from real Garmin activities. Prong B on real `coach` data 07-10→07-26: Green 11 / Amber 1 / Red 4 across 16 mornings incl. a genuine 4-Red cluster, no narrative softening (F8 stays RESOLVED), 19 chat turns with zero sycophancy. Grade **B+ held**; one new low-severity finding **F9** (confirmed memory has no cap/decay); remediation stubs R155-A (close F2) / R155-B (close F1) / R155-C (F9) / R155-D (chat anti-sycophancy line). Artifacts: refreshed `COACHING_INTEGRITY_AUDIT.md` (2026-07-26 section) + Mark-safe `docs/reviews/BATCH_155_MARK_SCORECARD.md`. Both CI waves + Vercel preview green; production Railway/Vercel health served exact SHA `42637f3`, web `/` 200, unauthenticated daily-loop 401 direct + proxied. No product/verdict/threshold/prompt/migration change. |
| ~~Batch 156 — Full-app UX / live-app review (diagnose-only)~~ | 🟢 Mid | Shipped | 156.1 **Walk every route** (~15) via the constrained [[ui-walkthrough-method]] (headless Chromium + dev build proxied to the prod API + `/activate` login, since live-browser tools are blocked on this Mac): Home, Check-in, Week, Sleep, Trends, Environment, Settings, Coach-state/memory, and the brief/read views.<br>156.2 **Exercise the new surfaces specifically:** the Week completed-workout read sheet (152), the per-interval editor (147), the post-workout follow-up chat (150), and the Coach-memory proposal review (151). Confirm honest-state rendering (no blank sheets / infinite spinners), mobile-first at 390px, and light + dark.<br>156.3 **Cross-screen consistency / drift pass** the per-screen batches couldn't see: toasts, skeletons, save affordances (cf. Batch 137's SaveButton finding), and empty/error states across screens. **Decide at `/batch-start`:** prod-data walkthrough vs a seeded local dataset; report location. | Judge the real experience across the whole app and catch cross-screen inconsistency, since UX quality is a whole-app property — not just the three new surfaces. | **Shipped via PR [#185](https://github.com/CraigR973/garmin-coach/pull/185), squash `7e77169` (Decision #236):** production-data walkthrough captured all 15 routes at 390×844 in light and dark plus 12 focused/mobile/desktop states; completed-read/chat and honest loading/null/failure states passed; report ranks 1 High / 4 Medium / 1 Low with remediation stubs R156-A…F (lead: live Coach memory rejects `holiday_windows` and exposes raw Zod output). Artifacts: `docs/reviews/BATCH_156_UX_LIVE_APP_REVIEW.md`, sanitized audit, and 42 screenshots. Branch-push, PR-context, and post-merge `main` CI all passed seven jobs; Vercel preview/production reached `READY`; exact-SHA Railway/Vercel health, web route, direct/proxied auth-gate, and Vercel runtime-error smokes passed. Diagnose-only; no product behavior/config change. |

## Post-roadmap — 2026-07-26 — Batch 153–156 audit remediation

Remediation batches for the 21 findings from the four diagnose-only reviews, triaged and sequenced in [`reviews/BATCH_153-156_REMEDIATION_ROADMAP.md`](reviews/BATCH_153-156_REMEDIATION_ROADMAP.md) (slots R1–R9 + defer): live-broken → coaching-safety → security Highs → Mediums. **Tier here reflects the implementation model** (🔴 High = Opus, 🟢 Mid = Sonnet), *not* review severity — each row states the finding's severity. Slot→batch: R1→157, R2→158, R3→159, R4→160, R5→161, R6→162, R7→163, R8→164, R9→165, deferred CR153-06→166 (CR153-05 subsequently folded from 165 into 159 — same post-* services). Decision numbers are assigned at `/batch-start`; rows stay unstruck and `Planned` (166 `Deferred`) until started. **Note:** Batch 155's coaching-integrity remediation (stubs R155-A…D, from `COACHING_INTEGRITY_AUDIT.md`) is authored in the next section — Batches 167–169, with R155-D folded into Batch 165.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 157 — Coach-memory public contract (UX156-01)~~ | 🟢 Mid | Shipped | 157.1 **Public Coach-memory DTO + filter internal sections.** `CoachingStateService.get_snapshot` (`services/coaching_state.py:638-650`) selects every KB row with no public-section filter and the route serialises it unchanged (`routers/coaching_state.py:258-270`); prod stores internal sections `holiday_windows` and `generated_block`, which the shared `knowledgeBaseSectionSchema` (10 public sections, `packages/shared/src/schemas.ts:281-292`) rejects, failing `coachingStateEnvelopeSchema.parse` for the whole response (`CoachStatePage.tsx:94-101`). Filter internal rows server-side (or intentionally add + render them) — never leak arbitrary storage-section names into a closed client enum; keep the admin editor contract separate.<br>157.2 **User-safe error + retry.** The error card renders `readQuery.error.message` directly (`CoachStatePage.tsx:432-444`), dumping a raw Zod issue array on Mark's phone; show short user-safe copy + retry, keep structured detail in diagnostics only.<br>157.3 **Contract tests** across backend/shared/frontend containing the live internal section set + a production-shaped route test proving one unexpected row can't blank the page. | Coach memory renders against current production data — restoring the shipped Batch 150/151 proposal-review surface — instead of failing its parse and printing raw validation output to Mark. | Shipped via PR #186, squash `f717802` (Decision #237): route-facing Coach-memory snapshots now filter `KnowledgeBase` rows to the public/editor section allowlist (`PUBLIC_KNOWLEDGE_BASE_SECTIONS = tuple(KB_SECTION_BUILDERS)`), so internal production rows including `holiday_windows` and `generated_block` never leak into the shared closed enum; the admin editor remains scoped to the known editable/public sections. The Coach-memory error card now shows short user-safe retry copy instead of raw Zod/API text. No migration, prompt, verdict, plan, memory write, or architecture change. Branch-push, PR-context, and post-merge `main` CI passed seven jobs; Vercel production was `READY/PROMOTED` at `f717802`; Railway direct and Vercel same-origin health served `f717802`; web `/` and `/coach-state` returned 200; unauthenticated Coach-memory returned 401 direct and proxied. |
| ~~Batch 158 — Delivery-rail atomicity & observability (CR153-01 + CR153-08)~~ | 🔴 High | Shipped | 158.1 **`commit=False` must never commit the unit of work.** On an intervals.icu `HTTPException`, `replace_event` writes `last_error` and calls `session.commit()` even when the caller passed `commit=False` (`services/workout_delivery.py:651-675`), persisting caller-owned dirty state incl. the new `live.planned_workout_id`/`planned_workout_version` set before the cloud call (`services/executable_coaching.py:602-617`). Roll back or isolate caller mutations on cloud failure.<br>158.2 **Advance the pointer only after cloud success** (or restore on failure). The next `_deliver_one` treats pointer/version equality as proof the workout is live and returns early (`executable_coaching.py:605-610`) while Zwift still holds the old IR, blocking the reconciliation retry; same pre-repoint pattern in `_resync_event` (`:1101-1116`). Keep a content/version fingerprint or retryable sync state rather than trusting pointer equality.<br>158.3 **Observable non-deliverable state (folds CR153-08).** `_deliver_one` swallows an `HTTPException` from FTP-load/IR-build and returns `None` (`:591-598`), indistinguishable from an intentional no-op; persist/emit a structured retryable/non-retryable failure with user/workout/version context and distinguish it from expected non-bike no-ops.<br>158.4 **Tests.** DB-backed failure → persisted state → successful reconciliation retry across interval-edit and generic resync; malformed-IR, missing-FTP, and no-op paths tested separately. | A transient external-update failure can never leave the app claiming the newest workout is live while Zwift holds the old one, and a silently-skipped delivery leaves a durable diagnostic instead of vanishing. | Shipped via PR #187, squash `bf7474b` (Decision #238): `commit=False` is caller-transaction-safe, pointers advance only after cloud success, and persisted delivered IR id/version is the content fingerprint used by reconciliation. Interval-edit and generic re-sync failure→retry paths preserve desired state without duplicate creates or false live pointers; malformed/missing-FTP bike rows persist structured deduplicated failures while expected no-ops remain row-free. No migration or verdict/prompt/safety-rule change. Local gates passed; branch, PR-context, and post-merge main CI passed all seven jobs, including 918 PostgreSQL-backed tests and the Alembic round trip. Vercel production was READY; Railway direct and Vercel same-origin health served exact SHA `bf7474b`; web `/` returned 200; unauthenticated workout-delivery proposals returned 401 direct and proxied; Vercel's one-hour runtime-error scan was clean. Closes 🔴 **CR153-01** + **CR153-08**. |
| ~~Batch 159 — Completed-session reads: non-ride linkage, states + prompt-currentness (CR153-02 + CR153-05)~~ | 🔴 High | Shipped | 159.1 **Generic completed-session linkage.** Batch 152's lookup selects any `Analysis` with a matching `planned_workout_id` (`routers/plan_actions.py:412-458`), but only `PostWorkoutAnalysisService.generate_and_store` writes that column (`services/post_workout_analysis.py:426-466`); strength/flexibility/walk generators write `activity_id` + their post-* type but not `planned_workout_id` (`post_strength_analysis.py:273-309`, `post_flexibility_analysis.py:375-410`, `post_walk_analysis.py:283-318`), so their completed reads can't be returned. Move planned-workout completion/linking to a shared post-activity seam for cycle/strength/flexibility/walk.<br>159.2 **Constrain + scope the selector** to explicitly-supported post-session analysis types and verify the workout belongs to the caller.<br>159.3 **Generation-state contract.** The endpoint has no state source — missing, in-flight, and failed all return `read: null` and render one "No read yet" state; add a small persisted generation-status contract (reuse `brief_generation_status` if it fits) so absent / generating / failed render distinct honest states (Batch 152.3). **Decide at `/batch-start`:** reuse `brief_generation_status` vs a new source.<br>159.4 **Prompt-currentness for these non-ride reads (folds CR153-05).** `_analysis_covers_activity_checkin` in strength/flex/walk compares only the check-in timestamp (`post_flexibility_analysis.py:556-566` + peers), so a `PROMPT_VERSION` bump is ignored for an unchanged activity; handover returns latest-for-date before checking prompt version (`handover.py:614-626`). Give these regenerable artifacts a shared currentness helper using prompt version + input/check-in version — folded here from R9/165 because it rewrites the same three post-* services as 159.1.<br>159.5 **Tests** — positive+negative DB tests for every post-* type, no-read, generating, failed, foreign-user, unrelated-analysis; old-version/changed-input currentness for strength/flex/walk/handover; frontend state tests. | An existing strength/mobility/walk read shows on the Week view instead of "no read", absent/generating/failed reads each render the honest state Batch 152 promised, and a prompt-version bump refreshes these non-ride reads. | Completed strength/flexibility/walk sessions with a read return it via the shared lookup; the selector is limited to supported post-session types and ownership-checked; absent, generating, and failed render distinct honest states; a `PROMPT_VERSION` bump invalidates unchanged strength/flex/walk/handover reads; DB tests cover every post-* type + no-read/generating/failed/foreign-user + old-version currentness, and frontend state tests pass. **As built: migration `022`** (new activity-scoped status source; existing `planned_workout_id`). Closes 🔴 **CR153-02** + 🟠 **CR153-05** (`docs/reviews/BATCH_153_CODE_REVIEW.md`); fixes a shipped Batch 152 product-acceptance gap and folds R9's prompt-currentness (same post-* files). Decision assigned at `/batch-start`. **Shipped via PR #188, squash `5aaa594` (Decision #239):** shared cycle/strength/mobility/walk completion-linking, activity-scoped honest read states, migration-022 legacy backfill, ownership/type-scoped lookup, and prompt+input currentness are live. Branch, PR-context, and post-merge main CI passed all seven jobs; Vercel preview/production reached READY; exact-SHA Railway/Vercel health, web 200, direct/proxied auth-gate, and Vercel runtime-error smokes passed. |
| ~~Batch 160 — Auth Phase 3: remove the PIN/JWT fallback (DS154-01)~~ | 🔴 High | Shipped | 160.1 **Remove the production name/PIN surfaces.** Delete `login`/`refresh`/`logout`/`change_pin`/`pin_reset_request`/`pin_reset` + schemas from `routers/auth.py`, the bcrypt-PIN + HS256 helpers in `auth.py`, and the auth-specific rate-limit keys; `get_current_user` becomes device-token-only (keep a minimal `revoke`). Closes the dead-lockout gap (login never reads/writes `failed_login_count`/`locked_until`; only an in-memory SlowAPI bucket guards a 10k-value PIN) and stops `POST /pin/reset-request` logging the live reset bearer JWT at INFO (`routers/auth.py:398-423`).<br>160.2 **Cutover.** Ensure every intended device has an activation/recovery path, revoke residual PIN-era refresh tokens, verify the device-token revocation/recovery runbook. If full removal can't ship immediately, at minimum stop logging reset JWTs and replace log-as-delivery with an authenticated single-use operator flow.<br>160.3 **Phase 4 schema/config cleanup.** Migration dropping `profiles.pin_hash`/`failed_login_count`/`locked_until`; remove `jwt_access_secret`/`jwt_refresh_secret` from `config.py` + Railway. **Decide at `/batch-start`:** land 160.3 here or a follow-up; confirm no device is stranded first.<br>160.4 **Tests** — replace PIN auth tests with device-token activate/verify/revoke; prove PIN routes 404 in prod mode and no raw bearer reaches structured logs. | Retire the temporary name+4-digit-PIN fallback the architecture always meant to delete — closing the account-takeover path (tiny PIN space, no durable lockout, reset JWT logged) — leaving passwordless device tokens the sole credential. | PIN login/change/reset endpoints + helpers are gone; `get_current_user` is device-token-only; no reset bearer (or any raw credential) is logged; residual PIN-era tokens revoked; PIN columns + JWT secrets dropped here or in an explicit follow-up; device-token activate/verify/revoke tests pass and prove PIN routes 404 in prod. **Migration required.** Closes 🔴 **DS154-01** (= v1-v2 P1-1/P3-3; `docs/reviews/BATCH_154_DATA_SECURITY_REVIEW.md`, runbook `docs/reviews/auth-simplification-plan.md`). **Decision-gated:** confirm trigger-now vs keep deferring at `/batch-start`. Decision assigned at `/batch-start`. **Shipped via PR #189, squash `a0dd28c` (Decision #240):** device tokens are the sole credential; migration `023` revoked legacy refresh rows and removed the PIN/lockout schema plus the obsolete purpose default; Railway's JWT secrets were removed after exact-SHA health. Branch, PR-context, and post-merge `main` CI passed all seven jobs; Vercel preview/production reached READY; direct and same-origin health served `a0dd28c`; legacy login/refresh returned 404; unauthenticated device routes returned 401; production schema/token verification and Vercel runtime-error smokes passed. |
| ~~Batch 161 — Concurrency, idempotency & workload budgets (UX156-02 + CR153-04 + DS154-05)~~ | 🔴 High | Shipped | 161.1 **Atomic one-time activation (UX156-02).** The exchange selects an unused row then sets `used_at` non-atomically (`routers/auth.py:278-324`), so two transactions both see `used_at IS NULL` — the review saw two `purpose="device"` rows per activation, leaving orphan long-lived credentials. Consume with a locking read or atomic `UPDATE … WHERE used_at IS NULL … RETURNING`, minting the device credential only for the winner; add a client ref/idempotency guard for Strict-Mode/remount double-fire (`ActivatePage.tsx:19-65`).<br>161.2 **Generation lease + request identity (CR153-04).** Current-day manual-entry PUTs mark the brief generating and enqueue a force-generate (`routers/daily_loop.py:1482-1513`, `executable_coaching.py:342-370`); post-session force-gen check/generate/inserts without a lease (`daily_loop.py:1564-1580`); `analyses` has no unique constraint by design (Decision #219). Add an idempotency identity (user/date/source-version morning; user/activity/check-in-version/prompt-version post-session) + a short DB-backed lease/advisory lock before the paid boundary, preserving intentional regenerations.<br>161.3 **Authenticated workload budgets (DS154-05).** `POST /tts/synthesize` + other paid/CPU-heavy routes have no per-user rate/concurrency boundary (`routers/tts.py:75-110`; Piper subprocess); a stolen 365-day token can drive them. Add per-user rate limits + a semaphore/queue, bound queue depth, return honest 429/503 without spawning more subprocesses.<br>161.4 **Tests** — DB concurrent-redemption (one 200 / one 401 / one device row); identical-generation one paid call (two historical rows only for genuinely-distinct input/prompt); unique-input TTS cache bypass; stolen-token revocation; timeout recovery. | Operations meant to happen once or stay bounded actually do — no double-redeemed activation codes or orphan tokens, no duplicate paid model calls from two tabs/retries, no unbounded expensive work per user. | Activation is atomic (one device row per code under concurrency); identical generation makes one paid call while genuinely-changed input still regenerates (Decision #219 history intact); TTS + paid boundaries are per-user rate/concurrency-limited with honest 429/503; concurrency/idempotency/cache-bypass/revocation tests pass. **As built:** migration `024` supplies durable request identities/leases and artifact-scoped advisory locks; fail-fast zero-depth slots allow one active paid call per user, four Anthropic globally, and one Piper globally, with shared authenticated `30/hour` paid and `60/hour` TTS route limits. Activation was fixed first inside the same batch, not split. Closes 🟠 **UX156-02 / CR153-04 / DS154-05** (BATCH_153/154/156 reviews). **Shipped via PR #190, squash `bc057ea`:** final branch, PR-context, and post-merge `main` CI passed all seven jobs; Vercel preview/production reached READY; exact-SHA Railway/Vercel health, web, auth-gate, schema, and runtime-error verification passed. Decision #241. |
| ~~Batch 162 — Learning-memory integrity (DS154-02 + DS154-07)~~ | 🔴 High | Shipped | 162.1 **Evidence-bound acceptance (DS154-02).** On accepting a proposal, caller-edited text is checked only by `statement_is_durable`, not for support by the cited evidence (`conversation_learning.py:589-622`), then appended to `knowledge_base.section='learned_context'` (`:624-662`) — so a blacklist-avoiding instruction-shaped statement can be substituted and bias future morning/post-session prose. Prefer immutable extracted statements at acceptance, or constrain editing to an evidence-supported correction flow that re-runs candidate validation against current user-authored sources.<br>162.2 **Structural instruction/data separation.** Separate factual memory from instructions structurally and delimit learned statements as untrusted quoted data in every prompt (`learned_context.py:9-20` labels but doesn't sanitize); don't rely on a growing blacklist. (Confirmed unable to reach the deterministic verdict/thresholds/#133-#135/Red-VO2, but can make prose contradict a correct verdict.)<br>162.3 **Ownership regression + route-inventory lint (DS154-07).** Ownership is enforced (`conversation_learning.py:552-587`) but untested cross-user; add DB tests proving a second profile can't list/accept/edit/reject the owner's proposal and no KB version is written; add a lint requiring every non-public endpoint to declare `CurrentUser`/`AdminUser`.<br>162.4 **Adversarial tests** — unrelated edited statement, injection wording, stale/missing evidence, supported paraphrase; a contradiction test proving accepted memory can't make output advise against the verdict/guardrails. | Protect the flagship conversational-learning feature so accepted memory can never smuggle instruction-shaped, evidence-unbound content into the coach's narrative, and pin its cross-user ownership contract with tests. | Accepted statements are immutable or re-validated against their cited evidence; learned text is structurally delimited as untrusted data in prompts; a contradiction test proves accepted memory can't make prose advise against the verdict; cross-user list/accept/edit/reject is blocked with no KB write; a route-inventory lint enforces auth on every non-public endpoint; adversarial tests pass. **No migration.** Closes 🟠 **DS154-02** + **DS154-07** (`docs/reviews/BATCH_154_DATA_SECURITY_REVIEW.md`). **Shipped via PR #192, squash `3a58debe` (Decision #243):** confirmation is exact-wording accept/reject; every current cited user-owned source is revalidated before the KB write; unsupported and instruction-shaped candidates are rejected; all five learned-context prompt consumers receive only structurally delimited untrusted quoted data behind the same fixed guardrail; cross-user list/accept/edit/reject leaves the proposal and KB untouched; and the registered-route auth inventory requires `CurrentUser`/`AdminUser` for every non-public endpoint. Branch-push, PR-context, and post-merge `main` CI passed all seven jobs; Vercel preview/production reached READY; exact-SHA Railway/Vercel health, web `/` + `/coach-state`, direct/proxied learning-route auth gates, and runtime-error smokes passed. |
| ~~Batch 163 — Mobile a11y & responsive (UX156-03/04/05/06)~~ | 🟢 Mid | Shipped | 163.1 **Semantic text vs fill tokens (UX156-03).** Light semantic foregrounds `#059669`/`#A77C2A`/`#D97706` (`index.css:178-201`) are ~3.2–3.8:1 on white, below AA 4.5:1 for the 10–12px text in use; axe flagged 12/15 light routes (Week 41 nodes). Add AA-verified semantic *text* tokens per theme distinct from decorative fills/borders/chart-marks/button-backgrounds; fix the shared Badge (`badge.tsx:5-20`) first, then remove page-local `text-primary`/`text-success` on light surfaces.<br>163.2 **Responsive interval-edit rows (UX156-04).** The Setting/Current/Change-to table is `min-w-[34rem]` inside `overflow-x-auto` (`IntervalWorkoutEditor.tsx:175-182`); at 390px the editable column starts off-screen. Use stacked per-setting cards or a two-row layout below the small breakpoint, label+control+unit/bounds together with a 44px target; keep the compact table on wider screens.<br>163.3 **Enforce 44px touch targets (UX156-05).** `.tap-target` declares 44×44 (`index.css:351-355`) but the account trigger measures 32×20 (`TopBar.tsx:44-51`), `Button size="sm"` is 36px (`button.tsx:26-31`, used by feedback + chat Ask), Week chips ~123×22. Give the account trigger, activity chips, feedback controls, and chat action a 44px hit area (invisible padding where the chip stays compact); define a mobile-safe small-button primitive.<br>163.4 **Keyboard-reachable scroll regions (UX156-06).** Markdown wrapper (`Markdown.tsx:65-68`) + both Handover containers (`HandoverPage.tsx:162-170`) scroll without keyboard focus; add `tabIndex` + accessible label + visible focus to overflowing shared scroll containers.<br>163.5 **Tests** — contrast coverage per badge variant + representative rows in both themes; 390px DOM-size + reach-every-field interaction (incl. Z2 zero-rest); keyboard-scroll. | The phone UI meets the app's own accessibility bar for its older primary user — readable status colour, a usable interval editor, 44px hit areas, keyboard-reachable content. | Semantic text colours pass AA in both themes via tokens distinct from fills; the 390px interval editor shows every editable field without horizontal scrolling; account trigger, activity chips, feedback, and chat action meet 44px; overflowing scroll regions are keyboard-focusable; contrast + 390px-size + keyboard tests pass. **Frontend-only, no migration.** Closes 🟠 **UX156-03/04/05** + **UX156-06** (`docs/reviews/BATCH_156_UX_LIVE_APP_REVIEW.md`). **Shipped via PR #193, squash `5bcb888` (Decision #244):** semantic text tokens and Badge foregrounds are AA-verified; small controls/account/activity-chip hit areas meet 44px; Markdown/Handover scroll regions are keyboard-focusable; the interval editor uses responsive 44px setting rows with Z2 zero-rest coverage; and branch-push, PR-context, plus post-merge `main` CI passed all seven jobs. Production verified exact-SHA health direct + same-origin, web `/` 200, and served CSS containing the new semantic text tokens + 44px min-height utilities. |
| ~~Batch 164 — Security / ops hygiene (DS154-03/04/06)~~ | 🟢 Mid | Shipped | 164.1 **Reproducible dependency security (DS154-03).** JS lockfile has 1 High (`postcss@8.5.14`, GHSA-r28c-9q8g-f849) + 3 Moderate (`react-router*@6.30.4`); CI's `security-audit` runs only `pip-audit` (`.github/workflows/ci.yml:162-173`); Python runtime is lower-bounds on mutable `python:3.12-slim`. Update/override PostCSS ≥8.5.18; add a maintained JS advisory gate to CI failing on applicable High (reviewed exceptions for non-reachable build/SSR advisories); produce a hashed Python runtime lock + pin the image by version/digest; add a history-aware secret scanner. **Decide at `/batch-start`:** React Router 7 migration here vs its own batch.<br>164.2 **Secure operator artifacts & backups (DS154-04).** Garmin/Hive bootstrap scripts print bearer blobs by default / write without `0600` (`bootstrap_garmin_tokenstore.py:15-46`, `bootstrap_hive_tokenstore.py:23-59`); the daily `pg_dump` writes unbounded plaintext with no `--schema=coach` on the shared Supabase DB and no retention (`services/backup.py:53-103`). Make stdout secret emission explicit opt-in defaulting to a `0600` file; create tokenstores with restrictive modes; decide whether the in-container backup is still useful beside Supabase managed backups, and if kept restrict to `coach`, `0700`/`0600`, encrypt, cap retention, remove partials.<br>164.3 **Backend-role / live-RLS drift checks (DS154-06).** RLS is a Data-API backstop only — FastAPI connects as the `postgres` owner and bypasses every policy (`relforcerowsecurity=false`); legacy policies omit `TO authenticated`/`WITH CHECK` and use bare `auth.uid()`. Add a read-only CI/deploy posture check failing if any `coach` table loses RLS, Data-API roles gain privileges, or the exposed-schema list changes; harden the four legacy policies; set a safe `search_path` on `coach.set_updated_at`. **Decide at `/batch-start` (defer candidate):** moving FastAPI to a least-privilege role + `FORCE RLS` is a larger decision — likely defer that half.<br>164.4 **Tests** — JS/Python audit gates green; backup mode/scope/expiry if kept; the RLS drift-check fails on a simulated regression. | Close the audited supply-chain, secret-at-rest, and RLS-drift gaps — hardening posture without pretending RLS isolates the backend. | PostCSS updated + a JS advisory gate in CI (reviewed exceptions); a hashed Python lock + pinned image; secret emission opt-in `0600`, tokenstores restrictive, the backup scoped/permissioned/retained-or-removed; a posture check fails on RLS/grant/exposed-schema drift; legacy policies hardened + function `search_path` fixed. **Partial migration** (RLS policy hardening); least-privilege-role change deferred. Closes 🟠 **DS154-03/04/06** (role-migration half deferred; `docs/reviews/BATCH_154_DATA_SECURITY_REVIEW.md`). Decision assigned at `/batch-start`. **Shipped via PR #194, squash `8c71003` (Decision #245):** PostCSS 8.5.24 clears the High advisory; CI now runs hashed `pip-audit`, the reviewed pnpm advisory gate, full-history Gitleaks, and the optional RLS posture verifier; Docker is digest-pinned and hash-installs `requirements.lock`; token bootstrap output is explicit `--stdout` or atomic 0600 file; backups are `coach`-schema-only, 0700/0600, partial-safe, and retained to seven dumps; migration `025` hardens legacy Supabase policies and `coach.set_updated_at`. React Router 7 and least-privilege backend role/FORCE RLS remain deferred. Branch-push, PR-context, and post-merge `main` CI passed all seven jobs; Vercel preview/production reached READY; Railway exact-SHA health, web `/` 200, direct auth-gate 401, and runtime-error smokes passed. Same-origin API paths on Vercel currently return 404, so production API smokes used the direct backend. |
| ~~Batch 165 — Small correctness + chat-prompt hardening (CR153-03 + CR153-07 + R155-D)~~ | 🟢 Mid | Shipped | 165.1 **Deterministic chat turns + honest post-session capability (CR153-03).** A user message + its assistant reply share an identical `created_utc` (`brief_chat.py:275-290`) and history/context order only by it (`:192-205`, `:243-257`), so equal-timestamp rows can render assistant-before-user; and the universal prompt promises a confirmable adjustment (`:55-73`) that post-session reads deliberately withhold. Add a monotonic turn/sequence (or composite deterministic order) and build the prompt from an explicit per-read capability flag. **Decide at `/batch-start`:** `brief_messages` sequence column vs composite order (avoids a migration).<br>165.2 **Anti-sycophancy chat directive (R155-D).** Unlike the post-workout *read* prompt ("the one place you must not be sycophantic"), the brief-chat system prompt carries no explicit anti-sycophancy directive (`brief_chat.py:65-68` floors); add a "do not cave to reassurance pressure" line matching the read prompt. Tone-only hardening — the verdict is immutable in the chat packet — folded here because 165.1 already rebuilds this prompt.<br>165.3 **Timezone-correct Plan Week grouping (CR153-07).** `PlanActionService._activities_by_date` queries naive UTC bounds and groups with `start_utc.date()` (`plan_actions.py:641-691`), so during BST a 00:00–01:00 activity lands on the previous day (Batch 149 anchors the Week in profile-local time). Pass the profile IANA timezone in, convert local bounds to UTC for the query, group by local date.<br>165.4 **Tests** — same-timestamp ordering + morning-vs-post-session capability wording; the chat prompt carries the anti-sycophancy directive; GMT + BST midnight-boundary grouping incl. completed-kind dedup. | Deterministic chat turn order + truthful post-session capability, an explicit anti-sycophancy chat directive, and profile-local Week grouping. | Shipped via PR #195, squash `c1c3896` (Decision #246): brief-chat history and prompt context now order by `created_utc`, user-before-assistant role, then id, avoiding a `brief_messages` migration; the chat prompt carries read-type capability wording so morning reads may mention a confirmable adjustment proposal while completed-session/retrospective reads stay advisory-only; the same prompt has the explicit anti-sycophancy directive from R155-D; and Plan Week activity chips query/group/dedup by profile-local date across GMT/BST. Backend-only; no migration, shared-schema, verdict, plan, delivery, or hosting change. Branch, PR-context, and post-merge `main` CI passed all seven jobs; Vercel preview passed; production verified exact-SHA Railway health, web `/` 200, and protected direct API smokes for brief chat + Week schedule returned 401. Same-origin Vercel API paths still return the known 404, so production API smokes used the direct backend. |
| ~~Batch 166 — Bulk source & post-activity lookups (CR153-06)~~ | 🟢 Mid | Shipped | 166.1 **Bulk-fetch latest analysis + check-in rows** keyed by activity/date and reduce latest-wins once, replacing the N+1 in conversation learning (up to 60 sources × a per-entry latest-analysis query, `conversation_learning.py:366-405`) and the per-activity pending scans (`post_flexibility_analysis.py:230-257` + strength/walk/ride peers); preserve source ordering + the Decision #219 audit trail.<br>166.2 **Tests** — query-count regression for a multi-activity week + a 60-entry learning window. | Remove avoidable N+1 round-trips from the learning + pending-read scans. | Shipped via PR #196, squash `aa65a9e` (Decision #247): shared bulk helpers now fetch latest analyses by activity/date, latest activity check-ins, and generation statuses once per scan, then reduce latest-wins in Python so Decision #219's historical audit rows stay intact. Conversation learning links up to 60 check-in sources without per-entry analysis queries while preserving source ordering; ride/strength/flexibility/walk pending scans use bounded activity/check-in/status maps. DB-backed query-count regressions cover a 60-entry learning window and a multi-activity week. Backend-only; no migration/shared-schema/verdict/plan/delivery change. Branch-push, PR-context, and post-merge `main` CI passed all seven jobs; production verified exact-SHA Railway health, web `/` 200, and protected direct learning + Week schedule smokes 401. Same-origin Vercel API paths still return the known 404, so production API smokes used the direct backend. Closes 🟠 **CR153-06** (`docs/reviews/BATCH_153_CODE_REVIEW.md`). |

## Post-roadmap — 2026-07-26 — Batch 155 coaching-integrity remediation

Remediation for the open findings in the Batch 155 coaching-integrity refresh (`COACHING_INTEGRITY_AUDIT.md`), which the previous section deferred. These close the chronic-drift gaps the audit says would lift the grade from B+ to A−. **These are the first batches in the wave that deliberately change the deterministic Green/Amber/Red verdict engine** — so every acceptance criterion requires the change to only ever *harden* the light (never soften it) and to keep the light deterministic (computed in Python, not model-set). Stub→batch: R155-A→167, R155-B→168, R155-C→169; **R155-D (anti-sycophancy chat line) is folded into Batch 165** (same `brief_chat.py` prompt). 167/168 are 🔴 High (verdict core, paired — could be run as one batch if preferred); 169 is 🟢 Mid. The older 2026-07-10 verdict findings are now batched too: F3 (MED-HIGH — sleep credit can carry a night across the Green line), F4 (MED — no cumulative escalation to Red), and F5 (MED — optimistic missing-data policy) are **clubbed into Batch 170** (all three reshape the same `_morning_verdict` ladder and interact); F6 (MED — chronic surveillance is advisory-only) is **Batch 171** (🟢 Mid unless it takes the verdict-cap option). F7's decay half is covered by 169. The verdict-engine safety rule (only ever harden the light, keep it deterministic) applies to 170, and to 171 if it caps the verdict. Decision numbers assigned at `/batch-start`; rows stay `Planned`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 167 — Close F2: training load can cap the verdict (R155-A)~~ | 🔴 High | Shipped | 167.1 **Give `_morning_verdict` a load input.** The deterministic verdict takes no ACWR/ramp/recovery-time parameter (`services/morning_analysis.py:1450-1461`); ACWR/chronic-load/balance are prose-only to the model (`:69-72`), a hard prior day only appends a note when the day is already non-Green (`:1547-1550`), and the one place load enters the ladder *relaxes* it — a Low Garmin readiness with clean recovery + any load present reads `load_driven` and escapes auto-Amber (`:1503-1508,1522`). Pass a real load signal (computed ACWR and/or Garmin recovery-time) into the verdict.<br>167.2 **Add a hard load cap (never a relax).** When ACWR ≥ ~1.5 (or recovery-time over a threshold), cap the day at **Amber** independent of how good last night looked; keep the acute Red gates. This only *hardens* the light. **Decide at `/batch-start`:** exact thresholds + source (computed ACWR vs Garmin recovery-time vs both), and whether to remove or subordinate the `load_driven` relax path.<br>167.3 **Motivating real case + regression.** 07-24 fired Green on readiness LOW + yesterday HARD via `readinessInterpretation="load_driven"`; after this change that morning caps at Amber — add a deterministic test on that shape.<br>167.4 **DECISIONS + tests** proving the cap fires on high ACWR/recovery-time, never lowers caution, and leaves the acute Red/Amber gates unchanged. | Let accumulating training load *cap* the daily light at Amber (not merely relax it), closing the audit's F2 gap so a fast ramp with clean overnight signals can no longer green-light overreaching. | Shipped via PR #197 / squash `596b2b9` (Decision #248): `_morning_verdict` consumes computed ACWR and Garmin recovery time; ACWR ≥1.50 or recovery >24h applies a one-way Amber ceiling; `load_driven` is subordinate; the structured cap packet and prompt v17 preserve deterministic ownership; 07-24 and only-hardens regressions cover the real case. No migration, shared-schema, plan, delivery, auth, or hosting change. Three CI waves passed; Vercel preview/production and Railway production were verified at the exact SHA; web, readiness, and protected daily-loop smokes passed. Closes 🔴 **F2 / R155-A** (`COACHING_INTEGRITY_AUDIT.md`). |
| ~~Batch 168 — Close F1: absolute baseline anchor + drift alarm (R155-B)~~ | 🔴 High | Shipped | 168.1 **Anchor personal floors with an absolute floor.** The soft-sleep→Green override floors readiness at Mark's own rolling median (`morning_analysis.py:1315-1321`, `:1696`), "in band" keys off personal quartiles (`personal_baselines.py:49-62`), the window is 84 days (`metric_baselines.py:36`), and no absolute anchor exists — so a multi-week fatigue slide lets the floors sink and old-Amber becomes new-Green (Probe 4: readiness 52 → Green against a drifted median of 50). Add an absolute floor the personal floor can't sink below (e.g. never treat readiness < 50 as "at median", regardless of history).<br>168.2 **Alarm on the baseline trend itself.** Flag when the 84-day median is *declining* so a slow slide is visible instead of absorbed as the new normal. **Decide at `/batch-start`:** whether the trend signal caps the verdict (like F2) or is a strong surfaced warning; exact absolute-floor constants.<br>168.3 **DECISIONS + tests** — a drifted-baseline scenario no longer grants the override below the absolute floor, healthy-baseline behaviour is unchanged, and the trend alarm fires on a sustained decline. | Stop "normal" self-recalibrating — anchor the verdict's personal floors so a slow multi-week fatigue slide can't quietly become the new Green, closing the audit's F1 gap. | **Shipped via PR #198 / squash `d6fa01c` (Decision #249):** the effective soft-sleep readiness floor is `max(personal 84-day median, 60)`, so readiness 52 against a drifted median of 50 remains Amber while readiness 66 on a healthy baseline remains Green. Two calendar 42-day halves inside the trailing 84-day window, with at least 21 samples each, trigger a structured warning when the recent median declines by at least five points; prompt v18 must surface it, but the alarm cannot set or soften the light. Deterministic drift, sampling-boundary, healthy-baseline, and warning-only regressions pass. Backend + docs only; no migration, shared-schema, plan, delivery, auth, or hosting change. Branch-push, PR-context, and implementation-merge `main` CI passed all seven jobs; Vercel preview/production reached Ready; exact-SHA Railway health, API readiness, web `/`, and protected daily-loop smokes passed. Closes 🔴 **F1 / R155-B** (`COACHING_INTEGRITY_AUDIT.md`). |
| ~~Batch 169 — Bound & decay accumulating memory: learned_context + corrections (R155-C)~~ | 🟢 Mid | Shipped | 169.1 **Cap and/or age `learned_context` (F9).** `learned_context_packet` returns *all* confirmed items with no volume cap and no decay (`services/learned_context.py:13-20`), so over long accumulation it could grow the prompt and subtly warm tone (human-curated, cannot touch the light). Add a volume cap and/or aging so confirmed memory can't grow unboundedly.<br>169.2 **Decay old corrections (F7).** Corrections are fed to the model as ground truth (last 5) with no decay (`morning_analysis.py:80-84`); age/decay them so a repeated correction can't indefinitely steer narrative tone, and never let a correction restate an objective metric as better than measured.<br>169.3 **Tests** — the packet respects the cap/age policy; stale corrections decay; `_morning_verdict` remains unable to read `learned_context` (unchanged invariant re-asserted). | Bound and age the coach's accumulating memory — learned context and corrections — so it can't grow the prompt or warm tone over time, closing F9 and F7's decay half. | **Shipped via PR #199 / squash `353c61c` (Decision #250):** `learnedContext` now emits at most the latest 12 eligible confirmed items; dated items older than 365 days decay out; undated legacy items sort oldest and survive only when the cap has room. Fed-forward corrections keep the existing five-row cap but now decay after 45 days, and morning/post-workout prompts v19/v11 forbid using corrections to restate objective metrics, completed workouts, or deterministic verdicts as better than measured. `_morning_verdict` still has no learned-memory parameter. Backend + docs only; no migration, shared-schema, plan, delivery, auth, or hosting change. Branch-push, PR-context, and implementation-merge `main` CI passed all seven jobs; Vercel preview/production reached Ready; exact-SHA Railway health, web `/`, and protected Coach-memory learning/daily-loop smokes passed. Closes **F9 / R155-C** and **F7 decay** (`COACHING_INTEGRITY_AUDIT.md`). |
| ~~Batch 170 — Verdict-ladder hardening: sleep-credit ceiling, cumulative escalation, missing-data policy (F3 + F4 + F5)~~ | 🔴 High | Shipped | 170.1 **Sleep-credit ceiling (F3, MED-HIGH).** `age_adjusted_sleep_score` only ever raises the raw Garmin score (credit ≥ 0, capped +12; `sleep_scoring.py:45-47,159`) and feeds the only hard gates — Red < 60, Amber < 74, Green ≥ 74 (`morning_analysis.py:1180-1206`); +12 can jump a full band and once a credited night reaches ≥74 the soft-sleep-caution path is skipped. Real data 07-08: raw 53 (POOR) → age-adj 65 kept a would-be-Red night at Amber. Let age-credit lift *within* a band but not alone carry a night *across* the Green threshold — require a corroborating objective signal (or a minimum raw score) to convert a credited night to Green.<br>170.2 **Cumulative escalation to Red (F4, MED).** The ladder is first-match with no stacking (`morning_analysis.py:1180-1206`): Poor readiness alone caps at Amber and moderate-bad signals never compound (Probe 5: age-adj 62 + Poor readiness + subjective 3 + hard-yesterday + 1400-min recovery → Amber). Add a stacking rule: Poor readiness co-occurring with any second negative (soft sleep, low subjective, hard-yesterday, elevated RHR) escalates Amber → Red.<br>170.3 **Missing-data policy (F5, MED).** `None` is treated as passing for subjective (`:1152`) and HRV/readiness (`:1322-1331`) while missing RHR fails safe, so the null move (feel awful, log nothing) keeps a Green honesty would lose (no-HRV → Green; blank subjective → Green; honest subjective 3 → Amber). Require *positive* HRV evidence to convert a soft night to Green (absent HRV neutralises the override, like absent RHR), and treat a stale/absent subjective as neutral, not positive.<br>170.4 **Combined tests + DECISIONS.** Because all three reshape the same ladder, test their interactions as one matrix (credited-night-not-Green, stacking→Red, null-move-no-longer-Green) plus the existing audit Probes; DECISIONS entry; prove every change only *hardens* the light and the acute gates + #133/#135 data-quality rules hold. | Make the deterministic ladder resistant to the slow/second-order moves — a credited night crossing the Green line, a pile-up of moderate-bad signals never reaching Red, and the "log nothing" null move — closing audit findings F3/F4/F5. | **Shipped via PR #200 / squash `6ad70a0` (Decision #251):** a raw-below-74 night credited across the Green threshold now requires the complete measured #133 recovery bundle plus a current subjective score ≥5; Poor readiness plus soft sleep, current subjective <5, a hard prior day, or proved-elevated RHR escalates to Red; and absent HRV/readiness/check-in or an absent RHR baseline stays neutral and cannot unlock the discretionary Green exception or Low-readiness relaxation. Age credit still lifts within Red/Amber; acute Red/Amber, #133/#135, the Batch 167 load cap, and Red-never-VO2 remain intact. Prompt v20 exposes structured `sleepCreditCeiling` / `cumulativeEscalation` evidence without controlling the deterministic result, and the combined matrix proves every changed outcome is equally or more cautious. Backend + docs only; no migration, shared-schema, plan, delivery, auth, or hosting change. The PR check rollup and implementation-merge `main` CI passed all seven jobs; Vercel preview/production reached Ready; exact-SHA Railway + Vercel health, web `/`, and protected daily-loop smokes passed. Closes **F3/F4/F5** (`COACHING_INTEGRITY_AUDIT.md`). |
| ~~Batch 171 — Chronic overreaching drives an action, not just advice (F6)~~ | 🟢 Mid | Shipped | 171.1 **Wire sustained chronic decline into a real action.** `chronic_patterns` watches HRV/readiness/RHR/sleep over 28 days but is explicitly read-only ("no verdict or delivery-rule change", `chronic_patterns.py:1-6`) and only *suggests* ("do not chase load", `:713-716`); `block_progression` already computes a ≥2-Red-morning signal. Turn a sustained recovery-marker decline (or that ≥2-Red signal) into an actual action instead of a dismissible card. **Decide at `/batch-start`:** an automatic **deload proposal** via the existing restructure/delivery rail (lighter, no verdict change → stays 🟢 Mid) vs a **temporary daily-verdict cap** (touches the verdict core → bump to 🔴 High and apply the only-harden/deterministic rule).<br>171.2 **Guardrails.** Don't let a motivated user dismiss the signal indefinitely; avoid false deloads (require genuine sustained decline, not a single bad day); reuse the existing propose→approve→push contract if using the deload path.<br>171.3 **Tests** — a sustained decline (or ≥2-Red) triggers the chosen action; a single bad day does not; the existing chronic advisory copy still renders. | Let the app *act* on a sustained overreaching signature it can already see — an auto deload proposal or a temporary verdict cap — instead of only showing an advisory card a motivated athlete can ignore, closing audit finding F6. | **Shipped via PR #201 / squash `d6cd890` (Decision #252):** an already-qualified personal recovery marker missed on ≥70% of observed nights, or at least two Red mornings in seven days, emits a deterministic `verdict.chronicAction` and persistent approval-gated deload proposals for active indoor-bike workouts through day six. The established transform cuts duration 25%, drops one zone, and removes HIT/VO2; sparse/isolated evidence is neutral, completed sessions are excluded, acute/acted adjustments win, and stable audit tags make retries idempotent. The daily verdict remains unchanged (`verdictImpact="none"`), prompt v21 is explanatory only, and the existing chronic advisory packet/copy is regression-protected. Backend + docs only; no migration, shared schema, new endpoint, frontend, provider, auth, or hosting change. Branch-push, PR-context, and implementation-merge `main` CI passed all seven jobs; Vercel preview/production reached Ready; exact-SHA Railway + Vercel health, web `/`, and protected daily-loop smokes passed. Closes 🟠 **F6** (`COACHING_INTEGRITY_AUDIT.md`). |

## Post-roadmap — 2026-07-27 — Installed home-screen PWA can't activate (Batch 160 iOS regression)

Reported 2026-07-27: after a successful Safari activation, the iOS **Add to Home Screen** app still shows "Set up this device". Batch 160 removed the in-app login path, and iOS isolates a standalone home-screen app's storage from Safari, so the device token minted through the Safari activation link never reaches the installed app — and `AccessPage` offers no way to authenticate from inside the app. See DECISIONS #242.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 172 — Installed-PWA activation entry (Batch 160 iOS home-screen regression)~~ | 🟢 Mid | Shipped | 172.1 **In-app activation entry (the iOS fix).** `AccessPage` (`apps/web/src/pages/AccessPage.tsx`) shows only static "ask Craig for a link" copy, so the installed standalone PWA can't authenticate — iOS isolates its `localStorage` from the Safari tab the link opened in. Add a text field that accepts a full `…/activate?code=XXX` URL **or** a bare code, extracts the code, and calls the existing `activateDevice(code)` from `useAuth()` (the same path `ActivatePage` uses), storing the device token in the standalone app's own storage; on success navigate to `/`.<br>172.2 **Single-use reconciliation + setup copy.** Activation codes are single-use and opening the Safari link consumes them, so instruct iOS users to add to Home Screen, open the app, then paste the link/code (not tap it in Safari first). Keep the `/activate?code=` link flow unchanged for Safari/desktop. Show a retryable inline error on an invalid/expired/used code (reuse `ActivatePage`'s message style).<br>172.3 **Tests** — `AccessPage` renders the input; a bare code and a pasted full `/activate?code=` URL both extract the same code; submitting a valid code calls `activateDevice` and routes to the dashboard; an invalid code surfaces the error and stays on the setup screen; the existing link-activation path is untouched. | Restore "add to home screen" setup on iOS: let the installed standalone app authenticate itself with a pasted activation code/link, closing the Batch 160 regression that left the home-screen app with no sign-in path. | The device-setup screen exposes an activation field that accepts a bare code or a full `/activate?code=` URL; entering a valid code activates the device in the installed app's own storage and lands on the dashboard; an invalid/expired/used code shows a retryable error without leaving the screen; the Safari/desktop link flow still works; web unit tests cover input render, URL + bare-code extraction, success routing, and failure. **Frontend-only, no migration, no backend change.** New regression from Batch 160 (DS154-01), found 2026-07-27. Decision #242 assigned at `/batch-start`. |

## Post-roadmap — 2026-07-29 — Mark feedback 2026-07-25/28/29: adjustment coherence + packet honesty

Feedback wave from Mark across 2026-07-25, 07-28, and 07-29 (screenshots in `~/Downloads/Dad Fitness/`), reconciled against the code and confirmed against prod on 2026-07-29. Five findings, all overtraining-guardrail-adjacent, grouped into three batches. **Batch 173 (coherence)** closes the two linked adjustment defects: the morning Amber/Red adjustment is generated too late to accept — the check-in path gates it behind `ride_still_pending` while plan rides are pre-delivered to Zwift days ahead, so only the ungated 11:00 backstop produces it (prod-confirmed: the 07-29 `amber_regeneration` proposal was created at 11:00, ~85 min after the 09:35 verdict) — and four code paths scale an Amber ride to four different intensities for the *same* session (KB rule keeps 67%, narrative keeps 67%, editor "Scale down" preset gives 60%, delivery transform gives 54%). **Batch 174 (packet honesty)** fills two gaps where the packet omits real data and the narrative fills it with a template or a partial signal: the post-workout read carries only today's planned row so it parrots the prose `weeklyRhythm` for "tomorrow", and the morning read's "yesterday" is exercise-only so a high-cost non-exercise day is invisible. **Batch 175 (chat policy)** relaxes the brief-chat fence so the coach answers general training-science questions instead of refusing. Where a change touches the deterministic Green/Amber/Red ladder (174.2's optional verdict-hardening), the verdict-engine safety rule applies: only ever *harden* the light, keep it deterministic. Decision numbers assigned at `/batch-start`; rows stay `Planned`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 173 — One coherent morning adjustment: generate it at check-in, scale it one way~~ | 🔴 High | Shipped | 173.1 **Offer the Amber/Red adjustment at check-in, not only at the 11:00 backstop.** `regenerate_after_morning_checkin` calls `regenerate_for_verdict` (`executable_coaching.py:513-525`) but gates it behind `ride_still_pending` — False whenever the deliverable bike ride already has an approved/pushed proposal. Plan rides are pushed to Zwift days ahead (07-29's ride pushed since 07-08), so the check-in almost always sees an already-pushed ride and skips the adjustment; the ungated 11:00 backstop (`run_morning_weather_sync` → `regenerate_for_verdict`, `scheduler.py:459`) then creates it ~85 min later (prod-confirmed: proposal `bf964ccc`, `amber_regeneration`, 10:00 UTC vs the 08:35 UTC verdict). Distinguish a ride **pre-delivered by plan import/schedule** from one **Mark deliberately approved or edited today** (e.g. by proposal `origin`/approver), so a pre-delivered ride still gets a *proposed* (never auto-pushed) morning adjustment surfaced front-and-centre, while Decision #29's real intent — never silently overwrite a ride Mark acted on *today* — is preserved. Align the backstop and check-in paths to the same rule (the backstop currently ignores the gate). **Decide at `/batch-start`:** the exact "acted-on-today" predicate, and whether the backstop keeps proposing on already-pushed rides.<br>173.2 **One verdict-scaling rule, and stop dropping a Zone-2 ride below Zone 2.** Four paths scale an Amber ride independently: the KB rule "cut duration 20-30% and remove HIT" (`coaching_state.py:186`), the `_eased_ride_detail` narrative guidance "drop a zone" (`morning_analysis.py:1412-1414`), the delivery transform `adjust_ir_for_verdict` (×0.75 duration, cap 98%, −13pt zone-drop → 54% on a 67% ride; `executable_coaching.py:83-88,209-214`), and the manual editor "Scale down" preset `scale_block` (×0.75 duration, ×0.9 power → 60%; `interval_workout_editor.py:137-151`). Unify on one deterministic scaling function that the editor preset, the delivery transform, and the narrative guidance all describe identically. For an already-Zone-2 endurance ride, cut duration only — do **not** drop intensity below the Zone-2 floor (the KB rule and the narrative are right; "drop a zone" + ×0.9 are wrong for Z2, which is why Mark hand-reset the editor's 60% back to 67% on 07-29). **Decide at `/batch-start`:** the canonical constants and how "already at/below Z2 → cut duration only" is detected.<br>173.3 **Surface the deterministic adjustment numbers in the packet.** Brief-chat couldn't explain the 60% because no packet held it ("the packet doesn't give me visibility into how the app calculated its 60% suggestion"). Emit the verdict-adjustment (duration/intensity deltas + resulting targets) into the morning analysis packet so the narrative and the chat (Batch 175) quote the same figures instead of guessing. Explanatory only — never sets the light or the numbers.<br>173.4 **Tests + DECISIONS.** A check-in Amber/Red morning on a pre-delivered, un-acted-on ride produces a *proposed* adjustment (front-and-centre `delivery.changed`) at check-in, not only at the backstop; a ride Mark approved/edited today is still not silently re-adjusted; the editor preset, delivery transform, and narrative guidance produce identical numbers for the same ride; a Zone-2 ride is duration-cut only; the packet carries the adjustment figures; every path stays deterministic. | Make the morning Amber/Red adjustment appear when the verdict does — and describe it with one set of numbers everywhere (home card, editor, narrative, chat) — instead of a late backstop proposal that disagrees with three other scaling rules. | On an Amber/Red check-in with a pre-delivered ride still un-acted-on today, a *proposed* (never auto-pushed) coach adjustment is created at check-in and flips the Home card into its approve-and-upload state; a ride Mark approved or edited today is never silently re-adjusted (Decision #29 intent preserved); the check-in and 11:00-backstop paths apply the same re-adjust rule. The editor "Scale down" preset, the `adjust_ir_for_verdict` transform, and the `_eased_ride_detail` guidance return identical duration/intensity for a given ride from one shared scaling function; an already-Zone-2 endurance ride is cut in duration only, never pushed below the Zone-2 floor. The morning packet carries the structured adjustment (deltas + resulting targets), explanatory-only and unable to set the verdict or numbers. Deterministic tests cover check-in-vs-backstop timing, the acted-on-today guard, cross-path number equality, the Z2-duration-only rule, and packet exposure. Backend-only; no migration or shared-schema change; delivery stays propose→approve→push (Decision #29). Prod-confirmed root cause 2026-07-29 (proposal `bf964ccc` at 11:00 vs verdict at 09:35). **Shipped via PR #202 / squash `04efcb3` (Decision #253):** built as one batch (not split). New leaf `services/verdict_scaling.py` shares `ease_amber_power_pct`/`ENDURANCE_CEILING_PCT=75` across the delivery transform, editor "Scale down" preset, and narrative (an already-Zone-2 ride is duration-cut only, held at 67%; harder intervals drop a zone floored at the ceiling, capped at threshold); the `_deliberately_acted` guard moved into `regenerate_for_verdict`, so a ride pre-delivered by push-on-plan-set gets a *proposed* (never auto-pushed) adjustment at check-in while a ride Mark approved/edited today is untouched, and the check-in and 11:00 backstop share the rule; `verdict.verdictAdjustment` surfaces the deterministic figures (explanatory-only, prompt v22). Backend + docs only; no migration/shared-schema/frontend change. Branch-push + PR-context CI passed all seven jobs; production verified on merge SHA `04efcb3` (Railway direct + Vercel same-origin health, web `/` 200, daily-loop 401 direct/proxied). |
| ~~Batch 174 — Analysis packets carry the real schedule + non-exercise load, not templates~~ | 🔴 High | Shipped | 174.1 **Post-workout read sees the real upcoming sessions, not the `weeklyRhythm` template.** `post_workout_analysis._planned_workouts` filters `workout_date == subject_date` (`post_workout_analysis.py:656-676`), so the packet's `plannedWorkouts` holds only the completed day; the prompt still carries `include_tomorrow_impact` (`:447`) and the prose `weeklyRhythm` (`coaching_state.py:172-178`) — so the model parrots the template for "tomorrow" (07-25: said "Sunday is light strength or mobility" while the plan had Sunday = Long Z2 120 min, and when challenged could only guess a "swap"). Fetch the next 1–2 days' active planned workouts and add them to the packet as `upcomingWorkouts`; update the output rule so the model treats actual upcoming sessions as ground truth and names them (e.g. "tomorrow's Long Z2, 120 min"), using `weeklyRhythm` only as fallback when no real row exists.<br>174.2 **Morning "yesterday" includes non-exercise load, not just training load.** `_yesterday_load` queries only `Activity` rows (`morning_analysis._yesterday_load` + `_yesterday_load_packet`) → `totalTrainingLoad`/`hardestActivity` only; `_training_load_signal` (`:1014`) feeds the verdict only ACWR + recovery-time; yesterday's `DailyMetric` (`stress_avg`, `body_battery_drained`, `body_battery_end`) is never loaded. So the narrative reduced "yesterday" to "training load 5.2" and a high-cost non-exercise day is invisible (Mark's 07-28 concern). Load yesterday's `DailyMetric` and add all-day stress + Body-Battery drain to the yesterday packet; update the prompt so the read stops equating "yesterday" with training load alone. **Decide at `/batch-start`:** narrative-only (surfaced context) vs also *hardening* the deterministic verdict (very high all-day stress / deep Body-Battery drain adds caution). If it touches the ladder it must **only ever harden** the light and stay deterministic (verdict-engine safety rule); HRV/Body-Battery already partially reflect non-exercise stress, so narrative-only is the conservative default.<br>174.3 **Tests + DECISIONS.** Post-workout: a packet whose next day is a planned Long Z2 120 min carries that row in `upcomingWorkouts` and the prompt ranks it above `weeklyRhythm` (deterministic packet test). Morning: yesterday's stress/Body-Battery-drain is loaded and present even on a zero-exercise day; if verdict-hardening is chosen, a deterministic test proves it only ever hardens. | Stop the reads inventing "tomorrow" from a prose template and reducing "yesterday" to exercise load — feed them the actual upcoming sessions and yesterday's whole-day cost. | The post-workout packet includes the next 1–2 days' active planned workouts as `upcomingWorkouts` and the prompt names the real upcoming session over the `weeklyRhythm` template (falling back to the template only when no row exists); a regression proves tomorrow's real row is present and preferred. The morning packet loads yesterday's `DailyMetric` and carries all-day stress + Body-Battery drain in the "yesterday" context; the prompt no longer equates "yesterday" with training load alone; a high-stress zero-exercise yesterday is represented. If 174.2 hardens the verdict, deterministic tests prove it only ever hardens and stays computed in Python. 174.1 is additive context (independently shippable, ~🟢 Mid); 174.2 carries the verdict decision. Backend-only; no migration or shared-schema change. **Shipped via PR #203 / squash `b051b7c` (Decision #254):** all active planned rows on subject day +1/+2 now populate `upcomingWorkouts`, and prompt v12 makes them authoritative over the prose rhythm; morning prompt v23 receives yesterday's `wholeDayCost` even with zero exercise, while `statusScope="exercise_only"` and `classificationImpact="none"` preserve the narrative-only boundary. The deterministic verdict/load inputs are unchanged. After fixture-order repair `0260bfc`, branch, PR-context, and implementation-merge `main` CI passed all seven jobs; production verified exact-SHA Railway + Vercel health, DB readiness, web `/` 200, and protected daily-loop 401 direct/proxied. |
| ~~Batch 175 — Brief-chat answers general training-science, not just the packet~~ | 🟢 Mid | Shipped | 175.1 **Permit general, labelled sport-science answers.** `brief_chat` fences the model to "Answer only from the packet and the read… If the packet does not hold what is needed to answer, say so plainly" (`brief_chat.py:61-63`), which made it refuse Mark's answerable "what %FTP could I drop the VO₂ intervals to and keep similar benefit?" (07-28). Relax the fence for **general, non-personalized endurance-science** (minimum-effective VO₂ intensity, intensity/duration trade-offs, why a zone matters): let the model answer from established exercise physiology, **clearly labelled as general principle**. Keep the packet-only rule for anything about **Mark's own metrics/plan/history** — those may not be invented.<br>175.2 **Keep plan changes in propose/confirm; quote the app's own numbers.** Any actual prescription change still routes through the existing propose/confirm affordance (it already does — preserve it). When Batch 173.3 surfaces the deterministic adjustment in the packet, the chat quotes those figures when asked "why 60%/54%?" instead of punting. State the boundary explicitly: general science = allowed + labelled; personal prescription = packet-only or propose/confirm.<br>175.3 **Guardrails + tests.** The relaxation must not invent Mark-specific numbers or override the deterministic verdict/floors — the existing floors stay (`brief_chat.py:66`: never recommend VO2 on a Red day, etc.). Tests: a general min-effective-%FTP question returns a substantive, labelled answer (not a refusal); a question about Mark's specific readiness answers only from the packet; a plan-change request yields propose/confirm, not a direct change; the Red-day-VO2 floor holds. **Decided at `/batch-start`:** use a broad established-endurance-science lane rather than a curated whitelist, with exact label `General principle:`. | Let the coach answer legitimate training-science questions ("how far can I dial this back and keep the benefit?") from general physiology, clearly labelled, instead of refusing because it isn't in the packet. | **Shipped via PR #204 / squash `4aeb5c1` (Decision #255):** brief-chat prompt v4 permits general non-personalized endurance-science answers labelled `General principle:`, while Mark-specific metrics/plan/history/readiness/prescription stay packet-bound and actual workout changes remain confirm-before-apply/propose-confirm. Red-never-VO2 and all deterministic floors are unchanged. Added DB-backed guardrail tests for general science, packet-only personal answers, propose/confirm, and Red-day VO2, plus a pure prompt-policy test for local no-Postgres verification. Backend prompt-policy + tests/docs only; no migration, backend-service rewrite, frontend, shared-schema, provider, auth, hosting, verdict, or plan/delivery change. First PR pytest found newline-sensitive assertions in new tests; amended branch commit `9750d22` fixed them. Branch, PR-context, and post-merge `main` CI passed all seven jobs. Production verified exact-SHA Railway + Vercel health on `4aeb5c1`, web `/` 200, and protected brief-chat POST 401 direct/proxied. |

## Post-roadmap — 2026-07-29 — Mark feedback: live body metrics (weight → W/kg) + dynamic VO2 max

Separate feedback thread from Mark on 2026-07-29 (a question thread over a ride read, distinct from the 07-25/28/29 adjustment-coherence wave above), reconciled against the code and confirmed that the underlying Garmin data is already synced. Two findings with one root cause: **the live Garmin body metrics the coach needs are captured on the daily `DailyMetric` row every sync, but the coaching packets either omit them or are shadowed by a stale static copy.** Weight is fetched (`garmin_sync.py:203`) but kept only on an exact same-day weigh-in (`_extract_weight_kg`, `:758-769`) — so it is null on most days and reaches only the morning packet, never the post-workout/chat packet where power-to-weight matters — and **watts-per-kg is computed nowhere** (API + web greps, 0 hits), which is why the read admitted it could not give a W/kg figure. VO2 max is synced live (`get_max_metrics`, `:204`; currently 55) and already feeds the age-norm (`morning_analysis.py:1328`), yet the `profile` KB hardcodes `vo2max: 54` (`coaching_state.py:31`) and that stale copy is injected as `athleteProfile` into every read (`post_workout_analysis.py:426`), so the coach narrates a fixed 54. **Batch 176** carries weight forward and computes W/kg into the ride read (and, via `brief_chat.py:300`, the chat over it); **Batch 177** overlays the live daily VO2 onto the packet so real changes flow into the coaching. Both share one mechanism — resolve an *effective* body metric at packet-build (live daily value, profile as fallback, carry an as-of date) and overlay it onto the packet — and could factor a shared helper; because KB seeding fills only missing sections (`coaching_state.py:562`), neither can be fixed by editing the seed, so both live in the packet-build layer. Both are explanatory-only: nothing here touches the deterministic Green/Amber/Red ladder (verdict-engine safety rule). Decision numbers assigned at `/batch-start`; rows stay `Planned`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 176 — Bodyweight from Garmin → watts-per-kg in the ride read (and chat)~~ | 🟢 Mid | Shipped | 176.1 **Carry the most-recent weigh-in forward.** `_extract_weight_kg` (`garmin_sync.py:758-769`) returns a value only when a weigh-in is dated exactly the calendar day, with no carry-forward, so `weight_kg` (`coaching.py:58`) is null on every day Mark does not step on the scale (most days). `get_weigh_ins` already fetches a 7-day range (`garmin_sync.py:203`) — resolve the most-recent weigh-in within the window and carry it forward, retaining its as-of date so a stale weight is never shown as today's. **Decide at `/batch-start`:** lookback window length; whether to persist effective weight + as-of date on the daily row or resolve at packet-build.<br>176.2 **Weight + W/kg into the post-workout packet.** The packet (`post_workout_analysis.py:422-433`) carries `ftpWatts` (280, via `_ftp_watts` `:922-928`) but no bodyweight, and W/kg is computed nowhere (API + web greps, 0 hits) — the reason the read said it could not give a W/kg figure. Add `weightKg` (effective + as-of date) and a computed `powerToWeight` block: FTP W/kg (ftp ÷ weight) as the headline, plus session W/kg from executed power (avg / normalized / per-work-interval, reusing the existing power machinery `:997,1013`). Prompt update so the read leads with W/kg as the more meaningful comparison. Brief-chat runs over `analysis.context_packet` (`brief_chat.py:300`), so the chat over the read inherits the figure. **Decide at `/batch-start`:** which session W/kg flavours to surface (FTP-only vs also NP/interval); label format.<br>176.3 **Degrade honestly with no weight on file.** If no weigh-in exists in the window, never divide by a fabricated weight — omit `powerToWeight` and carry an explicit "no recent weight" flag so the read says weight is not on file rather than inventing one. Optional scope: surface FTP W/kg in the morning packet too (it already carries `weightKg` `morning_analysis.py:1044` + FTP) and add a W/kg trend series alongside the existing weight/VO2 trends (`trends.py:116`). **Decide at `/batch-start`:** ride-only vs morning + trends.<br>176.4 **Tests + DECISIONS.** Carry-forward returns the most-recent weigh-in within the window and null beyond it; the ride packet carries `weightKg` + `powerToWeight` and the read quotes FTP + session W/kg; a no-weigh-in-in-window case omits `powerToWeight` and the read states weight is not on file (no fabricated value, no divide-by-zero); brief-chat over the read quotes the same figure. Explanatory-only — W/kg never touches the Green/Amber/Red ladder. | Give the coach Mark's bodyweight — already synced, just not carried forward or put in the ride packet — and compute watts-per-kg so the post-workout read and chat use the standard power-to-weight comparison instead of admitting they cannot. | The importer carries the most-recent weigh-in within the lookback window forward with an as-of date (never presents a stale weight as today's); the post-workout packet carries `weightKg` + a computed `powerToWeight` (FTP W/kg headline + session W/kg from executed power) and the read leads with W/kg as the more meaningful comparison; brief-chat over the read quotes the same numbers; with no weigh-in in the window the packet omits `powerToWeight` and the read honestly says weight is not on file (no fabricated weight, no divide-by-zero). W/kg is explanatory-only and cannot move the verdict or any deterministic floor; morning/trends scope decided at `/batch-start`. Backend-only; additive packet fields (+ optional daily-row column decided at start), no shared-schema break. Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-29 question-thread feedback. |
| ~~Batch 177 — VO2 max read live from Garmin, not the static profile 54~~ | 🟢 Mid | Shipped | 177.1 **Overlay the live daily VO2 onto the packet profile.** The `profile` KB hardcodes `"vo2max": 54` (`coaching_state.py:31`) and it reaches the model as `athleteProfile` in the post-workout packet (`post_workout_analysis.py:426`) and the morning read, so the coach narrates a fixed 54 while Garmin's live value is 55. The live value is synced daily (`get_max_metrics` `garmin_sync.py:204` → `_extract_vo2max` `:772-782` → `vo2max` `coaching.py:59`) and already drives the age-norm (`morning_analysis.py:1328`). Seeding fills only missing sections (`coaching_state.py:562-567`), so editing the seed will not update Mark's existing prod `profile` row — resolve an effective VO2 at packet-build (live daily preferred, profile as fallback) and overlay it onto `athleteProfile` in both packets. **Decide at `/batch-start`:** overlay-only vs also writing the live value through to the `profile` KB row; whether to drop the static number from `_profile_content()` for future users.<br>177.2 **Let genuine VO2 movement inform the narrative.** With the live value in every packet the read may note a real VO2 trend (the `trends.py:116` series already exists), up or down, as explanatory context — as Mark asked. Explanatory-only: VO2 must never move the deterministic Green/Amber/Red ladder (verdict-engine safety rule). Prompt update so the read uses the effective (live) figure and references a change only when it is real.<br>177.3 **Tests + DECISIONS.** The effective `vo2max` in the morning and post-workout packets equals the live daily value when present (55, not 54) and falls back to the profile only when the daily value is missing; the static seed number is reconciled (dropped or documented as fallback-only); the verdict ladder is unchanged; a deterministic test proves the overlay prefers live. | Stop the coach reading a stale hardcoded VO2 max (54) when Garmin already syncs the live value (55) daily — source it live at packet-build so real changes flow into the coaching, as Mark asked. | The morning and post-workout packets carry an effective `vo2max` equal to the live daily value when present (not the static 54), falling back to the profile number only when no daily value exists; the read references the live figure and may note a genuine VO2 trend as explanatory context; the deterministic Green/Amber/Red ladder and all floors are unchanged (verdict-engine safety rule); the static `vo2max` in `_profile_content()` is reconciled (dropped for future seeds or documented fallback-only), accounting for the missing-only seeding behaviour (`coaching_state.py:562`). Backend-only; no migration (packet overlay; optional profile write-through decided at `/batch-start`). Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-29 question-thread feedback. |

## Post-roadmap — 2026-07-30 — Mark feedback: the chat and the app feel "almost disconnected"

Mark reported on 2026-07-30 that the LLM conversations and the app itself feel *almost disconnected*, citing a reply that referred to something not being in the **packet**. Reconciled against the code: the report is accurate and **structural, not a wording defect**. Brief-chat's entire world is one stored `context_packet` plus the read's own markdown (`brief_chat.py:297-302`) in a single Anthropic call with no retrieval — `generate_anthropic_text` (`anthropic_text.py:150-159`) takes only system/user/prior-messages and has **no tool-use support**. Three consequences: (1) the chat system prompt uses the internal noun "packet" eight times and `:61-63` instructs the model to say plainly when the packet lacks something, so our schema vocabulary reaches Mark verbatim — the exact leak he saw; (2) everything else the app computes is invisible to a conversation — Trends, Reviews, Week Ahead, Sleep, Environment are separate routers with their own packet types (`deep_review`, `seasonal_trend`), so a question whose answer is rendered one tab away gets an honest refusal; (3) the packet is frozen at generation, so anything after the brief (a completed ride, an edited plan) cannot be discussed on that read. Underneath sits a schema assertion that a conversation cannot exist without a document: `BriefMessage.analysis_id` is `NOT NULL` with an FK (`coaching.py:565`), and the Sleep page has no `Analysis` of its own — it borrows `morningAnalysis` (`SleepPage.tsx:123`), as do the breathwork/strength/walking briefs, which are not `analyses` rows at all. So "chat after the sleep brief" has nothing to attach to today. **Batch 178** feeds the conversation live app state and stops it narrating its own plumbing; **Batch 179** unifies the per-read chats into one coach reachable from anywhere. **Sequencing is load-bearing: 178 must land before 179** — a general chat surface built on today's empty context would refuse *more* than the current per-read chat, shipping Mark's complaint at a larger scale. Note also that Batch 175 (Decision #255) already patched one instance of this by relaxing the fence for general sport science; the residual is structural, and the fix is **feeding the model real data, not loosening the fence further** — the packet-bound rule is what stops the LLM inventing Mark's metrics or talking around the deterministic verdict, and that stays. Both batches are explanatory/surface work: nothing here touches the deterministic Green/Amber/Red ladder (verdict-engine safety rule), and any plan change stays on the existing propose/confirm rail (Decision #29). Decision numbers assigned at `/batch-start`; rows stay `Planned`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 178 — Chat answers from live app state, and stops narrating its own plumbing~~ | 🔴 High | Shipped | 178.1 **Stop leaking the internal vocabulary.** `brief_chat.SYSTEM_PROMPT` (`brief_chat.py:56-85`) says "packet" eight times and `:61-63` tells the model to say so plainly when the packet cannot answer — so Mark is told things are not "in the packet". Rewrite the prompt so the internal noun never reaches the user and the genuinely-unknown case reads as a coach's sentence ("I don't have your sleep history in front of me here") rather than a schema complaint. The honesty requirement is unchanged — only the register. Add a test asserting the plumbing vocabulary (`packet`, `context packet`, `JSON`) is absent from answers, including on the not-known path.<br>178.2 **Assemble chat context at ask-time, not read-time.** `ask()` freezes on `analysis.context_packet` stored at generation (`brief_chat.py:297-302`), so a ride completed after the morning brief is invisible to that brief's chat. Build the conversation's context when the question is asked. Reuse `analysis_currentness.py` (Batch 159) for the staleness rules rather than inventing a second notion of "current". **Decide at `/batch-start`:** rebuild the read packet wholesale vs keep the stored packet as the read's own record and layer a fresh delta over it.<br>178.3 **Widen the context to the rest of the app.** The deterministic builders already exist and are reusable as-is — `build_training_week_packet` (`training_week.py:206`) for the week ahead, `compute_trend_windows` plus `window_json` (`trends.py:289,471`) and `compute_year_on_year` (`:360`) for trends, `compute_review_rollup` (`reviews.py:355`) for the latest review conclusions, plus recent activities and sleep history. Compose a compact app-state block so the questions Mark actually asks ("has my HRV been trending down?") are answerable from data the app has already computed. **Decide at `/batch-start`:** deterministic pre-assembly (cheap, no client change) vs a real tool-use retrieval loop, which would require extending `generate_anthropic_text` (`anthropic_text.py:150-159`) with tool support — recommend pre-assembly for v1 and tool-use only if the block proves too coarse. Also decide the token budget and what gets truncated first.<br>178.4 **Tests + DECISIONS.** A trend question answers from the real series instead of refusing; a question about an activity completed after the read's generation is answered rather than being invisible; the not-known path produces a coach's sentence with no internal vocabulary; the Batch 175 general-science lane and its `General principle:` label still work; Mark-specific figures are still never invented; Red-never-VO2 and every deterministic floor hold; a plan-change request still yields propose/confirm and no direct mutation. | Make the conversation answer from what the app already knows — the week, trends, reviews, recent sessions, current state — instead of from one frozen document, and stop it telling Mark about "the packet". This is the substance behind his "almost disconnected", and it is worth doing on its own even if Batch 179 never ships. | Chat context is assembled at ask-time and includes app state beyond the single read (week ahead, trend series, latest review conclusions, recent activities, sleep history), reusing the existing deterministic builders rather than duplicating them; a question whose answer is already rendered elsewhere in the app is answered rather than refused; an event occurring after the read's generation is visible to that read's chat; no answer contains internal schema vocabulary on any path, including the not-known path; Mark-specific metrics/plan/history are still never invented and the Batch 175 labelled general-science lane is preserved; the deterministic Green/Amber/Red ladder, Red-never-VO2 and all floors are unchanged, and plan changes remain propose/confirm (Decision #29). Backend-only; no migration (context assembly, not storage). Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-30 feedback. **Shipped via PR #207 / squash `5d5abee`** (Decision #258): new `services/chat_context.py` assembles the conversation's context at ask-time — the stored packet stays the read's own record and an `appState` block is layered alongside it carrying a `sinceThisRead` delta, today's plan, the week ahead, the monthly trend series + year-on-year, the latest review conclusions, recent sessions and sleep history — reusing `TrainingWeekService.build_window` (new windowed entry point, explicit subject-date anchor) and a new public `TrendsService.windows`, with stored review rows rather than a recomputed rollup. Deterministic pre-assembly, not tool-use; 30k-character budget with every trim named in `omittedForLength`. Prompt → `brief-chat-v5-2026-07-30` keeps the internal nouns to the single sentence that forbids them. Branch-push, PR-context and post-merge `main` CI all passed seven jobs (1043 pytest against Postgres); one branch push failed pytest first on a same-date fixture needing distinct plan versions, fixed in `11b69c8`. **Production deploy needed the documented break-glass path:** a Railway platform incident (2026-07-30 16:32 UTC) paused Hobby-plan deploys and dropped the merge webhook, so `railway redeploy --from-source` shipped `main` HEAD once the incident cleared. Production verified on `5d5abee`: Railway direct + Vercel same-origin health both returned the SHA, `health/ready` db ok, web `/` 200, and protected brief-chat POST + daily-loop 401 direct and proxied. |
| ~~Batch 179 — One coach conversation, reachable from anywhere~~ | 🔴 High | Shipped | **Depends on Batch 178 — do not start before it ships.** 179.1 **Let a conversation exist without a document.** `BriefMessage.analysis_id` is `NOT NULL` with an FK to `analyses` (`coaching.py:565`), which is why chat can only hang off a read and why the Sleep page — which has no `Analysis` of its own, borrowing `morningAnalysis` (`SleepPage.tsx:123`) — cannot host one without inventing a sleep analysis row purely as a conversation anchor. Make the anchor optional. **Decide at `/batch-start`:** nullable `analysis_id` (smaller migration, keeps one table) vs a proper `conversations` plus `conversation_messages` pair (cleaner threading, larger change). Either way this is the batch's one migration.<br>179.2 **Do not silently break the learning loop.** `conversation_learning` inner-joins `BriefMessage` to `Analysis` in both its source query (`conversation_learning.py:433-434`) and its single-record lookup (`:717-718`). Making the anchor nullable without touching these means every unanchored message vanishes from Batch 151's learning pipeline with no error — the best conversations would stop being learning sources. Convert to outer joins and add a regression test asserting an unanchored user message is picked up as a learning candidate.<br>179.3 **Key the capability gate off today's state, not the document type.** `_analysis_allows_adjustment_proposal` (`brief_chat.py:182-186`) tests `analysis_type == "morning"` as a proxy for "there is a live adjustable workout"; the real question is already answered deterministically by `_todays_adjustable_workout_id` (`:160-179`). Re-key the gate to today's actual plan state so the propose affordance appears exactly when a live adjustable workout exists, from any entry point, and never on a retrospective one. The deterministic keyword check on Mark's own words stays, the model still triggers nothing, and the propose→approve→push gate (Decision #29) is untouched.<br>179.4 **One entry point, context seeded by origin.** Replace the three read-scoped mounts (`MorningBriefPage.tsx:108`, `DashboardPage.tsx:1856`, `WorkoutDetailSheet.tsx:231`) with a single coach surface reachable app-wide, including from Sleep and the breathwork/strength/walking briefs that have no analysis row. Opening it from a page seeds that context ("we're talking about last night's sleep") without fencing the conversation to it. Rethink `MAX_USER_TURNS_PER_ANALYSIS` and `MAX_HISTORY_TURNS_IN_PROMPT` (`brief_chat.py:50-51`) as per-thread rather than per-document, so a conversation is not lost when the read rolls over. **Decide at `/batch-start`:** thread model (one rolling thread vs threads with optional read anchors), history retention, and whether the existing per-read history is migrated or left addressable in place.<br>179.5 **Hoist the guardrails into one policy surface.** The verdict floors, anti-sycophancy rules and honesty rules currently live per-read and drift apart; with one conversation they should be one shared module, applied wherever the coach speaks. Tests + DECISIONS: the floors hold identically from every entry point.<br>179.6 **Tests + DECISIONS.** A conversation can start with no analysis anchor and persists across reads; unanchored messages reach the learning pipeline; the propose affordance appears for a live adjustable workout from any entry point and never on a retrospective read; chat works from Sleep with no analysis row invented; existing per-read history remains readable. | Turn N amnesiac per-document chats into one coach Mark can talk to from anywhere, because the discontinuity is the cause of "almost disconnected" — today the schema asserts a conversation cannot exist without a document, which is why there is no way to just ask the coach something. | The chat is reachable app-wide including from surfaces with no `Analysis` row (Sleep, breathwork/strength/walking briefs), with context seeded by origin but not fenced to it; a conversation can exist with no analysis anchor and survives a read rolling over; unanchored messages are still learning sources (outer join, regression-tested) so Batch 151 is not silently degraded; the propose affordance is gated on today's real plan state rather than `analysis_type`, appears from any entry point when a live adjustable workout exists, never on a retrospective read, and Decision #29's propose→approve→push gate is unchanged; verdict floors and anti-sycophancy rules live in one shared policy surface and hold identically from every entry point; existing per-read history stays readable. One migration (anchor optionality, shape decided at `/batch-start`); frontend surface change; no provider/auth/hosting change and no verdict change. Backend pytest/ruff/mypy plus web test/build green. Surfaced by Mark's 2026-07-30 feedback. **Shipped via PR #208 / squash `94f10b8`** (Decision #259). **Kickoff decisions:** *179.1* one rolling thread with a nullable anchor rather than a `conversations`/`conversation_messages` pair — the rows already were the thread, so migration `026` drops the `NOT NULL`, adds `origin_kind`/`origin_date` for the surfaces with no read plus `ix_brief_messages_user_created`, and existing per-read history stays readable **in place**; RLS needed no work (019 already enabled it). *179.3* the acceptance criteria's ambiguous "never on a retrospective read" resolved as **purely today's live plan state** — a completed or skipped ride already yields no affordance, while blocking a genuine "make today easier" asked from yesterday's read would recreate the fencing this batch removes; `ChatContext.adjustable_workout_id` answers it from live rows (active, unclosed, structured, bike, not inside a holiday window), superseding Batch 178's subject-date liveness set, which was removed rather than left as a second wrong answer. *179.4* the three inline read chats **stay** and still list only their own read's turns; both surfaces render one extracted `CoachConversation` into the same thread, reached app-wide from `CoachLauncher` + `GET|POST /api/v1/coach/messages`, with a controlled origin vocabulary that degrades unknown values so client text can never enter the prompt. The cap moved from per-read (10) to per-local-day (20). *179.5* `services/coach_policy.py` is a **registry**, not a rewrite of five prompts: the conversation composes from it and the deterministic reads are audited against it (`missing_floors`), because the walk/strength/mobility reads legitimately state neither Red-never-VO2 nor the power-balance floor and making them recite inapplicable rules would bump every prompt version. Prompt → `coach-chat-v6-2026-07-31`. **179.2 was the silent-regression risk:** `conversation_learning` inner-joined `BriefMessage` to `Analysis` in both its source query and its evidence lookup, so a nullable anchor alone would have dropped every unanchored message out of Batch 151's pipeline with no error at all; both are now outer joins, regression-tested. Branch-push, PR-context and post-merge `main` CI all passed seven jobs (1059 pytest against Postgres); the first branch push failed pytest alone on the same fixture class that failed Batch 178's first push — two plan rows on one `workout_date` are siblings needing ascending versions. Production verified on `94f10b8`: exact-SHA Railway + Vercel health, `health/ready` db ok, web `/` 200, and 401 direct/proxied on the new coach surface, the per-read surface and daily-loop. Migration verified live via read-only `railway run` — head `026`, anchor nullable, both origin columns and the new index present, all 64 existing messages preserved. |

## Post-roadmap — 2026-08-01 — Mark feedback 2026-07-30/08-01: the day the app reads is not the day Mark lived

Feedback wave from Mark on 2026-07-30 and 2026-08-01 (screenshots `WhatsApp Image 2026-07-30 at 09.33.25/26/27` in `~/Downloads/Dad Fitness/`), reconciled against the code and **confirmed against prod on 2026-08-01**. Mark reported the morning brief quoting *"the all-day stress average was a very low 13"* for a day his Garmin showed as **24**, and inferred we were reading his *sleep* stress. He is right about the symptom, and the fix is not the one his diagnosis implies: **we are not reading the wrong field, we are reading the right field too early and never again.** Garmin's stress and Body-Battery aggregates are computed over a local-midnight-to-now window (`stress.startTimestampLocal`/`endTimestampLocal`; canonical sample `~/garmin-spike/out/stress.json`), and the scheduler syncs only `_profile_today(profile)` (`scheduler.py:277,283,874`) — so the morning run freezes each `DailyMetric` at ~09:00 over a window that is ~85% sleep (2026-07-29: 8h04m asleep inside a 9h39m window), and nothing ever re-reads it. Prod proof from the stored `raw_payload`: every row written by `garmin_history_backfill.py:188` ends at midnight with plausible whole-day values (18 Jun — stress 29, Body Battery drained 75, ended 18), while **every live-synced row since ~25 June ends 08:00–10:24** and carries a waking snapshot (31 Jul — stress 12, drained 1, ended 92; Mark's completed day was **28 / 70 / 16**). Our "daytime stress" therefore tracks his sleep stress to within 0–3 points on almost every live day, which is precisely why his sleep-stress inference fitted the evidence. The blast radius runs well past the sentence he quoted: the same truncation corrupts `body_battery_drained`/`body_battery_end` (both narrated by Batch 174's `wholeDayCost`, `morning_analysis.py:1627-1647`) and the `daytime_stress_avg` chronic driver (`insights.py:822` → `chronic_patterns.py`, `rem_interventions.py`, `sleep_projection.py`, `experiment_evaluation.py`), and it introduces a **step discontinuity at 24/25 June** between correct backfilled history and truncated live rows, so any trend or chronic comparison spanning that boundary sees a fabricated late-June collapse in stress. The deterministic Green/Amber/Red ladder is **not** affected — `_morning_verdict` (`morning_analysis.py:1867`) consumes neither field — so Mark's "false green light" worry does not hold literally, though the narrative did draw false reassurance from it ("a genuinely low-cost day") and chronic detection genuinely is degraded. Three batches: **Batch 180** finishes the day and repairs the history; **Batch 181** stops the coach asserting a stale packet is *correct* when Mark contradicts it from his own device; **Batch 182** revisits what evidence justifies a week-long deload. **Batch 182 was blocked on a design answer from Mark; he answered on 2026-08-01 and it is now partially unblocked** — the other two are defects with an obvious correct answer, 182 is a judgement about how much evidence should buy a week-long intervention. Rather than name a threshold he asked the app to derive it (*"I would hope the app could effectively tell me from it's knowledge"*), so 404 days of his history were analysed against his reply; findings, caveats and reproduction SQL are in **`docs/reviews/deload-escalation-evidence.md`**, and the headline is that his recovery markers are *worst* after rest and that a Red driven by planned training debt is trivially separable from a Red driven by a systemic crash. 180 and 181 are independent and may ship in either order, though 180 first is preferred so 181's tests are not written against known-bad data. Nothing here touches the deterministic ladder (verdict-engine safety rule) and plan changes stay on the propose/confirm rail (Decision #29). Decision numbers assigned at `/batch-start`; rows stay `Planned`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 180 — Daily rows finish the day, not the morning~~ | 🔴 High | Shipped | 180.1 **Re-sync the previous day once it has closed.** `_sync_garmin_daily` fetches and stores only `_profile_today(profile)` (`scheduler.py:277-288`), so a `DailyMetric` is written once, mid-morning, and never revisited; `parse_daily_metric_fields` (`garmin_sync.py:426-483`) then reads whole-day aggregates — `stress_avg` from `avgStressLevel or averageStressLevel` (`:461`) and `body_battery_charged/drained/end` (`:462-469`) — out of a window that has barely started. Re-sync D-1 alongside D in the morning run so yesterday is re-read after it has actually finished. `sync_daily` already upserts on `(user_id, calendar_date)` (`garmin_sync.py:331-343`), so this corrects the row in place with no migration and no new endpoint. **Decide at `/batch-start`:** D-1 only vs a short rolling re-sync window (D-1..D-3, which also self-heals a missed run); whether the extra Garmin fetch rides inside the existing `_retry_sync` backoff and its 429 budget; and whether a same-day evening pass is worth it or D-1 alone is sufficient.<br>180.2 **Never present a truncated window as a finished day.** The payload states its own coverage (`startTimestampLocal`/`endTimestampLocal`), so partial-day is *detectable*, not a guess. Carry the window (or a derived completeness flag/as-of time) onto the daily row or resolve it at packet-build, and make `_yesterday_load_packet`'s `wholeDayCost` (`morning_analysis.py:1627-1647`, `allDayStressAvg` at `:1631`) either omit the figures or state them as incomplete when the window does not cover the day. This is the durable guard: even with 180.1 shipped, a missed or failed overnight sync must degrade to honest silence rather than to a confident wrong number. **Decide at `/batch-start`:** persisted column vs packet-build resolution; the completeness predicate (window end ≥ local midnight vs a coverage fraction); and whether the same guard is applied to the `daytime_stress_avg` chronic driver (`insights.py:822`) so a partial day is excluded from baselines rather than counted low.<br>180.3 **Repair the stored history.** Roughly five weeks of rows (~25 June → present, ~38 days) hold a waking snapshot; the earlier backfilled rows are correct, so the series currently contains a fabricated step change at the boundary. Re-run the existing backfill path (`garmin_history_backfill.py:188` → the same `sync_daily` upsert) over the affected range to overwrite them with completed-day values. **Decided 2026-08-01: the repair rides in this batch**, not staged separately — the fix and the history it invalidates land together, so Mark is never reading a corrected brief against uncorrected trends, and the fabricated late-June step is not left standing behind a working sync. **Decide at `/batch-start`:** the exact start date (verify the boundary from `endTimestampLocal` rather than assuming 25 June); and whether personal baselines / chronic flags need explicit recomputation afterwards or regenerate naturally.<br>180.4 **Tests + DECISIONS.** A payload whose window ends mid-morning is not presented as a whole day (packet omits or flags it); a re-sync of a past date overwrites the earlier snapshot in place via the existing upsert; a completed-day payload yields the whole-day figures; the 31 July regression case specifically (stored 12 / 1 / 92 → 28 / 70 / 16) is covered from a fixture; the deterministic verdict is unchanged either way. | Stop writing each day's Body Battery and stress from a window that is ~85% sleep and never re-reading it — finish the day, refuse to narrate an unfinished one as finished, and repair the five weeks already stored wrong. | The morning run re-syncs the previous day (window decided at `/batch-start`) so `DailyMetric` holds completed-day aggregates rather than a ~09:00 snapshot; the upsert corrects rows in place with no migration. A daily payload whose window does not cover the calendar day can be detected from `startTimestampLocal`/`endTimestampLocal`, and `wholeDayCost` omits or explicitly qualifies stress and Body-Battery figures in that case rather than stating them as the day's totals — so a missed overnight sync degrades to silence, never to a confident wrong number. The ~38 affected rows since the backfill/live boundary are repaired from Garmin, removing the step discontinuity that currently makes late June look like a sharp drop in stress; whether baselines/chronic flags are recomputed is decided at `/batch-start`. Deterministic tests cover the partial-window guard, in-place re-sync, the completed-day path, and the 31 July regression case. The Green/Amber/Red ladder is untouched (`_morning_verdict` consumes neither field). Backend-only; no shared-schema break; optional daily-row column decided at `/batch-start`. Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-07-30 and 2026-08-01 feedback; root cause prod-confirmed 2026-08-01. |
| ~~Batch 181 — The coach reports what it was given, and never insists it is true~~ | 🔴 High | Shipped | 181.1 **Distinguish "what I was handed" from "what is true".** Mark challenged the figures with the correct ones off his own watch and, in his words, *"its answer said this was correct data from the packet it got."* The chat is packet-bound by design (Batch 175 / Decision #255) and had no way to see Garmin — that fence is right and stays — but being unable to verify a number is not grounds for asserting it is *correct*. Add the rule to the shared policy registry `services/coach_policy.py` (`FLOORS`, `:80-111`; Batch 179 / Decision #259) so it composes into the coach conversation and is audited into the deterministic reads: figures are reported as what the app recorded, never asserted as ground truth about Mark's body.<br>181.2 **When Mark contradicts a figure from his own device, his reading wins.** The correct response to "you said 12, it was actually 28" is to accept the discrepancy, say plainly that the app's stored figure disagrees with his device and that his device is the better evidence, and treat it as a data problem to surface — not to defend the packet. This must not become blanket capitulation: the coach still holds its ground on *coaching judgement* (the Red-never-VO2 floor, the deterministic verdict, propose/confirm), and the existing anti-sycophancy rules stay. The boundary is **observed data vs coaching decisions** — Mark is the authority on the former, the deterministic engine on the latter. **Decide at `/batch-start`:** whether a contradiction is merely acknowledged in-conversation, or also recorded (Batch 151/179 conversational learning already captures these turns — a user correcting a metric is arguably the highest-signal learning event we get) and surfaced to admin as a data-quality flag.<br>181.3 **Tests + DECISIONS.** A packet figure the user disputes produces acknowledgement, not insistence, and no claim that the stored value is correct; the coach still refuses to move a Red-day VO2 session or the deterministic verdict when pushed (anti-sycophancy intact); the Batch 175 `General principle:` lane and Batch 178's no-internal-vocabulary rule still hold; Mark-specific figures are still never invented. | Stop the coach telling Mark he is wrong about his own data when he is right — report figures as what the app recorded rather than as fact, and when his device contradicts them, treat his reading as the better evidence. | The coach never asserts that a stored figure is *correct*, only that it is what the app recorded, and says so in a coach's register with no internal vocabulary (Batch 178 rule preserved); when the user contradicts a figure from their own device, the answer acknowledges the discrepancy and defers to their reading rather than defending the stored value. The distinction between **observed data** (user is the authority) and **coaching judgement** (deterministic engine is the authority) is explicit, so the change cannot be read as licence to capitulate on the verdict, the floors, or propose/confirm — regression tests prove Red-never-VO2 and the deterministic verdict still hold under pressure. The rule lives in `coach_policy.py` so it applies wherever the coach speaks rather than in one prompt. Whether a disputed figure is also recorded as a data-quality signal is decided at `/batch-start`. Backend prompt-policy + tests/docs only; no migration, frontend, provider, auth, hosting, or verdict change. Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-08-01 feedback; worth shipping independently of Batch 180 because it protects against every future data defect, not this one. **Shipped 2026-08-03 via PR #210 / squash `65f7e87`: all three CI waves passed; exact-SHA Railway/Vercel health, DB readiness, web, and protected coach-route smokes passed. Decision #261.** |
| ~~Batch 182 — A deload that understands why the Red happened~~ | 🔴 High | Shipped | **Unblocked 2026-08-04 — and the shape changed.** Mark answered the outstanding question and the answer *removed* the duration cap this batch was going to carry: **eight of the nine low-training stretches were holidays**, so the day-4 inflection in `docs/reviews/deload-escalation-evidence.md` is mostly a travel effect, not a rest effect. His own reading is the disciplined one — *"don't think right to say 100% of impact was holiday and expect not training would at least have been a factor… impossible to establish what % … which means it is more something to monitor going forward."* He also corrected two readings of his position: rearranging a week is **a tool he actively uses and wants** (*"a key tool I currently use so I'm totally bought into it"* — swap Tuesday's VO2 for a Z2 when below par, preserving the weekly mix), and the assumption that he would train at all costs is wrong — when Copilot dropped recovery weeks from his 13-week block **he was the one who challenged it and argued for keeping them** (corroborated in `plan_blocks`: W04–W08 are five consecutive BUILD weeks). Evidence doc amended 2026-08-04.<br>182.1 **The red-cluster path is acute evidence wearing chronic clothing.** `_chronic_action_signal` (`chronic_patterns.py:636-691`) has two independent triggers and they are wildly asymmetric. The sustained-marker path requires a recovery marker (`readiness_score`, `hrv_7_day_avg_ms`, `resting_heart_rate_bpm`; `:39-41`) to miss its personal band on **≥70% of ≥10 samples** (`CHRONIC_ACTION_MISS_RATIO = 0.7`, `MIN_METRIC_SAMPLES = 10`, `:32-34`) inside a **≥21-night** window (`MIN_OBSERVED_NIGHTS = 21`, `:31`). The red-cluster path requires **2 Red mornings in 7 days** (`CHRONIC_ACTION_RED_THRESHOLD = 2`, `CHRONIC_ACTION_RED_WINDOW_DAYS = 7`, `:35-36`; `:678`) — no sample floor, no requirement that anything else look bad — and yields the same seven-day deload. Prod 2026-08-01: Red, Red, Green, Amber, Green, Green, Green, Green, Green, Amber over ten mornings, so two adjacent Reds against six Greens fired a full week's easing. Mark's structural objection: his plan puts three sessions Tue–Thu with two of them hard, so **every Friday has a standing chance of being Red by design** — meaning one further poor night is all it ever takes. The Red calls themselves are sound (`_morning_verdict` runs off sleep/readiness/HRV and never consumed the fields Batch 180 fixed); this is an escalation-policy defect. Also re-word the capability text, which says *"a sustained run of poor recovery"* while the red path clears on two mornings.<br>182.2 **Qualify a Red by why it happened, using two signals we already store.** *Physiology:* **Fri 31 Jul** was readiness 20 POOR on `recovery_time_min` **2584** (43 h of debt from a Tue/Wed/Thu block of 214/66/198) while HRV was 52 BALANCED and RHR 43, his lowest of the fortnight — an expected, planned consequence of training on healthy systemic markers. **Sat 1 Aug** was HRV 52 → 35, RHR 43 → 48, sleep stress 12 → 28 — a genuine acute hit. *Mark's own words:* **he explained both Reds in the check-in before either brief ran, and the escalation read neither.** Saturday's `manual_entries` row at 08:44 carried `subjective_score` 3, feel *"Have a bit of a hangover today"*, notes *"Was out last night drinking… 13 uk units… poor sleep and feel the effects this morning"* — and the morning analysis generated at **08:45:49, one minute later**. Friday's carried *"presumably due to a harder day's training yesterday and cumulative 3 day training load"*. Qualify a Red before it counts toward `red_count` (`:678`) from `recovery_time_min` / `acute_load` / HRV / RHR against `metric_baselines`, **plus the check-in `feel` and `notes` text for the day**. **Decide at `/batch-start`:** the physiological predicate and its lookback; whether check-in text is interpreted by the model or matched deterministically (the verdict path must stay deterministic, so a model reading can qualify escalation only); whether an explained Red is excluded or down-weighted.<br>182.3 **Know where he is in the plan before proposing to change it.** The 1 Aug proposal was not merely severe, it was **redundant** — `plan_blocks` already held **PN2 W03 RECOVERY, 2026-08-03 → 08-09**, a scheduled recovery week starting two days later, and the escalation read none of it. Mark: *"if we are moving into a '1' lighter week anyway, then plan already allowing for this and it doesn't need to change anything."* The plan is a 2121 (two build weeks, one lighter) and manages fatigue by design. Read `plan_blocks.block_type` / `start_date` / `end_date` and suppress or soften a chronic proposal when a recovery, taper or consolidation week is already scheduled inside the horizon it would cover. **Decide at `/batch-start`:** suppress entirely vs propose only the part the plan does not already cover; the horizon.<br>182.4 **Propose a rearrange, not a reduction.** The app's only chronic lever is a *deload* (75% duration, one zone drop, no HIT — Batch 171). What Mark actually does, and wants, is **rearrange**: swap a hard session for an easier one within the week so the weekly mix survives and each session type lands on a day his feel and metrics support. These are different interventions and he is bought into the one we do not offer. For a short cluster, propose a swap (defer the hard session, pull an easier one forward) and reserve the deload for the sustained-marker path. The restructure engine and endpoints already exist from the 2026-07-07 wave. **Decide at `/batch-start`:** reuse the existing restructure preview→apply path vs a narrower swap proposal; whether it stays on the propose→approve→push rail (it should — Decision #29).<br>182.5 **Record why a quiet period happened.** Nothing in the schema says *why* training stopped — the gap that made the whole 404-day analysis unfalsifiable and cost a round-trip to Mark to resolve. His conclusion was *"something to monitor going forward"*, which is exactly right and small: capture a reason for a low-training stretch (holiday, illness, travel, deliberate rest) so the question is answerable from data next time instead of from memory. **Decide at `/batch-start`:** whether this is inferred from existing plan/holiday state plus check-in text, or a light explicit capture; whether it ships here or splits out.<br>182.6 **Tests + DECISIONS.** Two adjacent Reds in an otherwise-Green week do not by themselves propose a seven-day deload; a Red carrying heavy recovery debt with intact HRV/RHR does not count toward escalation while a markers-crashed Red does; a check-in naming a cause is honoured; a chronic proposal is suppressed when a recovery week is already scheduled; a short cluster yields a swap proposal rather than a week-long reduction; the sustained-marker path still fires unchanged; nothing here can soften today's deterministic Green/Amber/Red (verdict-engine safety rule); proposals stay approval-gated and idempotent (Batch 171 / Decision #252). | Stop two explained bad mornings buying a week-long reduction — read why the Red happened, check whether the plan already handles it, and offer the rearrange Mark actually uses instead of a deload he does not want. | A Red is qualified before it counts toward chronic escalation, using stored physiology (recovery debt vs HRV/RHR against baseline) and the check-in text Mark already writes — both 31 Jul (training debt, markers intact) and 1 Aug (explicitly reported drinking) are recognised as explained and neither feeds a seven-day proposal. A chronic proposal is suppressed or narrowed when `plan_blocks` already schedules a recovery/taper/consolidation week across the horizon it would cover, so the 1 Aug case (RECOVERY week starting 3 Aug) produces nothing. For a short cluster the app proposes a **rearrange** — deferring a hard session and pulling an easier one forward, weekly mix intact — with the deload reserved for the sustained-marker path (≥70% miss over ≥10 samples in a ≥21-night window), which is unchanged. The capability wording matches the bar that actually applies. Reasons for low-training stretches become recordable so the rest-versus-recovery question is answerable from data in future. Nothing alters the deterministic daily verdict; proposals stay approval-gated, idempotent and on the propose→approve→push rail (Decision #29). Backend-first; any restructure surface reuses the existing engine. Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-08-01 feedback and his 2026-08-04 answers; evidence and amendment in `docs/reviews/deload-escalation-evidence.md`. **Shipped 2026-08-04 via PR #213 / squash `7819434`: final branch, PR-context, and post-merge `main` CI passed all seven jobs; exact-SHA Railway/Vercel health, DB readiness, web, protected-route, deployed-classifier, and frontend-copy smokes passed. Decision #263.** |
| ~~Batch 183 — Dates on the coach conversation~~ | 🟢 Mid | Shipped | 183.1 **Show when each message was sent.** Mark, 2026-08-04: *"the Chat function, its really good and shows full chat history but would be even better if it could show date of questions (like a traditional mssg chat) for ease of reference so I can see what I asked and when."* `brief_messages` already stores `created_at` and Batch 179's `ix_brief_messages_user_created` already orders on it, so no schema or query work is needed — the timestamp simply is not rendered. Add date separators between days in the conversation (the familiar messaging pattern Mark names) and a per-message time, in his local timezone rather than UTC. Applies to both surfaces Batch 179 left in place: the app-wide `CoachLauncher` thread and the three inline per-read chats.<br>183.2 **Tests.** A conversation spanning several days renders one separator per day in local time; same-day messages carry a time; the existing ordering and the per-read filtered view are unchanged. | Let Mark see what he asked and when, which he cannot currently tell from a continuous unbroken thread. | The coach conversation renders day separators and per-message times in the user's local timezone, on both the app-wide thread and the inline per-read chats; ordering and the per-read filter are unchanged; no schema, migration or API change is required (`brief_messages.created_at` is already stored and already indexed). Frontend-first; web test/build green. Surfaced by Mark's 2026-08-04 feedback. **Shipped 2026-08-04 via PR #214 / squash `7d1d479`: branch-push and PR-context CI passed all seven jobs, Vercel preview and production succeeded, and production exact-SHA health, DB readiness, web, deployed-bundle and protected-route smokes passed.** |

## Post-roadmap — 2026-08-04 — Mark feedback: proactive guidance, not just day-by-day

Mark asked on 2026-08-04 for the coach to guide him **proactively** on training and sleep, rather than only day-by-day or when he asks a question. Reconciled against the code, the gap is not that the app lacks a longer horizon — it is that **everything with a horizon longer than one day is pull-only, and the one thing the app says unprompted every day is a fixed string.** Three findings. **(1)** The evening nudge is the highest-frequency proactive touchpoint in the app and the least intelligent thing in it: `build_evening_nudge_plan` (`nudge_alerts.py:132`) returns the same hardcoded body every night — *"20:00 breathing, pre-cool the room toward 17C, finish snack by 21:30, seal the bedroom near 22:00, bed 23:15"* — regardless of what Mark did that day or what the room and the forecast are about to do, while `project_sleep` (`sleep_projection.py:72`) already derives a tone (`routine`/`protect`/`watch`), a headline, evidence and up to two `prep_actions` from exactly that data and serves them only into the pull-only daily-loop payload (`daily_loop.py:1180` → `:1445`). **(2)** `ReviewService` is constructed in exactly two places, both in `routers/reviews.py` (`:173`, `:194`) — **no scheduler job ever creates one** — so the weekly and monthly reviews, the most substantial multi-day artefacts the app builds, exist only if Mark opens Reviews and taps "Generate review" (`ReviewsPage.tsx:247`). **(3)** The same holds for every other multi-day computation: the chronic 28-day patterns and their action signal (`chronic_patterns.py:1015`), experiment evaluations (`experiment_evaluation.py:464`), weekly-mix `at_risk` accounting (`weekly_mix.py:183`), the rotating REM lever library (`rem_interventions.py:187`) — all computed, none ever mentioned unless he opens the screen that renders it. And nothing anywhere is *forward*-looking: the Week tab is a schedule, not guidance about what the coming week asks of him.

**The delivery rail is the coach thread, and it needs no migration.** Batch 179 (Decision #259) already made `BriefMessage.analysis_id` nullable, added `origin_kind`/`origin_date` for surfaces with no read, and `role` already takes `user`/`assistant` (`coaching.py:546,576-584`) — so a coach-*initiated* message is simply an assistant row in the one rolling thread. That is better than four new notification types: proactivity gets a single home, Mark can reply to it in place, and because 179.2 converted the learning joins to outer joins those turns still feed Batch 151's conversational learning. New origins go into the controlled `ORIGIN_KINDS` vocabulary (`chat_context.py:119`), never as free text — that vocabulary exists precisely so app-supplied strings cannot reach the prompt.

**Two guardrails bind all four batches.** *Proactive is not the same as more notifications:* the static 20:00 nudge is the cautionary case — identical words every night is how a channel stops being read — so every unprompted message must earn its place by saying something Mark does not already know, and Batch 187 carries an explicit budget. *Batch 182's discipline carries forward:* Decision #263 established that acute evidence is qualified before it buys an intervention, and nothing here may notify off evidence 182 would have excluded — a proactive "you may be overreaching" fired on two explained Reds is worse than silence. **Sequencing:** 184 is independent and can ship first; **185 must precede 187**, which delivers on the coach-message rail 185 builds; 186 is best folded into 185's Sunday message rather than becoming a second push; and 185.4 builds directly on the chat components **Batch 183 shipped**. Nothing here touches the deterministic Green/Amber/Red ladder (verdict-engine safety rule) and any plan change stays on the propose/confirm rail (Decision #29). Decision numbers assigned at `/batch-start`; rows stay `Planned`.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 184 — Tonight's nudge is about tonight~~ | 🔴 High | Shipped | 184.1 **Lift the projection's input assembly out of the router.** `project_sleep` (`sleep_projection.py:72`) is pure and reusable, but everything it consumes is assembled router-locally: `_activity_training_signals` (`daily_loop.py:1144`), the measured sleep drivers from `InsightsService.cached_drivers` (`:1342`), the snapshot's `sleep_protocol` and overnight weather, and the fresh bedroom temperature (`:1346`). A scheduler job cannot reach any of it without duplicating that assembly — which is why the nudge sends a constant. Move the assembly into a service that both the router and the nudge call, so the evening push, the daily-loop payload and the Sleep page can never disagree about tonight. **Decide at `/batch-start`:** a new leaf service vs extending an existing one; whether the nudge reuses `cached_drivers` or tolerates a colder read; how it degrades when the bedroom reading is stale (`environment_freshness.py` already answers that question).<br>184.2 **Send tonight's projection, and stay quiet when there is nothing to add.** Replace the hardcoded body in `build_evening_nudge_plan` (`nudge_alerts.py:132`) with the projection's `headline` plus its one or two `prep_actions` (`sleep_projection.py:136`), carrying the tone as the push severity. The fixed protocol stays as the honest fallback for `status="fallback"` — fewer than `MIN_DRIVER_SAMPLES` (8) measured drivers, or no training signals at all (`:7`, `:151`) — which is correct behaviour, not a defect. **Decide at `/batch-start`:** whether a `routine` night sends at all. A nightly *"tonight looks like a standard protocol night"* re-creates precisely the ignorable-channel problem this batch exists to fix; the recommendation is that `protect`/`watch` always send, while `routine` and fallback nights send the protocol at most once a week.<br>184.3 **Tests + DECISIONS.** A `protect` night names the real driver (late or hard session, warm room, warm overnight low) rather than reciting the protocol; a fallback night degrades to the protocol text; a `routine` night obeys whichever quiet rule is chosen; per-day idempotency through `_send_once`'s `sleep-protocol-{date}` tag (`nudge_alerts.py:703`) is unchanged; the 20:00 local fire window (`EVENING_NUDGE_TIME`, `EVENING_NUDGE_WINDOW_MIN` = 20, `:40-41`) and the holiday-away skip (`scheduler.py:196-206`) still hold; the push and the Sleep page describe the same projection. | Make the app's most frequent unprompted message say something true about tonight — it already computes exactly that and currently shows it only to someone who opens the app. | The 20:00 nudge carries tonight's projected sleep risk — a headline plus up to two prep actions derived from today's actual training, the measured sleep drivers, the live bedroom temperature and the overnight forecast — with the fixed protocol retained as the fallback whenever the projection is unpersonalized, so the nudge never invents a driver it has not measured. The projection is assembled once in a service, so the evening push, the daily-loop payload and the Sleep page cannot disagree about the same night. A `routine` night does not repeat the same words nightly (exact quiet rule decided at `/batch-start`). Per-day idempotency, the 20:00 window and the holiday-away skip are unchanged; the deterministic verdict is untouched. Backend-only; no migration or shared-schema change. Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-08-04 request for proactive guidance. **Shipped 2026-08-04 via PR #215 / squash `107b5c4`: the shared context service supplies both app and scheduler; fresh `watch`/`protect` pushes carry evidence-naming headlines plus up to two actions, personalised `routine` stays silent, and generic fallback is capped at once per profile-local week. Local gates and all seven branch-push, PR-context and post-merge `main` CI jobs passed; Vercel preview and exact-SHA Railway/Vercel production checks passed, including DB readiness, web, deployed-policy and protected-route smokes. Part of Decision #264.** |
| ~~Batch 185 — The weekly review arrives instead of waiting to be asked for~~ | 🔴 High | Shipped | 185.1 **Schedule the review.** `ReviewService` is constructed only from the router (`reviews.py:173,194`), so the weekly review is generated only on a tap. `run` is already idempotent per period — it returns the stored review untouched unless `prompt_version` moved or `force` is set (`:630-647`) — and already runs its Anthropic call inside `workload_slot`, so it is safe to fire unattended. Add the job and register it in the single-job runner (`run_scheduled.py:46-54`), which is how wall-clock work actually fires in prod: the in-process APScheduler is unreliable in the Railway container (`docs/runbooks/scheduled-jobs-cron.md`). Note that `resolve_period_window` (`reviews.py:129`) defines weekly as the ISO week *containing* `as_of`, so the run date selects the week — a Sunday-evening run reviews the week just ending, whereas a Monday-morning run would review a new and empty one. **Decide at `/batch-start`:** the exact local time; whether the monthly review is also scheduled at month end or stays manual; the holiday-away skip; and the behaviour when the Anthropic call fails (Batch 141's failure card + admin alert is the existing pattern to reuse, not reinvent).<br>185.2 **Deliver it into the coach thread, not as a document to go and fetch.** Write an assistant-role `BriefMessage` (`coaching.py:546`) carrying the review's conclusions, anchored by `analysis_id` to the stored review row, with a new entry in `ORIGIN_KINDS` (`chat_context.py:119`) naming the surface. No migration is required — Batch 179 already made the anchor nullable and `role` already takes `assistant` (`:576-584`). Mark's reply then lands in the same thread with the review as its anchor, and Batch 178's ask-time assembly gives that conversation the rest of the app's state for free. **Decide at `/batch-start`:** whether the message is the review's own opening lines or a separately generated short summary.<br>185.3 **One push, carrying the conclusion.** Reuse `_send_once` (`nudge_alerts.py:703`) so the weekly push is idempotent per (profile, subject_date) exactly like every other notification. The body must be the single most important conclusion of the week — never "your weekly review is ready", which is the failure mode this whole plan exists to avoid. **Decide at `/batch-start`:** the deep-link target (`/reviews` vs the coach thread).<br>185.4 **Make an unread coach message visible.** `CoachLauncher` (`CoachLauncher.tsx:42`) fetches the thread but has no notion of an unseen turn — with the coach now speaking first, the thread is only a delivery surface if Mark can see that something is waiting in it. Batch 183 has already shipped the per-message dates on these same components, so this extends them rather than competing with them. **Decide at `/batch-start`:** a client-side last-seen marker (no migration) vs a persisted read marker (a migration).<br>185.5 **Tests + DECISIONS.** The scheduled run generates the ISO week just ended rather than the week starting; re-running the same week neither regenerates nor re-notifies; an assistant-authored message appears in the thread and Mark's reply anchors to the review; the push body carries a conclusion rather than an announcement; the new origin resolves through `normalize_origin_kind` (`chat_context.py:137`) while unknown values still degrade to `general`; coach-initiated turns still reach the Batch 151 learning pipeline through 179.2's outer joins. | Turn the app's most substantial multi-day artefact from something Mark has to remember to generate into something that arrives and starts a conversation. | The weekly review is generated on a schedule (cadence and local time decided at `/batch-start`) through the existing idempotent `ReviewService.run`, registered in the single-job runner so it fires reliably in prod, and covering the ISO week that just ended rather than the one starting. It is delivered as an assistant-authored turn in the app-wide coach thread — anchored to the stored review, with its origin added to the controlled vocabulary rather than passed as free text — so Mark can reply in place and Batch 178's ask-time context answers the follow-up. Exactly one push accompanies it, idempotent per day through the existing `_send_once` rail, and its body states the week's most important conclusion rather than announcing that a document exists. An unseen coach turn is visible on the launcher (mechanism decided at `/batch-start`). Coach-initiated turns remain learning sources; the deterministic ladder, the floors and propose/confirm are untouched. Backend-first plus the launcher change; migration only if a persisted read marker is chosen. Backend pytest/ruff/mypy plus web test/build green. **Builds on Batch 183**, which already ships per-message dates on these components. Surfaced by Mark's 2026-08-04 request for proactive guidance. **Shipped 2026-08-05 via PR #216 / squash `368ef69`: local gates and all seven branch-push, PR-context and post-merge `main` CI jobs passed; Vercel preview and exact-SHA Railway/Vercel production checks passed, including DB readiness, web, protected coach routes and deployed launcher chunk. The DST-safe Railway `weekly-review` cron service is built from `main` and scheduled for Sunday 18:00 Europe/London. Decisions #265-266.** |
| ~~Batch 186 — The week ahead, before it starts~~ | 🟢 Mid | Shipped | 186.1 **Assemble the week that is coming.** Every horizon the app currently narrates is retrospective; the Week tab renders a schedule and says nothing about what it will ask of him. The pieces already exist and none needs new ingestion: `TrainingWeekService.build_window` (`training_week.py:52`) reduces planned → changed → executed over an arbitrary window and Batch 178 already runs it *forward*; `summarize_weekly_mix` (`weekly_mix.py:183`) derives each bucket's target from the week's own plan rather than a hardcoded number; and `PlanBlock` (`coaching.py:333`) carries `block_type`/`start_date`/`end_date`, which Batch 182 already taught the escalation path to read for recovery, taper and consolidation weeks inside a horizon. Compose a forward Mon–Sun packet: the week's sessions, which of them are the quality sessions, the block it sits in and whether that block changes mid-week, and the mix targets.<br>186.2 **Say what the week asks of him.** A short narrative through the existing Anthropic boundary: which day carries the week's hardest session and therefore which night's sleep protects it, whether this is a build or a recovery week, and what the mix still needs. Explanatory only — it proposes nothing, and any actual plan change stays on the propose/confirm rail (Decision #29). **Decide at `/batch-start`:** a new analysis type vs folding this into Batch 185's Sunday message. The recommendation is **one Sunday message carrying both halves** — the week that ended and the week that starts — so Sunday evening is a single conversation rather than two competing pushes, which is exactly the notification-fatigue failure this plan is written against.<br>186.3 **Tests + DECISIONS.** The forward packet carries the coming week's active planned rows, its block type and the mix targets derived from that week's own plan; a recovery week is described as one rather than as a shortfall (`target=0` buckets never read short); the narrative names the week's hardest session and the night before it; nothing in the packet or the narrative can move the deterministic verdict or propose a change directly. | Give Mark the forward half of the horizon — what the coming week asks of him and which night's sleep matters most — instead of only ever explaining the week that has already happened. | Shipped via PR #217 / squash `ccaed51` (Decision #267): a deterministic `weekAhead` packet is assembled for the coming Monday-Sunday week from the existing forward `TrainingWeekService.build_window`, active `PlanBlock` rows, and pure `summarize_weekly_mix`, carrying the plan rows, quality sessions, block type/mid-week changes, week-derived mix targets, hardest planned session and previous protecting night. Review prompt v7 folds the narrative into Batch 185's Sunday weekly-review coach message rather than a second push; monthly reviews remain retrospective with `weekAhead: null`. Recovery/taper/consolidation weeks read as deliberate structure, not quality shortfall when bucket targets are zero. Explanatory-only: no deterministic-verdict effect and no direct plan mutation/proposal; Decision #29's propose/confirm rail is unchanged. Backend-only; no migration/shared-schema/new endpoint/frontend/provider/auth/hosting/scheduler/push change. Local backend gates passed (767 / 338 expected local PostgreSQL skips, Ruff clean, mypy clean across 132 source files), and branch, PR-context and post-merge `main` CI all passed seven jobs. Production exact-SHA Railway/Vercel health, DB readiness, web and protected weekly-review route smokes passed. Surfaced by Mark's 2026-08-04 request for proactive guidance. |
| ~~Batch 187 — The coach speaks when something changes~~ | 🟢 Mid | Shipped | **Depends on Batch 185 — it delivers on the coach-message rail 185 builds.** 187.1 **Detect transitions, not levels.** Scheduled messages are only half of proactive; the other half is noticing. The app already computes state that genuinely *changes* — the chronic suggestion status (`insufficient_history` / `clear` / `active`) and its action signal (`chronic_patterns.py:1015`), experiment evaluations reaching an outcome (`experiment_evaluation.py:464`), a weekly-mix bucket going `at_risk` while there is still time to fix it (`weekly_mix.py:183`) — but a level is re-derived every day, so notifying on one would produce a daily message about a standing condition. Fire only on a *transition*. **Decide at `/batch-start`:** a persisted last-notified state (a migration) vs deriving the previous state from the stored `analyses` rows that already record each evaluation.<br>187.2 **Route through Batch 185's rail, under a hard budget.** Coach-initiated messages land in the one thread rather than spawning new notification types. Cap unprompted state-change messages at roughly one per week: if two transitions fire together the more important one wins and the other waits or is dropped, because a burst is how a channel is taught to be ignored. **Decide at `/batch-start`:** the ranking between transition kinds, the budget window, and whether a suppressed transition is queued or discarded.<br>187.3 **Inherit Batch 182's qualification rather than re-deriving it.** Decision #263 established that acute evidence must be qualified before it buys an intervention — the deterministic cause classifier reads the check-in text Mark already writes, and a Red carrying recovery debt with healthy HRV and RHR is excluded from cluster escalation. Nothing in this batch may notify on evidence 182 would have excluded, and a transition that 182's plan-awareness would suppress (a recovery, taper or consolidation block already scheduled in the horizon) must not produce a message either. This is the batch's main safety surface: an unprompted "you may be overreaching" is a much stronger claim than a card on a screen he chose to open.<br>187.4 **Tests + DECISIONS.** A standing `active` pattern produces one message on the transition and nothing on subsequent days; a `clear` → `active` flip notifies while `active` → `active` does not; two transitions in one window yield one message, ranked; a transition whose underlying Reds 182 would have excluded produces nothing; a transition covered by an already-scheduled recovery block produces nothing; the message lands as an assistant turn Mark can reply to; the deterministic verdict, the floors and propose/confirm are untouched. | Make the coach notice things — a pattern flipping, an experiment concluding, a mix quietly going at risk — instead of computing them and waiting to be asked, without turning that into a daily stream of noise. | Unprompted messages fire on a *transition* in already-computed state (chronic suggestion status and action signal, experiment outcome, weekly-mix `at_risk`) and never on a standing level, so a persistent condition is mentioned once rather than daily; the previous-state mechanism is decided at `/batch-start` and adds a migration only if a persisted marker is chosen. All of it routes through Batch 185's coach-thread rail under an explicit budget of about one unprompted message per week, with a defined ranking when transitions collide. No message fires on evidence Batch 182 would have excluded from escalation (Decision #263's cause classifier and recovery-debt-with-intact-markers rule) or on a horizon where a recovery/taper/consolidation block is already scheduled. Each message is an assistant turn Mark can reply to and which still feeds the Batch 151 learning pipeline. The deterministic Green/Amber/Red ladder, the safety floors and the propose→approve→push rail are unchanged. Backend-only. Backend pytest/ruff/mypy green. Surfaced by Mark's 2026-08-04 request for proactive guidance. |

## Post-roadmap — 2026-08-05 — Full-app review wave 2 (five-lane audit) batch plan

Craig asked (2026-08-05) for a **fresh review of the app**. The previous wave
(Batches 153–156, 2026-07-25/26) is genuinely closed: its 21 findings were
remediated by 157–166 and the coaching-integrity findings by 167–171. Since that
wave's ship SHA `7e77169`: **79 commits, 227 files changed, +24,083 / −3,362,
migrations 022→026, 15 new backend modules, Batches 157–187** — more change than
the codebase held *at* the last audit.

**Why now, and why the lanes are re-cut.** The centre of gravity moved. At the
last review the app was a document generator (morning brief, post-workout read,
reviews). It now **initiates conversation and answers from live state**: Batch
178 (ask-time app state, 30k budget with named trims), 179 (one rolling
conversation, migration `026`, `services/coach_policy.py` as the floors module),
183 (local dates), and 184/185/187 — the 20:00 sleep nudge, the Sunday review
push, and unprompted state-change turns. That is a risk class nothing has looked
at: the coach saying something wrong, saying too much, or quietly contradicting
the deterministic verdict. Hence the new **Lane B** (188), which did not exist in
wave 1.

**Model — diagnose, then remediate** (unchanged from wave 1, Craig 2026-08-05).
Each review batch is **diagnose-only**: it produces a committed report doc in
`docs/reviews/` and ranks its findings; **no product code change lands inside a
review batch**. Every material finding becomes its own scoped remediation batch
afterward, triaged into a roadmap for Craig's approval before any code starts —
exactly as `reviews/BATCH_153-156_REMEDIATION_ROADMAP.md` sequenced R1–R9. A
review never edits verdict logic, Green/Amber/Red, #133/#135, or Red-never-VO2.

**Scope decision (Craig, 2026-08-05).** All five lanes; diagnose-only.

**Ordering.** Recommended **188 → 189 → 190 → 191 → 192** — the two repo-only
lanes first (they need nothing but `main`), then ops/security, then behaviour and
experience (which need a production data pull and the live app). The batches are
independent and any can be pulled forward without renumbering. `/batch-start`
assigns the DECISIONS number when each is actually started.

**Deliberate scoping.** 190/191/192 are **full-app** because the property each
judges is weakest-link or emergent — security is only as strong as the
least-guarded route, and coaching integrity and UX consistency arise from the
whole system. 189 is **diff-anchored** at `7e77169..HEAD` plus the engine
hotspots: an every-file read of 47k backend + 28k web LOC of shipped, CI-green
code is low-yield. 188 is full-app across the *prompt surface* specifically.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 188 — Coach-conversation & prompt-policy integrity review (diagnose-only)~~ | 🔴 High | Shipped | 188.1 **Map the whole prompt surface and its floors coverage.** There are **16** `PROMPT_VERSION` constants across `services/`, but `services/coach_policy.py` (202 lines) is imported by exactly **one** service (`services/brief_chat.py`) and audited by one test (`tests/test_brief_chat_prompt_policy.py`) covering 8 read prompts via `READ_PROMPT_FLOORS`. Establish which surfaces are outside the audit — on a first pass: `state_change_coach.py` (v1), `nudge_alerts.py` (v3), `insights.py` (v2), `weekly_restructure.py`, `executable_coaching.py`, `experiment_tracker.py`, `experiment_evaluation.py`, `wake_detection.py` — and judge for each whether it *should* be, or whether being outside is correct and simply undocumented. **The newest unprompted-speech surface being the least covered is the lead question, not a settled finding.**<br>188.2 **Coherence audit across prompt versions.** The floors accreted batch by batch (165 capability wording, 173.3 adjustment figures, 175 labelled general-science lane, 181 observed-data humility, 178 not-known sentence). Read them as one system and find contradictions — the sharpest candidate is Batch 181's "his own-device reading wins on observed data" against the standing rule that the deterministic engine owns the verdict and the numbers. Determine what the model actually does when both bite at once.<br>188.3 **Ask-time context assembly (`services/chat_context.py`, 769 lines).** Verify the `APP_STATE_CHAR_BUDGET = 30_000` trim order genuinely names every drop in `omittedForLength`, that a trimmed field can never read as "the app does not hold it", and that the `sinceThisRead` delta off `analysis.generated_at_utc` cannot present stale state as current. Check `adjustable_workout_id` gates the propose affordance exactly when a live, deliverable, non-holiday session exists.<br>188.4 **Unprompted speech (`services/state_change_coach.py`, 513 lines).** Audit the seven-day budget, the chronic > experiment > mix ranking, and the drop-don't-queue rule against what Batch 182's qualification would have excluded. Judge whether a transition can fire on evidence the deterministic side would not act on, and whether `verdictImpact="none"` / `planMutation="none"` is enforced in code or only asserted in the prompt.<br>188.5 **Report.** `docs/reviews/BATCH_188_COACH_CONVERSATION_REVIEW.md`, findings ranked High/Med/Low with IDs `CC188-*` and remediation stubs. | Judge whether the coach's *spoken* layer is as disciplined as its deterministic layer, now that the app talks unprompted and answers from live state — the one part of the system that grew fastest and has never been audited. | The full prompt surface is enumerated with an explicit in-audit / out-of-audit disposition per surface and a reasoned judgement for each omission; contradictions between accumulated floors are named with the code path that resolves them; the 30k trim order, the not-known sentence and the propose gate are verified against `chat_context.py` rather than the prompt's claims about itself; unprompted-speech budget, ranking and safety inheritance from Batch 182 are verified in code; every finding carries severity, evidence (`file:line`) and a remediation stub. Diagnose-only — no product behaviour, prompt text, prompt version, verdict logic or floor changes. **Shipped 2026-08-05 via PR #219 / squash `0d9384f`: report at `docs/reviews/BATCH_188_COACH_CONVERSATION_REVIEW.md`, 2 High / 8 Medium / 7 Low (`CC188-01…20`). The lane's lead question inverts — `state_change_coach` is not a prompt surface (no model call, hand-written templates), and of 17 version constants only 10 modules call a model; the genuinely uncovered LLM surface is the unversioned `conversation_learning`. Batch 181's flagged contradiction is disproved, and the hard verdict/plan/floor boundary is confirmed intact. Branch-push, PR-context and post-merge `main` CI passed all seven jobs; exact-SHA Railway/Vercel production verification passed. Decision #269.** |
| ~~Batch 189 — Code review: the 157–187 delta + the new rails (scoped, diagnose-only)~~ | 🔴 High | Shipped | 189.1 **Deep-read the diff `7e77169..HEAD`** (79 commits, 227 files) and re-verify each batch's own "boundaries untouched" claim actually holds in code — particularly the batches that claimed backend-only or no-verdict-change while touching packet assembly (176, 177, 180) or verdict inputs (173 `verdict_scaling.py`, 182 Red-cluster qualification).<br>189.2 **The new asynchronous rails.** Batches 184/185/187 added scheduled and event-driven writers (`weekly_review_delivery.py`, `sleep_projection_context.py`, `state_change_coach.py`, the Railway `weekly-review` cron). Audit transaction boundaries, idempotency under a double-fire or a retried cron, and the migration-free "per-profile marker" and "derive previous state from stored packets" mechanisms for races against a concurrent daily loop.<br>189.3 **Migrations 022–026 as a set** — constraint coverage, nullable-column consequences (`026` made `brief_messages.analysis_id` nullable; verify every consumer and every join, including the outer joins `conversation_learning` moved to), and backfill correctness.<br>189.4 **Test-gap analysis.** Which of the 157–187 behaviours rest only on mocked Anthropic calls or on prompt-string assertions rather than on behaviour? 34.7k lines of backend tests exist — identify where the coverage is nominal.<br>189.5 **Report.** `docs/reviews/BATCH_189_CODE_REVIEW.md`, `CR189-*` IDs, ranked, with remediation stubs. | Catch correctness and concurrency defects in the largest un-reviewed change window the project has had, targeting where bugs actually hide rather than re-reading CI-green code. | The delta is read with each batch's boundary claim independently verified; the scheduled/event-driven rails are assessed for idempotency and races with evidence; migrations 022–026 are audited as a set including every consumer of the `026` nullable anchor; the test-gap analysis names specific behaviours whose only coverage is a mock or a prompt-string assertion; findings ranked with `file:line` evidence and remediation stubs. Diagnose-only. **Shipped 2026-08-06 via PR #220 / squash `1839f79`: report at `docs/reviews/BATCH_189_CODE_REVIEW.md`, 2 High / 9 Medium / 9 Low (`CR189-01…20`) over 82 commits / 228 files / +25,086 −3,362. Nine of ten boundary claims verified as holding (173/176/177/178/179/180/182/185 + a checked non-finding that `022`/`024`'s RLS-without-policies matches `015`/`019`/`020`/`021`); the tenth fails — Batch 187's state-change turn lights no unread dot and sends no push, so its stated visibility rail is mute. Second High: only the two jobs added by Batches 185/187 roll back, so one DB error poisons the shared Session and aborts the morning pipeline before the verdict (reproduced by execution). Branch-push, PR-context and post-merge `main` CI passed all seven jobs; exact-SHA Railway/Vercel production verification passed. Decision #270.** |
| ~~Batch 190 — Data / security / ops review refresh (diagnose-only)~~ | 🔴 High | Shipped | 190.1 **Authorization sweep over the routes added since the last sweep** — `routers/coach_chat.py` (Batch 179's app-wide surface), plus every router touched by 157–187: ownership checks, 403 vs 404 behaviour, and the controlled-vocabulary degradation for `origin_kind` that keeps client text out of the prompt.<br>190.2 **RLS verification against what migration `025` actually shipped** (`025_security_ops_rls_hardening.py`), not what it intended: confirm every `coach.*` table has RLS enabled and correctly user-scoped, cross-checked with Supabase `get_advisors`. Note the same Supabase project also hosts an unrelated app in `public` — verify the boundary holds.<br>190.3 **Backup durability — the known open follow-up.** `services/backup.py` (153 lines) writes to `BACKUP_DIR` on the **ephemeral Railway container filesystem**, and on the Supabase free plan these dumps are the only backup that exists (no managed backups, no PITR). Assess actual recoverability today, and the `BACKUP_DIR` → Railway volume follow-up left open by Decision #262.<br>190.4 **Egress and quota headroom** after the 2026-08-04 incident (15.19 GB against a 5.5 GB cap): verify the narrowed dump and the settled-stream skip are holding in production, and identify any remaining amplification path.<br>190.5 **Scheduler / cron reliability**, including the Decision #258 gotcha where a Railway platform incident **dropped** the merge webhook rather than queueing it and production silently served a stale SHA with no failed deployment to signal it. Is there a detection path today, or does it still depend on a human checking the served SHA at closeout?<br>190.6 **Report.** `docs/reviews/BATCH_190_DATA_SECURITY_OPS_REVIEW.md`, `DS190-*` IDs, ranked, with remediation stubs. | Re-establish the weakest-link properties — authorization, data isolation, recoverability and deployment truth — against a system that has grown five migrations, a new public surface and two production incidents since they were last checked. | Every route added or touched since `7e77169` has its ownership/authorization behaviour verified; RLS coverage is confirmed against the deployed schema and `get_advisors`, including the shared-project boundary; backup recoverability is assessed as it stands today with the ephemeral-filesystem exposure stated plainly; egress mitigations are verified live; the stale-SHA detection gap is characterised; findings ranked with evidence and remediation stubs. Diagnose-only — the review changes no config, no policy and no data. |
| ~~Batch 191 — Coaching-integrity audit refresh (diagnose-only)~~ | 🔴 High | Shipped | 191.1 **Re-run the `COACHING_INTEGRITY_AUDIT.md` framework** (strong-vs-acute gaming/sycophancy, weak-vs-chronic drift). Prior grade **B+** at Batch 155. F1 (baseline drift) and F2 (load not gated) were closed by Batches 168 and 167 — verify on real data that they are *actually* closed, not just implemented.<br>191.2 **Audit the coaching changes since, against real production data**: the load cap (167), absolute baseline anchor (168), bounded/decaying memory (169), chronic action (171), verdict scaling (173 `verdict_scaling.py`), Red-cluster qualification (182), and live weight/VO2 resolution (176/177). Does the accumulated machinery still produce a coherent verdict distribution, or has it become over-conditioned?<br>191.3 **Audit the new spoken surfaces for integrity regressions the deterministic layer cannot see** — can an unprompted state-change turn (187), a weekly review conclusion (185/186) or an ask-time chat answer (178) soften, contradict or pre-empt the verdict Mark is about to be given?<br>191.4 **Real-data pass** over the `coach` schema as Batch 155 did (Prong B), reporting the verdict distribution and any narrative softening, with a Mark-safe scorecard artifact alongside the internal report.<br>191.5 **Report.** `docs/reviews/BATCH_191_COACHING_INTEGRITY_REFRESH.md` + a Mark-facing scorecard; `CI191-*` IDs, ranked, with remediation stubs. | Answer the question the code cannot: after 31 batches of added coaching machinery, is the coaching *better* on Mark's real data, or merely more complex — and can the new spoken layer undermine the deterministic one? | The prior framework is re-run with an explicit grade and a stated delta from B+; F1/F2 closure is evidenced on real data rather than assumed from the remediation batches; each coaching change since 155 is assessed for integrity effect; the spoken surfaces are tested for their ability to soften or contradict the verdict; a real-data pass reports the verdict distribution with no narrative softening; a Mark-safe scorecard ships alongside the internal report. Diagnose-only — no verdict logic, threshold, prompt or plan change. **Shipped 2026-08-06 via PR #222 / squash `92453a7`: report at `docs/reviews/BATCH_191_COACHING_INTEGRITY_REFRESH.md`, Mark-safe scorecard at `docs/reviews/BATCH_191_MARK_SCORECARD.md`, refresh block added to the running framework doc `COACHING_INTEGRITY_AUDIT.md`. 2 High / 5 Medium / 1 Low (`CI191-01…08`). Kickoff rule: a remediation counts as closed only when it is observed working on real data. On that rule F2 closed and fired for real (07-29, ACWR 1.61 / 1502 min recovery, Green → Amber), F4 closed and fired for real (07-31, Amber → Red on a night whose sleep score was 76), F3/F5 closed, F8 still RESOLVED, F9 and both Batch 155 stubs shipped, and F1's anchor proved load-bearing on 12 of 12 mornings — the personal 84-day readiness median is 50, so `readinessEffectiveFloor` was the absolute 60 every day. The lane's answer is that one new gap opened in the old shape and inverts a property the original audit credited: Batch 182's Red qualification lets one matching phrase in the user's own check-in text retire a Red from the chronic cluster, uncapped and with no physiology check — observed switching off a live seven-day deload escalation on 08-05 whose first four sessions had already been approved and pushed. Second High is structural: `daily_metrics` is one mutable row per day overwritten by the evening sync while the verdict is computed at wake, so every retrospective consumer reads the evening value. The spoken layer has not undermined the deterministic one, largely because it has barely spoken (0 state-change turns ever, newest scheduled weekly review 2026-06-29, 0 sycophancy attempts in 41 user turns) — but none of the five deterministic protections added since Batch 155 is in `coach_policy.FLOORS`. Grade **B+ held, for entirely different reasons**; closing CI191-01/02 is what moves it to A−. Branch-push, PR-context and post-merge `main` CI passed all seven jobs; exact-SHA Railway/Vercel production verification passed and the deployed container reported the same coaching constants and floor set the review measured. Decision #272.** |
| ~~Batch 192 — Full-app UX / live-app review (diagnose-only)~~ | 🟢 Mid | Shipped | 192.1 **Walk every route** via the constrained [[ui-walkthrough-method]] (headless Chromium + dev build proxied to the prod API + `/activate` login), at 390×844 in light and dark: Home, Check-in, Week, Week-ahead, Sleep, Trends, Environment, Settings, Coach-state/memory, Experiments, Handover, Block generator, Reviews, and the brief/read views.<br>192.2 **Exercise the surfaces that did not exist at Batch 156:** the app-wide `CoachLauncher` thread and `CoachConversation` (179), local-date separators and per-message times (183), the weekly-review coach turn and its unseen marker (185), the week-ahead packet surface (186), and an unprompted state-change turn (187). Confirm honest-state rendering — no blank sheets, no infinite spinners, distinct absent/generating/failed states.<br>192.3 **Cross-screen consistency and drift** the per-screen batches cannot see: toasts, skeletons, save affordances, empty and error states, and whether the coach thread now reachable from every page reads coherently against the three inline per-read chats that remain.<br>192.4 **Report.** `docs/reviews/BATCH_192_UX_LIVE_APP_REVIEW.md` with captured screenshots; `UX192-*` IDs, ranked, with remediation stubs. **Decide at `/batch-start`:** production-data walkthrough vs a seeded local dataset. | Judge the real experience across the whole app now that a conversation surface sits on every page, since UX quality is a whole-app property that no per-screen batch can see. | Every route is captured at 390×844 in light and dark; the six post-156 surfaces are exercised including honest loading/absent/failed states; cross-screen consistency is assessed and drift named; the coexistence of the app-wide thread and the inline per-read chats is judged explicitly; findings ranked with screenshots as evidence and remediation stubs. Diagnose-only — no product behaviour or styling change. **Shipped 2026-08-06 via PR #223 / squash `1f7507e`: report at `docs/reviews/BATCH_192_UX_LIVE_APP_REVIEW.md`, 44 captures and three machine-readable records in `docs/reviews/batch-192/`. 3 High / 5 Medium / 2 Low (`UX192-01…10`), stubs `R192-A…J`. Kickoff dataset decision (Craig): production data through the local bundle proxied to the production API — the Batch 156 method — with browser-only fixtures only for the two surfaces production provably lacks, verified before being simulated (`coach.brief_messages` holds zero `state_change` and zero `weekly_review` rows, ever). Every route works: 15/15 rendered real production content in both themes, zero page-level horizontal overflow across 30 captures, zero script/console errors, zero API responses ≥ 400; `UX156-01` and `UX156-02` are both closed, dark mode is contrast-clean on all 15 routes, and Batch 183's local dates and per-message times are correct. The lane's answer is that the whole-app property does not hold and three defects compound on one surface — the coach: `cn()` discards `fixed` in favour of the `relative` Batch 185 added in `368ef69`, so the launcher computes `position: relative; left: -16px` and sits off-viewport at the end of page flow (live in the deployed bundle, desktop included); the thread opens 27,992 px from its newest turn on an already-truncated 60-of-82-message window; and a failed thread fetch renders as "Nothing here yet", making 82 messages read as deleted. Batch 189's `CR189-01` is confirmed by execution — a `state_change` turn lights nothing — and 75 light-mode AA failures across 13/15 routes re-open `UX156-03` past Batch 163, whose guard stays green because it checks tokens in a CSS file rather than rendered pages. **Merged without a green CI run:** GitHub Actions was in a critical outage (githubstatus.com "Incident with Actions", 15:22 UTC) and all seven jobs were cancelled while queued; Craig authorised the merge on the grounds that the diff is docs-only. Compensating evidence: the Vercel preview built this tree, and local web `tsc` plus 382 vitest tests across 62 files passed. Production verified on the exact merge SHA `1f7507e` (Railway direct + Vercel same-origin health), database readiness `ok`, web `/` 200, protected coach-thread and daily-loop routes 401 direct and proxied, and the batch-specific smoke confirmed the freshly-rebuilt frontend chunk (`Layout-Dwq4Qqxf.js`) carries the launcher call byte-identical to what the review measured — proving the review changed nothing and that `UX192-01` is still live. Decision #273.** |

## Post-roadmap — 2026-08-06 — Batch 188–192 remediation roadmap (13 batches)

Craig approved the triage in [`docs/reviews/BATCH_188-192_REMEDIATION_ROADMAP.md`](reviews/BATCH_188-192_REMEDIATION_ROADMAP.md), which sequences the wave-2 audit's **67 findings** (12 High, 33 Medium, 22 Low; `CC188-*`, `CR189-*`, `DS190-*`, `CI191-*`, `UX192-*`) into 14 roadmap-local slots (`W0`–`W13`). Per the roadmap's own numbering discipline, `W0` is **not a batch** — it is a read-only production SQL query (CR189-06: does migration `022`'s coin-flip pairing hold a mislinked read or a resurrected skip in Mark's real history?) that can run alongside any other slot without a `/batch-start`. `W1`–`W13` are authored below as Batches 193–205, in roadmap order. Tier reflects the roadmap's suggested coding-agent model (🔴 High = Opus 5, 🟢 Mid = Sonnet 5/Haiku 4.5), not finding severity — several Medium-max slots (199/201/205's siblings W7/W9) are Opus-tier because the guard itself is the safety-critical product. DECISIONS numbers are assigned individually at each batch's own `/batch-start`, not here, per the numbering-collision lesson in [[reference_decisions_numbering_and_shared_dir_race]].

**Two standing zero-code decisions expire this weekend independently of any batch:** the 08-08/08-09 chronic-deload proposals (evidence withdrawn by CI191-03, addressed inside Batch 194) and DS190-01's always-on-vs-externalise-jobs call (gates half of Batch 195). Both are Craig's to make, not implementation.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 193 — The coach actually reaches Mark~~ | 🟢 Mid | Shipped | 193.1 **UX192-01.** `CoachLauncher.tsx:126` passes `'fixed … relative'` to `cn()`; tailwind-merge keeps the last position utility and discards `fixed`, so the launcher renders in normal flow off-viewport. Drop the redundant `relative` — a `fixed` element is already its own containing block for the unread-dot anchor Batch 185 added it for.<br>193.2 **UX192-02.** The thread opens at `scrollTop: 0` of a 28,380px pane; scroll to the newest turn on open and after each reply, and decide what the 60-message ceiling means against a real 82-message history with no "load older".<br>193.3 **UX192-03.** A failed thread fetch renders as "Nothing here yet" (no error branch in `CoachLauncher.tsx:78,149-153`); give `CoachConversation` an explicit status input with three distinct presentations and a retry.<br>193.4 **CR189-01 / UX192-04.** The unread predicate hard-codes `originKind === 'weekly_review'` so a `state_change` turn lights nothing; widen to role-and-recency (or an explicit complete allowlist) behind a shared constant in `packages/shared`, and bring `coachOriginKindSchema` + `ORIGIN_PROMPTS` up to `ORIGIN_KINDS` (backend 15 kinds vs. shared/client 14).<br>193.5 **UX192-06.** Append the user's turn optimistically and add a thinking affordance for the multi-second Anthropic call.<br>193.6 **Verification gate (UX192-10).** Before closing, force one weekly review into a disposable context and confirm turn written → dot lit → launcher visible → sheet opens on the new turn → week-ahead prose readable — this chain has never been exercised in front of a user. | Fix the only findings currently broken in production, all on one surface, before the first-ever Sunday weekly review (2026-08-09) lands on every one of them — this also retro-unblocks Batches 184–187's entire proactive-coaching output, currently undeliverable. | Assert the rendered DOM, not the class string: `getComputedStyle(button).position === 'fixed'` and in-viewport on a long page; rendered copy verified per status, not per component; a test that fails when the backend gains an origin kind the client does not know; the UX192-10 gate executed and confirmed before closeout. No migration. **Shipped 2026-08-15 via PR #226 / squash `104dd24`: dropped the redundant `relative` class (verified live via `getComputedStyle` — `position: fixed`, in-viewport, not the class string); the rolling thread scrolls to `scrollHeight` on open and after every reply (the 60-message window was already the most recent 60 — the bug was hiding its newest end, not the window); `CoachConversation` takes an explicit `loading`/`error`/`ready` status with retry; `coachOriginKindSchema`/`ORIGIN_PROMPTS` gain `state_change` (client now matches the backend's 15 `ORIGIN_KINDS`) and the unread predicate widens to a shared `PROACTIVE_COACH_ORIGIN_KINDS` constant, guarded by a new backend parity test; the composer shows an optimistic bubble + thinking indicator while pending. The UX192-10 chain was executed live in a real browser via a disposable, reverted dev-server mock (no prod contact): turn written → dot lit → launcher visible/fixed → sheet opens on the turn → prose readable → read-state persisted. An unrelated CI blocker (a newly-published high-severity `nanoid` advisory, transitive dev-tooling only) was fixed at closeout as a fourth reviewed exception in `scripts/audit_js_advisories.py`, approved by Craig. Both CI waves passed all 7 jobs; production verified on the exact merge SHA `104dd24` (Railway direct + Vercel same-origin health), `db: ok`, web `/` 200, protected coach-thread/daily-loop routes 401 direct and proxied, and the freshly rebuilt `Layout-BZwJ0uwD.js` chunk confirmed the `relative` class is gone from the live bundle. Decisions #274/#275.** |
| ~~Batch 194 — A Red cannot be talked away~~ | 🔴 High | Shipped | 194.1 **CI191-01.** Batch 182's Red qualification lets one matching check-in phrase remove a Red from the chronic cluster — uncapped, undecaying, no physiology check; `training_load`/`deliberate_rest` are endogenous training signals, not acute events, and must stop excusing a Red at all. Separate acute exogenous causes (alcohol, illness, travel) from endogenous training causes.<br>194.2 Bound whatever exclusion remains: physiology may contradict it, cap how many of the last N Reds may be excluded, decay the tag's power over time.<br>194.3 **CI191-03.** When the 08-05 escalation was withdrawn, its two undecided proposals for 08-08/08-09 stayed `proposed`. Expire undecided chronic-origin proposals when `chronicAction.triggered` goes false, with an audit row saying why (already-pushed sessions stay). | Restore the property the coaching-integrity audit was built on — a Red caused by training load is the signal, not the noise — after it was observed live switching off a real seven-day deload escalation on 08-05. | A regression on the real 07-31/08-01 shape proving those Reds now count; proof a habitual check-in annotation cannot permanently silence the rail; proof the change only ever hardens, never softens, Green/Amber/Red. No migration expected. Craig's separate call on the standing 08-08/08-09 proposals (retract vs. let expire) sits outside this batch. **Shipped 2026-08-15 via PR #227 / squash `813c449`:** `training_load` and `deliberate_rest` always count as endogenous structural evidence; only acute `alcohol`/`illness`/`travel` may exclude the newest one Red in the seven-day window, for the Red day plus two days, and only while HRV/RHR remain unstrained. The packet exposes the cap, age and reason partition; prompt v28 explains the frozen result. Clearing/replacing a deload signal expires only future proposed no-event `chronic_deload` offers to `deleted`, with idempotent `workout_proposal_expired` audits; decided, pushed, past, other-origin and event-bearing rows stay untouched. The real 07-31/08-01 DB regression counts both Reds instead of zero, habitual-note/decay/only-hardens matrices pass, and proposal expiry is covered against PostgreSQL. Local gates passed (781 / 340 expected skips; Ruff/mypy clean); branch, PR-context and post-merge `main` CI passed all seven jobs. Production Railway and Vercel served exact SHA `813c449`, web `/` returned 200, protected reads returned 401 direct/proxied, and a read-only deployed-container probe returned cap 1, age 2, prompt v28 and the two expected classifications/count 2. No migration, daily-verdict change, or production data write. Decision #276.** |
| ~~Batch 195 — Jobs that fail say so~~ | 🔴 High | Shipped | 195.1 **CR189-02.** Every per-step handler in the morning pipeline catches, logs and continues on the same `AsyncSession`; a failed flush invalidates the Session so everything downstream raises `PendingRollbackError`, confirmed by execution — one bad D-2 row can cost Mark a whole day's verdict. Add `await session.rollback()` to every per-step handler that can follow a DB write, and give `_sync_morning_inputs`'s commits their own guard so a commit failure degrades the inputs, not the verdict.<br>195.2 **DS190-02 / CR189-20.** Coroutines swallow their own exceptions and the runner exits 0 regardless, so cron cannot alert from exit state. Ship a typed job result/failure contract with non-zero exit in external mode and a persisted per-job run row (window, timestamps, status, reason, counters).<br>195.3 **DS190-04.** Deploy freshness is human-only (Decision #258's dropped-webhook incident produced no failed deployment). Add a post-merge monitor polling both health paths to the expected SHA with a bounded timeout, alerting on mismatch.<br>195.4 **Decision-gated (DS190-01).** The API is sleep-enabled and still hosts 10 of 11 scheduled workloads; do not flip the in-process scheduler off until each external job has proved successful — this half is a hosting-cost call for Craig, not an implementation detail. | Stop a single poisoned step or a silent job failure from costing Mark a verdict with no signal anywhere. | A test that a poisoned step does not cost the verdict; a job-failure test asserting non-zero exit; a simulated stale-SHA alert. Likely a small run-log table; no other migration. **Shipped 2026-08-15 via PR #228 / squash `2b47745`:** morning write-capable steps roll back before continuing and input commits degrade rather than suppressing verdict generation; all real APScheduler/external invocations return a typed outcome; migration `027` adds independent operator-only `job_runs`; degraded/failed external runs exit 1, including ledger-persistence failure; and a post-successful-`main`-CI workflow polls both production health paths for the exact merge SHA with a 15-minute bound. Poisoned-session, external-exit, ledger, and stale-SHA regressions pass. Branch, PR-context and post-merge `main` CI passed all seven jobs; the new freshness workflow passed its first production run against exact SHA `2b47745`. Railway/Vercel health, DB readiness, web and protected-route smokes passed; read-only production DB verification confirmed Alembic `027`, `job_runs` with RLS enabled and zero authenticated policies, and two successful scheduler records. The in-process scheduler and in-process-only 20:30 backstop remain unchanged pending Craig's hosting decision. Decision #277.** |
| ~~Batch 196 — A backup you have actually restored~~ | 🟢 Mid | Shipped | 196.1 **DS190-03.** Seven production backup-failure rows have paged nobody; alert outside the end-user profile model (provider log alert, operator-only channel, or external monitor).<br>196.2 No full restore has ever been performed. Add a scheduled disposable restore with row/schema invariants, recording the result.<br>196.3 The Supabase free plan has no managed backups and no PITR, so this dump is the only line of defence, not a second one. Add an encrypted off-site cadence with a stated RPO/RTO. | Prove the only backup this app has is actually recoverable at the moment it's urgently needed, not merely syntactically valid. | Shipped via PR #229, squash `b78a4f2` (Decision #278): external `backup-drill` restores the newest `coach_*.dump` into `BACKUP_RESTORE_DATABASE_URL`, refuses the production DB, runs `pg_restore --clean --if-exists --no-owner`, and asserts restored coach table count, Alembic version, profile rows, analysis rows and zero restored `activity_timeseries` rows. Backup and restore-drill failures emit structured `operator backup alert` logs outside the end-user push/profile model and fail through Batch 195's external-runner exit/`job_runs` boundary. Runbooks schedule a weekly Sunday 04:00 UTC drill after the daily backup and define encrypted off-site copies with RPO/RTO (24h while the Railway volume survives, 7d for platform-loss after weekly off-site cadence starts, 4h RTO once a target/env exists). No migration. Local backend gates passed (794 / 341 expected skips; Ruff/mypy clean); branch, PR-context and post-merge `main` CI passed all seven jobs; production freshness workflow and exact-SHA Railway/Vercel health, readiness, web and protected-route smokes passed. |
| ~~Batch 197 — Unprompted speech agrees with the brief~~ | 🔴 High | Shipped | 197.1 **CC188-01.** The seven-day unprompted-speech budget is checked before candidates are computed and fires on any `state_change_coach` row regardless of kind, so a trivial heads-up can permanently silence a chronic deload transition. Evaluate candidates first, then apply the budget as a ranked spend (a strictly higher-ranked transition may pre-empt a lower-ranked one, at most once per window) or scope it per `TransitionKind`.<br>197.2 **CC188-02 / CI191-07 rail half.** The coach recomputes "current" state with different parameters from the brief Mark already read, compared against yesterday's packet. Read today's stored morning packet as the "current" snapshot instead of recomputing — removes both parameter asymmetries and three service calls per profile per day.<br>197.3 **CC188-08.** An absent previous morning read makes every `previous_value` `None`, so a fortnight-old standing state announces as "Something changed" on the first day back from holiday. Treat "no previous packet" as unknown, not "different".<br>197.4 **CC188-10.** No holiday guard on unprompted speech at all. Apply the weekly-review job's holiday check before candidate generation.<br>197.5 **CR189-09 / CC188-20.** The one DB test subclasses the service and overrides `_candidates` — the entire detection layer is never executed under test. Write DB tests that seed a real previous-morning packet and run `_candidates` unstubbed: standing state → no message, genuine flip → message, missing previous read → no message. | Stop the app's unprompted speech from contradicting, pre-empting, or announcing a change the brief already covered — sequenced after Batch 193 (nothing here is delivered until the launcher works) but must land before the rail carries real traffic. | The unstubbed DB tests above, all passing. No migration. **Shipped 2026-08-15 via PR #230 / squash `972c5be`:** stored morning packets are authoritative for chronic-action and weekly-mix transitions, missing comparison state is unknown, the seven-day budget ranks chronic action > experiment outcome > weekly mix with one recorded strictly higher-ranked pre-emption, and active holidays skip before candidate generation. Local backend gates passed (795 / 346 expected PostgreSQL skips; Ruff/format/mypy clean); branch, PR-context and post-merge `main` CI passed all seven jobs; production freshness and exact-SHA Railway/Vercel health passed; web, protected coach-route and deployed-container policy smokes passed. Decision #279. |
| ~~Batch 198 — The unprompted writer is safe to run twice~~ | 🟢 Mid | Shipped | 198.1 **CR189-03.** `run()` reads the budget, computes candidates, re-reads, then inserts, with no lock and no unique constraint on `analyses`. Add `pg_advisory_xact_lock` as the first statement in `run()`, mirroring `ReviewService.run`'s cron/in-process overlap guard.<br>198.2 **CR189-04.** `_experiment_candidates` calls `ExperimentEvaluationService.run(commit=False)` purely to read a recommendation, and that call inserts an audit row that the `already_delivered` exit never commits — those rows land in a later profile's transaction or are discarded. Switch to the existing pure `ExperimentEvaluationService.evaluate` read-only path.<br>198.3 **CR189-07.** An unbounded Python-filtered scan of every historical evaluation packet, once per active experiment, every run. Push the `experimentId` predicate into SQL with `.limit(1)`.<br>198.4 **CR189-16 / CC188-17 / CC188-19 fold in.** Runbook note that the current omission is protective, not a gap; delete the 48-line unreachable `already_delivered` branch; record in DECISIONS that a dropped transition is dropped permanently. | Make the unprompted-speech writer safe under a double-fire or overlapping run, matching the lock discipline `reviews.py` already has. Fold into Batch 197 if touching `state_change_coach.py` once is preferred — the reason to keep separate is that 197 changes behaviour and this does not. | **Shipped 2026-08-15 via PR #231 / squash `3990ce9` (Decision #280):** `StateChangeCoachService.run()` now takes a transaction-scoped `pg_advisory_xact_lock` keyed by user/date as its first DB statement, so overlapping scheduler/external fires serialize before budget/candidate reads. Experiment candidates use the pure `ExperimentEvaluationService.evaluate()` path plus `evaluation_packet()`, so the state-change read no longer flushes uncommitted evaluation audit rows. Previous experiment comparison now filters in SQL with `context_packet ->> 'experimentId'` and `limit(1)`. The unreachable `already_delivered` branch is deleted, and dropped/budget-suppressed transitions are documented as permanently dropped rather than queued for replay. A real two-session PostgreSQL regression pauses one writer after budget approval and proves the final state is one analysis and one message. Local backend gates passed (795 / 347 expected PostgreSQL skips; Ruff/format/mypy clean); branch, PR-context and post-merge `main` CI passed all seven jobs; production freshness and exact-SHA Railway/Vercel health passed; web, protected-route and deployed-code smokes passed. No migration or product-surface change. |
| ~~Batch 199 — Floors that can actually fail~~ | 🔴 High | Shipped | 199.1 **CC188-04.** `missing_floors("On a Red day, VO2 intervals are absolutely fine.", ("never_vo2_on_red",))` returns `()`, verified by execution — the pattern matches topic adjacency, not the prohibition. Require the prohibition inside the same clause as the subject, and add a negative control per floor (an inverted sentence must be reported missing).<br>199.2 **CC188-05.** The audit is closed-world, asserting a hand-written dict equals itself. Discover prompt surfaces by enumerating modules that call `generate_anthropic_text` rather than listing them, with a commented opt-out list.<br>199.3 **CC188-07.** `local_clock_times` is registered and stated verbatim to Mark but audited against zero prompts. Widen the pattern and add it to the three entries that own it.<br>199.4 **CC188-06.** The app-state block asserts "this block is the current truth", contradicting the Batch 181 floor in the same prompt. Restate the two `chat_context` strings in record vocabulary.<br>199.5 **CI191-04.** None of the five deterministic protections (`trainingLoadCap`, `sleepCreditCeiling`, `cumulativeEscalation`, `readinessBaselineTrend`, `chronicAction`) is in `FLOORS` — deleting them would not fail CI. Promote all five, with the narrow rule: explain a deterministic ceiling, never argue it down.<br>199.6 **CC188-11.** `verdictImpact: "none"` is a literal the test asserts is present, not a property anything verifies. Replace with a shared helper proving no plan/verdict row was written.<br>199.7 **CC188-14 / CC188-18 fold in.** Version and stamp `conversation_learning` (the only unversioned LLM prompt writing into persistent memory); document in `coach_policy.py`'s docstring why the deterministic template surfaces are out of scope. | Make the mechanism that stops the most safety-critical rule in the system from silently regressing actually work — it currently passes an exactly inverted rule. | Every promoted/new floor ships with a negative control; prompt versions bumped where wording changes. No migration. **Shipped 2026-08-15 via PR #232 / squash `88a8441` (Decision #281):** the registry now carries ten canonical statements, semantic recognizers and inverted negative controls; caller discovery covers eight audited Anthropic surfaces plus the two documented `brief_chat` / `conversation_learning` exemptions; local-clock ownership reaches morning and all four post-session reads; all five deterministic morning protections are shared explain-but-never-argue-down floors; app-state copy uses recorded-evidence vocabulary; stored plan/verdict snapshots replace trust in `"none"` labels; and conversation learning is versioned and provenance-stamped. No migration or verdict/plan behavior change. Local gates passed (798 / 347 expected PostgreSQL skips; Ruff/format/mypy clean). Branch-push, PR-context and post-merge `main` CI passed all seven jobs; Vercel preview and production freshness passed; Railway and Vercel served exact SHA `88a84410467292a27ad3e10bf7332a4835b49692`; web, protected-route and live-container floor/version smokes passed. |
| ~~Batch 200 — Contrast and touch where they render~~ | 🟢 Mid | Shipped | 200.1 **UX192-05.** 75 light-mode text nodes fail AA across 13 of 15 routes — the active bottom-tab label at 3.55:1, Home workout-type chips, the sleep table's own health warning — because Batch 163's tokens (`--primary-text`, etc.) are defined but not used at these sites. Replace the decorative-fill tokens used as text at the measured sites; fix `--text-muted` on `--surface-elevated` (4.35:1) or stop pairing them.<br>200.2 **UX192-08.** Seven controls sit under the app's own 44px floor, including the 16px feel-score slider (Mark's one daily interaction) and the 16px back link. Extend the floor to the slider, inline nav links, switches, and form inputs — Batch 163 raised buttons only. | Close `UX156-03`, re-opened past the Batch 163 batch that was meant to close it, with an assertion that touches the rendered page rather than a token in a CSS file. | A rendered-DOM contrast assertion over a representative page set in both themes; a rendered hit-area assertion across the primary daily path — per-property, not per-component. No migration. **Shipped 2026-08-15 via PR #233 / squash `2608646`:** rendered decorative text colors were replaced with semantic text tokens across the affected UI, light `--text-muted` now clears elevated surfaces, the sleep-calendar selected foreground uses the defined `text-on-primary`, and daily-path controls clear the 44 px hit-area floor through shared primitives plus the check-in range and page back affordance. New Playwright coverage walks visible text nodes and computed backgrounds on `/`, `/brief`, and `/sleep` in both themes, and checks rendered control boxes on `/check-in`, `/brief`, and `/settings` at 390 px; Vitest excludes `apps/web/e2e` so unit tests stay jsdom-only. Local web gates, branch-push CI, PR-context CI and post-merge `main` CI passed; Vercel preview and production freshness passed; Railway/Vercel served exact SHA `2608646566caba90044ebc22b77f311a52a91252`; web, protected-route and live-asset smokes passed.** |
| ~~Batch 201 — Verdict-ladder residuals~~ | 🔴 High | Shipped | 201.1 **CI191-06.** Batch 170 stopped the age sleep credit carrying a night across the Green line; nothing tests the Red line, so a raw 53/POOR night can still earn the full +12 and resolve Green with clean corroboration. Apply the `crossedGreenThreshold` treatment to the Red line.<br>201.2 **CI191-07 load half.** Between "load present" and "load high" load can only relax — the cap only bites at ACWR ≥1.5. Make the `load_driven` escape symmetric with the cap (require load genuinely benign, e.g. ACWR ≤1.3, not merely present).<br>201.3 **CI191-05.** A 115% VO2 interval eases to 98% FTP for 75% of the duration on an Amber day — still a quality session, but the generic wording says "remove HIT/VO2 work". Consider whether a load-cap Amber should ease differently from a recovery Amber, and align the wording with what the transform actually produces. | Close the three residuals Batches 167/170 left in the same verdict ladder — the credit ceiling guards the Green line but not the Red line, and load still relaxes below the cap. | The combined-matrix pattern Batch 170 used, proving every changed outcome is equally or more cautious. Only-harden rule applies throughout. No migration expected. **Shipped 2026-08-15 via PR #234 / squash `c6e5f21` (Decision #282):** raw sleep below 60 can rise only to Amber; Low readiness needs positive sleep/HRV/check-in evidence, observed ACWR ≤1.30 and no >24h recovery conflict to be load-driven; and the shared Amber transform caps harder work at 94% FTP while preserving its 75% duration and Zone-2 hold. The combined only-harden matrix covers changed and preserved controls across verdict, delivery, editor, packet/action and prompt paths. One stale PostgreSQL-only expected string was corrected with Craig's approval after the first branch run. Final branch-push, PR-context and post-merge `main` CI passed all seven jobs; Vercel preview and production freshness passed; Railway/Vercel served exact SHA `c6e5f21280d82078da112b594a6e7b6ec243217f`; web/protected-route and read-only live-container smokes passed. No migration or shared/frontend contract change.** |
| ~~Batch 202 — Chat context tells the truth about itself~~ | 🟢 Mid | Shipped | 202.1 **CC188-09.** All eight read packets store `prompt.system` verbatim (morning: 11,254 chars) and the generation call passes the same prompt separately, doubling tokens and re-injecting the morning read's entire instruction set into anchored chat labelled as Mark's information. Stop storing `prompt.system`; keep `prompt.version` (what currentness actually keys on) and a hash if forensics wants it.<br>202.2 **CC188-03.** `subjectDateWorkoutsClosedSinceRead` filters on current status alone, so a post-workout read reports the very ride it is about as a change since itself. Timestamp the closure comparison, or rename the field honestly if adding a `PlannedWorkout` status-change timestamp is deferred as a migration decision.<br>202.3 **CC188-12.** The propose affordance attaches on question keywords alone (including "harder"), never consulting the model's actual answer. Require both a keyword and a structured marker from the model before attaching it.<br>202.4 **CC188-13/15/16 fold in.** Anchor the activity delta on ingest time rather than ride time; name field truncations in `omittedForLength`; document the char budget as best-effort and log when the trim exits above it.<br>202.5 **CI191-08 folds in.** Extend the effective weight/VO2max resolution to the morning packet so a metrics question is answered from the app's own dated figures rather than numbers Mark supplies in chat. | Stop the chat context from mislabelling its own instructions as Mark's information, and from reporting stale or self-referential state as a change. | **Shipped via PR #235 / squash `214f9e0` (Decision #283):** generated read packets now persist `prompt.version` + `prompt.systemHash` rather than full system prompts, with chat-time sanitation for legacy `prompt.system`; `sinceThisRead` uses `activitiesIngestedSinceRead` keyed to ingestion time and keeps `startUtc`; the planned-workout closure field shipped as the honest `subjectDateClosedWorkoutsCurrent` rename rather than a migration; truncation/over-budget evidence is explicit; dated effective Garmin weight and VO2max are included in morning/chat context; proposal ids attach only when Mark intent, a live adjustable bike row, and the model's stripped marker all agree. Backend-only, no migration/shared-schema/frontend/provider/auth/hosting/verdict/plan mutation. Local backend gates, branch/PR/main CI, Vercel preview/freshness, and exact-SHA production health/web/protected-route smokes passed. |
| ~~Batch 203 — Correctness cleanup~~ | 🟢 Mid | Shipped | 203.1 **CR189-05.** The morning read matches "VO2 today" by workout type name; the safety layer matches by intensity (≥106% FTP) — a manually-overridden session can make the read stay silent about VO2 while `blocks_red_vo2` refuses the push, with nothing connecting the two for Mark. Compute `has_vo2` from the IR where one is buildable, falling back to the type name (the pattern `_verdict_adjustment_packet` already uses).<br>203.2 **CR189-08.** Batch 180's coverage guard reaches three call sites; five more consumers read the same columns raw, feeding partial rows into rolling baselines and weekly averages. Bring them up to the same guard.<br>203.3 **CR189-10.** Every idempotency test calls the service twice on one session, passing identically whether the advisory lock exists or not. Add a real two-session concurrency test per the pattern at `test_auth.py:131`.<br>203.4 **Low folds.** `safetyRulesApplied` lists a rule that was not applied; dedupe a prompt-version bump mid-Sunday on the week not the row; dedupe the failure turn on more than exact string equality; push dedupe is a read-then-write; `_activity_local_date` now exists in four files. | Ship the independent small backend correctness fixes CR189 found, as one batch or split into three. | Shipped via PR #236, squash `f7be1c3` (Decision #284): morning Red/VO2 detection now uses a buildable structured-workout IR first, falling back to the type name only when the IR cannot be built, and `red_never_vo2` lists in `safetyRulesApplied` only when a Red day actually has VO2-intensity work to block. Garmin local-day aggregate coverage guards now reach all consumers (morning baselines, yesterday whole-day cost, DB-history baselines, review averages, insight drivers). Notification `_send_once` takes a transaction-scoped advisory lock, proven by a real two-session concurrency test. Weekly-review coach turns dedupe by delivered week/date, failure turns dedupe by origin/date, and `_activity_local_date` is one shared helper. Backend-only, no migration. CI (the first real run against Postgres) caught a FK-ordering gap in three new tests and a mypy no-implicit-reexport violation from the date-helper consolidation, both fixed pre-merge; production verified exact-SHA Railway + Vercel health, web `/` 200, protected daily-loop 401. |
| ~~Batch 204 — Security hygiene~~ | 🟢 Mid | Shipped | 204.1 **DS190-07.** Egress has no budget control, and the 5.5GB org-wide cap was already blown on 2026-08-04. Capture the provider usage meter daily, alert at staged thresholds, document only the scoped dump command.<br>204.2 **CR189-11.** The RLS guard test reads each migration's `RLS_TABLES` tuple and never inspects the `upgrade()` body, so a migration declaring a table but omitting `ENABLE ROW LEVEL SECURITY` passes the suite. Add a `pg_class.relrowsecurity` assertion after `alembic upgrade head`.<br>204.3 **Lows.** Return the same 404 for absent and foreign anchors; add `BriefMessage.user_id` to the inline-history query; add the missing `Analysis.user_id` predicate to `_sources`. | Close the posture gaps DS190/CR189 found in RLS test coverage and egress monitoring — no live exploit, sequence after user-facing work. | Shipped via PR #237, squash `4478ad5` (Decision #285): Supabase has no public egress-usage API, so `services/egress_budget.py` + `EgressBudgetMiddleware` build a documented leading-indicator proxy — response bytes plus the daily backup size, staged-alerted every 15 min via a `job_runs`-backed `egress-budget` job against the known 5.5GB cap — and the runbook now documents the scoped `pg_dump` command for manual use. CR189-11's guard test is now a pure check that runs each RLS migration's real `upgrade()` against a recording mock `op` and asserts the ENABLE statement is actually emitted per table (a first live `pg_class.relrowsecurity` attempt failed correctly-by-design, since CI's plain Postgres has no `auth` schema and every RLS migration no-ops there on purpose). `brief_chat` anchors 404 consistently for absent/foreign reads, `history()` filters `BriefMessage.user_id`, and `conversation_learning._sources` carries the same `Analysis.user_id` predicate its sibling join already had. Backend-only, no migration. Production verified exact-SHA Railway + Vercel health, web `/` 200, protected daily-loop 401. |
| ~~Batch 205 — The day's record means one thing~~ | 🔴 High | Shipped | 205.1 **CI191-02.** `daily_metrics` is one mutable row per day, overwritten by the evening sync (`recorded_at_utc` 19:00–21:30 on 12/14 days) while the verdict is computed at wake — the packet's readiness exceeds the surviving row on 11/18 mornings and its recovery clock is lower on 12/18; every retrospective consumer reads the evening value.<br>205.2 **Craig's shape decision, best made after Batch 194 lands:** an as-of column preserving the morning observation, or repointing retrospective consumers at the stored morning packet (which already holds the figures).<br>205.3 Reconcile with Batches 194 and 201: the Red qualification and the readiness baseline should at minimum agree with each other about which time of day they mean. | Stop `expected_training_debt`, the personal baselines, and any stored verdict from silently reading the evening-overwritten row instead of the wake-time observation Mark's verdict was actually computed from. | Decision-gated — not buildable until Craig picks the shape. Requires a migration either way. **Shipped via PR #238, squash `08e6859` (Decision #286).** Shape decided at `/batch-start`: phase-keyed rows, not a JSONB snapshot column and not reading the stored packet. Migration `028` keys `daily_metrics` on `(user_id, calendar_date, phase)` with a `phase IN ('morning','settled')` check; the wake sync writes `morning`, the closed-day `D-1..D-3` re-sync and both history importers write `settled`. **The finding's mechanism was corrected during the build:** `recorded_at_utc` is Garmin's own timestamp on the training-readiness reading, not our sync clock, and there is no evening sync job — the overwrite came from the *next* morning's run re-fetching each closed day and receiving its final reading, so the write pattern was already split by intent and only the storage key was not. `sync_daily` takes `phase` as a required keyword and rejects an unknown value while the model defaults to `settled`, so an indifferent fixture keeps the historically correct meaning but the one deliberate writer cannot silently regress; the migration adds the column with a default, backfills, then drops the default. Historical `morning` rows are reconstructed from the earliest stored morning `context_packet -> 'dailyMetrics'` per date — already a column-for-column mirror of the wake row — with two documented limits: `raw_payload` was never in the packet so a reconstructed row carries the same-date settled blob, and a date with no stored morning read gets no `morning` row rather than an invented one. All nineteen read sites now declare a phase: `morning` for the engine and every retrospective narrative (baselines, the 84-day readiness history, the Red cluster and its expected-training-debt exclusion, trends, reviews, insight early-warning, block progression, the post-session and Home reads, the weekly-restructure recovery signal, weight/VO2max tie-breaks); `settled` in exactly two places — yesterday's whole-day cost and the Garmin freshness check. **A field-level exception was necessary:** stress and Body Battery are running local-day totals that `daily_metric_coverage` already gates to `None` on a partial wake window, so `index_day_aggregates_by_date` sources those three from the settled row — otherwise they would have been blanked on every historical day and the `body_battery_charge` baseline killed. That makes Batch 180's in-place repair of a partial aggregate and this batch's preservation of the wake reading complementary, and 180's regression now asserts both halves of one seeded date. 205.3 is met by new leaf `services/delivered_verdict.py`, which pins each date's counted verdict to the last morning read generated at or before the local close of the wake-detection window (a post-check-in regeneration counts, an evening one does not; a date generated entirely late keeps its earliest read) and is shared by the Red cluster, the reviews and the block-progression trend, replacing three separate latest-wins collapses — it selects among stored rows only and never recomputes a verdict. Backend + migration only; no prompt, threshold, verdict logic, plan/proposal, delivery rail, endpoint, shared schema, frontend, provider, auth or hosting change. Local gates 840 passed / 362 expected PostgreSQL skips, Ruff check+format clean, mypy clean across 140 source files; branch, PR-context and post-merge CI all passed seven jobs with **1202 passed, 0 skipped** — every locally-skipped Postgres test really ran, including the twelve new ones, plus the real `upgrade head`/`downgrade base` round trip over `028`. Production verified on the exact merge SHA: Railway + Vercel health both `08e6859be8086a633b03893e8fe5634af7c011dc`, readiness `db: ok`, web `/` 200, protected `/api/v1/daily-loop` 401 direct and proxied. Batch smoke against the live database: `alembic_version` `028`, 475 rows (419 settled + 56 morning, 419 distinct days), old unique constraint gone, new unique + check constraints present, `phase` server default `NULL`, zero morning rows missing `recorded_at_utc` or carrying an empty `raw_payload` — and **2026-07-30, the audit's own headline case, now holds both observations**: `morning` MODERATE/64 with a 943-minute clock recorded 07:15, `settled` POOR/19 with 3233 minutes recorded 20:36. |

**One gotcha carried from the roadmap into every slot above.** Three separate wave-2 findings (UX192-01, UX192-05, CR189-11) shipped past green tests because the assertion never touched the thing it claimed to protect — a class string instead of a computed style, a token in a CSS file instead of a rendered node, a declared tuple instead of the SQL that ran. Where a slot's acceptance criterion is a property, assert the property, not its declaration.


## Post-roadmap — 2026-08-16 — The five deferred wave-2 items, specced (Batches 206–210)

With Batch 205 shipped the wave-2 roadmap's thirteen scheduled slots are complete and the
ledger had no unshipped rows. What remained were the five items the approved triage in
[`docs/reviews/BATCH_188-192_REMEDIATION_ROADMAP.md`](reviews/BATCH_188-192_REMEDIATION_ROADMAP.md)
put under *Deferred / accept-and-close*. Craig asked (2026-08-16) for all five to be
specced. **Specced is not scheduled:** the triage deferred three of them on value grounds
at a 1–2-user scale and that call still stands, so each row below carries the revisit
trigger the roadmap gave it. Authoring them now means that when a trigger fires the work is
ready rather than needing a fresh dig through three review documents.

**Two rows changed materially between the review and this authoring, because deployed state
was verified rather than transcribed** (the Batch 190 lesson):

* **Batch 206 is no longer expensive.** UX192-09 was graded Low and deferred on the
  assumption that slimming `/api/v1/daily-loop` meant splitting the endpoint. It does not:
  `DailyMetricOut.rawPayload` and `SleepOut.rawPayload` ship the whole Garmin blob to the
  browser on every call — measured in production at ~14 kB and ~22 kB stored per row,
  before JSON expansion — and **`rawPayload` has zero non-test readers anywhere in
  `apps/web/src`**. It is parsed by the shared zod schema and then never used. That makes
  the first phase a deletion, not an architecture change.
* **Batch 209's numbers moved.** DS190-05 reported 28/28 coach tables under RLS; live it is
  now **29/29** (`job_runs` arrived with Batch 195 / migration `027`). `relforcerowsecurity`
  is still **0 of 29** and every table is still owned by `postgres`, the Railway connection
  role, so the finding itself is unchanged — only its denominator.

DECISIONS numbers are **not** pre-assigned; each is allocated at its own `/batch-start`, per
the collision lesson recorded in `DECISIONS.md`. Tier reflects the suggested coding-agent
model, not finding severity.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 206 — The daily loop stops shipping what nothing reads~~ | 🟢 Mid | Shipped | 206.1 **UX192-09, first phase — delete the dead weight.** `DailyMetricOut.rawPayload` (`routers/daily_loop.py:965`) and `SleepOut.rawPayload` (`:997`) serialise the entire stored Garmin blob into every `/api/v1/daily-loop` response. Verified 2026-08-16: production rows average ~14 kB (`daily_metrics`) and ~22 kB (`sleep`) stored, and `grep -rn rawPayload apps/web/src` returns **zero** non-test matches — the field is validated by `packages/shared/src/schemas.ts` and never read. Drop it from both response models and from the shared schema, keeping the server-side readers (`routers/bedroom.py:248` `extract_hypnogram`, `morning_analysis` `_training_and_activity_fields`/`_extract_fitness_age`, `daily_metric_coverage`) which read the column directly and are unaffected.<br>206.2 **Re-measure before going further.** The review measured 5.4 s / 275,530 bytes against production. Re-measure after 206.1 and record the new figure in the batch row. **Decide at `/batch-start` on the strength of that number:** stop here, or continue into a hero-first split of the endpoint.<br>206.3 **Only if 206.2 says continue** — split the first-paint payload (verdict, today's actions, plan) from the long tail (the four post-analysis lists, briefs, projections) so Home and `/brief` paint before the rest arrives. Batch 62's `localStorage` persistence stays.<br>206.4 Tests: shared-schema round trip without `rawPayload`; a daily-loop response-shape assertion that fails if either blob is reintroduced; existing bedroom/hypnogram and morning-packet coverage stays green. | The morning path stops paying, on every cold load, for two blobs the browser never opens — and any further splitting is decided against a measured number rather than an assumption. | The morning path stops paying, on every cold load, for two blobs the browser never opens. **206.2 MEASURED AND DECIDED — stop at 206.1; 206.3 is not needed.** Measured against production on 2026-08-16: the two blobs serialise to **264,729 bytes on average** over the last 14 days (metric ~59 kB, sleep ~206 kB; peak 281 kB combined) against the review's measured 275,530-byte payload — **96.1% of it**. The earlier `pg_column_size` figures (~14 kB / ~22 kB) are TOAST-*compressed* storage; JSON text is what goes on the wire, and **there is no compression middleware anywhere in the API** (no `GZipMiddleware`, nothing in `vercel.json`), so the response really was served uncompressed. Removing the two fields takes `/api/v1/daily-loop` from ~275 kB to roughly **11 kB**, which resolves UX192-09 without touching the endpoint's shape — a hero-first split would now be optimising an 11 kB payload. Shipped as backend + shared-schema only: `DailyMetricOut.rawPayload` and `SleepOut.rawPayload` deleted from `routers/daily_loop.py` and from `dailyMetricSchema`/`sleepSchema` in `packages/shared/src/schemas.ts`. The columns are untouched and every server-side reader still works (`routers/bedroom.py` `extract_hypnogram`, `morning_analysis._training_and_activity_fields` / `_extract_fitness_age`, `daily_metric_coverage`) — they read the row directly. **Rollout is safe in both directions:** zod strips unknown keys, so a new client against an old server ignores the field, and the old client's schema had `.default({})`, so a cached bundle against the new server does not fail. Guards added because the regression is silent: two backend assertions that the field is absent from both response models (paired with a positive assertion that the neighbouring readings Home and the sleep page do render are still present), and a shared-schema test proving the parsed object has no `rawPayload` key even when the input contains one — zod's silent stripping means a regressed server would otherwise raise no error anywhere. **Noted, not taken:** `SleepOut.factorsJson` is also unread by the web but is ~1.1 kB and plausibly displayable later; and the absence of response compression is a real cross-cutting win left for its own batch. Gates: backend 842 passed / 362 expected PostgreSQL skips, Ruff check+format clean, mypy clean across 140 source files; `pnpm -r test` green (shared 27, web 386), shared `tsc` clean, web lint 0 errors, production build clean under Node 20. **Shipped via PR #239 / squash `c5231ca`:** branch, PR-context and post-merge `main` CI all passed seven jobs (`1204 passed, 0 skipped`); the Vercel preview built the tree. Production verified on the exact merge SHA `c5231cac46ee67bfe3cdf68f4dbc45df2c3ce338` — Railway direct and Vercel same-origin health both served it, readiness `db: ok`, web `/` 200, protected `/api/v1/daily-loop` 401 direct and proxied. **The batch smoke is a read-only introspection of the running container** (`railway ssh --service api`), because the endpoint is authenticated and production exposes no OpenAPI schema (`/openapi.json` and `/docs` both 404): the deployed `DailyMetricOut` and `SleepOut` report `rawPayload` **absent from both** (22 and 24 fields), `readinessScore`/`recoveryTimeMin`/`vo2max` all still present, and the deliberately-kept `factorsJson` intact. A deployed-bundle grep was tried first and rejected as an unsound smoke: the shared schema lives in a lazily-imported route chunk not linked from `index.html`, so the check found nothing even against the *pre-deploy* build and could never have distinguished before from after. |
| ~~Batch 207 — One coach everywhere~~ | 🟢 Mid | Shipped | 207.1 **UX192-07 — resolve the split.** `/brief` shows an inline read chat headed **"Ask about this read"** (`BriefFollowUpChat.tsx:54`) beside a launcher headed **"Ask about this morning's brief"** (`lib/coachOrigin.ts:33`) — two names for one coach on one page, still live as of 2026-08-16. Membership is **one-way**: the launcher sets `analysisId` only when replying to a weekly review (`CoachLauncher.tsx:93-101`) and `BriefChatService.ask` resolves an anchor only from an explicit `analysis_id`, never inferring one from origin (`services/brief_chat.py:381-397`), so a launcher question asked while standing on `/brief` is stored with `analysis_id = NULL` and can never appear in that brief's inline chat — though inline questions appear in both. The same split exists on Home (`DashboardPage.tsx:1866`) and in the Week workout sheet (`WorkoutDetailSheet.tsx:239`): three inline views against one app-wide view.<br>207.2 **DECIDED (Craig, 2026-08-16): shape (c), one coach everywhere.** Retire the three inline per-page chats entirely; the floating launcher becomes the only way to talk to the coach, anywhere in the app. Chosen because it is the simplest model to explain to a daily 57-year-old user — there is one conversation and it is always the same one — and because it deletes code rather than adding rules. **The known cost to design around:** the "ask about *this* read/ride" context disappears, so the build must decide how the coach knows what Mark is referring to (Batch 178's ask-time `appState` already assembles today's plan, the week, recent sessions and the latest reads, so the context largely exists; what is missing is which *screen* he was on). The options rejected, recorded so they are not re-litigated: **(a) anchor by route** — same button behaving differently per screen; **(b) explain the relationship** — signposts the wrong-box trap instead of removing it. (a) **Anchor by route** — a launcher question asked on a route showing a read attaches to that read; membership becomes symmetric, at the cost of the same button behaving differently depending on where Mark stands. (b) **Explain the relationship** — leave membership alone, have the inline view state that it shows only this read's turns and link to the full thread; honest and least magic, but Mark must understand a distinction he never asked for. (c) **One coach everywhere** — retire the three inline views entirely; simplest mental model and deletes code, but loses the "ask about *this* thing" context. Mark's own input may be worth having before choosing.<br>207.3 Whichever shape wins, align the headings so one page never names the same coach two ways, and make the two empty states consistent (`CoachConversation.tsx:298` says "Nothing here yet. Ask whatever's on your mind."; the inline view says nothing). Note Batch 193 fixed the *error* state (UX192-03); the empty-state divergence is separate and still live.<br>207.4 Tests at the chosen shape: anchor resolution for a launcher question asked on an anchored route; heading consistency; both empty states. | Mark can tell whether he is in one conversation or several, and a question asked in the "wrong" box does not vanish from the read it was obviously about. | Mark can tell whether he is in one conversation or several, and a question asked in the "wrong" box does not vanish from the read it was obviously about. **Shipped: shape (c).** The three inline views and `BriefFollowUpChat` are deleted; the floating launcher is the only coach surface, so the two-headings problem and the one-way membership both dissolve rather than being signposted. **The stated cost was avoided, not paid:** new `contexts/CoachAnchorContext` lets a screen showing a read register it and the launcher anchor that screen's questions to it, so asking "why?" on the brief still reaches the coach with that brief attached — one box, one thread, context intact. An explicit weekly-review reply still wins over the page anchor. **Registrations are a stack, not a single value, and that was a real defect its own test caught:** a workout sheet opening over an already-anchored page cleared the anchor on close while the page's effect never re-ran to restore it, leaving a visible read unanchored; reading the top of a stack means closing the sheet uncovers the page again. Home anchors only while its analysis is expanded. The `timeZone` prop chain that existed solely to timestamp inline chat turns was dead through four levels and was removed rather than left as plumbing to nowhere. `GET /api/v1/briefs/{analysisId}/messages` loses its only consumer but is deliberately kept — authenticated, tested, harmless, and removing an endpoint is a separate concern. Frontend-only; no backend, schema, migration, prompt, verdict, plan, auth or hosting change. Gates: `pnpm -r test` green (shared 26, web 386 across 61 files), web `tsc` clean, ESLint 0 errors / 8 warnings (`CoachAnchorContext` joins `AuthContext`/`ThemeContext` in the existing provider-plus-hooks warning), production build clean under Node 20; dev server boots with the provider in the tree with zero console errors. A logged-in `/brief` walkthrough was deliberately not run — it needs a minted production credential (the Batch 192 method) which is disproportionate for a deletion whose three affected screens each carry their own unit tests. Decision #288. **Shipped via PR #240 / squash `cb9d6a7`:** rebased onto the Batch 206 merge (three doc conflicts — `DECISIONS.md`, `STATUS.md` and the ledger — all resolved keep-both; the ledger block spanned *two* rows, so taking main's side silently reverted this batch's own row to `Planned` and it had to be re-struck, which is why a resolution is worth re-reading rather than trusting). Local gates re-ran green on the rebased tree. All nine PR checks passed, as did post-merge `main` CI. Production verified on the exact merge SHA `cb9d6a735c3d865eafc6967ac249617ef8e49559` — Railway direct and Vercel same-origin health both served it, readiness `db: ok`, web `/` 200, protected `/api/v1/coach/messages` POST and `/api/v1/daily-loop` both 401 proxied. **The batch smoke was baselined against production *before* the merge**, per the lesson Batch 206 recorded: the pre-merge build served a lazily-loaded chunk literally named `BriefFollowUpChat-DNv5HFUG.js` containing the string `Ask about this read`, proving the check could detect the old state. After deploy: the index chunk hash changed (`index-z2JTNt5Q` → `index-ByUTmOiC`, so Vercel really rebuilt), lazy chunks went 70 → 69, **no chunk is named `BriefFollowUpChat`**, and `Ask about this read` appears in **0 of 69 chunks** against a baseline of 1. |
| Batch 208 — The co-resident app stops being our problem | 🟢 Mid | Planned | 208.1 **DS190-06, the cheap half — route the public app's advisor warnings to its owner.** Verified live 2026-08-16: the project still holds **27 `public` tables** belonging to an unrelated app alongside the 29 `coach` tables, and its advisor WARNs (mutable search paths, a public extension, callable security-definer functions) land in the same Supabase advisor queue as `coach`'s. No live cross-schema object or grant reaches `coach` — the SQL boundary passes — so this phase is purely about whose queue the noise lands in. Configure notification routing (or document the split explicitly in the ops runbook if Supabase's free plan cannot route per-schema), so a `coach` WARN is never lost in another app's backlog.<br>208.2 **The durable half is a decision, not a task.** The residual risk is shared project owner, shared connection pool, shared egress quota (the 2026-08-04 cap blowout was a shared-quota event — see `supabase_free_plan_backup_constraints`), shared maintenance, and an owner-credential compromise reaching private health data regardless of object boundaries. The fix is moving `coach` to its own Supabase project, preferably its own organisation so billing is not shared. **Decide at `/batch-start`: do 208.1 only, or commit to the migration.**<br>208.3 **Only if 208.2 commits to the move** — rehearse export/import against a disposable target first, reusing Batch 196's `backup-drill` restore machinery and its same-host refusal guard; then cut over with credential rotation, `DATABASE_URL` session-mode port 5432 (never 6543), and exact-SHA health verification on both surfaces. Treat it as a rehearsed cutover with a rollback plan, not a config edit. | A `coach` security warning is never buried in an unrelated app's backlog, and the shared-project residual is either accepted deliberately or removed. | 208.1 ships standalone and is worth doing regardless. 208.3 only proceeds on an explicit decision, with a rehearsed restore and verified post-cutover health. **Trigger for 208.3:** another shared-quota incident, a second real user, or any move to paid infrastructure. |
| Batch 209 — RLS that constrains the application, not just the client | 🔴 High | Planned | 209.1 **DS190-05 — close the server-side bypass.** Verified live 2026-08-16: **29/29** coach tables have RLS enabled (28/28 at the review; `job_runs` arrived with migration `027`), **0 of 29** have `FORCE ROW LEVEL SECURITY`, and all 29 are owned by `postgres` — the role FastAPI connects as. Client roles hold no coach grants, so the *external* boundary genuinely passes; what RLS does not currently do is constrain the application itself, so a missing `user_id` predicate in any FastAPI query can cross the two private users while RLS reads as "enabled".<br>209.2 **Two shapes, decided at `/batch-start`.** (a) **Least-privilege application login** — a non-owner role with explicit grants, exercised against every write path including the scheduler, the backup job and the migration runner. (b) **`FORCE RLS` plus a deliberate server policy and request-scoped identity** — narrower blast radius but needs a session-variable identity plumbed through `AsyncSessionLocal` and every background job that has no request.<br>209.3 Whichever shape: a migration, a connection-role rehearsal against a disposable database, documented exceptions for the scheduler/backup/migration paths, and a rollback plan. **Do not flip `FORCE` on production without all four.** Reuse `scripts/check_rls_posture.py` + `RLS_POSTURE_DATABASE_URL` for deployed-state verification, and Batch 204's recording-mock migration test technique for the SQL-level assertion — a `RLS_TABLES` constant is not proof, and CI's plain `postgres:16` has no `auth` schema so the enable-guards no-op there by design.<br>209.4 Tests: every write path under the constrained role; each documented exception exercised; a negative control proving a query missing its `user_id` predicate now fails rather than crossing users. | RLS protects against an application authorization defect, not only against PostgREST/client-role exposure. | Every write path passes under the constrained role, each exception is documented and exercised, and a deliberately unfiltered query is proved to fail. Migration + rehearsal + rollback plan all present. **Trigger:** a genuine second user, or any move to a least-privilege application login. At one user this remains the same deferral wave 1 made for DS154-06's second half. |
| Batch 210 — No pooled connection idles across a model call | 🟢 Mid | Planned | 210.1 **CR189-12 — shorten the lock's reach.** `reviews.py:709` takes `pg_advisory_xact_lock` and the Anthropic call at `:725` runs inside the same transaction (confirmed still true 2026-08-16); `generation_requests.py:192` does the same through its `yield`. The connection, its transaction and the lock are all held for a 60-second-timeout external HTTP call — on the Supabase pooler in session mode, a pooled connection sitting idle-in-transaction.<br>210.2 Restructure so the lock covers the *claim* and the *write* but not the network call: take the lock, claim the work, commit; make the model call outside any transaction; reopen a transaction to record the result, re-checking the claim so a concurrent worker cannot double-write. Batch 198's `state-change` writer already demonstrates the claim-then-work shape and Batch 204/195's `generation_requests` lease is the existing durable-identity mechanism to build on — this is a restructure of existing pieces, not a new one.<br>210.3 Tests: a real two-session PostgreSQL regression proving the second worker observes the claim rather than duplicating the paid call, and that a crash between claim and record leaves a re-claimable state rather than a stuck lease. | A slow model call stops holding a pooled connection open, without weakening the guarantee that one review is generated once. | Two concurrent workers produce exactly one generation; no transaction spans the HTTP call; the crash-between-phases case is re-claimable. **Trigger:** connection-pool pressure on the Supabase pooler, or a second user. Recorded by the review as the correct trade at one user — this row exists so the fix is ready, not because it is due. |

**Why these five sat in Deferred.** The roadmap's disposition legend distinguishes *Do now*
from *Defer — real but low value at current 1–2-user scale; revisit on a trigger*. Nothing
below invalidates that reasoning; Batches 207 and 208.1 are the two that were never really
about scale (a product decision and a notification route), and Batch 206 turned out cheaper
than its grade assumed once deployed state was measured. 209 and 210 remain genuinely
trigger-gated and should not be scheduled ahead of user-facing work.

## Post-roadmap — 2026-08-17 — Coaching-integrity audit refresh (Batch 211)

Craig asked for an audit refresh once Batches 194 and 205 had shipped the two
findings Batch 191 said were blocking an A−. Diagnose-only, following the Batch
155/191 pattern: a full report in `docs/reviews/`, a Mark-safe scorecard beside
it, and a summary block appended to the running framework doc.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 211 — Coaching-integrity audit refresh~~ | 🔴 High | Shipped | 211.1 Re-run the framework's two lenses against the current engine, applying Batch 191's own kickoff rule — a remediation counts as closed only when observed working on **real data**.<br>211.2 Answer the live question: are CI191-01 (Batch 194) and CI191-02 (Batch 205) closed, and does the grade move off B+?<br>211.3 Prong A — drive the real functions with real recorded inputs; Prong B — read-only SQL over the `coach` schema.<br>211.4 Report + Mark-safe scorecard + framework summary block. | Establish, on evidence rather than on the presence of code, whether the two Highs are closed — and say plainly what the observation window can and cannot support. | **Grade moved to A− (from B+)** — the first refresh where it moved. **CI191-02 closed and *observed***: the single wake sync since Batch 205 shipped (2026-08-17) wrote today's `morning` row and re-synced closed days into `settled` rows without touching a wake row; 08-15 holds `69/MODERATE/1min` at wake against `26/LOW/2849min` settled, a 43-point readiness and 2,848-minute gap on a day the coaching called moderate. **CI191-01 closed and *proved***: driving the real `_qualify_red_morning` with the actual 2026-08-07 Red (`alcohol`, HRV 43 vs floor 45, RHR 47 vs ceiling 45) — which the old rule excused — now returns `acute_cause_with_systemic_strain` and counts; all four of Batch 194.2's bounds are independently demonstrable (physiology contradicts, endogenous tags never excuse, decay after a few days, cap of one). Not yet fired live because no Red has occurred since 2026-08-08, which predates the fix — **that reservation is the only thing between A− and A**. **CI191-08 closed**: all five deterministic protections are now in `coach_policy.FLOORS` (10 floors, each with a failing negative control). The spoken layer has started speaking (`state_change` 0 → 1, newest weekly review 2026-06-29 → 2026-08-16, user turns 41 → 92). Verdict distribution reversed 48% → 71% → 75% → **59% Green** (19/5/8 over 32 reads), the hardening working as intended. **One new finding, `CI211-01` (Low):** sixteen `workout_delivery_proposals` sit at `proposed` and every one is for a workout date already past (27 Jun → 13 Aug), none ever pushed, with no expiry path — hygiene rather than a live defect, since the daily loop looks proposals up by `planned_workout_id` and they cannot surface against a current session. **Observation window stated as a first-class caveat:** Batch 194 had two mornings and Batch 205 exactly **one**, against Batch 191's twelve, so every claim is labelled *observed*, *proved* or *implemented*. Diagnose-only — no product code, prompt, threshold, verdict, plan, proposal, migration, config or production row changed. Decision #289. **Shipped via PR #241 / squash `e54f60f`:** all nine PR checks and post-merge `main` CI passed. Production verified on the exact merge SHA `e54f60f51bfc5144b30960c41c618dfff7edc8f4` — Railway direct and Vercel same-origin health both served it, readiness `db: ok`, web `/` 200, protected `/api/v1/daily-loop` 401 proxied. **The diagnose-only boundary is evidenced rather than asserted:** `git diff --name-only origin/main...` returned **zero** non-`.md` files before the merge, so there is no functional surface for a behavioural smoke to test — the health/readiness/route checks confirm the deploy is intact and unchanged, which is the whole claim for a review batch. |


## Post-roadmap — 2026-08-19 — Mark feedback 2026-08-08/13/14/15 + the Claude-integration review (Batches 212–220)

Craig relayed six points of Mark's feedback (2026-08-08, 08-13, 08-14 ×1, 08-15 ×2 — sent
before Batches 206–211 shipped but reconciled here against current `main`), then asked
whether the way the app talks to the Claude API is optimal. Both threads are authored
together because the review changed the priority order of the feedback: **five of the six
points are ordinary defects in deterministic Python and cost nothing in Claude spend.** Only
the sixth (the room-temperature question) needs a capability the app does not have.

**Every claim below was verified against production, not transcribed from the reports.**
Three of the six root causes turned out to be different from what the in-app coach told Mark
when he challenged it:

* **The "illness context" is the word *cold*.** `_CHECK_IN_CAUSE_PATTERNS["illness"]`
  (`chronic_patterns.py:69`) contains `\b(?:head|chest\s+)?cold\b` — the optional group means
  the bare word matches. Driving the real dict against Mark's actual 08-14 note ("*more awake
  from this time as felt cold from drafts*") and his 08-15 note ("*backfired as too cold*")
  returns `['illness']` for **both**. He was describing his bedroom, and the classifier read
  it as sickness. This collides head-on with point 3: the more he reports on room
  temperature, the more the app thinks he is ill.
* **The VO2 "2-session target" is a stale row, not a hardcoded number.** `target =
  len(in_bucket)` (`weekly_mix.py:206`) counts rows, and the query filters only `is_active`
  (`:292`). Production for 2026-08-11 holds **three** `bike_vo2` rows for one session: v1
  `skipped`/`is_active=true`, v2 `planned`/`is_active=false`, v3 `completed`/`is_active=true`.
  The supersede path deactivated v2 but left the skipped v1 active, so `in_bucket` is 2 and
  `done` is 1 — exactly the string Mark saw. His own note records the origin: "*I accidentally
  skipped this day in app. I reinstated a vo2 workout.*"
* **The Red-day cut Mark experienced never went through `verdict_scaling`.** The 08-08 rows
  are v1 45 min (`plan_no2_import`), v2 **37 min**, v3 45 min completed — and **all three have
  no `origin` and no `adjustment`** in `structured_workout`, so `adjust_ir_for_verdict` did
  not author any of them. The v2 work block went 35 min → 1575 s (26.25 min) = **×0.75, the
  *Amber* scale**, applied via `source=interval_editor`, with the fixed 5+5 warm-up/cool-down
  diluting it to an 18% overall cut. So the brief's "50%" and the delivered ride's 37 minutes
  came from different code paths — the same fragmentation Batch 173.2 fixed for Amber, still
  live for Red.

The remaining two were confirmed as reported. The **sprint misread is observed in the stored
packet**, not inferred: every interval carries `boundarySource: planned_durations` (the
fallback — both alignment paths declined), a 12-second sprint holds **6 samples** (~1 sample /
2 s, against `TRACE_ALIGNMENT_BIN_SEC = 5`), there is no `maxPowerWatts` field anywhere in
`_build_interval` even though `maxHeartRateBpm` exists, `_adherence` has **no** duration or
sample guard while `_fade`/`_hr_drift_pct` correctly refuse below 180 s (`:570`), and
`prescribedErgMode` is **null** despite the key existing — which is why Mark had to write
"*ride was done in erg mode throughout*" in his own note. The cheapest tell is internal: the
recovery valleys recorded **63.5–68.6% FTP against the sprints' 54.1–54.6%**, so the work
intervals read *below* the rests they sit between. And **Body Battery drain** is a one-line
gap: `body_battery_drained` exists on `DailyMetric` (`coaching.py:88`) and is populated
**109/109** settled days since 1 May (mean **63.8**, range 31–89), but is absent from the
eight baseline keys in `metric_baselines.sample_values` (`:94-112`). Mark called his drain of
60 "normal" and he was right — the app simply never told him.

**What the Claude-integration review found, and why it demoted itself.** The boundary is a
hand-rolled `httpx.post` (`anthropic_text.py:150`) sending four keys — `model`, `max_tokens`,
`system`, `messages`. No `anthropic` SDK in the project; `cache_control` appears **zero**
times in the codebase; no tools, `thinking`, `output_config`, structured outputs or
streaming. Model is `claude-sonnet-4-6` at `max_tokens: 4096`. Measured in production, the
morning packet is **46 kB avg / 57 kB max ≈ 13,000 input tokens** (knowledge base ~24% of
it), and chat re-sends its whole prefix on every turn across an average **5.3 messages per
thread** (197 messages / 37 threads).

The finding that reordered everything: **Mark's entire sleep-and-recovery history — all 485
nights, every analytical column plus his own free-text notes — is 44 kB ≈ 12,700 tokens,
smaller than a single morning packet.** The app spends more tokens each morning describing
one day than it would cost to send every night Mark has ever recorded, and it is using ~1.3%
of Sonnet's 1M context window. The constraint was never context, cost or latency; the history
was simply never sent. That makes Batch 220 — no tools, no SDK change, no agent loop, just a
different packet at a different cadence — the best value in the whole review at **~$0.18 a
run via the Batch API**, against a current total spend of roughly $8–15/month.

**Prior art that must not be re-specced.** Batches 178/179 (2026-07-30) already answered the
earlier "*almost disconnected*" report: 178 shipped an ask-time `app_state`
(`brief_chat.py:530`) so the chat is no longer a frozen packet, and 207 later collapsed the
per-page chats into one coach. What remains from that complaint is narrower and is what
Batches 217–218 address: Claude still cannot say **how** a conclusion was reached, and it
re-pays for its own prefix every turn.

**Tools and the agent loop are deliberately not authored here** — see *Future enhancements*
below for the reasoning and the trigger. DECISIONS numbers are **not** pre-assigned; each is
allocated at its own `/batch-start`, per the collision lesson in `DECISIONS.md`. Tier
reflects the suggested coding-agent model, not severity.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| ~~Batch 212 — The check-in classifier stops diagnosing illness from the weather~~ | 🟢 Mid | Shipped | 212.1 **Mark's point (2026-08-14).** The morning brief carried a line about illness context being logged and pending review, with zero verdict impact. He never reported illness, it appeared nowhere else, and when he asked the coach directly it could not account for it. Root cause proved: `\b(?:head\|chest\s+)?cold\b` in `_CHECK_IN_CAUSE_PATTERNS["illness"]` (`chronic_patterns.py:69`) matches the bare word.<br>212.2 Narrow the pattern so a bedroom-temperature report is never an illness signal — require an illness-bearing qualifier (`head cold`, `chest cold`, `streaming cold`, `come down with`) and drop bare `cold`. Audit the other four cause families in the same dict for the same class of over-match (`\bill(?:ness)?\b`, `\bsick\b`, `travel`'s `\bholiday\b`) rather than fixing only the reported one.<br>212.3 **Stop the brief narrating a zero-impact internal signal at all.** The line was self-defeating by its own admission ("zero impact on today's verdict … pending human review"). Either suppress structural/pending-proposal plumbing from user-facing copy, or give it enough provenance to be explainable — Batch 217 supplies the mechanism; this phase decides which side of that line the tag sits on.<br>212.4 Tests: Mark's **verbatim** 08-14 and 08-15 notes must classify as no acute cause (both currently return `['illness']`); a set of genuine illness phrasings must still match; a negative control per cause family. | A note about a cold room never becomes a medical record, and any structural signal that does reach the brief can explain itself when challenged. | The two real notes classify clean, true-illness phrasings still match, and every cause family carries a negative control. The brief no longer emits a signal it cannot account for. **This is the highest-trust-per-line fix in the wave:** it is the one failure where the coach told Mark something false about his health and then could not say why. **Shipped via PR #242 / squash `9f72e4f`.** All sixteen PR checks and post-merge `main` CI green (**1224 passed, 0 skipped** against a 1204 baseline; locally 862 / 362 expected PostgreSQL skips). **Two findings during the build raised the severity beyond the spec:** the bogus tags *accumulate* (one row 08-14, a second 08-15, both live on 08-16 inside the 28-day window), and `illness` reaches `_qualify_red_morning`'s acute branch, so a Red with intact physiology could have been excused — bounded by Batch 194, but never merely cosmetic. Auditing all five families found a second over-match (`drinking`/`drank` tagged "drinking plenty of water"). `PROMPT_VERSION` v30 → `morning-analysis-v31-2026-08-19`, which is the regeneration cache key rather than a label. Production verified on the exact merge SHA `9f72e4fe9a02f228dceb805cc791946883bdf691` — Railway direct and Vercel same-origin health both served it, web `/` 200, protected daily-loop 401 proxied. **The batch smoke is a read-only introspection of the running container** (`railway ssh --service api`), which is the only honest test for a change with no unauthenticated surface: the *deployed* classifier returns `()` for both of Mark's verbatim notes, still returns `('illness',)` for "streaming head cold" and "caught a cold", returns `()` for "drinking plenty of water" and `('alcohol',)` for "drinking wine", reports `morning-analysis-v31-2026-08-19`, and confirms both the new gate and the surviving never-soften rule in the live prompt. Decision #290. |
| ~~Batch 213 — The weekly mix counts sessions, not rows~~ | 🟢 Mid | Shipped | 213.1 **Mark's point (2026-08-15):** "*Not sure where you're getting this from 'VO2 has 1 of a 2-session target done' — There is only ever 1 vo2 session in my weekly mix.*" He is right; the second row is a ghost.<br>213.2 **Fix the data-integrity half.** The 08-11 chain left v1 `skipped` with `is_active=true` while v2/v3 superseded it. Find the supersede path that deactivates a replaced `planned` row but not a replaced `skipped` one, and close it. Repair the live 08-11 row read-modify-write (`railway run`, the Batch 152 pattern) — **not** by hand-editing production without an audit trail.<br>213.3 **Fix the accounting half independently**, because correct data should not be the only thing standing between Mark and a wrong denominator. `summarize_weekly_mix` must count *sessions*, not rows: dedupe by (date, bucket) or by version chain, and decide explicitly whether a `skipped` row may ever contribute to `target` (it should not — a skipped session is not a plan commitment).<br>213.4 Check the same shape on 08-13 and 08-15, where superseded rows *are* correctly `is_active=false`, so Z2/Sweet-Spot targets are currently right by luck rather than by rule.<br>213.5 Tests: the real 08-11 three-row chain must yield `target=1, done=1`; a skip-then-reinstate lifecycle asserted end to end; a pure-reducer case with duplicate rows per bucket. | Mark is never shown a weekly target his plan does not contain, and never marked partially compliant for a session he completed. | The 08-11 fixture yields `target=1, done=1`; the supersede path leaves exactly one active row per session slot; the reducer is proved robust to duplicates even when the data is wrong. **Shipped via PR #243 / squash `1d3cac6`.** Both spec facts (the exact 08-11 row shape; 08-13/08-15 already correct) were re-verified against live production before building and held exactly as written. The supersede gap was `swap_day`'s target-date lookup via `_active_workout_on`, which deliberately excludes `skipped` rows and so never deactivated one left behind — fixed with a new unfiltered `_active_occupants_on` query so `swap_day` now deactivates every active occupant on both dates, not just the chosen swap partners. `summarize_weekly_mix` dedupes to one session per `workout_date` and a skipped-only date contributes nothing to `target`. All sixteen PR checks and post-merge `main` CI green (865 passed / 364 expected PostgreSQL skips). Production verified on the exact merge SHA `1d3cac63c4d60691559e11bb849384733c526af4` — Railway direct and Vercel same-origin health both served it, web `/` 200, protected daily-loop 401 proxied. **The batch smoke is a read-only introspection of the live database**: 2026-08-11 holds exactly one active `planned_workouts` row (down from three) and the repair is recorded in a queryable `coach.analyses` audit row. Decision #291. |
| Batch 214 — A 12-second sprint is graded on evidence that exists | 🔴 High | Planned | 214.1 **Mark's point (2026-08-13):** the app reported his 6 sprints at 54–65% FTP against a 185% target when ERG delivered the prescribed 515 W on all six. The in-app coach conceded both halves — the recording problem *and* that grading a 12 s sprint against a flat 185% standard is physically impossible, since a sprint has an acceleration ramp and the prescription means *peak at or near* target.<br>214.2 **Add the missing metric.** `_build_interval` (`ride_intervals.py:238-239`) computes `avgPowerWatts` → `pctFtp` → `adherence`, and there is no `maxPowerWatts` even though `maxHeartRateBpm` exists (`:256`). A 515 W peak is currently unrepresentable, so Mark's argument cannot be answered from the data model. Add peak power, and grade short neuromuscular efforts on peak/near-peak rather than window mean.<br>214.3 **Guard the grade on the evidence.** `_adherence` (`:557`) has no duration or sample-count floor, while `_fade` and `_hr_drift_pct` both correctly return `None` below `FADE_MIN_DURATION_SEC` (`:570`) — the guard concept already exists and grading simply does not use it. In the observed packet a 12 s window held **6 samples** (~1 per 2 s) and every interval fell back to `boundarySource: planned_durations`, with `TRACE_ALIGNMENT_BIN_SEC = 5` coarser than half a sprint.<br>214.4 **Add the internal consistency check** — cheap, general, and would have caught this alone: a work interval recording *below* its adjacent recovery is not a performance result, it is a segmentation failure. In the real packet the recoveries read 63.5–68.6% against sprints at 54.1–54.6%. Suppress the grade and say so.<br>214.5 **Populate `prescribedErgMode`.** The packet key exists and was `null` for this ride; in ERG the trainer holds the target, so under-delivery is near-impossible and an "under" grade should be suppressed on that basis alone. Mark had to supply this himself in a note.<br>214.6 Tests: the real 08-13 interval array must not grade the six sprints "under"; a peak-graded short sprint passes; the work-below-recovery control fires; a long steady interval still grades on mean as today. | The app never tells Mark he under-performed an effort it lacked the resolution to measure — and when it cannot measure, it says so. | The recorded 08-13 sprints stop grading "under"; peak power is present; adherence refuses below a sample/duration floor; the work-below-recovery guard fires on the real data. **This and Batch 213 are the same failure class:** the coach asserting a standard Mark was never set and then marking him down against it. |
| Batch 215 — Red protects recovery without deleting the sleep benefit | 🔴 High | Planned | 215.1 **Mark's point (2026-08-08), and he is arguing physiology, not preference:** "*I totally agree that on red days hard workouts should be cut but would rather keep original 45 minutes light Z2 … because I want to do all I can to ensure good sleep.*" Sustained low-intensity work builds sleep pressure without the cortisol/sympathetic arousal harder sessions produce, so cutting the easy session works against the Red rule's own purpose. The in-app coach agreed and could not counter it.<br>215.2 **Give Red the endurance-awareness Amber already has.** `RED_DURATION_SCALE = 0.5` (`verdict_scaling.py:38`) with `ease=None` (`:159`) is a blanket half-duration cut plus a 60% cap; Amber routes through `ease_amber_power_pct` with `ENDURANCE_CEILING_PCT = 75` so a Z2 ride keeps its intensity and only loses duration. Red has no equivalent. **The module docstring states the current behaviour is deliberate** ("Red keeps its own recovery-substitution shape … which deliberately *does* drop below Zone 2 — recovery is the point"), so this changes a settled decision: **append a new DECISIONS entry with the physiological reasoning, do not silently edit the old one.**<br>215.3 **Close the path fragmentation, which is the larger defect.** All three 08-08 rows have no `origin`/`adjustment`, so the canonical transform never ran; the delivered 37 minutes came from the interval editor applying the *Amber* ×0.75 scale on a Red morning, and the fixed 5+5 warm-up/cool-down diluted it to 18% while the brief said 50%. Route the Red duration cut through `adjust_ir_for_verdict` so the number Mark is told is the number he gets — the Batch 173.2 fix, applied to Red.<br>215.4 **Keep the safety rail untouched.** `blocks_red_vo2` and the intensity ceilings stay exactly as they are: intensity-aware *duration* is the change, not a relaxation of what may be pushed on a Red day.<br>215.5 **Gate on combined load, per the coach's own caveat to Mark:** with a bodyweight session alongside a 45-minute Z2, the total is what matters, not the per-session percentage.<br>215.6 Tests: an already-endurance ride on Red keeps its intensity and takes a materially lighter duration cut; a VO2 ride on Red is still substituted and still blocked; brief-quoted and delivered durations asserted equal; the 08-08 case as a regression fixture. | On a Red morning the hard work is still cut, the easy work that helps Mark sleep is not gutted, and the brief and the ride finally agree on one number. | An endurance ride on Red keeps intensity with a light duration change; VO2-on-Red is unchanged and still blocked; `summarize_verdict_adjustment` and the delivered row quote the same figure; a new DECISIONS entry records the reversal and why. |
| Batch 216 — Running totals reach the baseline table at all, then drain joins them | 🟢 Mid | Planned | **Rewritten 2026-08-19** after Mark's third feedback point that morning. It was authored as "add one key"; production shows the key it would have copied is **itself broken**, so the order is now fix-then-add. Ordering is load-bearing: adding drain first ships a *second* blank row.<br>216.1 **Mark's point, asked twice (2026-08-15, repeated 2026-08-19):** "*Worth the developer adding your average daily Body Battery drain to the baseline comparisons table*" — he flagged a drain of 60 as normal and had nothing to check it against.<br>216.2 **The regression to fix first (Mark's 2026-08-19 screenshot).** The Body Battery charge row renders with its baseline (**59–71**) but a blank "Last night" value — the row is present, the value is missing. Root cause is exact and is a **Batch 205 regression**: `_metrics_vs_baselines` (`morning_analysis.py:1387`) takes a single `daily_metric` — post-205 the *morning-phase* row — and calls `complete_body_battery_charged(daily_metric)` at `:1401`; that gate requires a **complete local-day window** (`daily_metric_coverage.py:114`), which a 09:10 wake row can never have, so it returns `None`. The data is present: the 08-19 morning row holds `body_battery_charged: 80`. **This is Batch 205's own documented field-level exception applied on one side only** — `metric_baselines.sample_values` correctly reads `day_aggregates` (the settled row), which is why the baseline computed while the value went blank. `_metrics_vs_baselines` has no `day_aggregates` parameter at all.<br>216.3 Give the current-value path the same settled-row source the baseline path already has, so a running local-day total is read from the phase that can actually complete it. Keep recovery readings (readiness, RHR, HRV) on the morning row — that split is the whole point of Batch 205 and must not be undone.<br>216.4 **Audit the siblings in the same pass:** `complete_body_battery_drained` and `complete_body_battery_end` already exist beside the charge gate (`daily_metric_coverage.py:121`, `:128`), and `stress_avg` is the third running total Batch 205 named. Anything reading one of these off the morning row has the same defect whether or not it is currently rendered.<br>216.5 **Only now add drain.** `body_battery_drain` joins `metric_baselines.sample_values` (`:94-112`), which holds eight keys including `body_battery_charge` but not its sibling. **No migration and no data backfill:** `body_battery_drained` exists (`coaching.py:88`) and is populated **109/109** settled days since 1 May (mean **63.8**, range 31–89). Mirror it in `_metrics_vs_baselines` in the same commit — the `sample_values` docstring requires baseline and live current to be drawn from the same columns or the comparison silently diverges.<br>216.6 Run the existing baseline backfill and add the row to the web comparisons table.<br>216.7 Tests: a **failing-first regression** that a morning-phase row still yields a charge value (the 08-19 case as a fixture); drain sourced from the settled row and not the wake row; recovery metrics asserted *still* on the morning row so the fix cannot over-correct; baseline/current parity; the web table renders both rows. | Mark can see whether today's drain is normal *for him* — and the charge figure beside it stops being a dash on a metric the app is discussing in prose two paragraphs earlier. | Charge renders a value on a live morning; drain appears in `metric_baselines` with a plausible mean near 63.8; recovery metrics still read the morning row; the 08-19 blank-value case is pinned as a regression. **Priority raised:** this is the only *live user-visible regression* in the wave, and the underlying split has been wrong since Batch 205 shipped on 2026-08-16. |
| Batch 217 — Facts arrive with their basis, so the coach can explain itself | 🔴 High | Planned | 217.1 **Why this exists.** When Mark challenged the illness line the coach could not explain it, because the *reason* — a regex matching one word — is nowhere in the packet. The packet asserts `pctFtp: 54.5` and `target: 2` as bare facts indistinguishable from ones the app is certain of. This is the cheap majority of what tool-use would buy for explainability, with no tools, no SDK change and no agent loop.<br>217.2 Establish a provenance/confidence convention for derived packet facts: value, `confidence`, and a short `basis` naming how it was reached — e.g. `{tag: "illness", matched: "cold", source: "regex over check-in note", confidence: "low"}`, or the sprint's `{pctFtp: 54.5, confidence: "low", basis: "6 samples over 12s, planned-clock boundaries"}`. With that present the coach can answer the question Mark actually asked: *your note said "felt cold from drafts" and my classifier read "cold" as illness — that is my error, not your health.*<br>217.3 Apply it first to the facts with a known failure history rather than boiling the ocean: check-in cause tags (212), weekly-mix targets (213), interval adherence and boundary source (214), verdict adjustment provenance (215).<br>217.4 Extend the prompt guidance so a low-confidence fact is hedged and its basis quoted, and so the coach never restates a derived number more confidently than the packet marks it.<br>217.5 Tests: a low-confidence fact is asserted to carry a basis string; a packet-shape assertion so a bare unannotated value in the covered set fails; existing packet coverage stays green. | The coach can say *how* it reached a conclusion, and hedges facts the app is not sure of — without needing to look anything up. | Every fact in the covered set carries basis and confidence; a regression test fails if one reverts to a bare value; the illness case is answerable from the packet alone. **Sequencing:** land after 212–215 so the annotations describe fixed logic rather than being written twice. |
| Batch 218 — Chat stops re-paying for its own prefix | 🟢 Mid | Planned | 218.1 **Measured, not assumed.** `cache_control` appears **zero** times in the codebase. `_build_system_prompt` (`brief_chat.py:518-530`) assembles `SYSTEM_PROMPT` → frozen packet → Batch 178's live `app_state`, and at 5.3 messages per thread (197 / 37 in production) a conversation re-processes ~13,000 identical tokens on every turn.<br>218.2 **The assembly order is already correct for caching** — stable content precedes volatile — which makes this small: a breakpoint after the frozen packet and before `app_state` caches the prefix while the live state stays fresh. Sonnet 4.6's 1024-token minimum is cleared comfortably.<br>218.3 **The one real code change:** `generate_anthropic_text` takes `system_prompt: str` and sends it as a bare string (`anthropic_text.py:154`, `:175`). Accept a list of system blocks so a `cache_control` marker can be placed. Keep the string path working for the seven other call sites.<br>218.4 **Audit for silent invalidators before claiming a win** — caching is a prefix match, so any per-request timestamp or non-deterministic JSON ordering ahead of the breakpoint kills it. Serialise the packet deterministically (`sort_keys`), and confirm `generatedAtUtc` is the stored value rather than a fresh clock read.<br>218.5 **Verify against `usage.cache_read_input_tokens`, not by inspection.** Zero reads across repeated turns means an invalidator is live and the batch has not shipped its goal. Log the field.<br>218.6 Tests: a system-block list round-trips with the marker; the string path is unchanged for existing callers; a determinism test proving two identical packets serialise byte-identically. | Chat turns 2..N stop re-processing a prefix that has not changed, cutting a thread from ~65K input tokens to ~21K. | `cache_read_input_tokens` is non-zero on the second turn of a real thread; the seven non-chat callers are untouched; packet serialisation is proved deterministic. **The reason to do this is latency, not cost** — at 1–2 users the saving is a few dollars a month; the win is that a follow-up question stops waiting on 13K tokens of re-processing. |
| Batch 219 — Log the levers before analysing them | 🟢 Mid | Planned | 219.1 **Why this must precede Batch 220.** Mark changed his **bedding on the same nights he changed the windows** (08-14 single sheet; 08-15 thin cover *plus* two windows at 10 cm), and said himself he could not tell temperature from draught: "*Not sure if due to temp or draught as wind was at angle cold air coming more directly into room.*" An analyst run over today's data would produce a confident answer from confounded evidence — the same mistake at higher resolution.<br>219.2 **Capture the levers Mark is already varying nightly**, currently trapped in free text: bedding weight (quilt / thin cover / sheet), window aperture and count, blind position, and pre-cool start time. He begins cooling around 18:30 by instinct, three hours before the fan window opens at `WINDOW_START = 21:30`.<br>219.3 **Widen the Open-Meteo request.** The stored payload holds daily aggregates only (`temperature_2m_max/min`, `wind_speed_10m_max`, `wind_gusts_10m_max`, precipitation, sunrise/sunset) — **no hourly series, no wind direction, no humidity**. All three are available keyless and free; wind direction is what separates "too cold" from "draught", and it is the confound Mark explicitly could not resolve. Persist per-night aggregates so they can enter the correlation as drivers.<br>219.4 **Add the continuous temperature drivers the engine lacks.** `DRIVER_KEYS` (`insights.py:93`) carries only threshold proxies — minutes above 19.5 °C, minutes above 20.0 °C, fan minutes, peak fan speed — so "too cold" is structurally invisible and no optimum can exist. Add mean/min/max overnight bedroom temperature. `overnight_low_c` is the *outdoor* low and is not a substitute.<br>219.5 **Wire the REM and awake-time outcomes.** `OUTCOME_SLEEP_SCORE` and `OUTCOME_RECOVERY_HRV` are the only outcomes; `overnight_awake_min` has a label in `_OUTCOME_SENTENCE_LABELS` but is never computed, and REM is absent entirely. Both columns exist (`coaching.py:116-117`). These are the two Mark named.<br>219.6 Tests: new fields round-trip; per-night weather aggregation; the new drivers and outcomes appear in the driver report. | Every lever Mark varies is recorded on the night it applied, so the analysis that follows can be falsified rather than merely believed. | Bedding/aperture/pre-cool are capturable and stored per night; wind direction and humidity are persisted; continuous bedroom temperature is a driver; REM and awake time are computed outcomes. **This batch adds no Claude spend and is the prerequisite for 220 — shipping 220 first wastes it.** |
| Batch 220 — The analyst run: 485 nights, and a real answer on temperature | 🔴 High | Planned | 220.1 **Wire the dormant billing alert first.** Batch 141 built the admin billing alert but `admin_alert_user_id` is still unset in production, so the push half does nothing — and on 2026-07-21 an empty credit balance froze the brief and took manual diagnosis to find. This batch is the one that raises spend; it should not be the one that discovers the alarm is off. Config, not code.<br>220.2 **Mark's point (2026-08-13):** "*Is this something you could get the app to monitor over the longer term including how both lower and higher temp affects my rem levels and awake time … with aim of confirming if there is actually an optimum temperature to target.*" He is explicit that his own sample is too small and wants the app to confirm or kill the theory.<br>220.3 **Why this needs Claude and not another deterministic engine.** The existing 120-day Pearson driver engine is linear and monotonic — it structurally *cannot* find an optimum, because an optimum is a U-shape. Nor can it notice that bedding changed on the same nights. Both are inference over messy multi-variate history, which is what a model is for.<br>220.4 **The economics are the reason this ranks so high.** Mark's whole analytical history — 485 nights, every column plus his verbatim notes — is **44 kB ≈ 12,700 tokens**, *smaller than one morning packet* (46 kB); with per-night temperature aggregates it is still ~20K, against a 1M context window currently ~1.3% used. **No tools, no SDK change, no agent loop** — a different packet at a different cadence.<br>220.5 **Send evidence, not conclusions.** The daily packet is the *output* of deterministic Python that already decided everything, which is precisely why Claude cannot catch an error in it. This packet carries raw per-night rows and Mark's own notes; the reasoning is the deliverable. Include temperature *banding* rather than a coefficient, so an optimum can surface.<br>220.6 **Run it on the Batch API at monthly or weekly cadence** — completely latency-insensitive, and batch is **50% off**: ~$0.18 a run, so ~£0.20–0.75/month against a current total of roughly $8–15.<br>220.7 **Return structured findings, not prose — this is the first durable-findings channel.** On 13 August the coach correctly diagnosed the sprint recording bug and said it would flag it; it had no mechanism, and the finding sat in a chat log until 18 August. Each finding carries an observation, a confidence, and where applicable a proposed experiment or data-quality flag, routed into the `experiments` table (`experiment_tracker.py` already has the full `active`/`paused`/`concluded` + `supported`/`refuted`/`inconclusive` lifecycle) or queued for Mark's review via the `conversation_learning` review rail.<br>220.8 **Handle the unreachable case, per Mark's own caveat:** "*on hot days impossible to get it to right temp*". A target he cannot hit on a warm night is not actionable advice.<br>220.9 Tests: a deterministic longitudinal-packet builder (pure, no clock); findings-schema validation; a banding fixture with a synthetic U-shape the reducer must not flatten; a confound-present fixture where the honest answer is "inconclusive". | For the first time the app tells Mark something about himself he did not already know and could not have computed — and either confirms or kills the temperature theory on 485 nights instead of two. | A findings run completes on the Batch API, produces at least one structured finding with a confidence, and routes it to `experiments` rather than to prose. The temperature answer is banded, states its confounds, and handles the unreachable-target case. **Depends on Batch 219** — without the levers logged this produces a confident answer from the same confounded data Mark already has. |

### 2026-08-19 — Mark's REM feedback adds Batch 221

Mark's most important point that morning: *"App continually flags low rem in sleep and says
worth tracking but doesn't actually track it. Would be really helpful if it could track
patterns / movement and any obvious causes so I can see what things to try and if they're
working."* His screenshot shows the brief flagging **REM 9.8% against a 15–23% age band** and
saying "worth tracking" — then, one line later, "*worth noting whether that pattern appeared
last night*", **asking Mark to observe something the app holds the hypnogram to compute.**

Reconciled against production, this is not one gap but a loop broken in three places, and
only the first was already planned:

* **REM is not an outcome.** Covered by Batch 219.5 and analysed by 220.
* **No observation is ever recorded.** All three standing experiments — collagen,
  recovery-week disruption, and the 04:00 waking pattern — hold
  `observations: {"entries": []}` and status `active`, unchanged since they seeded. "The
  04:00 waking hypothesis remains active" is literally true and always will be.
* **Evaluation results never reach the brief.** `experiment_evaluation.py` has real
  evaluators (a gate for collagen, a **Pearson correlation** for `early_waking_0400`, a
  group-compare) and has run **21 times since 1 August** — but `experiments` is **absent from
  the morning packet entirely**, so the coach cannot see its own findings. That is exactly
  why it outsources the observation to Mark.

And the sharpest one, which nothing covers: **`rem_interventions.py` (Batch 72) hands Mark a
rotating library of 12 REM levers every week and contains no measurement of any kind** — the
module selects and renders, nothing more. He has been told to try things, weekly, with no
feedback loop ever closing. Batch 221 closes it. **Sequencing:** 219 logs the levers → 221
makes the loop real → 220 reasons over it.

| Batch | Tier | Status | Phases | Goal | Acceptance criteria |
|---|---|---|---|---|---|
| Batch 221 — The experiment loop closes: what was tried, and did it work | 🔴 High | Planned | 221.1 **Record observations.** The tracker has a full lifecycle (`active`/`paused`/`concluded`, `supported`/`refuted`/`inconclusive`) and `observations_json`, and writes nothing into it. Give each standing hypothesis a deterministic per-night observation write so evidence accumulates instead of the experiment sitting `active` forever.<br>221.2 **Surface the evaluation.** `experiments` is not in the morning packet, so 21 completed evaluations are invisible to the read. Carry the current state and latest result, and let the sleep narrative use it. The brief must stop asking Mark to notice a pattern the app already evaluates — it either reports the finding or says plainly it does not have enough nights yet.<br>221.3 **Make the REM levers measurable — the half Mark actually asked for.** `select_rem_interventions` rotates 12 levers weekly with no record of which was issued, whether it was applied, or what followed. Record the issued lever per week, and pair it with the REM and awake-time outcomes Batch 219.5 wires, so "what to try and whether it's working" becomes answerable. Weekly rotation over a 12-lever library is a slow read at n=1: **state the confidence honestly rather than manufacturing a verdict**, and prefer "not enough nights yet" over a number.<br>221.4 **Close the promise loop in the copy.** "Worth tracking" must mean it *is* tracked; if it is not yet measurable, the read says what would make it measurable. No instruction to Mark to observe something derivable from the hypnogram.<br>221.5 Tests: an observation write per standing hypothesis; the packet carries experiment state and a fixture where the coach must report a finding rather than defer to Mark; a lever-issued record round-trips; an insufficient-evidence fixture must yield "inconclusive" and never a fabricated direction. | Mark can see which REM lever he was given, what happened after it, and whether it is working — and the app stops asking him to do the noticing. | Observations accumulate against all three standing hypotheses; evaluation state reaches the morning packet; issued levers are recorded and joinable to REM/awake outcomes; an underpowered comparison reports inconclusive rather than inventing a direction. **Depends on 219.5** for the REM and awake-time outcomes; **feeds 220**, whose findings route into the same `experiments` rail (220.7). |

### Future enhancements — recorded, not scheduled

Authored here so the reasoning survives, each with the trigger that would make it due. **None
of these is blocked; all are deliberately deferred on value-per-batch at a 1–2-user scale.**

* **Tool use / the agent loop (Claude can verify its own inputs).** The full fix for
  "disconnected": read-only tools — `get_interval_detail`, `get_planned_week`,
  `get_metric_baseline`, `explain_tag` — so that when Mark says "*not sure where you're
  getting this from*" the coach can go and look, and tell him the app is wrong. Requires
  adopting the Python SDK's tool runner in place of the single POST. **This is where the
  entire 2–4× cost increase lives** (an agent loop re-sends the whole conversation each
  iteration: a 13K packet with four tool calls is ~75K input where today it is 13K), plus the
  largest build cost — tool definitions, iteration caps, and a safety review of what Claude
  may read. **Why deferred, and why the case genuinely weakens:** tools are insurance against
  a class of bug that Batches 212–217 largely eliminate. After them the VO2 target is right,
  the sprints are not misgraded, and the illness tag does not fire — so the value of "Claude
  can go and check" drops sharply. **Trigger:** a *new* class of error that 212–217 did not
  catch, or Mark still reporting the app feels disconnected after 217 and 220 have been live
  for a few weeks. At that point the complaint will name exactly what to build.
* **Claude replaces regex classification of Mark's own words.** The deeper fix behind Batch
  212: `_CHECK_IN_CAUSE_PATTERNS` does natural-language classification with regex across five
  cause families, and no amount of pattern-tightening makes that safe — the next failure is
  just a different word. A model reads "*more awake as felt cold from drafts*" correctly.
  **Trigger:** a second over-match reaching Mark after 212, or Batch 220 proving out, since
  the analyst run is already reading his notes longitudinally and is the natural home for it.
* **Anthropic SDK adoption.** Would replace the hand-rolled `classify_anthropic_error` with
  typed exceptions, and unlock structured outputs (`output_config.format` — Batch 143 existed
  to fix a chat/post-workout JSON-500 that the current join-the-text-blocks approach makes
  possible by design) and streaming (Batch 144's "orphaned generating spinner" is the UX
  streaming removes; the client is non-streaming on a 60 s timeout). **Trigger:** the tools
  work, which needs it anyway — or a third JSON-parse defect.
* **Model review — `claude-sonnet-4-6` → Sonnet 5.** A dated decision (paid API chosen for
  health-data privacy; "keep Sonnet 4.6, revisit ~Sept 2026") now nearly due. Sonnet 5 is
  materially better on agentic work at the same tier. **Not a drop-in:** adaptive thinking is
  on by default, non-default sampling parameters are rejected, and the tokenizer produces
  ~30% more tokens for the same text, so `max_tokens` and cost baselines shift. **Trigger:**
  the September review, or the tools work — tool-triggering behaviour is where the
  generational gap is largest.
* **Room cooling beyond the fan.** Today the thermal model is one scalar and one actuator, and
  it is **one-directional**: `ON_THRESHOLD_C = 19.5` / `OFF_THRESHOLD_C = 19.0`
  (`fan_control.py:52`) can only cool, so both of Mark's recent failures — cold-side, 08-14
  and 08-15 — are events the autopilot cannot represent. Candidate levers: a computed
  pre-cool crossover time (he guesses 18:30), an earlier control window, cold-side thresholds
  and a draught alert, cross-ventilation guidance from wind direction, dawn planning from the
  hourly curve, and daytime solar-gain exclusion. **Shape matters:** the app cannot open a
  window, so this is an evening *cooling plan* nudge, not an extension of the autopilot.
  **Trigger:** Batch 220 identifying an optimum worth actively steering toward — advice
  without a target is guesswork.
* **Cooling Mark rather than the room.** Sleep onset is driven by core temperature *falling*,
  achieved by dumping heat through the skin — mainly hands and feet — via vasodilation. An
  over-cold room triggers vasoconstriction and *impairs* that, which is the physiological
  reason an optimum exists and the reason both his cold nights failed. The most app-native
  lever is **workout timing versus bedtime** (training elevates core temperature for hours;
  the app already owns every session time, and this sits directly on top of Batch 215's
  sleep-pressure argument — sleep pressure and residual thermal load pull in opposite
  directions late in the evening). Others: a warm shower ~1–2 h before bed, feet outside the
  covers as a heat dump that needs no cold room, and bedding/mattress microclimate. Every one
  is a natural fit for the existing `experiments` lifecycle. **Trigger:** Batch 219/220
  proving the logging and findings rails work — these should be *tested* on Mark, not
  asserted at him.
* **Widen the findings-to-state channel.** Batch 220.7 opens it for analyst findings only.
  The general form is that any Claude conclusion — from chat, from a post-workout read —
  can become an experiment, a data-quality flag, or a KB entry rather than prose that dies.
  Without this, the coach can be argued into agreement and change nothing, which is its own
  slow-burn disconnection: Mark learns that winning the argument does not matter. **Trigger:**
  a second instance of the coach conceding a real defect with no way to record it.
* **Response compression.** Noted by Batch 206 and still true: there is no `GZipMiddleware`
  anywhere in the API and nothing in `vercel.json`, so every response is served
  uncompressed. A cross-cutting win, unrelated to this wave. **Trigger:** its own batch.
